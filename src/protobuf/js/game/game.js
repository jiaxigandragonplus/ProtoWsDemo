/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
"use strict";

var $protobuf = require("protobufjs/minimal");

// Common aliases
var $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;

// Exported root namespace
var $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

$root.game = (function() {

    /**
     * Namespace game.
     * @exports game
     * @namespace
     */
    var game = {};

    game.CStartExplore = (function() {

        /**
         * Properties of a CStartExplore.
         * @memberof game
         * @interface ICStartExplore
         */

        /**
         * Constructs a new CStartExplore.
         * @memberof game
         * @classdesc Represents a CStartExplore.
         * @implements ICStartExplore
         * @constructor
         * @param {game.ICStartExplore=} [properties] Properties to set
         */
        function CStartExplore(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CStartExplore instance using the specified properties.
         * @function create
         * @memberof game.CStartExplore
         * @static
         * @param {game.ICStartExplore=} [properties] Properties to set
         * @returns {game.CStartExplore} CStartExplore instance
         */
        CStartExplore.create = function create(properties) {
            return new CStartExplore(properties);
        };

        /**
         * Encodes the specified CStartExplore message. Does not implicitly {@link game.CStartExplore.verify|verify} messages.
         * @function encode
         * @memberof game.CStartExplore
         * @static
         * @param {game.ICStartExplore} message CStartExplore message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CStartExplore.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CStartExplore message, length delimited. Does not implicitly {@link game.CStartExplore.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CStartExplore
         * @static
         * @param {game.ICStartExplore} message CStartExplore message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CStartExplore.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CStartExplore message from the specified reader or buffer.
         * @function decode
         * @memberof game.CStartExplore
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CStartExplore} CStartExplore
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CStartExplore.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CStartExplore();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CStartExplore message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CStartExplore
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CStartExplore} CStartExplore
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CStartExplore.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CStartExplore message.
         * @function verify
         * @memberof game.CStartExplore
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CStartExplore.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CStartExplore message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CStartExplore
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CStartExplore} CStartExplore
         */
        CStartExplore.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CStartExplore)
                return object;
            return new $root.game.CStartExplore();
        };

        /**
         * Creates a plain object from a CStartExplore message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CStartExplore
         * @static
         * @param {game.CStartExplore} message CStartExplore
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CStartExplore.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CStartExplore to JSON.
         * @function toJSON
         * @memberof game.CStartExplore
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CStartExplore.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CStartExplore
         * @function getTypeUrl
         * @memberof game.CStartExplore
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CStartExplore.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CStartExplore";
        };

        return CStartExplore;
    })();

    game.SStartExplore = (function() {

        /**
         * Properties of a SStartExplore.
         * @memberof game
         * @interface ISStartExplore
         * @property {number|Long|null} [startTime] SStartExplore startTime
         * @property {number|Long|null} [costTime] SStartExplore costTime
         * @property {Array.<common.IAdventureTeamInfo>|null} [teams] SStartExplore teams
         */

        /**
         * Constructs a new SStartExplore.
         * @memberof game
         * @classdesc Represents a SStartExplore.
         * @implements ISStartExplore
         * @constructor
         * @param {game.ISStartExplore=} [properties] Properties to set
         */
        function SStartExplore(properties) {
            this.teams = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SStartExplore startTime.
         * @member {number|Long} startTime
         * @memberof game.SStartExplore
         * @instance
         */
        SStartExplore.prototype.startTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SStartExplore costTime.
         * @member {number|Long} costTime
         * @memberof game.SStartExplore
         * @instance
         */
        SStartExplore.prototype.costTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SStartExplore teams.
         * @member {Array.<common.IAdventureTeamInfo>} teams
         * @memberof game.SStartExplore
         * @instance
         */
        SStartExplore.prototype.teams = $util.emptyArray;

        /**
         * Creates a new SStartExplore instance using the specified properties.
         * @function create
         * @memberof game.SStartExplore
         * @static
         * @param {game.ISStartExplore=} [properties] Properties to set
         * @returns {game.SStartExplore} SStartExplore instance
         */
        SStartExplore.create = function create(properties) {
            return new SStartExplore(properties);
        };

        /**
         * Encodes the specified SStartExplore message. Does not implicitly {@link game.SStartExplore.verify|verify} messages.
         * @function encode
         * @memberof game.SStartExplore
         * @static
         * @param {game.ISStartExplore} message SStartExplore message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SStartExplore.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.startTime);
            if (message.costTime != null && Object.hasOwnProperty.call(message, "costTime"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.costTime);
            if (message.teams != null && message.teams.length)
                for (var i = 0; i < message.teams.length; ++i)
                    $root.common.AdventureTeamInfo.encode(message.teams[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SStartExplore message, length delimited. Does not implicitly {@link game.SStartExplore.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SStartExplore
         * @static
         * @param {game.ISStartExplore} message SStartExplore message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SStartExplore.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SStartExplore message from the specified reader or buffer.
         * @function decode
         * @memberof game.SStartExplore
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SStartExplore} SStartExplore
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SStartExplore.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SStartExplore();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.startTime = reader.int64();
                        break;
                    }
                case 2: {
                        message.costTime = reader.int64();
                        break;
                    }
                case 3: {
                        if (!(message.teams && message.teams.length))
                            message.teams = [];
                        message.teams.push($root.common.AdventureTeamInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SStartExplore message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SStartExplore
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SStartExplore} SStartExplore
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SStartExplore.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SStartExplore message.
         * @function verify
         * @memberof game.SStartExplore
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SStartExplore.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.startTime != null && message.hasOwnProperty("startTime"))
                if (!$util.isInteger(message.startTime) && !(message.startTime && $util.isInteger(message.startTime.low) && $util.isInteger(message.startTime.high)))
                    return "startTime: integer|Long expected";
            if (message.costTime != null && message.hasOwnProperty("costTime"))
                if (!$util.isInteger(message.costTime) && !(message.costTime && $util.isInteger(message.costTime.low) && $util.isInteger(message.costTime.high)))
                    return "costTime: integer|Long expected";
            if (message.teams != null && message.hasOwnProperty("teams")) {
                if (!Array.isArray(message.teams))
                    return "teams: array expected";
                for (var i = 0; i < message.teams.length; ++i) {
                    var error = $root.common.AdventureTeamInfo.verify(message.teams[i]);
                    if (error)
                        return "teams." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SStartExplore message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SStartExplore
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SStartExplore} SStartExplore
         */
        SStartExplore.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SStartExplore)
                return object;
            var message = new $root.game.SStartExplore();
            if (object.startTime != null)
                if ($util.Long)
                    (message.startTime = $util.Long.fromValue(object.startTime)).unsigned = false;
                else if (typeof object.startTime === "string")
                    message.startTime = parseInt(object.startTime, 10);
                else if (typeof object.startTime === "number")
                    message.startTime = object.startTime;
                else if (typeof object.startTime === "object")
                    message.startTime = new $util.LongBits(object.startTime.low >>> 0, object.startTime.high >>> 0).toNumber();
            if (object.costTime != null)
                if ($util.Long)
                    (message.costTime = $util.Long.fromValue(object.costTime)).unsigned = false;
                else if (typeof object.costTime === "string")
                    message.costTime = parseInt(object.costTime, 10);
                else if (typeof object.costTime === "number")
                    message.costTime = object.costTime;
                else if (typeof object.costTime === "object")
                    message.costTime = new $util.LongBits(object.costTime.low >>> 0, object.costTime.high >>> 0).toNumber();
            if (object.teams) {
                if (!Array.isArray(object.teams))
                    throw TypeError(".game.SStartExplore.teams: array expected");
                message.teams = [];
                for (var i = 0; i < object.teams.length; ++i) {
                    if (typeof object.teams[i] !== "object")
                        throw TypeError(".game.SStartExplore.teams: object expected");
                    message.teams[i] = $root.common.AdventureTeamInfo.fromObject(object.teams[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SStartExplore message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SStartExplore
         * @static
         * @param {game.SStartExplore} message SStartExplore
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SStartExplore.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.teams = [];
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.startTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.startTime = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.costTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.costTime = options.longs === String ? "0" : 0;
            }
            if (message.startTime != null && message.hasOwnProperty("startTime"))
                if (typeof message.startTime === "number")
                    object.startTime = options.longs === String ? String(message.startTime) : message.startTime;
                else
                    object.startTime = options.longs === String ? $util.Long.prototype.toString.call(message.startTime) : options.longs === Number ? new $util.LongBits(message.startTime.low >>> 0, message.startTime.high >>> 0).toNumber() : message.startTime;
            if (message.costTime != null && message.hasOwnProperty("costTime"))
                if (typeof message.costTime === "number")
                    object.costTime = options.longs === String ? String(message.costTime) : message.costTime;
                else
                    object.costTime = options.longs === String ? $util.Long.prototype.toString.call(message.costTime) : options.longs === Number ? new $util.LongBits(message.costTime.low >>> 0, message.costTime.high >>> 0).toNumber() : message.costTime;
            if (message.teams && message.teams.length) {
                object.teams = [];
                for (var j = 0; j < message.teams.length; ++j)
                    object.teams[j] = $root.common.AdventureTeamInfo.toObject(message.teams[j], options);
            }
            return object;
        };

        /**
         * Converts this SStartExplore to JSON.
         * @function toJSON
         * @memberof game.SStartExplore
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SStartExplore.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SStartExplore
         * @function getTypeUrl
         * @memberof game.SStartExplore
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SStartExplore.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SStartExplore";
        };

        return SStartExplore;
    })();

    game.CStopExplore = (function() {

        /**
         * Properties of a CStopExplore.
         * @memberof game
         * @interface ICStopExplore
         */

        /**
         * Constructs a new CStopExplore.
         * @memberof game
         * @classdesc Represents a CStopExplore.
         * @implements ICStopExplore
         * @constructor
         * @param {game.ICStopExplore=} [properties] Properties to set
         */
        function CStopExplore(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CStopExplore instance using the specified properties.
         * @function create
         * @memberof game.CStopExplore
         * @static
         * @param {game.ICStopExplore=} [properties] Properties to set
         * @returns {game.CStopExplore} CStopExplore instance
         */
        CStopExplore.create = function create(properties) {
            return new CStopExplore(properties);
        };

        /**
         * Encodes the specified CStopExplore message. Does not implicitly {@link game.CStopExplore.verify|verify} messages.
         * @function encode
         * @memberof game.CStopExplore
         * @static
         * @param {game.ICStopExplore} message CStopExplore message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CStopExplore.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CStopExplore message, length delimited. Does not implicitly {@link game.CStopExplore.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CStopExplore
         * @static
         * @param {game.ICStopExplore} message CStopExplore message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CStopExplore.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CStopExplore message from the specified reader or buffer.
         * @function decode
         * @memberof game.CStopExplore
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CStopExplore} CStopExplore
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CStopExplore.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CStopExplore();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CStopExplore message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CStopExplore
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CStopExplore} CStopExplore
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CStopExplore.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CStopExplore message.
         * @function verify
         * @memberof game.CStopExplore
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CStopExplore.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CStopExplore message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CStopExplore
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CStopExplore} CStopExplore
         */
        CStopExplore.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CStopExplore)
                return object;
            return new $root.game.CStopExplore();
        };

        /**
         * Creates a plain object from a CStopExplore message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CStopExplore
         * @static
         * @param {game.CStopExplore} message CStopExplore
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CStopExplore.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CStopExplore to JSON.
         * @function toJSON
         * @memberof game.CStopExplore
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CStopExplore.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CStopExplore
         * @function getTypeUrl
         * @memberof game.CStopExplore
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CStopExplore.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CStopExplore";
        };

        return CStopExplore;
    })();

    game.SStopExplore = (function() {

        /**
         * Properties of a SStopExplore.
         * @memberof game
         * @interface ISStopExplore
         * @property {number|Long|null} [startTime] SStopExplore startTime
         */

        /**
         * Constructs a new SStopExplore.
         * @memberof game
         * @classdesc Represents a SStopExplore.
         * @implements ISStopExplore
         * @constructor
         * @param {game.ISStopExplore=} [properties] Properties to set
         */
        function SStopExplore(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SStopExplore startTime.
         * @member {number|Long} startTime
         * @memberof game.SStopExplore
         * @instance
         */
        SStopExplore.prototype.startTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new SStopExplore instance using the specified properties.
         * @function create
         * @memberof game.SStopExplore
         * @static
         * @param {game.ISStopExplore=} [properties] Properties to set
         * @returns {game.SStopExplore} SStopExplore instance
         */
        SStopExplore.create = function create(properties) {
            return new SStopExplore(properties);
        };

        /**
         * Encodes the specified SStopExplore message. Does not implicitly {@link game.SStopExplore.verify|verify} messages.
         * @function encode
         * @memberof game.SStopExplore
         * @static
         * @param {game.ISStopExplore} message SStopExplore message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SStopExplore.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.startTime);
            return writer;
        };

        /**
         * Encodes the specified SStopExplore message, length delimited. Does not implicitly {@link game.SStopExplore.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SStopExplore
         * @static
         * @param {game.ISStopExplore} message SStopExplore message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SStopExplore.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SStopExplore message from the specified reader or buffer.
         * @function decode
         * @memberof game.SStopExplore
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SStopExplore} SStopExplore
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SStopExplore.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SStopExplore();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.startTime = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SStopExplore message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SStopExplore
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SStopExplore} SStopExplore
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SStopExplore.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SStopExplore message.
         * @function verify
         * @memberof game.SStopExplore
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SStopExplore.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.startTime != null && message.hasOwnProperty("startTime"))
                if (!$util.isInteger(message.startTime) && !(message.startTime && $util.isInteger(message.startTime.low) && $util.isInteger(message.startTime.high)))
                    return "startTime: integer|Long expected";
            return null;
        };

        /**
         * Creates a SStopExplore message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SStopExplore
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SStopExplore} SStopExplore
         */
        SStopExplore.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SStopExplore)
                return object;
            var message = new $root.game.SStopExplore();
            if (object.startTime != null)
                if ($util.Long)
                    (message.startTime = $util.Long.fromValue(object.startTime)).unsigned = false;
                else if (typeof object.startTime === "string")
                    message.startTime = parseInt(object.startTime, 10);
                else if (typeof object.startTime === "number")
                    message.startTime = object.startTime;
                else if (typeof object.startTime === "object")
                    message.startTime = new $util.LongBits(object.startTime.low >>> 0, object.startTime.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a SStopExplore message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SStopExplore
         * @static
         * @param {game.SStopExplore} message SStopExplore
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SStopExplore.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.startTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.startTime = options.longs === String ? "0" : 0;
            if (message.startTime != null && message.hasOwnProperty("startTime"))
                if (typeof message.startTime === "number")
                    object.startTime = options.longs === String ? String(message.startTime) : message.startTime;
                else
                    object.startTime = options.longs === String ? $util.Long.prototype.toString.call(message.startTime) : options.longs === Number ? new $util.LongBits(message.startTime.low >>> 0, message.startTime.high >>> 0).toNumber() : message.startTime;
            return object;
        };

        /**
         * Converts this SStopExplore to JSON.
         * @function toJSON
         * @memberof game.SStopExplore
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SStopExplore.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SStopExplore
         * @function getTypeUrl
         * @memberof game.SStopExplore
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SStopExplore.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SStopExplore";
        };

        return SStopExplore;
    })();

    game.CGotoArea = (function() {

        /**
         * Properties of a CGotoArea.
         * @memberof game
         * @interface ICGotoArea
         * @property {number|null} [id] CGotoArea id
         */

        /**
         * Constructs a new CGotoArea.
         * @memberof game
         * @classdesc Represents a CGotoArea.
         * @implements ICGotoArea
         * @constructor
         * @param {game.ICGotoArea=} [properties] Properties to set
         */
        function CGotoArea(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CGotoArea id.
         * @member {number} id
         * @memberof game.CGotoArea
         * @instance
         */
        CGotoArea.prototype.id = 0;

        /**
         * Creates a new CGotoArea instance using the specified properties.
         * @function create
         * @memberof game.CGotoArea
         * @static
         * @param {game.ICGotoArea=} [properties] Properties to set
         * @returns {game.CGotoArea} CGotoArea instance
         */
        CGotoArea.create = function create(properties) {
            return new CGotoArea(properties);
        };

        /**
         * Encodes the specified CGotoArea message. Does not implicitly {@link game.CGotoArea.verify|verify} messages.
         * @function encode
         * @memberof game.CGotoArea
         * @static
         * @param {game.ICGotoArea} message CGotoArea message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGotoArea.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            return writer;
        };

        /**
         * Encodes the specified CGotoArea message, length delimited. Does not implicitly {@link game.CGotoArea.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGotoArea
         * @static
         * @param {game.ICGotoArea} message CGotoArea message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGotoArea.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGotoArea message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGotoArea
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGotoArea} CGotoArea
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGotoArea.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGotoArea();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGotoArea message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGotoArea
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGotoArea} CGotoArea
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGotoArea.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGotoArea message.
         * @function verify
         * @memberof game.CGotoArea
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGotoArea.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        /**
         * Creates a CGotoArea message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGotoArea
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGotoArea} CGotoArea
         */
        CGotoArea.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGotoArea)
                return object;
            var message = new $root.game.CGotoArea();
            if (object.id != null)
                message.id = object.id | 0;
            return message;
        };

        /**
         * Creates a plain object from a CGotoArea message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGotoArea
         * @static
         * @param {game.CGotoArea} message CGotoArea
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGotoArea.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        /**
         * Converts this CGotoArea to JSON.
         * @function toJSON
         * @memberof game.CGotoArea
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGotoArea.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGotoArea
         * @function getTypeUrl
         * @memberof game.CGotoArea
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGotoArea.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGotoArea";
        };

        return CGotoArea;
    })();

    game.SGotoArea = (function() {

        /**
         * Properties of a SGotoArea.
         * @memberof game
         * @interface ISGotoArea
         * @property {number|null} [moveStartPoint] SGotoArea moveStartPoint
         * @property {number|null} [target] SGotoArea target
         * @property {number|Long|null} [moveStartTime] SGotoArea moveStartTime
         */

        /**
         * Constructs a new SGotoArea.
         * @memberof game
         * @classdesc Represents a SGotoArea.
         * @implements ISGotoArea
         * @constructor
         * @param {game.ISGotoArea=} [properties] Properties to set
         */
        function SGotoArea(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGotoArea moveStartPoint.
         * @member {number} moveStartPoint
         * @memberof game.SGotoArea
         * @instance
         */
        SGotoArea.prototype.moveStartPoint = 0;

        /**
         * SGotoArea target.
         * @member {number} target
         * @memberof game.SGotoArea
         * @instance
         */
        SGotoArea.prototype.target = 0;

        /**
         * SGotoArea moveStartTime.
         * @member {number|Long} moveStartTime
         * @memberof game.SGotoArea
         * @instance
         */
        SGotoArea.prototype.moveStartTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new SGotoArea instance using the specified properties.
         * @function create
         * @memberof game.SGotoArea
         * @static
         * @param {game.ISGotoArea=} [properties] Properties to set
         * @returns {game.SGotoArea} SGotoArea instance
         */
        SGotoArea.create = function create(properties) {
            return new SGotoArea(properties);
        };

        /**
         * Encodes the specified SGotoArea message. Does not implicitly {@link game.SGotoArea.verify|verify} messages.
         * @function encode
         * @memberof game.SGotoArea
         * @static
         * @param {game.ISGotoArea} message SGotoArea message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGotoArea.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.moveStartPoint != null && Object.hasOwnProperty.call(message, "moveStartPoint"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.moveStartPoint);
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.target);
            if (message.moveStartTime != null && Object.hasOwnProperty.call(message, "moveStartTime"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.moveStartTime);
            return writer;
        };

        /**
         * Encodes the specified SGotoArea message, length delimited. Does not implicitly {@link game.SGotoArea.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGotoArea
         * @static
         * @param {game.ISGotoArea} message SGotoArea message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGotoArea.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGotoArea message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGotoArea
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGotoArea} SGotoArea
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGotoArea.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGotoArea();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.moveStartPoint = reader.int32();
                        break;
                    }
                case 2: {
                        message.target = reader.int32();
                        break;
                    }
                case 3: {
                        message.moveStartTime = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGotoArea message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGotoArea
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGotoArea} SGotoArea
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGotoArea.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGotoArea message.
         * @function verify
         * @memberof game.SGotoArea
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGotoArea.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.moveStartPoint != null && message.hasOwnProperty("moveStartPoint"))
                if (!$util.isInteger(message.moveStartPoint))
                    return "moveStartPoint: integer expected";
            if (message.target != null && message.hasOwnProperty("target"))
                if (!$util.isInteger(message.target))
                    return "target: integer expected";
            if (message.moveStartTime != null && message.hasOwnProperty("moveStartTime"))
                if (!$util.isInteger(message.moveStartTime) && !(message.moveStartTime && $util.isInteger(message.moveStartTime.low) && $util.isInteger(message.moveStartTime.high)))
                    return "moveStartTime: integer|Long expected";
            return null;
        };

        /**
         * Creates a SGotoArea message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGotoArea
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGotoArea} SGotoArea
         */
        SGotoArea.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGotoArea)
                return object;
            var message = new $root.game.SGotoArea();
            if (object.moveStartPoint != null)
                message.moveStartPoint = object.moveStartPoint | 0;
            if (object.target != null)
                message.target = object.target | 0;
            if (object.moveStartTime != null)
                if ($util.Long)
                    (message.moveStartTime = $util.Long.fromValue(object.moveStartTime)).unsigned = false;
                else if (typeof object.moveStartTime === "string")
                    message.moveStartTime = parseInt(object.moveStartTime, 10);
                else if (typeof object.moveStartTime === "number")
                    message.moveStartTime = object.moveStartTime;
                else if (typeof object.moveStartTime === "object")
                    message.moveStartTime = new $util.LongBits(object.moveStartTime.low >>> 0, object.moveStartTime.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a SGotoArea message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGotoArea
         * @static
         * @param {game.SGotoArea} message SGotoArea
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGotoArea.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.moveStartPoint = 0;
                object.target = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.moveStartTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.moveStartTime = options.longs === String ? "0" : 0;
            }
            if (message.moveStartPoint != null && message.hasOwnProperty("moveStartPoint"))
                object.moveStartPoint = message.moveStartPoint;
            if (message.target != null && message.hasOwnProperty("target"))
                object.target = message.target;
            if (message.moveStartTime != null && message.hasOwnProperty("moveStartTime"))
                if (typeof message.moveStartTime === "number")
                    object.moveStartTime = options.longs === String ? String(message.moveStartTime) : message.moveStartTime;
                else
                    object.moveStartTime = options.longs === String ? $util.Long.prototype.toString.call(message.moveStartTime) : options.longs === Number ? new $util.LongBits(message.moveStartTime.low >>> 0, message.moveStartTime.high >>> 0).toNumber() : message.moveStartTime;
            return object;
        };

        /**
         * Converts this SGotoArea to JSON.
         * @function toJSON
         * @memberof game.SGotoArea
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGotoArea.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGotoArea
         * @function getTypeUrl
         * @memberof game.SGotoArea
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGotoArea.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGotoArea";
        };

        return SGotoArea;
    })();

    game.CTriggerEvent = (function() {

        /**
         * Properties of a CTriggerEvent.
         * @memberof game
         * @interface ICTriggerEvent
         */

        /**
         * Constructs a new CTriggerEvent.
         * @memberof game
         * @classdesc Represents a CTriggerEvent.
         * @implements ICTriggerEvent
         * @constructor
         * @param {game.ICTriggerEvent=} [properties] Properties to set
         */
        function CTriggerEvent(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CTriggerEvent instance using the specified properties.
         * @function create
         * @memberof game.CTriggerEvent
         * @static
         * @param {game.ICTriggerEvent=} [properties] Properties to set
         * @returns {game.CTriggerEvent} CTriggerEvent instance
         */
        CTriggerEvent.create = function create(properties) {
            return new CTriggerEvent(properties);
        };

        /**
         * Encodes the specified CTriggerEvent message. Does not implicitly {@link game.CTriggerEvent.verify|verify} messages.
         * @function encode
         * @memberof game.CTriggerEvent
         * @static
         * @param {game.ICTriggerEvent} message CTriggerEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CTriggerEvent.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CTriggerEvent message, length delimited. Does not implicitly {@link game.CTriggerEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CTriggerEvent
         * @static
         * @param {game.ICTriggerEvent} message CTriggerEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CTriggerEvent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CTriggerEvent message from the specified reader or buffer.
         * @function decode
         * @memberof game.CTriggerEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CTriggerEvent} CTriggerEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CTriggerEvent.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CTriggerEvent();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CTriggerEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CTriggerEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CTriggerEvent} CTriggerEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CTriggerEvent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CTriggerEvent message.
         * @function verify
         * @memberof game.CTriggerEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CTriggerEvent.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CTriggerEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CTriggerEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CTriggerEvent} CTriggerEvent
         */
        CTriggerEvent.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CTriggerEvent)
                return object;
            return new $root.game.CTriggerEvent();
        };

        /**
         * Creates a plain object from a CTriggerEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CTriggerEvent
         * @static
         * @param {game.CTriggerEvent} message CTriggerEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CTriggerEvent.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CTriggerEvent to JSON.
         * @function toJSON
         * @memberof game.CTriggerEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CTriggerEvent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CTriggerEvent
         * @function getTypeUrl
         * @memberof game.CTriggerEvent
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CTriggerEvent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CTriggerEvent";
        };

        return CTriggerEvent;
    })();

    game.STriggerEvent = (function() {

        /**
         * Properties of a STriggerEvent.
         * @memberof game
         * @interface ISTriggerEvent
         * @property {number|null} [eventCount] STriggerEvent eventCount
         * @property {number|null} [id] STriggerEvent id
         */

        /**
         * Constructs a new STriggerEvent.
         * @memberof game
         * @classdesc Represents a STriggerEvent.
         * @implements ISTriggerEvent
         * @constructor
         * @param {game.ISTriggerEvent=} [properties] Properties to set
         */
        function STriggerEvent(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * STriggerEvent eventCount.
         * @member {number} eventCount
         * @memberof game.STriggerEvent
         * @instance
         */
        STriggerEvent.prototype.eventCount = 0;

        /**
         * STriggerEvent id.
         * @member {number} id
         * @memberof game.STriggerEvent
         * @instance
         */
        STriggerEvent.prototype.id = 0;

        /**
         * Creates a new STriggerEvent instance using the specified properties.
         * @function create
         * @memberof game.STriggerEvent
         * @static
         * @param {game.ISTriggerEvent=} [properties] Properties to set
         * @returns {game.STriggerEvent} STriggerEvent instance
         */
        STriggerEvent.create = function create(properties) {
            return new STriggerEvent(properties);
        };

        /**
         * Encodes the specified STriggerEvent message. Does not implicitly {@link game.STriggerEvent.verify|verify} messages.
         * @function encode
         * @memberof game.STriggerEvent
         * @static
         * @param {game.ISTriggerEvent} message STriggerEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        STriggerEvent.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.eventCount != null && Object.hasOwnProperty.call(message, "eventCount"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.eventCount);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.id);
            return writer;
        };

        /**
         * Encodes the specified STriggerEvent message, length delimited. Does not implicitly {@link game.STriggerEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.STriggerEvent
         * @static
         * @param {game.ISTriggerEvent} message STriggerEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        STriggerEvent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a STriggerEvent message from the specified reader or buffer.
         * @function decode
         * @memberof game.STriggerEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.STriggerEvent} STriggerEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        STriggerEvent.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.STriggerEvent();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.eventCount = reader.int32();
                        break;
                    }
                case 2: {
                        message.id = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a STriggerEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.STriggerEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.STriggerEvent} STriggerEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        STriggerEvent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a STriggerEvent message.
         * @function verify
         * @memberof game.STriggerEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        STriggerEvent.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.eventCount != null && message.hasOwnProperty("eventCount"))
                if (!$util.isInteger(message.eventCount))
                    return "eventCount: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        /**
         * Creates a STriggerEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.STriggerEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.STriggerEvent} STriggerEvent
         */
        STriggerEvent.fromObject = function fromObject(object) {
            if (object instanceof $root.game.STriggerEvent)
                return object;
            var message = new $root.game.STriggerEvent();
            if (object.eventCount != null)
                message.eventCount = object.eventCount | 0;
            if (object.id != null)
                message.id = object.id | 0;
            return message;
        };

        /**
         * Creates a plain object from a STriggerEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.STriggerEvent
         * @static
         * @param {game.STriggerEvent} message STriggerEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        STriggerEvent.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.eventCount = 0;
                object.id = 0;
            }
            if (message.eventCount != null && message.hasOwnProperty("eventCount"))
                object.eventCount = message.eventCount;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        /**
         * Converts this STriggerEvent to JSON.
         * @function toJSON
         * @memberof game.STriggerEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        STriggerEvent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for STriggerEvent
         * @function getTypeUrl
         * @memberof game.STriggerEvent
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        STriggerEvent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.STriggerEvent";
        };

        return STriggerEvent;
    })();

    game.CSendHero = (function() {

        /**
         * Properties of a CSendHero.
         * @memberof game
         * @interface ICSendHero
         * @property {number|null} [cardId] CSendHero cardId
         * @property {number|null} [index] CSendHero index
         */

        /**
         * Constructs a new CSendHero.
         * @memberof game
         * @classdesc Represents a CSendHero.
         * @implements ICSendHero
         * @constructor
         * @param {game.ICSendHero=} [properties] Properties to set
         */
        function CSendHero(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSendHero cardId.
         * @member {number} cardId
         * @memberof game.CSendHero
         * @instance
         */
        CSendHero.prototype.cardId = 0;

        /**
         * CSendHero index.
         * @member {number} index
         * @memberof game.CSendHero
         * @instance
         */
        CSendHero.prototype.index = 0;

        /**
         * Creates a new CSendHero instance using the specified properties.
         * @function create
         * @memberof game.CSendHero
         * @static
         * @param {game.ICSendHero=} [properties] Properties to set
         * @returns {game.CSendHero} CSendHero instance
         */
        CSendHero.create = function create(properties) {
            return new CSendHero(properties);
        };

        /**
         * Encodes the specified CSendHero message. Does not implicitly {@link game.CSendHero.verify|verify} messages.
         * @function encode
         * @memberof game.CSendHero
         * @static
         * @param {game.ICSendHero} message CSendHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSendHero.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.cardId != null && Object.hasOwnProperty.call(message, "cardId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.cardId);
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.index);
            return writer;
        };

        /**
         * Encodes the specified CSendHero message, length delimited. Does not implicitly {@link game.CSendHero.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CSendHero
         * @static
         * @param {game.ICSendHero} message CSendHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSendHero.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSendHero message from the specified reader or buffer.
         * @function decode
         * @memberof game.CSendHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CSendHero} CSendHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSendHero.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CSendHero();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.cardId = reader.int32();
                        break;
                    }
                case 2: {
                        message.index = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSendHero message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CSendHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CSendHero} CSendHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSendHero.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSendHero message.
         * @function verify
         * @memberof game.CSendHero
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSendHero.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.cardId != null && message.hasOwnProperty("cardId"))
                if (!$util.isInteger(message.cardId))
                    return "cardId: integer expected";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            return null;
        };

        /**
         * Creates a CSendHero message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CSendHero
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CSendHero} CSendHero
         */
        CSendHero.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CSendHero)
                return object;
            var message = new $root.game.CSendHero();
            if (object.cardId != null)
                message.cardId = object.cardId | 0;
            if (object.index != null)
                message.index = object.index | 0;
            return message;
        };

        /**
         * Creates a plain object from a CSendHero message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CSendHero
         * @static
         * @param {game.CSendHero} message CSendHero
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSendHero.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.cardId = 0;
                object.index = 0;
            }
            if (message.cardId != null && message.hasOwnProperty("cardId"))
                object.cardId = message.cardId;
            if (message.index != null && message.hasOwnProperty("index"))
                object.index = message.index;
            return object;
        };

        /**
         * Converts this CSendHero to JSON.
         * @function toJSON
         * @memberof game.CSendHero
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSendHero.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSendHero
         * @function getTypeUrl
         * @memberof game.CSendHero
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSendHero.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CSendHero";
        };

        return CSendHero;
    })();

    game.SSendHero = (function() {

        /**
         * Properties of a SSendHero.
         * @memberof game
         * @interface ISSendHero
         * @property {Array.<number>|null} [slots] SSendHero slots
         */

        /**
         * Constructs a new SSendHero.
         * @memberof game
         * @classdesc Represents a SSendHero.
         * @implements ISSendHero
         * @constructor
         * @param {game.ISSendHero=} [properties] Properties to set
         */
        function SSendHero(properties) {
            this.slots = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SSendHero slots.
         * @member {Array.<number>} slots
         * @memberof game.SSendHero
         * @instance
         */
        SSendHero.prototype.slots = $util.emptyArray;

        /**
         * Creates a new SSendHero instance using the specified properties.
         * @function create
         * @memberof game.SSendHero
         * @static
         * @param {game.ISSendHero=} [properties] Properties to set
         * @returns {game.SSendHero} SSendHero instance
         */
        SSendHero.create = function create(properties) {
            return new SSendHero(properties);
        };

        /**
         * Encodes the specified SSendHero message. Does not implicitly {@link game.SSendHero.verify|verify} messages.
         * @function encode
         * @memberof game.SSendHero
         * @static
         * @param {game.ISSendHero} message SSendHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSendHero.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.slots != null && message.slots.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (var i = 0; i < message.slots.length; ++i)
                    writer.int32(message.slots[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified SSendHero message, length delimited. Does not implicitly {@link game.SSendHero.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SSendHero
         * @static
         * @param {game.ISSendHero} message SSendHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSendHero.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SSendHero message from the specified reader or buffer.
         * @function decode
         * @memberof game.SSendHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SSendHero} SSendHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSendHero.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SSendHero();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.slots && message.slots.length))
                            message.slots = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.slots.push(reader.int32());
                        } else
                            message.slots.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SSendHero message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SSendHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SSendHero} SSendHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSendHero.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SSendHero message.
         * @function verify
         * @memberof game.SSendHero
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SSendHero.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.slots != null && message.hasOwnProperty("slots")) {
                if (!Array.isArray(message.slots))
                    return "slots: array expected";
                for (var i = 0; i < message.slots.length; ++i)
                    if (!$util.isInteger(message.slots[i]))
                        return "slots: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a SSendHero message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SSendHero
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SSendHero} SSendHero
         */
        SSendHero.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SSendHero)
                return object;
            var message = new $root.game.SSendHero();
            if (object.slots) {
                if (!Array.isArray(object.slots))
                    throw TypeError(".game.SSendHero.slots: array expected");
                message.slots = [];
                for (var i = 0; i < object.slots.length; ++i)
                    message.slots[i] = object.slots[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a SSendHero message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SSendHero
         * @static
         * @param {game.SSendHero} message SSendHero
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SSendHero.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.slots = [];
            if (message.slots && message.slots.length) {
                object.slots = [];
                for (var j = 0; j < message.slots.length; ++j)
                    object.slots[j] = message.slots[j];
            }
            return object;
        };

        /**
         * Converts this SSendHero to JSON.
         * @function toJSON
         * @memberof game.SSendHero
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SSendHero.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SSendHero
         * @function getTypeUrl
         * @memberof game.SSendHero
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SSendHero.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SSendHero";
        };

        return SSendHero;
    })();

    game.CSetTools = (function() {

        /**
         * Properties of a CSetTools.
         * @memberof game
         * @interface ICSetTools
         * @property {number|null} [id] CSetTools id
         */

        /**
         * Constructs a new CSetTools.
         * @memberof game
         * @classdesc Represents a CSetTools.
         * @implements ICSetTools
         * @constructor
         * @param {game.ICSetTools=} [properties] Properties to set
         */
        function CSetTools(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSetTools id.
         * @member {number} id
         * @memberof game.CSetTools
         * @instance
         */
        CSetTools.prototype.id = 0;

        /**
         * Creates a new CSetTools instance using the specified properties.
         * @function create
         * @memberof game.CSetTools
         * @static
         * @param {game.ICSetTools=} [properties] Properties to set
         * @returns {game.CSetTools} CSetTools instance
         */
        CSetTools.create = function create(properties) {
            return new CSetTools(properties);
        };

        /**
         * Encodes the specified CSetTools message. Does not implicitly {@link game.CSetTools.verify|verify} messages.
         * @function encode
         * @memberof game.CSetTools
         * @static
         * @param {game.ICSetTools} message CSetTools message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSetTools.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            return writer;
        };

        /**
         * Encodes the specified CSetTools message, length delimited. Does not implicitly {@link game.CSetTools.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CSetTools
         * @static
         * @param {game.ICSetTools} message CSetTools message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSetTools.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSetTools message from the specified reader or buffer.
         * @function decode
         * @memberof game.CSetTools
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CSetTools} CSetTools
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSetTools.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CSetTools();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSetTools message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CSetTools
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CSetTools} CSetTools
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSetTools.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSetTools message.
         * @function verify
         * @memberof game.CSetTools
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSetTools.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        /**
         * Creates a CSetTools message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CSetTools
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CSetTools} CSetTools
         */
        CSetTools.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CSetTools)
                return object;
            var message = new $root.game.CSetTools();
            if (object.id != null)
                message.id = object.id | 0;
            return message;
        };

        /**
         * Creates a plain object from a CSetTools message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CSetTools
         * @static
         * @param {game.CSetTools} message CSetTools
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSetTools.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        /**
         * Converts this CSetTools to JSON.
         * @function toJSON
         * @memberof game.CSetTools
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSetTools.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSetTools
         * @function getTypeUrl
         * @memberof game.CSetTools
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSetTools.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CSetTools";
        };

        return CSetTools;
    })();

    game.SSetTools = (function() {

        /**
         * Properties of a SSetTools.
         * @memberof game
         * @interface ISSetTools
         */

        /**
         * Constructs a new SSetTools.
         * @memberof game
         * @classdesc Represents a SSetTools.
         * @implements ISSetTools
         * @constructor
         * @param {game.ISSetTools=} [properties] Properties to set
         */
        function SSetTools(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SSetTools instance using the specified properties.
         * @function create
         * @memberof game.SSetTools
         * @static
         * @param {game.ISSetTools=} [properties] Properties to set
         * @returns {game.SSetTools} SSetTools instance
         */
        SSetTools.create = function create(properties) {
            return new SSetTools(properties);
        };

        /**
         * Encodes the specified SSetTools message. Does not implicitly {@link game.SSetTools.verify|verify} messages.
         * @function encode
         * @memberof game.SSetTools
         * @static
         * @param {game.ISSetTools} message SSetTools message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSetTools.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SSetTools message, length delimited. Does not implicitly {@link game.SSetTools.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SSetTools
         * @static
         * @param {game.ISSetTools} message SSetTools message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSetTools.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SSetTools message from the specified reader or buffer.
         * @function decode
         * @memberof game.SSetTools
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SSetTools} SSetTools
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSetTools.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SSetTools();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SSetTools message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SSetTools
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SSetTools} SSetTools
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSetTools.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SSetTools message.
         * @function verify
         * @memberof game.SSetTools
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SSetTools.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SSetTools message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SSetTools
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SSetTools} SSetTools
         */
        SSetTools.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SSetTools)
                return object;
            return new $root.game.SSetTools();
        };

        /**
         * Creates a plain object from a SSetTools message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SSetTools
         * @static
         * @param {game.SSetTools} message SSetTools
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SSetTools.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SSetTools to JSON.
         * @function toJSON
         * @memberof game.SSetTools
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SSetTools.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SSetTools
         * @function getTypeUrl
         * @memberof game.SSetTools
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SSetTools.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SSetTools";
        };

        return SSetTools;
    })();

    game.CClaimOnceAward = (function() {

        /**
         * Properties of a CClaimOnceAward.
         * @memberof game
         * @interface ICClaimOnceAward
         * @property {number|null} [index] CClaimOnceAward index
         */

        /**
         * Constructs a new CClaimOnceAward.
         * @memberof game
         * @classdesc Represents a CClaimOnceAward.
         * @implements ICClaimOnceAward
         * @constructor
         * @param {game.ICClaimOnceAward=} [properties] Properties to set
         */
        function CClaimOnceAward(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CClaimOnceAward index.
         * @member {number} index
         * @memberof game.CClaimOnceAward
         * @instance
         */
        CClaimOnceAward.prototype.index = 0;

        /**
         * Creates a new CClaimOnceAward instance using the specified properties.
         * @function create
         * @memberof game.CClaimOnceAward
         * @static
         * @param {game.ICClaimOnceAward=} [properties] Properties to set
         * @returns {game.CClaimOnceAward} CClaimOnceAward instance
         */
        CClaimOnceAward.create = function create(properties) {
            return new CClaimOnceAward(properties);
        };

        /**
         * Encodes the specified CClaimOnceAward message. Does not implicitly {@link game.CClaimOnceAward.verify|verify} messages.
         * @function encode
         * @memberof game.CClaimOnceAward
         * @static
         * @param {game.ICClaimOnceAward} message CClaimOnceAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CClaimOnceAward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.index);
            return writer;
        };

        /**
         * Encodes the specified CClaimOnceAward message, length delimited. Does not implicitly {@link game.CClaimOnceAward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CClaimOnceAward
         * @static
         * @param {game.ICClaimOnceAward} message CClaimOnceAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CClaimOnceAward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CClaimOnceAward message from the specified reader or buffer.
         * @function decode
         * @memberof game.CClaimOnceAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CClaimOnceAward} CClaimOnceAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CClaimOnceAward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CClaimOnceAward();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.index = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CClaimOnceAward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CClaimOnceAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CClaimOnceAward} CClaimOnceAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CClaimOnceAward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CClaimOnceAward message.
         * @function verify
         * @memberof game.CClaimOnceAward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CClaimOnceAward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            return null;
        };

        /**
         * Creates a CClaimOnceAward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CClaimOnceAward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CClaimOnceAward} CClaimOnceAward
         */
        CClaimOnceAward.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CClaimOnceAward)
                return object;
            var message = new $root.game.CClaimOnceAward();
            if (object.index != null)
                message.index = object.index | 0;
            return message;
        };

        /**
         * Creates a plain object from a CClaimOnceAward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CClaimOnceAward
         * @static
         * @param {game.CClaimOnceAward} message CClaimOnceAward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CClaimOnceAward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.index = 0;
            if (message.index != null && message.hasOwnProperty("index"))
                object.index = message.index;
            return object;
        };

        /**
         * Converts this CClaimOnceAward to JSON.
         * @function toJSON
         * @memberof game.CClaimOnceAward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CClaimOnceAward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CClaimOnceAward
         * @function getTypeUrl
         * @memberof game.CClaimOnceAward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CClaimOnceAward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CClaimOnceAward";
        };

        return CClaimOnceAward;
    })();

    game.SClaimOnceAward = (function() {

        /**
         * Properties of a SClaimOnceAward.
         * @memberof game
         * @interface ISClaimOnceAward
         * @property {Array.<common.IReward>|null} [awards] SClaimOnceAward awards
         */

        /**
         * Constructs a new SClaimOnceAward.
         * @memberof game
         * @classdesc Represents a SClaimOnceAward.
         * @implements ISClaimOnceAward
         * @constructor
         * @param {game.ISClaimOnceAward=} [properties] Properties to set
         */
        function SClaimOnceAward(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SClaimOnceAward awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SClaimOnceAward
         * @instance
         */
        SClaimOnceAward.prototype.awards = $util.emptyArray;

        /**
         * Creates a new SClaimOnceAward instance using the specified properties.
         * @function create
         * @memberof game.SClaimOnceAward
         * @static
         * @param {game.ISClaimOnceAward=} [properties] Properties to set
         * @returns {game.SClaimOnceAward} SClaimOnceAward instance
         */
        SClaimOnceAward.create = function create(properties) {
            return new SClaimOnceAward(properties);
        };

        /**
         * Encodes the specified SClaimOnceAward message. Does not implicitly {@link game.SClaimOnceAward.verify|verify} messages.
         * @function encode
         * @memberof game.SClaimOnceAward
         * @static
         * @param {game.ISClaimOnceAward} message SClaimOnceAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SClaimOnceAward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SClaimOnceAward message, length delimited. Does not implicitly {@link game.SClaimOnceAward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SClaimOnceAward
         * @static
         * @param {game.ISClaimOnceAward} message SClaimOnceAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SClaimOnceAward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SClaimOnceAward message from the specified reader or buffer.
         * @function decode
         * @memberof game.SClaimOnceAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SClaimOnceAward} SClaimOnceAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SClaimOnceAward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SClaimOnceAward();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SClaimOnceAward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SClaimOnceAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SClaimOnceAward} SClaimOnceAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SClaimOnceAward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SClaimOnceAward message.
         * @function verify
         * @memberof game.SClaimOnceAward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SClaimOnceAward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SClaimOnceAward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SClaimOnceAward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SClaimOnceAward} SClaimOnceAward
         */
        SClaimOnceAward.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SClaimOnceAward)
                return object;
            var message = new $root.game.SClaimOnceAward();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SClaimOnceAward.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SClaimOnceAward.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SClaimOnceAward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SClaimOnceAward
         * @static
         * @param {game.SClaimOnceAward} message SClaimOnceAward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SClaimOnceAward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            return object;
        };

        /**
         * Converts this SClaimOnceAward to JSON.
         * @function toJSON
         * @memberof game.SClaimOnceAward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SClaimOnceAward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SClaimOnceAward
         * @function getTypeUrl
         * @memberof game.SClaimOnceAward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SClaimOnceAward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SClaimOnceAward";
        };

        return SClaimOnceAward;
    })();

    game.CSwitchTeam = (function() {

        /**
         * Properties of a CSwitchTeam.
         * @memberof game
         * @interface ICSwitchTeam
         * @property {number|null} [teamId] CSwitchTeam teamId
         */

        /**
         * Constructs a new CSwitchTeam.
         * @memberof game
         * @classdesc Represents a CSwitchTeam.
         * @implements ICSwitchTeam
         * @constructor
         * @param {game.ICSwitchTeam=} [properties] Properties to set
         */
        function CSwitchTeam(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSwitchTeam teamId.
         * @member {number} teamId
         * @memberof game.CSwitchTeam
         * @instance
         */
        CSwitchTeam.prototype.teamId = 0;

        /**
         * Creates a new CSwitchTeam instance using the specified properties.
         * @function create
         * @memberof game.CSwitchTeam
         * @static
         * @param {game.ICSwitchTeam=} [properties] Properties to set
         * @returns {game.CSwitchTeam} CSwitchTeam instance
         */
        CSwitchTeam.create = function create(properties) {
            return new CSwitchTeam(properties);
        };

        /**
         * Encodes the specified CSwitchTeam message. Does not implicitly {@link game.CSwitchTeam.verify|verify} messages.
         * @function encode
         * @memberof game.CSwitchTeam
         * @static
         * @param {game.ICSwitchTeam} message CSwitchTeam message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSwitchTeam.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.teamId != null && Object.hasOwnProperty.call(message, "teamId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.teamId);
            return writer;
        };

        /**
         * Encodes the specified CSwitchTeam message, length delimited. Does not implicitly {@link game.CSwitchTeam.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CSwitchTeam
         * @static
         * @param {game.ICSwitchTeam} message CSwitchTeam message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSwitchTeam.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSwitchTeam message from the specified reader or buffer.
         * @function decode
         * @memberof game.CSwitchTeam
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CSwitchTeam} CSwitchTeam
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSwitchTeam.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CSwitchTeam();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.teamId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSwitchTeam message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CSwitchTeam
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CSwitchTeam} CSwitchTeam
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSwitchTeam.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSwitchTeam message.
         * @function verify
         * @memberof game.CSwitchTeam
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSwitchTeam.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.teamId != null && message.hasOwnProperty("teamId"))
                if (!$util.isInteger(message.teamId))
                    return "teamId: integer expected";
            return null;
        };

        /**
         * Creates a CSwitchTeam message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CSwitchTeam
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CSwitchTeam} CSwitchTeam
         */
        CSwitchTeam.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CSwitchTeam)
                return object;
            var message = new $root.game.CSwitchTeam();
            if (object.teamId != null)
                message.teamId = object.teamId | 0;
            return message;
        };

        /**
         * Creates a plain object from a CSwitchTeam message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CSwitchTeam
         * @static
         * @param {game.CSwitchTeam} message CSwitchTeam
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSwitchTeam.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.teamId = 0;
            if (message.teamId != null && message.hasOwnProperty("teamId"))
                object.teamId = message.teamId;
            return object;
        };

        /**
         * Converts this CSwitchTeam to JSON.
         * @function toJSON
         * @memberof game.CSwitchTeam
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSwitchTeam.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSwitchTeam
         * @function getTypeUrl
         * @memberof game.CSwitchTeam
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSwitchTeam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CSwitchTeam";
        };

        return CSwitchTeam;
    })();

    game.SSwitchTeam = (function() {

        /**
         * Properties of a SSwitchTeam.
         * @memberof game
         * @interface ISSwitchTeam
         */

        /**
         * Constructs a new SSwitchTeam.
         * @memberof game
         * @classdesc Represents a SSwitchTeam.
         * @implements ISSwitchTeam
         * @constructor
         * @param {game.ISSwitchTeam=} [properties] Properties to set
         */
        function SSwitchTeam(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SSwitchTeam instance using the specified properties.
         * @function create
         * @memberof game.SSwitchTeam
         * @static
         * @param {game.ISSwitchTeam=} [properties] Properties to set
         * @returns {game.SSwitchTeam} SSwitchTeam instance
         */
        SSwitchTeam.create = function create(properties) {
            return new SSwitchTeam(properties);
        };

        /**
         * Encodes the specified SSwitchTeam message. Does not implicitly {@link game.SSwitchTeam.verify|verify} messages.
         * @function encode
         * @memberof game.SSwitchTeam
         * @static
         * @param {game.ISSwitchTeam} message SSwitchTeam message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSwitchTeam.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SSwitchTeam message, length delimited. Does not implicitly {@link game.SSwitchTeam.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SSwitchTeam
         * @static
         * @param {game.ISSwitchTeam} message SSwitchTeam message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSwitchTeam.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SSwitchTeam message from the specified reader or buffer.
         * @function decode
         * @memberof game.SSwitchTeam
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SSwitchTeam} SSwitchTeam
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSwitchTeam.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SSwitchTeam();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SSwitchTeam message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SSwitchTeam
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SSwitchTeam} SSwitchTeam
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSwitchTeam.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SSwitchTeam message.
         * @function verify
         * @memberof game.SSwitchTeam
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SSwitchTeam.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SSwitchTeam message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SSwitchTeam
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SSwitchTeam} SSwitchTeam
         */
        SSwitchTeam.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SSwitchTeam)
                return object;
            return new $root.game.SSwitchTeam();
        };

        /**
         * Creates a plain object from a SSwitchTeam message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SSwitchTeam
         * @static
         * @param {game.SSwitchTeam} message SSwitchTeam
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SSwitchTeam.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SSwitchTeam to JSON.
         * @function toJSON
         * @memberof game.SSwitchTeam
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SSwitchTeam.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SSwitchTeam
         * @function getTypeUrl
         * @memberof game.SSwitchTeam
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SSwitchTeam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SSwitchTeam";
        };

        return SSwitchTeam;
    })();

    game.CBuyTools = (function() {

        /**
         * Properties of a CBuyTools.
         * @memberof game
         * @interface ICBuyTools
         * @property {number|null} [id] CBuyTools id
         */

        /**
         * Constructs a new CBuyTools.
         * @memberof game
         * @classdesc Represents a CBuyTools.
         * @implements ICBuyTools
         * @constructor
         * @param {game.ICBuyTools=} [properties] Properties to set
         */
        function CBuyTools(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CBuyTools id.
         * @member {number} id
         * @memberof game.CBuyTools
         * @instance
         */
        CBuyTools.prototype.id = 0;

        /**
         * Creates a new CBuyTools instance using the specified properties.
         * @function create
         * @memberof game.CBuyTools
         * @static
         * @param {game.ICBuyTools=} [properties] Properties to set
         * @returns {game.CBuyTools} CBuyTools instance
         */
        CBuyTools.create = function create(properties) {
            return new CBuyTools(properties);
        };

        /**
         * Encodes the specified CBuyTools message. Does not implicitly {@link game.CBuyTools.verify|verify} messages.
         * @function encode
         * @memberof game.CBuyTools
         * @static
         * @param {game.ICBuyTools} message CBuyTools message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CBuyTools.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            return writer;
        };

        /**
         * Encodes the specified CBuyTools message, length delimited. Does not implicitly {@link game.CBuyTools.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CBuyTools
         * @static
         * @param {game.ICBuyTools} message CBuyTools message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CBuyTools.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CBuyTools message from the specified reader or buffer.
         * @function decode
         * @memberof game.CBuyTools
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CBuyTools} CBuyTools
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CBuyTools.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CBuyTools();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CBuyTools message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CBuyTools
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CBuyTools} CBuyTools
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CBuyTools.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CBuyTools message.
         * @function verify
         * @memberof game.CBuyTools
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CBuyTools.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        /**
         * Creates a CBuyTools message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CBuyTools
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CBuyTools} CBuyTools
         */
        CBuyTools.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CBuyTools)
                return object;
            var message = new $root.game.CBuyTools();
            if (object.id != null)
                message.id = object.id | 0;
            return message;
        };

        /**
         * Creates a plain object from a CBuyTools message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CBuyTools
         * @static
         * @param {game.CBuyTools} message CBuyTools
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CBuyTools.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        /**
         * Converts this CBuyTools to JSON.
         * @function toJSON
         * @memberof game.CBuyTools
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CBuyTools.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CBuyTools
         * @function getTypeUrl
         * @memberof game.CBuyTools
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CBuyTools.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CBuyTools";
        };

        return CBuyTools;
    })();

    game.SBuyTools = (function() {

        /**
         * Properties of a SBuyTools.
         * @memberof game
         * @interface ISBuyTools
         * @property {Array.<common.IReward>|null} [costs] SBuyTools costs
         */

        /**
         * Constructs a new SBuyTools.
         * @memberof game
         * @classdesc Represents a SBuyTools.
         * @implements ISBuyTools
         * @constructor
         * @param {game.ISBuyTools=} [properties] Properties to set
         */
        function SBuyTools(properties) {
            this.costs = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SBuyTools costs.
         * @member {Array.<common.IReward>} costs
         * @memberof game.SBuyTools
         * @instance
         */
        SBuyTools.prototype.costs = $util.emptyArray;

        /**
         * Creates a new SBuyTools instance using the specified properties.
         * @function create
         * @memberof game.SBuyTools
         * @static
         * @param {game.ISBuyTools=} [properties] Properties to set
         * @returns {game.SBuyTools} SBuyTools instance
         */
        SBuyTools.create = function create(properties) {
            return new SBuyTools(properties);
        };

        /**
         * Encodes the specified SBuyTools message. Does not implicitly {@link game.SBuyTools.verify|verify} messages.
         * @function encode
         * @memberof game.SBuyTools
         * @static
         * @param {game.ISBuyTools} message SBuyTools message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SBuyTools.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.costs != null && message.costs.length)
                for (var i = 0; i < message.costs.length; ++i)
                    $root.common.Reward.encode(message.costs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SBuyTools message, length delimited. Does not implicitly {@link game.SBuyTools.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SBuyTools
         * @static
         * @param {game.ISBuyTools} message SBuyTools message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SBuyTools.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SBuyTools message from the specified reader or buffer.
         * @function decode
         * @memberof game.SBuyTools
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SBuyTools} SBuyTools
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SBuyTools.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SBuyTools();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.costs && message.costs.length))
                            message.costs = [];
                        message.costs.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SBuyTools message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SBuyTools
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SBuyTools} SBuyTools
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SBuyTools.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SBuyTools message.
         * @function verify
         * @memberof game.SBuyTools
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SBuyTools.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.costs != null && message.hasOwnProperty("costs")) {
                if (!Array.isArray(message.costs))
                    return "costs: array expected";
                for (var i = 0; i < message.costs.length; ++i) {
                    var error = $root.common.Reward.verify(message.costs[i]);
                    if (error)
                        return "costs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SBuyTools message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SBuyTools
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SBuyTools} SBuyTools
         */
        SBuyTools.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SBuyTools)
                return object;
            var message = new $root.game.SBuyTools();
            if (object.costs) {
                if (!Array.isArray(object.costs))
                    throw TypeError(".game.SBuyTools.costs: array expected");
                message.costs = [];
                for (var i = 0; i < object.costs.length; ++i) {
                    if (typeof object.costs[i] !== "object")
                        throw TypeError(".game.SBuyTools.costs: object expected");
                    message.costs[i] = $root.common.Reward.fromObject(object.costs[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SBuyTools message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SBuyTools
         * @static
         * @param {game.SBuyTools} message SBuyTools
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SBuyTools.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.costs = [];
            if (message.costs && message.costs.length) {
                object.costs = [];
                for (var j = 0; j < message.costs.length; ++j)
                    object.costs[j] = $root.common.Reward.toObject(message.costs[j], options);
            }
            return object;
        };

        /**
         * Converts this SBuyTools to JSON.
         * @function toJSON
         * @memberof game.SBuyTools
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SBuyTools.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SBuyTools
         * @function getTypeUrl
         * @memberof game.SBuyTools
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SBuyTools.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SBuyTools";
        };

        return SBuyTools;
    })();

    game.CUpgradeArea = (function() {

        /**
         * Properties of a CUpgradeArea.
         * @memberof game
         * @interface ICUpgradeArea
         * @property {number|null} [areaId] CUpgradeArea areaId
         */

        /**
         * Constructs a new CUpgradeArea.
         * @memberof game
         * @classdesc Represents a CUpgradeArea.
         * @implements ICUpgradeArea
         * @constructor
         * @param {game.ICUpgradeArea=} [properties] Properties to set
         */
        function CUpgradeArea(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CUpgradeArea areaId.
         * @member {number} areaId
         * @memberof game.CUpgradeArea
         * @instance
         */
        CUpgradeArea.prototype.areaId = 0;

        /**
         * Creates a new CUpgradeArea instance using the specified properties.
         * @function create
         * @memberof game.CUpgradeArea
         * @static
         * @param {game.ICUpgradeArea=} [properties] Properties to set
         * @returns {game.CUpgradeArea} CUpgradeArea instance
         */
        CUpgradeArea.create = function create(properties) {
            return new CUpgradeArea(properties);
        };

        /**
         * Encodes the specified CUpgradeArea message. Does not implicitly {@link game.CUpgradeArea.verify|verify} messages.
         * @function encode
         * @memberof game.CUpgradeArea
         * @static
         * @param {game.ICUpgradeArea} message CUpgradeArea message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CUpgradeArea.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.areaId != null && Object.hasOwnProperty.call(message, "areaId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.areaId);
            return writer;
        };

        /**
         * Encodes the specified CUpgradeArea message, length delimited. Does not implicitly {@link game.CUpgradeArea.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CUpgradeArea
         * @static
         * @param {game.ICUpgradeArea} message CUpgradeArea message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CUpgradeArea.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CUpgradeArea message from the specified reader or buffer.
         * @function decode
         * @memberof game.CUpgradeArea
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CUpgradeArea} CUpgradeArea
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CUpgradeArea.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CUpgradeArea();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.areaId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CUpgradeArea message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CUpgradeArea
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CUpgradeArea} CUpgradeArea
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CUpgradeArea.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CUpgradeArea message.
         * @function verify
         * @memberof game.CUpgradeArea
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CUpgradeArea.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.areaId != null && message.hasOwnProperty("areaId"))
                if (!$util.isInteger(message.areaId))
                    return "areaId: integer expected";
            return null;
        };

        /**
         * Creates a CUpgradeArea message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CUpgradeArea
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CUpgradeArea} CUpgradeArea
         */
        CUpgradeArea.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CUpgradeArea)
                return object;
            var message = new $root.game.CUpgradeArea();
            if (object.areaId != null)
                message.areaId = object.areaId | 0;
            return message;
        };

        /**
         * Creates a plain object from a CUpgradeArea message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CUpgradeArea
         * @static
         * @param {game.CUpgradeArea} message CUpgradeArea
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CUpgradeArea.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.areaId = 0;
            if (message.areaId != null && message.hasOwnProperty("areaId"))
                object.areaId = message.areaId;
            return object;
        };

        /**
         * Converts this CUpgradeArea to JSON.
         * @function toJSON
         * @memberof game.CUpgradeArea
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CUpgradeArea.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CUpgradeArea
         * @function getTypeUrl
         * @memberof game.CUpgradeArea
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CUpgradeArea.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CUpgradeArea";
        };

        return CUpgradeArea;
    })();

    game.SUpgradeArea = (function() {

        /**
         * Properties of a SUpgradeArea.
         * @memberof game
         * @interface ISUpgradeArea
         * @property {Array.<common.IReward>|null} [costs] SUpgradeArea costs
         * @property {Array.<common.IReward>|null} [awards] SUpgradeArea awards
         * @property {number|null} [level] SUpgradeArea level
         */

        /**
         * Constructs a new SUpgradeArea.
         * @memberof game
         * @classdesc Represents a SUpgradeArea.
         * @implements ISUpgradeArea
         * @constructor
         * @param {game.ISUpgradeArea=} [properties] Properties to set
         */
        function SUpgradeArea(properties) {
            this.costs = [];
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SUpgradeArea costs.
         * @member {Array.<common.IReward>} costs
         * @memberof game.SUpgradeArea
         * @instance
         */
        SUpgradeArea.prototype.costs = $util.emptyArray;

        /**
         * SUpgradeArea awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SUpgradeArea
         * @instance
         */
        SUpgradeArea.prototype.awards = $util.emptyArray;

        /**
         * SUpgradeArea level.
         * @member {number} level
         * @memberof game.SUpgradeArea
         * @instance
         */
        SUpgradeArea.prototype.level = 0;

        /**
         * Creates a new SUpgradeArea instance using the specified properties.
         * @function create
         * @memberof game.SUpgradeArea
         * @static
         * @param {game.ISUpgradeArea=} [properties] Properties to set
         * @returns {game.SUpgradeArea} SUpgradeArea instance
         */
        SUpgradeArea.create = function create(properties) {
            return new SUpgradeArea(properties);
        };

        /**
         * Encodes the specified SUpgradeArea message. Does not implicitly {@link game.SUpgradeArea.verify|verify} messages.
         * @function encode
         * @memberof game.SUpgradeArea
         * @static
         * @param {game.ISUpgradeArea} message SUpgradeArea message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SUpgradeArea.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.costs != null && message.costs.length)
                for (var i = 0; i < message.costs.length; ++i)
                    $root.common.Reward.encode(message.costs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.level);
            return writer;
        };

        /**
         * Encodes the specified SUpgradeArea message, length delimited. Does not implicitly {@link game.SUpgradeArea.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SUpgradeArea
         * @static
         * @param {game.ISUpgradeArea} message SUpgradeArea message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SUpgradeArea.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SUpgradeArea message from the specified reader or buffer.
         * @function decode
         * @memberof game.SUpgradeArea
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SUpgradeArea} SUpgradeArea
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SUpgradeArea.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SUpgradeArea();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.costs && message.costs.length))
                            message.costs = [];
                        message.costs.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.level = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SUpgradeArea message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SUpgradeArea
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SUpgradeArea} SUpgradeArea
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SUpgradeArea.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SUpgradeArea message.
         * @function verify
         * @memberof game.SUpgradeArea
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SUpgradeArea.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.costs != null && message.hasOwnProperty("costs")) {
                if (!Array.isArray(message.costs))
                    return "costs: array expected";
                for (var i = 0; i < message.costs.length; ++i) {
                    var error = $root.common.Reward.verify(message.costs[i]);
                    if (error)
                        return "costs." + error;
                }
            }
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            return null;
        };

        /**
         * Creates a SUpgradeArea message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SUpgradeArea
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SUpgradeArea} SUpgradeArea
         */
        SUpgradeArea.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SUpgradeArea)
                return object;
            var message = new $root.game.SUpgradeArea();
            if (object.costs) {
                if (!Array.isArray(object.costs))
                    throw TypeError(".game.SUpgradeArea.costs: array expected");
                message.costs = [];
                for (var i = 0; i < object.costs.length; ++i) {
                    if (typeof object.costs[i] !== "object")
                        throw TypeError(".game.SUpgradeArea.costs: object expected");
                    message.costs[i] = $root.common.Reward.fromObject(object.costs[i]);
                }
            }
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SUpgradeArea.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SUpgradeArea.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            if (object.level != null)
                message.level = object.level | 0;
            return message;
        };

        /**
         * Creates a plain object from a SUpgradeArea message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SUpgradeArea
         * @static
         * @param {game.SUpgradeArea} message SUpgradeArea
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SUpgradeArea.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.costs = [];
                object.awards = [];
            }
            if (options.defaults)
                object.level = 0;
            if (message.costs && message.costs.length) {
                object.costs = [];
                for (var j = 0; j < message.costs.length; ++j)
                    object.costs[j] = $root.common.Reward.toObject(message.costs[j], options);
            }
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            return object;
        };

        /**
         * Converts this SUpgradeArea to JSON.
         * @function toJSON
         * @memberof game.SUpgradeArea
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SUpgradeArea.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SUpgradeArea
         * @function getTypeUrl
         * @memberof game.SUpgradeArea
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SUpgradeArea.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SUpgradeArea";
        };

        return SUpgradeArea;
    })();

    game.CClaimAreaUpgradeAward = (function() {

        /**
         * Properties of a CClaimAreaUpgradeAward.
         * @memberof game
         * @interface ICClaimAreaUpgradeAward
         * @property {number|null} [areaId] CClaimAreaUpgradeAward areaId
         * @property {number|null} [level] CClaimAreaUpgradeAward level
         */

        /**
         * Constructs a new CClaimAreaUpgradeAward.
         * @memberof game
         * @classdesc Represents a CClaimAreaUpgradeAward.
         * @implements ICClaimAreaUpgradeAward
         * @constructor
         * @param {game.ICClaimAreaUpgradeAward=} [properties] Properties to set
         */
        function CClaimAreaUpgradeAward(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CClaimAreaUpgradeAward areaId.
         * @member {number} areaId
         * @memberof game.CClaimAreaUpgradeAward
         * @instance
         */
        CClaimAreaUpgradeAward.prototype.areaId = 0;

        /**
         * CClaimAreaUpgradeAward level.
         * @member {number} level
         * @memberof game.CClaimAreaUpgradeAward
         * @instance
         */
        CClaimAreaUpgradeAward.prototype.level = 0;

        /**
         * Creates a new CClaimAreaUpgradeAward instance using the specified properties.
         * @function create
         * @memberof game.CClaimAreaUpgradeAward
         * @static
         * @param {game.ICClaimAreaUpgradeAward=} [properties] Properties to set
         * @returns {game.CClaimAreaUpgradeAward} CClaimAreaUpgradeAward instance
         */
        CClaimAreaUpgradeAward.create = function create(properties) {
            return new CClaimAreaUpgradeAward(properties);
        };

        /**
         * Encodes the specified CClaimAreaUpgradeAward message. Does not implicitly {@link game.CClaimAreaUpgradeAward.verify|verify} messages.
         * @function encode
         * @memberof game.CClaimAreaUpgradeAward
         * @static
         * @param {game.ICClaimAreaUpgradeAward} message CClaimAreaUpgradeAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CClaimAreaUpgradeAward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.areaId != null && Object.hasOwnProperty.call(message, "areaId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.areaId);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.level);
            return writer;
        };

        /**
         * Encodes the specified CClaimAreaUpgradeAward message, length delimited. Does not implicitly {@link game.CClaimAreaUpgradeAward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CClaimAreaUpgradeAward
         * @static
         * @param {game.ICClaimAreaUpgradeAward} message CClaimAreaUpgradeAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CClaimAreaUpgradeAward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CClaimAreaUpgradeAward message from the specified reader or buffer.
         * @function decode
         * @memberof game.CClaimAreaUpgradeAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CClaimAreaUpgradeAward} CClaimAreaUpgradeAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CClaimAreaUpgradeAward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CClaimAreaUpgradeAward();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.areaId = reader.int32();
                        break;
                    }
                case 2: {
                        message.level = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CClaimAreaUpgradeAward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CClaimAreaUpgradeAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CClaimAreaUpgradeAward} CClaimAreaUpgradeAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CClaimAreaUpgradeAward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CClaimAreaUpgradeAward message.
         * @function verify
         * @memberof game.CClaimAreaUpgradeAward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CClaimAreaUpgradeAward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.areaId != null && message.hasOwnProperty("areaId"))
                if (!$util.isInteger(message.areaId))
                    return "areaId: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            return null;
        };

        /**
         * Creates a CClaimAreaUpgradeAward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CClaimAreaUpgradeAward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CClaimAreaUpgradeAward} CClaimAreaUpgradeAward
         */
        CClaimAreaUpgradeAward.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CClaimAreaUpgradeAward)
                return object;
            var message = new $root.game.CClaimAreaUpgradeAward();
            if (object.areaId != null)
                message.areaId = object.areaId | 0;
            if (object.level != null)
                message.level = object.level | 0;
            return message;
        };

        /**
         * Creates a plain object from a CClaimAreaUpgradeAward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CClaimAreaUpgradeAward
         * @static
         * @param {game.CClaimAreaUpgradeAward} message CClaimAreaUpgradeAward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CClaimAreaUpgradeAward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.areaId = 0;
                object.level = 0;
            }
            if (message.areaId != null && message.hasOwnProperty("areaId"))
                object.areaId = message.areaId;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            return object;
        };

        /**
         * Converts this CClaimAreaUpgradeAward to JSON.
         * @function toJSON
         * @memberof game.CClaimAreaUpgradeAward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CClaimAreaUpgradeAward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CClaimAreaUpgradeAward
         * @function getTypeUrl
         * @memberof game.CClaimAreaUpgradeAward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CClaimAreaUpgradeAward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CClaimAreaUpgradeAward";
        };

        return CClaimAreaUpgradeAward;
    })();

    game.SClaimAreaUpgradeAward = (function() {

        /**
         * Properties of a SClaimAreaUpgradeAward.
         * @memberof game
         * @interface ISClaimAreaUpgradeAward
         * @property {Array.<common.IReward>|null} [awards] SClaimAreaUpgradeAward awards
         */

        /**
         * Constructs a new SClaimAreaUpgradeAward.
         * @memberof game
         * @classdesc Represents a SClaimAreaUpgradeAward.
         * @implements ISClaimAreaUpgradeAward
         * @constructor
         * @param {game.ISClaimAreaUpgradeAward=} [properties] Properties to set
         */
        function SClaimAreaUpgradeAward(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SClaimAreaUpgradeAward awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SClaimAreaUpgradeAward
         * @instance
         */
        SClaimAreaUpgradeAward.prototype.awards = $util.emptyArray;

        /**
         * Creates a new SClaimAreaUpgradeAward instance using the specified properties.
         * @function create
         * @memberof game.SClaimAreaUpgradeAward
         * @static
         * @param {game.ISClaimAreaUpgradeAward=} [properties] Properties to set
         * @returns {game.SClaimAreaUpgradeAward} SClaimAreaUpgradeAward instance
         */
        SClaimAreaUpgradeAward.create = function create(properties) {
            return new SClaimAreaUpgradeAward(properties);
        };

        /**
         * Encodes the specified SClaimAreaUpgradeAward message. Does not implicitly {@link game.SClaimAreaUpgradeAward.verify|verify} messages.
         * @function encode
         * @memberof game.SClaimAreaUpgradeAward
         * @static
         * @param {game.ISClaimAreaUpgradeAward} message SClaimAreaUpgradeAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SClaimAreaUpgradeAward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SClaimAreaUpgradeAward message, length delimited. Does not implicitly {@link game.SClaimAreaUpgradeAward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SClaimAreaUpgradeAward
         * @static
         * @param {game.ISClaimAreaUpgradeAward} message SClaimAreaUpgradeAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SClaimAreaUpgradeAward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SClaimAreaUpgradeAward message from the specified reader or buffer.
         * @function decode
         * @memberof game.SClaimAreaUpgradeAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SClaimAreaUpgradeAward} SClaimAreaUpgradeAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SClaimAreaUpgradeAward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SClaimAreaUpgradeAward();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SClaimAreaUpgradeAward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SClaimAreaUpgradeAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SClaimAreaUpgradeAward} SClaimAreaUpgradeAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SClaimAreaUpgradeAward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SClaimAreaUpgradeAward message.
         * @function verify
         * @memberof game.SClaimAreaUpgradeAward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SClaimAreaUpgradeAward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SClaimAreaUpgradeAward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SClaimAreaUpgradeAward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SClaimAreaUpgradeAward} SClaimAreaUpgradeAward
         */
        SClaimAreaUpgradeAward.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SClaimAreaUpgradeAward)
                return object;
            var message = new $root.game.SClaimAreaUpgradeAward();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SClaimAreaUpgradeAward.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SClaimAreaUpgradeAward.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SClaimAreaUpgradeAward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SClaimAreaUpgradeAward
         * @static
         * @param {game.SClaimAreaUpgradeAward} message SClaimAreaUpgradeAward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SClaimAreaUpgradeAward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            return object;
        };

        /**
         * Converts this SClaimAreaUpgradeAward to JSON.
         * @function toJSON
         * @memberof game.SClaimAreaUpgradeAward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SClaimAreaUpgradeAward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SClaimAreaUpgradeAward
         * @function getTypeUrl
         * @memberof game.SClaimAreaUpgradeAward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SClaimAreaUpgradeAward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SClaimAreaUpgradeAward";
        };

        return SClaimAreaUpgradeAward;
    })();

    game.CUseBagItem = (function() {

        /**
         * Properties of a CUseBagItem.
         * @memberof game
         * @interface ICUseBagItem
         * @property {number|null} [id] CUseBagItem id
         * @property {number|null} [num] CUseBagItem num
         */

        /**
         * Constructs a new CUseBagItem.
         * @memberof game
         * @classdesc Represents a CUseBagItem.
         * @implements ICUseBagItem
         * @constructor
         * @param {game.ICUseBagItem=} [properties] Properties to set
         */
        function CUseBagItem(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CUseBagItem id.
         * @member {number} id
         * @memberof game.CUseBagItem
         * @instance
         */
        CUseBagItem.prototype.id = 0;

        /**
         * CUseBagItem num.
         * @member {number} num
         * @memberof game.CUseBagItem
         * @instance
         */
        CUseBagItem.prototype.num = 0;

        /**
         * Creates a new CUseBagItem instance using the specified properties.
         * @function create
         * @memberof game.CUseBagItem
         * @static
         * @param {game.ICUseBagItem=} [properties] Properties to set
         * @returns {game.CUseBagItem} CUseBagItem instance
         */
        CUseBagItem.create = function create(properties) {
            return new CUseBagItem(properties);
        };

        /**
         * Encodes the specified CUseBagItem message. Does not implicitly {@link game.CUseBagItem.verify|verify} messages.
         * @function encode
         * @memberof game.CUseBagItem
         * @static
         * @param {game.ICUseBagItem} message CUseBagItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CUseBagItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.num != null && Object.hasOwnProperty.call(message, "num"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.num);
            return writer;
        };

        /**
         * Encodes the specified CUseBagItem message, length delimited. Does not implicitly {@link game.CUseBagItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CUseBagItem
         * @static
         * @param {game.ICUseBagItem} message CUseBagItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CUseBagItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CUseBagItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.CUseBagItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CUseBagItem} CUseBagItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CUseBagItem.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CUseBagItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.num = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CUseBagItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CUseBagItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CUseBagItem} CUseBagItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CUseBagItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CUseBagItem message.
         * @function verify
         * @memberof game.CUseBagItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CUseBagItem.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.num != null && message.hasOwnProperty("num"))
                if (!$util.isInteger(message.num))
                    return "num: integer expected";
            return null;
        };

        /**
         * Creates a CUseBagItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CUseBagItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CUseBagItem} CUseBagItem
         */
        CUseBagItem.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CUseBagItem)
                return object;
            var message = new $root.game.CUseBagItem();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.num != null)
                message.num = object.num | 0;
            return message;
        };

        /**
         * Creates a plain object from a CUseBagItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CUseBagItem
         * @static
         * @param {game.CUseBagItem} message CUseBagItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CUseBagItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.num = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.num != null && message.hasOwnProperty("num"))
                object.num = message.num;
            return object;
        };

        /**
         * Converts this CUseBagItem to JSON.
         * @function toJSON
         * @memberof game.CUseBagItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CUseBagItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CUseBagItem
         * @function getTypeUrl
         * @memberof game.CUseBagItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CUseBagItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CUseBagItem";
        };

        return CUseBagItem;
    })();

    game.SUseBagItem = (function() {

        /**
         * Properties of a SUseBagItem.
         * @memberof game
         * @interface ISUseBagItem
         * @property {Array.<common.IReward>|null} [awards] SUseBagItem awards
         * @property {Array.<common.IReward>|null} [costs] SUseBagItem costs
         */

        /**
         * Constructs a new SUseBagItem.
         * @memberof game
         * @classdesc Represents a SUseBagItem.
         * @implements ISUseBagItem
         * @constructor
         * @param {game.ISUseBagItem=} [properties] Properties to set
         */
        function SUseBagItem(properties) {
            this.awards = [];
            this.costs = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SUseBagItem awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SUseBagItem
         * @instance
         */
        SUseBagItem.prototype.awards = $util.emptyArray;

        /**
         * SUseBagItem costs.
         * @member {Array.<common.IReward>} costs
         * @memberof game.SUseBagItem
         * @instance
         */
        SUseBagItem.prototype.costs = $util.emptyArray;

        /**
         * Creates a new SUseBagItem instance using the specified properties.
         * @function create
         * @memberof game.SUseBagItem
         * @static
         * @param {game.ISUseBagItem=} [properties] Properties to set
         * @returns {game.SUseBagItem} SUseBagItem instance
         */
        SUseBagItem.create = function create(properties) {
            return new SUseBagItem(properties);
        };

        /**
         * Encodes the specified SUseBagItem message. Does not implicitly {@link game.SUseBagItem.verify|verify} messages.
         * @function encode
         * @memberof game.SUseBagItem
         * @static
         * @param {game.ISUseBagItem} message SUseBagItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SUseBagItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.costs != null && message.costs.length)
                for (var i = 0; i < message.costs.length; ++i)
                    $root.common.Reward.encode(message.costs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SUseBagItem message, length delimited. Does not implicitly {@link game.SUseBagItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SUseBagItem
         * @static
         * @param {game.ISUseBagItem} message SUseBagItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SUseBagItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SUseBagItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.SUseBagItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SUseBagItem} SUseBagItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SUseBagItem.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SUseBagItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.costs && message.costs.length))
                            message.costs = [];
                        message.costs.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SUseBagItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SUseBagItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SUseBagItem} SUseBagItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SUseBagItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SUseBagItem message.
         * @function verify
         * @memberof game.SUseBagItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SUseBagItem.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            if (message.costs != null && message.hasOwnProperty("costs")) {
                if (!Array.isArray(message.costs))
                    return "costs: array expected";
                for (var i = 0; i < message.costs.length; ++i) {
                    var error = $root.common.Reward.verify(message.costs[i]);
                    if (error)
                        return "costs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SUseBagItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SUseBagItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SUseBagItem} SUseBagItem
         */
        SUseBagItem.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SUseBagItem)
                return object;
            var message = new $root.game.SUseBagItem();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SUseBagItem.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SUseBagItem.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            if (object.costs) {
                if (!Array.isArray(object.costs))
                    throw TypeError(".game.SUseBagItem.costs: array expected");
                message.costs = [];
                for (var i = 0; i < object.costs.length; ++i) {
                    if (typeof object.costs[i] !== "object")
                        throw TypeError(".game.SUseBagItem.costs: object expected");
                    message.costs[i] = $root.common.Reward.fromObject(object.costs[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SUseBagItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SUseBagItem
         * @static
         * @param {game.SUseBagItem} message SUseBagItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SUseBagItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.awards = [];
                object.costs = [];
            }
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            if (message.costs && message.costs.length) {
                object.costs = [];
                for (var j = 0; j < message.costs.length; ++j)
                    object.costs[j] = $root.common.Reward.toObject(message.costs[j], options);
            }
            return object;
        };

        /**
         * Converts this SUseBagItem to JSON.
         * @function toJSON
         * @memberof game.SUseBagItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SUseBagItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SUseBagItem
         * @function getTypeUrl
         * @memberof game.SUseBagItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SUseBagItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SUseBagItem";
        };

        return SUseBagItem;
    })();

    game.CSellBagItem = (function() {

        /**
         * Properties of a CSellBagItem.
         * @memberof game
         * @interface ICSellBagItem
         * @property {number|null} [id] CSellBagItem id
         * @property {number|null} [num] CSellBagItem num
         */

        /**
         * Constructs a new CSellBagItem.
         * @memberof game
         * @classdesc Represents a CSellBagItem.
         * @implements ICSellBagItem
         * @constructor
         * @param {game.ICSellBagItem=} [properties] Properties to set
         */
        function CSellBagItem(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSellBagItem id.
         * @member {number} id
         * @memberof game.CSellBagItem
         * @instance
         */
        CSellBagItem.prototype.id = 0;

        /**
         * CSellBagItem num.
         * @member {number} num
         * @memberof game.CSellBagItem
         * @instance
         */
        CSellBagItem.prototype.num = 0;

        /**
         * Creates a new CSellBagItem instance using the specified properties.
         * @function create
         * @memberof game.CSellBagItem
         * @static
         * @param {game.ICSellBagItem=} [properties] Properties to set
         * @returns {game.CSellBagItem} CSellBagItem instance
         */
        CSellBagItem.create = function create(properties) {
            return new CSellBagItem(properties);
        };

        /**
         * Encodes the specified CSellBagItem message. Does not implicitly {@link game.CSellBagItem.verify|verify} messages.
         * @function encode
         * @memberof game.CSellBagItem
         * @static
         * @param {game.ICSellBagItem} message CSellBagItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSellBagItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.num != null && Object.hasOwnProperty.call(message, "num"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.num);
            return writer;
        };

        /**
         * Encodes the specified CSellBagItem message, length delimited. Does not implicitly {@link game.CSellBagItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CSellBagItem
         * @static
         * @param {game.ICSellBagItem} message CSellBagItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSellBagItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSellBagItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.CSellBagItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CSellBagItem} CSellBagItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSellBagItem.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CSellBagItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.num = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSellBagItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CSellBagItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CSellBagItem} CSellBagItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSellBagItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSellBagItem message.
         * @function verify
         * @memberof game.CSellBagItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSellBagItem.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.num != null && message.hasOwnProperty("num"))
                if (!$util.isInteger(message.num))
                    return "num: integer expected";
            return null;
        };

        /**
         * Creates a CSellBagItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CSellBagItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CSellBagItem} CSellBagItem
         */
        CSellBagItem.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CSellBagItem)
                return object;
            var message = new $root.game.CSellBagItem();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.num != null)
                message.num = object.num | 0;
            return message;
        };

        /**
         * Creates a plain object from a CSellBagItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CSellBagItem
         * @static
         * @param {game.CSellBagItem} message CSellBagItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSellBagItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.num = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.num != null && message.hasOwnProperty("num"))
                object.num = message.num;
            return object;
        };

        /**
         * Converts this CSellBagItem to JSON.
         * @function toJSON
         * @memberof game.CSellBagItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSellBagItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSellBagItem
         * @function getTypeUrl
         * @memberof game.CSellBagItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSellBagItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CSellBagItem";
        };

        return CSellBagItem;
    })();

    game.SSellBagItem = (function() {

        /**
         * Properties of a SSellBagItem.
         * @memberof game
         * @interface ISSellBagItem
         * @property {Array.<common.IReward>|null} [awards] SSellBagItem awards
         * @property {Array.<common.IReward>|null} [costs] SSellBagItem costs
         */

        /**
         * Constructs a new SSellBagItem.
         * @memberof game
         * @classdesc Represents a SSellBagItem.
         * @implements ISSellBagItem
         * @constructor
         * @param {game.ISSellBagItem=} [properties] Properties to set
         */
        function SSellBagItem(properties) {
            this.awards = [];
            this.costs = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SSellBagItem awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SSellBagItem
         * @instance
         */
        SSellBagItem.prototype.awards = $util.emptyArray;

        /**
         * SSellBagItem costs.
         * @member {Array.<common.IReward>} costs
         * @memberof game.SSellBagItem
         * @instance
         */
        SSellBagItem.prototype.costs = $util.emptyArray;

        /**
         * Creates a new SSellBagItem instance using the specified properties.
         * @function create
         * @memberof game.SSellBagItem
         * @static
         * @param {game.ISSellBagItem=} [properties] Properties to set
         * @returns {game.SSellBagItem} SSellBagItem instance
         */
        SSellBagItem.create = function create(properties) {
            return new SSellBagItem(properties);
        };

        /**
         * Encodes the specified SSellBagItem message. Does not implicitly {@link game.SSellBagItem.verify|verify} messages.
         * @function encode
         * @memberof game.SSellBagItem
         * @static
         * @param {game.ISSellBagItem} message SSellBagItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSellBagItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.costs != null && message.costs.length)
                for (var i = 0; i < message.costs.length; ++i)
                    $root.common.Reward.encode(message.costs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SSellBagItem message, length delimited. Does not implicitly {@link game.SSellBagItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SSellBagItem
         * @static
         * @param {game.ISSellBagItem} message SSellBagItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSellBagItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SSellBagItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.SSellBagItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SSellBagItem} SSellBagItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSellBagItem.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SSellBagItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.costs && message.costs.length))
                            message.costs = [];
                        message.costs.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SSellBagItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SSellBagItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SSellBagItem} SSellBagItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSellBagItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SSellBagItem message.
         * @function verify
         * @memberof game.SSellBagItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SSellBagItem.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            if (message.costs != null && message.hasOwnProperty("costs")) {
                if (!Array.isArray(message.costs))
                    return "costs: array expected";
                for (var i = 0; i < message.costs.length; ++i) {
                    var error = $root.common.Reward.verify(message.costs[i]);
                    if (error)
                        return "costs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SSellBagItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SSellBagItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SSellBagItem} SSellBagItem
         */
        SSellBagItem.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SSellBagItem)
                return object;
            var message = new $root.game.SSellBagItem();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SSellBagItem.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SSellBagItem.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            if (object.costs) {
                if (!Array.isArray(object.costs))
                    throw TypeError(".game.SSellBagItem.costs: array expected");
                message.costs = [];
                for (var i = 0; i < object.costs.length; ++i) {
                    if (typeof object.costs[i] !== "object")
                        throw TypeError(".game.SSellBagItem.costs: object expected");
                    message.costs[i] = $root.common.Reward.fromObject(object.costs[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SSellBagItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SSellBagItem
         * @static
         * @param {game.SSellBagItem} message SSellBagItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SSellBagItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.awards = [];
                object.costs = [];
            }
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            if (message.costs && message.costs.length) {
                object.costs = [];
                for (var j = 0; j < message.costs.length; ++j)
                    object.costs[j] = $root.common.Reward.toObject(message.costs[j], options);
            }
            return object;
        };

        /**
         * Converts this SSellBagItem to JSON.
         * @function toJSON
         * @memberof game.SSellBagItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SSellBagItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SSellBagItem
         * @function getTypeUrl
         * @memberof game.SSellBagItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SSellBagItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SSellBagItem";
        };

        return SSellBagItem;
    })();

    game.CExtendEquipBag = (function() {

        /**
         * Properties of a CExtendEquipBag.
         * @memberof game
         * @interface ICExtendEquipBag
         */

        /**
         * Constructs a new CExtendEquipBag.
         * @memberof game
         * @classdesc Represents a CExtendEquipBag.
         * @implements ICExtendEquipBag
         * @constructor
         * @param {game.ICExtendEquipBag=} [properties] Properties to set
         */
        function CExtendEquipBag(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CExtendEquipBag instance using the specified properties.
         * @function create
         * @memberof game.CExtendEquipBag
         * @static
         * @param {game.ICExtendEquipBag=} [properties] Properties to set
         * @returns {game.CExtendEquipBag} CExtendEquipBag instance
         */
        CExtendEquipBag.create = function create(properties) {
            return new CExtendEquipBag(properties);
        };

        /**
         * Encodes the specified CExtendEquipBag message. Does not implicitly {@link game.CExtendEquipBag.verify|verify} messages.
         * @function encode
         * @memberof game.CExtendEquipBag
         * @static
         * @param {game.ICExtendEquipBag} message CExtendEquipBag message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CExtendEquipBag.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CExtendEquipBag message, length delimited. Does not implicitly {@link game.CExtendEquipBag.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CExtendEquipBag
         * @static
         * @param {game.ICExtendEquipBag} message CExtendEquipBag message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CExtendEquipBag.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CExtendEquipBag message from the specified reader or buffer.
         * @function decode
         * @memberof game.CExtendEquipBag
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CExtendEquipBag} CExtendEquipBag
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CExtendEquipBag.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CExtendEquipBag();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CExtendEquipBag message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CExtendEquipBag
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CExtendEquipBag} CExtendEquipBag
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CExtendEquipBag.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CExtendEquipBag message.
         * @function verify
         * @memberof game.CExtendEquipBag
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CExtendEquipBag.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CExtendEquipBag message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CExtendEquipBag
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CExtendEquipBag} CExtendEquipBag
         */
        CExtendEquipBag.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CExtendEquipBag)
                return object;
            return new $root.game.CExtendEquipBag();
        };

        /**
         * Creates a plain object from a CExtendEquipBag message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CExtendEquipBag
         * @static
         * @param {game.CExtendEquipBag} message CExtendEquipBag
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CExtendEquipBag.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CExtendEquipBag to JSON.
         * @function toJSON
         * @memberof game.CExtendEquipBag
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CExtendEquipBag.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CExtendEquipBag
         * @function getTypeUrl
         * @memberof game.CExtendEquipBag
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CExtendEquipBag.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CExtendEquipBag";
        };

        return CExtendEquipBag;
    })();

    game.SExtendEquipBag = (function() {

        /**
         * Properties of a SExtendEquipBag.
         * @memberof game
         * @interface ISExtendEquipBag
         * @property {Array.<common.IReward>|null} [costs] SExtendEquipBag costs
         */

        /**
         * Constructs a new SExtendEquipBag.
         * @memberof game
         * @classdesc Represents a SExtendEquipBag.
         * @implements ISExtendEquipBag
         * @constructor
         * @param {game.ISExtendEquipBag=} [properties] Properties to set
         */
        function SExtendEquipBag(properties) {
            this.costs = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SExtendEquipBag costs.
         * @member {Array.<common.IReward>} costs
         * @memberof game.SExtendEquipBag
         * @instance
         */
        SExtendEquipBag.prototype.costs = $util.emptyArray;

        /**
         * Creates a new SExtendEquipBag instance using the specified properties.
         * @function create
         * @memberof game.SExtendEquipBag
         * @static
         * @param {game.ISExtendEquipBag=} [properties] Properties to set
         * @returns {game.SExtendEquipBag} SExtendEquipBag instance
         */
        SExtendEquipBag.create = function create(properties) {
            return new SExtendEquipBag(properties);
        };

        /**
         * Encodes the specified SExtendEquipBag message. Does not implicitly {@link game.SExtendEquipBag.verify|verify} messages.
         * @function encode
         * @memberof game.SExtendEquipBag
         * @static
         * @param {game.ISExtendEquipBag} message SExtendEquipBag message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SExtendEquipBag.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.costs != null && message.costs.length)
                for (var i = 0; i < message.costs.length; ++i)
                    $root.common.Reward.encode(message.costs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SExtendEquipBag message, length delimited. Does not implicitly {@link game.SExtendEquipBag.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SExtendEquipBag
         * @static
         * @param {game.ISExtendEquipBag} message SExtendEquipBag message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SExtendEquipBag.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SExtendEquipBag message from the specified reader or buffer.
         * @function decode
         * @memberof game.SExtendEquipBag
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SExtendEquipBag} SExtendEquipBag
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SExtendEquipBag.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SExtendEquipBag();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.costs && message.costs.length))
                            message.costs = [];
                        message.costs.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SExtendEquipBag message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SExtendEquipBag
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SExtendEquipBag} SExtendEquipBag
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SExtendEquipBag.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SExtendEquipBag message.
         * @function verify
         * @memberof game.SExtendEquipBag
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SExtendEquipBag.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.costs != null && message.hasOwnProperty("costs")) {
                if (!Array.isArray(message.costs))
                    return "costs: array expected";
                for (var i = 0; i < message.costs.length; ++i) {
                    var error = $root.common.Reward.verify(message.costs[i]);
                    if (error)
                        return "costs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SExtendEquipBag message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SExtendEquipBag
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SExtendEquipBag} SExtendEquipBag
         */
        SExtendEquipBag.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SExtendEquipBag)
                return object;
            var message = new $root.game.SExtendEquipBag();
            if (object.costs) {
                if (!Array.isArray(object.costs))
                    throw TypeError(".game.SExtendEquipBag.costs: array expected");
                message.costs = [];
                for (var i = 0; i < object.costs.length; ++i) {
                    if (typeof object.costs[i] !== "object")
                        throw TypeError(".game.SExtendEquipBag.costs: object expected");
                    message.costs[i] = $root.common.Reward.fromObject(object.costs[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SExtendEquipBag message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SExtendEquipBag
         * @static
         * @param {game.SExtendEquipBag} message SExtendEquipBag
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SExtendEquipBag.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.costs = [];
            if (message.costs && message.costs.length) {
                object.costs = [];
                for (var j = 0; j < message.costs.length; ++j)
                    object.costs[j] = $root.common.Reward.toObject(message.costs[j], options);
            }
            return object;
        };

        /**
         * Converts this SExtendEquipBag to JSON.
         * @function toJSON
         * @memberof game.SExtendEquipBag
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SExtendEquipBag.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SExtendEquipBag
         * @function getTypeUrl
         * @memberof game.SExtendEquipBag
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SExtendEquipBag.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SExtendEquipBag";
        };

        return SExtendEquipBag;
    })();

    game.CDecomposeEquip = (function() {

        /**
         * Properties of a CDecomposeEquip.
         * @memberof game
         * @interface ICDecomposeEquip
         * @property {Array.<number|Long>|null} [equipIds] CDecomposeEquip equipIds
         * @property {number|null} [quality] CDecomposeEquip quality
         */

        /**
         * Constructs a new CDecomposeEquip.
         * @memberof game
         * @classdesc Represents a CDecomposeEquip.
         * @implements ICDecomposeEquip
         * @constructor
         * @param {game.ICDecomposeEquip=} [properties] Properties to set
         */
        function CDecomposeEquip(properties) {
            this.equipIds = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDecomposeEquip equipIds.
         * @member {Array.<number|Long>} equipIds
         * @memberof game.CDecomposeEquip
         * @instance
         */
        CDecomposeEquip.prototype.equipIds = $util.emptyArray;

        /**
         * CDecomposeEquip quality.
         * @member {number} quality
         * @memberof game.CDecomposeEquip
         * @instance
         */
        CDecomposeEquip.prototype.quality = 0;

        /**
         * Creates a new CDecomposeEquip instance using the specified properties.
         * @function create
         * @memberof game.CDecomposeEquip
         * @static
         * @param {game.ICDecomposeEquip=} [properties] Properties to set
         * @returns {game.CDecomposeEquip} CDecomposeEquip instance
         */
        CDecomposeEquip.create = function create(properties) {
            return new CDecomposeEquip(properties);
        };

        /**
         * Encodes the specified CDecomposeEquip message. Does not implicitly {@link game.CDecomposeEquip.verify|verify} messages.
         * @function encode
         * @memberof game.CDecomposeEquip
         * @static
         * @param {game.ICDecomposeEquip} message CDecomposeEquip message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDecomposeEquip.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.equipIds != null && message.equipIds.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (var i = 0; i < message.equipIds.length; ++i)
                    writer.int64(message.equipIds[i]);
                writer.ldelim();
            }
            if (message.quality != null && Object.hasOwnProperty.call(message, "quality"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.quality);
            return writer;
        };

        /**
         * Encodes the specified CDecomposeEquip message, length delimited. Does not implicitly {@link game.CDecomposeEquip.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDecomposeEquip
         * @static
         * @param {game.ICDecomposeEquip} message CDecomposeEquip message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDecomposeEquip.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDecomposeEquip message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDecomposeEquip
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDecomposeEquip} CDecomposeEquip
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDecomposeEquip.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDecomposeEquip();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.equipIds && message.equipIds.length))
                            message.equipIds = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.equipIds.push(reader.int64());
                        } else
                            message.equipIds.push(reader.int64());
                        break;
                    }
                case 2: {
                        message.quality = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDecomposeEquip message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDecomposeEquip
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDecomposeEquip} CDecomposeEquip
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDecomposeEquip.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDecomposeEquip message.
         * @function verify
         * @memberof game.CDecomposeEquip
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDecomposeEquip.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.equipIds != null && message.hasOwnProperty("equipIds")) {
                if (!Array.isArray(message.equipIds))
                    return "equipIds: array expected";
                for (var i = 0; i < message.equipIds.length; ++i)
                    if (!$util.isInteger(message.equipIds[i]) && !(message.equipIds[i] && $util.isInteger(message.equipIds[i].low) && $util.isInteger(message.equipIds[i].high)))
                        return "equipIds: integer|Long[] expected";
            }
            if (message.quality != null && message.hasOwnProperty("quality"))
                if (!$util.isInteger(message.quality))
                    return "quality: integer expected";
            return null;
        };

        /**
         * Creates a CDecomposeEquip message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDecomposeEquip
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDecomposeEquip} CDecomposeEquip
         */
        CDecomposeEquip.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDecomposeEquip)
                return object;
            var message = new $root.game.CDecomposeEquip();
            if (object.equipIds) {
                if (!Array.isArray(object.equipIds))
                    throw TypeError(".game.CDecomposeEquip.equipIds: array expected");
                message.equipIds = [];
                for (var i = 0; i < object.equipIds.length; ++i)
                    if ($util.Long)
                        (message.equipIds[i] = $util.Long.fromValue(object.equipIds[i])).unsigned = false;
                    else if (typeof object.equipIds[i] === "string")
                        message.equipIds[i] = parseInt(object.equipIds[i], 10);
                    else if (typeof object.equipIds[i] === "number")
                        message.equipIds[i] = object.equipIds[i];
                    else if (typeof object.equipIds[i] === "object")
                        message.equipIds[i] = new $util.LongBits(object.equipIds[i].low >>> 0, object.equipIds[i].high >>> 0).toNumber();
            }
            if (object.quality != null)
                message.quality = object.quality | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDecomposeEquip message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDecomposeEquip
         * @static
         * @param {game.CDecomposeEquip} message CDecomposeEquip
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDecomposeEquip.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.equipIds = [];
            if (options.defaults)
                object.quality = 0;
            if (message.equipIds && message.equipIds.length) {
                object.equipIds = [];
                for (var j = 0; j < message.equipIds.length; ++j)
                    if (typeof message.equipIds[j] === "number")
                        object.equipIds[j] = options.longs === String ? String(message.equipIds[j]) : message.equipIds[j];
                    else
                        object.equipIds[j] = options.longs === String ? $util.Long.prototype.toString.call(message.equipIds[j]) : options.longs === Number ? new $util.LongBits(message.equipIds[j].low >>> 0, message.equipIds[j].high >>> 0).toNumber() : message.equipIds[j];
            }
            if (message.quality != null && message.hasOwnProperty("quality"))
                object.quality = message.quality;
            return object;
        };

        /**
         * Converts this CDecomposeEquip to JSON.
         * @function toJSON
         * @memberof game.CDecomposeEquip
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDecomposeEquip.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDecomposeEquip
         * @function getTypeUrl
         * @memberof game.CDecomposeEquip
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDecomposeEquip.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDecomposeEquip";
        };

        return CDecomposeEquip;
    })();

    game.SDecomposeEquip = (function() {

        /**
         * Properties of a SDecomposeEquip.
         * @memberof game
         * @interface ISDecomposeEquip
         * @property {Array.<common.IReward>|null} [awards] SDecomposeEquip awards
         * @property {Array.<common.IReward>|null} [costs] SDecomposeEquip costs
         */

        /**
         * Constructs a new SDecomposeEquip.
         * @memberof game
         * @classdesc Represents a SDecomposeEquip.
         * @implements ISDecomposeEquip
         * @constructor
         * @param {game.ISDecomposeEquip=} [properties] Properties to set
         */
        function SDecomposeEquip(properties) {
            this.awards = [];
            this.costs = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SDecomposeEquip awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SDecomposeEquip
         * @instance
         */
        SDecomposeEquip.prototype.awards = $util.emptyArray;

        /**
         * SDecomposeEquip costs.
         * @member {Array.<common.IReward>} costs
         * @memberof game.SDecomposeEquip
         * @instance
         */
        SDecomposeEquip.prototype.costs = $util.emptyArray;

        /**
         * Creates a new SDecomposeEquip instance using the specified properties.
         * @function create
         * @memberof game.SDecomposeEquip
         * @static
         * @param {game.ISDecomposeEquip=} [properties] Properties to set
         * @returns {game.SDecomposeEquip} SDecomposeEquip instance
         */
        SDecomposeEquip.create = function create(properties) {
            return new SDecomposeEquip(properties);
        };

        /**
         * Encodes the specified SDecomposeEquip message. Does not implicitly {@link game.SDecomposeEquip.verify|verify} messages.
         * @function encode
         * @memberof game.SDecomposeEquip
         * @static
         * @param {game.ISDecomposeEquip} message SDecomposeEquip message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDecomposeEquip.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.costs != null && message.costs.length)
                for (var i = 0; i < message.costs.length; ++i)
                    $root.common.Reward.encode(message.costs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SDecomposeEquip message, length delimited. Does not implicitly {@link game.SDecomposeEquip.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDecomposeEquip
         * @static
         * @param {game.ISDecomposeEquip} message SDecomposeEquip message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDecomposeEquip.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDecomposeEquip message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDecomposeEquip
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDecomposeEquip} SDecomposeEquip
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDecomposeEquip.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDecomposeEquip();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.costs && message.costs.length))
                            message.costs = [];
                        message.costs.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDecomposeEquip message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDecomposeEquip
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDecomposeEquip} SDecomposeEquip
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDecomposeEquip.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDecomposeEquip message.
         * @function verify
         * @memberof game.SDecomposeEquip
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDecomposeEquip.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            if (message.costs != null && message.hasOwnProperty("costs")) {
                if (!Array.isArray(message.costs))
                    return "costs: array expected";
                for (var i = 0; i < message.costs.length; ++i) {
                    var error = $root.common.Reward.verify(message.costs[i]);
                    if (error)
                        return "costs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SDecomposeEquip message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDecomposeEquip
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDecomposeEquip} SDecomposeEquip
         */
        SDecomposeEquip.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDecomposeEquip)
                return object;
            var message = new $root.game.SDecomposeEquip();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SDecomposeEquip.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SDecomposeEquip.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            if (object.costs) {
                if (!Array.isArray(object.costs))
                    throw TypeError(".game.SDecomposeEquip.costs: array expected");
                message.costs = [];
                for (var i = 0; i < object.costs.length; ++i) {
                    if (typeof object.costs[i] !== "object")
                        throw TypeError(".game.SDecomposeEquip.costs: object expected");
                    message.costs[i] = $root.common.Reward.fromObject(object.costs[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SDecomposeEquip message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDecomposeEquip
         * @static
         * @param {game.SDecomposeEquip} message SDecomposeEquip
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDecomposeEquip.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.awards = [];
                object.costs = [];
            }
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            if (message.costs && message.costs.length) {
                object.costs = [];
                for (var j = 0; j < message.costs.length; ++j)
                    object.costs[j] = $root.common.Reward.toObject(message.costs[j], options);
            }
            return object;
        };

        /**
         * Converts this SDecomposeEquip to JSON.
         * @function toJSON
         * @memberof game.SDecomposeEquip
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDecomposeEquip.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDecomposeEquip
         * @function getTypeUrl
         * @memberof game.SDecomposeEquip
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDecomposeEquip.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDecomposeEquip";
        };

        return SDecomposeEquip;
    })();

    game.CEcho = (function() {

        /**
         * Properties of a CEcho.
         * @memberof game
         * @interface ICEcho
         * @property {string|null} [text] CEcho text
         */

        /**
         * Constructs a new CEcho.
         * @memberof game
         * @classdesc Represents a CEcho.
         * @implements ICEcho
         * @constructor
         * @param {game.ICEcho=} [properties] Properties to set
         */
        function CEcho(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CEcho text.
         * @member {string} text
         * @memberof game.CEcho
         * @instance
         */
        CEcho.prototype.text = "";

        /**
         * Creates a new CEcho instance using the specified properties.
         * @function create
         * @memberof game.CEcho
         * @static
         * @param {game.ICEcho=} [properties] Properties to set
         * @returns {game.CEcho} CEcho instance
         */
        CEcho.create = function create(properties) {
            return new CEcho(properties);
        };

        /**
         * Encodes the specified CEcho message. Does not implicitly {@link game.CEcho.verify|verify} messages.
         * @function encode
         * @memberof game.CEcho
         * @static
         * @param {game.ICEcho} message CEcho message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CEcho.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.text);
            return writer;
        };

        /**
         * Encodes the specified CEcho message, length delimited. Does not implicitly {@link game.CEcho.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CEcho
         * @static
         * @param {game.ICEcho} message CEcho message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CEcho.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CEcho message from the specified reader or buffer.
         * @function decode
         * @memberof game.CEcho
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CEcho} CEcho
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CEcho.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CEcho();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.text = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CEcho message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CEcho
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CEcho} CEcho
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CEcho.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CEcho message.
         * @function verify
         * @memberof game.CEcho
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CEcho.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.text != null && message.hasOwnProperty("text"))
                if (!$util.isString(message.text))
                    return "text: string expected";
            return null;
        };

        /**
         * Creates a CEcho message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CEcho
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CEcho} CEcho
         */
        CEcho.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CEcho)
                return object;
            var message = new $root.game.CEcho();
            if (object.text != null)
                message.text = String(object.text);
            return message;
        };

        /**
         * Creates a plain object from a CEcho message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CEcho
         * @static
         * @param {game.CEcho} message CEcho
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CEcho.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.text = "";
            if (message.text != null && message.hasOwnProperty("text"))
                object.text = message.text;
            return object;
        };

        /**
         * Converts this CEcho to JSON.
         * @function toJSON
         * @memberof game.CEcho
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CEcho.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CEcho
         * @function getTypeUrl
         * @memberof game.CEcho
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CEcho.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CEcho";
        };

        return CEcho;
    })();

    game.SEcho = (function() {

        /**
         * Properties of a SEcho.
         * @memberof game
         * @interface ISEcho
         * @property {string|null} [text] SEcho text
         */

        /**
         * Constructs a new SEcho.
         * @memberof game
         * @classdesc Represents a SEcho.
         * @implements ISEcho
         * @constructor
         * @param {game.ISEcho=} [properties] Properties to set
         */
        function SEcho(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SEcho text.
         * @member {string} text
         * @memberof game.SEcho
         * @instance
         */
        SEcho.prototype.text = "";

        /**
         * Creates a new SEcho instance using the specified properties.
         * @function create
         * @memberof game.SEcho
         * @static
         * @param {game.ISEcho=} [properties] Properties to set
         * @returns {game.SEcho} SEcho instance
         */
        SEcho.create = function create(properties) {
            return new SEcho(properties);
        };

        /**
         * Encodes the specified SEcho message. Does not implicitly {@link game.SEcho.verify|verify} messages.
         * @function encode
         * @memberof game.SEcho
         * @static
         * @param {game.ISEcho} message SEcho message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SEcho.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.text);
            return writer;
        };

        /**
         * Encodes the specified SEcho message, length delimited. Does not implicitly {@link game.SEcho.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SEcho
         * @static
         * @param {game.ISEcho} message SEcho message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SEcho.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SEcho message from the specified reader or buffer.
         * @function decode
         * @memberof game.SEcho
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SEcho} SEcho
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SEcho.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SEcho();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.text = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SEcho message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SEcho
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SEcho} SEcho
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SEcho.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SEcho message.
         * @function verify
         * @memberof game.SEcho
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SEcho.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.text != null && message.hasOwnProperty("text"))
                if (!$util.isString(message.text))
                    return "text: string expected";
            return null;
        };

        /**
         * Creates a SEcho message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SEcho
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SEcho} SEcho
         */
        SEcho.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SEcho)
                return object;
            var message = new $root.game.SEcho();
            if (object.text != null)
                message.text = String(object.text);
            return message;
        };

        /**
         * Creates a plain object from a SEcho message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SEcho
         * @static
         * @param {game.SEcho} message SEcho
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SEcho.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.text = "";
            if (message.text != null && message.hasOwnProperty("text"))
                object.text = message.text;
            return object;
        };

        /**
         * Converts this SEcho to JSON.
         * @function toJSON
         * @memberof game.SEcho
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SEcho.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SEcho
         * @function getTypeUrl
         * @memberof game.SEcho
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SEcho.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SEcho";
        };

        return SEcho;
    })();

    game.CDebugAddEquip = (function() {

        /**
         * Properties of a CDebugAddEquip.
         * @memberof game
         * @interface ICDebugAddEquip
         * @property {number|null} [id] CDebugAddEquip id
         * @property {number|null} [num] CDebugAddEquip num
         */

        /**
         * Constructs a new CDebugAddEquip.
         * @memberof game
         * @classdesc Represents a CDebugAddEquip.
         * @implements ICDebugAddEquip
         * @constructor
         * @param {game.ICDebugAddEquip=} [properties] Properties to set
         */
        function CDebugAddEquip(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddEquip id.
         * @member {number} id
         * @memberof game.CDebugAddEquip
         * @instance
         */
        CDebugAddEquip.prototype.id = 0;

        /**
         * CDebugAddEquip num.
         * @member {number} num
         * @memberof game.CDebugAddEquip
         * @instance
         */
        CDebugAddEquip.prototype.num = 0;

        /**
         * Creates a new CDebugAddEquip instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddEquip
         * @static
         * @param {game.ICDebugAddEquip=} [properties] Properties to set
         * @returns {game.CDebugAddEquip} CDebugAddEquip instance
         */
        CDebugAddEquip.create = function create(properties) {
            return new CDebugAddEquip(properties);
        };

        /**
         * Encodes the specified CDebugAddEquip message. Does not implicitly {@link game.CDebugAddEquip.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddEquip
         * @static
         * @param {game.ICDebugAddEquip} message CDebugAddEquip message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddEquip.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.num != null && Object.hasOwnProperty.call(message, "num"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.num);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddEquip message, length delimited. Does not implicitly {@link game.CDebugAddEquip.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddEquip
         * @static
         * @param {game.ICDebugAddEquip} message CDebugAddEquip message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddEquip.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddEquip message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddEquip
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddEquip} CDebugAddEquip
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddEquip.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddEquip();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.num = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddEquip message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddEquip
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddEquip} CDebugAddEquip
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddEquip.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddEquip message.
         * @function verify
         * @memberof game.CDebugAddEquip
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddEquip.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.num != null && message.hasOwnProperty("num"))
                if (!$util.isInteger(message.num))
                    return "num: integer expected";
            return null;
        };

        /**
         * Creates a CDebugAddEquip message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddEquip
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddEquip} CDebugAddEquip
         */
        CDebugAddEquip.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddEquip)
                return object;
            var message = new $root.game.CDebugAddEquip();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.num != null)
                message.num = object.num | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddEquip message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddEquip
         * @static
         * @param {game.CDebugAddEquip} message CDebugAddEquip
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddEquip.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.num = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.num != null && message.hasOwnProperty("num"))
                object.num = message.num;
            return object;
        };

        /**
         * Converts this CDebugAddEquip to JSON.
         * @function toJSON
         * @memberof game.CDebugAddEquip
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddEquip.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddEquip
         * @function getTypeUrl
         * @memberof game.CDebugAddEquip
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddEquip.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddEquip";
        };

        return CDebugAddEquip;
    })();

    game.SDebugAddEquip = (function() {

        /**
         * Properties of a SDebugAddEquip.
         * @memberof game
         * @interface ISDebugAddEquip
         * @property {Array.<common.IReward>|null} [awards] SDebugAddEquip awards
         */

        /**
         * Constructs a new SDebugAddEquip.
         * @memberof game
         * @classdesc Represents a SDebugAddEquip.
         * @implements ISDebugAddEquip
         * @constructor
         * @param {game.ISDebugAddEquip=} [properties] Properties to set
         */
        function SDebugAddEquip(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SDebugAddEquip awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SDebugAddEquip
         * @instance
         */
        SDebugAddEquip.prototype.awards = $util.emptyArray;

        /**
         * Creates a new SDebugAddEquip instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddEquip
         * @static
         * @param {game.ISDebugAddEquip=} [properties] Properties to set
         * @returns {game.SDebugAddEquip} SDebugAddEquip instance
         */
        SDebugAddEquip.create = function create(properties) {
            return new SDebugAddEquip(properties);
        };

        /**
         * Encodes the specified SDebugAddEquip message. Does not implicitly {@link game.SDebugAddEquip.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddEquip
         * @static
         * @param {game.ISDebugAddEquip} message SDebugAddEquip message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddEquip.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddEquip message, length delimited. Does not implicitly {@link game.SDebugAddEquip.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddEquip
         * @static
         * @param {game.ISDebugAddEquip} message SDebugAddEquip message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddEquip.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddEquip message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddEquip
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddEquip} SDebugAddEquip
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddEquip.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddEquip();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddEquip message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddEquip
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddEquip} SDebugAddEquip
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddEquip.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddEquip message.
         * @function verify
         * @memberof game.SDebugAddEquip
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddEquip.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SDebugAddEquip message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddEquip
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddEquip} SDebugAddEquip
         */
        SDebugAddEquip.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddEquip)
                return object;
            var message = new $root.game.SDebugAddEquip();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SDebugAddEquip.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SDebugAddEquip.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SDebugAddEquip message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddEquip
         * @static
         * @param {game.SDebugAddEquip} message SDebugAddEquip
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddEquip.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            return object;
        };

        /**
         * Converts this SDebugAddEquip to JSON.
         * @function toJSON
         * @memberof game.SDebugAddEquip
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddEquip.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddEquip
         * @function getTypeUrl
         * @memberof game.SDebugAddEquip
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddEquip.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddEquip";
        };

        return SDebugAddEquip;
    })();

    game.CDebugAddItem = (function() {

        /**
         * Properties of a CDebugAddItem.
         * @memberof game
         * @interface ICDebugAddItem
         * @property {number|null} [id] CDebugAddItem id
         * @property {number|null} [num] CDebugAddItem num
         */

        /**
         * Constructs a new CDebugAddItem.
         * @memberof game
         * @classdesc Represents a CDebugAddItem.
         * @implements ICDebugAddItem
         * @constructor
         * @param {game.ICDebugAddItem=} [properties] Properties to set
         */
        function CDebugAddItem(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddItem id.
         * @member {number} id
         * @memberof game.CDebugAddItem
         * @instance
         */
        CDebugAddItem.prototype.id = 0;

        /**
         * CDebugAddItem num.
         * @member {number} num
         * @memberof game.CDebugAddItem
         * @instance
         */
        CDebugAddItem.prototype.num = 0;

        /**
         * Creates a new CDebugAddItem instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddItem
         * @static
         * @param {game.ICDebugAddItem=} [properties] Properties to set
         * @returns {game.CDebugAddItem} CDebugAddItem instance
         */
        CDebugAddItem.create = function create(properties) {
            return new CDebugAddItem(properties);
        };

        /**
         * Encodes the specified CDebugAddItem message. Does not implicitly {@link game.CDebugAddItem.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddItem
         * @static
         * @param {game.ICDebugAddItem} message CDebugAddItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.num != null && Object.hasOwnProperty.call(message, "num"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.num);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddItem message, length delimited. Does not implicitly {@link game.CDebugAddItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddItem
         * @static
         * @param {game.ICDebugAddItem} message CDebugAddItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddItem} CDebugAddItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddItem.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.num = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddItem} CDebugAddItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddItem message.
         * @function verify
         * @memberof game.CDebugAddItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddItem.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.num != null && message.hasOwnProperty("num"))
                if (!$util.isInteger(message.num))
                    return "num: integer expected";
            return null;
        };

        /**
         * Creates a CDebugAddItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddItem} CDebugAddItem
         */
        CDebugAddItem.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddItem)
                return object;
            var message = new $root.game.CDebugAddItem();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.num != null)
                message.num = object.num | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddItem
         * @static
         * @param {game.CDebugAddItem} message CDebugAddItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.num = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.num != null && message.hasOwnProperty("num"))
                object.num = message.num;
            return object;
        };

        /**
         * Converts this CDebugAddItem to JSON.
         * @function toJSON
         * @memberof game.CDebugAddItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddItem
         * @function getTypeUrl
         * @memberof game.CDebugAddItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddItem";
        };

        return CDebugAddItem;
    })();

    game.SDebugAddItem = (function() {

        /**
         * Properties of a SDebugAddItem.
         * @memberof game
         * @interface ISDebugAddItem
         * @property {Array.<common.IReward>|null} [awards] SDebugAddItem awards
         */

        /**
         * Constructs a new SDebugAddItem.
         * @memberof game
         * @classdesc Represents a SDebugAddItem.
         * @implements ISDebugAddItem
         * @constructor
         * @param {game.ISDebugAddItem=} [properties] Properties to set
         */
        function SDebugAddItem(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SDebugAddItem awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SDebugAddItem
         * @instance
         */
        SDebugAddItem.prototype.awards = $util.emptyArray;

        /**
         * Creates a new SDebugAddItem instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddItem
         * @static
         * @param {game.ISDebugAddItem=} [properties] Properties to set
         * @returns {game.SDebugAddItem} SDebugAddItem instance
         */
        SDebugAddItem.create = function create(properties) {
            return new SDebugAddItem(properties);
        };

        /**
         * Encodes the specified SDebugAddItem message. Does not implicitly {@link game.SDebugAddItem.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddItem
         * @static
         * @param {game.ISDebugAddItem} message SDebugAddItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddItem message, length delimited. Does not implicitly {@link game.SDebugAddItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddItem
         * @static
         * @param {game.ISDebugAddItem} message SDebugAddItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddItem} SDebugAddItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddItem.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddItem} SDebugAddItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddItem message.
         * @function verify
         * @memberof game.SDebugAddItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddItem.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SDebugAddItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddItem} SDebugAddItem
         */
        SDebugAddItem.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddItem)
                return object;
            var message = new $root.game.SDebugAddItem();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SDebugAddItem.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SDebugAddItem.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SDebugAddItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddItem
         * @static
         * @param {game.SDebugAddItem} message SDebugAddItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            return object;
        };

        /**
         * Converts this SDebugAddItem to JSON.
         * @function toJSON
         * @memberof game.SDebugAddItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddItem
         * @function getTypeUrl
         * @memberof game.SDebugAddItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddItem";
        };

        return SDebugAddItem;
    })();

    game.CDebugAddCard = (function() {

        /**
         * Properties of a CDebugAddCard.
         * @memberof game
         * @interface ICDebugAddCard
         * @property {number|null} [id] CDebugAddCard id
         * @property {number|null} [num] CDebugAddCard num
         * @property {boolean|null} [tradable] CDebugAddCard tradable
         */

        /**
         * Constructs a new CDebugAddCard.
         * @memberof game
         * @classdesc Represents a CDebugAddCard.
         * @implements ICDebugAddCard
         * @constructor
         * @param {game.ICDebugAddCard=} [properties] Properties to set
         */
        function CDebugAddCard(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddCard id.
         * @member {number} id
         * @memberof game.CDebugAddCard
         * @instance
         */
        CDebugAddCard.prototype.id = 0;

        /**
         * CDebugAddCard num.
         * @member {number} num
         * @memberof game.CDebugAddCard
         * @instance
         */
        CDebugAddCard.prototype.num = 0;

        /**
         * CDebugAddCard tradable.
         * @member {boolean} tradable
         * @memberof game.CDebugAddCard
         * @instance
         */
        CDebugAddCard.prototype.tradable = false;

        /**
         * Creates a new CDebugAddCard instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddCard
         * @static
         * @param {game.ICDebugAddCard=} [properties] Properties to set
         * @returns {game.CDebugAddCard} CDebugAddCard instance
         */
        CDebugAddCard.create = function create(properties) {
            return new CDebugAddCard(properties);
        };

        /**
         * Encodes the specified CDebugAddCard message. Does not implicitly {@link game.CDebugAddCard.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddCard
         * @static
         * @param {game.ICDebugAddCard} message CDebugAddCard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddCard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.num != null && Object.hasOwnProperty.call(message, "num"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.num);
            if (message.tradable != null && Object.hasOwnProperty.call(message, "tradable"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.tradable);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddCard message, length delimited. Does not implicitly {@link game.CDebugAddCard.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddCard
         * @static
         * @param {game.ICDebugAddCard} message CDebugAddCard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddCard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddCard message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddCard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddCard} CDebugAddCard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddCard.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddCard();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.num = reader.int32();
                        break;
                    }
                case 3: {
                        message.tradable = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddCard message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddCard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddCard} CDebugAddCard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddCard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddCard message.
         * @function verify
         * @memberof game.CDebugAddCard
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddCard.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.num != null && message.hasOwnProperty("num"))
                if (!$util.isInteger(message.num))
                    return "num: integer expected";
            if (message.tradable != null && message.hasOwnProperty("tradable"))
                if (typeof message.tradable !== "boolean")
                    return "tradable: boolean expected";
            return null;
        };

        /**
         * Creates a CDebugAddCard message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddCard
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddCard} CDebugAddCard
         */
        CDebugAddCard.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddCard)
                return object;
            var message = new $root.game.CDebugAddCard();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.num != null)
                message.num = object.num | 0;
            if (object.tradable != null)
                message.tradable = Boolean(object.tradable);
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddCard message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddCard
         * @static
         * @param {game.CDebugAddCard} message CDebugAddCard
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddCard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.num = 0;
                object.tradable = false;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.num != null && message.hasOwnProperty("num"))
                object.num = message.num;
            if (message.tradable != null && message.hasOwnProperty("tradable"))
                object.tradable = message.tradable;
            return object;
        };

        /**
         * Converts this CDebugAddCard to JSON.
         * @function toJSON
         * @memberof game.CDebugAddCard
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddCard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddCard
         * @function getTypeUrl
         * @memberof game.CDebugAddCard
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddCard.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddCard";
        };

        return CDebugAddCard;
    })();

    game.SDebugAddCard = (function() {

        /**
         * Properties of a SDebugAddCard.
         * @memberof game
         * @interface ISDebugAddCard
         * @property {Array.<common.IReward>|null} [awards] SDebugAddCard awards
         */

        /**
         * Constructs a new SDebugAddCard.
         * @memberof game
         * @classdesc Represents a SDebugAddCard.
         * @implements ISDebugAddCard
         * @constructor
         * @param {game.ISDebugAddCard=} [properties] Properties to set
         */
        function SDebugAddCard(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SDebugAddCard awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SDebugAddCard
         * @instance
         */
        SDebugAddCard.prototype.awards = $util.emptyArray;

        /**
         * Creates a new SDebugAddCard instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddCard
         * @static
         * @param {game.ISDebugAddCard=} [properties] Properties to set
         * @returns {game.SDebugAddCard} SDebugAddCard instance
         */
        SDebugAddCard.create = function create(properties) {
            return new SDebugAddCard(properties);
        };

        /**
         * Encodes the specified SDebugAddCard message. Does not implicitly {@link game.SDebugAddCard.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddCard
         * @static
         * @param {game.ISDebugAddCard} message SDebugAddCard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddCard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddCard message, length delimited. Does not implicitly {@link game.SDebugAddCard.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddCard
         * @static
         * @param {game.ISDebugAddCard} message SDebugAddCard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddCard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddCard message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddCard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddCard} SDebugAddCard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddCard.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddCard();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddCard message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddCard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddCard} SDebugAddCard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddCard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddCard message.
         * @function verify
         * @memberof game.SDebugAddCard
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddCard.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SDebugAddCard message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddCard
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddCard} SDebugAddCard
         */
        SDebugAddCard.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddCard)
                return object;
            var message = new $root.game.SDebugAddCard();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SDebugAddCard.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SDebugAddCard.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SDebugAddCard message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddCard
         * @static
         * @param {game.SDebugAddCard} message SDebugAddCard
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddCard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            return object;
        };

        /**
         * Converts this SDebugAddCard to JSON.
         * @function toJSON
         * @memberof game.SDebugAddCard
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddCard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddCard
         * @function getTypeUrl
         * @memberof game.SDebugAddCard
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddCard.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddCard";
        };

        return SDebugAddCard;
    })();

    game.CDebugAddCups = (function() {

        /**
         * Properties of a CDebugAddCups.
         * @memberof game
         * @interface ICDebugAddCups
         * @property {number|null} [cups] CDebugAddCups cups
         */

        /**
         * Constructs a new CDebugAddCups.
         * @memberof game
         * @classdesc Represents a CDebugAddCups.
         * @implements ICDebugAddCups
         * @constructor
         * @param {game.ICDebugAddCups=} [properties] Properties to set
         */
        function CDebugAddCups(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddCups cups.
         * @member {number} cups
         * @memberof game.CDebugAddCups
         * @instance
         */
        CDebugAddCups.prototype.cups = 0;

        /**
         * Creates a new CDebugAddCups instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddCups
         * @static
         * @param {game.ICDebugAddCups=} [properties] Properties to set
         * @returns {game.CDebugAddCups} CDebugAddCups instance
         */
        CDebugAddCups.create = function create(properties) {
            return new CDebugAddCups(properties);
        };

        /**
         * Encodes the specified CDebugAddCups message. Does not implicitly {@link game.CDebugAddCups.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddCups
         * @static
         * @param {game.ICDebugAddCups} message CDebugAddCups message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddCups.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.cups != null && Object.hasOwnProperty.call(message, "cups"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.cups);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddCups message, length delimited. Does not implicitly {@link game.CDebugAddCups.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddCups
         * @static
         * @param {game.ICDebugAddCups} message CDebugAddCups message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddCups.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddCups message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddCups
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddCups} CDebugAddCups
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddCups.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddCups();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.cups = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddCups message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddCups
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddCups} CDebugAddCups
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddCups.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddCups message.
         * @function verify
         * @memberof game.CDebugAddCups
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddCups.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.cups != null && message.hasOwnProperty("cups"))
                if (!$util.isInteger(message.cups))
                    return "cups: integer expected";
            return null;
        };

        /**
         * Creates a CDebugAddCups message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddCups
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddCups} CDebugAddCups
         */
        CDebugAddCups.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddCups)
                return object;
            var message = new $root.game.CDebugAddCups();
            if (object.cups != null)
                message.cups = object.cups | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddCups message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddCups
         * @static
         * @param {game.CDebugAddCups} message CDebugAddCups
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddCups.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.cups = 0;
            if (message.cups != null && message.hasOwnProperty("cups"))
                object.cups = message.cups;
            return object;
        };

        /**
         * Converts this CDebugAddCups to JSON.
         * @function toJSON
         * @memberof game.CDebugAddCups
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddCups.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddCups
         * @function getTypeUrl
         * @memberof game.CDebugAddCups
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddCups.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddCups";
        };

        return CDebugAddCups;
    })();

    game.SDebugAddCups = (function() {

        /**
         * Properties of a SDebugAddCups.
         * @memberof game
         * @interface ISDebugAddCups
         */

        /**
         * Constructs a new SDebugAddCups.
         * @memberof game
         * @classdesc Represents a SDebugAddCups.
         * @implements ISDebugAddCups
         * @constructor
         * @param {game.ISDebugAddCups=} [properties] Properties to set
         */
        function SDebugAddCups(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SDebugAddCups instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddCups
         * @static
         * @param {game.ISDebugAddCups=} [properties] Properties to set
         * @returns {game.SDebugAddCups} SDebugAddCups instance
         */
        SDebugAddCups.create = function create(properties) {
            return new SDebugAddCups(properties);
        };

        /**
         * Encodes the specified SDebugAddCups message. Does not implicitly {@link game.SDebugAddCups.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddCups
         * @static
         * @param {game.ISDebugAddCups} message SDebugAddCups message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddCups.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddCups message, length delimited. Does not implicitly {@link game.SDebugAddCups.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddCups
         * @static
         * @param {game.ISDebugAddCups} message SDebugAddCups message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddCups.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddCups message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddCups
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddCups} SDebugAddCups
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddCups.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddCups();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddCups message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddCups
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddCups} SDebugAddCups
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddCups.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddCups message.
         * @function verify
         * @memberof game.SDebugAddCups
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddCups.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SDebugAddCups message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddCups
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddCups} SDebugAddCups
         */
        SDebugAddCups.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddCups)
                return object;
            return new $root.game.SDebugAddCups();
        };

        /**
         * Creates a plain object from a SDebugAddCups message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddCups
         * @static
         * @param {game.SDebugAddCups} message SDebugAddCups
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddCups.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SDebugAddCups to JSON.
         * @function toJSON
         * @memberof game.SDebugAddCups
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddCups.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddCups
         * @function getTypeUrl
         * @memberof game.SDebugAddCups
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddCups.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddCups";
        };

        return SDebugAddCups;
    })();

    game.CDebugAddExp = (function() {

        /**
         * Properties of a CDebugAddExp.
         * @memberof game
         * @interface ICDebugAddExp
         * @property {number|null} [exp] CDebugAddExp exp
         */

        /**
         * Constructs a new CDebugAddExp.
         * @memberof game
         * @classdesc Represents a CDebugAddExp.
         * @implements ICDebugAddExp
         * @constructor
         * @param {game.ICDebugAddExp=} [properties] Properties to set
         */
        function CDebugAddExp(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddExp exp.
         * @member {number} exp
         * @memberof game.CDebugAddExp
         * @instance
         */
        CDebugAddExp.prototype.exp = 0;

        /**
         * Creates a new CDebugAddExp instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddExp
         * @static
         * @param {game.ICDebugAddExp=} [properties] Properties to set
         * @returns {game.CDebugAddExp} CDebugAddExp instance
         */
        CDebugAddExp.create = function create(properties) {
            return new CDebugAddExp(properties);
        };

        /**
         * Encodes the specified CDebugAddExp message. Does not implicitly {@link game.CDebugAddExp.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddExp
         * @static
         * @param {game.ICDebugAddExp} message CDebugAddExp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddExp.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.exp != null && Object.hasOwnProperty.call(message, "exp"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.exp);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddExp message, length delimited. Does not implicitly {@link game.CDebugAddExp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddExp
         * @static
         * @param {game.ICDebugAddExp} message CDebugAddExp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddExp.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddExp message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddExp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddExp} CDebugAddExp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddExp.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddExp();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.exp = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddExp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddExp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddExp} CDebugAddExp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddExp.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddExp message.
         * @function verify
         * @memberof game.CDebugAddExp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddExp.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.exp != null && message.hasOwnProperty("exp"))
                if (!$util.isInteger(message.exp))
                    return "exp: integer expected";
            return null;
        };

        /**
         * Creates a CDebugAddExp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddExp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddExp} CDebugAddExp
         */
        CDebugAddExp.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddExp)
                return object;
            var message = new $root.game.CDebugAddExp();
            if (object.exp != null)
                message.exp = object.exp | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddExp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddExp
         * @static
         * @param {game.CDebugAddExp} message CDebugAddExp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddExp.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.exp = 0;
            if (message.exp != null && message.hasOwnProperty("exp"))
                object.exp = message.exp;
            return object;
        };

        /**
         * Converts this CDebugAddExp to JSON.
         * @function toJSON
         * @memberof game.CDebugAddExp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddExp.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddExp
         * @function getTypeUrl
         * @memberof game.CDebugAddExp
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddExp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddExp";
        };

        return CDebugAddExp;
    })();

    game.SDebugAddExp = (function() {

        /**
         * Properties of a SDebugAddExp.
         * @memberof game
         * @interface ISDebugAddExp
         */

        /**
         * Constructs a new SDebugAddExp.
         * @memberof game
         * @classdesc Represents a SDebugAddExp.
         * @implements ISDebugAddExp
         * @constructor
         * @param {game.ISDebugAddExp=} [properties] Properties to set
         */
        function SDebugAddExp(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SDebugAddExp instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddExp
         * @static
         * @param {game.ISDebugAddExp=} [properties] Properties to set
         * @returns {game.SDebugAddExp} SDebugAddExp instance
         */
        SDebugAddExp.create = function create(properties) {
            return new SDebugAddExp(properties);
        };

        /**
         * Encodes the specified SDebugAddExp message. Does not implicitly {@link game.SDebugAddExp.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddExp
         * @static
         * @param {game.ISDebugAddExp} message SDebugAddExp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddExp.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddExp message, length delimited. Does not implicitly {@link game.SDebugAddExp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddExp
         * @static
         * @param {game.ISDebugAddExp} message SDebugAddExp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddExp.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddExp message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddExp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddExp} SDebugAddExp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddExp.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddExp();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddExp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddExp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddExp} SDebugAddExp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddExp.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddExp message.
         * @function verify
         * @memberof game.SDebugAddExp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddExp.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SDebugAddExp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddExp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddExp} SDebugAddExp
         */
        SDebugAddExp.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddExp)
                return object;
            return new $root.game.SDebugAddExp();
        };

        /**
         * Creates a plain object from a SDebugAddExp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddExp
         * @static
         * @param {game.SDebugAddExp} message SDebugAddExp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddExp.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SDebugAddExp to JSON.
         * @function toJSON
         * @memberof game.SDebugAddExp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddExp.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddExp
         * @function getTypeUrl
         * @memberof game.SDebugAddExp
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddExp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddExp";
        };

        return SDebugAddExp;
    })();

    game.CDebugAddCash = (function() {

        /**
         * Properties of a CDebugAddCash.
         * @memberof game
         * @interface ICDebugAddCash
         * @property {number|null} [val] CDebugAddCash val
         */

        /**
         * Constructs a new CDebugAddCash.
         * @memberof game
         * @classdesc Represents a CDebugAddCash.
         * @implements ICDebugAddCash
         * @constructor
         * @param {game.ICDebugAddCash=} [properties] Properties to set
         */
        function CDebugAddCash(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddCash val.
         * @member {number} val
         * @memberof game.CDebugAddCash
         * @instance
         */
        CDebugAddCash.prototype.val = 0;

        /**
         * Creates a new CDebugAddCash instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddCash
         * @static
         * @param {game.ICDebugAddCash=} [properties] Properties to set
         * @returns {game.CDebugAddCash} CDebugAddCash instance
         */
        CDebugAddCash.create = function create(properties) {
            return new CDebugAddCash(properties);
        };

        /**
         * Encodes the specified CDebugAddCash message. Does not implicitly {@link game.CDebugAddCash.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddCash
         * @static
         * @param {game.ICDebugAddCash} message CDebugAddCash message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddCash.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.val != null && Object.hasOwnProperty.call(message, "val"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.val);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddCash message, length delimited. Does not implicitly {@link game.CDebugAddCash.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddCash
         * @static
         * @param {game.ICDebugAddCash} message CDebugAddCash message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddCash.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddCash message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddCash
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddCash} CDebugAddCash
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddCash.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddCash();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.val = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddCash message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddCash
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddCash} CDebugAddCash
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddCash.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddCash message.
         * @function verify
         * @memberof game.CDebugAddCash
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddCash.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.val != null && message.hasOwnProperty("val"))
                if (!$util.isInteger(message.val))
                    return "val: integer expected";
            return null;
        };

        /**
         * Creates a CDebugAddCash message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddCash
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddCash} CDebugAddCash
         */
        CDebugAddCash.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddCash)
                return object;
            var message = new $root.game.CDebugAddCash();
            if (object.val != null)
                message.val = object.val | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddCash message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddCash
         * @static
         * @param {game.CDebugAddCash} message CDebugAddCash
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddCash.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.val = 0;
            if (message.val != null && message.hasOwnProperty("val"))
                object.val = message.val;
            return object;
        };

        /**
         * Converts this CDebugAddCash to JSON.
         * @function toJSON
         * @memberof game.CDebugAddCash
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddCash.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddCash
         * @function getTypeUrl
         * @memberof game.CDebugAddCash
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddCash.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddCash";
        };

        return CDebugAddCash;
    })();

    game.SDebugAddCash = (function() {

        /**
         * Properties of a SDebugAddCash.
         * @memberof game
         * @interface ISDebugAddCash
         */

        /**
         * Constructs a new SDebugAddCash.
         * @memberof game
         * @classdesc Represents a SDebugAddCash.
         * @implements ISDebugAddCash
         * @constructor
         * @param {game.ISDebugAddCash=} [properties] Properties to set
         */
        function SDebugAddCash(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SDebugAddCash instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddCash
         * @static
         * @param {game.ISDebugAddCash=} [properties] Properties to set
         * @returns {game.SDebugAddCash} SDebugAddCash instance
         */
        SDebugAddCash.create = function create(properties) {
            return new SDebugAddCash(properties);
        };

        /**
         * Encodes the specified SDebugAddCash message. Does not implicitly {@link game.SDebugAddCash.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddCash
         * @static
         * @param {game.ISDebugAddCash} message SDebugAddCash message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddCash.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddCash message, length delimited. Does not implicitly {@link game.SDebugAddCash.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddCash
         * @static
         * @param {game.ISDebugAddCash} message SDebugAddCash message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddCash.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddCash message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddCash
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddCash} SDebugAddCash
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddCash.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddCash();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddCash message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddCash
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddCash} SDebugAddCash
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddCash.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddCash message.
         * @function verify
         * @memberof game.SDebugAddCash
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddCash.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SDebugAddCash message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddCash
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddCash} SDebugAddCash
         */
        SDebugAddCash.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddCash)
                return object;
            return new $root.game.SDebugAddCash();
        };

        /**
         * Creates a plain object from a SDebugAddCash message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddCash
         * @static
         * @param {game.SDebugAddCash} message SDebugAddCash
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddCash.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SDebugAddCash to JSON.
         * @function toJSON
         * @memberof game.SDebugAddCash
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddCash.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddCash
         * @function getTypeUrl
         * @memberof game.SDebugAddCash
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddCash.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddCash";
        };

        return SDebugAddCash;
    })();

    game.CDebugAddBox = (function() {

        /**
         * Properties of a CDebugAddBox.
         * @memberof game
         * @interface ICDebugAddBox
         * @property {number|null} [boxId] CDebugAddBox boxId
         * @property {number|null} [num] CDebugAddBox num
         */

        /**
         * Constructs a new CDebugAddBox.
         * @memberof game
         * @classdesc Represents a CDebugAddBox.
         * @implements ICDebugAddBox
         * @constructor
         * @param {game.ICDebugAddBox=} [properties] Properties to set
         */
        function CDebugAddBox(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddBox boxId.
         * @member {number} boxId
         * @memberof game.CDebugAddBox
         * @instance
         */
        CDebugAddBox.prototype.boxId = 0;

        /**
         * CDebugAddBox num.
         * @member {number} num
         * @memberof game.CDebugAddBox
         * @instance
         */
        CDebugAddBox.prototype.num = 0;

        /**
         * Creates a new CDebugAddBox instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddBox
         * @static
         * @param {game.ICDebugAddBox=} [properties] Properties to set
         * @returns {game.CDebugAddBox} CDebugAddBox instance
         */
        CDebugAddBox.create = function create(properties) {
            return new CDebugAddBox(properties);
        };

        /**
         * Encodes the specified CDebugAddBox message. Does not implicitly {@link game.CDebugAddBox.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddBox
         * @static
         * @param {game.ICDebugAddBox} message CDebugAddBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddBox.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.boxId != null && Object.hasOwnProperty.call(message, "boxId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.boxId);
            if (message.num != null && Object.hasOwnProperty.call(message, "num"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.num);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddBox message, length delimited. Does not implicitly {@link game.CDebugAddBox.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddBox
         * @static
         * @param {game.ICDebugAddBox} message CDebugAddBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddBox.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddBox message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddBox} CDebugAddBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddBox.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddBox();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.boxId = reader.int32();
                        break;
                    }
                case 2: {
                        message.num = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddBox message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddBox} CDebugAddBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddBox.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddBox message.
         * @function verify
         * @memberof game.CDebugAddBox
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddBox.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.boxId != null && message.hasOwnProperty("boxId"))
                if (!$util.isInteger(message.boxId))
                    return "boxId: integer expected";
            if (message.num != null && message.hasOwnProperty("num"))
                if (!$util.isInteger(message.num))
                    return "num: integer expected";
            return null;
        };

        /**
         * Creates a CDebugAddBox message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddBox
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddBox} CDebugAddBox
         */
        CDebugAddBox.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddBox)
                return object;
            var message = new $root.game.CDebugAddBox();
            if (object.boxId != null)
                message.boxId = object.boxId | 0;
            if (object.num != null)
                message.num = object.num | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddBox message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddBox
         * @static
         * @param {game.CDebugAddBox} message CDebugAddBox
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddBox.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.boxId = 0;
                object.num = 0;
            }
            if (message.boxId != null && message.hasOwnProperty("boxId"))
                object.boxId = message.boxId;
            if (message.num != null && message.hasOwnProperty("num"))
                object.num = message.num;
            return object;
        };

        /**
         * Converts this CDebugAddBox to JSON.
         * @function toJSON
         * @memberof game.CDebugAddBox
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddBox.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddBox
         * @function getTypeUrl
         * @memberof game.CDebugAddBox
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddBox.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddBox";
        };

        return CDebugAddBox;
    })();

    game.SDebugAddBox = (function() {

        /**
         * Properties of a SDebugAddBox.
         * @memberof game
         * @interface ISDebugAddBox
         * @property {Array.<common.IReward>|null} [awards] SDebugAddBox awards
         */

        /**
         * Constructs a new SDebugAddBox.
         * @memberof game
         * @classdesc Represents a SDebugAddBox.
         * @implements ISDebugAddBox
         * @constructor
         * @param {game.ISDebugAddBox=} [properties] Properties to set
         */
        function SDebugAddBox(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SDebugAddBox awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SDebugAddBox
         * @instance
         */
        SDebugAddBox.prototype.awards = $util.emptyArray;

        /**
         * Creates a new SDebugAddBox instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddBox
         * @static
         * @param {game.ISDebugAddBox=} [properties] Properties to set
         * @returns {game.SDebugAddBox} SDebugAddBox instance
         */
        SDebugAddBox.create = function create(properties) {
            return new SDebugAddBox(properties);
        };

        /**
         * Encodes the specified SDebugAddBox message. Does not implicitly {@link game.SDebugAddBox.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddBox
         * @static
         * @param {game.ISDebugAddBox} message SDebugAddBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddBox.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddBox message, length delimited. Does not implicitly {@link game.SDebugAddBox.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddBox
         * @static
         * @param {game.ISDebugAddBox} message SDebugAddBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddBox.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddBox message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddBox} SDebugAddBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddBox.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddBox();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddBox message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddBox} SDebugAddBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddBox.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddBox message.
         * @function verify
         * @memberof game.SDebugAddBox
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddBox.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SDebugAddBox message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddBox
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddBox} SDebugAddBox
         */
        SDebugAddBox.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddBox)
                return object;
            var message = new $root.game.SDebugAddBox();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SDebugAddBox.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SDebugAddBox.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SDebugAddBox message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddBox
         * @static
         * @param {game.SDebugAddBox} message SDebugAddBox
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddBox.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            return object;
        };

        /**
         * Converts this SDebugAddBox to JSON.
         * @function toJSON
         * @memberof game.SDebugAddBox
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddBox.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddBox
         * @function getTypeUrl
         * @memberof game.SDebugAddBox
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddBox.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddBox";
        };

        return SDebugAddBox;
    })();

    game.CDebugAddPages = (function() {

        /**
         * Properties of a CDebugAddPages.
         * @memberof game
         * @interface ICDebugAddPages
         * @property {number|null} [pages] CDebugAddPages pages
         */

        /**
         * Constructs a new CDebugAddPages.
         * @memberof game
         * @classdesc Represents a CDebugAddPages.
         * @implements ICDebugAddPages
         * @constructor
         * @param {game.ICDebugAddPages=} [properties] Properties to set
         */
        function CDebugAddPages(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddPages pages.
         * @member {number} pages
         * @memberof game.CDebugAddPages
         * @instance
         */
        CDebugAddPages.prototype.pages = 0;

        /**
         * Creates a new CDebugAddPages instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddPages
         * @static
         * @param {game.ICDebugAddPages=} [properties] Properties to set
         * @returns {game.CDebugAddPages} CDebugAddPages instance
         */
        CDebugAddPages.create = function create(properties) {
            return new CDebugAddPages(properties);
        };

        /**
         * Encodes the specified CDebugAddPages message. Does not implicitly {@link game.CDebugAddPages.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddPages
         * @static
         * @param {game.ICDebugAddPages} message CDebugAddPages message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddPages.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.pages != null && Object.hasOwnProperty.call(message, "pages"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.pages);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddPages message, length delimited. Does not implicitly {@link game.CDebugAddPages.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddPages
         * @static
         * @param {game.ICDebugAddPages} message CDebugAddPages message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddPages.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddPages message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddPages
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddPages} CDebugAddPages
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddPages.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddPages();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.pages = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddPages message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddPages
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddPages} CDebugAddPages
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddPages.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddPages message.
         * @function verify
         * @memberof game.CDebugAddPages
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddPages.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.pages != null && message.hasOwnProperty("pages"))
                if (!$util.isInteger(message.pages))
                    return "pages: integer expected";
            return null;
        };

        /**
         * Creates a CDebugAddPages message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddPages
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddPages} CDebugAddPages
         */
        CDebugAddPages.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddPages)
                return object;
            var message = new $root.game.CDebugAddPages();
            if (object.pages != null)
                message.pages = object.pages | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddPages message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddPages
         * @static
         * @param {game.CDebugAddPages} message CDebugAddPages
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddPages.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.pages = 0;
            if (message.pages != null && message.hasOwnProperty("pages"))
                object.pages = message.pages;
            return object;
        };

        /**
         * Converts this CDebugAddPages to JSON.
         * @function toJSON
         * @memberof game.CDebugAddPages
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddPages.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddPages
         * @function getTypeUrl
         * @memberof game.CDebugAddPages
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddPages.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddPages";
        };

        return CDebugAddPages;
    })();

    game.SDebugAddPages = (function() {

        /**
         * Properties of a SDebugAddPages.
         * @memberof game
         * @interface ISDebugAddPages
         */

        /**
         * Constructs a new SDebugAddPages.
         * @memberof game
         * @classdesc Represents a SDebugAddPages.
         * @implements ISDebugAddPages
         * @constructor
         * @param {game.ISDebugAddPages=} [properties] Properties to set
         */
        function SDebugAddPages(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SDebugAddPages instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddPages
         * @static
         * @param {game.ISDebugAddPages=} [properties] Properties to set
         * @returns {game.SDebugAddPages} SDebugAddPages instance
         */
        SDebugAddPages.create = function create(properties) {
            return new SDebugAddPages(properties);
        };

        /**
         * Encodes the specified SDebugAddPages message. Does not implicitly {@link game.SDebugAddPages.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddPages
         * @static
         * @param {game.ISDebugAddPages} message SDebugAddPages message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddPages.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddPages message, length delimited. Does not implicitly {@link game.SDebugAddPages.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddPages
         * @static
         * @param {game.ISDebugAddPages} message SDebugAddPages message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddPages.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddPages message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddPages
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddPages} SDebugAddPages
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddPages.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddPages();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddPages message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddPages
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddPages} SDebugAddPages
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddPages.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddPages message.
         * @function verify
         * @memberof game.SDebugAddPages
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddPages.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SDebugAddPages message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddPages
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddPages} SDebugAddPages
         */
        SDebugAddPages.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddPages)
                return object;
            return new $root.game.SDebugAddPages();
        };

        /**
         * Creates a plain object from a SDebugAddPages message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddPages
         * @static
         * @param {game.SDebugAddPages} message SDebugAddPages
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddPages.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SDebugAddPages to JSON.
         * @function toJSON
         * @memberof game.SDebugAddPages
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddPages.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddPages
         * @function getTypeUrl
         * @memberof game.SDebugAddPages
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddPages.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddPages";
        };

        return SDebugAddPages;
    })();

    game.CDebugAddCandy = (function() {

        /**
         * Properties of a CDebugAddCandy.
         * @memberof game
         * @interface ICDebugAddCandy
         * @property {number|null} [val] CDebugAddCandy val
         */

        /**
         * Constructs a new CDebugAddCandy.
         * @memberof game
         * @classdesc Represents a CDebugAddCandy.
         * @implements ICDebugAddCandy
         * @constructor
         * @param {game.ICDebugAddCandy=} [properties] Properties to set
         */
        function CDebugAddCandy(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddCandy val.
         * @member {number} val
         * @memberof game.CDebugAddCandy
         * @instance
         */
        CDebugAddCandy.prototype.val = 0;

        /**
         * Creates a new CDebugAddCandy instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddCandy
         * @static
         * @param {game.ICDebugAddCandy=} [properties] Properties to set
         * @returns {game.CDebugAddCandy} CDebugAddCandy instance
         */
        CDebugAddCandy.create = function create(properties) {
            return new CDebugAddCandy(properties);
        };

        /**
         * Encodes the specified CDebugAddCandy message. Does not implicitly {@link game.CDebugAddCandy.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddCandy
         * @static
         * @param {game.ICDebugAddCandy} message CDebugAddCandy message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddCandy.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.val != null && Object.hasOwnProperty.call(message, "val"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.val);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddCandy message, length delimited. Does not implicitly {@link game.CDebugAddCandy.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddCandy
         * @static
         * @param {game.ICDebugAddCandy} message CDebugAddCandy message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddCandy.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddCandy message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddCandy
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddCandy} CDebugAddCandy
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddCandy.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddCandy();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.val = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddCandy message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddCandy
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddCandy} CDebugAddCandy
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddCandy.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddCandy message.
         * @function verify
         * @memberof game.CDebugAddCandy
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddCandy.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.val != null && message.hasOwnProperty("val"))
                if (!$util.isInteger(message.val))
                    return "val: integer expected";
            return null;
        };

        /**
         * Creates a CDebugAddCandy message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddCandy
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddCandy} CDebugAddCandy
         */
        CDebugAddCandy.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddCandy)
                return object;
            var message = new $root.game.CDebugAddCandy();
            if (object.val != null)
                message.val = object.val | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddCandy message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddCandy
         * @static
         * @param {game.CDebugAddCandy} message CDebugAddCandy
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddCandy.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.val = 0;
            if (message.val != null && message.hasOwnProperty("val"))
                object.val = message.val;
            return object;
        };

        /**
         * Converts this CDebugAddCandy to JSON.
         * @function toJSON
         * @memberof game.CDebugAddCandy
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddCandy.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddCandy
         * @function getTypeUrl
         * @memberof game.CDebugAddCandy
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddCandy.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddCandy";
        };

        return CDebugAddCandy;
    })();

    game.SDebugAddCandy = (function() {

        /**
         * Properties of a SDebugAddCandy.
         * @memberof game
         * @interface ISDebugAddCandy
         */

        /**
         * Constructs a new SDebugAddCandy.
         * @memberof game
         * @classdesc Represents a SDebugAddCandy.
         * @implements ISDebugAddCandy
         * @constructor
         * @param {game.ISDebugAddCandy=} [properties] Properties to set
         */
        function SDebugAddCandy(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SDebugAddCandy instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddCandy
         * @static
         * @param {game.ISDebugAddCandy=} [properties] Properties to set
         * @returns {game.SDebugAddCandy} SDebugAddCandy instance
         */
        SDebugAddCandy.create = function create(properties) {
            return new SDebugAddCandy(properties);
        };

        /**
         * Encodes the specified SDebugAddCandy message. Does not implicitly {@link game.SDebugAddCandy.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddCandy
         * @static
         * @param {game.ISDebugAddCandy} message SDebugAddCandy message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddCandy.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddCandy message, length delimited. Does not implicitly {@link game.SDebugAddCandy.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddCandy
         * @static
         * @param {game.ISDebugAddCandy} message SDebugAddCandy message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddCandy.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddCandy message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddCandy
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddCandy} SDebugAddCandy
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddCandy.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddCandy();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddCandy message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddCandy
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddCandy} SDebugAddCandy
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddCandy.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddCandy message.
         * @function verify
         * @memberof game.SDebugAddCandy
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddCandy.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SDebugAddCandy message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddCandy
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddCandy} SDebugAddCandy
         */
        SDebugAddCandy.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddCandy)
                return object;
            return new $root.game.SDebugAddCandy();
        };

        /**
         * Creates a plain object from a SDebugAddCandy message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddCandy
         * @static
         * @param {game.SDebugAddCandy} message SDebugAddCandy
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddCandy.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SDebugAddCandy to JSON.
         * @function toJSON
         * @memberof game.SDebugAddCandy
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddCandy.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddCandy
         * @function getTypeUrl
         * @memberof game.SDebugAddCandy
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddCandy.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddCandy";
        };

        return SDebugAddCandy;
    })();

    game.CDebugTriggerEvent = (function() {

        /**
         * Properties of a CDebugTriggerEvent.
         * @memberof game
         * @interface ICDebugTriggerEvent
         * @property {number|null} [id] CDebugTriggerEvent id
         * @property {number|null} [tool] CDebugTriggerEvent tool
         * @property {number|null} [teamId] CDebugTriggerEvent teamId
         */

        /**
         * Constructs a new CDebugTriggerEvent.
         * @memberof game
         * @classdesc Represents a CDebugTriggerEvent.
         * @implements ICDebugTriggerEvent
         * @constructor
         * @param {game.ICDebugTriggerEvent=} [properties] Properties to set
         */
        function CDebugTriggerEvent(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugTriggerEvent id.
         * @member {number} id
         * @memberof game.CDebugTriggerEvent
         * @instance
         */
        CDebugTriggerEvent.prototype.id = 0;

        /**
         * CDebugTriggerEvent tool.
         * @member {number} tool
         * @memberof game.CDebugTriggerEvent
         * @instance
         */
        CDebugTriggerEvent.prototype.tool = 0;

        /**
         * CDebugTriggerEvent teamId.
         * @member {number} teamId
         * @memberof game.CDebugTriggerEvent
         * @instance
         */
        CDebugTriggerEvent.prototype.teamId = 0;

        /**
         * Creates a new CDebugTriggerEvent instance using the specified properties.
         * @function create
         * @memberof game.CDebugTriggerEvent
         * @static
         * @param {game.ICDebugTriggerEvent=} [properties] Properties to set
         * @returns {game.CDebugTriggerEvent} CDebugTriggerEvent instance
         */
        CDebugTriggerEvent.create = function create(properties) {
            return new CDebugTriggerEvent(properties);
        };

        /**
         * Encodes the specified CDebugTriggerEvent message. Does not implicitly {@link game.CDebugTriggerEvent.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugTriggerEvent
         * @static
         * @param {game.ICDebugTriggerEvent} message CDebugTriggerEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugTriggerEvent.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.tool != null && Object.hasOwnProperty.call(message, "tool"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.tool);
            if (message.teamId != null && Object.hasOwnProperty.call(message, "teamId"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.teamId);
            return writer;
        };

        /**
         * Encodes the specified CDebugTriggerEvent message, length delimited. Does not implicitly {@link game.CDebugTriggerEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugTriggerEvent
         * @static
         * @param {game.ICDebugTriggerEvent} message CDebugTriggerEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugTriggerEvent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugTriggerEvent message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugTriggerEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugTriggerEvent} CDebugTriggerEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugTriggerEvent.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugTriggerEvent();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.tool = reader.int32();
                        break;
                    }
                case 3: {
                        message.teamId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugTriggerEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugTriggerEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugTriggerEvent} CDebugTriggerEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugTriggerEvent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugTriggerEvent message.
         * @function verify
         * @memberof game.CDebugTriggerEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugTriggerEvent.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.tool != null && message.hasOwnProperty("tool"))
                if (!$util.isInteger(message.tool))
                    return "tool: integer expected";
            if (message.teamId != null && message.hasOwnProperty("teamId"))
                if (!$util.isInteger(message.teamId))
                    return "teamId: integer expected";
            return null;
        };

        /**
         * Creates a CDebugTriggerEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugTriggerEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugTriggerEvent} CDebugTriggerEvent
         */
        CDebugTriggerEvent.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugTriggerEvent)
                return object;
            var message = new $root.game.CDebugTriggerEvent();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.tool != null)
                message.tool = object.tool | 0;
            if (object.teamId != null)
                message.teamId = object.teamId | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugTriggerEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugTriggerEvent
         * @static
         * @param {game.CDebugTriggerEvent} message CDebugTriggerEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugTriggerEvent.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.tool = 0;
                object.teamId = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.tool != null && message.hasOwnProperty("tool"))
                object.tool = message.tool;
            if (message.teamId != null && message.hasOwnProperty("teamId"))
                object.teamId = message.teamId;
            return object;
        };

        /**
         * Converts this CDebugTriggerEvent to JSON.
         * @function toJSON
         * @memberof game.CDebugTriggerEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugTriggerEvent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugTriggerEvent
         * @function getTypeUrl
         * @memberof game.CDebugTriggerEvent
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugTriggerEvent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugTriggerEvent";
        };

        return CDebugTriggerEvent;
    })();

    game.SDebugTriggerEvent = (function() {

        /**
         * Properties of a SDebugTriggerEvent.
         * @memberof game
         * @interface ISDebugTriggerEvent
         */

        /**
         * Constructs a new SDebugTriggerEvent.
         * @memberof game
         * @classdesc Represents a SDebugTriggerEvent.
         * @implements ISDebugTriggerEvent
         * @constructor
         * @param {game.ISDebugTriggerEvent=} [properties] Properties to set
         */
        function SDebugTriggerEvent(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SDebugTriggerEvent instance using the specified properties.
         * @function create
         * @memberof game.SDebugTriggerEvent
         * @static
         * @param {game.ISDebugTriggerEvent=} [properties] Properties to set
         * @returns {game.SDebugTriggerEvent} SDebugTriggerEvent instance
         */
        SDebugTriggerEvent.create = function create(properties) {
            return new SDebugTriggerEvent(properties);
        };

        /**
         * Encodes the specified SDebugTriggerEvent message. Does not implicitly {@link game.SDebugTriggerEvent.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugTriggerEvent
         * @static
         * @param {game.ISDebugTriggerEvent} message SDebugTriggerEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugTriggerEvent.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SDebugTriggerEvent message, length delimited. Does not implicitly {@link game.SDebugTriggerEvent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugTriggerEvent
         * @static
         * @param {game.ISDebugTriggerEvent} message SDebugTriggerEvent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugTriggerEvent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugTriggerEvent message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugTriggerEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugTriggerEvent} SDebugTriggerEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugTriggerEvent.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugTriggerEvent();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugTriggerEvent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugTriggerEvent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugTriggerEvent} SDebugTriggerEvent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugTriggerEvent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugTriggerEvent message.
         * @function verify
         * @memberof game.SDebugTriggerEvent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugTriggerEvent.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SDebugTriggerEvent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugTriggerEvent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugTriggerEvent} SDebugTriggerEvent
         */
        SDebugTriggerEvent.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugTriggerEvent)
                return object;
            return new $root.game.SDebugTriggerEvent();
        };

        /**
         * Creates a plain object from a SDebugTriggerEvent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugTriggerEvent
         * @static
         * @param {game.SDebugTriggerEvent} message SDebugTriggerEvent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugTriggerEvent.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SDebugTriggerEvent to JSON.
         * @function toJSON
         * @memberof game.SDebugTriggerEvent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugTriggerEvent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugTriggerEvent
         * @function getTypeUrl
         * @memberof game.SDebugTriggerEvent
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugTriggerEvent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugTriggerEvent";
        };

        return SDebugTriggerEvent;
    })();

    game.CDebugSetTradingBalance = (function() {

        /**
         * Properties of a CDebugSetTradingBalance.
         * @memberof game
         * @interface ICDebugSetTradingBalance
         */

        /**
         * Constructs a new CDebugSetTradingBalance.
         * @memberof game
         * @classdesc Represents a CDebugSetTradingBalance.
         * @implements ICDebugSetTradingBalance
         * @constructor
         * @param {game.ICDebugSetTradingBalance=} [properties] Properties to set
         */
        function CDebugSetTradingBalance(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CDebugSetTradingBalance instance using the specified properties.
         * @function create
         * @memberof game.CDebugSetTradingBalance
         * @static
         * @param {game.ICDebugSetTradingBalance=} [properties] Properties to set
         * @returns {game.CDebugSetTradingBalance} CDebugSetTradingBalance instance
         */
        CDebugSetTradingBalance.create = function create(properties) {
            return new CDebugSetTradingBalance(properties);
        };

        /**
         * Encodes the specified CDebugSetTradingBalance message. Does not implicitly {@link game.CDebugSetTradingBalance.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugSetTradingBalance
         * @static
         * @param {game.ICDebugSetTradingBalance} message CDebugSetTradingBalance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugSetTradingBalance.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CDebugSetTradingBalance message, length delimited. Does not implicitly {@link game.CDebugSetTradingBalance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugSetTradingBalance
         * @static
         * @param {game.ICDebugSetTradingBalance} message CDebugSetTradingBalance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugSetTradingBalance.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugSetTradingBalance message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugSetTradingBalance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugSetTradingBalance} CDebugSetTradingBalance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugSetTradingBalance.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugSetTradingBalance();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugSetTradingBalance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugSetTradingBalance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugSetTradingBalance} CDebugSetTradingBalance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugSetTradingBalance.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugSetTradingBalance message.
         * @function verify
         * @memberof game.CDebugSetTradingBalance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugSetTradingBalance.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CDebugSetTradingBalance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugSetTradingBalance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugSetTradingBalance} CDebugSetTradingBalance
         */
        CDebugSetTradingBalance.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugSetTradingBalance)
                return object;
            return new $root.game.CDebugSetTradingBalance();
        };

        /**
         * Creates a plain object from a CDebugSetTradingBalance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugSetTradingBalance
         * @static
         * @param {game.CDebugSetTradingBalance} message CDebugSetTradingBalance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugSetTradingBalance.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CDebugSetTradingBalance to JSON.
         * @function toJSON
         * @memberof game.CDebugSetTradingBalance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugSetTradingBalance.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugSetTradingBalance
         * @function getTypeUrl
         * @memberof game.CDebugSetTradingBalance
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugSetTradingBalance.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugSetTradingBalance";
        };

        return CDebugSetTradingBalance;
    })();

    game.SDebugSetTradingBalance = (function() {

        /**
         * Properties of a SDebugSetTradingBalance.
         * @memberof game
         * @interface ISDebugSetTradingBalance
         */

        /**
         * Constructs a new SDebugSetTradingBalance.
         * @memberof game
         * @classdesc Represents a SDebugSetTradingBalance.
         * @implements ISDebugSetTradingBalance
         * @constructor
         * @param {game.ISDebugSetTradingBalance=} [properties] Properties to set
         */
        function SDebugSetTradingBalance(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SDebugSetTradingBalance instance using the specified properties.
         * @function create
         * @memberof game.SDebugSetTradingBalance
         * @static
         * @param {game.ISDebugSetTradingBalance=} [properties] Properties to set
         * @returns {game.SDebugSetTradingBalance} SDebugSetTradingBalance instance
         */
        SDebugSetTradingBalance.create = function create(properties) {
            return new SDebugSetTradingBalance(properties);
        };

        /**
         * Encodes the specified SDebugSetTradingBalance message. Does not implicitly {@link game.SDebugSetTradingBalance.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugSetTradingBalance
         * @static
         * @param {game.ISDebugSetTradingBalance} message SDebugSetTradingBalance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugSetTradingBalance.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SDebugSetTradingBalance message, length delimited. Does not implicitly {@link game.SDebugSetTradingBalance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugSetTradingBalance
         * @static
         * @param {game.ISDebugSetTradingBalance} message SDebugSetTradingBalance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugSetTradingBalance.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugSetTradingBalance message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugSetTradingBalance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugSetTradingBalance} SDebugSetTradingBalance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugSetTradingBalance.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugSetTradingBalance();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugSetTradingBalance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugSetTradingBalance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugSetTradingBalance} SDebugSetTradingBalance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugSetTradingBalance.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugSetTradingBalance message.
         * @function verify
         * @memberof game.SDebugSetTradingBalance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugSetTradingBalance.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SDebugSetTradingBalance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugSetTradingBalance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugSetTradingBalance} SDebugSetTradingBalance
         */
        SDebugSetTradingBalance.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugSetTradingBalance)
                return object;
            return new $root.game.SDebugSetTradingBalance();
        };

        /**
         * Creates a plain object from a SDebugSetTradingBalance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugSetTradingBalance
         * @static
         * @param {game.SDebugSetTradingBalance} message SDebugSetTradingBalance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugSetTradingBalance.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SDebugSetTradingBalance to JSON.
         * @function toJSON
         * @memberof game.SDebugSetTradingBalance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugSetTradingBalance.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugSetTradingBalance
         * @function getTypeUrl
         * @memberof game.SDebugSetTradingBalance
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugSetTradingBalance.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugSetTradingBalance";
        };

        return SDebugSetTradingBalance;
    })();

    game.CDebugAddDriftBottle = (function() {

        /**
         * Properties of a CDebugAddDriftBottle.
         * @memberof game
         * @interface ICDebugAddDriftBottle
         * @property {number|null} [id] CDebugAddDriftBottle id
         * @property {number|null} [num] CDebugAddDriftBottle num
         */

        /**
         * Constructs a new CDebugAddDriftBottle.
         * @memberof game
         * @classdesc Represents a CDebugAddDriftBottle.
         * @implements ICDebugAddDriftBottle
         * @constructor
         * @param {game.ICDebugAddDriftBottle=} [properties] Properties to set
         */
        function CDebugAddDriftBottle(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddDriftBottle id.
         * @member {number} id
         * @memberof game.CDebugAddDriftBottle
         * @instance
         */
        CDebugAddDriftBottle.prototype.id = 0;

        /**
         * CDebugAddDriftBottle num.
         * @member {number} num
         * @memberof game.CDebugAddDriftBottle
         * @instance
         */
        CDebugAddDriftBottle.prototype.num = 0;

        /**
         * Creates a new CDebugAddDriftBottle instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddDriftBottle
         * @static
         * @param {game.ICDebugAddDriftBottle=} [properties] Properties to set
         * @returns {game.CDebugAddDriftBottle} CDebugAddDriftBottle instance
         */
        CDebugAddDriftBottle.create = function create(properties) {
            return new CDebugAddDriftBottle(properties);
        };

        /**
         * Encodes the specified CDebugAddDriftBottle message. Does not implicitly {@link game.CDebugAddDriftBottle.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddDriftBottle
         * @static
         * @param {game.ICDebugAddDriftBottle} message CDebugAddDriftBottle message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddDriftBottle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.num != null && Object.hasOwnProperty.call(message, "num"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.num);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddDriftBottle message, length delimited. Does not implicitly {@link game.CDebugAddDriftBottle.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddDriftBottle
         * @static
         * @param {game.ICDebugAddDriftBottle} message CDebugAddDriftBottle message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddDriftBottle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddDriftBottle message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddDriftBottle
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddDriftBottle} CDebugAddDriftBottle
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddDriftBottle.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddDriftBottle();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.num = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddDriftBottle message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddDriftBottle
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddDriftBottle} CDebugAddDriftBottle
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddDriftBottle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddDriftBottle message.
         * @function verify
         * @memberof game.CDebugAddDriftBottle
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddDriftBottle.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.num != null && message.hasOwnProperty("num"))
                if (!$util.isInteger(message.num))
                    return "num: integer expected";
            return null;
        };

        /**
         * Creates a CDebugAddDriftBottle message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddDriftBottle
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddDriftBottle} CDebugAddDriftBottle
         */
        CDebugAddDriftBottle.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddDriftBottle)
                return object;
            var message = new $root.game.CDebugAddDriftBottle();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.num != null)
                message.num = object.num | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddDriftBottle message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddDriftBottle
         * @static
         * @param {game.CDebugAddDriftBottle} message CDebugAddDriftBottle
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddDriftBottle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.num = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.num != null && message.hasOwnProperty("num"))
                object.num = message.num;
            return object;
        };

        /**
         * Converts this CDebugAddDriftBottle to JSON.
         * @function toJSON
         * @memberof game.CDebugAddDriftBottle
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddDriftBottle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddDriftBottle
         * @function getTypeUrl
         * @memberof game.CDebugAddDriftBottle
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddDriftBottle.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddDriftBottle";
        };

        return CDebugAddDriftBottle;
    })();

    game.SDebugAddDriftBottle = (function() {

        /**
         * Properties of a SDebugAddDriftBottle.
         * @memberof game
         * @interface ISDebugAddDriftBottle
         * @property {Array.<common.IReward>|null} [awards] SDebugAddDriftBottle awards
         */

        /**
         * Constructs a new SDebugAddDriftBottle.
         * @memberof game
         * @classdesc Represents a SDebugAddDriftBottle.
         * @implements ISDebugAddDriftBottle
         * @constructor
         * @param {game.ISDebugAddDriftBottle=} [properties] Properties to set
         */
        function SDebugAddDriftBottle(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SDebugAddDriftBottle awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SDebugAddDriftBottle
         * @instance
         */
        SDebugAddDriftBottle.prototype.awards = $util.emptyArray;

        /**
         * Creates a new SDebugAddDriftBottle instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddDriftBottle
         * @static
         * @param {game.ISDebugAddDriftBottle=} [properties] Properties to set
         * @returns {game.SDebugAddDriftBottle} SDebugAddDriftBottle instance
         */
        SDebugAddDriftBottle.create = function create(properties) {
            return new SDebugAddDriftBottle(properties);
        };

        /**
         * Encodes the specified SDebugAddDriftBottle message. Does not implicitly {@link game.SDebugAddDriftBottle.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddDriftBottle
         * @static
         * @param {game.ISDebugAddDriftBottle} message SDebugAddDriftBottle message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddDriftBottle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddDriftBottle message, length delimited. Does not implicitly {@link game.SDebugAddDriftBottle.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddDriftBottle
         * @static
         * @param {game.ISDebugAddDriftBottle} message SDebugAddDriftBottle message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddDriftBottle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddDriftBottle message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddDriftBottle
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddDriftBottle} SDebugAddDriftBottle
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddDriftBottle.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddDriftBottle();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddDriftBottle message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddDriftBottle
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddDriftBottle} SDebugAddDriftBottle
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddDriftBottle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddDriftBottle message.
         * @function verify
         * @memberof game.SDebugAddDriftBottle
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddDriftBottle.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SDebugAddDriftBottle message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddDriftBottle
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddDriftBottle} SDebugAddDriftBottle
         */
        SDebugAddDriftBottle.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddDriftBottle)
                return object;
            var message = new $root.game.SDebugAddDriftBottle();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SDebugAddDriftBottle.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SDebugAddDriftBottle.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SDebugAddDriftBottle message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddDriftBottle
         * @static
         * @param {game.SDebugAddDriftBottle} message SDebugAddDriftBottle
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddDriftBottle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            return object;
        };

        /**
         * Converts this SDebugAddDriftBottle to JSON.
         * @function toJSON
         * @memberof game.SDebugAddDriftBottle
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddDriftBottle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddDriftBottle
         * @function getTypeUrl
         * @memberof game.SDebugAddDriftBottle
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddDriftBottle.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddDriftBottle";
        };

        return SDebugAddDriftBottle;
    })();

    game.CDebugAddPuzzlePiece = (function() {

        /**
         * Properties of a CDebugAddPuzzlePiece.
         * @memberof game
         * @interface ICDebugAddPuzzlePiece
         * @property {number|null} [id] CDebugAddPuzzlePiece id
         * @property {number|null} [num] CDebugAddPuzzlePiece num
         */

        /**
         * Constructs a new CDebugAddPuzzlePiece.
         * @memberof game
         * @classdesc Represents a CDebugAddPuzzlePiece.
         * @implements ICDebugAddPuzzlePiece
         * @constructor
         * @param {game.ICDebugAddPuzzlePiece=} [properties] Properties to set
         */
        function CDebugAddPuzzlePiece(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddPuzzlePiece id.
         * @member {number} id
         * @memberof game.CDebugAddPuzzlePiece
         * @instance
         */
        CDebugAddPuzzlePiece.prototype.id = 0;

        /**
         * CDebugAddPuzzlePiece num.
         * @member {number} num
         * @memberof game.CDebugAddPuzzlePiece
         * @instance
         */
        CDebugAddPuzzlePiece.prototype.num = 0;

        /**
         * Creates a new CDebugAddPuzzlePiece instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddPuzzlePiece
         * @static
         * @param {game.ICDebugAddPuzzlePiece=} [properties] Properties to set
         * @returns {game.CDebugAddPuzzlePiece} CDebugAddPuzzlePiece instance
         */
        CDebugAddPuzzlePiece.create = function create(properties) {
            return new CDebugAddPuzzlePiece(properties);
        };

        /**
         * Encodes the specified CDebugAddPuzzlePiece message. Does not implicitly {@link game.CDebugAddPuzzlePiece.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddPuzzlePiece
         * @static
         * @param {game.ICDebugAddPuzzlePiece} message CDebugAddPuzzlePiece message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddPuzzlePiece.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.num != null && Object.hasOwnProperty.call(message, "num"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.num);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddPuzzlePiece message, length delimited. Does not implicitly {@link game.CDebugAddPuzzlePiece.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddPuzzlePiece
         * @static
         * @param {game.ICDebugAddPuzzlePiece} message CDebugAddPuzzlePiece message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddPuzzlePiece.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddPuzzlePiece message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddPuzzlePiece
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddPuzzlePiece} CDebugAddPuzzlePiece
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddPuzzlePiece.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddPuzzlePiece();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.num = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddPuzzlePiece message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddPuzzlePiece
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddPuzzlePiece} CDebugAddPuzzlePiece
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddPuzzlePiece.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddPuzzlePiece message.
         * @function verify
         * @memberof game.CDebugAddPuzzlePiece
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddPuzzlePiece.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.num != null && message.hasOwnProperty("num"))
                if (!$util.isInteger(message.num))
                    return "num: integer expected";
            return null;
        };

        /**
         * Creates a CDebugAddPuzzlePiece message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddPuzzlePiece
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddPuzzlePiece} CDebugAddPuzzlePiece
         */
        CDebugAddPuzzlePiece.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddPuzzlePiece)
                return object;
            var message = new $root.game.CDebugAddPuzzlePiece();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.num != null)
                message.num = object.num | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddPuzzlePiece message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddPuzzlePiece
         * @static
         * @param {game.CDebugAddPuzzlePiece} message CDebugAddPuzzlePiece
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddPuzzlePiece.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.num = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.num != null && message.hasOwnProperty("num"))
                object.num = message.num;
            return object;
        };

        /**
         * Converts this CDebugAddPuzzlePiece to JSON.
         * @function toJSON
         * @memberof game.CDebugAddPuzzlePiece
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddPuzzlePiece.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddPuzzlePiece
         * @function getTypeUrl
         * @memberof game.CDebugAddPuzzlePiece
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddPuzzlePiece.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddPuzzlePiece";
        };

        return CDebugAddPuzzlePiece;
    })();

    game.SDebugAddPuzzlePiece = (function() {

        /**
         * Properties of a SDebugAddPuzzlePiece.
         * @memberof game
         * @interface ISDebugAddPuzzlePiece
         * @property {Array.<common.IReward>|null} [awards] SDebugAddPuzzlePiece awards
         */

        /**
         * Constructs a new SDebugAddPuzzlePiece.
         * @memberof game
         * @classdesc Represents a SDebugAddPuzzlePiece.
         * @implements ISDebugAddPuzzlePiece
         * @constructor
         * @param {game.ISDebugAddPuzzlePiece=} [properties] Properties to set
         */
        function SDebugAddPuzzlePiece(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SDebugAddPuzzlePiece awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SDebugAddPuzzlePiece
         * @instance
         */
        SDebugAddPuzzlePiece.prototype.awards = $util.emptyArray;

        /**
         * Creates a new SDebugAddPuzzlePiece instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddPuzzlePiece
         * @static
         * @param {game.ISDebugAddPuzzlePiece=} [properties] Properties to set
         * @returns {game.SDebugAddPuzzlePiece} SDebugAddPuzzlePiece instance
         */
        SDebugAddPuzzlePiece.create = function create(properties) {
            return new SDebugAddPuzzlePiece(properties);
        };

        /**
         * Encodes the specified SDebugAddPuzzlePiece message. Does not implicitly {@link game.SDebugAddPuzzlePiece.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddPuzzlePiece
         * @static
         * @param {game.ISDebugAddPuzzlePiece} message SDebugAddPuzzlePiece message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddPuzzlePiece.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddPuzzlePiece message, length delimited. Does not implicitly {@link game.SDebugAddPuzzlePiece.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddPuzzlePiece
         * @static
         * @param {game.ISDebugAddPuzzlePiece} message SDebugAddPuzzlePiece message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddPuzzlePiece.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddPuzzlePiece message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddPuzzlePiece
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddPuzzlePiece} SDebugAddPuzzlePiece
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddPuzzlePiece.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddPuzzlePiece();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddPuzzlePiece message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddPuzzlePiece
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddPuzzlePiece} SDebugAddPuzzlePiece
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddPuzzlePiece.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddPuzzlePiece message.
         * @function verify
         * @memberof game.SDebugAddPuzzlePiece
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddPuzzlePiece.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SDebugAddPuzzlePiece message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddPuzzlePiece
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddPuzzlePiece} SDebugAddPuzzlePiece
         */
        SDebugAddPuzzlePiece.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddPuzzlePiece)
                return object;
            var message = new $root.game.SDebugAddPuzzlePiece();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SDebugAddPuzzlePiece.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SDebugAddPuzzlePiece.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SDebugAddPuzzlePiece message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddPuzzlePiece
         * @static
         * @param {game.SDebugAddPuzzlePiece} message SDebugAddPuzzlePiece
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddPuzzlePiece.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            return object;
        };

        /**
         * Converts this SDebugAddPuzzlePiece to JSON.
         * @function toJSON
         * @memberof game.SDebugAddPuzzlePiece
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddPuzzlePiece.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddPuzzlePiece
         * @function getTypeUrl
         * @memberof game.SDebugAddPuzzlePiece
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddPuzzlePiece.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddPuzzlePiece";
        };

        return SDebugAddPuzzlePiece;
    })();

    game.CDebugAddCollection = (function() {

        /**
         * Properties of a CDebugAddCollection.
         * @memberof game
         * @interface ICDebugAddCollection
         * @property {number|null} [id] CDebugAddCollection id
         * @property {number|null} [num] CDebugAddCollection num
         */

        /**
         * Constructs a new CDebugAddCollection.
         * @memberof game
         * @classdesc Represents a CDebugAddCollection.
         * @implements ICDebugAddCollection
         * @constructor
         * @param {game.ICDebugAddCollection=} [properties] Properties to set
         */
        function CDebugAddCollection(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddCollection id.
         * @member {number} id
         * @memberof game.CDebugAddCollection
         * @instance
         */
        CDebugAddCollection.prototype.id = 0;

        /**
         * CDebugAddCollection num.
         * @member {number} num
         * @memberof game.CDebugAddCollection
         * @instance
         */
        CDebugAddCollection.prototype.num = 0;

        /**
         * Creates a new CDebugAddCollection instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddCollection
         * @static
         * @param {game.ICDebugAddCollection=} [properties] Properties to set
         * @returns {game.CDebugAddCollection} CDebugAddCollection instance
         */
        CDebugAddCollection.create = function create(properties) {
            return new CDebugAddCollection(properties);
        };

        /**
         * Encodes the specified CDebugAddCollection message. Does not implicitly {@link game.CDebugAddCollection.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddCollection
         * @static
         * @param {game.ICDebugAddCollection} message CDebugAddCollection message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddCollection.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.num != null && Object.hasOwnProperty.call(message, "num"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.num);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddCollection message, length delimited. Does not implicitly {@link game.CDebugAddCollection.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddCollection
         * @static
         * @param {game.ICDebugAddCollection} message CDebugAddCollection message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddCollection.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddCollection message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddCollection
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddCollection} CDebugAddCollection
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddCollection.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddCollection();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.num = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddCollection message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddCollection
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddCollection} CDebugAddCollection
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddCollection.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddCollection message.
         * @function verify
         * @memberof game.CDebugAddCollection
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddCollection.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.num != null && message.hasOwnProperty("num"))
                if (!$util.isInteger(message.num))
                    return "num: integer expected";
            return null;
        };

        /**
         * Creates a CDebugAddCollection message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddCollection
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddCollection} CDebugAddCollection
         */
        CDebugAddCollection.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddCollection)
                return object;
            var message = new $root.game.CDebugAddCollection();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.num != null)
                message.num = object.num | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddCollection message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddCollection
         * @static
         * @param {game.CDebugAddCollection} message CDebugAddCollection
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddCollection.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.num = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.num != null && message.hasOwnProperty("num"))
                object.num = message.num;
            return object;
        };

        /**
         * Converts this CDebugAddCollection to JSON.
         * @function toJSON
         * @memberof game.CDebugAddCollection
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddCollection.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddCollection
         * @function getTypeUrl
         * @memberof game.CDebugAddCollection
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddCollection.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddCollection";
        };

        return CDebugAddCollection;
    })();

    game.SDebugAddCollection = (function() {

        /**
         * Properties of a SDebugAddCollection.
         * @memberof game
         * @interface ISDebugAddCollection
         * @property {Array.<common.IReward>|null} [awards] SDebugAddCollection awards
         */

        /**
         * Constructs a new SDebugAddCollection.
         * @memberof game
         * @classdesc Represents a SDebugAddCollection.
         * @implements ISDebugAddCollection
         * @constructor
         * @param {game.ISDebugAddCollection=} [properties] Properties to set
         */
        function SDebugAddCollection(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SDebugAddCollection awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SDebugAddCollection
         * @instance
         */
        SDebugAddCollection.prototype.awards = $util.emptyArray;

        /**
         * Creates a new SDebugAddCollection instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddCollection
         * @static
         * @param {game.ISDebugAddCollection=} [properties] Properties to set
         * @returns {game.SDebugAddCollection} SDebugAddCollection instance
         */
        SDebugAddCollection.create = function create(properties) {
            return new SDebugAddCollection(properties);
        };

        /**
         * Encodes the specified SDebugAddCollection message. Does not implicitly {@link game.SDebugAddCollection.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddCollection
         * @static
         * @param {game.ISDebugAddCollection} message SDebugAddCollection message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddCollection.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddCollection message, length delimited. Does not implicitly {@link game.SDebugAddCollection.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddCollection
         * @static
         * @param {game.ISDebugAddCollection} message SDebugAddCollection message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddCollection.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddCollection message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddCollection
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddCollection} SDebugAddCollection
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddCollection.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddCollection();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddCollection message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddCollection
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddCollection} SDebugAddCollection
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddCollection.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddCollection message.
         * @function verify
         * @memberof game.SDebugAddCollection
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddCollection.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SDebugAddCollection message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddCollection
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddCollection} SDebugAddCollection
         */
        SDebugAddCollection.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddCollection)
                return object;
            var message = new $root.game.SDebugAddCollection();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SDebugAddCollection.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SDebugAddCollection.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SDebugAddCollection message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddCollection
         * @static
         * @param {game.SDebugAddCollection} message SDebugAddCollection
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddCollection.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            return object;
        };

        /**
         * Converts this SDebugAddCollection to JSON.
         * @function toJSON
         * @memberof game.SDebugAddCollection
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddCollection.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddCollection
         * @function getTypeUrl
         * @memberof game.SDebugAddCollection
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddCollection.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddCollection";
        };

        return SDebugAddCollection;
    })();

    game.CDebugAddAdventureProgress = (function() {

        /**
         * Properties of a CDebugAddAdventureProgress.
         * @memberof game
         * @interface ICDebugAddAdventureProgress
         * @property {number|null} [val] CDebugAddAdventureProgress val
         */

        /**
         * Constructs a new CDebugAddAdventureProgress.
         * @memberof game
         * @classdesc Represents a CDebugAddAdventureProgress.
         * @implements ICDebugAddAdventureProgress
         * @constructor
         * @param {game.ICDebugAddAdventureProgress=} [properties] Properties to set
         */
        function CDebugAddAdventureProgress(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CDebugAddAdventureProgress val.
         * @member {number} val
         * @memberof game.CDebugAddAdventureProgress
         * @instance
         */
        CDebugAddAdventureProgress.prototype.val = 0;

        /**
         * Creates a new CDebugAddAdventureProgress instance using the specified properties.
         * @function create
         * @memberof game.CDebugAddAdventureProgress
         * @static
         * @param {game.ICDebugAddAdventureProgress=} [properties] Properties to set
         * @returns {game.CDebugAddAdventureProgress} CDebugAddAdventureProgress instance
         */
        CDebugAddAdventureProgress.create = function create(properties) {
            return new CDebugAddAdventureProgress(properties);
        };

        /**
         * Encodes the specified CDebugAddAdventureProgress message. Does not implicitly {@link game.CDebugAddAdventureProgress.verify|verify} messages.
         * @function encode
         * @memberof game.CDebugAddAdventureProgress
         * @static
         * @param {game.ICDebugAddAdventureProgress} message CDebugAddAdventureProgress message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddAdventureProgress.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.val != null && Object.hasOwnProperty.call(message, "val"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.val);
            return writer;
        };

        /**
         * Encodes the specified CDebugAddAdventureProgress message, length delimited. Does not implicitly {@link game.CDebugAddAdventureProgress.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CDebugAddAdventureProgress
         * @static
         * @param {game.ICDebugAddAdventureProgress} message CDebugAddAdventureProgress message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CDebugAddAdventureProgress.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CDebugAddAdventureProgress message from the specified reader or buffer.
         * @function decode
         * @memberof game.CDebugAddAdventureProgress
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CDebugAddAdventureProgress} CDebugAddAdventureProgress
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddAdventureProgress.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CDebugAddAdventureProgress();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.val = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CDebugAddAdventureProgress message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CDebugAddAdventureProgress
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CDebugAddAdventureProgress} CDebugAddAdventureProgress
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CDebugAddAdventureProgress.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CDebugAddAdventureProgress message.
         * @function verify
         * @memberof game.CDebugAddAdventureProgress
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CDebugAddAdventureProgress.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.val != null && message.hasOwnProperty("val"))
                if (!$util.isInteger(message.val))
                    return "val: integer expected";
            return null;
        };

        /**
         * Creates a CDebugAddAdventureProgress message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CDebugAddAdventureProgress
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CDebugAddAdventureProgress} CDebugAddAdventureProgress
         */
        CDebugAddAdventureProgress.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CDebugAddAdventureProgress)
                return object;
            var message = new $root.game.CDebugAddAdventureProgress();
            if (object.val != null)
                message.val = object.val | 0;
            return message;
        };

        /**
         * Creates a plain object from a CDebugAddAdventureProgress message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CDebugAddAdventureProgress
         * @static
         * @param {game.CDebugAddAdventureProgress} message CDebugAddAdventureProgress
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CDebugAddAdventureProgress.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.val = 0;
            if (message.val != null && message.hasOwnProperty("val"))
                object.val = message.val;
            return object;
        };

        /**
         * Converts this CDebugAddAdventureProgress to JSON.
         * @function toJSON
         * @memberof game.CDebugAddAdventureProgress
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CDebugAddAdventureProgress.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CDebugAddAdventureProgress
         * @function getTypeUrl
         * @memberof game.CDebugAddAdventureProgress
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CDebugAddAdventureProgress.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CDebugAddAdventureProgress";
        };

        return CDebugAddAdventureProgress;
    })();

    game.SDebugAddAdventureProgress = (function() {

        /**
         * Properties of a SDebugAddAdventureProgress.
         * @memberof game
         * @interface ISDebugAddAdventureProgress
         */

        /**
         * Constructs a new SDebugAddAdventureProgress.
         * @memberof game
         * @classdesc Represents a SDebugAddAdventureProgress.
         * @implements ISDebugAddAdventureProgress
         * @constructor
         * @param {game.ISDebugAddAdventureProgress=} [properties] Properties to set
         */
        function SDebugAddAdventureProgress(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SDebugAddAdventureProgress instance using the specified properties.
         * @function create
         * @memberof game.SDebugAddAdventureProgress
         * @static
         * @param {game.ISDebugAddAdventureProgress=} [properties] Properties to set
         * @returns {game.SDebugAddAdventureProgress} SDebugAddAdventureProgress instance
         */
        SDebugAddAdventureProgress.create = function create(properties) {
            return new SDebugAddAdventureProgress(properties);
        };

        /**
         * Encodes the specified SDebugAddAdventureProgress message. Does not implicitly {@link game.SDebugAddAdventureProgress.verify|verify} messages.
         * @function encode
         * @memberof game.SDebugAddAdventureProgress
         * @static
         * @param {game.ISDebugAddAdventureProgress} message SDebugAddAdventureProgress message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddAdventureProgress.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SDebugAddAdventureProgress message, length delimited. Does not implicitly {@link game.SDebugAddAdventureProgress.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SDebugAddAdventureProgress
         * @static
         * @param {game.ISDebugAddAdventureProgress} message SDebugAddAdventureProgress message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SDebugAddAdventureProgress.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SDebugAddAdventureProgress message from the specified reader or buffer.
         * @function decode
         * @memberof game.SDebugAddAdventureProgress
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SDebugAddAdventureProgress} SDebugAddAdventureProgress
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddAdventureProgress.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SDebugAddAdventureProgress();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SDebugAddAdventureProgress message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SDebugAddAdventureProgress
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SDebugAddAdventureProgress} SDebugAddAdventureProgress
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SDebugAddAdventureProgress.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SDebugAddAdventureProgress message.
         * @function verify
         * @memberof game.SDebugAddAdventureProgress
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SDebugAddAdventureProgress.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SDebugAddAdventureProgress message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SDebugAddAdventureProgress
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SDebugAddAdventureProgress} SDebugAddAdventureProgress
         */
        SDebugAddAdventureProgress.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SDebugAddAdventureProgress)
                return object;
            return new $root.game.SDebugAddAdventureProgress();
        };

        /**
         * Creates a plain object from a SDebugAddAdventureProgress message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SDebugAddAdventureProgress
         * @static
         * @param {game.SDebugAddAdventureProgress} message SDebugAddAdventureProgress
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SDebugAddAdventureProgress.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SDebugAddAdventureProgress to JSON.
         * @function toJSON
         * @memberof game.SDebugAddAdventureProgress
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SDebugAddAdventureProgress.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SDebugAddAdventureProgress
         * @function getTypeUrl
         * @memberof game.SDebugAddAdventureProgress
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SDebugAddAdventureProgress.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SDebugAddAdventureProgress";
        };

        return SDebugAddAdventureProgress;
    })();

    game.CClaimFairyThemeReward = (function() {

        /**
         * Properties of a CClaimFairyThemeReward.
         * @memberof game
         * @interface ICClaimFairyThemeReward
         * @property {number|null} [level] CClaimFairyThemeReward level
         * @property {boolean|null} [special] CClaimFairyThemeReward special
         */

        /**
         * Constructs a new CClaimFairyThemeReward.
         * @memberof game
         * @classdesc Represents a CClaimFairyThemeReward.
         * @implements ICClaimFairyThemeReward
         * @constructor
         * @param {game.ICClaimFairyThemeReward=} [properties] Properties to set
         */
        function CClaimFairyThemeReward(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CClaimFairyThemeReward level.
         * @member {number} level
         * @memberof game.CClaimFairyThemeReward
         * @instance
         */
        CClaimFairyThemeReward.prototype.level = 0;

        /**
         * CClaimFairyThemeReward special.
         * @member {boolean} special
         * @memberof game.CClaimFairyThemeReward
         * @instance
         */
        CClaimFairyThemeReward.prototype.special = false;

        /**
         * Creates a new CClaimFairyThemeReward instance using the specified properties.
         * @function create
         * @memberof game.CClaimFairyThemeReward
         * @static
         * @param {game.ICClaimFairyThemeReward=} [properties] Properties to set
         * @returns {game.CClaimFairyThemeReward} CClaimFairyThemeReward instance
         */
        CClaimFairyThemeReward.create = function create(properties) {
            return new CClaimFairyThemeReward(properties);
        };

        /**
         * Encodes the specified CClaimFairyThemeReward message. Does not implicitly {@link game.CClaimFairyThemeReward.verify|verify} messages.
         * @function encode
         * @memberof game.CClaimFairyThemeReward
         * @static
         * @param {game.ICClaimFairyThemeReward} message CClaimFairyThemeReward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CClaimFairyThemeReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.level);
            if (message.special != null && Object.hasOwnProperty.call(message, "special"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.special);
            return writer;
        };

        /**
         * Encodes the specified CClaimFairyThemeReward message, length delimited. Does not implicitly {@link game.CClaimFairyThemeReward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CClaimFairyThemeReward
         * @static
         * @param {game.ICClaimFairyThemeReward} message CClaimFairyThemeReward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CClaimFairyThemeReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CClaimFairyThemeReward message from the specified reader or buffer.
         * @function decode
         * @memberof game.CClaimFairyThemeReward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CClaimFairyThemeReward} CClaimFairyThemeReward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CClaimFairyThemeReward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CClaimFairyThemeReward();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.level = reader.int32();
                        break;
                    }
                case 2: {
                        message.special = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CClaimFairyThemeReward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CClaimFairyThemeReward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CClaimFairyThemeReward} CClaimFairyThemeReward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CClaimFairyThemeReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CClaimFairyThemeReward message.
         * @function verify
         * @memberof game.CClaimFairyThemeReward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CClaimFairyThemeReward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.special != null && message.hasOwnProperty("special"))
                if (typeof message.special !== "boolean")
                    return "special: boolean expected";
            return null;
        };

        /**
         * Creates a CClaimFairyThemeReward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CClaimFairyThemeReward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CClaimFairyThemeReward} CClaimFairyThemeReward
         */
        CClaimFairyThemeReward.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CClaimFairyThemeReward)
                return object;
            var message = new $root.game.CClaimFairyThemeReward();
            if (object.level != null)
                message.level = object.level | 0;
            if (object.special != null)
                message.special = Boolean(object.special);
            return message;
        };

        /**
         * Creates a plain object from a CClaimFairyThemeReward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CClaimFairyThemeReward
         * @static
         * @param {game.CClaimFairyThemeReward} message CClaimFairyThemeReward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CClaimFairyThemeReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.level = 0;
                object.special = false;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.special != null && message.hasOwnProperty("special"))
                object.special = message.special;
            return object;
        };

        /**
         * Converts this CClaimFairyThemeReward to JSON.
         * @function toJSON
         * @memberof game.CClaimFairyThemeReward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CClaimFairyThemeReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CClaimFairyThemeReward
         * @function getTypeUrl
         * @memberof game.CClaimFairyThemeReward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CClaimFairyThemeReward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CClaimFairyThemeReward";
        };

        return CClaimFairyThemeReward;
    })();

    game.SClaimFairyThemeReward = (function() {

        /**
         * Properties of a SClaimFairyThemeReward.
         * @memberof game
         * @interface ISClaimFairyThemeReward
         * @property {Array.<common.IReward>|null} [awards] SClaimFairyThemeReward awards
         */

        /**
         * Constructs a new SClaimFairyThemeReward.
         * @memberof game
         * @classdesc Represents a SClaimFairyThemeReward.
         * @implements ISClaimFairyThemeReward
         * @constructor
         * @param {game.ISClaimFairyThemeReward=} [properties] Properties to set
         */
        function SClaimFairyThemeReward(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SClaimFairyThemeReward awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SClaimFairyThemeReward
         * @instance
         */
        SClaimFairyThemeReward.prototype.awards = $util.emptyArray;

        /**
         * Creates a new SClaimFairyThemeReward instance using the specified properties.
         * @function create
         * @memberof game.SClaimFairyThemeReward
         * @static
         * @param {game.ISClaimFairyThemeReward=} [properties] Properties to set
         * @returns {game.SClaimFairyThemeReward} SClaimFairyThemeReward instance
         */
        SClaimFairyThemeReward.create = function create(properties) {
            return new SClaimFairyThemeReward(properties);
        };

        /**
         * Encodes the specified SClaimFairyThemeReward message. Does not implicitly {@link game.SClaimFairyThemeReward.verify|verify} messages.
         * @function encode
         * @memberof game.SClaimFairyThemeReward
         * @static
         * @param {game.ISClaimFairyThemeReward} message SClaimFairyThemeReward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SClaimFairyThemeReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SClaimFairyThemeReward message, length delimited. Does not implicitly {@link game.SClaimFairyThemeReward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SClaimFairyThemeReward
         * @static
         * @param {game.ISClaimFairyThemeReward} message SClaimFairyThemeReward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SClaimFairyThemeReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SClaimFairyThemeReward message from the specified reader or buffer.
         * @function decode
         * @memberof game.SClaimFairyThemeReward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SClaimFairyThemeReward} SClaimFairyThemeReward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SClaimFairyThemeReward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SClaimFairyThemeReward();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SClaimFairyThemeReward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SClaimFairyThemeReward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SClaimFairyThemeReward} SClaimFairyThemeReward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SClaimFairyThemeReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SClaimFairyThemeReward message.
         * @function verify
         * @memberof game.SClaimFairyThemeReward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SClaimFairyThemeReward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SClaimFairyThemeReward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SClaimFairyThemeReward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SClaimFairyThemeReward} SClaimFairyThemeReward
         */
        SClaimFairyThemeReward.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SClaimFairyThemeReward)
                return object;
            var message = new $root.game.SClaimFairyThemeReward();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SClaimFairyThemeReward.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SClaimFairyThemeReward.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SClaimFairyThemeReward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SClaimFairyThemeReward
         * @static
         * @param {game.SClaimFairyThemeReward} message SClaimFairyThemeReward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SClaimFairyThemeReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            return object;
        };

        /**
         * Converts this SClaimFairyThemeReward to JSON.
         * @function toJSON
         * @memberof game.SClaimFairyThemeReward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SClaimFairyThemeReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SClaimFairyThemeReward
         * @function getTypeUrl
         * @memberof game.SClaimFairyThemeReward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SClaimFairyThemeReward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SClaimFairyThemeReward";
        };

        return SClaimFairyThemeReward;
    })();

    game.COpenSpecialFairyTheme = (function() {

        /**
         * Properties of a COpenSpecialFairyTheme.
         * @memberof game
         * @interface ICOpenSpecialFairyTheme
         */

        /**
         * Constructs a new COpenSpecialFairyTheme.
         * @memberof game
         * @classdesc Represents a COpenSpecialFairyTheme.
         * @implements ICOpenSpecialFairyTheme
         * @constructor
         * @param {game.ICOpenSpecialFairyTheme=} [properties] Properties to set
         */
        function COpenSpecialFairyTheme(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new COpenSpecialFairyTheme instance using the specified properties.
         * @function create
         * @memberof game.COpenSpecialFairyTheme
         * @static
         * @param {game.ICOpenSpecialFairyTheme=} [properties] Properties to set
         * @returns {game.COpenSpecialFairyTheme} COpenSpecialFairyTheme instance
         */
        COpenSpecialFairyTheme.create = function create(properties) {
            return new COpenSpecialFairyTheme(properties);
        };

        /**
         * Encodes the specified COpenSpecialFairyTheme message. Does not implicitly {@link game.COpenSpecialFairyTheme.verify|verify} messages.
         * @function encode
         * @memberof game.COpenSpecialFairyTheme
         * @static
         * @param {game.ICOpenSpecialFairyTheme} message COpenSpecialFairyTheme message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        COpenSpecialFairyTheme.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified COpenSpecialFairyTheme message, length delimited. Does not implicitly {@link game.COpenSpecialFairyTheme.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.COpenSpecialFairyTheme
         * @static
         * @param {game.ICOpenSpecialFairyTheme} message COpenSpecialFairyTheme message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        COpenSpecialFairyTheme.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a COpenSpecialFairyTheme message from the specified reader or buffer.
         * @function decode
         * @memberof game.COpenSpecialFairyTheme
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.COpenSpecialFairyTheme} COpenSpecialFairyTheme
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        COpenSpecialFairyTheme.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.COpenSpecialFairyTheme();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a COpenSpecialFairyTheme message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.COpenSpecialFairyTheme
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.COpenSpecialFairyTheme} COpenSpecialFairyTheme
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        COpenSpecialFairyTheme.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a COpenSpecialFairyTheme message.
         * @function verify
         * @memberof game.COpenSpecialFairyTheme
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        COpenSpecialFairyTheme.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a COpenSpecialFairyTheme message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.COpenSpecialFairyTheme
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.COpenSpecialFairyTheme} COpenSpecialFairyTheme
         */
        COpenSpecialFairyTheme.fromObject = function fromObject(object) {
            if (object instanceof $root.game.COpenSpecialFairyTheme)
                return object;
            return new $root.game.COpenSpecialFairyTheme();
        };

        /**
         * Creates a plain object from a COpenSpecialFairyTheme message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.COpenSpecialFairyTheme
         * @static
         * @param {game.COpenSpecialFairyTheme} message COpenSpecialFairyTheme
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        COpenSpecialFairyTheme.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this COpenSpecialFairyTheme to JSON.
         * @function toJSON
         * @memberof game.COpenSpecialFairyTheme
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        COpenSpecialFairyTheme.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for COpenSpecialFairyTheme
         * @function getTypeUrl
         * @memberof game.COpenSpecialFairyTheme
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        COpenSpecialFairyTheme.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.COpenSpecialFairyTheme";
        };

        return COpenSpecialFairyTheme;
    })();

    game.SOpenSpecialFairyTheme = (function() {

        /**
         * Properties of a SOpenSpecialFairyTheme.
         * @memberof game
         * @interface ISOpenSpecialFairyTheme
         * @property {Array.<common.IReward>|null} [costs] SOpenSpecialFairyTheme costs
         */

        /**
         * Constructs a new SOpenSpecialFairyTheme.
         * @memberof game
         * @classdesc Represents a SOpenSpecialFairyTheme.
         * @implements ISOpenSpecialFairyTheme
         * @constructor
         * @param {game.ISOpenSpecialFairyTheme=} [properties] Properties to set
         */
        function SOpenSpecialFairyTheme(properties) {
            this.costs = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SOpenSpecialFairyTheme costs.
         * @member {Array.<common.IReward>} costs
         * @memberof game.SOpenSpecialFairyTheme
         * @instance
         */
        SOpenSpecialFairyTheme.prototype.costs = $util.emptyArray;

        /**
         * Creates a new SOpenSpecialFairyTheme instance using the specified properties.
         * @function create
         * @memberof game.SOpenSpecialFairyTheme
         * @static
         * @param {game.ISOpenSpecialFairyTheme=} [properties] Properties to set
         * @returns {game.SOpenSpecialFairyTheme} SOpenSpecialFairyTheme instance
         */
        SOpenSpecialFairyTheme.create = function create(properties) {
            return new SOpenSpecialFairyTheme(properties);
        };

        /**
         * Encodes the specified SOpenSpecialFairyTheme message. Does not implicitly {@link game.SOpenSpecialFairyTheme.verify|verify} messages.
         * @function encode
         * @memberof game.SOpenSpecialFairyTheme
         * @static
         * @param {game.ISOpenSpecialFairyTheme} message SOpenSpecialFairyTheme message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SOpenSpecialFairyTheme.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.costs != null && message.costs.length)
                for (var i = 0; i < message.costs.length; ++i)
                    $root.common.Reward.encode(message.costs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SOpenSpecialFairyTheme message, length delimited. Does not implicitly {@link game.SOpenSpecialFairyTheme.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SOpenSpecialFairyTheme
         * @static
         * @param {game.ISOpenSpecialFairyTheme} message SOpenSpecialFairyTheme message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SOpenSpecialFairyTheme.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SOpenSpecialFairyTheme message from the specified reader or buffer.
         * @function decode
         * @memberof game.SOpenSpecialFairyTheme
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SOpenSpecialFairyTheme} SOpenSpecialFairyTheme
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SOpenSpecialFairyTheme.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SOpenSpecialFairyTheme();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.costs && message.costs.length))
                            message.costs = [];
                        message.costs.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SOpenSpecialFairyTheme message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SOpenSpecialFairyTheme
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SOpenSpecialFairyTheme} SOpenSpecialFairyTheme
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SOpenSpecialFairyTheme.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SOpenSpecialFairyTheme message.
         * @function verify
         * @memberof game.SOpenSpecialFairyTheme
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SOpenSpecialFairyTheme.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.costs != null && message.hasOwnProperty("costs")) {
                if (!Array.isArray(message.costs))
                    return "costs: array expected";
                for (var i = 0; i < message.costs.length; ++i) {
                    var error = $root.common.Reward.verify(message.costs[i]);
                    if (error)
                        return "costs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SOpenSpecialFairyTheme message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SOpenSpecialFairyTheme
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SOpenSpecialFairyTheme} SOpenSpecialFairyTheme
         */
        SOpenSpecialFairyTheme.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SOpenSpecialFairyTheme)
                return object;
            var message = new $root.game.SOpenSpecialFairyTheme();
            if (object.costs) {
                if (!Array.isArray(object.costs))
                    throw TypeError(".game.SOpenSpecialFairyTheme.costs: array expected");
                message.costs = [];
                for (var i = 0; i < object.costs.length; ++i) {
                    if (typeof object.costs[i] !== "object")
                        throw TypeError(".game.SOpenSpecialFairyTheme.costs: object expected");
                    message.costs[i] = $root.common.Reward.fromObject(object.costs[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SOpenSpecialFairyTheme message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SOpenSpecialFairyTheme
         * @static
         * @param {game.SOpenSpecialFairyTheme} message SOpenSpecialFairyTheme
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SOpenSpecialFairyTheme.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.costs = [];
            if (message.costs && message.costs.length) {
                object.costs = [];
                for (var j = 0; j < message.costs.length; ++j)
                    object.costs[j] = $root.common.Reward.toObject(message.costs[j], options);
            }
            return object;
        };

        /**
         * Converts this SOpenSpecialFairyTheme to JSON.
         * @function toJSON
         * @memberof game.SOpenSpecialFairyTheme
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SOpenSpecialFairyTheme.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SOpenSpecialFairyTheme
         * @function getTypeUrl
         * @memberof game.SOpenSpecialFairyTheme
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SOpenSpecialFairyTheme.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SOpenSpecialFairyTheme";
        };

        return SOpenSpecialFairyTheme;
    })();

    game.CBuyPages = (function() {

        /**
         * Properties of a CBuyPages.
         * @memberof game
         * @interface ICBuyPages
         */

        /**
         * Constructs a new CBuyPages.
         * @memberof game
         * @classdesc Represents a CBuyPages.
         * @implements ICBuyPages
         * @constructor
         * @param {game.ICBuyPages=} [properties] Properties to set
         */
        function CBuyPages(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CBuyPages instance using the specified properties.
         * @function create
         * @memberof game.CBuyPages
         * @static
         * @param {game.ICBuyPages=} [properties] Properties to set
         * @returns {game.CBuyPages} CBuyPages instance
         */
        CBuyPages.create = function create(properties) {
            return new CBuyPages(properties);
        };

        /**
         * Encodes the specified CBuyPages message. Does not implicitly {@link game.CBuyPages.verify|verify} messages.
         * @function encode
         * @memberof game.CBuyPages
         * @static
         * @param {game.ICBuyPages} message CBuyPages message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CBuyPages.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CBuyPages message, length delimited. Does not implicitly {@link game.CBuyPages.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CBuyPages
         * @static
         * @param {game.ICBuyPages} message CBuyPages message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CBuyPages.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CBuyPages message from the specified reader or buffer.
         * @function decode
         * @memberof game.CBuyPages
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CBuyPages} CBuyPages
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CBuyPages.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CBuyPages();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CBuyPages message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CBuyPages
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CBuyPages} CBuyPages
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CBuyPages.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CBuyPages message.
         * @function verify
         * @memberof game.CBuyPages
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CBuyPages.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CBuyPages message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CBuyPages
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CBuyPages} CBuyPages
         */
        CBuyPages.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CBuyPages)
                return object;
            return new $root.game.CBuyPages();
        };

        /**
         * Creates a plain object from a CBuyPages message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CBuyPages
         * @static
         * @param {game.CBuyPages} message CBuyPages
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CBuyPages.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CBuyPages to JSON.
         * @function toJSON
         * @memberof game.CBuyPages
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CBuyPages.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CBuyPages
         * @function getTypeUrl
         * @memberof game.CBuyPages
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CBuyPages.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CBuyPages";
        };

        return CBuyPages;
    })();

    game.SBuyPages = (function() {

        /**
         * Properties of a SBuyPages.
         * @memberof game
         * @interface ISBuyPages
         * @property {Array.<common.IReward>|null} [costs] SBuyPages costs
         * @property {Array.<common.IReward>|null} [awards] SBuyPages awards
         * @property {number|null} [level] SBuyPages level
         */

        /**
         * Constructs a new SBuyPages.
         * @memberof game
         * @classdesc Represents a SBuyPages.
         * @implements ISBuyPages
         * @constructor
         * @param {game.ISBuyPages=} [properties] Properties to set
         */
        function SBuyPages(properties) {
            this.costs = [];
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SBuyPages costs.
         * @member {Array.<common.IReward>} costs
         * @memberof game.SBuyPages
         * @instance
         */
        SBuyPages.prototype.costs = $util.emptyArray;

        /**
         * SBuyPages awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SBuyPages
         * @instance
         */
        SBuyPages.prototype.awards = $util.emptyArray;

        /**
         * SBuyPages level.
         * @member {number} level
         * @memberof game.SBuyPages
         * @instance
         */
        SBuyPages.prototype.level = 0;

        /**
         * Creates a new SBuyPages instance using the specified properties.
         * @function create
         * @memberof game.SBuyPages
         * @static
         * @param {game.ISBuyPages=} [properties] Properties to set
         * @returns {game.SBuyPages} SBuyPages instance
         */
        SBuyPages.create = function create(properties) {
            return new SBuyPages(properties);
        };

        /**
         * Encodes the specified SBuyPages message. Does not implicitly {@link game.SBuyPages.verify|verify} messages.
         * @function encode
         * @memberof game.SBuyPages
         * @static
         * @param {game.ISBuyPages} message SBuyPages message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SBuyPages.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.costs != null && message.costs.length)
                for (var i = 0; i < message.costs.length; ++i)
                    $root.common.Reward.encode(message.costs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.level);
            return writer;
        };

        /**
         * Encodes the specified SBuyPages message, length delimited. Does not implicitly {@link game.SBuyPages.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SBuyPages
         * @static
         * @param {game.ISBuyPages} message SBuyPages message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SBuyPages.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SBuyPages message from the specified reader or buffer.
         * @function decode
         * @memberof game.SBuyPages
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SBuyPages} SBuyPages
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SBuyPages.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SBuyPages();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.costs && message.costs.length))
                            message.costs = [];
                        message.costs.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.level = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SBuyPages message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SBuyPages
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SBuyPages} SBuyPages
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SBuyPages.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SBuyPages message.
         * @function verify
         * @memberof game.SBuyPages
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SBuyPages.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.costs != null && message.hasOwnProperty("costs")) {
                if (!Array.isArray(message.costs))
                    return "costs: array expected";
                for (var i = 0; i < message.costs.length; ++i) {
                    var error = $root.common.Reward.verify(message.costs[i]);
                    if (error)
                        return "costs." + error;
                }
            }
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            return null;
        };

        /**
         * Creates a SBuyPages message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SBuyPages
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SBuyPages} SBuyPages
         */
        SBuyPages.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SBuyPages)
                return object;
            var message = new $root.game.SBuyPages();
            if (object.costs) {
                if (!Array.isArray(object.costs))
                    throw TypeError(".game.SBuyPages.costs: array expected");
                message.costs = [];
                for (var i = 0; i < object.costs.length; ++i) {
                    if (typeof object.costs[i] !== "object")
                        throw TypeError(".game.SBuyPages.costs: object expected");
                    message.costs[i] = $root.common.Reward.fromObject(object.costs[i]);
                }
            }
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SBuyPages.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SBuyPages.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            if (object.level != null)
                message.level = object.level | 0;
            return message;
        };

        /**
         * Creates a plain object from a SBuyPages message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SBuyPages
         * @static
         * @param {game.SBuyPages} message SBuyPages
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SBuyPages.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.costs = [];
                object.awards = [];
            }
            if (options.defaults)
                object.level = 0;
            if (message.costs && message.costs.length) {
                object.costs = [];
                for (var j = 0; j < message.costs.length; ++j)
                    object.costs[j] = $root.common.Reward.toObject(message.costs[j], options);
            }
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            return object;
        };

        /**
         * Converts this SBuyPages to JSON.
         * @function toJSON
         * @memberof game.SBuyPages
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SBuyPages.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SBuyPages
         * @function getTypeUrl
         * @memberof game.SBuyPages
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SBuyPages.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SBuyPages";
        };

        return SBuyPages;
    })();

    game.CGetFriendList = (function() {

        /**
         * Properties of a CGetFriendList.
         * @memberof game
         * @interface ICGetFriendList
         */

        /**
         * Constructs a new CGetFriendList.
         * @memberof game
         * @classdesc Represents a CGetFriendList.
         * @implements ICGetFriendList
         * @constructor
         * @param {game.ICGetFriendList=} [properties] Properties to set
         */
        function CGetFriendList(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CGetFriendList instance using the specified properties.
         * @function create
         * @memberof game.CGetFriendList
         * @static
         * @param {game.ICGetFriendList=} [properties] Properties to set
         * @returns {game.CGetFriendList} CGetFriendList instance
         */
        CGetFriendList.create = function create(properties) {
            return new CGetFriendList(properties);
        };

        /**
         * Encodes the specified CGetFriendList message. Does not implicitly {@link game.CGetFriendList.verify|verify} messages.
         * @function encode
         * @memberof game.CGetFriendList
         * @static
         * @param {game.ICGetFriendList} message CGetFriendList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetFriendList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CGetFriendList message, length delimited. Does not implicitly {@link game.CGetFriendList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetFriendList
         * @static
         * @param {game.ICGetFriendList} message CGetFriendList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetFriendList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetFriendList message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetFriendList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetFriendList} CGetFriendList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetFriendList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetFriendList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetFriendList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetFriendList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetFriendList} CGetFriendList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetFriendList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetFriendList message.
         * @function verify
         * @memberof game.CGetFriendList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetFriendList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CGetFriendList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetFriendList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetFriendList} CGetFriendList
         */
        CGetFriendList.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetFriendList)
                return object;
            return new $root.game.CGetFriendList();
        };

        /**
         * Creates a plain object from a CGetFriendList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetFriendList
         * @static
         * @param {game.CGetFriendList} message CGetFriendList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetFriendList.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CGetFriendList to JSON.
         * @function toJSON
         * @memberof game.CGetFriendList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetFriendList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetFriendList
         * @function getTypeUrl
         * @memberof game.CGetFriendList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetFriendList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetFriendList";
        };

        return CGetFriendList;
    })();

    game.SGetFriendList = (function() {

        /**
         * Properties of a SGetFriendList.
         * @memberof game
         * @interface ISGetFriendList
         * @property {Array.<common.IFriendUserInfo>|null} [list] SGetFriendList list
         */

        /**
         * Constructs a new SGetFriendList.
         * @memberof game
         * @classdesc Represents a SGetFriendList.
         * @implements ISGetFriendList
         * @constructor
         * @param {game.ISGetFriendList=} [properties] Properties to set
         */
        function SGetFriendList(properties) {
            this.list = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGetFriendList list.
         * @member {Array.<common.IFriendUserInfo>} list
         * @memberof game.SGetFriendList
         * @instance
         */
        SGetFriendList.prototype.list = $util.emptyArray;

        /**
         * Creates a new SGetFriendList instance using the specified properties.
         * @function create
         * @memberof game.SGetFriendList
         * @static
         * @param {game.ISGetFriendList=} [properties] Properties to set
         * @returns {game.SGetFriendList} SGetFriendList instance
         */
        SGetFriendList.create = function create(properties) {
            return new SGetFriendList(properties);
        };

        /**
         * Encodes the specified SGetFriendList message. Does not implicitly {@link game.SGetFriendList.verify|verify} messages.
         * @function encode
         * @memberof game.SGetFriendList
         * @static
         * @param {game.ISGetFriendList} message SGetFriendList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetFriendList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.list != null && message.list.length)
                for (var i = 0; i < message.list.length; ++i)
                    $root.common.FriendUserInfo.encode(message.list[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SGetFriendList message, length delimited. Does not implicitly {@link game.SGetFriendList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetFriendList
         * @static
         * @param {game.ISGetFriendList} message SGetFriendList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetFriendList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetFriendList message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetFriendList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetFriendList} SGetFriendList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetFriendList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetFriendList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.list && message.list.length))
                            message.list = [];
                        message.list.push($root.common.FriendUserInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetFriendList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetFriendList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetFriendList} SGetFriendList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetFriendList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetFriendList message.
         * @function verify
         * @memberof game.SGetFriendList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetFriendList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.list != null && message.hasOwnProperty("list")) {
                if (!Array.isArray(message.list))
                    return "list: array expected";
                for (var i = 0; i < message.list.length; ++i) {
                    var error = $root.common.FriendUserInfo.verify(message.list[i]);
                    if (error)
                        return "list." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SGetFriendList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetFriendList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetFriendList} SGetFriendList
         */
        SGetFriendList.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetFriendList)
                return object;
            var message = new $root.game.SGetFriendList();
            if (object.list) {
                if (!Array.isArray(object.list))
                    throw TypeError(".game.SGetFriendList.list: array expected");
                message.list = [];
                for (var i = 0; i < object.list.length; ++i) {
                    if (typeof object.list[i] !== "object")
                        throw TypeError(".game.SGetFriendList.list: object expected");
                    message.list[i] = $root.common.FriendUserInfo.fromObject(object.list[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SGetFriendList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetFriendList
         * @static
         * @param {game.SGetFriendList} message SGetFriendList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetFriendList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.list = [];
            if (message.list && message.list.length) {
                object.list = [];
                for (var j = 0; j < message.list.length; ++j)
                    object.list[j] = $root.common.FriendUserInfo.toObject(message.list[j], options);
            }
            return object;
        };

        /**
         * Converts this SGetFriendList to JSON.
         * @function toJSON
         * @memberof game.SGetFriendList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetFriendList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetFriendList
         * @function getTypeUrl
         * @memberof game.SGetFriendList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetFriendList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetFriendList";
        };

        return SGetFriendList;
    })();

    game.CGetApplyList = (function() {

        /**
         * Properties of a CGetApplyList.
         * @memberof game
         * @interface ICGetApplyList
         */

        /**
         * Constructs a new CGetApplyList.
         * @memberof game
         * @classdesc Represents a CGetApplyList.
         * @implements ICGetApplyList
         * @constructor
         * @param {game.ICGetApplyList=} [properties] Properties to set
         */
        function CGetApplyList(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CGetApplyList instance using the specified properties.
         * @function create
         * @memberof game.CGetApplyList
         * @static
         * @param {game.ICGetApplyList=} [properties] Properties to set
         * @returns {game.CGetApplyList} CGetApplyList instance
         */
        CGetApplyList.create = function create(properties) {
            return new CGetApplyList(properties);
        };

        /**
         * Encodes the specified CGetApplyList message. Does not implicitly {@link game.CGetApplyList.verify|verify} messages.
         * @function encode
         * @memberof game.CGetApplyList
         * @static
         * @param {game.ICGetApplyList} message CGetApplyList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetApplyList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CGetApplyList message, length delimited. Does not implicitly {@link game.CGetApplyList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetApplyList
         * @static
         * @param {game.ICGetApplyList} message CGetApplyList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetApplyList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetApplyList message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetApplyList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetApplyList} CGetApplyList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetApplyList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetApplyList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetApplyList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetApplyList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetApplyList} CGetApplyList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetApplyList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetApplyList message.
         * @function verify
         * @memberof game.CGetApplyList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetApplyList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CGetApplyList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetApplyList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetApplyList} CGetApplyList
         */
        CGetApplyList.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetApplyList)
                return object;
            return new $root.game.CGetApplyList();
        };

        /**
         * Creates a plain object from a CGetApplyList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetApplyList
         * @static
         * @param {game.CGetApplyList} message CGetApplyList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetApplyList.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CGetApplyList to JSON.
         * @function toJSON
         * @memberof game.CGetApplyList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetApplyList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetApplyList
         * @function getTypeUrl
         * @memberof game.CGetApplyList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetApplyList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetApplyList";
        };

        return CGetApplyList;
    })();

    game.SGetApplyList = (function() {

        /**
         * Properties of a SGetApplyList.
         * @memberof game
         * @interface ISGetApplyList
         * @property {Array.<common.IApplyInfo>|null} [list] SGetApplyList list
         */

        /**
         * Constructs a new SGetApplyList.
         * @memberof game
         * @classdesc Represents a SGetApplyList.
         * @implements ISGetApplyList
         * @constructor
         * @param {game.ISGetApplyList=} [properties] Properties to set
         */
        function SGetApplyList(properties) {
            this.list = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGetApplyList list.
         * @member {Array.<common.IApplyInfo>} list
         * @memberof game.SGetApplyList
         * @instance
         */
        SGetApplyList.prototype.list = $util.emptyArray;

        /**
         * Creates a new SGetApplyList instance using the specified properties.
         * @function create
         * @memberof game.SGetApplyList
         * @static
         * @param {game.ISGetApplyList=} [properties] Properties to set
         * @returns {game.SGetApplyList} SGetApplyList instance
         */
        SGetApplyList.create = function create(properties) {
            return new SGetApplyList(properties);
        };

        /**
         * Encodes the specified SGetApplyList message. Does not implicitly {@link game.SGetApplyList.verify|verify} messages.
         * @function encode
         * @memberof game.SGetApplyList
         * @static
         * @param {game.ISGetApplyList} message SGetApplyList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetApplyList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.list != null && message.list.length)
                for (var i = 0; i < message.list.length; ++i)
                    $root.common.ApplyInfo.encode(message.list[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SGetApplyList message, length delimited. Does not implicitly {@link game.SGetApplyList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetApplyList
         * @static
         * @param {game.ISGetApplyList} message SGetApplyList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetApplyList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetApplyList message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetApplyList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetApplyList} SGetApplyList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetApplyList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetApplyList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.list && message.list.length))
                            message.list = [];
                        message.list.push($root.common.ApplyInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetApplyList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetApplyList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetApplyList} SGetApplyList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetApplyList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetApplyList message.
         * @function verify
         * @memberof game.SGetApplyList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetApplyList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.list != null && message.hasOwnProperty("list")) {
                if (!Array.isArray(message.list))
                    return "list: array expected";
                for (var i = 0; i < message.list.length; ++i) {
                    var error = $root.common.ApplyInfo.verify(message.list[i]);
                    if (error)
                        return "list." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SGetApplyList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetApplyList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetApplyList} SGetApplyList
         */
        SGetApplyList.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetApplyList)
                return object;
            var message = new $root.game.SGetApplyList();
            if (object.list) {
                if (!Array.isArray(object.list))
                    throw TypeError(".game.SGetApplyList.list: array expected");
                message.list = [];
                for (var i = 0; i < object.list.length; ++i) {
                    if (typeof object.list[i] !== "object")
                        throw TypeError(".game.SGetApplyList.list: object expected");
                    message.list[i] = $root.common.ApplyInfo.fromObject(object.list[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SGetApplyList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetApplyList
         * @static
         * @param {game.SGetApplyList} message SGetApplyList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetApplyList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.list = [];
            if (message.list && message.list.length) {
                object.list = [];
                for (var j = 0; j < message.list.length; ++j)
                    object.list[j] = $root.common.ApplyInfo.toObject(message.list[j], options);
            }
            return object;
        };

        /**
         * Converts this SGetApplyList to JSON.
         * @function toJSON
         * @memberof game.SGetApplyList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetApplyList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetApplyList
         * @function getTypeUrl
         * @memberof game.SGetApplyList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetApplyList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetApplyList";
        };

        return SGetApplyList;
    })();

    game.CAddFriend = (function() {

        /**
         * Properties of a CAddFriend.
         * @memberof game
         * @interface ICAddFriend
         * @property {string|null} [target] CAddFriend target
         * @property {common.IFriendUserInfo|null} [user] CAddFriend user
         * @property {string|null} [extra] CAddFriend extra
         */

        /**
         * Constructs a new CAddFriend.
         * @memberof game
         * @classdesc Represents a CAddFriend.
         * @implements ICAddFriend
         * @constructor
         * @param {game.ICAddFriend=} [properties] Properties to set
         */
        function CAddFriend(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CAddFriend target.
         * @member {string} target
         * @memberof game.CAddFriend
         * @instance
         */
        CAddFriend.prototype.target = "";

        /**
         * CAddFriend user.
         * @member {common.IFriendUserInfo|null|undefined} user
         * @memberof game.CAddFriend
         * @instance
         */
        CAddFriend.prototype.user = null;

        /**
         * CAddFriend extra.
         * @member {string} extra
         * @memberof game.CAddFriend
         * @instance
         */
        CAddFriend.prototype.extra = "";

        /**
         * Creates a new CAddFriend instance using the specified properties.
         * @function create
         * @memberof game.CAddFriend
         * @static
         * @param {game.ICAddFriend=} [properties] Properties to set
         * @returns {game.CAddFriend} CAddFriend instance
         */
        CAddFriend.create = function create(properties) {
            return new CAddFriend(properties);
        };

        /**
         * Encodes the specified CAddFriend message. Does not implicitly {@link game.CAddFriend.verify|verify} messages.
         * @function encode
         * @memberof game.CAddFriend
         * @static
         * @param {game.ICAddFriend} message CAddFriend message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CAddFriend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.target);
            if (message.user != null && Object.hasOwnProperty.call(message, "user"))
                $root.common.FriendUserInfo.encode(message.user, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.extra != null && Object.hasOwnProperty.call(message, "extra"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.extra);
            return writer;
        };

        /**
         * Encodes the specified CAddFriend message, length delimited. Does not implicitly {@link game.CAddFriend.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CAddFriend
         * @static
         * @param {game.ICAddFriend} message CAddFriend message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CAddFriend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CAddFriend message from the specified reader or buffer.
         * @function decode
         * @memberof game.CAddFriend
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CAddFriend} CAddFriend
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CAddFriend.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CAddFriend();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.target = reader.string();
                        break;
                    }
                case 2: {
                        message.user = $root.common.FriendUserInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.extra = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CAddFriend message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CAddFriend
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CAddFriend} CAddFriend
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CAddFriend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CAddFriend message.
         * @function verify
         * @memberof game.CAddFriend
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CAddFriend.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.target != null && message.hasOwnProperty("target"))
                if (!$util.isString(message.target))
                    return "target: string expected";
            if (message.user != null && message.hasOwnProperty("user")) {
                var error = $root.common.FriendUserInfo.verify(message.user);
                if (error)
                    return "user." + error;
            }
            if (message.extra != null && message.hasOwnProperty("extra"))
                if (!$util.isString(message.extra))
                    return "extra: string expected";
            return null;
        };

        /**
         * Creates a CAddFriend message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CAddFriend
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CAddFriend} CAddFriend
         */
        CAddFriend.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CAddFriend)
                return object;
            var message = new $root.game.CAddFriend();
            if (object.target != null)
                message.target = String(object.target);
            if (object.user != null) {
                if (typeof object.user !== "object")
                    throw TypeError(".game.CAddFriend.user: object expected");
                message.user = $root.common.FriendUserInfo.fromObject(object.user);
            }
            if (object.extra != null)
                message.extra = String(object.extra);
            return message;
        };

        /**
         * Creates a plain object from a CAddFriend message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CAddFriend
         * @static
         * @param {game.CAddFriend} message CAddFriend
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CAddFriend.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.target = "";
                object.user = null;
                object.extra = "";
            }
            if (message.target != null && message.hasOwnProperty("target"))
                object.target = message.target;
            if (message.user != null && message.hasOwnProperty("user"))
                object.user = $root.common.FriendUserInfo.toObject(message.user, options);
            if (message.extra != null && message.hasOwnProperty("extra"))
                object.extra = message.extra;
            return object;
        };

        /**
         * Converts this CAddFriend to JSON.
         * @function toJSON
         * @memberof game.CAddFriend
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CAddFriend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CAddFriend
         * @function getTypeUrl
         * @memberof game.CAddFriend
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CAddFriend.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CAddFriend";
        };

        return CAddFriend;
    })();

    game.SAddFriend = (function() {

        /**
         * Properties of a SAddFriend.
         * @memberof game
         * @interface ISAddFriend
         */

        /**
         * Constructs a new SAddFriend.
         * @memberof game
         * @classdesc Represents a SAddFriend.
         * @implements ISAddFriend
         * @constructor
         * @param {game.ISAddFriend=} [properties] Properties to set
         */
        function SAddFriend(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SAddFriend instance using the specified properties.
         * @function create
         * @memberof game.SAddFriend
         * @static
         * @param {game.ISAddFriend=} [properties] Properties to set
         * @returns {game.SAddFriend} SAddFriend instance
         */
        SAddFriend.create = function create(properties) {
            return new SAddFriend(properties);
        };

        /**
         * Encodes the specified SAddFriend message. Does not implicitly {@link game.SAddFriend.verify|verify} messages.
         * @function encode
         * @memberof game.SAddFriend
         * @static
         * @param {game.ISAddFriend} message SAddFriend message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SAddFriend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SAddFriend message, length delimited. Does not implicitly {@link game.SAddFriend.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SAddFriend
         * @static
         * @param {game.ISAddFriend} message SAddFriend message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SAddFriend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SAddFriend message from the specified reader or buffer.
         * @function decode
         * @memberof game.SAddFriend
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SAddFriend} SAddFriend
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SAddFriend.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SAddFriend();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SAddFriend message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SAddFriend
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SAddFriend} SAddFriend
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SAddFriend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SAddFriend message.
         * @function verify
         * @memberof game.SAddFriend
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SAddFriend.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SAddFriend message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SAddFriend
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SAddFriend} SAddFriend
         */
        SAddFriend.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SAddFriend)
                return object;
            return new $root.game.SAddFriend();
        };

        /**
         * Creates a plain object from a SAddFriend message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SAddFriend
         * @static
         * @param {game.SAddFriend} message SAddFriend
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SAddFriend.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SAddFriend to JSON.
         * @function toJSON
         * @memberof game.SAddFriend
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SAddFriend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SAddFriend
         * @function getTypeUrl
         * @memberof game.SAddFriend
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SAddFriend.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SAddFriend";
        };

        return SAddFriend;
    })();

    game.CRemoveFriend = (function() {

        /**
         * Properties of a CRemoveFriend.
         * @memberof game
         * @interface ICRemoveFriend
         * @property {string|null} [target] CRemoveFriend target
         */

        /**
         * Constructs a new CRemoveFriend.
         * @memberof game
         * @classdesc Represents a CRemoveFriend.
         * @implements ICRemoveFriend
         * @constructor
         * @param {game.ICRemoveFriend=} [properties] Properties to set
         */
        function CRemoveFriend(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CRemoveFriend target.
         * @member {string} target
         * @memberof game.CRemoveFriend
         * @instance
         */
        CRemoveFriend.prototype.target = "";

        /**
         * Creates a new CRemoveFriend instance using the specified properties.
         * @function create
         * @memberof game.CRemoveFriend
         * @static
         * @param {game.ICRemoveFriend=} [properties] Properties to set
         * @returns {game.CRemoveFriend} CRemoveFriend instance
         */
        CRemoveFriend.create = function create(properties) {
            return new CRemoveFriend(properties);
        };

        /**
         * Encodes the specified CRemoveFriend message. Does not implicitly {@link game.CRemoveFriend.verify|verify} messages.
         * @function encode
         * @memberof game.CRemoveFriend
         * @static
         * @param {game.ICRemoveFriend} message CRemoveFriend message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CRemoveFriend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.target);
            return writer;
        };

        /**
         * Encodes the specified CRemoveFriend message, length delimited. Does not implicitly {@link game.CRemoveFriend.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CRemoveFriend
         * @static
         * @param {game.ICRemoveFriend} message CRemoveFriend message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CRemoveFriend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CRemoveFriend message from the specified reader or buffer.
         * @function decode
         * @memberof game.CRemoveFriend
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CRemoveFriend} CRemoveFriend
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CRemoveFriend.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CRemoveFriend();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.target = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CRemoveFriend message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CRemoveFriend
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CRemoveFriend} CRemoveFriend
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CRemoveFriend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CRemoveFriend message.
         * @function verify
         * @memberof game.CRemoveFriend
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CRemoveFriend.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.target != null && message.hasOwnProperty("target"))
                if (!$util.isString(message.target))
                    return "target: string expected";
            return null;
        };

        /**
         * Creates a CRemoveFriend message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CRemoveFriend
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CRemoveFriend} CRemoveFriend
         */
        CRemoveFriend.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CRemoveFriend)
                return object;
            var message = new $root.game.CRemoveFriend();
            if (object.target != null)
                message.target = String(object.target);
            return message;
        };

        /**
         * Creates a plain object from a CRemoveFriend message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CRemoveFriend
         * @static
         * @param {game.CRemoveFriend} message CRemoveFriend
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CRemoveFriend.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.target = "";
            if (message.target != null && message.hasOwnProperty("target"))
                object.target = message.target;
            return object;
        };

        /**
         * Converts this CRemoveFriend to JSON.
         * @function toJSON
         * @memberof game.CRemoveFriend
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CRemoveFriend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CRemoveFriend
         * @function getTypeUrl
         * @memberof game.CRemoveFriend
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CRemoveFriend.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CRemoveFriend";
        };

        return CRemoveFriend;
    })();

    game.SRemoveFriend = (function() {

        /**
         * Properties of a SRemoveFriend.
         * @memberof game
         * @interface ISRemoveFriend
         */

        /**
         * Constructs a new SRemoveFriend.
         * @memberof game
         * @classdesc Represents a SRemoveFriend.
         * @implements ISRemoveFriend
         * @constructor
         * @param {game.ISRemoveFriend=} [properties] Properties to set
         */
        function SRemoveFriend(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SRemoveFriend instance using the specified properties.
         * @function create
         * @memberof game.SRemoveFriend
         * @static
         * @param {game.ISRemoveFriend=} [properties] Properties to set
         * @returns {game.SRemoveFriend} SRemoveFriend instance
         */
        SRemoveFriend.create = function create(properties) {
            return new SRemoveFriend(properties);
        };

        /**
         * Encodes the specified SRemoveFriend message. Does not implicitly {@link game.SRemoveFriend.verify|verify} messages.
         * @function encode
         * @memberof game.SRemoveFriend
         * @static
         * @param {game.ISRemoveFriend} message SRemoveFriend message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SRemoveFriend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SRemoveFriend message, length delimited. Does not implicitly {@link game.SRemoveFriend.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SRemoveFriend
         * @static
         * @param {game.ISRemoveFriend} message SRemoveFriend message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SRemoveFriend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SRemoveFriend message from the specified reader or buffer.
         * @function decode
         * @memberof game.SRemoveFriend
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SRemoveFriend} SRemoveFriend
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SRemoveFriend.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SRemoveFriend();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SRemoveFriend message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SRemoveFriend
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SRemoveFriend} SRemoveFriend
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SRemoveFriend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SRemoveFriend message.
         * @function verify
         * @memberof game.SRemoveFriend
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SRemoveFriend.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SRemoveFriend message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SRemoveFriend
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SRemoveFriend} SRemoveFriend
         */
        SRemoveFriend.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SRemoveFriend)
                return object;
            return new $root.game.SRemoveFriend();
        };

        /**
         * Creates a plain object from a SRemoveFriend message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SRemoveFriend
         * @static
         * @param {game.SRemoveFriend} message SRemoveFriend
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SRemoveFriend.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SRemoveFriend to JSON.
         * @function toJSON
         * @memberof game.SRemoveFriend
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SRemoveFriend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SRemoveFriend
         * @function getTypeUrl
         * @memberof game.SRemoveFriend
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SRemoveFriend.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SRemoveFriend";
        };

        return SRemoveFriend;
    })();

    game.CReplyAdd = (function() {

        /**
         * Properties of a CReplyAdd.
         * @memberof game
         * @interface ICReplyAdd
         * @property {string|null} [target] CReplyAdd target
         * @property {number|null} [result] CReplyAdd result
         * @property {common.IFriendUserInfo|null} [user] CReplyAdd user
         */

        /**
         * Constructs a new CReplyAdd.
         * @memberof game
         * @classdesc Represents a CReplyAdd.
         * @implements ICReplyAdd
         * @constructor
         * @param {game.ICReplyAdd=} [properties] Properties to set
         */
        function CReplyAdd(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CReplyAdd target.
         * @member {string} target
         * @memberof game.CReplyAdd
         * @instance
         */
        CReplyAdd.prototype.target = "";

        /**
         * CReplyAdd result.
         * @member {number} result
         * @memberof game.CReplyAdd
         * @instance
         */
        CReplyAdd.prototype.result = 0;

        /**
         * CReplyAdd user.
         * @member {common.IFriendUserInfo|null|undefined} user
         * @memberof game.CReplyAdd
         * @instance
         */
        CReplyAdd.prototype.user = null;

        /**
         * Creates a new CReplyAdd instance using the specified properties.
         * @function create
         * @memberof game.CReplyAdd
         * @static
         * @param {game.ICReplyAdd=} [properties] Properties to set
         * @returns {game.CReplyAdd} CReplyAdd instance
         */
        CReplyAdd.create = function create(properties) {
            return new CReplyAdd(properties);
        };

        /**
         * Encodes the specified CReplyAdd message. Does not implicitly {@link game.CReplyAdd.verify|verify} messages.
         * @function encode
         * @memberof game.CReplyAdd
         * @static
         * @param {game.ICReplyAdd} message CReplyAdd message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CReplyAdd.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.target);
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.result);
            if (message.user != null && Object.hasOwnProperty.call(message, "user"))
                $root.common.FriendUserInfo.encode(message.user, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CReplyAdd message, length delimited. Does not implicitly {@link game.CReplyAdd.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CReplyAdd
         * @static
         * @param {game.ICReplyAdd} message CReplyAdd message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CReplyAdd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CReplyAdd message from the specified reader or buffer.
         * @function decode
         * @memberof game.CReplyAdd
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CReplyAdd} CReplyAdd
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CReplyAdd.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CReplyAdd();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.target = reader.string();
                        break;
                    }
                case 2: {
                        message.result = reader.uint32();
                        break;
                    }
                case 3: {
                        message.user = $root.common.FriendUserInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CReplyAdd message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CReplyAdd
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CReplyAdd} CReplyAdd
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CReplyAdd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CReplyAdd message.
         * @function verify
         * @memberof game.CReplyAdd
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CReplyAdd.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.target != null && message.hasOwnProperty("target"))
                if (!$util.isString(message.target))
                    return "target: string expected";
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isInteger(message.result))
                    return "result: integer expected";
            if (message.user != null && message.hasOwnProperty("user")) {
                var error = $root.common.FriendUserInfo.verify(message.user);
                if (error)
                    return "user." + error;
            }
            return null;
        };

        /**
         * Creates a CReplyAdd message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CReplyAdd
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CReplyAdd} CReplyAdd
         */
        CReplyAdd.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CReplyAdd)
                return object;
            var message = new $root.game.CReplyAdd();
            if (object.target != null)
                message.target = String(object.target);
            if (object.result != null)
                message.result = object.result >>> 0;
            if (object.user != null) {
                if (typeof object.user !== "object")
                    throw TypeError(".game.CReplyAdd.user: object expected");
                message.user = $root.common.FriendUserInfo.fromObject(object.user);
            }
            return message;
        };

        /**
         * Creates a plain object from a CReplyAdd message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CReplyAdd
         * @static
         * @param {game.CReplyAdd} message CReplyAdd
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CReplyAdd.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.target = "";
                object.result = 0;
                object.user = null;
            }
            if (message.target != null && message.hasOwnProperty("target"))
                object.target = message.target;
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            if (message.user != null && message.hasOwnProperty("user"))
                object.user = $root.common.FriendUserInfo.toObject(message.user, options);
            return object;
        };

        /**
         * Converts this CReplyAdd to JSON.
         * @function toJSON
         * @memberof game.CReplyAdd
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CReplyAdd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CReplyAdd
         * @function getTypeUrl
         * @memberof game.CReplyAdd
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CReplyAdd.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CReplyAdd";
        };

        return CReplyAdd;
    })();

    game.SReplyAdd = (function() {

        /**
         * Properties of a SReplyAdd.
         * @memberof game
         * @interface ISReplyAdd
         */

        /**
         * Constructs a new SReplyAdd.
         * @memberof game
         * @classdesc Represents a SReplyAdd.
         * @implements ISReplyAdd
         * @constructor
         * @param {game.ISReplyAdd=} [properties] Properties to set
         */
        function SReplyAdd(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SReplyAdd instance using the specified properties.
         * @function create
         * @memberof game.SReplyAdd
         * @static
         * @param {game.ISReplyAdd=} [properties] Properties to set
         * @returns {game.SReplyAdd} SReplyAdd instance
         */
        SReplyAdd.create = function create(properties) {
            return new SReplyAdd(properties);
        };

        /**
         * Encodes the specified SReplyAdd message. Does not implicitly {@link game.SReplyAdd.verify|verify} messages.
         * @function encode
         * @memberof game.SReplyAdd
         * @static
         * @param {game.ISReplyAdd} message SReplyAdd message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SReplyAdd.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SReplyAdd message, length delimited. Does not implicitly {@link game.SReplyAdd.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SReplyAdd
         * @static
         * @param {game.ISReplyAdd} message SReplyAdd message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SReplyAdd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SReplyAdd message from the specified reader or buffer.
         * @function decode
         * @memberof game.SReplyAdd
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SReplyAdd} SReplyAdd
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SReplyAdd.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SReplyAdd();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SReplyAdd message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SReplyAdd
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SReplyAdd} SReplyAdd
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SReplyAdd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SReplyAdd message.
         * @function verify
         * @memberof game.SReplyAdd
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SReplyAdd.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SReplyAdd message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SReplyAdd
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SReplyAdd} SReplyAdd
         */
        SReplyAdd.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SReplyAdd)
                return object;
            return new $root.game.SReplyAdd();
        };

        /**
         * Creates a plain object from a SReplyAdd message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SReplyAdd
         * @static
         * @param {game.SReplyAdd} message SReplyAdd
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SReplyAdd.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SReplyAdd to JSON.
         * @function toJSON
         * @memberof game.SReplyAdd
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SReplyAdd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SReplyAdd
         * @function getTypeUrl
         * @memberof game.SReplyAdd
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SReplyAdd.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SReplyAdd";
        };

        return SReplyAdd;
    })();

    game.CKickPlayers = (function() {

        /**
         * Properties of a CKickPlayers.
         * @memberof game
         * @interface ICKickPlayers
         * @property {Array.<number|Long>|null} [playerIds] CKickPlayers playerIds
         * @property {common.KickPlayerReason|null} [reason] CKickPlayers reason
         */

        /**
         * Constructs a new CKickPlayers.
         * @memberof game
         * @classdesc Represents a CKickPlayers.
         * @implements ICKickPlayers
         * @constructor
         * @param {game.ICKickPlayers=} [properties] Properties to set
         */
        function CKickPlayers(properties) {
            this.playerIds = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CKickPlayers playerIds.
         * @member {Array.<number|Long>} playerIds
         * @memberof game.CKickPlayers
         * @instance
         */
        CKickPlayers.prototype.playerIds = $util.emptyArray;

        /**
         * CKickPlayers reason.
         * @member {common.KickPlayerReason} reason
         * @memberof game.CKickPlayers
         * @instance
         */
        CKickPlayers.prototype.reason = 0;

        /**
         * Creates a new CKickPlayers instance using the specified properties.
         * @function create
         * @memberof game.CKickPlayers
         * @static
         * @param {game.ICKickPlayers=} [properties] Properties to set
         * @returns {game.CKickPlayers} CKickPlayers instance
         */
        CKickPlayers.create = function create(properties) {
            return new CKickPlayers(properties);
        };

        /**
         * Encodes the specified CKickPlayers message. Does not implicitly {@link game.CKickPlayers.verify|verify} messages.
         * @function encode
         * @memberof game.CKickPlayers
         * @static
         * @param {game.ICKickPlayers} message CKickPlayers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CKickPlayers.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.playerIds != null && message.playerIds.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (var i = 0; i < message.playerIds.length; ++i)
                    writer.int64(message.playerIds[i]);
                writer.ldelim();
            }
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.reason);
            return writer;
        };

        /**
         * Encodes the specified CKickPlayers message, length delimited. Does not implicitly {@link game.CKickPlayers.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CKickPlayers
         * @static
         * @param {game.ICKickPlayers} message CKickPlayers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CKickPlayers.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CKickPlayers message from the specified reader or buffer.
         * @function decode
         * @memberof game.CKickPlayers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CKickPlayers} CKickPlayers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CKickPlayers.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CKickPlayers();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.playerIds && message.playerIds.length))
                            message.playerIds = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.playerIds.push(reader.int64());
                        } else
                            message.playerIds.push(reader.int64());
                        break;
                    }
                case 2: {
                        message.reason = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CKickPlayers message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CKickPlayers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CKickPlayers} CKickPlayers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CKickPlayers.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CKickPlayers message.
         * @function verify
         * @memberof game.CKickPlayers
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CKickPlayers.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.playerIds != null && message.hasOwnProperty("playerIds")) {
                if (!Array.isArray(message.playerIds))
                    return "playerIds: array expected";
                for (var i = 0; i < message.playerIds.length; ++i)
                    if (!$util.isInteger(message.playerIds[i]) && !(message.playerIds[i] && $util.isInteger(message.playerIds[i].low) && $util.isInteger(message.playerIds[i].high)))
                        return "playerIds: integer|Long[] expected";
            }
            if (message.reason != null && message.hasOwnProperty("reason"))
                switch (message.reason) {
                default:
                    return "reason: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            return null;
        };

        /**
         * Creates a CKickPlayers message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CKickPlayers
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CKickPlayers} CKickPlayers
         */
        CKickPlayers.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CKickPlayers)
                return object;
            var message = new $root.game.CKickPlayers();
            if (object.playerIds) {
                if (!Array.isArray(object.playerIds))
                    throw TypeError(".game.CKickPlayers.playerIds: array expected");
                message.playerIds = [];
                for (var i = 0; i < object.playerIds.length; ++i)
                    if ($util.Long)
                        (message.playerIds[i] = $util.Long.fromValue(object.playerIds[i])).unsigned = false;
                    else if (typeof object.playerIds[i] === "string")
                        message.playerIds[i] = parseInt(object.playerIds[i], 10);
                    else if (typeof object.playerIds[i] === "number")
                        message.playerIds[i] = object.playerIds[i];
                    else if (typeof object.playerIds[i] === "object")
                        message.playerIds[i] = new $util.LongBits(object.playerIds[i].low >>> 0, object.playerIds[i].high >>> 0).toNumber();
            }
            switch (object.reason) {
            default:
                if (typeof object.reason === "number") {
                    message.reason = object.reason;
                    break;
                }
                break;
            case "ClientLeave":
            case 0:
                message.reason = 0;
                break;
            case "DuplicateLogin":
            case 1:
                message.reason = 1;
                break;
            case "ServerShutdown":
            case 2:
                message.reason = 2;
                break;
            case "GMKick":
            case 3:
                message.reason = 3;
                break;
            case "Banned":
            case 4:
                message.reason = 4;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a CKickPlayers message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CKickPlayers
         * @static
         * @param {game.CKickPlayers} message CKickPlayers
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CKickPlayers.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.playerIds = [];
            if (options.defaults)
                object.reason = options.enums === String ? "ClientLeave" : 0;
            if (message.playerIds && message.playerIds.length) {
                object.playerIds = [];
                for (var j = 0; j < message.playerIds.length; ++j)
                    if (typeof message.playerIds[j] === "number")
                        object.playerIds[j] = options.longs === String ? String(message.playerIds[j]) : message.playerIds[j];
                    else
                        object.playerIds[j] = options.longs === String ? $util.Long.prototype.toString.call(message.playerIds[j]) : options.longs === Number ? new $util.LongBits(message.playerIds[j].low >>> 0, message.playerIds[j].high >>> 0).toNumber() : message.playerIds[j];
            }
            if (message.reason != null && message.hasOwnProperty("reason"))
                object.reason = options.enums === String ? $root.common.KickPlayerReason[message.reason] === undefined ? message.reason : $root.common.KickPlayerReason[message.reason] : message.reason;
            return object;
        };

        /**
         * Converts this CKickPlayers to JSON.
         * @function toJSON
         * @memberof game.CKickPlayers
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CKickPlayers.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CKickPlayers
         * @function getTypeUrl
         * @memberof game.CKickPlayers
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CKickPlayers.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CKickPlayers";
        };

        return CKickPlayers;
    })();

    game.SKickPlayers = (function() {

        /**
         * Properties of a SKickPlayers.
         * @memberof game
         * @interface ISKickPlayers
         */

        /**
         * Constructs a new SKickPlayers.
         * @memberof game
         * @classdesc Represents a SKickPlayers.
         * @implements ISKickPlayers
         * @constructor
         * @param {game.ISKickPlayers=} [properties] Properties to set
         */
        function SKickPlayers(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SKickPlayers instance using the specified properties.
         * @function create
         * @memberof game.SKickPlayers
         * @static
         * @param {game.ISKickPlayers=} [properties] Properties to set
         * @returns {game.SKickPlayers} SKickPlayers instance
         */
        SKickPlayers.create = function create(properties) {
            return new SKickPlayers(properties);
        };

        /**
         * Encodes the specified SKickPlayers message. Does not implicitly {@link game.SKickPlayers.verify|verify} messages.
         * @function encode
         * @memberof game.SKickPlayers
         * @static
         * @param {game.ISKickPlayers} message SKickPlayers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SKickPlayers.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SKickPlayers message, length delimited. Does not implicitly {@link game.SKickPlayers.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SKickPlayers
         * @static
         * @param {game.ISKickPlayers} message SKickPlayers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SKickPlayers.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SKickPlayers message from the specified reader or buffer.
         * @function decode
         * @memberof game.SKickPlayers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SKickPlayers} SKickPlayers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SKickPlayers.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SKickPlayers();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SKickPlayers message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SKickPlayers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SKickPlayers} SKickPlayers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SKickPlayers.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SKickPlayers message.
         * @function verify
         * @memberof game.SKickPlayers
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SKickPlayers.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SKickPlayers message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SKickPlayers
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SKickPlayers} SKickPlayers
         */
        SKickPlayers.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SKickPlayers)
                return object;
            return new $root.game.SKickPlayers();
        };

        /**
         * Creates a plain object from a SKickPlayers message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SKickPlayers
         * @static
         * @param {game.SKickPlayers} message SKickPlayers
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SKickPlayers.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SKickPlayers to JSON.
         * @function toJSON
         * @memberof game.SKickPlayers
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SKickPlayers.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SKickPlayers
         * @function getTypeUrl
         * @memberof game.SKickPlayers
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SKickPlayers.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SKickPlayers";
        };

        return SKickPlayers;
    })();

    game.CKickAllPlayers = (function() {

        /**
         * Properties of a CKickAllPlayers.
         * @memberof game
         * @interface ICKickAllPlayers
         * @property {common.KickPlayerReason|null} [reason] CKickAllPlayers reason
         */

        /**
         * Constructs a new CKickAllPlayers.
         * @memberof game
         * @classdesc Represents a CKickAllPlayers.
         * @implements ICKickAllPlayers
         * @constructor
         * @param {game.ICKickAllPlayers=} [properties] Properties to set
         */
        function CKickAllPlayers(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CKickAllPlayers reason.
         * @member {common.KickPlayerReason} reason
         * @memberof game.CKickAllPlayers
         * @instance
         */
        CKickAllPlayers.prototype.reason = 0;

        /**
         * Creates a new CKickAllPlayers instance using the specified properties.
         * @function create
         * @memberof game.CKickAllPlayers
         * @static
         * @param {game.ICKickAllPlayers=} [properties] Properties to set
         * @returns {game.CKickAllPlayers} CKickAllPlayers instance
         */
        CKickAllPlayers.create = function create(properties) {
            return new CKickAllPlayers(properties);
        };

        /**
         * Encodes the specified CKickAllPlayers message. Does not implicitly {@link game.CKickAllPlayers.verify|verify} messages.
         * @function encode
         * @memberof game.CKickAllPlayers
         * @static
         * @param {game.ICKickAllPlayers} message CKickAllPlayers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CKickAllPlayers.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.reason);
            return writer;
        };

        /**
         * Encodes the specified CKickAllPlayers message, length delimited. Does not implicitly {@link game.CKickAllPlayers.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CKickAllPlayers
         * @static
         * @param {game.ICKickAllPlayers} message CKickAllPlayers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CKickAllPlayers.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CKickAllPlayers message from the specified reader or buffer.
         * @function decode
         * @memberof game.CKickAllPlayers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CKickAllPlayers} CKickAllPlayers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CKickAllPlayers.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CKickAllPlayers();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.reason = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CKickAllPlayers message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CKickAllPlayers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CKickAllPlayers} CKickAllPlayers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CKickAllPlayers.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CKickAllPlayers message.
         * @function verify
         * @memberof game.CKickAllPlayers
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CKickAllPlayers.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.reason != null && message.hasOwnProperty("reason"))
                switch (message.reason) {
                default:
                    return "reason: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            return null;
        };

        /**
         * Creates a CKickAllPlayers message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CKickAllPlayers
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CKickAllPlayers} CKickAllPlayers
         */
        CKickAllPlayers.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CKickAllPlayers)
                return object;
            var message = new $root.game.CKickAllPlayers();
            switch (object.reason) {
            default:
                if (typeof object.reason === "number") {
                    message.reason = object.reason;
                    break;
                }
                break;
            case "ClientLeave":
            case 0:
                message.reason = 0;
                break;
            case "DuplicateLogin":
            case 1:
                message.reason = 1;
                break;
            case "ServerShutdown":
            case 2:
                message.reason = 2;
                break;
            case "GMKick":
            case 3:
                message.reason = 3;
                break;
            case "Banned":
            case 4:
                message.reason = 4;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a CKickAllPlayers message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CKickAllPlayers
         * @static
         * @param {game.CKickAllPlayers} message CKickAllPlayers
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CKickAllPlayers.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.reason = options.enums === String ? "ClientLeave" : 0;
            if (message.reason != null && message.hasOwnProperty("reason"))
                object.reason = options.enums === String ? $root.common.KickPlayerReason[message.reason] === undefined ? message.reason : $root.common.KickPlayerReason[message.reason] : message.reason;
            return object;
        };

        /**
         * Converts this CKickAllPlayers to JSON.
         * @function toJSON
         * @memberof game.CKickAllPlayers
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CKickAllPlayers.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CKickAllPlayers
         * @function getTypeUrl
         * @memberof game.CKickAllPlayers
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CKickAllPlayers.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CKickAllPlayers";
        };

        return CKickAllPlayers;
    })();

    game.SKickAllPlayers = (function() {

        /**
         * Properties of a SKickAllPlayers.
         * @memberof game
         * @interface ISKickAllPlayers
         */

        /**
         * Constructs a new SKickAllPlayers.
         * @memberof game
         * @classdesc Represents a SKickAllPlayers.
         * @implements ISKickAllPlayers
         * @constructor
         * @param {game.ISKickAllPlayers=} [properties] Properties to set
         */
        function SKickAllPlayers(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SKickAllPlayers instance using the specified properties.
         * @function create
         * @memberof game.SKickAllPlayers
         * @static
         * @param {game.ISKickAllPlayers=} [properties] Properties to set
         * @returns {game.SKickAllPlayers} SKickAllPlayers instance
         */
        SKickAllPlayers.create = function create(properties) {
            return new SKickAllPlayers(properties);
        };

        /**
         * Encodes the specified SKickAllPlayers message. Does not implicitly {@link game.SKickAllPlayers.verify|verify} messages.
         * @function encode
         * @memberof game.SKickAllPlayers
         * @static
         * @param {game.ISKickAllPlayers} message SKickAllPlayers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SKickAllPlayers.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SKickAllPlayers message, length delimited. Does not implicitly {@link game.SKickAllPlayers.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SKickAllPlayers
         * @static
         * @param {game.ISKickAllPlayers} message SKickAllPlayers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SKickAllPlayers.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SKickAllPlayers message from the specified reader or buffer.
         * @function decode
         * @memberof game.SKickAllPlayers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SKickAllPlayers} SKickAllPlayers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SKickAllPlayers.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SKickAllPlayers();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SKickAllPlayers message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SKickAllPlayers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SKickAllPlayers} SKickAllPlayers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SKickAllPlayers.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SKickAllPlayers message.
         * @function verify
         * @memberof game.SKickAllPlayers
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SKickAllPlayers.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SKickAllPlayers message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SKickAllPlayers
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SKickAllPlayers} SKickAllPlayers
         */
        SKickAllPlayers.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SKickAllPlayers)
                return object;
            return new $root.game.SKickAllPlayers();
        };

        /**
         * Creates a plain object from a SKickAllPlayers message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SKickAllPlayers
         * @static
         * @param {game.SKickAllPlayers} message SKickAllPlayers
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SKickAllPlayers.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SKickAllPlayers to JSON.
         * @function toJSON
         * @memberof game.SKickAllPlayers
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SKickAllPlayers.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SKickAllPlayers
         * @function getTypeUrl
         * @memberof game.SKickAllPlayers
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SKickAllPlayers.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SKickAllPlayers";
        };

        return SKickAllPlayers;
    })();

    game.CGetGuild = (function() {

        /**
         * Properties of a CGetGuild.
         * @memberof game
         * @interface ICGetGuild
         * @property {string|null} [uid] CGetGuild uid
         * @property {string|null} [gid] CGetGuild gid
         */

        /**
         * Constructs a new CGetGuild.
         * @memberof game
         * @classdesc Represents a CGetGuild.
         * @implements ICGetGuild
         * @constructor
         * @param {game.ICGetGuild=} [properties] Properties to set
         */
        function CGetGuild(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CGetGuild uid.
         * @member {string} uid
         * @memberof game.CGetGuild
         * @instance
         */
        CGetGuild.prototype.uid = "";

        /**
         * CGetGuild gid.
         * @member {string} gid
         * @memberof game.CGetGuild
         * @instance
         */
        CGetGuild.prototype.gid = "";

        /**
         * Creates a new CGetGuild instance using the specified properties.
         * @function create
         * @memberof game.CGetGuild
         * @static
         * @param {game.ICGetGuild=} [properties] Properties to set
         * @returns {game.CGetGuild} CGetGuild instance
         */
        CGetGuild.create = function create(properties) {
            return new CGetGuild(properties);
        };

        /**
         * Encodes the specified CGetGuild message. Does not implicitly {@link game.CGetGuild.verify|verify} messages.
         * @function encode
         * @memberof game.CGetGuild
         * @static
         * @param {game.ICGetGuild} message CGetGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetGuild.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.gid != null && Object.hasOwnProperty.call(message, "gid"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.gid);
            return writer;
        };

        /**
         * Encodes the specified CGetGuild message, length delimited. Does not implicitly {@link game.CGetGuild.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetGuild
         * @static
         * @param {game.ICGetGuild} message CGetGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetGuild.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetGuild message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetGuild} CGetGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetGuild.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetGuild();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.gid = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetGuild message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetGuild} CGetGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetGuild.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetGuild message.
         * @function verify
         * @memberof game.CGetGuild
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetGuild.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.gid != null && message.hasOwnProperty("gid"))
                if (!$util.isString(message.gid))
                    return "gid: string expected";
            return null;
        };

        /**
         * Creates a CGetGuild message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetGuild
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetGuild} CGetGuild
         */
        CGetGuild.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetGuild)
                return object;
            var message = new $root.game.CGetGuild();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.gid != null)
                message.gid = String(object.gid);
            return message;
        };

        /**
         * Creates a plain object from a CGetGuild message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetGuild
         * @static
         * @param {game.CGetGuild} message CGetGuild
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetGuild.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.uid = "";
                object.gid = "";
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.gid != null && message.hasOwnProperty("gid"))
                object.gid = message.gid;
            return object;
        };

        /**
         * Converts this CGetGuild to JSON.
         * @function toJSON
         * @memberof game.CGetGuild
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetGuild.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetGuild
         * @function getTypeUrl
         * @memberof game.CGetGuild
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetGuild.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetGuild";
        };

        return CGetGuild;
    })();

    game.SGetGuild = (function() {

        /**
         * Properties of a SGetGuild.
         * @memberof game
         * @interface ISGetGuild
         * @property {common.IGuildInfo|null} [info] SGetGuild info
         */

        /**
         * Constructs a new SGetGuild.
         * @memberof game
         * @classdesc Represents a SGetGuild.
         * @implements ISGetGuild
         * @constructor
         * @param {game.ISGetGuild=} [properties] Properties to set
         */
        function SGetGuild(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGetGuild info.
         * @member {common.IGuildInfo|null|undefined} info
         * @memberof game.SGetGuild
         * @instance
         */
        SGetGuild.prototype.info = null;

        /**
         * Creates a new SGetGuild instance using the specified properties.
         * @function create
         * @memberof game.SGetGuild
         * @static
         * @param {game.ISGetGuild=} [properties] Properties to set
         * @returns {game.SGetGuild} SGetGuild instance
         */
        SGetGuild.create = function create(properties) {
            return new SGetGuild(properties);
        };

        /**
         * Encodes the specified SGetGuild message. Does not implicitly {@link game.SGetGuild.verify|verify} messages.
         * @function encode
         * @memberof game.SGetGuild
         * @static
         * @param {game.ISGetGuild} message SGetGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetGuild.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.info != null && Object.hasOwnProperty.call(message, "info"))
                $root.common.GuildInfo.encode(message.info, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SGetGuild message, length delimited. Does not implicitly {@link game.SGetGuild.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetGuild
         * @static
         * @param {game.ISGetGuild} message SGetGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetGuild.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetGuild message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetGuild} SGetGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetGuild.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetGuild();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.info = $root.common.GuildInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetGuild message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetGuild} SGetGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetGuild.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetGuild message.
         * @function verify
         * @memberof game.SGetGuild
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetGuild.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.info != null && message.hasOwnProperty("info")) {
                var error = $root.common.GuildInfo.verify(message.info);
                if (error)
                    return "info." + error;
            }
            return null;
        };

        /**
         * Creates a SGetGuild message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetGuild
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetGuild} SGetGuild
         */
        SGetGuild.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetGuild)
                return object;
            var message = new $root.game.SGetGuild();
            if (object.info != null) {
                if (typeof object.info !== "object")
                    throw TypeError(".game.SGetGuild.info: object expected");
                message.info = $root.common.GuildInfo.fromObject(object.info);
            }
            return message;
        };

        /**
         * Creates a plain object from a SGetGuild message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetGuild
         * @static
         * @param {game.SGetGuild} message SGetGuild
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetGuild.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.info = null;
            if (message.info != null && message.hasOwnProperty("info"))
                object.info = $root.common.GuildInfo.toObject(message.info, options);
            return object;
        };

        /**
         * Converts this SGetGuild to JSON.
         * @function toJSON
         * @memberof game.SGetGuild
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetGuild.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetGuild
         * @function getTypeUrl
         * @memberof game.SGetGuild
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetGuild.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetGuild";
        };

        return SGetGuild;
    })();

    game.CGameGetGuildList = (function() {

        /**
         * Properties of a CGameGetGuildList.
         * @memberof game
         * @interface ICGameGetGuildList
         * @property {number|null} [offset] CGameGetGuildList offset
         * @property {number|null} [limit] CGameGetGuildList limit
         */

        /**
         * Constructs a new CGameGetGuildList.
         * @memberof game
         * @classdesc Represents a CGameGetGuildList.
         * @implements ICGameGetGuildList
         * @constructor
         * @param {game.ICGameGetGuildList=} [properties] Properties to set
         */
        function CGameGetGuildList(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CGameGetGuildList offset.
         * @member {number} offset
         * @memberof game.CGameGetGuildList
         * @instance
         */
        CGameGetGuildList.prototype.offset = 0;

        /**
         * CGameGetGuildList limit.
         * @member {number} limit
         * @memberof game.CGameGetGuildList
         * @instance
         */
        CGameGetGuildList.prototype.limit = 0;

        /**
         * Creates a new CGameGetGuildList instance using the specified properties.
         * @function create
         * @memberof game.CGameGetGuildList
         * @static
         * @param {game.ICGameGetGuildList=} [properties] Properties to set
         * @returns {game.CGameGetGuildList} CGameGetGuildList instance
         */
        CGameGetGuildList.create = function create(properties) {
            return new CGameGetGuildList(properties);
        };

        /**
         * Encodes the specified CGameGetGuildList message. Does not implicitly {@link game.CGameGetGuildList.verify|verify} messages.
         * @function encode
         * @memberof game.CGameGetGuildList
         * @static
         * @param {game.ICGameGetGuildList} message CGameGetGuildList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGameGetGuildList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.offset != null && Object.hasOwnProperty.call(message, "offset"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.offset);
            if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.limit);
            return writer;
        };

        /**
         * Encodes the specified CGameGetGuildList message, length delimited. Does not implicitly {@link game.CGameGetGuildList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGameGetGuildList
         * @static
         * @param {game.ICGameGetGuildList} message CGameGetGuildList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGameGetGuildList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGameGetGuildList message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGameGetGuildList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGameGetGuildList} CGameGetGuildList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGameGetGuildList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGameGetGuildList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.offset = reader.int32();
                        break;
                    }
                case 2: {
                        message.limit = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGameGetGuildList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGameGetGuildList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGameGetGuildList} CGameGetGuildList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGameGetGuildList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGameGetGuildList message.
         * @function verify
         * @memberof game.CGameGetGuildList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGameGetGuildList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.offset != null && message.hasOwnProperty("offset"))
                if (!$util.isInteger(message.offset))
                    return "offset: integer expected";
            if (message.limit != null && message.hasOwnProperty("limit"))
                if (!$util.isInteger(message.limit))
                    return "limit: integer expected";
            return null;
        };

        /**
         * Creates a CGameGetGuildList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGameGetGuildList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGameGetGuildList} CGameGetGuildList
         */
        CGameGetGuildList.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGameGetGuildList)
                return object;
            var message = new $root.game.CGameGetGuildList();
            if (object.offset != null)
                message.offset = object.offset | 0;
            if (object.limit != null)
                message.limit = object.limit | 0;
            return message;
        };

        /**
         * Creates a plain object from a CGameGetGuildList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGameGetGuildList
         * @static
         * @param {game.CGameGetGuildList} message CGameGetGuildList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGameGetGuildList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.offset = 0;
                object.limit = 0;
            }
            if (message.offset != null && message.hasOwnProperty("offset"))
                object.offset = message.offset;
            if (message.limit != null && message.hasOwnProperty("limit"))
                object.limit = message.limit;
            return object;
        };

        /**
         * Converts this CGameGetGuildList to JSON.
         * @function toJSON
         * @memberof game.CGameGetGuildList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGameGetGuildList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGameGetGuildList
         * @function getTypeUrl
         * @memberof game.CGameGetGuildList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGameGetGuildList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGameGetGuildList";
        };

        return CGameGetGuildList;
    })();

    game.SGameGetGuildList = (function() {

        /**
         * Properties of a SGameGetGuildList.
         * @memberof game
         * @interface ISGameGetGuildList
         * @property {Array.<common.IGuildSimpleInfo>|null} [list] SGameGetGuildList list
         */

        /**
         * Constructs a new SGameGetGuildList.
         * @memberof game
         * @classdesc Represents a SGameGetGuildList.
         * @implements ISGameGetGuildList
         * @constructor
         * @param {game.ISGameGetGuildList=} [properties] Properties to set
         */
        function SGameGetGuildList(properties) {
            this.list = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGameGetGuildList list.
         * @member {Array.<common.IGuildSimpleInfo>} list
         * @memberof game.SGameGetGuildList
         * @instance
         */
        SGameGetGuildList.prototype.list = $util.emptyArray;

        /**
         * Creates a new SGameGetGuildList instance using the specified properties.
         * @function create
         * @memberof game.SGameGetGuildList
         * @static
         * @param {game.ISGameGetGuildList=} [properties] Properties to set
         * @returns {game.SGameGetGuildList} SGameGetGuildList instance
         */
        SGameGetGuildList.create = function create(properties) {
            return new SGameGetGuildList(properties);
        };

        /**
         * Encodes the specified SGameGetGuildList message. Does not implicitly {@link game.SGameGetGuildList.verify|verify} messages.
         * @function encode
         * @memberof game.SGameGetGuildList
         * @static
         * @param {game.ISGameGetGuildList} message SGameGetGuildList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGameGetGuildList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.list != null && message.list.length)
                for (var i = 0; i < message.list.length; ++i)
                    $root.common.GuildSimpleInfo.encode(message.list[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SGameGetGuildList message, length delimited. Does not implicitly {@link game.SGameGetGuildList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGameGetGuildList
         * @static
         * @param {game.ISGameGetGuildList} message SGameGetGuildList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGameGetGuildList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGameGetGuildList message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGameGetGuildList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGameGetGuildList} SGameGetGuildList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGameGetGuildList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGameGetGuildList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.list && message.list.length))
                            message.list = [];
                        message.list.push($root.common.GuildSimpleInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGameGetGuildList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGameGetGuildList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGameGetGuildList} SGameGetGuildList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGameGetGuildList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGameGetGuildList message.
         * @function verify
         * @memberof game.SGameGetGuildList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGameGetGuildList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.list != null && message.hasOwnProperty("list")) {
                if (!Array.isArray(message.list))
                    return "list: array expected";
                for (var i = 0; i < message.list.length; ++i) {
                    var error = $root.common.GuildSimpleInfo.verify(message.list[i]);
                    if (error)
                        return "list." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SGameGetGuildList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGameGetGuildList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGameGetGuildList} SGameGetGuildList
         */
        SGameGetGuildList.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGameGetGuildList)
                return object;
            var message = new $root.game.SGameGetGuildList();
            if (object.list) {
                if (!Array.isArray(object.list))
                    throw TypeError(".game.SGameGetGuildList.list: array expected");
                message.list = [];
                for (var i = 0; i < object.list.length; ++i) {
                    if (typeof object.list[i] !== "object")
                        throw TypeError(".game.SGameGetGuildList.list: object expected");
                    message.list[i] = $root.common.GuildSimpleInfo.fromObject(object.list[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SGameGetGuildList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGameGetGuildList
         * @static
         * @param {game.SGameGetGuildList} message SGameGetGuildList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGameGetGuildList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.list = [];
            if (message.list && message.list.length) {
                object.list = [];
                for (var j = 0; j < message.list.length; ++j)
                    object.list[j] = $root.common.GuildSimpleInfo.toObject(message.list[j], options);
            }
            return object;
        };

        /**
         * Converts this SGameGetGuildList to JSON.
         * @function toJSON
         * @memberof game.SGameGetGuildList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGameGetGuildList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGameGetGuildList
         * @function getTypeUrl
         * @memberof game.SGameGetGuildList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGameGetGuildList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGameGetGuildList";
        };

        return SGameGetGuildList;
    })();

    game.CCreateGuild = (function() {

        /**
         * Properties of a CCreateGuild.
         * @memberof game
         * @interface ICCreateGuild
         * @property {string|null} [name] CCreateGuild name
         * @property {number|null} [flag] CCreateGuild flag
         * @property {string|null} [location] CCreateGuild location
         * @property {number|null} [joinType] CCreateGuild joinType
         * @property {number|null} [joinLimit] CCreateGuild joinLimit
         */

        /**
         * Constructs a new CCreateGuild.
         * @memberof game
         * @classdesc Represents a CCreateGuild.
         * @implements ICCreateGuild
         * @constructor
         * @param {game.ICCreateGuild=} [properties] Properties to set
         */
        function CCreateGuild(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CCreateGuild name.
         * @member {string} name
         * @memberof game.CCreateGuild
         * @instance
         */
        CCreateGuild.prototype.name = "";

        /**
         * CCreateGuild flag.
         * @member {number} flag
         * @memberof game.CCreateGuild
         * @instance
         */
        CCreateGuild.prototype.flag = 0;

        /**
         * CCreateGuild location.
         * @member {string} location
         * @memberof game.CCreateGuild
         * @instance
         */
        CCreateGuild.prototype.location = "";

        /**
         * CCreateGuild joinType.
         * @member {number} joinType
         * @memberof game.CCreateGuild
         * @instance
         */
        CCreateGuild.prototype.joinType = 0;

        /**
         * CCreateGuild joinLimit.
         * @member {number} joinLimit
         * @memberof game.CCreateGuild
         * @instance
         */
        CCreateGuild.prototype.joinLimit = 0;

        /**
         * Creates a new CCreateGuild instance using the specified properties.
         * @function create
         * @memberof game.CCreateGuild
         * @static
         * @param {game.ICCreateGuild=} [properties] Properties to set
         * @returns {game.CCreateGuild} CCreateGuild instance
         */
        CCreateGuild.create = function create(properties) {
            return new CCreateGuild(properties);
        };

        /**
         * Encodes the specified CCreateGuild message. Does not implicitly {@link game.CCreateGuild.verify|verify} messages.
         * @function encode
         * @memberof game.CCreateGuild
         * @static
         * @param {game.ICCreateGuild} message CCreateGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CCreateGuild.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.flag);
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.location);
            if (message.joinType != null && Object.hasOwnProperty.call(message, "joinType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.joinType);
            if (message.joinLimit != null && Object.hasOwnProperty.call(message, "joinLimit"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.joinLimit);
            return writer;
        };

        /**
         * Encodes the specified CCreateGuild message, length delimited. Does not implicitly {@link game.CCreateGuild.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CCreateGuild
         * @static
         * @param {game.ICCreateGuild} message CCreateGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CCreateGuild.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CCreateGuild message from the specified reader or buffer.
         * @function decode
         * @memberof game.CCreateGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CCreateGuild} CCreateGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CCreateGuild.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CCreateGuild();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.flag = reader.uint32();
                        break;
                    }
                case 3: {
                        message.location = reader.string();
                        break;
                    }
                case 4: {
                        message.joinType = reader.int32();
                        break;
                    }
                case 5: {
                        message.joinLimit = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CCreateGuild message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CCreateGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CCreateGuild} CCreateGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CCreateGuild.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CCreateGuild message.
         * @function verify
         * @memberof game.CCreateGuild
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CCreateGuild.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.flag != null && message.hasOwnProperty("flag"))
                if (!$util.isInteger(message.flag))
                    return "flag: integer expected";
            if (message.location != null && message.hasOwnProperty("location"))
                if (!$util.isString(message.location))
                    return "location: string expected";
            if (message.joinType != null && message.hasOwnProperty("joinType"))
                if (!$util.isInteger(message.joinType))
                    return "joinType: integer expected";
            if (message.joinLimit != null && message.hasOwnProperty("joinLimit"))
                if (!$util.isInteger(message.joinLimit))
                    return "joinLimit: integer expected";
            return null;
        };

        /**
         * Creates a CCreateGuild message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CCreateGuild
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CCreateGuild} CCreateGuild
         */
        CCreateGuild.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CCreateGuild)
                return object;
            var message = new $root.game.CCreateGuild();
            if (object.name != null)
                message.name = String(object.name);
            if (object.flag != null)
                message.flag = object.flag >>> 0;
            if (object.location != null)
                message.location = String(object.location);
            if (object.joinType != null)
                message.joinType = object.joinType | 0;
            if (object.joinLimit != null)
                message.joinLimit = object.joinLimit | 0;
            return message;
        };

        /**
         * Creates a plain object from a CCreateGuild message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CCreateGuild
         * @static
         * @param {game.CCreateGuild} message CCreateGuild
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CCreateGuild.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.name = "";
                object.flag = 0;
                object.location = "";
                object.joinType = 0;
                object.joinLimit = 0;
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.flag != null && message.hasOwnProperty("flag"))
                object.flag = message.flag;
            if (message.location != null && message.hasOwnProperty("location"))
                object.location = message.location;
            if (message.joinType != null && message.hasOwnProperty("joinType"))
                object.joinType = message.joinType;
            if (message.joinLimit != null && message.hasOwnProperty("joinLimit"))
                object.joinLimit = message.joinLimit;
            return object;
        };

        /**
         * Converts this CCreateGuild to JSON.
         * @function toJSON
         * @memberof game.CCreateGuild
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CCreateGuild.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CCreateGuild
         * @function getTypeUrl
         * @memberof game.CCreateGuild
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CCreateGuild.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CCreateGuild";
        };

        return CCreateGuild;
    })();

    game.SCreateGuild = (function() {

        /**
         * Properties of a SCreateGuild.
         * @memberof game
         * @interface ISCreateGuild
         * @property {common.IGuildInfo|null} [info] SCreateGuild info
         */

        /**
         * Constructs a new SCreateGuild.
         * @memberof game
         * @classdesc Represents a SCreateGuild.
         * @implements ISCreateGuild
         * @constructor
         * @param {game.ISCreateGuild=} [properties] Properties to set
         */
        function SCreateGuild(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SCreateGuild info.
         * @member {common.IGuildInfo|null|undefined} info
         * @memberof game.SCreateGuild
         * @instance
         */
        SCreateGuild.prototype.info = null;

        /**
         * Creates a new SCreateGuild instance using the specified properties.
         * @function create
         * @memberof game.SCreateGuild
         * @static
         * @param {game.ISCreateGuild=} [properties] Properties to set
         * @returns {game.SCreateGuild} SCreateGuild instance
         */
        SCreateGuild.create = function create(properties) {
            return new SCreateGuild(properties);
        };

        /**
         * Encodes the specified SCreateGuild message. Does not implicitly {@link game.SCreateGuild.verify|verify} messages.
         * @function encode
         * @memberof game.SCreateGuild
         * @static
         * @param {game.ISCreateGuild} message SCreateGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SCreateGuild.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.info != null && Object.hasOwnProperty.call(message, "info"))
                $root.common.GuildInfo.encode(message.info, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SCreateGuild message, length delimited. Does not implicitly {@link game.SCreateGuild.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SCreateGuild
         * @static
         * @param {game.ISCreateGuild} message SCreateGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SCreateGuild.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SCreateGuild message from the specified reader or buffer.
         * @function decode
         * @memberof game.SCreateGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SCreateGuild} SCreateGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SCreateGuild.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SCreateGuild();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.info = $root.common.GuildInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SCreateGuild message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SCreateGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SCreateGuild} SCreateGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SCreateGuild.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SCreateGuild message.
         * @function verify
         * @memberof game.SCreateGuild
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SCreateGuild.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.info != null && message.hasOwnProperty("info")) {
                var error = $root.common.GuildInfo.verify(message.info);
                if (error)
                    return "info." + error;
            }
            return null;
        };

        /**
         * Creates a SCreateGuild message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SCreateGuild
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SCreateGuild} SCreateGuild
         */
        SCreateGuild.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SCreateGuild)
                return object;
            var message = new $root.game.SCreateGuild();
            if (object.info != null) {
                if (typeof object.info !== "object")
                    throw TypeError(".game.SCreateGuild.info: object expected");
                message.info = $root.common.GuildInfo.fromObject(object.info);
            }
            return message;
        };

        /**
         * Creates a plain object from a SCreateGuild message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SCreateGuild
         * @static
         * @param {game.SCreateGuild} message SCreateGuild
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SCreateGuild.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.info = null;
            if (message.info != null && message.hasOwnProperty("info"))
                object.info = $root.common.GuildInfo.toObject(message.info, options);
            return object;
        };

        /**
         * Converts this SCreateGuild to JSON.
         * @function toJSON
         * @memberof game.SCreateGuild
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SCreateGuild.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SCreateGuild
         * @function getTypeUrl
         * @memberof game.SCreateGuild
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SCreateGuild.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SCreateGuild";
        };

        return SCreateGuild;
    })();

    game.CLeaveGuild = (function() {

        /**
         * Properties of a CLeaveGuild.
         * @memberof game
         * @interface ICLeaveGuild
         */

        /**
         * Constructs a new CLeaveGuild.
         * @memberof game
         * @classdesc Represents a CLeaveGuild.
         * @implements ICLeaveGuild
         * @constructor
         * @param {game.ICLeaveGuild=} [properties] Properties to set
         */
        function CLeaveGuild(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CLeaveGuild instance using the specified properties.
         * @function create
         * @memberof game.CLeaveGuild
         * @static
         * @param {game.ICLeaveGuild=} [properties] Properties to set
         * @returns {game.CLeaveGuild} CLeaveGuild instance
         */
        CLeaveGuild.create = function create(properties) {
            return new CLeaveGuild(properties);
        };

        /**
         * Encodes the specified CLeaveGuild message. Does not implicitly {@link game.CLeaveGuild.verify|verify} messages.
         * @function encode
         * @memberof game.CLeaveGuild
         * @static
         * @param {game.ICLeaveGuild} message CLeaveGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CLeaveGuild.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CLeaveGuild message, length delimited. Does not implicitly {@link game.CLeaveGuild.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CLeaveGuild
         * @static
         * @param {game.ICLeaveGuild} message CLeaveGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CLeaveGuild.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CLeaveGuild message from the specified reader or buffer.
         * @function decode
         * @memberof game.CLeaveGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CLeaveGuild} CLeaveGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CLeaveGuild.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CLeaveGuild();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CLeaveGuild message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CLeaveGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CLeaveGuild} CLeaveGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CLeaveGuild.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CLeaveGuild message.
         * @function verify
         * @memberof game.CLeaveGuild
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CLeaveGuild.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CLeaveGuild message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CLeaveGuild
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CLeaveGuild} CLeaveGuild
         */
        CLeaveGuild.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CLeaveGuild)
                return object;
            return new $root.game.CLeaveGuild();
        };

        /**
         * Creates a plain object from a CLeaveGuild message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CLeaveGuild
         * @static
         * @param {game.CLeaveGuild} message CLeaveGuild
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CLeaveGuild.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CLeaveGuild to JSON.
         * @function toJSON
         * @memberof game.CLeaveGuild
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CLeaveGuild.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CLeaveGuild
         * @function getTypeUrl
         * @memberof game.CLeaveGuild
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CLeaveGuild.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CLeaveGuild";
        };

        return CLeaveGuild;
    })();

    game.SLeaveGuild = (function() {

        /**
         * Properties of a SLeaveGuild.
         * @memberof game
         * @interface ISLeaveGuild
         * @property {string|null} [gid] SLeaveGuild gid
         */

        /**
         * Constructs a new SLeaveGuild.
         * @memberof game
         * @classdesc Represents a SLeaveGuild.
         * @implements ISLeaveGuild
         * @constructor
         * @param {game.ISLeaveGuild=} [properties] Properties to set
         */
        function SLeaveGuild(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SLeaveGuild gid.
         * @member {string} gid
         * @memberof game.SLeaveGuild
         * @instance
         */
        SLeaveGuild.prototype.gid = "";

        /**
         * Creates a new SLeaveGuild instance using the specified properties.
         * @function create
         * @memberof game.SLeaveGuild
         * @static
         * @param {game.ISLeaveGuild=} [properties] Properties to set
         * @returns {game.SLeaveGuild} SLeaveGuild instance
         */
        SLeaveGuild.create = function create(properties) {
            return new SLeaveGuild(properties);
        };

        /**
         * Encodes the specified SLeaveGuild message. Does not implicitly {@link game.SLeaveGuild.verify|verify} messages.
         * @function encode
         * @memberof game.SLeaveGuild
         * @static
         * @param {game.ISLeaveGuild} message SLeaveGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SLeaveGuild.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gid != null && Object.hasOwnProperty.call(message, "gid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.gid);
            return writer;
        };

        /**
         * Encodes the specified SLeaveGuild message, length delimited. Does not implicitly {@link game.SLeaveGuild.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SLeaveGuild
         * @static
         * @param {game.ISLeaveGuild} message SLeaveGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SLeaveGuild.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SLeaveGuild message from the specified reader or buffer.
         * @function decode
         * @memberof game.SLeaveGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SLeaveGuild} SLeaveGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SLeaveGuild.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SLeaveGuild();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.gid = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SLeaveGuild message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SLeaveGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SLeaveGuild} SLeaveGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SLeaveGuild.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SLeaveGuild message.
         * @function verify
         * @memberof game.SLeaveGuild
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SLeaveGuild.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.gid != null && message.hasOwnProperty("gid"))
                if (!$util.isString(message.gid))
                    return "gid: string expected";
            return null;
        };

        /**
         * Creates a SLeaveGuild message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SLeaveGuild
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SLeaveGuild} SLeaveGuild
         */
        SLeaveGuild.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SLeaveGuild)
                return object;
            var message = new $root.game.SLeaveGuild();
            if (object.gid != null)
                message.gid = String(object.gid);
            return message;
        };

        /**
         * Creates a plain object from a SLeaveGuild message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SLeaveGuild
         * @static
         * @param {game.SLeaveGuild} message SLeaveGuild
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SLeaveGuild.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.gid = "";
            if (message.gid != null && message.hasOwnProperty("gid"))
                object.gid = message.gid;
            return object;
        };

        /**
         * Converts this SLeaveGuild to JSON.
         * @function toJSON
         * @memberof game.SLeaveGuild
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SLeaveGuild.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SLeaveGuild
         * @function getTypeUrl
         * @memberof game.SLeaveGuild
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SLeaveGuild.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SLeaveGuild";
        };

        return SLeaveGuild;
    })();

    game.CApplyJoin = (function() {

        /**
         * Properties of a CApplyJoin.
         * @memberof game
         * @interface ICApplyJoin
         * @property {string|null} [gid] CApplyJoin gid
         * @property {common.IGuildApplyInfo|null} [apply] CApplyJoin apply
         */

        /**
         * Constructs a new CApplyJoin.
         * @memberof game
         * @classdesc Represents a CApplyJoin.
         * @implements ICApplyJoin
         * @constructor
         * @param {game.ICApplyJoin=} [properties] Properties to set
         */
        function CApplyJoin(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CApplyJoin gid.
         * @member {string} gid
         * @memberof game.CApplyJoin
         * @instance
         */
        CApplyJoin.prototype.gid = "";

        /**
         * CApplyJoin apply.
         * @member {common.IGuildApplyInfo|null|undefined} apply
         * @memberof game.CApplyJoin
         * @instance
         */
        CApplyJoin.prototype.apply = null;

        /**
         * Creates a new CApplyJoin instance using the specified properties.
         * @function create
         * @memberof game.CApplyJoin
         * @static
         * @param {game.ICApplyJoin=} [properties] Properties to set
         * @returns {game.CApplyJoin} CApplyJoin instance
         */
        CApplyJoin.create = function create(properties) {
            return new CApplyJoin(properties);
        };

        /**
         * Encodes the specified CApplyJoin message. Does not implicitly {@link game.CApplyJoin.verify|verify} messages.
         * @function encode
         * @memberof game.CApplyJoin
         * @static
         * @param {game.ICApplyJoin} message CApplyJoin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CApplyJoin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gid != null && Object.hasOwnProperty.call(message, "gid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.gid);
            if (message.apply != null && Object.hasOwnProperty.call(message, "apply"))
                $root.common.GuildApplyInfo.encode(message.apply, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CApplyJoin message, length delimited. Does not implicitly {@link game.CApplyJoin.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CApplyJoin
         * @static
         * @param {game.ICApplyJoin} message CApplyJoin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CApplyJoin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CApplyJoin message from the specified reader or buffer.
         * @function decode
         * @memberof game.CApplyJoin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CApplyJoin} CApplyJoin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CApplyJoin.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CApplyJoin();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.gid = reader.string();
                        break;
                    }
                case 2: {
                        message.apply = $root.common.GuildApplyInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CApplyJoin message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CApplyJoin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CApplyJoin} CApplyJoin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CApplyJoin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CApplyJoin message.
         * @function verify
         * @memberof game.CApplyJoin
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CApplyJoin.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.gid != null && message.hasOwnProperty("gid"))
                if (!$util.isString(message.gid))
                    return "gid: string expected";
            if (message.apply != null && message.hasOwnProperty("apply")) {
                var error = $root.common.GuildApplyInfo.verify(message.apply);
                if (error)
                    return "apply." + error;
            }
            return null;
        };

        /**
         * Creates a CApplyJoin message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CApplyJoin
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CApplyJoin} CApplyJoin
         */
        CApplyJoin.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CApplyJoin)
                return object;
            var message = new $root.game.CApplyJoin();
            if (object.gid != null)
                message.gid = String(object.gid);
            if (object.apply != null) {
                if (typeof object.apply !== "object")
                    throw TypeError(".game.CApplyJoin.apply: object expected");
                message.apply = $root.common.GuildApplyInfo.fromObject(object.apply);
            }
            return message;
        };

        /**
         * Creates a plain object from a CApplyJoin message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CApplyJoin
         * @static
         * @param {game.CApplyJoin} message CApplyJoin
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CApplyJoin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.gid = "";
                object.apply = null;
            }
            if (message.gid != null && message.hasOwnProperty("gid"))
                object.gid = message.gid;
            if (message.apply != null && message.hasOwnProperty("apply"))
                object.apply = $root.common.GuildApplyInfo.toObject(message.apply, options);
            return object;
        };

        /**
         * Converts this CApplyJoin to JSON.
         * @function toJSON
         * @memberof game.CApplyJoin
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CApplyJoin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CApplyJoin
         * @function getTypeUrl
         * @memberof game.CApplyJoin
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CApplyJoin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CApplyJoin";
        };

        return CApplyJoin;
    })();

    game.SApplyJoin = (function() {

        /**
         * Properties of a SApplyJoin.
         * @memberof game
         * @interface ISApplyJoin
         */

        /**
         * Constructs a new SApplyJoin.
         * @memberof game
         * @classdesc Represents a SApplyJoin.
         * @implements ISApplyJoin
         * @constructor
         * @param {game.ISApplyJoin=} [properties] Properties to set
         */
        function SApplyJoin(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SApplyJoin instance using the specified properties.
         * @function create
         * @memberof game.SApplyJoin
         * @static
         * @param {game.ISApplyJoin=} [properties] Properties to set
         * @returns {game.SApplyJoin} SApplyJoin instance
         */
        SApplyJoin.create = function create(properties) {
            return new SApplyJoin(properties);
        };

        /**
         * Encodes the specified SApplyJoin message. Does not implicitly {@link game.SApplyJoin.verify|verify} messages.
         * @function encode
         * @memberof game.SApplyJoin
         * @static
         * @param {game.ISApplyJoin} message SApplyJoin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SApplyJoin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SApplyJoin message, length delimited. Does not implicitly {@link game.SApplyJoin.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SApplyJoin
         * @static
         * @param {game.ISApplyJoin} message SApplyJoin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SApplyJoin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SApplyJoin message from the specified reader or buffer.
         * @function decode
         * @memberof game.SApplyJoin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SApplyJoin} SApplyJoin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SApplyJoin.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SApplyJoin();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SApplyJoin message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SApplyJoin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SApplyJoin} SApplyJoin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SApplyJoin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SApplyJoin message.
         * @function verify
         * @memberof game.SApplyJoin
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SApplyJoin.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SApplyJoin message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SApplyJoin
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SApplyJoin} SApplyJoin
         */
        SApplyJoin.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SApplyJoin)
                return object;
            return new $root.game.SApplyJoin();
        };

        /**
         * Creates a plain object from a SApplyJoin message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SApplyJoin
         * @static
         * @param {game.SApplyJoin} message SApplyJoin
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SApplyJoin.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SApplyJoin to JSON.
         * @function toJSON
         * @memberof game.SApplyJoin
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SApplyJoin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SApplyJoin
         * @function getTypeUrl
         * @memberof game.SApplyJoin
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SApplyJoin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SApplyJoin";
        };

        return SApplyJoin;
    })();

    game.CGetGuildApplyList = (function() {

        /**
         * Properties of a CGetGuildApplyList.
         * @memberof game
         * @interface ICGetGuildApplyList
         */

        /**
         * Constructs a new CGetGuildApplyList.
         * @memberof game
         * @classdesc Represents a CGetGuildApplyList.
         * @implements ICGetGuildApplyList
         * @constructor
         * @param {game.ICGetGuildApplyList=} [properties] Properties to set
         */
        function CGetGuildApplyList(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CGetGuildApplyList instance using the specified properties.
         * @function create
         * @memberof game.CGetGuildApplyList
         * @static
         * @param {game.ICGetGuildApplyList=} [properties] Properties to set
         * @returns {game.CGetGuildApplyList} CGetGuildApplyList instance
         */
        CGetGuildApplyList.create = function create(properties) {
            return new CGetGuildApplyList(properties);
        };

        /**
         * Encodes the specified CGetGuildApplyList message. Does not implicitly {@link game.CGetGuildApplyList.verify|verify} messages.
         * @function encode
         * @memberof game.CGetGuildApplyList
         * @static
         * @param {game.ICGetGuildApplyList} message CGetGuildApplyList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetGuildApplyList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CGetGuildApplyList message, length delimited. Does not implicitly {@link game.CGetGuildApplyList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetGuildApplyList
         * @static
         * @param {game.ICGetGuildApplyList} message CGetGuildApplyList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetGuildApplyList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetGuildApplyList message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetGuildApplyList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetGuildApplyList} CGetGuildApplyList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetGuildApplyList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetGuildApplyList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetGuildApplyList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetGuildApplyList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetGuildApplyList} CGetGuildApplyList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetGuildApplyList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetGuildApplyList message.
         * @function verify
         * @memberof game.CGetGuildApplyList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetGuildApplyList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CGetGuildApplyList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetGuildApplyList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetGuildApplyList} CGetGuildApplyList
         */
        CGetGuildApplyList.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetGuildApplyList)
                return object;
            return new $root.game.CGetGuildApplyList();
        };

        /**
         * Creates a plain object from a CGetGuildApplyList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetGuildApplyList
         * @static
         * @param {game.CGetGuildApplyList} message CGetGuildApplyList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetGuildApplyList.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CGetGuildApplyList to JSON.
         * @function toJSON
         * @memberof game.CGetGuildApplyList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetGuildApplyList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetGuildApplyList
         * @function getTypeUrl
         * @memberof game.CGetGuildApplyList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetGuildApplyList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetGuildApplyList";
        };

        return CGetGuildApplyList;
    })();

    game.SGetGuildApplyList = (function() {

        /**
         * Properties of a SGetGuildApplyList.
         * @memberof game
         * @interface ISGetGuildApplyList
         * @property {Array.<common.IGuildApplyInfo>|null} [list] SGetGuildApplyList list
         */

        /**
         * Constructs a new SGetGuildApplyList.
         * @memberof game
         * @classdesc Represents a SGetGuildApplyList.
         * @implements ISGetGuildApplyList
         * @constructor
         * @param {game.ISGetGuildApplyList=} [properties] Properties to set
         */
        function SGetGuildApplyList(properties) {
            this.list = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGetGuildApplyList list.
         * @member {Array.<common.IGuildApplyInfo>} list
         * @memberof game.SGetGuildApplyList
         * @instance
         */
        SGetGuildApplyList.prototype.list = $util.emptyArray;

        /**
         * Creates a new SGetGuildApplyList instance using the specified properties.
         * @function create
         * @memberof game.SGetGuildApplyList
         * @static
         * @param {game.ISGetGuildApplyList=} [properties] Properties to set
         * @returns {game.SGetGuildApplyList} SGetGuildApplyList instance
         */
        SGetGuildApplyList.create = function create(properties) {
            return new SGetGuildApplyList(properties);
        };

        /**
         * Encodes the specified SGetGuildApplyList message. Does not implicitly {@link game.SGetGuildApplyList.verify|verify} messages.
         * @function encode
         * @memberof game.SGetGuildApplyList
         * @static
         * @param {game.ISGetGuildApplyList} message SGetGuildApplyList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetGuildApplyList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.list != null && message.list.length)
                for (var i = 0; i < message.list.length; ++i)
                    $root.common.GuildApplyInfo.encode(message.list[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SGetGuildApplyList message, length delimited. Does not implicitly {@link game.SGetGuildApplyList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetGuildApplyList
         * @static
         * @param {game.ISGetGuildApplyList} message SGetGuildApplyList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetGuildApplyList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetGuildApplyList message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetGuildApplyList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetGuildApplyList} SGetGuildApplyList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetGuildApplyList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetGuildApplyList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.list && message.list.length))
                            message.list = [];
                        message.list.push($root.common.GuildApplyInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetGuildApplyList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetGuildApplyList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetGuildApplyList} SGetGuildApplyList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetGuildApplyList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetGuildApplyList message.
         * @function verify
         * @memberof game.SGetGuildApplyList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetGuildApplyList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.list != null && message.hasOwnProperty("list")) {
                if (!Array.isArray(message.list))
                    return "list: array expected";
                for (var i = 0; i < message.list.length; ++i) {
                    var error = $root.common.GuildApplyInfo.verify(message.list[i]);
                    if (error)
                        return "list." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SGetGuildApplyList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetGuildApplyList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetGuildApplyList} SGetGuildApplyList
         */
        SGetGuildApplyList.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetGuildApplyList)
                return object;
            var message = new $root.game.SGetGuildApplyList();
            if (object.list) {
                if (!Array.isArray(object.list))
                    throw TypeError(".game.SGetGuildApplyList.list: array expected");
                message.list = [];
                for (var i = 0; i < object.list.length; ++i) {
                    if (typeof object.list[i] !== "object")
                        throw TypeError(".game.SGetGuildApplyList.list: object expected");
                    message.list[i] = $root.common.GuildApplyInfo.fromObject(object.list[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SGetGuildApplyList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetGuildApplyList
         * @static
         * @param {game.SGetGuildApplyList} message SGetGuildApplyList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetGuildApplyList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.list = [];
            if (message.list && message.list.length) {
                object.list = [];
                for (var j = 0; j < message.list.length; ++j)
                    object.list[j] = $root.common.GuildApplyInfo.toObject(message.list[j], options);
            }
            return object;
        };

        /**
         * Converts this SGetGuildApplyList to JSON.
         * @function toJSON
         * @memberof game.SGetGuildApplyList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetGuildApplyList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetGuildApplyList
         * @function getTypeUrl
         * @memberof game.SGetGuildApplyList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetGuildApplyList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetGuildApplyList";
        };

        return SGetGuildApplyList;
    })();

    game.CReplyJoin = (function() {

        /**
         * Properties of a CReplyJoin.
         * @memberof game
         * @interface ICReplyJoin
         * @property {string|null} [targetUid] CReplyJoin targetUid
         * @property {boolean|null} [agree] CReplyJoin agree
         */

        /**
         * Constructs a new CReplyJoin.
         * @memberof game
         * @classdesc Represents a CReplyJoin.
         * @implements ICReplyJoin
         * @constructor
         * @param {game.ICReplyJoin=} [properties] Properties to set
         */
        function CReplyJoin(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CReplyJoin targetUid.
         * @member {string} targetUid
         * @memberof game.CReplyJoin
         * @instance
         */
        CReplyJoin.prototype.targetUid = "";

        /**
         * CReplyJoin agree.
         * @member {boolean} agree
         * @memberof game.CReplyJoin
         * @instance
         */
        CReplyJoin.prototype.agree = false;

        /**
         * Creates a new CReplyJoin instance using the specified properties.
         * @function create
         * @memberof game.CReplyJoin
         * @static
         * @param {game.ICReplyJoin=} [properties] Properties to set
         * @returns {game.CReplyJoin} CReplyJoin instance
         */
        CReplyJoin.create = function create(properties) {
            return new CReplyJoin(properties);
        };

        /**
         * Encodes the specified CReplyJoin message. Does not implicitly {@link game.CReplyJoin.verify|verify} messages.
         * @function encode
         * @memberof game.CReplyJoin
         * @static
         * @param {game.ICReplyJoin} message CReplyJoin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CReplyJoin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.targetUid != null && Object.hasOwnProperty.call(message, "targetUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.targetUid);
            if (message.agree != null && Object.hasOwnProperty.call(message, "agree"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.agree);
            return writer;
        };

        /**
         * Encodes the specified CReplyJoin message, length delimited. Does not implicitly {@link game.CReplyJoin.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CReplyJoin
         * @static
         * @param {game.ICReplyJoin} message CReplyJoin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CReplyJoin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CReplyJoin message from the specified reader or buffer.
         * @function decode
         * @memberof game.CReplyJoin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CReplyJoin} CReplyJoin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CReplyJoin.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CReplyJoin();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.targetUid = reader.string();
                        break;
                    }
                case 2: {
                        message.agree = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CReplyJoin message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CReplyJoin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CReplyJoin} CReplyJoin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CReplyJoin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CReplyJoin message.
         * @function verify
         * @memberof game.CReplyJoin
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CReplyJoin.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.targetUid != null && message.hasOwnProperty("targetUid"))
                if (!$util.isString(message.targetUid))
                    return "targetUid: string expected";
            if (message.agree != null && message.hasOwnProperty("agree"))
                if (typeof message.agree !== "boolean")
                    return "agree: boolean expected";
            return null;
        };

        /**
         * Creates a CReplyJoin message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CReplyJoin
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CReplyJoin} CReplyJoin
         */
        CReplyJoin.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CReplyJoin)
                return object;
            var message = new $root.game.CReplyJoin();
            if (object.targetUid != null)
                message.targetUid = String(object.targetUid);
            if (object.agree != null)
                message.agree = Boolean(object.agree);
            return message;
        };

        /**
         * Creates a plain object from a CReplyJoin message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CReplyJoin
         * @static
         * @param {game.CReplyJoin} message CReplyJoin
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CReplyJoin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.targetUid = "";
                object.agree = false;
            }
            if (message.targetUid != null && message.hasOwnProperty("targetUid"))
                object.targetUid = message.targetUid;
            if (message.agree != null && message.hasOwnProperty("agree"))
                object.agree = message.agree;
            return object;
        };

        /**
         * Converts this CReplyJoin to JSON.
         * @function toJSON
         * @memberof game.CReplyJoin
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CReplyJoin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CReplyJoin
         * @function getTypeUrl
         * @memberof game.CReplyJoin
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CReplyJoin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CReplyJoin";
        };

        return CReplyJoin;
    })();

    game.SReplyJoin = (function() {

        /**
         * Properties of a SReplyJoin.
         * @memberof game
         * @interface ISReplyJoin
         */

        /**
         * Constructs a new SReplyJoin.
         * @memberof game
         * @classdesc Represents a SReplyJoin.
         * @implements ISReplyJoin
         * @constructor
         * @param {game.ISReplyJoin=} [properties] Properties to set
         */
        function SReplyJoin(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SReplyJoin instance using the specified properties.
         * @function create
         * @memberof game.SReplyJoin
         * @static
         * @param {game.ISReplyJoin=} [properties] Properties to set
         * @returns {game.SReplyJoin} SReplyJoin instance
         */
        SReplyJoin.create = function create(properties) {
            return new SReplyJoin(properties);
        };

        /**
         * Encodes the specified SReplyJoin message. Does not implicitly {@link game.SReplyJoin.verify|verify} messages.
         * @function encode
         * @memberof game.SReplyJoin
         * @static
         * @param {game.ISReplyJoin} message SReplyJoin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SReplyJoin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SReplyJoin message, length delimited. Does not implicitly {@link game.SReplyJoin.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SReplyJoin
         * @static
         * @param {game.ISReplyJoin} message SReplyJoin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SReplyJoin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SReplyJoin message from the specified reader or buffer.
         * @function decode
         * @memberof game.SReplyJoin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SReplyJoin} SReplyJoin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SReplyJoin.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SReplyJoin();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SReplyJoin message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SReplyJoin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SReplyJoin} SReplyJoin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SReplyJoin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SReplyJoin message.
         * @function verify
         * @memberof game.SReplyJoin
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SReplyJoin.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SReplyJoin message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SReplyJoin
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SReplyJoin} SReplyJoin
         */
        SReplyJoin.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SReplyJoin)
                return object;
            return new $root.game.SReplyJoin();
        };

        /**
         * Creates a plain object from a SReplyJoin message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SReplyJoin
         * @static
         * @param {game.SReplyJoin} message SReplyJoin
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SReplyJoin.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SReplyJoin to JSON.
         * @function toJSON
         * @memberof game.SReplyJoin
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SReplyJoin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SReplyJoin
         * @function getTypeUrl
         * @memberof game.SReplyJoin
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SReplyJoin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SReplyJoin";
        };

        return SReplyJoin;
    })();

    game.CKickMember = (function() {

        /**
         * Properties of a CKickMember.
         * @memberof game
         * @interface ICKickMember
         * @property {string|null} [targetUid] CKickMember targetUid
         */

        /**
         * Constructs a new CKickMember.
         * @memberof game
         * @classdesc Represents a CKickMember.
         * @implements ICKickMember
         * @constructor
         * @param {game.ICKickMember=} [properties] Properties to set
         */
        function CKickMember(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CKickMember targetUid.
         * @member {string} targetUid
         * @memberof game.CKickMember
         * @instance
         */
        CKickMember.prototype.targetUid = "";

        /**
         * Creates a new CKickMember instance using the specified properties.
         * @function create
         * @memberof game.CKickMember
         * @static
         * @param {game.ICKickMember=} [properties] Properties to set
         * @returns {game.CKickMember} CKickMember instance
         */
        CKickMember.create = function create(properties) {
            return new CKickMember(properties);
        };

        /**
         * Encodes the specified CKickMember message. Does not implicitly {@link game.CKickMember.verify|verify} messages.
         * @function encode
         * @memberof game.CKickMember
         * @static
         * @param {game.ICKickMember} message CKickMember message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CKickMember.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.targetUid != null && Object.hasOwnProperty.call(message, "targetUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.targetUid);
            return writer;
        };

        /**
         * Encodes the specified CKickMember message, length delimited. Does not implicitly {@link game.CKickMember.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CKickMember
         * @static
         * @param {game.ICKickMember} message CKickMember message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CKickMember.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CKickMember message from the specified reader or buffer.
         * @function decode
         * @memberof game.CKickMember
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CKickMember} CKickMember
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CKickMember.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CKickMember();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.targetUid = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CKickMember message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CKickMember
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CKickMember} CKickMember
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CKickMember.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CKickMember message.
         * @function verify
         * @memberof game.CKickMember
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CKickMember.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.targetUid != null && message.hasOwnProperty("targetUid"))
                if (!$util.isString(message.targetUid))
                    return "targetUid: string expected";
            return null;
        };

        /**
         * Creates a CKickMember message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CKickMember
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CKickMember} CKickMember
         */
        CKickMember.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CKickMember)
                return object;
            var message = new $root.game.CKickMember();
            if (object.targetUid != null)
                message.targetUid = String(object.targetUid);
            return message;
        };

        /**
         * Creates a plain object from a CKickMember message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CKickMember
         * @static
         * @param {game.CKickMember} message CKickMember
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CKickMember.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.targetUid = "";
            if (message.targetUid != null && message.hasOwnProperty("targetUid"))
                object.targetUid = message.targetUid;
            return object;
        };

        /**
         * Converts this CKickMember to JSON.
         * @function toJSON
         * @memberof game.CKickMember
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CKickMember.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CKickMember
         * @function getTypeUrl
         * @memberof game.CKickMember
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CKickMember.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CKickMember";
        };

        return CKickMember;
    })();

    game.SKickMember = (function() {

        /**
         * Properties of a SKickMember.
         * @memberof game
         * @interface ISKickMember
         */

        /**
         * Constructs a new SKickMember.
         * @memberof game
         * @classdesc Represents a SKickMember.
         * @implements ISKickMember
         * @constructor
         * @param {game.ISKickMember=} [properties] Properties to set
         */
        function SKickMember(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SKickMember instance using the specified properties.
         * @function create
         * @memberof game.SKickMember
         * @static
         * @param {game.ISKickMember=} [properties] Properties to set
         * @returns {game.SKickMember} SKickMember instance
         */
        SKickMember.create = function create(properties) {
            return new SKickMember(properties);
        };

        /**
         * Encodes the specified SKickMember message. Does not implicitly {@link game.SKickMember.verify|verify} messages.
         * @function encode
         * @memberof game.SKickMember
         * @static
         * @param {game.ISKickMember} message SKickMember message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SKickMember.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SKickMember message, length delimited. Does not implicitly {@link game.SKickMember.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SKickMember
         * @static
         * @param {game.ISKickMember} message SKickMember message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SKickMember.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SKickMember message from the specified reader or buffer.
         * @function decode
         * @memberof game.SKickMember
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SKickMember} SKickMember
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SKickMember.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SKickMember();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SKickMember message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SKickMember
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SKickMember} SKickMember
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SKickMember.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SKickMember message.
         * @function verify
         * @memberof game.SKickMember
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SKickMember.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SKickMember message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SKickMember
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SKickMember} SKickMember
         */
        SKickMember.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SKickMember)
                return object;
            return new $root.game.SKickMember();
        };

        /**
         * Creates a plain object from a SKickMember message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SKickMember
         * @static
         * @param {game.SKickMember} message SKickMember
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SKickMember.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SKickMember to JSON.
         * @function toJSON
         * @memberof game.SKickMember
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SKickMember.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SKickMember
         * @function getTypeUrl
         * @memberof game.SKickMember
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SKickMember.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SKickMember";
        };

        return SKickMember;
    })();

    game.CModifyGuildName = (function() {

        /**
         * Properties of a CModifyGuildName.
         * @memberof game
         * @interface ICModifyGuildName
         * @property {string|null} [name] CModifyGuildName name
         */

        /**
         * Constructs a new CModifyGuildName.
         * @memberof game
         * @classdesc Represents a CModifyGuildName.
         * @implements ICModifyGuildName
         * @constructor
         * @param {game.ICModifyGuildName=} [properties] Properties to set
         */
        function CModifyGuildName(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CModifyGuildName name.
         * @member {string} name
         * @memberof game.CModifyGuildName
         * @instance
         */
        CModifyGuildName.prototype.name = "";

        /**
         * Creates a new CModifyGuildName instance using the specified properties.
         * @function create
         * @memberof game.CModifyGuildName
         * @static
         * @param {game.ICModifyGuildName=} [properties] Properties to set
         * @returns {game.CModifyGuildName} CModifyGuildName instance
         */
        CModifyGuildName.create = function create(properties) {
            return new CModifyGuildName(properties);
        };

        /**
         * Encodes the specified CModifyGuildName message. Does not implicitly {@link game.CModifyGuildName.verify|verify} messages.
         * @function encode
         * @memberof game.CModifyGuildName
         * @static
         * @param {game.ICModifyGuildName} message CModifyGuildName message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CModifyGuildName.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            return writer;
        };

        /**
         * Encodes the specified CModifyGuildName message, length delimited. Does not implicitly {@link game.CModifyGuildName.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CModifyGuildName
         * @static
         * @param {game.ICModifyGuildName} message CModifyGuildName message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CModifyGuildName.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CModifyGuildName message from the specified reader or buffer.
         * @function decode
         * @memberof game.CModifyGuildName
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CModifyGuildName} CModifyGuildName
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CModifyGuildName.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CModifyGuildName();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CModifyGuildName message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CModifyGuildName
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CModifyGuildName} CModifyGuildName
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CModifyGuildName.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CModifyGuildName message.
         * @function verify
         * @memberof game.CModifyGuildName
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CModifyGuildName.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            return null;
        };

        /**
         * Creates a CModifyGuildName message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CModifyGuildName
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CModifyGuildName} CModifyGuildName
         */
        CModifyGuildName.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CModifyGuildName)
                return object;
            var message = new $root.game.CModifyGuildName();
            if (object.name != null)
                message.name = String(object.name);
            return message;
        };

        /**
         * Creates a plain object from a CModifyGuildName message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CModifyGuildName
         * @static
         * @param {game.CModifyGuildName} message CModifyGuildName
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CModifyGuildName.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.name = "";
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            return object;
        };

        /**
         * Converts this CModifyGuildName to JSON.
         * @function toJSON
         * @memberof game.CModifyGuildName
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CModifyGuildName.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CModifyGuildName
         * @function getTypeUrl
         * @memberof game.CModifyGuildName
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CModifyGuildName.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CModifyGuildName";
        };

        return CModifyGuildName;
    })();

    game.SModifyGuildName = (function() {

        /**
         * Properties of a SModifyGuildName.
         * @memberof game
         * @interface ISModifyGuildName
         */

        /**
         * Constructs a new SModifyGuildName.
         * @memberof game
         * @classdesc Represents a SModifyGuildName.
         * @implements ISModifyGuildName
         * @constructor
         * @param {game.ISModifyGuildName=} [properties] Properties to set
         */
        function SModifyGuildName(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SModifyGuildName instance using the specified properties.
         * @function create
         * @memberof game.SModifyGuildName
         * @static
         * @param {game.ISModifyGuildName=} [properties] Properties to set
         * @returns {game.SModifyGuildName} SModifyGuildName instance
         */
        SModifyGuildName.create = function create(properties) {
            return new SModifyGuildName(properties);
        };

        /**
         * Encodes the specified SModifyGuildName message. Does not implicitly {@link game.SModifyGuildName.verify|verify} messages.
         * @function encode
         * @memberof game.SModifyGuildName
         * @static
         * @param {game.ISModifyGuildName} message SModifyGuildName message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SModifyGuildName.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SModifyGuildName message, length delimited. Does not implicitly {@link game.SModifyGuildName.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SModifyGuildName
         * @static
         * @param {game.ISModifyGuildName} message SModifyGuildName message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SModifyGuildName.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SModifyGuildName message from the specified reader or buffer.
         * @function decode
         * @memberof game.SModifyGuildName
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SModifyGuildName} SModifyGuildName
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SModifyGuildName.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SModifyGuildName();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SModifyGuildName message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SModifyGuildName
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SModifyGuildName} SModifyGuildName
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SModifyGuildName.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SModifyGuildName message.
         * @function verify
         * @memberof game.SModifyGuildName
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SModifyGuildName.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SModifyGuildName message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SModifyGuildName
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SModifyGuildName} SModifyGuildName
         */
        SModifyGuildName.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SModifyGuildName)
                return object;
            return new $root.game.SModifyGuildName();
        };

        /**
         * Creates a plain object from a SModifyGuildName message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SModifyGuildName
         * @static
         * @param {game.SModifyGuildName} message SModifyGuildName
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SModifyGuildName.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SModifyGuildName to JSON.
         * @function toJSON
         * @memberof game.SModifyGuildName
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SModifyGuildName.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SModifyGuildName
         * @function getTypeUrl
         * @memberof game.SModifyGuildName
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SModifyGuildName.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SModifyGuildName";
        };

        return SModifyGuildName;
    })();

    game.CModifyNotice = (function() {

        /**
         * Properties of a CModifyNotice.
         * @memberof game
         * @interface ICModifyNotice
         * @property {string|null} [notice] CModifyNotice notice
         */

        /**
         * Constructs a new CModifyNotice.
         * @memberof game
         * @classdesc Represents a CModifyNotice.
         * @implements ICModifyNotice
         * @constructor
         * @param {game.ICModifyNotice=} [properties] Properties to set
         */
        function CModifyNotice(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CModifyNotice notice.
         * @member {string} notice
         * @memberof game.CModifyNotice
         * @instance
         */
        CModifyNotice.prototype.notice = "";

        /**
         * Creates a new CModifyNotice instance using the specified properties.
         * @function create
         * @memberof game.CModifyNotice
         * @static
         * @param {game.ICModifyNotice=} [properties] Properties to set
         * @returns {game.CModifyNotice} CModifyNotice instance
         */
        CModifyNotice.create = function create(properties) {
            return new CModifyNotice(properties);
        };

        /**
         * Encodes the specified CModifyNotice message. Does not implicitly {@link game.CModifyNotice.verify|verify} messages.
         * @function encode
         * @memberof game.CModifyNotice
         * @static
         * @param {game.ICModifyNotice} message CModifyNotice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CModifyNotice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.notice != null && Object.hasOwnProperty.call(message, "notice"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.notice);
            return writer;
        };

        /**
         * Encodes the specified CModifyNotice message, length delimited. Does not implicitly {@link game.CModifyNotice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CModifyNotice
         * @static
         * @param {game.ICModifyNotice} message CModifyNotice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CModifyNotice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CModifyNotice message from the specified reader or buffer.
         * @function decode
         * @memberof game.CModifyNotice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CModifyNotice} CModifyNotice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CModifyNotice.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CModifyNotice();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.notice = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CModifyNotice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CModifyNotice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CModifyNotice} CModifyNotice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CModifyNotice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CModifyNotice message.
         * @function verify
         * @memberof game.CModifyNotice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CModifyNotice.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.notice != null && message.hasOwnProperty("notice"))
                if (!$util.isString(message.notice))
                    return "notice: string expected";
            return null;
        };

        /**
         * Creates a CModifyNotice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CModifyNotice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CModifyNotice} CModifyNotice
         */
        CModifyNotice.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CModifyNotice)
                return object;
            var message = new $root.game.CModifyNotice();
            if (object.notice != null)
                message.notice = String(object.notice);
            return message;
        };

        /**
         * Creates a plain object from a CModifyNotice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CModifyNotice
         * @static
         * @param {game.CModifyNotice} message CModifyNotice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CModifyNotice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.notice = "";
            if (message.notice != null && message.hasOwnProperty("notice"))
                object.notice = message.notice;
            return object;
        };

        /**
         * Converts this CModifyNotice to JSON.
         * @function toJSON
         * @memberof game.CModifyNotice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CModifyNotice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CModifyNotice
         * @function getTypeUrl
         * @memberof game.CModifyNotice
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CModifyNotice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CModifyNotice";
        };

        return CModifyNotice;
    })();

    game.SModifyNotice = (function() {

        /**
         * Properties of a SModifyNotice.
         * @memberof game
         * @interface ISModifyNotice
         */

        /**
         * Constructs a new SModifyNotice.
         * @memberof game
         * @classdesc Represents a SModifyNotice.
         * @implements ISModifyNotice
         * @constructor
         * @param {game.ISModifyNotice=} [properties] Properties to set
         */
        function SModifyNotice(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SModifyNotice instance using the specified properties.
         * @function create
         * @memberof game.SModifyNotice
         * @static
         * @param {game.ISModifyNotice=} [properties] Properties to set
         * @returns {game.SModifyNotice} SModifyNotice instance
         */
        SModifyNotice.create = function create(properties) {
            return new SModifyNotice(properties);
        };

        /**
         * Encodes the specified SModifyNotice message. Does not implicitly {@link game.SModifyNotice.verify|verify} messages.
         * @function encode
         * @memberof game.SModifyNotice
         * @static
         * @param {game.ISModifyNotice} message SModifyNotice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SModifyNotice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SModifyNotice message, length delimited. Does not implicitly {@link game.SModifyNotice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SModifyNotice
         * @static
         * @param {game.ISModifyNotice} message SModifyNotice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SModifyNotice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SModifyNotice message from the specified reader or buffer.
         * @function decode
         * @memberof game.SModifyNotice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SModifyNotice} SModifyNotice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SModifyNotice.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SModifyNotice();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SModifyNotice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SModifyNotice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SModifyNotice} SModifyNotice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SModifyNotice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SModifyNotice message.
         * @function verify
         * @memberof game.SModifyNotice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SModifyNotice.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SModifyNotice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SModifyNotice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SModifyNotice} SModifyNotice
         */
        SModifyNotice.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SModifyNotice)
                return object;
            return new $root.game.SModifyNotice();
        };

        /**
         * Creates a plain object from a SModifyNotice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SModifyNotice
         * @static
         * @param {game.SModifyNotice} message SModifyNotice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SModifyNotice.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SModifyNotice to JSON.
         * @function toJSON
         * @memberof game.SModifyNotice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SModifyNotice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SModifyNotice
         * @function getTypeUrl
         * @memberof game.SModifyNotice
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SModifyNotice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SModifyNotice";
        };

        return SModifyNotice;
    })();

    game.CModifyFlag = (function() {

        /**
         * Properties of a CModifyFlag.
         * @memberof game
         * @interface ICModifyFlag
         * @property {number|null} [flag] CModifyFlag flag
         */

        /**
         * Constructs a new CModifyFlag.
         * @memberof game
         * @classdesc Represents a CModifyFlag.
         * @implements ICModifyFlag
         * @constructor
         * @param {game.ICModifyFlag=} [properties] Properties to set
         */
        function CModifyFlag(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CModifyFlag flag.
         * @member {number} flag
         * @memberof game.CModifyFlag
         * @instance
         */
        CModifyFlag.prototype.flag = 0;

        /**
         * Creates a new CModifyFlag instance using the specified properties.
         * @function create
         * @memberof game.CModifyFlag
         * @static
         * @param {game.ICModifyFlag=} [properties] Properties to set
         * @returns {game.CModifyFlag} CModifyFlag instance
         */
        CModifyFlag.create = function create(properties) {
            return new CModifyFlag(properties);
        };

        /**
         * Encodes the specified CModifyFlag message. Does not implicitly {@link game.CModifyFlag.verify|verify} messages.
         * @function encode
         * @memberof game.CModifyFlag
         * @static
         * @param {game.ICModifyFlag} message CModifyFlag message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CModifyFlag.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.flag);
            return writer;
        };

        /**
         * Encodes the specified CModifyFlag message, length delimited. Does not implicitly {@link game.CModifyFlag.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CModifyFlag
         * @static
         * @param {game.ICModifyFlag} message CModifyFlag message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CModifyFlag.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CModifyFlag message from the specified reader or buffer.
         * @function decode
         * @memberof game.CModifyFlag
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CModifyFlag} CModifyFlag
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CModifyFlag.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CModifyFlag();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.flag = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CModifyFlag message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CModifyFlag
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CModifyFlag} CModifyFlag
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CModifyFlag.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CModifyFlag message.
         * @function verify
         * @memberof game.CModifyFlag
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CModifyFlag.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.flag != null && message.hasOwnProperty("flag"))
                if (!$util.isInteger(message.flag))
                    return "flag: integer expected";
            return null;
        };

        /**
         * Creates a CModifyFlag message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CModifyFlag
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CModifyFlag} CModifyFlag
         */
        CModifyFlag.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CModifyFlag)
                return object;
            var message = new $root.game.CModifyFlag();
            if (object.flag != null)
                message.flag = object.flag >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a CModifyFlag message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CModifyFlag
         * @static
         * @param {game.CModifyFlag} message CModifyFlag
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CModifyFlag.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.flag = 0;
            if (message.flag != null && message.hasOwnProperty("flag"))
                object.flag = message.flag;
            return object;
        };

        /**
         * Converts this CModifyFlag to JSON.
         * @function toJSON
         * @memberof game.CModifyFlag
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CModifyFlag.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CModifyFlag
         * @function getTypeUrl
         * @memberof game.CModifyFlag
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CModifyFlag.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CModifyFlag";
        };

        return CModifyFlag;
    })();

    game.SModifyFlag = (function() {

        /**
         * Properties of a SModifyFlag.
         * @memberof game
         * @interface ISModifyFlag
         */

        /**
         * Constructs a new SModifyFlag.
         * @memberof game
         * @classdesc Represents a SModifyFlag.
         * @implements ISModifyFlag
         * @constructor
         * @param {game.ISModifyFlag=} [properties] Properties to set
         */
        function SModifyFlag(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SModifyFlag instance using the specified properties.
         * @function create
         * @memberof game.SModifyFlag
         * @static
         * @param {game.ISModifyFlag=} [properties] Properties to set
         * @returns {game.SModifyFlag} SModifyFlag instance
         */
        SModifyFlag.create = function create(properties) {
            return new SModifyFlag(properties);
        };

        /**
         * Encodes the specified SModifyFlag message. Does not implicitly {@link game.SModifyFlag.verify|verify} messages.
         * @function encode
         * @memberof game.SModifyFlag
         * @static
         * @param {game.ISModifyFlag} message SModifyFlag message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SModifyFlag.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SModifyFlag message, length delimited. Does not implicitly {@link game.SModifyFlag.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SModifyFlag
         * @static
         * @param {game.ISModifyFlag} message SModifyFlag message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SModifyFlag.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SModifyFlag message from the specified reader or buffer.
         * @function decode
         * @memberof game.SModifyFlag
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SModifyFlag} SModifyFlag
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SModifyFlag.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SModifyFlag();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SModifyFlag message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SModifyFlag
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SModifyFlag} SModifyFlag
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SModifyFlag.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SModifyFlag message.
         * @function verify
         * @memberof game.SModifyFlag
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SModifyFlag.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SModifyFlag message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SModifyFlag
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SModifyFlag} SModifyFlag
         */
        SModifyFlag.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SModifyFlag)
                return object;
            return new $root.game.SModifyFlag();
        };

        /**
         * Creates a plain object from a SModifyFlag message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SModifyFlag
         * @static
         * @param {game.SModifyFlag} message SModifyFlag
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SModifyFlag.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SModifyFlag to JSON.
         * @function toJSON
         * @memberof game.SModifyFlag
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SModifyFlag.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SModifyFlag
         * @function getTypeUrl
         * @memberof game.SModifyFlag
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SModifyFlag.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SModifyFlag";
        };

        return SModifyFlag;
    })();

    game.CModifyGuild = (function() {

        /**
         * Properties of a CModifyGuild.
         * @memberof game
         * @interface ICModifyGuild
         * @property {string|null} [name] CModifyGuild name
         * @property {number|null} [flag] CModifyGuild flag
         * @property {string|null} [location] CModifyGuild location
         * @property {number|null} [joinType] CModifyGuild joinType
         * @property {number|null} [joinLimit] CModifyGuild joinLimit
         */

        /**
         * Constructs a new CModifyGuild.
         * @memberof game
         * @classdesc Represents a CModifyGuild.
         * @implements ICModifyGuild
         * @constructor
         * @param {game.ICModifyGuild=} [properties] Properties to set
         */
        function CModifyGuild(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CModifyGuild name.
         * @member {string} name
         * @memberof game.CModifyGuild
         * @instance
         */
        CModifyGuild.prototype.name = "";

        /**
         * CModifyGuild flag.
         * @member {number} flag
         * @memberof game.CModifyGuild
         * @instance
         */
        CModifyGuild.prototype.flag = 0;

        /**
         * CModifyGuild location.
         * @member {string} location
         * @memberof game.CModifyGuild
         * @instance
         */
        CModifyGuild.prototype.location = "";

        /**
         * CModifyGuild joinType.
         * @member {number} joinType
         * @memberof game.CModifyGuild
         * @instance
         */
        CModifyGuild.prototype.joinType = 0;

        /**
         * CModifyGuild joinLimit.
         * @member {number} joinLimit
         * @memberof game.CModifyGuild
         * @instance
         */
        CModifyGuild.prototype.joinLimit = 0;

        /**
         * Creates a new CModifyGuild instance using the specified properties.
         * @function create
         * @memberof game.CModifyGuild
         * @static
         * @param {game.ICModifyGuild=} [properties] Properties to set
         * @returns {game.CModifyGuild} CModifyGuild instance
         */
        CModifyGuild.create = function create(properties) {
            return new CModifyGuild(properties);
        };

        /**
         * Encodes the specified CModifyGuild message. Does not implicitly {@link game.CModifyGuild.verify|verify} messages.
         * @function encode
         * @memberof game.CModifyGuild
         * @static
         * @param {game.ICModifyGuild} message CModifyGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CModifyGuild.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.flag);
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.location);
            if (message.joinType != null && Object.hasOwnProperty.call(message, "joinType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.joinType);
            if (message.joinLimit != null && Object.hasOwnProperty.call(message, "joinLimit"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.joinLimit);
            return writer;
        };

        /**
         * Encodes the specified CModifyGuild message, length delimited. Does not implicitly {@link game.CModifyGuild.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CModifyGuild
         * @static
         * @param {game.ICModifyGuild} message CModifyGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CModifyGuild.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CModifyGuild message from the specified reader or buffer.
         * @function decode
         * @memberof game.CModifyGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CModifyGuild} CModifyGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CModifyGuild.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CModifyGuild();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.flag = reader.uint32();
                        break;
                    }
                case 3: {
                        message.location = reader.string();
                        break;
                    }
                case 4: {
                        message.joinType = reader.int32();
                        break;
                    }
                case 5: {
                        message.joinLimit = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CModifyGuild message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CModifyGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CModifyGuild} CModifyGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CModifyGuild.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CModifyGuild message.
         * @function verify
         * @memberof game.CModifyGuild
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CModifyGuild.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.flag != null && message.hasOwnProperty("flag"))
                if (!$util.isInteger(message.flag))
                    return "flag: integer expected";
            if (message.location != null && message.hasOwnProperty("location"))
                if (!$util.isString(message.location))
                    return "location: string expected";
            if (message.joinType != null && message.hasOwnProperty("joinType"))
                if (!$util.isInteger(message.joinType))
                    return "joinType: integer expected";
            if (message.joinLimit != null && message.hasOwnProperty("joinLimit"))
                if (!$util.isInteger(message.joinLimit))
                    return "joinLimit: integer expected";
            return null;
        };

        /**
         * Creates a CModifyGuild message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CModifyGuild
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CModifyGuild} CModifyGuild
         */
        CModifyGuild.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CModifyGuild)
                return object;
            var message = new $root.game.CModifyGuild();
            if (object.name != null)
                message.name = String(object.name);
            if (object.flag != null)
                message.flag = object.flag >>> 0;
            if (object.location != null)
                message.location = String(object.location);
            if (object.joinType != null)
                message.joinType = object.joinType | 0;
            if (object.joinLimit != null)
                message.joinLimit = object.joinLimit | 0;
            return message;
        };

        /**
         * Creates a plain object from a CModifyGuild message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CModifyGuild
         * @static
         * @param {game.CModifyGuild} message CModifyGuild
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CModifyGuild.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.name = "";
                object.flag = 0;
                object.location = "";
                object.joinType = 0;
                object.joinLimit = 0;
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.flag != null && message.hasOwnProperty("flag"))
                object.flag = message.flag;
            if (message.location != null && message.hasOwnProperty("location"))
                object.location = message.location;
            if (message.joinType != null && message.hasOwnProperty("joinType"))
                object.joinType = message.joinType;
            if (message.joinLimit != null && message.hasOwnProperty("joinLimit"))
                object.joinLimit = message.joinLimit;
            return object;
        };

        /**
         * Converts this CModifyGuild to JSON.
         * @function toJSON
         * @memberof game.CModifyGuild
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CModifyGuild.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CModifyGuild
         * @function getTypeUrl
         * @memberof game.CModifyGuild
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CModifyGuild.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CModifyGuild";
        };

        return CModifyGuild;
    })();

    game.SModifyGuild = (function() {

        /**
         * Properties of a SModifyGuild.
         * @memberof game
         * @interface ISModifyGuild
         */

        /**
         * Constructs a new SModifyGuild.
         * @memberof game
         * @classdesc Represents a SModifyGuild.
         * @implements ISModifyGuild
         * @constructor
         * @param {game.ISModifyGuild=} [properties] Properties to set
         */
        function SModifyGuild(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SModifyGuild instance using the specified properties.
         * @function create
         * @memberof game.SModifyGuild
         * @static
         * @param {game.ISModifyGuild=} [properties] Properties to set
         * @returns {game.SModifyGuild} SModifyGuild instance
         */
        SModifyGuild.create = function create(properties) {
            return new SModifyGuild(properties);
        };

        /**
         * Encodes the specified SModifyGuild message. Does not implicitly {@link game.SModifyGuild.verify|verify} messages.
         * @function encode
         * @memberof game.SModifyGuild
         * @static
         * @param {game.ISModifyGuild} message SModifyGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SModifyGuild.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SModifyGuild message, length delimited. Does not implicitly {@link game.SModifyGuild.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SModifyGuild
         * @static
         * @param {game.ISModifyGuild} message SModifyGuild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SModifyGuild.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SModifyGuild message from the specified reader or buffer.
         * @function decode
         * @memberof game.SModifyGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SModifyGuild} SModifyGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SModifyGuild.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SModifyGuild();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SModifyGuild message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SModifyGuild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SModifyGuild} SModifyGuild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SModifyGuild.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SModifyGuild message.
         * @function verify
         * @memberof game.SModifyGuild
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SModifyGuild.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SModifyGuild message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SModifyGuild
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SModifyGuild} SModifyGuild
         */
        SModifyGuild.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SModifyGuild)
                return object;
            return new $root.game.SModifyGuild();
        };

        /**
         * Creates a plain object from a SModifyGuild message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SModifyGuild
         * @static
         * @param {game.SModifyGuild} message SModifyGuild
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SModifyGuild.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SModifyGuild to JSON.
         * @function toJSON
         * @memberof game.SModifyGuild
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SModifyGuild.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SModifyGuild
         * @function getTypeUrl
         * @memberof game.SModifyGuild
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SModifyGuild.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SModifyGuild";
        };

        return SModifyGuild;
    })();

    game.CSetMemberType = (function() {

        /**
         * Properties of a CSetMemberType.
         * @memberof game
         * @interface ICSetMemberType
         * @property {string|null} [targetUid] CSetMemberType targetUid
         * @property {string|null} [type] CSetMemberType type
         */

        /**
         * Constructs a new CSetMemberType.
         * @memberof game
         * @classdesc Represents a CSetMemberType.
         * @implements ICSetMemberType
         * @constructor
         * @param {game.ICSetMemberType=} [properties] Properties to set
         */
        function CSetMemberType(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSetMemberType targetUid.
         * @member {string} targetUid
         * @memberof game.CSetMemberType
         * @instance
         */
        CSetMemberType.prototype.targetUid = "";

        /**
         * CSetMemberType type.
         * @member {string} type
         * @memberof game.CSetMemberType
         * @instance
         */
        CSetMemberType.prototype.type = "";

        /**
         * Creates a new CSetMemberType instance using the specified properties.
         * @function create
         * @memberof game.CSetMemberType
         * @static
         * @param {game.ICSetMemberType=} [properties] Properties to set
         * @returns {game.CSetMemberType} CSetMemberType instance
         */
        CSetMemberType.create = function create(properties) {
            return new CSetMemberType(properties);
        };

        /**
         * Encodes the specified CSetMemberType message. Does not implicitly {@link game.CSetMemberType.verify|verify} messages.
         * @function encode
         * @memberof game.CSetMemberType
         * @static
         * @param {game.ICSetMemberType} message CSetMemberType message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSetMemberType.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.targetUid != null && Object.hasOwnProperty.call(message, "targetUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.targetUid);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.type);
            return writer;
        };

        /**
         * Encodes the specified CSetMemberType message, length delimited. Does not implicitly {@link game.CSetMemberType.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CSetMemberType
         * @static
         * @param {game.ICSetMemberType} message CSetMemberType message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSetMemberType.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSetMemberType message from the specified reader or buffer.
         * @function decode
         * @memberof game.CSetMemberType
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CSetMemberType} CSetMemberType
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSetMemberType.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CSetMemberType();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.targetUid = reader.string();
                        break;
                    }
                case 2: {
                        message.type = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSetMemberType message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CSetMemberType
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CSetMemberType} CSetMemberType
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSetMemberType.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSetMemberType message.
         * @function verify
         * @memberof game.CSetMemberType
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSetMemberType.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.targetUid != null && message.hasOwnProperty("targetUid"))
                if (!$util.isString(message.targetUid))
                    return "targetUid: string expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            return null;
        };

        /**
         * Creates a CSetMemberType message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CSetMemberType
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CSetMemberType} CSetMemberType
         */
        CSetMemberType.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CSetMemberType)
                return object;
            var message = new $root.game.CSetMemberType();
            if (object.targetUid != null)
                message.targetUid = String(object.targetUid);
            if (object.type != null)
                message.type = String(object.type);
            return message;
        };

        /**
         * Creates a plain object from a CSetMemberType message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CSetMemberType
         * @static
         * @param {game.CSetMemberType} message CSetMemberType
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSetMemberType.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.targetUid = "";
                object.type = "";
            }
            if (message.targetUid != null && message.hasOwnProperty("targetUid"))
                object.targetUid = message.targetUid;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        /**
         * Converts this CSetMemberType to JSON.
         * @function toJSON
         * @memberof game.CSetMemberType
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSetMemberType.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSetMemberType
         * @function getTypeUrl
         * @memberof game.CSetMemberType
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSetMemberType.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CSetMemberType";
        };

        return CSetMemberType;
    })();

    game.SSetMemberType = (function() {

        /**
         * Properties of a SSetMemberType.
         * @memberof game
         * @interface ISSetMemberType
         */

        /**
         * Constructs a new SSetMemberType.
         * @memberof game
         * @classdesc Represents a SSetMemberType.
         * @implements ISSetMemberType
         * @constructor
         * @param {game.ISSetMemberType=} [properties] Properties to set
         */
        function SSetMemberType(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SSetMemberType instance using the specified properties.
         * @function create
         * @memberof game.SSetMemberType
         * @static
         * @param {game.ISSetMemberType=} [properties] Properties to set
         * @returns {game.SSetMemberType} SSetMemberType instance
         */
        SSetMemberType.create = function create(properties) {
            return new SSetMemberType(properties);
        };

        /**
         * Encodes the specified SSetMemberType message. Does not implicitly {@link game.SSetMemberType.verify|verify} messages.
         * @function encode
         * @memberof game.SSetMemberType
         * @static
         * @param {game.ISSetMemberType} message SSetMemberType message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSetMemberType.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SSetMemberType message, length delimited. Does not implicitly {@link game.SSetMemberType.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SSetMemberType
         * @static
         * @param {game.ISSetMemberType} message SSetMemberType message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSetMemberType.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SSetMemberType message from the specified reader or buffer.
         * @function decode
         * @memberof game.SSetMemberType
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SSetMemberType} SSetMemberType
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSetMemberType.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SSetMemberType();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SSetMemberType message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SSetMemberType
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SSetMemberType} SSetMemberType
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSetMemberType.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SSetMemberType message.
         * @function verify
         * @memberof game.SSetMemberType
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SSetMemberType.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SSetMemberType message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SSetMemberType
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SSetMemberType} SSetMemberType
         */
        SSetMemberType.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SSetMemberType)
                return object;
            return new $root.game.SSetMemberType();
        };

        /**
         * Creates a plain object from a SSetMemberType message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SSetMemberType
         * @static
         * @param {game.SSetMemberType} message SSetMemberType
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SSetMemberType.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SSetMemberType to JSON.
         * @function toJSON
         * @memberof game.SSetMemberType
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SSetMemberType.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SSetMemberType
         * @function getTypeUrl
         * @memberof game.SSetMemberType
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SSetMemberType.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SSetMemberType";
        };

        return SSetMemberType;
    })();

    game.CTransferLeader = (function() {

        /**
         * Properties of a CTransferLeader.
         * @memberof game
         * @interface ICTransferLeader
         * @property {string|null} [targetUid] CTransferLeader targetUid
         */

        /**
         * Constructs a new CTransferLeader.
         * @memberof game
         * @classdesc Represents a CTransferLeader.
         * @implements ICTransferLeader
         * @constructor
         * @param {game.ICTransferLeader=} [properties] Properties to set
         */
        function CTransferLeader(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CTransferLeader targetUid.
         * @member {string} targetUid
         * @memberof game.CTransferLeader
         * @instance
         */
        CTransferLeader.prototype.targetUid = "";

        /**
         * Creates a new CTransferLeader instance using the specified properties.
         * @function create
         * @memberof game.CTransferLeader
         * @static
         * @param {game.ICTransferLeader=} [properties] Properties to set
         * @returns {game.CTransferLeader} CTransferLeader instance
         */
        CTransferLeader.create = function create(properties) {
            return new CTransferLeader(properties);
        };

        /**
         * Encodes the specified CTransferLeader message. Does not implicitly {@link game.CTransferLeader.verify|verify} messages.
         * @function encode
         * @memberof game.CTransferLeader
         * @static
         * @param {game.ICTransferLeader} message CTransferLeader message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CTransferLeader.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.targetUid != null && Object.hasOwnProperty.call(message, "targetUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.targetUid);
            return writer;
        };

        /**
         * Encodes the specified CTransferLeader message, length delimited. Does not implicitly {@link game.CTransferLeader.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CTransferLeader
         * @static
         * @param {game.ICTransferLeader} message CTransferLeader message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CTransferLeader.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CTransferLeader message from the specified reader or buffer.
         * @function decode
         * @memberof game.CTransferLeader
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CTransferLeader} CTransferLeader
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CTransferLeader.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CTransferLeader();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.targetUid = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CTransferLeader message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CTransferLeader
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CTransferLeader} CTransferLeader
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CTransferLeader.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CTransferLeader message.
         * @function verify
         * @memberof game.CTransferLeader
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CTransferLeader.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.targetUid != null && message.hasOwnProperty("targetUid"))
                if (!$util.isString(message.targetUid))
                    return "targetUid: string expected";
            return null;
        };

        /**
         * Creates a CTransferLeader message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CTransferLeader
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CTransferLeader} CTransferLeader
         */
        CTransferLeader.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CTransferLeader)
                return object;
            var message = new $root.game.CTransferLeader();
            if (object.targetUid != null)
                message.targetUid = String(object.targetUid);
            return message;
        };

        /**
         * Creates a plain object from a CTransferLeader message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CTransferLeader
         * @static
         * @param {game.CTransferLeader} message CTransferLeader
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CTransferLeader.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.targetUid = "";
            if (message.targetUid != null && message.hasOwnProperty("targetUid"))
                object.targetUid = message.targetUid;
            return object;
        };

        /**
         * Converts this CTransferLeader to JSON.
         * @function toJSON
         * @memberof game.CTransferLeader
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CTransferLeader.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CTransferLeader
         * @function getTypeUrl
         * @memberof game.CTransferLeader
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CTransferLeader.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CTransferLeader";
        };

        return CTransferLeader;
    })();

    game.STransferLeader = (function() {

        /**
         * Properties of a STransferLeader.
         * @memberof game
         * @interface ISTransferLeader
         */

        /**
         * Constructs a new STransferLeader.
         * @memberof game
         * @classdesc Represents a STransferLeader.
         * @implements ISTransferLeader
         * @constructor
         * @param {game.ISTransferLeader=} [properties] Properties to set
         */
        function STransferLeader(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new STransferLeader instance using the specified properties.
         * @function create
         * @memberof game.STransferLeader
         * @static
         * @param {game.ISTransferLeader=} [properties] Properties to set
         * @returns {game.STransferLeader} STransferLeader instance
         */
        STransferLeader.create = function create(properties) {
            return new STransferLeader(properties);
        };

        /**
         * Encodes the specified STransferLeader message. Does not implicitly {@link game.STransferLeader.verify|verify} messages.
         * @function encode
         * @memberof game.STransferLeader
         * @static
         * @param {game.ISTransferLeader} message STransferLeader message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        STransferLeader.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified STransferLeader message, length delimited. Does not implicitly {@link game.STransferLeader.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.STransferLeader
         * @static
         * @param {game.ISTransferLeader} message STransferLeader message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        STransferLeader.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a STransferLeader message from the specified reader or buffer.
         * @function decode
         * @memberof game.STransferLeader
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.STransferLeader} STransferLeader
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        STransferLeader.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.STransferLeader();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a STransferLeader message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.STransferLeader
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.STransferLeader} STransferLeader
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        STransferLeader.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a STransferLeader message.
         * @function verify
         * @memberof game.STransferLeader
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        STransferLeader.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a STransferLeader message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.STransferLeader
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.STransferLeader} STransferLeader
         */
        STransferLeader.fromObject = function fromObject(object) {
            if (object instanceof $root.game.STransferLeader)
                return object;
            return new $root.game.STransferLeader();
        };

        /**
         * Creates a plain object from a STransferLeader message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.STransferLeader
         * @static
         * @param {game.STransferLeader} message STransferLeader
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        STransferLeader.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this STransferLeader to JSON.
         * @function toJSON
         * @memberof game.STransferLeader
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        STransferLeader.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for STransferLeader
         * @function getTypeUrl
         * @memberof game.STransferLeader
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        STransferLeader.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.STransferLeader";
        };

        return STransferLeader;
    })();

    game.CClaimLeagueReward = (function() {

        /**
         * Properties of a CClaimLeagueReward.
         * @memberof game
         * @interface ICClaimLeagueReward
         * @property {number|null} [level] CClaimLeagueReward level
         * @property {number|null} [id] CClaimLeagueReward id
         * @property {number|null} [index] CClaimLeagueReward index
         */

        /**
         * Constructs a new CClaimLeagueReward.
         * @memberof game
         * @classdesc Represents a CClaimLeagueReward.
         * @implements ICClaimLeagueReward
         * @constructor
         * @param {game.ICClaimLeagueReward=} [properties] Properties to set
         */
        function CClaimLeagueReward(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CClaimLeagueReward level.
         * @member {number} level
         * @memberof game.CClaimLeagueReward
         * @instance
         */
        CClaimLeagueReward.prototype.level = 0;

        /**
         * CClaimLeagueReward id.
         * @member {number} id
         * @memberof game.CClaimLeagueReward
         * @instance
         */
        CClaimLeagueReward.prototype.id = 0;

        /**
         * CClaimLeagueReward index.
         * @member {number} index
         * @memberof game.CClaimLeagueReward
         * @instance
         */
        CClaimLeagueReward.prototype.index = 0;

        /**
         * Creates a new CClaimLeagueReward instance using the specified properties.
         * @function create
         * @memberof game.CClaimLeagueReward
         * @static
         * @param {game.ICClaimLeagueReward=} [properties] Properties to set
         * @returns {game.CClaimLeagueReward} CClaimLeagueReward instance
         */
        CClaimLeagueReward.create = function create(properties) {
            return new CClaimLeagueReward(properties);
        };

        /**
         * Encodes the specified CClaimLeagueReward message. Does not implicitly {@link game.CClaimLeagueReward.verify|verify} messages.
         * @function encode
         * @memberof game.CClaimLeagueReward
         * @static
         * @param {game.ICClaimLeagueReward} message CClaimLeagueReward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CClaimLeagueReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.level);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.id);
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.index);
            return writer;
        };

        /**
         * Encodes the specified CClaimLeagueReward message, length delimited. Does not implicitly {@link game.CClaimLeagueReward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CClaimLeagueReward
         * @static
         * @param {game.ICClaimLeagueReward} message CClaimLeagueReward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CClaimLeagueReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CClaimLeagueReward message from the specified reader or buffer.
         * @function decode
         * @memberof game.CClaimLeagueReward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CClaimLeagueReward} CClaimLeagueReward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CClaimLeagueReward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CClaimLeagueReward();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.level = reader.int32();
                        break;
                    }
                case 2: {
                        message.id = reader.int32();
                        break;
                    }
                case 3: {
                        message.index = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CClaimLeagueReward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CClaimLeagueReward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CClaimLeagueReward} CClaimLeagueReward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CClaimLeagueReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CClaimLeagueReward message.
         * @function verify
         * @memberof game.CClaimLeagueReward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CClaimLeagueReward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            return null;
        };

        /**
         * Creates a CClaimLeagueReward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CClaimLeagueReward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CClaimLeagueReward} CClaimLeagueReward
         */
        CClaimLeagueReward.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CClaimLeagueReward)
                return object;
            var message = new $root.game.CClaimLeagueReward();
            if (object.level != null)
                message.level = object.level | 0;
            if (object.id != null)
                message.id = object.id | 0;
            if (object.index != null)
                message.index = object.index | 0;
            return message;
        };

        /**
         * Creates a plain object from a CClaimLeagueReward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CClaimLeagueReward
         * @static
         * @param {game.CClaimLeagueReward} message CClaimLeagueReward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CClaimLeagueReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.level = 0;
                object.id = 0;
                object.index = 0;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.index != null && message.hasOwnProperty("index"))
                object.index = message.index;
            return object;
        };

        /**
         * Converts this CClaimLeagueReward to JSON.
         * @function toJSON
         * @memberof game.CClaimLeagueReward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CClaimLeagueReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CClaimLeagueReward
         * @function getTypeUrl
         * @memberof game.CClaimLeagueReward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CClaimLeagueReward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CClaimLeagueReward";
        };

        return CClaimLeagueReward;
    })();

    game.SClaimLeagueReward = (function() {

        /**
         * Properties of a SClaimLeagueReward.
         * @memberof game
         * @interface ISClaimLeagueReward
         * @property {Array.<common.IReward>|null} [awards] SClaimLeagueReward awards
         */

        /**
         * Constructs a new SClaimLeagueReward.
         * @memberof game
         * @classdesc Represents a SClaimLeagueReward.
         * @implements ISClaimLeagueReward
         * @constructor
         * @param {game.ISClaimLeagueReward=} [properties] Properties to set
         */
        function SClaimLeagueReward(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SClaimLeagueReward awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SClaimLeagueReward
         * @instance
         */
        SClaimLeagueReward.prototype.awards = $util.emptyArray;

        /**
         * Creates a new SClaimLeagueReward instance using the specified properties.
         * @function create
         * @memberof game.SClaimLeagueReward
         * @static
         * @param {game.ISClaimLeagueReward=} [properties] Properties to set
         * @returns {game.SClaimLeagueReward} SClaimLeagueReward instance
         */
        SClaimLeagueReward.create = function create(properties) {
            return new SClaimLeagueReward(properties);
        };

        /**
         * Encodes the specified SClaimLeagueReward message. Does not implicitly {@link game.SClaimLeagueReward.verify|verify} messages.
         * @function encode
         * @memberof game.SClaimLeagueReward
         * @static
         * @param {game.ISClaimLeagueReward} message SClaimLeagueReward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SClaimLeagueReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SClaimLeagueReward message, length delimited. Does not implicitly {@link game.SClaimLeagueReward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SClaimLeagueReward
         * @static
         * @param {game.ISClaimLeagueReward} message SClaimLeagueReward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SClaimLeagueReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SClaimLeagueReward message from the specified reader or buffer.
         * @function decode
         * @memberof game.SClaimLeagueReward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SClaimLeagueReward} SClaimLeagueReward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SClaimLeagueReward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SClaimLeagueReward();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SClaimLeagueReward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SClaimLeagueReward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SClaimLeagueReward} SClaimLeagueReward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SClaimLeagueReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SClaimLeagueReward message.
         * @function verify
         * @memberof game.SClaimLeagueReward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SClaimLeagueReward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SClaimLeagueReward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SClaimLeagueReward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SClaimLeagueReward} SClaimLeagueReward
         */
        SClaimLeagueReward.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SClaimLeagueReward)
                return object;
            var message = new $root.game.SClaimLeagueReward();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SClaimLeagueReward.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SClaimLeagueReward.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SClaimLeagueReward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SClaimLeagueReward
         * @static
         * @param {game.SClaimLeagueReward} message SClaimLeagueReward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SClaimLeagueReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            return object;
        };

        /**
         * Converts this SClaimLeagueReward to JSON.
         * @function toJSON
         * @memberof game.SClaimLeagueReward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SClaimLeagueReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SClaimLeagueReward
         * @function getTypeUrl
         * @memberof game.SClaimLeagueReward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SClaimLeagueReward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SClaimLeagueReward";
        };

        return SClaimLeagueReward;
    })();

    game.CHeartBeat = (function() {

        /**
         * Properties of a CHeartBeat.
         * @memberof game
         * @interface ICHeartBeat
         */

        /**
         * Constructs a new CHeartBeat.
         * @memberof game
         * @classdesc Represents a CHeartBeat.
         * @implements ICHeartBeat
         * @constructor
         * @param {game.ICHeartBeat=} [properties] Properties to set
         */
        function CHeartBeat(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CHeartBeat instance using the specified properties.
         * @function create
         * @memberof game.CHeartBeat
         * @static
         * @param {game.ICHeartBeat=} [properties] Properties to set
         * @returns {game.CHeartBeat} CHeartBeat instance
         */
        CHeartBeat.create = function create(properties) {
            return new CHeartBeat(properties);
        };

        /**
         * Encodes the specified CHeartBeat message. Does not implicitly {@link game.CHeartBeat.verify|verify} messages.
         * @function encode
         * @memberof game.CHeartBeat
         * @static
         * @param {game.ICHeartBeat} message CHeartBeat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CHeartBeat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CHeartBeat message, length delimited. Does not implicitly {@link game.CHeartBeat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CHeartBeat
         * @static
         * @param {game.ICHeartBeat} message CHeartBeat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CHeartBeat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CHeartBeat message from the specified reader or buffer.
         * @function decode
         * @memberof game.CHeartBeat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CHeartBeat} CHeartBeat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CHeartBeat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CHeartBeat();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CHeartBeat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CHeartBeat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CHeartBeat} CHeartBeat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CHeartBeat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CHeartBeat message.
         * @function verify
         * @memberof game.CHeartBeat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CHeartBeat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CHeartBeat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CHeartBeat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CHeartBeat} CHeartBeat
         */
        CHeartBeat.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CHeartBeat)
                return object;
            return new $root.game.CHeartBeat();
        };

        /**
         * Creates a plain object from a CHeartBeat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CHeartBeat
         * @static
         * @param {game.CHeartBeat} message CHeartBeat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CHeartBeat.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CHeartBeat to JSON.
         * @function toJSON
         * @memberof game.CHeartBeat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CHeartBeat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CHeartBeat
         * @function getTypeUrl
         * @memberof game.CHeartBeat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CHeartBeat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CHeartBeat";
        };

        return CHeartBeat;
    })();

    game.SHeartBeat = (function() {

        /**
         * Properties of a SHeartBeat.
         * @memberof game
         * @interface ISHeartBeat
         * @property {number|Long|null} [timestamp] SHeartBeat timestamp
         */

        /**
         * Constructs a new SHeartBeat.
         * @memberof game
         * @classdesc Represents a SHeartBeat.
         * @implements ISHeartBeat
         * @constructor
         * @param {game.ISHeartBeat=} [properties] Properties to set
         */
        function SHeartBeat(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SHeartBeat timestamp.
         * @member {number|Long} timestamp
         * @memberof game.SHeartBeat
         * @instance
         */
        SHeartBeat.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new SHeartBeat instance using the specified properties.
         * @function create
         * @memberof game.SHeartBeat
         * @static
         * @param {game.ISHeartBeat=} [properties] Properties to set
         * @returns {game.SHeartBeat} SHeartBeat instance
         */
        SHeartBeat.create = function create(properties) {
            return new SHeartBeat(properties);
        };

        /**
         * Encodes the specified SHeartBeat message. Does not implicitly {@link game.SHeartBeat.verify|verify} messages.
         * @function encode
         * @memberof game.SHeartBeat
         * @static
         * @param {game.ISHeartBeat} message SHeartBeat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SHeartBeat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.timestamp);
            return writer;
        };

        /**
         * Encodes the specified SHeartBeat message, length delimited. Does not implicitly {@link game.SHeartBeat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SHeartBeat
         * @static
         * @param {game.ISHeartBeat} message SHeartBeat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SHeartBeat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SHeartBeat message from the specified reader or buffer.
         * @function decode
         * @memberof game.SHeartBeat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SHeartBeat} SHeartBeat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SHeartBeat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SHeartBeat();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.timestamp = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SHeartBeat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SHeartBeat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SHeartBeat} SHeartBeat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SHeartBeat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SHeartBeat message.
         * @function verify
         * @memberof game.SHeartBeat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SHeartBeat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            return null;
        };

        /**
         * Creates a SHeartBeat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SHeartBeat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SHeartBeat} SHeartBeat
         */
        SHeartBeat.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SHeartBeat)
                return object;
            var message = new $root.game.SHeartBeat();
            if (object.timestamp != null)
                if ($util.Long)
                    (message.timestamp = $util.Long.fromValue(object.timestamp)).unsigned = false;
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a SHeartBeat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SHeartBeat
         * @static
         * @param {game.SHeartBeat} message SHeartBeat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SHeartBeat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.timestamp = options.longs === String ? "0" : 0;
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
            return object;
        };

        /**
         * Converts this SHeartBeat to JSON.
         * @function toJSON
         * @memberof game.SHeartBeat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SHeartBeat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SHeartBeat
         * @function getTypeUrl
         * @memberof game.SHeartBeat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SHeartBeat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SHeartBeat";
        };

        return SHeartBeat;
    })();

    game.CLoginGame = (function() {

        /**
         * Properties of a CLoginGame.
         * @memberof game
         * @interface ICLoginGame
         */

        /**
         * Constructs a new CLoginGame.
         * @memberof game
         * @classdesc Represents a CLoginGame.
         * @implements ICLoginGame
         * @constructor
         * @param {game.ICLoginGame=} [properties] Properties to set
         */
        function CLoginGame(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CLoginGame instance using the specified properties.
         * @function create
         * @memberof game.CLoginGame
         * @static
         * @param {game.ICLoginGame=} [properties] Properties to set
         * @returns {game.CLoginGame} CLoginGame instance
         */
        CLoginGame.create = function create(properties) {
            return new CLoginGame(properties);
        };

        /**
         * Encodes the specified CLoginGame message. Does not implicitly {@link game.CLoginGame.verify|verify} messages.
         * @function encode
         * @memberof game.CLoginGame
         * @static
         * @param {game.ICLoginGame} message CLoginGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CLoginGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CLoginGame message, length delimited. Does not implicitly {@link game.CLoginGame.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CLoginGame
         * @static
         * @param {game.ICLoginGame} message CLoginGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CLoginGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CLoginGame message from the specified reader or buffer.
         * @function decode
         * @memberof game.CLoginGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CLoginGame} CLoginGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CLoginGame.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CLoginGame();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CLoginGame message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CLoginGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CLoginGame} CLoginGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CLoginGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CLoginGame message.
         * @function verify
         * @memberof game.CLoginGame
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CLoginGame.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CLoginGame message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CLoginGame
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CLoginGame} CLoginGame
         */
        CLoginGame.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CLoginGame)
                return object;
            return new $root.game.CLoginGame();
        };

        /**
         * Creates a plain object from a CLoginGame message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CLoginGame
         * @static
         * @param {game.CLoginGame} message CLoginGame
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CLoginGame.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CLoginGame to JSON.
         * @function toJSON
         * @memberof game.CLoginGame
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CLoginGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CLoginGame
         * @function getTypeUrl
         * @memberof game.CLoginGame
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CLoginGame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CLoginGame";
        };

        return CLoginGame;
    })();

    game.SLoginGame = (function() {

        /**
         * Properties of a SLoginGame.
         * @memberof game
         * @interface ISLoginGame
         * @property {common.IUserProfile|null} [userProfile] SLoginGame userProfile
         */

        /**
         * Constructs a new SLoginGame.
         * @memberof game
         * @classdesc Represents a SLoginGame.
         * @implements ISLoginGame
         * @constructor
         * @param {game.ISLoginGame=} [properties] Properties to set
         */
        function SLoginGame(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SLoginGame userProfile.
         * @member {common.IUserProfile|null|undefined} userProfile
         * @memberof game.SLoginGame
         * @instance
         */
        SLoginGame.prototype.userProfile = null;

        /**
         * Creates a new SLoginGame instance using the specified properties.
         * @function create
         * @memberof game.SLoginGame
         * @static
         * @param {game.ISLoginGame=} [properties] Properties to set
         * @returns {game.SLoginGame} SLoginGame instance
         */
        SLoginGame.create = function create(properties) {
            return new SLoginGame(properties);
        };

        /**
         * Encodes the specified SLoginGame message. Does not implicitly {@link game.SLoginGame.verify|verify} messages.
         * @function encode
         * @memberof game.SLoginGame
         * @static
         * @param {game.ISLoginGame} message SLoginGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SLoginGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.userProfile != null && Object.hasOwnProperty.call(message, "userProfile"))
                $root.common.UserProfile.encode(message.userProfile, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SLoginGame message, length delimited. Does not implicitly {@link game.SLoginGame.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SLoginGame
         * @static
         * @param {game.ISLoginGame} message SLoginGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SLoginGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SLoginGame message from the specified reader or buffer.
         * @function decode
         * @memberof game.SLoginGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SLoginGame} SLoginGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SLoginGame.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SLoginGame();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.userProfile = $root.common.UserProfile.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SLoginGame message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SLoginGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SLoginGame} SLoginGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SLoginGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SLoginGame message.
         * @function verify
         * @memberof game.SLoginGame
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SLoginGame.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.userProfile != null && message.hasOwnProperty("userProfile")) {
                var error = $root.common.UserProfile.verify(message.userProfile);
                if (error)
                    return "userProfile." + error;
            }
            return null;
        };

        /**
         * Creates a SLoginGame message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SLoginGame
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SLoginGame} SLoginGame
         */
        SLoginGame.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SLoginGame)
                return object;
            var message = new $root.game.SLoginGame();
            if (object.userProfile != null) {
                if (typeof object.userProfile !== "object")
                    throw TypeError(".game.SLoginGame.userProfile: object expected");
                message.userProfile = $root.common.UserProfile.fromObject(object.userProfile);
            }
            return message;
        };

        /**
         * Creates a plain object from a SLoginGame message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SLoginGame
         * @static
         * @param {game.SLoginGame} message SLoginGame
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SLoginGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.userProfile = null;
            if (message.userProfile != null && message.hasOwnProperty("userProfile"))
                object.userProfile = $root.common.UserProfile.toObject(message.userProfile, options);
            return object;
        };

        /**
         * Converts this SLoginGame to JSON.
         * @function toJSON
         * @memberof game.SLoginGame
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SLoginGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SLoginGame
         * @function getTypeUrl
         * @memberof game.SLoginGame
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SLoginGame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SLoginGame";
        };

        return SLoginGame;
    })();

    game.CLogoutGame = (function() {

        /**
         * Properties of a CLogoutGame.
         * @memberof game
         * @interface ICLogoutGame
         */

        /**
         * Constructs a new CLogoutGame.
         * @memberof game
         * @classdesc Represents a CLogoutGame.
         * @implements ICLogoutGame
         * @constructor
         * @param {game.ICLogoutGame=} [properties] Properties to set
         */
        function CLogoutGame(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CLogoutGame instance using the specified properties.
         * @function create
         * @memberof game.CLogoutGame
         * @static
         * @param {game.ICLogoutGame=} [properties] Properties to set
         * @returns {game.CLogoutGame} CLogoutGame instance
         */
        CLogoutGame.create = function create(properties) {
            return new CLogoutGame(properties);
        };

        /**
         * Encodes the specified CLogoutGame message. Does not implicitly {@link game.CLogoutGame.verify|verify} messages.
         * @function encode
         * @memberof game.CLogoutGame
         * @static
         * @param {game.ICLogoutGame} message CLogoutGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CLogoutGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CLogoutGame message, length delimited. Does not implicitly {@link game.CLogoutGame.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CLogoutGame
         * @static
         * @param {game.ICLogoutGame} message CLogoutGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CLogoutGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CLogoutGame message from the specified reader or buffer.
         * @function decode
         * @memberof game.CLogoutGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CLogoutGame} CLogoutGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CLogoutGame.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CLogoutGame();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CLogoutGame message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CLogoutGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CLogoutGame} CLogoutGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CLogoutGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CLogoutGame message.
         * @function verify
         * @memberof game.CLogoutGame
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CLogoutGame.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CLogoutGame message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CLogoutGame
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CLogoutGame} CLogoutGame
         */
        CLogoutGame.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CLogoutGame)
                return object;
            return new $root.game.CLogoutGame();
        };

        /**
         * Creates a plain object from a CLogoutGame message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CLogoutGame
         * @static
         * @param {game.CLogoutGame} message CLogoutGame
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CLogoutGame.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CLogoutGame to JSON.
         * @function toJSON
         * @memberof game.CLogoutGame
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CLogoutGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CLogoutGame
         * @function getTypeUrl
         * @memberof game.CLogoutGame
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CLogoutGame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CLogoutGame";
        };

        return CLogoutGame;
    })();

    game.SLogoutGame = (function() {

        /**
         * Properties of a SLogoutGame.
         * @memberof game
         * @interface ISLogoutGame
         */

        /**
         * Constructs a new SLogoutGame.
         * @memberof game
         * @classdesc Represents a SLogoutGame.
         * @implements ISLogoutGame
         * @constructor
         * @param {game.ISLogoutGame=} [properties] Properties to set
         */
        function SLogoutGame(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SLogoutGame instance using the specified properties.
         * @function create
         * @memberof game.SLogoutGame
         * @static
         * @param {game.ISLogoutGame=} [properties] Properties to set
         * @returns {game.SLogoutGame} SLogoutGame instance
         */
        SLogoutGame.create = function create(properties) {
            return new SLogoutGame(properties);
        };

        /**
         * Encodes the specified SLogoutGame message. Does not implicitly {@link game.SLogoutGame.verify|verify} messages.
         * @function encode
         * @memberof game.SLogoutGame
         * @static
         * @param {game.ISLogoutGame} message SLogoutGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SLogoutGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SLogoutGame message, length delimited. Does not implicitly {@link game.SLogoutGame.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SLogoutGame
         * @static
         * @param {game.ISLogoutGame} message SLogoutGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SLogoutGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SLogoutGame message from the specified reader or buffer.
         * @function decode
         * @memberof game.SLogoutGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SLogoutGame} SLogoutGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SLogoutGame.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SLogoutGame();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SLogoutGame message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SLogoutGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SLogoutGame} SLogoutGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SLogoutGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SLogoutGame message.
         * @function verify
         * @memberof game.SLogoutGame
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SLogoutGame.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SLogoutGame message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SLogoutGame
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SLogoutGame} SLogoutGame
         */
        SLogoutGame.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SLogoutGame)
                return object;
            return new $root.game.SLogoutGame();
        };

        /**
         * Creates a plain object from a SLogoutGame message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SLogoutGame
         * @static
         * @param {game.SLogoutGame} message SLogoutGame
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SLogoutGame.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SLogoutGame to JSON.
         * @function toJSON
         * @memberof game.SLogoutGame
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SLogoutGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SLogoutGame
         * @function getTypeUrl
         * @memberof game.SLogoutGame
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SLogoutGame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SLogoutGame";
        };

        return SLogoutGame;
    })();

    game.CViewUserInfo = (function() {

        /**
         * Properties of a CViewUserInfo.
         * @memberof game
         * @interface ICViewUserInfo
         */

        /**
         * Constructs a new CViewUserInfo.
         * @memberof game
         * @classdesc Represents a CViewUserInfo.
         * @implements ICViewUserInfo
         * @constructor
         * @param {game.ICViewUserInfo=} [properties] Properties to set
         */
        function CViewUserInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CViewUserInfo instance using the specified properties.
         * @function create
         * @memberof game.CViewUserInfo
         * @static
         * @param {game.ICViewUserInfo=} [properties] Properties to set
         * @returns {game.CViewUserInfo} CViewUserInfo instance
         */
        CViewUserInfo.create = function create(properties) {
            return new CViewUserInfo(properties);
        };

        /**
         * Encodes the specified CViewUserInfo message. Does not implicitly {@link game.CViewUserInfo.verify|verify} messages.
         * @function encode
         * @memberof game.CViewUserInfo
         * @static
         * @param {game.ICViewUserInfo} message CViewUserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CViewUserInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CViewUserInfo message, length delimited. Does not implicitly {@link game.CViewUserInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CViewUserInfo
         * @static
         * @param {game.ICViewUserInfo} message CViewUserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CViewUserInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CViewUserInfo message from the specified reader or buffer.
         * @function decode
         * @memberof game.CViewUserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CViewUserInfo} CViewUserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CViewUserInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CViewUserInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CViewUserInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CViewUserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CViewUserInfo} CViewUserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CViewUserInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CViewUserInfo message.
         * @function verify
         * @memberof game.CViewUserInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CViewUserInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CViewUserInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CViewUserInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CViewUserInfo} CViewUserInfo
         */
        CViewUserInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CViewUserInfo)
                return object;
            return new $root.game.CViewUserInfo();
        };

        /**
         * Creates a plain object from a CViewUserInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CViewUserInfo
         * @static
         * @param {game.CViewUserInfo} message CViewUserInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CViewUserInfo.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CViewUserInfo to JSON.
         * @function toJSON
         * @memberof game.CViewUserInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CViewUserInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CViewUserInfo
         * @function getTypeUrl
         * @memberof game.CViewUserInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CViewUserInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CViewUserInfo";
        };

        return CViewUserInfo;
    })();

    game.SViewUserInfo = (function() {

        /**
         * Properties of a SViewUserInfo.
         * @memberof game
         * @interface ISViewUserInfo
         */

        /**
         * Constructs a new SViewUserInfo.
         * @memberof game
         * @classdesc Represents a SViewUserInfo.
         * @implements ISViewUserInfo
         * @constructor
         * @param {game.ISViewUserInfo=} [properties] Properties to set
         */
        function SViewUserInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SViewUserInfo instance using the specified properties.
         * @function create
         * @memberof game.SViewUserInfo
         * @static
         * @param {game.ISViewUserInfo=} [properties] Properties to set
         * @returns {game.SViewUserInfo} SViewUserInfo instance
         */
        SViewUserInfo.create = function create(properties) {
            return new SViewUserInfo(properties);
        };

        /**
         * Encodes the specified SViewUserInfo message. Does not implicitly {@link game.SViewUserInfo.verify|verify} messages.
         * @function encode
         * @memberof game.SViewUserInfo
         * @static
         * @param {game.ISViewUserInfo} message SViewUserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SViewUserInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SViewUserInfo message, length delimited. Does not implicitly {@link game.SViewUserInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SViewUserInfo
         * @static
         * @param {game.ISViewUserInfo} message SViewUserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SViewUserInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SViewUserInfo message from the specified reader or buffer.
         * @function decode
         * @memberof game.SViewUserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SViewUserInfo} SViewUserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SViewUserInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SViewUserInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SViewUserInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SViewUserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SViewUserInfo} SViewUserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SViewUserInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SViewUserInfo message.
         * @function verify
         * @memberof game.SViewUserInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SViewUserInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SViewUserInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SViewUserInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SViewUserInfo} SViewUserInfo
         */
        SViewUserInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SViewUserInfo)
                return object;
            return new $root.game.SViewUserInfo();
        };

        /**
         * Creates a plain object from a SViewUserInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SViewUserInfo
         * @static
         * @param {game.SViewUserInfo} message SViewUserInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SViewUserInfo.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SViewUserInfo to JSON.
         * @function toJSON
         * @memberof game.SViewUserInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SViewUserInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SViewUserInfo
         * @function getTypeUrl
         * @memberof game.SViewUserInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SViewUserInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SViewUserInfo";
        };

        return SViewUserInfo;
    })();

    game.CModifyName = (function() {

        /**
         * Properties of a CModifyName.
         * @memberof game
         * @interface ICModifyName
         * @property {string|null} [newName] CModifyName newName
         */

        /**
         * Constructs a new CModifyName.
         * @memberof game
         * @classdesc Represents a CModifyName.
         * @implements ICModifyName
         * @constructor
         * @param {game.ICModifyName=} [properties] Properties to set
         */
        function CModifyName(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CModifyName newName.
         * @member {string} newName
         * @memberof game.CModifyName
         * @instance
         */
        CModifyName.prototype.newName = "";

        /**
         * Creates a new CModifyName instance using the specified properties.
         * @function create
         * @memberof game.CModifyName
         * @static
         * @param {game.ICModifyName=} [properties] Properties to set
         * @returns {game.CModifyName} CModifyName instance
         */
        CModifyName.create = function create(properties) {
            return new CModifyName(properties);
        };

        /**
         * Encodes the specified CModifyName message. Does not implicitly {@link game.CModifyName.verify|verify} messages.
         * @function encode
         * @memberof game.CModifyName
         * @static
         * @param {game.ICModifyName} message CModifyName message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CModifyName.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.newName != null && Object.hasOwnProperty.call(message, "newName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.newName);
            return writer;
        };

        /**
         * Encodes the specified CModifyName message, length delimited. Does not implicitly {@link game.CModifyName.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CModifyName
         * @static
         * @param {game.ICModifyName} message CModifyName message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CModifyName.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CModifyName message from the specified reader or buffer.
         * @function decode
         * @memberof game.CModifyName
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CModifyName} CModifyName
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CModifyName.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CModifyName();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.newName = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CModifyName message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CModifyName
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CModifyName} CModifyName
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CModifyName.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CModifyName message.
         * @function verify
         * @memberof game.CModifyName
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CModifyName.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.newName != null && message.hasOwnProperty("newName"))
                if (!$util.isString(message.newName))
                    return "newName: string expected";
            return null;
        };

        /**
         * Creates a CModifyName message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CModifyName
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CModifyName} CModifyName
         */
        CModifyName.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CModifyName)
                return object;
            var message = new $root.game.CModifyName();
            if (object.newName != null)
                message.newName = String(object.newName);
            return message;
        };

        /**
         * Creates a plain object from a CModifyName message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CModifyName
         * @static
         * @param {game.CModifyName} message CModifyName
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CModifyName.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.newName = "";
            if (message.newName != null && message.hasOwnProperty("newName"))
                object.newName = message.newName;
            return object;
        };

        /**
         * Converts this CModifyName to JSON.
         * @function toJSON
         * @memberof game.CModifyName
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CModifyName.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CModifyName
         * @function getTypeUrl
         * @memberof game.CModifyName
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CModifyName.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CModifyName";
        };

        return CModifyName;
    })();

    game.SModifyName = (function() {

        /**
         * Properties of a SModifyName.
         * @memberof game
         * @interface ISModifyName
         */

        /**
         * Constructs a new SModifyName.
         * @memberof game
         * @classdesc Represents a SModifyName.
         * @implements ISModifyName
         * @constructor
         * @param {game.ISModifyName=} [properties] Properties to set
         */
        function SModifyName(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SModifyName instance using the specified properties.
         * @function create
         * @memberof game.SModifyName
         * @static
         * @param {game.ISModifyName=} [properties] Properties to set
         * @returns {game.SModifyName} SModifyName instance
         */
        SModifyName.create = function create(properties) {
            return new SModifyName(properties);
        };

        /**
         * Encodes the specified SModifyName message. Does not implicitly {@link game.SModifyName.verify|verify} messages.
         * @function encode
         * @memberof game.SModifyName
         * @static
         * @param {game.ISModifyName} message SModifyName message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SModifyName.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SModifyName message, length delimited. Does not implicitly {@link game.SModifyName.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SModifyName
         * @static
         * @param {game.ISModifyName} message SModifyName message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SModifyName.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SModifyName message from the specified reader or buffer.
         * @function decode
         * @memberof game.SModifyName
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SModifyName} SModifyName
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SModifyName.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SModifyName();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SModifyName message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SModifyName
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SModifyName} SModifyName
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SModifyName.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SModifyName message.
         * @function verify
         * @memberof game.SModifyName
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SModifyName.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SModifyName message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SModifyName
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SModifyName} SModifyName
         */
        SModifyName.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SModifyName)
                return object;
            return new $root.game.SModifyName();
        };

        /**
         * Creates a plain object from a SModifyName message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SModifyName
         * @static
         * @param {game.SModifyName} message SModifyName
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SModifyName.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SModifyName to JSON.
         * @function toJSON
         * @memberof game.SModifyName
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SModifyName.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SModifyName
         * @function getTypeUrl
         * @memberof game.SModifyName
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SModifyName.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SModifyName";
        };

        return SModifyName;
    })();

    game.CModifyAvatar = (function() {

        /**
         * Properties of a CModifyAvatar.
         * @memberof game
         * @interface ICModifyAvatar
         * @property {number|null} [avatar] CModifyAvatar avatar
         */

        /**
         * Constructs a new CModifyAvatar.
         * @memberof game
         * @classdesc Represents a CModifyAvatar.
         * @implements ICModifyAvatar
         * @constructor
         * @param {game.ICModifyAvatar=} [properties] Properties to set
         */
        function CModifyAvatar(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CModifyAvatar avatar.
         * @member {number} avatar
         * @memberof game.CModifyAvatar
         * @instance
         */
        CModifyAvatar.prototype.avatar = 0;

        /**
         * Creates a new CModifyAvatar instance using the specified properties.
         * @function create
         * @memberof game.CModifyAvatar
         * @static
         * @param {game.ICModifyAvatar=} [properties] Properties to set
         * @returns {game.CModifyAvatar} CModifyAvatar instance
         */
        CModifyAvatar.create = function create(properties) {
            return new CModifyAvatar(properties);
        };

        /**
         * Encodes the specified CModifyAvatar message. Does not implicitly {@link game.CModifyAvatar.verify|verify} messages.
         * @function encode
         * @memberof game.CModifyAvatar
         * @static
         * @param {game.ICModifyAvatar} message CModifyAvatar message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CModifyAvatar.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.avatar != null && Object.hasOwnProperty.call(message, "avatar"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.avatar);
            return writer;
        };

        /**
         * Encodes the specified CModifyAvatar message, length delimited. Does not implicitly {@link game.CModifyAvatar.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CModifyAvatar
         * @static
         * @param {game.ICModifyAvatar} message CModifyAvatar message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CModifyAvatar.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CModifyAvatar message from the specified reader or buffer.
         * @function decode
         * @memberof game.CModifyAvatar
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CModifyAvatar} CModifyAvatar
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CModifyAvatar.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CModifyAvatar();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.avatar = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CModifyAvatar message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CModifyAvatar
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CModifyAvatar} CModifyAvatar
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CModifyAvatar.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CModifyAvatar message.
         * @function verify
         * @memberof game.CModifyAvatar
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CModifyAvatar.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                if (!$util.isInteger(message.avatar))
                    return "avatar: integer expected";
            return null;
        };

        /**
         * Creates a CModifyAvatar message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CModifyAvatar
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CModifyAvatar} CModifyAvatar
         */
        CModifyAvatar.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CModifyAvatar)
                return object;
            var message = new $root.game.CModifyAvatar();
            if (object.avatar != null)
                message.avatar = object.avatar | 0;
            return message;
        };

        /**
         * Creates a plain object from a CModifyAvatar message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CModifyAvatar
         * @static
         * @param {game.CModifyAvatar} message CModifyAvatar
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CModifyAvatar.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.avatar = 0;
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                object.avatar = message.avatar;
            return object;
        };

        /**
         * Converts this CModifyAvatar to JSON.
         * @function toJSON
         * @memberof game.CModifyAvatar
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CModifyAvatar.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CModifyAvatar
         * @function getTypeUrl
         * @memberof game.CModifyAvatar
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CModifyAvatar.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CModifyAvatar";
        };

        return CModifyAvatar;
    })();

    game.SModifyAvatar = (function() {

        /**
         * Properties of a SModifyAvatar.
         * @memberof game
         * @interface ISModifyAvatar
         */

        /**
         * Constructs a new SModifyAvatar.
         * @memberof game
         * @classdesc Represents a SModifyAvatar.
         * @implements ISModifyAvatar
         * @constructor
         * @param {game.ISModifyAvatar=} [properties] Properties to set
         */
        function SModifyAvatar(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SModifyAvatar instance using the specified properties.
         * @function create
         * @memberof game.SModifyAvatar
         * @static
         * @param {game.ISModifyAvatar=} [properties] Properties to set
         * @returns {game.SModifyAvatar} SModifyAvatar instance
         */
        SModifyAvatar.create = function create(properties) {
            return new SModifyAvatar(properties);
        };

        /**
         * Encodes the specified SModifyAvatar message. Does not implicitly {@link game.SModifyAvatar.verify|verify} messages.
         * @function encode
         * @memberof game.SModifyAvatar
         * @static
         * @param {game.ISModifyAvatar} message SModifyAvatar message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SModifyAvatar.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SModifyAvatar message, length delimited. Does not implicitly {@link game.SModifyAvatar.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SModifyAvatar
         * @static
         * @param {game.ISModifyAvatar} message SModifyAvatar message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SModifyAvatar.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SModifyAvatar message from the specified reader or buffer.
         * @function decode
         * @memberof game.SModifyAvatar
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SModifyAvatar} SModifyAvatar
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SModifyAvatar.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SModifyAvatar();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SModifyAvatar message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SModifyAvatar
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SModifyAvatar} SModifyAvatar
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SModifyAvatar.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SModifyAvatar message.
         * @function verify
         * @memberof game.SModifyAvatar
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SModifyAvatar.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SModifyAvatar message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SModifyAvatar
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SModifyAvatar} SModifyAvatar
         */
        SModifyAvatar.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SModifyAvatar)
                return object;
            return new $root.game.SModifyAvatar();
        };

        /**
         * Creates a plain object from a SModifyAvatar message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SModifyAvatar
         * @static
         * @param {game.SModifyAvatar} message SModifyAvatar
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SModifyAvatar.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SModifyAvatar to JSON.
         * @function toJSON
         * @memberof game.SModifyAvatar
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SModifyAvatar.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SModifyAvatar
         * @function getTypeUrl
         * @memberof game.SModifyAvatar
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SModifyAvatar.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SModifyAvatar";
        };

        return SModifyAvatar;
    })();

    game.CBindAccount = (function() {

        /**
         * Properties of a CBindAccount.
         * @memberof game
         * @interface ICBindAccount
         * @property {number|null} [bindType] CBindAccount bindType
         * @property {string|null} [bindAccount] CBindAccount bindAccount
         * @property {string|null} [bindOpenid] CBindAccount bindOpenid
         */

        /**
         * Constructs a new CBindAccount.
         * @memberof game
         * @classdesc Represents a CBindAccount.
         * @implements ICBindAccount
         * @constructor
         * @param {game.ICBindAccount=} [properties] Properties to set
         */
        function CBindAccount(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CBindAccount bindType.
         * @member {number} bindType
         * @memberof game.CBindAccount
         * @instance
         */
        CBindAccount.prototype.bindType = 0;

        /**
         * CBindAccount bindAccount.
         * @member {string} bindAccount
         * @memberof game.CBindAccount
         * @instance
         */
        CBindAccount.prototype.bindAccount = "";

        /**
         * CBindAccount bindOpenid.
         * @member {string} bindOpenid
         * @memberof game.CBindAccount
         * @instance
         */
        CBindAccount.prototype.bindOpenid = "";

        /**
         * Creates a new CBindAccount instance using the specified properties.
         * @function create
         * @memberof game.CBindAccount
         * @static
         * @param {game.ICBindAccount=} [properties] Properties to set
         * @returns {game.CBindAccount} CBindAccount instance
         */
        CBindAccount.create = function create(properties) {
            return new CBindAccount(properties);
        };

        /**
         * Encodes the specified CBindAccount message. Does not implicitly {@link game.CBindAccount.verify|verify} messages.
         * @function encode
         * @memberof game.CBindAccount
         * @static
         * @param {game.ICBindAccount} message CBindAccount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CBindAccount.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.bindType != null && Object.hasOwnProperty.call(message, "bindType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.bindType);
            if (message.bindAccount != null && Object.hasOwnProperty.call(message, "bindAccount"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.bindAccount);
            if (message.bindOpenid != null && Object.hasOwnProperty.call(message, "bindOpenid"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.bindOpenid);
            return writer;
        };

        /**
         * Encodes the specified CBindAccount message, length delimited. Does not implicitly {@link game.CBindAccount.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CBindAccount
         * @static
         * @param {game.ICBindAccount} message CBindAccount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CBindAccount.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CBindAccount message from the specified reader or buffer.
         * @function decode
         * @memberof game.CBindAccount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CBindAccount} CBindAccount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CBindAccount.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CBindAccount();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.bindType = reader.int32();
                        break;
                    }
                case 2: {
                        message.bindAccount = reader.string();
                        break;
                    }
                case 3: {
                        message.bindOpenid = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CBindAccount message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CBindAccount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CBindAccount} CBindAccount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CBindAccount.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CBindAccount message.
         * @function verify
         * @memberof game.CBindAccount
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CBindAccount.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.bindType != null && message.hasOwnProperty("bindType"))
                if (!$util.isInteger(message.bindType))
                    return "bindType: integer expected";
            if (message.bindAccount != null && message.hasOwnProperty("bindAccount"))
                if (!$util.isString(message.bindAccount))
                    return "bindAccount: string expected";
            if (message.bindOpenid != null && message.hasOwnProperty("bindOpenid"))
                if (!$util.isString(message.bindOpenid))
                    return "bindOpenid: string expected";
            return null;
        };

        /**
         * Creates a CBindAccount message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CBindAccount
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CBindAccount} CBindAccount
         */
        CBindAccount.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CBindAccount)
                return object;
            var message = new $root.game.CBindAccount();
            if (object.bindType != null)
                message.bindType = object.bindType | 0;
            if (object.bindAccount != null)
                message.bindAccount = String(object.bindAccount);
            if (object.bindOpenid != null)
                message.bindOpenid = String(object.bindOpenid);
            return message;
        };

        /**
         * Creates a plain object from a CBindAccount message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CBindAccount
         * @static
         * @param {game.CBindAccount} message CBindAccount
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CBindAccount.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.bindType = 0;
                object.bindAccount = "";
                object.bindOpenid = "";
            }
            if (message.bindType != null && message.hasOwnProperty("bindType"))
                object.bindType = message.bindType;
            if (message.bindAccount != null && message.hasOwnProperty("bindAccount"))
                object.bindAccount = message.bindAccount;
            if (message.bindOpenid != null && message.hasOwnProperty("bindOpenid"))
                object.bindOpenid = message.bindOpenid;
            return object;
        };

        /**
         * Converts this CBindAccount to JSON.
         * @function toJSON
         * @memberof game.CBindAccount
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CBindAccount.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CBindAccount
         * @function getTypeUrl
         * @memberof game.CBindAccount
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CBindAccount.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CBindAccount";
        };

        return CBindAccount;
    })();

    game.SBindAccount = (function() {

        /**
         * Properties of a SBindAccount.
         * @memberof game
         * @interface ISBindAccount
         */

        /**
         * Constructs a new SBindAccount.
         * @memberof game
         * @classdesc Represents a SBindAccount.
         * @implements ISBindAccount
         * @constructor
         * @param {game.ISBindAccount=} [properties] Properties to set
         */
        function SBindAccount(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SBindAccount instance using the specified properties.
         * @function create
         * @memberof game.SBindAccount
         * @static
         * @param {game.ISBindAccount=} [properties] Properties to set
         * @returns {game.SBindAccount} SBindAccount instance
         */
        SBindAccount.create = function create(properties) {
            return new SBindAccount(properties);
        };

        /**
         * Encodes the specified SBindAccount message. Does not implicitly {@link game.SBindAccount.verify|verify} messages.
         * @function encode
         * @memberof game.SBindAccount
         * @static
         * @param {game.ISBindAccount} message SBindAccount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SBindAccount.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SBindAccount message, length delimited. Does not implicitly {@link game.SBindAccount.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SBindAccount
         * @static
         * @param {game.ISBindAccount} message SBindAccount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SBindAccount.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SBindAccount message from the specified reader or buffer.
         * @function decode
         * @memberof game.SBindAccount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SBindAccount} SBindAccount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SBindAccount.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SBindAccount();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SBindAccount message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SBindAccount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SBindAccount} SBindAccount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SBindAccount.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SBindAccount message.
         * @function verify
         * @memberof game.SBindAccount
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SBindAccount.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SBindAccount message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SBindAccount
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SBindAccount} SBindAccount
         */
        SBindAccount.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SBindAccount)
                return object;
            return new $root.game.SBindAccount();
        };

        /**
         * Creates a plain object from a SBindAccount message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SBindAccount
         * @static
         * @param {game.SBindAccount} message SBindAccount
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SBindAccount.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SBindAccount to JSON.
         * @function toJSON
         * @memberof game.SBindAccount
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SBindAccount.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SBindAccount
         * @function getTypeUrl
         * @memberof game.SBindAccount
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SBindAccount.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SBindAccount";
        };

        return SBindAccount;
    })();

    game.CSetFightHero = (function() {

        /**
         * Properties of a CSetFightHero.
         * @memberof game
         * @interface ICSetFightHero
         * @property {number|null} [teamId] CSetFightHero teamId
         * @property {number|null} [slotId] CSetFightHero slotId
         * @property {number|null} [cardId] CSetFightHero cardId
         */

        /**
         * Constructs a new CSetFightHero.
         * @memberof game
         * @classdesc Represents a CSetFightHero.
         * @implements ICSetFightHero
         * @constructor
         * @param {game.ICSetFightHero=} [properties] Properties to set
         */
        function CSetFightHero(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSetFightHero teamId.
         * @member {number} teamId
         * @memberof game.CSetFightHero
         * @instance
         */
        CSetFightHero.prototype.teamId = 0;

        /**
         * CSetFightHero slotId.
         * @member {number} slotId
         * @memberof game.CSetFightHero
         * @instance
         */
        CSetFightHero.prototype.slotId = 0;

        /**
         * CSetFightHero cardId.
         * @member {number} cardId
         * @memberof game.CSetFightHero
         * @instance
         */
        CSetFightHero.prototype.cardId = 0;

        /**
         * Creates a new CSetFightHero instance using the specified properties.
         * @function create
         * @memberof game.CSetFightHero
         * @static
         * @param {game.ICSetFightHero=} [properties] Properties to set
         * @returns {game.CSetFightHero} CSetFightHero instance
         */
        CSetFightHero.create = function create(properties) {
            return new CSetFightHero(properties);
        };

        /**
         * Encodes the specified CSetFightHero message. Does not implicitly {@link game.CSetFightHero.verify|verify} messages.
         * @function encode
         * @memberof game.CSetFightHero
         * @static
         * @param {game.ICSetFightHero} message CSetFightHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSetFightHero.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.teamId != null && Object.hasOwnProperty.call(message, "teamId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.teamId);
            if (message.slotId != null && Object.hasOwnProperty.call(message, "slotId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.slotId);
            if (message.cardId != null && Object.hasOwnProperty.call(message, "cardId"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.cardId);
            return writer;
        };

        /**
         * Encodes the specified CSetFightHero message, length delimited. Does not implicitly {@link game.CSetFightHero.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CSetFightHero
         * @static
         * @param {game.ICSetFightHero} message CSetFightHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSetFightHero.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSetFightHero message from the specified reader or buffer.
         * @function decode
         * @memberof game.CSetFightHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CSetFightHero} CSetFightHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSetFightHero.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CSetFightHero();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.teamId = reader.int32();
                        break;
                    }
                case 2: {
                        message.slotId = reader.int32();
                        break;
                    }
                case 3: {
                        message.cardId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSetFightHero message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CSetFightHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CSetFightHero} CSetFightHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSetFightHero.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSetFightHero message.
         * @function verify
         * @memberof game.CSetFightHero
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSetFightHero.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.teamId != null && message.hasOwnProperty("teamId"))
                if (!$util.isInteger(message.teamId))
                    return "teamId: integer expected";
            if (message.slotId != null && message.hasOwnProperty("slotId"))
                if (!$util.isInteger(message.slotId))
                    return "slotId: integer expected";
            if (message.cardId != null && message.hasOwnProperty("cardId"))
                if (!$util.isInteger(message.cardId))
                    return "cardId: integer expected";
            return null;
        };

        /**
         * Creates a CSetFightHero message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CSetFightHero
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CSetFightHero} CSetFightHero
         */
        CSetFightHero.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CSetFightHero)
                return object;
            var message = new $root.game.CSetFightHero();
            if (object.teamId != null)
                message.teamId = object.teamId | 0;
            if (object.slotId != null)
                message.slotId = object.slotId | 0;
            if (object.cardId != null)
                message.cardId = object.cardId | 0;
            return message;
        };

        /**
         * Creates a plain object from a CSetFightHero message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CSetFightHero
         * @static
         * @param {game.CSetFightHero} message CSetFightHero
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSetFightHero.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.teamId = 0;
                object.slotId = 0;
                object.cardId = 0;
            }
            if (message.teamId != null && message.hasOwnProperty("teamId"))
                object.teamId = message.teamId;
            if (message.slotId != null && message.hasOwnProperty("slotId"))
                object.slotId = message.slotId;
            if (message.cardId != null && message.hasOwnProperty("cardId"))
                object.cardId = message.cardId;
            return object;
        };

        /**
         * Converts this CSetFightHero to JSON.
         * @function toJSON
         * @memberof game.CSetFightHero
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSetFightHero.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSetFightHero
         * @function getTypeUrl
         * @memberof game.CSetFightHero
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSetFightHero.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CSetFightHero";
        };

        return CSetFightHero;
    })();

    game.SSetFightHero = (function() {

        /**
         * Properties of a SSetFightHero.
         * @memberof game
         * @interface ISSetFightHero
         * @property {number|null} [old] SSetFightHero old
         */

        /**
         * Constructs a new SSetFightHero.
         * @memberof game
         * @classdesc Represents a SSetFightHero.
         * @implements ISSetFightHero
         * @constructor
         * @param {game.ISSetFightHero=} [properties] Properties to set
         */
        function SSetFightHero(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SSetFightHero old.
         * @member {number} old
         * @memberof game.SSetFightHero
         * @instance
         */
        SSetFightHero.prototype.old = 0;

        /**
         * Creates a new SSetFightHero instance using the specified properties.
         * @function create
         * @memberof game.SSetFightHero
         * @static
         * @param {game.ISSetFightHero=} [properties] Properties to set
         * @returns {game.SSetFightHero} SSetFightHero instance
         */
        SSetFightHero.create = function create(properties) {
            return new SSetFightHero(properties);
        };

        /**
         * Encodes the specified SSetFightHero message. Does not implicitly {@link game.SSetFightHero.verify|verify} messages.
         * @function encode
         * @memberof game.SSetFightHero
         * @static
         * @param {game.ISSetFightHero} message SSetFightHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSetFightHero.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.old != null && Object.hasOwnProperty.call(message, "old"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.old);
            return writer;
        };

        /**
         * Encodes the specified SSetFightHero message, length delimited. Does not implicitly {@link game.SSetFightHero.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SSetFightHero
         * @static
         * @param {game.ISSetFightHero} message SSetFightHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSetFightHero.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SSetFightHero message from the specified reader or buffer.
         * @function decode
         * @memberof game.SSetFightHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SSetFightHero} SSetFightHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSetFightHero.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SSetFightHero();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.old = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SSetFightHero message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SSetFightHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SSetFightHero} SSetFightHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSetFightHero.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SSetFightHero message.
         * @function verify
         * @memberof game.SSetFightHero
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SSetFightHero.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.old != null && message.hasOwnProperty("old"))
                if (!$util.isInteger(message.old))
                    return "old: integer expected";
            return null;
        };

        /**
         * Creates a SSetFightHero message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SSetFightHero
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SSetFightHero} SSetFightHero
         */
        SSetFightHero.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SSetFightHero)
                return object;
            var message = new $root.game.SSetFightHero();
            if (object.old != null)
                message.old = object.old | 0;
            return message;
        };

        /**
         * Creates a plain object from a SSetFightHero message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SSetFightHero
         * @static
         * @param {game.SSetFightHero} message SSetFightHero
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SSetFightHero.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.old = 0;
            if (message.old != null && message.hasOwnProperty("old"))
                object.old = message.old;
            return object;
        };

        /**
         * Converts this SSetFightHero to JSON.
         * @function toJSON
         * @memberof game.SSetFightHero
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SSetFightHero.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SSetFightHero
         * @function getTypeUrl
         * @memberof game.SSetFightHero
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SSetFightHero.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SSetFightHero";
        };

        return SSetFightHero;
    })();

    game.CUnsetFightHero = (function() {

        /**
         * Properties of a CUnsetFightHero.
         * @memberof game
         * @interface ICUnsetFightHero
         * @property {number|null} [teamId] CUnsetFightHero teamId
         * @property {number|null} [slotId] CUnsetFightHero slotId
         */

        /**
         * Constructs a new CUnsetFightHero.
         * @memberof game
         * @classdesc Represents a CUnsetFightHero.
         * @implements ICUnsetFightHero
         * @constructor
         * @param {game.ICUnsetFightHero=} [properties] Properties to set
         */
        function CUnsetFightHero(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CUnsetFightHero teamId.
         * @member {number} teamId
         * @memberof game.CUnsetFightHero
         * @instance
         */
        CUnsetFightHero.prototype.teamId = 0;

        /**
         * CUnsetFightHero slotId.
         * @member {number} slotId
         * @memberof game.CUnsetFightHero
         * @instance
         */
        CUnsetFightHero.prototype.slotId = 0;

        /**
         * Creates a new CUnsetFightHero instance using the specified properties.
         * @function create
         * @memberof game.CUnsetFightHero
         * @static
         * @param {game.ICUnsetFightHero=} [properties] Properties to set
         * @returns {game.CUnsetFightHero} CUnsetFightHero instance
         */
        CUnsetFightHero.create = function create(properties) {
            return new CUnsetFightHero(properties);
        };

        /**
         * Encodes the specified CUnsetFightHero message. Does not implicitly {@link game.CUnsetFightHero.verify|verify} messages.
         * @function encode
         * @memberof game.CUnsetFightHero
         * @static
         * @param {game.ICUnsetFightHero} message CUnsetFightHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CUnsetFightHero.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.teamId != null && Object.hasOwnProperty.call(message, "teamId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.teamId);
            if (message.slotId != null && Object.hasOwnProperty.call(message, "slotId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.slotId);
            return writer;
        };

        /**
         * Encodes the specified CUnsetFightHero message, length delimited. Does not implicitly {@link game.CUnsetFightHero.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CUnsetFightHero
         * @static
         * @param {game.ICUnsetFightHero} message CUnsetFightHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CUnsetFightHero.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CUnsetFightHero message from the specified reader or buffer.
         * @function decode
         * @memberof game.CUnsetFightHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CUnsetFightHero} CUnsetFightHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CUnsetFightHero.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CUnsetFightHero();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.teamId = reader.int32();
                        break;
                    }
                case 2: {
                        message.slotId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CUnsetFightHero message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CUnsetFightHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CUnsetFightHero} CUnsetFightHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CUnsetFightHero.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CUnsetFightHero message.
         * @function verify
         * @memberof game.CUnsetFightHero
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CUnsetFightHero.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.teamId != null && message.hasOwnProperty("teamId"))
                if (!$util.isInteger(message.teamId))
                    return "teamId: integer expected";
            if (message.slotId != null && message.hasOwnProperty("slotId"))
                if (!$util.isInteger(message.slotId))
                    return "slotId: integer expected";
            return null;
        };

        /**
         * Creates a CUnsetFightHero message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CUnsetFightHero
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CUnsetFightHero} CUnsetFightHero
         */
        CUnsetFightHero.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CUnsetFightHero)
                return object;
            var message = new $root.game.CUnsetFightHero();
            if (object.teamId != null)
                message.teamId = object.teamId | 0;
            if (object.slotId != null)
                message.slotId = object.slotId | 0;
            return message;
        };

        /**
         * Creates a plain object from a CUnsetFightHero message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CUnsetFightHero
         * @static
         * @param {game.CUnsetFightHero} message CUnsetFightHero
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CUnsetFightHero.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.teamId = 0;
                object.slotId = 0;
            }
            if (message.teamId != null && message.hasOwnProperty("teamId"))
                object.teamId = message.teamId;
            if (message.slotId != null && message.hasOwnProperty("slotId"))
                object.slotId = message.slotId;
            return object;
        };

        /**
         * Converts this CUnsetFightHero to JSON.
         * @function toJSON
         * @memberof game.CUnsetFightHero
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CUnsetFightHero.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CUnsetFightHero
         * @function getTypeUrl
         * @memberof game.CUnsetFightHero
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CUnsetFightHero.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CUnsetFightHero";
        };

        return CUnsetFightHero;
    })();

    game.SUnsetFightHero = (function() {

        /**
         * Properties of a SUnsetFightHero.
         * @memberof game
         * @interface ISUnsetFightHero
         * @property {number|null} [old] SUnsetFightHero old
         */

        /**
         * Constructs a new SUnsetFightHero.
         * @memberof game
         * @classdesc Represents a SUnsetFightHero.
         * @implements ISUnsetFightHero
         * @constructor
         * @param {game.ISUnsetFightHero=} [properties] Properties to set
         */
        function SUnsetFightHero(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SUnsetFightHero old.
         * @member {number} old
         * @memberof game.SUnsetFightHero
         * @instance
         */
        SUnsetFightHero.prototype.old = 0;

        /**
         * Creates a new SUnsetFightHero instance using the specified properties.
         * @function create
         * @memberof game.SUnsetFightHero
         * @static
         * @param {game.ISUnsetFightHero=} [properties] Properties to set
         * @returns {game.SUnsetFightHero} SUnsetFightHero instance
         */
        SUnsetFightHero.create = function create(properties) {
            return new SUnsetFightHero(properties);
        };

        /**
         * Encodes the specified SUnsetFightHero message. Does not implicitly {@link game.SUnsetFightHero.verify|verify} messages.
         * @function encode
         * @memberof game.SUnsetFightHero
         * @static
         * @param {game.ISUnsetFightHero} message SUnsetFightHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SUnsetFightHero.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.old != null && Object.hasOwnProperty.call(message, "old"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.old);
            return writer;
        };

        /**
         * Encodes the specified SUnsetFightHero message, length delimited. Does not implicitly {@link game.SUnsetFightHero.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SUnsetFightHero
         * @static
         * @param {game.ISUnsetFightHero} message SUnsetFightHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SUnsetFightHero.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SUnsetFightHero message from the specified reader or buffer.
         * @function decode
         * @memberof game.SUnsetFightHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SUnsetFightHero} SUnsetFightHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SUnsetFightHero.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SUnsetFightHero();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.old = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SUnsetFightHero message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SUnsetFightHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SUnsetFightHero} SUnsetFightHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SUnsetFightHero.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SUnsetFightHero message.
         * @function verify
         * @memberof game.SUnsetFightHero
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SUnsetFightHero.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.old != null && message.hasOwnProperty("old"))
                if (!$util.isInteger(message.old))
                    return "old: integer expected";
            return null;
        };

        /**
         * Creates a SUnsetFightHero message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SUnsetFightHero
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SUnsetFightHero} SUnsetFightHero
         */
        SUnsetFightHero.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SUnsetFightHero)
                return object;
            var message = new $root.game.SUnsetFightHero();
            if (object.old != null)
                message.old = object.old | 0;
            return message;
        };

        /**
         * Creates a plain object from a SUnsetFightHero message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SUnsetFightHero
         * @static
         * @param {game.SUnsetFightHero} message SUnsetFightHero
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SUnsetFightHero.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.old = 0;
            if (message.old != null && message.hasOwnProperty("old"))
                object.old = message.old;
            return object;
        };

        /**
         * Converts this SUnsetFightHero to JSON.
         * @function toJSON
         * @memberof game.SUnsetFightHero
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SUnsetFightHero.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SUnsetFightHero
         * @function getTypeUrl
         * @memberof game.SUnsetFightHero
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SUnsetFightHero.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SUnsetFightHero";
        };

        return SUnsetFightHero;
    })();

    game.CUpgradeHero = (function() {

        /**
         * Properties of a CUpgradeHero.
         * @memberof game
         * @interface ICUpgradeHero
         * @property {number|null} [cardId] CUpgradeHero cardId
         */

        /**
         * Constructs a new CUpgradeHero.
         * @memberof game
         * @classdesc Represents a CUpgradeHero.
         * @implements ICUpgradeHero
         * @constructor
         * @param {game.ICUpgradeHero=} [properties] Properties to set
         */
        function CUpgradeHero(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CUpgradeHero cardId.
         * @member {number} cardId
         * @memberof game.CUpgradeHero
         * @instance
         */
        CUpgradeHero.prototype.cardId = 0;

        /**
         * Creates a new CUpgradeHero instance using the specified properties.
         * @function create
         * @memberof game.CUpgradeHero
         * @static
         * @param {game.ICUpgradeHero=} [properties] Properties to set
         * @returns {game.CUpgradeHero} CUpgradeHero instance
         */
        CUpgradeHero.create = function create(properties) {
            return new CUpgradeHero(properties);
        };

        /**
         * Encodes the specified CUpgradeHero message. Does not implicitly {@link game.CUpgradeHero.verify|verify} messages.
         * @function encode
         * @memberof game.CUpgradeHero
         * @static
         * @param {game.ICUpgradeHero} message CUpgradeHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CUpgradeHero.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.cardId != null && Object.hasOwnProperty.call(message, "cardId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.cardId);
            return writer;
        };

        /**
         * Encodes the specified CUpgradeHero message, length delimited. Does not implicitly {@link game.CUpgradeHero.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CUpgradeHero
         * @static
         * @param {game.ICUpgradeHero} message CUpgradeHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CUpgradeHero.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CUpgradeHero message from the specified reader or buffer.
         * @function decode
         * @memberof game.CUpgradeHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CUpgradeHero} CUpgradeHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CUpgradeHero.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CUpgradeHero();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.cardId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CUpgradeHero message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CUpgradeHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CUpgradeHero} CUpgradeHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CUpgradeHero.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CUpgradeHero message.
         * @function verify
         * @memberof game.CUpgradeHero
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CUpgradeHero.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.cardId != null && message.hasOwnProperty("cardId"))
                if (!$util.isInteger(message.cardId))
                    return "cardId: integer expected";
            return null;
        };

        /**
         * Creates a CUpgradeHero message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CUpgradeHero
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CUpgradeHero} CUpgradeHero
         */
        CUpgradeHero.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CUpgradeHero)
                return object;
            var message = new $root.game.CUpgradeHero();
            if (object.cardId != null)
                message.cardId = object.cardId | 0;
            return message;
        };

        /**
         * Creates a plain object from a CUpgradeHero message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CUpgradeHero
         * @static
         * @param {game.CUpgradeHero} message CUpgradeHero
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CUpgradeHero.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.cardId = 0;
            if (message.cardId != null && message.hasOwnProperty("cardId"))
                object.cardId = message.cardId;
            return object;
        };

        /**
         * Converts this CUpgradeHero to JSON.
         * @function toJSON
         * @memberof game.CUpgradeHero
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CUpgradeHero.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CUpgradeHero
         * @function getTypeUrl
         * @memberof game.CUpgradeHero
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CUpgradeHero.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CUpgradeHero";
        };

        return CUpgradeHero;
    })();

    game.SUpgradeHero = (function() {

        /**
         * Properties of a SUpgradeHero.
         * @memberof game
         * @interface ISUpgradeHero
         * @property {number|null} [newLevel] SUpgradeHero newLevel
         * @property {Array.<common.IReward>|null} [costs] SUpgradeHero costs
         * @property {Array.<common.IReward>|null} [awards] SUpgradeHero awards
         */

        /**
         * Constructs a new SUpgradeHero.
         * @memberof game
         * @classdesc Represents a SUpgradeHero.
         * @implements ISUpgradeHero
         * @constructor
         * @param {game.ISUpgradeHero=} [properties] Properties to set
         */
        function SUpgradeHero(properties) {
            this.costs = [];
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SUpgradeHero newLevel.
         * @member {number} newLevel
         * @memberof game.SUpgradeHero
         * @instance
         */
        SUpgradeHero.prototype.newLevel = 0;

        /**
         * SUpgradeHero costs.
         * @member {Array.<common.IReward>} costs
         * @memberof game.SUpgradeHero
         * @instance
         */
        SUpgradeHero.prototype.costs = $util.emptyArray;

        /**
         * SUpgradeHero awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SUpgradeHero
         * @instance
         */
        SUpgradeHero.prototype.awards = $util.emptyArray;

        /**
         * Creates a new SUpgradeHero instance using the specified properties.
         * @function create
         * @memberof game.SUpgradeHero
         * @static
         * @param {game.ISUpgradeHero=} [properties] Properties to set
         * @returns {game.SUpgradeHero} SUpgradeHero instance
         */
        SUpgradeHero.create = function create(properties) {
            return new SUpgradeHero(properties);
        };

        /**
         * Encodes the specified SUpgradeHero message. Does not implicitly {@link game.SUpgradeHero.verify|verify} messages.
         * @function encode
         * @memberof game.SUpgradeHero
         * @static
         * @param {game.ISUpgradeHero} message SUpgradeHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SUpgradeHero.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.newLevel != null && Object.hasOwnProperty.call(message, "newLevel"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.newLevel);
            if (message.costs != null && message.costs.length)
                for (var i = 0; i < message.costs.length; ++i)
                    $root.common.Reward.encode(message.costs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SUpgradeHero message, length delimited. Does not implicitly {@link game.SUpgradeHero.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SUpgradeHero
         * @static
         * @param {game.ISUpgradeHero} message SUpgradeHero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SUpgradeHero.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SUpgradeHero message from the specified reader or buffer.
         * @function decode
         * @memberof game.SUpgradeHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SUpgradeHero} SUpgradeHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SUpgradeHero.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SUpgradeHero();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.newLevel = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.costs && message.costs.length))
                            message.costs = [];
                        message.costs.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SUpgradeHero message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SUpgradeHero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SUpgradeHero} SUpgradeHero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SUpgradeHero.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SUpgradeHero message.
         * @function verify
         * @memberof game.SUpgradeHero
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SUpgradeHero.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.newLevel != null && message.hasOwnProperty("newLevel"))
                if (!$util.isInteger(message.newLevel))
                    return "newLevel: integer expected";
            if (message.costs != null && message.hasOwnProperty("costs")) {
                if (!Array.isArray(message.costs))
                    return "costs: array expected";
                for (var i = 0; i < message.costs.length; ++i) {
                    var error = $root.common.Reward.verify(message.costs[i]);
                    if (error)
                        return "costs." + error;
                }
            }
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SUpgradeHero message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SUpgradeHero
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SUpgradeHero} SUpgradeHero
         */
        SUpgradeHero.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SUpgradeHero)
                return object;
            var message = new $root.game.SUpgradeHero();
            if (object.newLevel != null)
                message.newLevel = object.newLevel | 0;
            if (object.costs) {
                if (!Array.isArray(object.costs))
                    throw TypeError(".game.SUpgradeHero.costs: array expected");
                message.costs = [];
                for (var i = 0; i < object.costs.length; ++i) {
                    if (typeof object.costs[i] !== "object")
                        throw TypeError(".game.SUpgradeHero.costs: object expected");
                    message.costs[i] = $root.common.Reward.fromObject(object.costs[i]);
                }
            }
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SUpgradeHero.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SUpgradeHero.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SUpgradeHero message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SUpgradeHero
         * @static
         * @param {game.SUpgradeHero} message SUpgradeHero
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SUpgradeHero.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.costs = [];
                object.awards = [];
            }
            if (options.defaults)
                object.newLevel = 0;
            if (message.newLevel != null && message.hasOwnProperty("newLevel"))
                object.newLevel = message.newLevel;
            if (message.costs && message.costs.length) {
                object.costs = [];
                for (var j = 0; j < message.costs.length; ++j)
                    object.costs[j] = $root.common.Reward.toObject(message.costs[j], options);
            }
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            return object;
        };

        /**
         * Converts this SUpgradeHero to JSON.
         * @function toJSON
         * @memberof game.SUpgradeHero
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SUpgradeHero.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SUpgradeHero
         * @function getTypeUrl
         * @memberof game.SUpgradeHero
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SUpgradeHero.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SUpgradeHero";
        };

        return SUpgradeHero;
    })();

    game.CChangeHeroSkin = (function() {

        /**
         * Properties of a CChangeHeroSkin.
         * @memberof game
         * @interface ICChangeHeroSkin
         * @property {number|null} [heroId] CChangeHeroSkin heroId
         * @property {number|null} [skinId] CChangeHeroSkin skinId
         */

        /**
         * Constructs a new CChangeHeroSkin.
         * @memberof game
         * @classdesc Represents a CChangeHeroSkin.
         * @implements ICChangeHeroSkin
         * @constructor
         * @param {game.ICChangeHeroSkin=} [properties] Properties to set
         */
        function CChangeHeroSkin(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CChangeHeroSkin heroId.
         * @member {number} heroId
         * @memberof game.CChangeHeroSkin
         * @instance
         */
        CChangeHeroSkin.prototype.heroId = 0;

        /**
         * CChangeHeroSkin skinId.
         * @member {number} skinId
         * @memberof game.CChangeHeroSkin
         * @instance
         */
        CChangeHeroSkin.prototype.skinId = 0;

        /**
         * Creates a new CChangeHeroSkin instance using the specified properties.
         * @function create
         * @memberof game.CChangeHeroSkin
         * @static
         * @param {game.ICChangeHeroSkin=} [properties] Properties to set
         * @returns {game.CChangeHeroSkin} CChangeHeroSkin instance
         */
        CChangeHeroSkin.create = function create(properties) {
            return new CChangeHeroSkin(properties);
        };

        /**
         * Encodes the specified CChangeHeroSkin message. Does not implicitly {@link game.CChangeHeroSkin.verify|verify} messages.
         * @function encode
         * @memberof game.CChangeHeroSkin
         * @static
         * @param {game.ICChangeHeroSkin} message CChangeHeroSkin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CChangeHeroSkin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.heroId != null && Object.hasOwnProperty.call(message, "heroId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.heroId);
            if (message.skinId != null && Object.hasOwnProperty.call(message, "skinId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.skinId);
            return writer;
        };

        /**
         * Encodes the specified CChangeHeroSkin message, length delimited. Does not implicitly {@link game.CChangeHeroSkin.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CChangeHeroSkin
         * @static
         * @param {game.ICChangeHeroSkin} message CChangeHeroSkin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CChangeHeroSkin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CChangeHeroSkin message from the specified reader or buffer.
         * @function decode
         * @memberof game.CChangeHeroSkin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CChangeHeroSkin} CChangeHeroSkin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CChangeHeroSkin.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CChangeHeroSkin();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.heroId = reader.int32();
                        break;
                    }
                case 2: {
                        message.skinId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CChangeHeroSkin message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CChangeHeroSkin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CChangeHeroSkin} CChangeHeroSkin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CChangeHeroSkin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CChangeHeroSkin message.
         * @function verify
         * @memberof game.CChangeHeroSkin
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CChangeHeroSkin.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.heroId != null && message.hasOwnProperty("heroId"))
                if (!$util.isInteger(message.heroId))
                    return "heroId: integer expected";
            if (message.skinId != null && message.hasOwnProperty("skinId"))
                if (!$util.isInteger(message.skinId))
                    return "skinId: integer expected";
            return null;
        };

        /**
         * Creates a CChangeHeroSkin message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CChangeHeroSkin
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CChangeHeroSkin} CChangeHeroSkin
         */
        CChangeHeroSkin.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CChangeHeroSkin)
                return object;
            var message = new $root.game.CChangeHeroSkin();
            if (object.heroId != null)
                message.heroId = object.heroId | 0;
            if (object.skinId != null)
                message.skinId = object.skinId | 0;
            return message;
        };

        /**
         * Creates a plain object from a CChangeHeroSkin message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CChangeHeroSkin
         * @static
         * @param {game.CChangeHeroSkin} message CChangeHeroSkin
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CChangeHeroSkin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.heroId = 0;
                object.skinId = 0;
            }
            if (message.heroId != null && message.hasOwnProperty("heroId"))
                object.heroId = message.heroId;
            if (message.skinId != null && message.hasOwnProperty("skinId"))
                object.skinId = message.skinId;
            return object;
        };

        /**
         * Converts this CChangeHeroSkin to JSON.
         * @function toJSON
         * @memberof game.CChangeHeroSkin
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CChangeHeroSkin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CChangeHeroSkin
         * @function getTypeUrl
         * @memberof game.CChangeHeroSkin
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CChangeHeroSkin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CChangeHeroSkin";
        };

        return CChangeHeroSkin;
    })();

    game.SChangeHeroSkin = (function() {

        /**
         * Properties of a SChangeHeroSkin.
         * @memberof game
         * @interface ISChangeHeroSkin
         */

        /**
         * Constructs a new SChangeHeroSkin.
         * @memberof game
         * @classdesc Represents a SChangeHeroSkin.
         * @implements ISChangeHeroSkin
         * @constructor
         * @param {game.ISChangeHeroSkin=} [properties] Properties to set
         */
        function SChangeHeroSkin(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SChangeHeroSkin instance using the specified properties.
         * @function create
         * @memberof game.SChangeHeroSkin
         * @static
         * @param {game.ISChangeHeroSkin=} [properties] Properties to set
         * @returns {game.SChangeHeroSkin} SChangeHeroSkin instance
         */
        SChangeHeroSkin.create = function create(properties) {
            return new SChangeHeroSkin(properties);
        };

        /**
         * Encodes the specified SChangeHeroSkin message. Does not implicitly {@link game.SChangeHeroSkin.verify|verify} messages.
         * @function encode
         * @memberof game.SChangeHeroSkin
         * @static
         * @param {game.ISChangeHeroSkin} message SChangeHeroSkin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SChangeHeroSkin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SChangeHeroSkin message, length delimited. Does not implicitly {@link game.SChangeHeroSkin.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SChangeHeroSkin
         * @static
         * @param {game.ISChangeHeroSkin} message SChangeHeroSkin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SChangeHeroSkin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SChangeHeroSkin message from the specified reader or buffer.
         * @function decode
         * @memberof game.SChangeHeroSkin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SChangeHeroSkin} SChangeHeroSkin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SChangeHeroSkin.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SChangeHeroSkin();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SChangeHeroSkin message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SChangeHeroSkin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SChangeHeroSkin} SChangeHeroSkin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SChangeHeroSkin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SChangeHeroSkin message.
         * @function verify
         * @memberof game.SChangeHeroSkin
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SChangeHeroSkin.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SChangeHeroSkin message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SChangeHeroSkin
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SChangeHeroSkin} SChangeHeroSkin
         */
        SChangeHeroSkin.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SChangeHeroSkin)
                return object;
            return new $root.game.SChangeHeroSkin();
        };

        /**
         * Creates a plain object from a SChangeHeroSkin message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SChangeHeroSkin
         * @static
         * @param {game.SChangeHeroSkin} message SChangeHeroSkin
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SChangeHeroSkin.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SChangeHeroSkin to JSON.
         * @function toJSON
         * @memberof game.SChangeHeroSkin
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SChangeHeroSkin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SChangeHeroSkin
         * @function getTypeUrl
         * @memberof game.SChangeHeroSkin
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SChangeHeroSkin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SChangeHeroSkin";
        };

        return SChangeHeroSkin;
    })();

    game.CSwitchHeroTeam = (function() {

        /**
         * Properties of a CSwitchHeroTeam.
         * @memberof game
         * @interface ICSwitchHeroTeam
         * @property {number|null} [teamId] CSwitchHeroTeam teamId
         */

        /**
         * Constructs a new CSwitchHeroTeam.
         * @memberof game
         * @classdesc Represents a CSwitchHeroTeam.
         * @implements ICSwitchHeroTeam
         * @constructor
         * @param {game.ICSwitchHeroTeam=} [properties] Properties to set
         */
        function CSwitchHeroTeam(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSwitchHeroTeam teamId.
         * @member {number} teamId
         * @memberof game.CSwitchHeroTeam
         * @instance
         */
        CSwitchHeroTeam.prototype.teamId = 0;

        /**
         * Creates a new CSwitchHeroTeam instance using the specified properties.
         * @function create
         * @memberof game.CSwitchHeroTeam
         * @static
         * @param {game.ICSwitchHeroTeam=} [properties] Properties to set
         * @returns {game.CSwitchHeroTeam} CSwitchHeroTeam instance
         */
        CSwitchHeroTeam.create = function create(properties) {
            return new CSwitchHeroTeam(properties);
        };

        /**
         * Encodes the specified CSwitchHeroTeam message. Does not implicitly {@link game.CSwitchHeroTeam.verify|verify} messages.
         * @function encode
         * @memberof game.CSwitchHeroTeam
         * @static
         * @param {game.ICSwitchHeroTeam} message CSwitchHeroTeam message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSwitchHeroTeam.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.teamId != null && Object.hasOwnProperty.call(message, "teamId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.teamId);
            return writer;
        };

        /**
         * Encodes the specified CSwitchHeroTeam message, length delimited. Does not implicitly {@link game.CSwitchHeroTeam.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CSwitchHeroTeam
         * @static
         * @param {game.ICSwitchHeroTeam} message CSwitchHeroTeam message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSwitchHeroTeam.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSwitchHeroTeam message from the specified reader or buffer.
         * @function decode
         * @memberof game.CSwitchHeroTeam
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CSwitchHeroTeam} CSwitchHeroTeam
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSwitchHeroTeam.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CSwitchHeroTeam();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.teamId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSwitchHeroTeam message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CSwitchHeroTeam
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CSwitchHeroTeam} CSwitchHeroTeam
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSwitchHeroTeam.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSwitchHeroTeam message.
         * @function verify
         * @memberof game.CSwitchHeroTeam
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSwitchHeroTeam.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.teamId != null && message.hasOwnProperty("teamId"))
                if (!$util.isInteger(message.teamId))
                    return "teamId: integer expected";
            return null;
        };

        /**
         * Creates a CSwitchHeroTeam message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CSwitchHeroTeam
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CSwitchHeroTeam} CSwitchHeroTeam
         */
        CSwitchHeroTeam.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CSwitchHeroTeam)
                return object;
            var message = new $root.game.CSwitchHeroTeam();
            if (object.teamId != null)
                message.teamId = object.teamId | 0;
            return message;
        };

        /**
         * Creates a plain object from a CSwitchHeroTeam message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CSwitchHeroTeam
         * @static
         * @param {game.CSwitchHeroTeam} message CSwitchHeroTeam
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSwitchHeroTeam.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.teamId = 0;
            if (message.teamId != null && message.hasOwnProperty("teamId"))
                object.teamId = message.teamId;
            return object;
        };

        /**
         * Converts this CSwitchHeroTeam to JSON.
         * @function toJSON
         * @memberof game.CSwitchHeroTeam
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSwitchHeroTeam.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSwitchHeroTeam
         * @function getTypeUrl
         * @memberof game.CSwitchHeroTeam
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSwitchHeroTeam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CSwitchHeroTeam";
        };

        return CSwitchHeroTeam;
    })();

    game.SSwitchHeroTeam = (function() {

        /**
         * Properties of a SSwitchHeroTeam.
         * @memberof game
         * @interface ISSwitchHeroTeam
         * @property {number|null} [curHeroTeam] SSwitchHeroTeam curHeroTeam
         * @property {Object.<string,common.IHeroTeam>|null} [heroTeams] SSwitchHeroTeam heroTeams
         */

        /**
         * Constructs a new SSwitchHeroTeam.
         * @memberof game
         * @classdesc Represents a SSwitchHeroTeam.
         * @implements ISSwitchHeroTeam
         * @constructor
         * @param {game.ISSwitchHeroTeam=} [properties] Properties to set
         */
        function SSwitchHeroTeam(properties) {
            this.heroTeams = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SSwitchHeroTeam curHeroTeam.
         * @member {number} curHeroTeam
         * @memberof game.SSwitchHeroTeam
         * @instance
         */
        SSwitchHeroTeam.prototype.curHeroTeam = 0;

        /**
         * SSwitchHeroTeam heroTeams.
         * @member {Object.<string,common.IHeroTeam>} heroTeams
         * @memberof game.SSwitchHeroTeam
         * @instance
         */
        SSwitchHeroTeam.prototype.heroTeams = $util.emptyObject;

        /**
         * Creates a new SSwitchHeroTeam instance using the specified properties.
         * @function create
         * @memberof game.SSwitchHeroTeam
         * @static
         * @param {game.ISSwitchHeroTeam=} [properties] Properties to set
         * @returns {game.SSwitchHeroTeam} SSwitchHeroTeam instance
         */
        SSwitchHeroTeam.create = function create(properties) {
            return new SSwitchHeroTeam(properties);
        };

        /**
         * Encodes the specified SSwitchHeroTeam message. Does not implicitly {@link game.SSwitchHeroTeam.verify|verify} messages.
         * @function encode
         * @memberof game.SSwitchHeroTeam
         * @static
         * @param {game.ISSwitchHeroTeam} message SSwitchHeroTeam message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSwitchHeroTeam.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.curHeroTeam != null && Object.hasOwnProperty.call(message, "curHeroTeam"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.curHeroTeam);
            if (message.heroTeams != null && Object.hasOwnProperty.call(message, "heroTeams"))
                for (var keys = Object.keys(message.heroTeams), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.HeroTeam.encode(message.heroTeams[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            return writer;
        };

        /**
         * Encodes the specified SSwitchHeroTeam message, length delimited. Does not implicitly {@link game.SSwitchHeroTeam.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SSwitchHeroTeam
         * @static
         * @param {game.ISSwitchHeroTeam} message SSwitchHeroTeam message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSwitchHeroTeam.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SSwitchHeroTeam message from the specified reader or buffer.
         * @function decode
         * @memberof game.SSwitchHeroTeam
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SSwitchHeroTeam} SSwitchHeroTeam
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSwitchHeroTeam.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SSwitchHeroTeam(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.curHeroTeam = reader.int32();
                        break;
                    }
                case 2: {
                        if (message.heroTeams === $util.emptyObject)
                            message.heroTeams = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.HeroTeam.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.heroTeams[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SSwitchHeroTeam message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SSwitchHeroTeam
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SSwitchHeroTeam} SSwitchHeroTeam
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSwitchHeroTeam.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SSwitchHeroTeam message.
         * @function verify
         * @memberof game.SSwitchHeroTeam
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SSwitchHeroTeam.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.curHeroTeam != null && message.hasOwnProperty("curHeroTeam"))
                if (!$util.isInteger(message.curHeroTeam))
                    return "curHeroTeam: integer expected";
            if (message.heroTeams != null && message.hasOwnProperty("heroTeams")) {
                if (!$util.isObject(message.heroTeams))
                    return "heroTeams: object expected";
                var key = Object.keys(message.heroTeams);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "heroTeams: integer key{k:int32} expected";
                    {
                        var error = $root.common.HeroTeam.verify(message.heroTeams[key[i]]);
                        if (error)
                            return "heroTeams." + error;
                    }
                }
            }
            return null;
        };

        /**
         * Creates a SSwitchHeroTeam message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SSwitchHeroTeam
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SSwitchHeroTeam} SSwitchHeroTeam
         */
        SSwitchHeroTeam.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SSwitchHeroTeam)
                return object;
            var message = new $root.game.SSwitchHeroTeam();
            if (object.curHeroTeam != null)
                message.curHeroTeam = object.curHeroTeam | 0;
            if (object.heroTeams) {
                if (typeof object.heroTeams !== "object")
                    throw TypeError(".game.SSwitchHeroTeam.heroTeams: object expected");
                message.heroTeams = {};
                for (var keys = Object.keys(object.heroTeams), i = 0; i < keys.length; ++i) {
                    if (typeof object.heroTeams[keys[i]] !== "object")
                        throw TypeError(".game.SSwitchHeroTeam.heroTeams: object expected");
                    message.heroTeams[keys[i]] = $root.common.HeroTeam.fromObject(object.heroTeams[keys[i]]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SSwitchHeroTeam message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SSwitchHeroTeam
         * @static
         * @param {game.SSwitchHeroTeam} message SSwitchHeroTeam
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SSwitchHeroTeam.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.objects || options.defaults)
                object.heroTeams = {};
            if (options.defaults)
                object.curHeroTeam = 0;
            if (message.curHeroTeam != null && message.hasOwnProperty("curHeroTeam"))
                object.curHeroTeam = message.curHeroTeam;
            var keys2;
            if (message.heroTeams && (keys2 = Object.keys(message.heroTeams)).length) {
                object.heroTeams = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.heroTeams[keys2[j]] = $root.common.HeroTeam.toObject(message.heroTeams[keys2[j]], options);
            }
            return object;
        };

        /**
         * Converts this SSwitchHeroTeam to JSON.
         * @function toJSON
         * @memberof game.SSwitchHeroTeam
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SSwitchHeroTeam.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SSwitchHeroTeam
         * @function getTypeUrl
         * @memberof game.SSwitchHeroTeam
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SSwitchHeroTeam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SSwitchHeroTeam";
        };

        return SSwitchHeroTeam;
    })();

    game.CUpdateGuide = (function() {

        /**
         * Properties of a CUpdateGuide.
         * @memberof game
         * @interface ICUpdateGuide
         * @property {Object.<string,string>|null} [valMap] CUpdateGuide valMap
         */

        /**
         * Constructs a new CUpdateGuide.
         * @memberof game
         * @classdesc Represents a CUpdateGuide.
         * @implements ICUpdateGuide
         * @constructor
         * @param {game.ICUpdateGuide=} [properties] Properties to set
         */
        function CUpdateGuide(properties) {
            this.valMap = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CUpdateGuide valMap.
         * @member {Object.<string,string>} valMap
         * @memberof game.CUpdateGuide
         * @instance
         */
        CUpdateGuide.prototype.valMap = $util.emptyObject;

        /**
         * Creates a new CUpdateGuide instance using the specified properties.
         * @function create
         * @memberof game.CUpdateGuide
         * @static
         * @param {game.ICUpdateGuide=} [properties] Properties to set
         * @returns {game.CUpdateGuide} CUpdateGuide instance
         */
        CUpdateGuide.create = function create(properties) {
            return new CUpdateGuide(properties);
        };

        /**
         * Encodes the specified CUpdateGuide message. Does not implicitly {@link game.CUpdateGuide.verify|verify} messages.
         * @function encode
         * @memberof game.CUpdateGuide
         * @static
         * @param {game.ICUpdateGuide} message CUpdateGuide message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CUpdateGuide.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.valMap != null && Object.hasOwnProperty.call(message, "valMap"))
                for (var keys = Object.keys(message.valMap), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.valMap[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CUpdateGuide message, length delimited. Does not implicitly {@link game.CUpdateGuide.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CUpdateGuide
         * @static
         * @param {game.ICUpdateGuide} message CUpdateGuide message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CUpdateGuide.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CUpdateGuide message from the specified reader or buffer.
         * @function decode
         * @memberof game.CUpdateGuide
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CUpdateGuide} CUpdateGuide
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CUpdateGuide.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CUpdateGuide(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.valMap === $util.emptyObject)
                            message.valMap = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = "";
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = reader.string();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.valMap[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CUpdateGuide message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CUpdateGuide
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CUpdateGuide} CUpdateGuide
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CUpdateGuide.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CUpdateGuide message.
         * @function verify
         * @memberof game.CUpdateGuide
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CUpdateGuide.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.valMap != null && message.hasOwnProperty("valMap")) {
                if (!$util.isObject(message.valMap))
                    return "valMap: object expected";
                var key = Object.keys(message.valMap);
                for (var i = 0; i < key.length; ++i)
                    if (!$util.isString(message.valMap[key[i]]))
                        return "valMap: string{k:string} expected";
            }
            return null;
        };

        /**
         * Creates a CUpdateGuide message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CUpdateGuide
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CUpdateGuide} CUpdateGuide
         */
        CUpdateGuide.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CUpdateGuide)
                return object;
            var message = new $root.game.CUpdateGuide();
            if (object.valMap) {
                if (typeof object.valMap !== "object")
                    throw TypeError(".game.CUpdateGuide.valMap: object expected");
                message.valMap = {};
                for (var keys = Object.keys(object.valMap), i = 0; i < keys.length; ++i)
                    message.valMap[keys[i]] = String(object.valMap[keys[i]]);
            }
            return message;
        };

        /**
         * Creates a plain object from a CUpdateGuide message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CUpdateGuide
         * @static
         * @param {game.CUpdateGuide} message CUpdateGuide
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CUpdateGuide.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.objects || options.defaults)
                object.valMap = {};
            var keys2;
            if (message.valMap && (keys2 = Object.keys(message.valMap)).length) {
                object.valMap = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.valMap[keys2[j]] = message.valMap[keys2[j]];
            }
            return object;
        };

        /**
         * Converts this CUpdateGuide to JSON.
         * @function toJSON
         * @memberof game.CUpdateGuide
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CUpdateGuide.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CUpdateGuide
         * @function getTypeUrl
         * @memberof game.CUpdateGuide
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CUpdateGuide.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CUpdateGuide";
        };

        return CUpdateGuide;
    })();

    game.SUpdateGuide = (function() {

        /**
         * Properties of a SUpdateGuide.
         * @memberof game
         * @interface ISUpdateGuide
         */

        /**
         * Constructs a new SUpdateGuide.
         * @memberof game
         * @classdesc Represents a SUpdateGuide.
         * @implements ISUpdateGuide
         * @constructor
         * @param {game.ISUpdateGuide=} [properties] Properties to set
         */
        function SUpdateGuide(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SUpdateGuide instance using the specified properties.
         * @function create
         * @memberof game.SUpdateGuide
         * @static
         * @param {game.ISUpdateGuide=} [properties] Properties to set
         * @returns {game.SUpdateGuide} SUpdateGuide instance
         */
        SUpdateGuide.create = function create(properties) {
            return new SUpdateGuide(properties);
        };

        /**
         * Encodes the specified SUpdateGuide message. Does not implicitly {@link game.SUpdateGuide.verify|verify} messages.
         * @function encode
         * @memberof game.SUpdateGuide
         * @static
         * @param {game.ISUpdateGuide} message SUpdateGuide message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SUpdateGuide.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SUpdateGuide message, length delimited. Does not implicitly {@link game.SUpdateGuide.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SUpdateGuide
         * @static
         * @param {game.ISUpdateGuide} message SUpdateGuide message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SUpdateGuide.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SUpdateGuide message from the specified reader or buffer.
         * @function decode
         * @memberof game.SUpdateGuide
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SUpdateGuide} SUpdateGuide
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SUpdateGuide.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SUpdateGuide();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SUpdateGuide message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SUpdateGuide
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SUpdateGuide} SUpdateGuide
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SUpdateGuide.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SUpdateGuide message.
         * @function verify
         * @memberof game.SUpdateGuide
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SUpdateGuide.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SUpdateGuide message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SUpdateGuide
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SUpdateGuide} SUpdateGuide
         */
        SUpdateGuide.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SUpdateGuide)
                return object;
            return new $root.game.SUpdateGuide();
        };

        /**
         * Creates a plain object from a SUpdateGuide message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SUpdateGuide
         * @static
         * @param {game.SUpdateGuide} message SUpdateGuide
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SUpdateGuide.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SUpdateGuide to JSON.
         * @function toJSON
         * @memberof game.SUpdateGuide
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SUpdateGuide.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SUpdateGuide
         * @function getTypeUrl
         * @memberof game.SUpdateGuide
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SUpdateGuide.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SUpdateGuide";
        };

        return SUpdateGuide;
    })();

    game.CEquipDiyFlag = (function() {

        /**
         * Properties of a CEquipDiyFlag.
         * @memberof game
         * @interface ICEquipDiyFlag
         * @property {number|null} [id] CEquipDiyFlag id
         * @property {boolean|null} [cancel] CEquipDiyFlag cancel
         */

        /**
         * Constructs a new CEquipDiyFlag.
         * @memberof game
         * @classdesc Represents a CEquipDiyFlag.
         * @implements ICEquipDiyFlag
         * @constructor
         * @param {game.ICEquipDiyFlag=} [properties] Properties to set
         */
        function CEquipDiyFlag(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CEquipDiyFlag id.
         * @member {number} id
         * @memberof game.CEquipDiyFlag
         * @instance
         */
        CEquipDiyFlag.prototype.id = 0;

        /**
         * CEquipDiyFlag cancel.
         * @member {boolean} cancel
         * @memberof game.CEquipDiyFlag
         * @instance
         */
        CEquipDiyFlag.prototype.cancel = false;

        /**
         * Creates a new CEquipDiyFlag instance using the specified properties.
         * @function create
         * @memberof game.CEquipDiyFlag
         * @static
         * @param {game.ICEquipDiyFlag=} [properties] Properties to set
         * @returns {game.CEquipDiyFlag} CEquipDiyFlag instance
         */
        CEquipDiyFlag.create = function create(properties) {
            return new CEquipDiyFlag(properties);
        };

        /**
         * Encodes the specified CEquipDiyFlag message. Does not implicitly {@link game.CEquipDiyFlag.verify|verify} messages.
         * @function encode
         * @memberof game.CEquipDiyFlag
         * @static
         * @param {game.ICEquipDiyFlag} message CEquipDiyFlag message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CEquipDiyFlag.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.cancel != null && Object.hasOwnProperty.call(message, "cancel"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.cancel);
            return writer;
        };

        /**
         * Encodes the specified CEquipDiyFlag message, length delimited. Does not implicitly {@link game.CEquipDiyFlag.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CEquipDiyFlag
         * @static
         * @param {game.ICEquipDiyFlag} message CEquipDiyFlag message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CEquipDiyFlag.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CEquipDiyFlag message from the specified reader or buffer.
         * @function decode
         * @memberof game.CEquipDiyFlag
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CEquipDiyFlag} CEquipDiyFlag
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CEquipDiyFlag.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CEquipDiyFlag();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.cancel = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CEquipDiyFlag message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CEquipDiyFlag
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CEquipDiyFlag} CEquipDiyFlag
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CEquipDiyFlag.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CEquipDiyFlag message.
         * @function verify
         * @memberof game.CEquipDiyFlag
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CEquipDiyFlag.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.cancel != null && message.hasOwnProperty("cancel"))
                if (typeof message.cancel !== "boolean")
                    return "cancel: boolean expected";
            return null;
        };

        /**
         * Creates a CEquipDiyFlag message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CEquipDiyFlag
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CEquipDiyFlag} CEquipDiyFlag
         */
        CEquipDiyFlag.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CEquipDiyFlag)
                return object;
            var message = new $root.game.CEquipDiyFlag();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.cancel != null)
                message.cancel = Boolean(object.cancel);
            return message;
        };

        /**
         * Creates a plain object from a CEquipDiyFlag message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CEquipDiyFlag
         * @static
         * @param {game.CEquipDiyFlag} message CEquipDiyFlag
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CEquipDiyFlag.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.cancel = false;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.cancel != null && message.hasOwnProperty("cancel"))
                object.cancel = message.cancel;
            return object;
        };

        /**
         * Converts this CEquipDiyFlag to JSON.
         * @function toJSON
         * @memberof game.CEquipDiyFlag
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CEquipDiyFlag.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CEquipDiyFlag
         * @function getTypeUrl
         * @memberof game.CEquipDiyFlag
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CEquipDiyFlag.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CEquipDiyFlag";
        };

        return CEquipDiyFlag;
    })();

    game.SEquipDiyFlag = (function() {

        /**
         * Properties of a SEquipDiyFlag.
         * @memberof game
         * @interface ISEquipDiyFlag
         */

        /**
         * Constructs a new SEquipDiyFlag.
         * @memberof game
         * @classdesc Represents a SEquipDiyFlag.
         * @implements ISEquipDiyFlag
         * @constructor
         * @param {game.ISEquipDiyFlag=} [properties] Properties to set
         */
        function SEquipDiyFlag(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SEquipDiyFlag instance using the specified properties.
         * @function create
         * @memberof game.SEquipDiyFlag
         * @static
         * @param {game.ISEquipDiyFlag=} [properties] Properties to set
         * @returns {game.SEquipDiyFlag} SEquipDiyFlag instance
         */
        SEquipDiyFlag.create = function create(properties) {
            return new SEquipDiyFlag(properties);
        };

        /**
         * Encodes the specified SEquipDiyFlag message. Does not implicitly {@link game.SEquipDiyFlag.verify|verify} messages.
         * @function encode
         * @memberof game.SEquipDiyFlag
         * @static
         * @param {game.ISEquipDiyFlag} message SEquipDiyFlag message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SEquipDiyFlag.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SEquipDiyFlag message, length delimited. Does not implicitly {@link game.SEquipDiyFlag.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SEquipDiyFlag
         * @static
         * @param {game.ISEquipDiyFlag} message SEquipDiyFlag message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SEquipDiyFlag.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SEquipDiyFlag message from the specified reader or buffer.
         * @function decode
         * @memberof game.SEquipDiyFlag
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SEquipDiyFlag} SEquipDiyFlag
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SEquipDiyFlag.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SEquipDiyFlag();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SEquipDiyFlag message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SEquipDiyFlag
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SEquipDiyFlag} SEquipDiyFlag
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SEquipDiyFlag.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SEquipDiyFlag message.
         * @function verify
         * @memberof game.SEquipDiyFlag
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SEquipDiyFlag.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SEquipDiyFlag message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SEquipDiyFlag
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SEquipDiyFlag} SEquipDiyFlag
         */
        SEquipDiyFlag.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SEquipDiyFlag)
                return object;
            return new $root.game.SEquipDiyFlag();
        };

        /**
         * Creates a plain object from a SEquipDiyFlag message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SEquipDiyFlag
         * @static
         * @param {game.SEquipDiyFlag} message SEquipDiyFlag
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SEquipDiyFlag.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SEquipDiyFlag to JSON.
         * @function toJSON
         * @memberof game.SEquipDiyFlag
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SEquipDiyFlag.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SEquipDiyFlag
         * @function getTypeUrl
         * @memberof game.SEquipDiyFlag
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SEquipDiyFlag.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SEquipDiyFlag";
        };

        return SEquipDiyFlag;
    })();

    game.CSetUserData = (function() {

        /**
         * Properties of a CSetUserData.
         * @memberof game
         * @interface ICSetUserData
         * @property {string|null} [customData] CSetUserData customData
         */

        /**
         * Constructs a new CSetUserData.
         * @memberof game
         * @classdesc Represents a CSetUserData.
         * @implements ICSetUserData
         * @constructor
         * @param {game.ICSetUserData=} [properties] Properties to set
         */
        function CSetUserData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSetUserData customData.
         * @member {string} customData
         * @memberof game.CSetUserData
         * @instance
         */
        CSetUserData.prototype.customData = "";

        /**
         * Creates a new CSetUserData instance using the specified properties.
         * @function create
         * @memberof game.CSetUserData
         * @static
         * @param {game.ICSetUserData=} [properties] Properties to set
         * @returns {game.CSetUserData} CSetUserData instance
         */
        CSetUserData.create = function create(properties) {
            return new CSetUserData(properties);
        };

        /**
         * Encodes the specified CSetUserData message. Does not implicitly {@link game.CSetUserData.verify|verify} messages.
         * @function encode
         * @memberof game.CSetUserData
         * @static
         * @param {game.ICSetUserData} message CSetUserData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSetUserData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.customData != null && Object.hasOwnProperty.call(message, "customData"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.customData);
            return writer;
        };

        /**
         * Encodes the specified CSetUserData message, length delimited. Does not implicitly {@link game.CSetUserData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CSetUserData
         * @static
         * @param {game.ICSetUserData} message CSetUserData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSetUserData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSetUserData message from the specified reader or buffer.
         * @function decode
         * @memberof game.CSetUserData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CSetUserData} CSetUserData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSetUserData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CSetUserData();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.customData = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSetUserData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CSetUserData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CSetUserData} CSetUserData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSetUserData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSetUserData message.
         * @function verify
         * @memberof game.CSetUserData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSetUserData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.customData != null && message.hasOwnProperty("customData"))
                if (!$util.isString(message.customData))
                    return "customData: string expected";
            return null;
        };

        /**
         * Creates a CSetUserData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CSetUserData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CSetUserData} CSetUserData
         */
        CSetUserData.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CSetUserData)
                return object;
            var message = new $root.game.CSetUserData();
            if (object.customData != null)
                message.customData = String(object.customData);
            return message;
        };

        /**
         * Creates a plain object from a CSetUserData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CSetUserData
         * @static
         * @param {game.CSetUserData} message CSetUserData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSetUserData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.customData = "";
            if (message.customData != null && message.hasOwnProperty("customData"))
                object.customData = message.customData;
            return object;
        };

        /**
         * Converts this CSetUserData to JSON.
         * @function toJSON
         * @memberof game.CSetUserData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSetUserData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSetUserData
         * @function getTypeUrl
         * @memberof game.CSetUserData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSetUserData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CSetUserData";
        };

        return CSetUserData;
    })();

    game.SSetUserData = (function() {

        /**
         * Properties of a SSetUserData.
         * @memberof game
         * @interface ISSetUserData
         */

        /**
         * Constructs a new SSetUserData.
         * @memberof game
         * @classdesc Represents a SSetUserData.
         * @implements ISSetUserData
         * @constructor
         * @param {game.ISSetUserData=} [properties] Properties to set
         */
        function SSetUserData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SSetUserData instance using the specified properties.
         * @function create
         * @memberof game.SSetUserData
         * @static
         * @param {game.ISSetUserData=} [properties] Properties to set
         * @returns {game.SSetUserData} SSetUserData instance
         */
        SSetUserData.create = function create(properties) {
            return new SSetUserData(properties);
        };

        /**
         * Encodes the specified SSetUserData message. Does not implicitly {@link game.SSetUserData.verify|verify} messages.
         * @function encode
         * @memberof game.SSetUserData
         * @static
         * @param {game.ISSetUserData} message SSetUserData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSetUserData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SSetUserData message, length delimited. Does not implicitly {@link game.SSetUserData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SSetUserData
         * @static
         * @param {game.ISSetUserData} message SSetUserData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSetUserData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SSetUserData message from the specified reader or buffer.
         * @function decode
         * @memberof game.SSetUserData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SSetUserData} SSetUserData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSetUserData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SSetUserData();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SSetUserData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SSetUserData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SSetUserData} SSetUserData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSetUserData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SSetUserData message.
         * @function verify
         * @memberof game.SSetUserData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SSetUserData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SSetUserData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SSetUserData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SSetUserData} SSetUserData
         */
        SSetUserData.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SSetUserData)
                return object;
            return new $root.game.SSetUserData();
        };

        /**
         * Creates a plain object from a SSetUserData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SSetUserData
         * @static
         * @param {game.SSetUserData} message SSetUserData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SSetUserData.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SSetUserData to JSON.
         * @function toJSON
         * @memberof game.SSetUserData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SSetUserData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SSetUserData
         * @function getTypeUrl
         * @memberof game.SSetUserData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SSetUserData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SSetUserData";
        };

        return SSetUserData;
    })();

    game.CGetLastWinData = (function() {

        /**
         * Properties of a CGetLastWinData.
         * @memberof game
         * @interface ICGetLastWinData
         */

        /**
         * Constructs a new CGetLastWinData.
         * @memberof game
         * @classdesc Represents a CGetLastWinData.
         * @implements ICGetLastWinData
         * @constructor
         * @param {game.ICGetLastWinData=} [properties] Properties to set
         */
        function CGetLastWinData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CGetLastWinData instance using the specified properties.
         * @function create
         * @memberof game.CGetLastWinData
         * @static
         * @param {game.ICGetLastWinData=} [properties] Properties to set
         * @returns {game.CGetLastWinData} CGetLastWinData instance
         */
        CGetLastWinData.create = function create(properties) {
            return new CGetLastWinData(properties);
        };

        /**
         * Encodes the specified CGetLastWinData message. Does not implicitly {@link game.CGetLastWinData.verify|verify} messages.
         * @function encode
         * @memberof game.CGetLastWinData
         * @static
         * @param {game.ICGetLastWinData} message CGetLastWinData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetLastWinData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CGetLastWinData message, length delimited. Does not implicitly {@link game.CGetLastWinData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetLastWinData
         * @static
         * @param {game.ICGetLastWinData} message CGetLastWinData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetLastWinData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetLastWinData message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetLastWinData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetLastWinData} CGetLastWinData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetLastWinData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetLastWinData();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetLastWinData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetLastWinData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetLastWinData} CGetLastWinData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetLastWinData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetLastWinData message.
         * @function verify
         * @memberof game.CGetLastWinData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetLastWinData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CGetLastWinData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetLastWinData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetLastWinData} CGetLastWinData
         */
        CGetLastWinData.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetLastWinData)
                return object;
            return new $root.game.CGetLastWinData();
        };

        /**
         * Creates a plain object from a CGetLastWinData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetLastWinData
         * @static
         * @param {game.CGetLastWinData} message CGetLastWinData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetLastWinData.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CGetLastWinData to JSON.
         * @function toJSON
         * @memberof game.CGetLastWinData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetLastWinData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetLastWinData
         * @function getTypeUrl
         * @memberof game.CGetLastWinData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetLastWinData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetLastWinData";
        };

        return CGetLastWinData;
    })();

    game.SGetLastWinData = (function() {

        /**
         * Properties of a SGetLastWinData.
         * @memberof game
         * @interface ISGetLastWinData
         */

        /**
         * Constructs a new SGetLastWinData.
         * @memberof game
         * @classdesc Represents a SGetLastWinData.
         * @implements ISGetLastWinData
         * @constructor
         * @param {game.ISGetLastWinData=} [properties] Properties to set
         */
        function SGetLastWinData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SGetLastWinData instance using the specified properties.
         * @function create
         * @memberof game.SGetLastWinData
         * @static
         * @param {game.ISGetLastWinData=} [properties] Properties to set
         * @returns {game.SGetLastWinData} SGetLastWinData instance
         */
        SGetLastWinData.create = function create(properties) {
            return new SGetLastWinData(properties);
        };

        /**
         * Encodes the specified SGetLastWinData message. Does not implicitly {@link game.SGetLastWinData.verify|verify} messages.
         * @function encode
         * @memberof game.SGetLastWinData
         * @static
         * @param {game.ISGetLastWinData} message SGetLastWinData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetLastWinData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SGetLastWinData message, length delimited. Does not implicitly {@link game.SGetLastWinData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetLastWinData
         * @static
         * @param {game.ISGetLastWinData} message SGetLastWinData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetLastWinData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetLastWinData message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetLastWinData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetLastWinData} SGetLastWinData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetLastWinData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetLastWinData();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetLastWinData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetLastWinData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetLastWinData} SGetLastWinData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetLastWinData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetLastWinData message.
         * @function verify
         * @memberof game.SGetLastWinData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetLastWinData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SGetLastWinData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetLastWinData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetLastWinData} SGetLastWinData
         */
        SGetLastWinData.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetLastWinData)
                return object;
            return new $root.game.SGetLastWinData();
        };

        /**
         * Creates a plain object from a SGetLastWinData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetLastWinData
         * @static
         * @param {game.SGetLastWinData} message SGetLastWinData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetLastWinData.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SGetLastWinData to JSON.
         * @function toJSON
         * @memberof game.SGetLastWinData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetLastWinData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetLastWinData
         * @function getTypeUrl
         * @memberof game.SGetLastWinData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetLastWinData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetLastWinData";
        };

        return SGetLastWinData;
    })();

    game.CGetReplayList = (function() {

        /**
         * Properties of a CGetReplayList.
         * @memberof game
         * @interface ICGetReplayList
         */

        /**
         * Constructs a new CGetReplayList.
         * @memberof game
         * @classdesc Represents a CGetReplayList.
         * @implements ICGetReplayList
         * @constructor
         * @param {game.ICGetReplayList=} [properties] Properties to set
         */
        function CGetReplayList(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CGetReplayList instance using the specified properties.
         * @function create
         * @memberof game.CGetReplayList
         * @static
         * @param {game.ICGetReplayList=} [properties] Properties to set
         * @returns {game.CGetReplayList} CGetReplayList instance
         */
        CGetReplayList.create = function create(properties) {
            return new CGetReplayList(properties);
        };

        /**
         * Encodes the specified CGetReplayList message. Does not implicitly {@link game.CGetReplayList.verify|verify} messages.
         * @function encode
         * @memberof game.CGetReplayList
         * @static
         * @param {game.ICGetReplayList} message CGetReplayList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetReplayList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CGetReplayList message, length delimited. Does not implicitly {@link game.CGetReplayList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetReplayList
         * @static
         * @param {game.ICGetReplayList} message CGetReplayList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetReplayList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetReplayList message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetReplayList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetReplayList} CGetReplayList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetReplayList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetReplayList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetReplayList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetReplayList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetReplayList} CGetReplayList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetReplayList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetReplayList message.
         * @function verify
         * @memberof game.CGetReplayList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetReplayList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CGetReplayList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetReplayList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetReplayList} CGetReplayList
         */
        CGetReplayList.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetReplayList)
                return object;
            return new $root.game.CGetReplayList();
        };

        /**
         * Creates a plain object from a CGetReplayList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetReplayList
         * @static
         * @param {game.CGetReplayList} message CGetReplayList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetReplayList.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CGetReplayList to JSON.
         * @function toJSON
         * @memberof game.CGetReplayList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetReplayList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetReplayList
         * @function getTypeUrl
         * @memberof game.CGetReplayList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetReplayList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetReplayList";
        };

        return CGetReplayList;
    })();

    game.SGetReplayList = (function() {

        /**
         * Properties of a SGetReplayList.
         * @memberof game
         * @interface ISGetReplayList
         */

        /**
         * Constructs a new SGetReplayList.
         * @memberof game
         * @classdesc Represents a SGetReplayList.
         * @implements ISGetReplayList
         * @constructor
         * @param {game.ISGetReplayList=} [properties] Properties to set
         */
        function SGetReplayList(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SGetReplayList instance using the specified properties.
         * @function create
         * @memberof game.SGetReplayList
         * @static
         * @param {game.ISGetReplayList=} [properties] Properties to set
         * @returns {game.SGetReplayList} SGetReplayList instance
         */
        SGetReplayList.create = function create(properties) {
            return new SGetReplayList(properties);
        };

        /**
         * Encodes the specified SGetReplayList message. Does not implicitly {@link game.SGetReplayList.verify|verify} messages.
         * @function encode
         * @memberof game.SGetReplayList
         * @static
         * @param {game.ISGetReplayList} message SGetReplayList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetReplayList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified SGetReplayList message, length delimited. Does not implicitly {@link game.SGetReplayList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetReplayList
         * @static
         * @param {game.ISGetReplayList} message SGetReplayList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetReplayList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetReplayList message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetReplayList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetReplayList} SGetReplayList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetReplayList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetReplayList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetReplayList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetReplayList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetReplayList} SGetReplayList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetReplayList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetReplayList message.
         * @function verify
         * @memberof game.SGetReplayList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetReplayList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a SGetReplayList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetReplayList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetReplayList} SGetReplayList
         */
        SGetReplayList.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetReplayList)
                return object;
            return new $root.game.SGetReplayList();
        };

        /**
         * Creates a plain object from a SGetReplayList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetReplayList
         * @static
         * @param {game.SGetReplayList} message SGetReplayList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetReplayList.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SGetReplayList to JSON.
         * @function toJSON
         * @memberof game.SGetReplayList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetReplayList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetReplayList
         * @function getTypeUrl
         * @memberof game.SGetReplayList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetReplayList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetReplayList";
        };

        return SGetReplayList;
    })();

    game.CGetReplay = (function() {

        /**
         * Properties of a CGetReplay.
         * @memberof game
         * @interface ICGetReplay
         * @property {string|null} [key] CGetReplay key
         */

        /**
         * Constructs a new CGetReplay.
         * @memberof game
         * @classdesc Represents a CGetReplay.
         * @implements ICGetReplay
         * @constructor
         * @param {game.ICGetReplay=} [properties] Properties to set
         */
        function CGetReplay(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CGetReplay key.
         * @member {string} key
         * @memberof game.CGetReplay
         * @instance
         */
        CGetReplay.prototype.key = "";

        /**
         * Creates a new CGetReplay instance using the specified properties.
         * @function create
         * @memberof game.CGetReplay
         * @static
         * @param {game.ICGetReplay=} [properties] Properties to set
         * @returns {game.CGetReplay} CGetReplay instance
         */
        CGetReplay.create = function create(properties) {
            return new CGetReplay(properties);
        };

        /**
         * Encodes the specified CGetReplay message. Does not implicitly {@link game.CGetReplay.verify|verify} messages.
         * @function encode
         * @memberof game.CGetReplay
         * @static
         * @param {game.ICGetReplay} message CGetReplay message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetReplay.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            return writer;
        };

        /**
         * Encodes the specified CGetReplay message, length delimited. Does not implicitly {@link game.CGetReplay.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetReplay
         * @static
         * @param {game.ICGetReplay} message CGetReplay message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetReplay.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetReplay message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetReplay
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetReplay} CGetReplay
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetReplay.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetReplay();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.key = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetReplay message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetReplay
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetReplay} CGetReplay
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetReplay.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetReplay message.
         * @function verify
         * @memberof game.CGetReplay
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetReplay.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.key != null && message.hasOwnProperty("key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            return null;
        };

        /**
         * Creates a CGetReplay message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetReplay
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetReplay} CGetReplay
         */
        CGetReplay.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetReplay)
                return object;
            var message = new $root.game.CGetReplay();
            if (object.key != null)
                message.key = String(object.key);
            return message;
        };

        /**
         * Creates a plain object from a CGetReplay message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetReplay
         * @static
         * @param {game.CGetReplay} message CGetReplay
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetReplay.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.key = "";
            if (message.key != null && message.hasOwnProperty("key"))
                object.key = message.key;
            return object;
        };

        /**
         * Converts this CGetReplay to JSON.
         * @function toJSON
         * @memberof game.CGetReplay
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetReplay.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetReplay
         * @function getTypeUrl
         * @memberof game.CGetReplay
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetReplay.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetReplay";
        };

        return CGetReplay;
    })();

    game.SGetReplay = (function() {

        /**
         * Properties of a SGetReplay.
         * @memberof game
         * @interface ISGetReplay
         * @property {common.IReplayData|null} [replay] SGetReplay replay
         */

        /**
         * Constructs a new SGetReplay.
         * @memberof game
         * @classdesc Represents a SGetReplay.
         * @implements ISGetReplay
         * @constructor
         * @param {game.ISGetReplay=} [properties] Properties to set
         */
        function SGetReplay(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGetReplay replay.
         * @member {common.IReplayData|null|undefined} replay
         * @memberof game.SGetReplay
         * @instance
         */
        SGetReplay.prototype.replay = null;

        /**
         * Creates a new SGetReplay instance using the specified properties.
         * @function create
         * @memberof game.SGetReplay
         * @static
         * @param {game.ISGetReplay=} [properties] Properties to set
         * @returns {game.SGetReplay} SGetReplay instance
         */
        SGetReplay.create = function create(properties) {
            return new SGetReplay(properties);
        };

        /**
         * Encodes the specified SGetReplay message. Does not implicitly {@link game.SGetReplay.verify|verify} messages.
         * @function encode
         * @memberof game.SGetReplay
         * @static
         * @param {game.ISGetReplay} message SGetReplay message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetReplay.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.replay != null && Object.hasOwnProperty.call(message, "replay"))
                $root.common.ReplayData.encode(message.replay, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SGetReplay message, length delimited. Does not implicitly {@link game.SGetReplay.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetReplay
         * @static
         * @param {game.ISGetReplay} message SGetReplay message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetReplay.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetReplay message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetReplay
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetReplay} SGetReplay
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetReplay.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetReplay();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.replay = $root.common.ReplayData.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetReplay message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetReplay
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetReplay} SGetReplay
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetReplay.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetReplay message.
         * @function verify
         * @memberof game.SGetReplay
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetReplay.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.replay != null && message.hasOwnProperty("replay")) {
                var error = $root.common.ReplayData.verify(message.replay);
                if (error)
                    return "replay." + error;
            }
            return null;
        };

        /**
         * Creates a SGetReplay message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetReplay
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetReplay} SGetReplay
         */
        SGetReplay.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetReplay)
                return object;
            var message = new $root.game.SGetReplay();
            if (object.replay != null) {
                if (typeof object.replay !== "object")
                    throw TypeError(".game.SGetReplay.replay: object expected");
                message.replay = $root.common.ReplayData.fromObject(object.replay);
            }
            return message;
        };

        /**
         * Creates a plain object from a SGetReplay message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetReplay
         * @static
         * @param {game.SGetReplay} message SGetReplay
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetReplay.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.replay = null;
            if (message.replay != null && message.hasOwnProperty("replay"))
                object.replay = $root.common.ReplayData.toObject(message.replay, options);
            return object;
        };

        /**
         * Converts this SGetReplay to JSON.
         * @function toJSON
         * @memberof game.SGetReplay
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetReplay.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetReplay
         * @function getTypeUrl
         * @memberof game.SGetReplay
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetReplay.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetReplay";
        };

        return SGetReplay;
    })();

    game.COpenTaskBox = (function() {

        /**
         * Properties of a COpenTaskBox.
         * @memberof game
         * @interface ICOpenTaskBox
         * @property {boolean|null} [isPayOpen] COpenTaskBox isPayOpen
         */

        /**
         * Constructs a new COpenTaskBox.
         * @memberof game
         * @classdesc Represents a COpenTaskBox.
         * @implements ICOpenTaskBox
         * @constructor
         * @param {game.ICOpenTaskBox=} [properties] Properties to set
         */
        function COpenTaskBox(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * COpenTaskBox isPayOpen.
         * @member {boolean} isPayOpen
         * @memberof game.COpenTaskBox
         * @instance
         */
        COpenTaskBox.prototype.isPayOpen = false;

        /**
         * Creates a new COpenTaskBox instance using the specified properties.
         * @function create
         * @memberof game.COpenTaskBox
         * @static
         * @param {game.ICOpenTaskBox=} [properties] Properties to set
         * @returns {game.COpenTaskBox} COpenTaskBox instance
         */
        COpenTaskBox.create = function create(properties) {
            return new COpenTaskBox(properties);
        };

        /**
         * Encodes the specified COpenTaskBox message. Does not implicitly {@link game.COpenTaskBox.verify|verify} messages.
         * @function encode
         * @memberof game.COpenTaskBox
         * @static
         * @param {game.ICOpenTaskBox} message COpenTaskBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        COpenTaskBox.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.isPayOpen != null && Object.hasOwnProperty.call(message, "isPayOpen"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isPayOpen);
            return writer;
        };

        /**
         * Encodes the specified COpenTaskBox message, length delimited. Does not implicitly {@link game.COpenTaskBox.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.COpenTaskBox
         * @static
         * @param {game.ICOpenTaskBox} message COpenTaskBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        COpenTaskBox.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a COpenTaskBox message from the specified reader or buffer.
         * @function decode
         * @memberof game.COpenTaskBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.COpenTaskBox} COpenTaskBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        COpenTaskBox.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.COpenTaskBox();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.isPayOpen = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a COpenTaskBox message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.COpenTaskBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.COpenTaskBox} COpenTaskBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        COpenTaskBox.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a COpenTaskBox message.
         * @function verify
         * @memberof game.COpenTaskBox
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        COpenTaskBox.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.isPayOpen != null && message.hasOwnProperty("isPayOpen"))
                if (typeof message.isPayOpen !== "boolean")
                    return "isPayOpen: boolean expected";
            return null;
        };

        /**
         * Creates a COpenTaskBox message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.COpenTaskBox
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.COpenTaskBox} COpenTaskBox
         */
        COpenTaskBox.fromObject = function fromObject(object) {
            if (object instanceof $root.game.COpenTaskBox)
                return object;
            var message = new $root.game.COpenTaskBox();
            if (object.isPayOpen != null)
                message.isPayOpen = Boolean(object.isPayOpen);
            return message;
        };

        /**
         * Creates a plain object from a COpenTaskBox message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.COpenTaskBox
         * @static
         * @param {game.COpenTaskBox} message COpenTaskBox
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        COpenTaskBox.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.isPayOpen = false;
            if (message.isPayOpen != null && message.hasOwnProperty("isPayOpen"))
                object.isPayOpen = message.isPayOpen;
            return object;
        };

        /**
         * Converts this COpenTaskBox to JSON.
         * @function toJSON
         * @memberof game.COpenTaskBox
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        COpenTaskBox.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for COpenTaskBox
         * @function getTypeUrl
         * @memberof game.COpenTaskBox
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        COpenTaskBox.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.COpenTaskBox";
        };

        return COpenTaskBox;
    })();

    game.SOpenTaskBox = (function() {

        /**
         * Properties of a SOpenTaskBox.
         * @memberof game
         * @interface ISOpenTaskBox
         * @property {Array.<common.IReward>|null} [awards] SOpenTaskBox awards
         * @property {number|null} [newBox] SOpenTaskBox newBox
         * @property {number|null} [awardPoint] SOpenTaskBox awardPoint
         */

        /**
         * Constructs a new SOpenTaskBox.
         * @memberof game
         * @classdesc Represents a SOpenTaskBox.
         * @implements ISOpenTaskBox
         * @constructor
         * @param {game.ISOpenTaskBox=} [properties] Properties to set
         */
        function SOpenTaskBox(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SOpenTaskBox awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SOpenTaskBox
         * @instance
         */
        SOpenTaskBox.prototype.awards = $util.emptyArray;

        /**
         * SOpenTaskBox newBox.
         * @member {number} newBox
         * @memberof game.SOpenTaskBox
         * @instance
         */
        SOpenTaskBox.prototype.newBox = 0;

        /**
         * SOpenTaskBox awardPoint.
         * @member {number} awardPoint
         * @memberof game.SOpenTaskBox
         * @instance
         */
        SOpenTaskBox.prototype.awardPoint = 0;

        /**
         * Creates a new SOpenTaskBox instance using the specified properties.
         * @function create
         * @memberof game.SOpenTaskBox
         * @static
         * @param {game.ISOpenTaskBox=} [properties] Properties to set
         * @returns {game.SOpenTaskBox} SOpenTaskBox instance
         */
        SOpenTaskBox.create = function create(properties) {
            return new SOpenTaskBox(properties);
        };

        /**
         * Encodes the specified SOpenTaskBox message. Does not implicitly {@link game.SOpenTaskBox.verify|verify} messages.
         * @function encode
         * @memberof game.SOpenTaskBox
         * @static
         * @param {game.ISOpenTaskBox} message SOpenTaskBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SOpenTaskBox.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.newBox != null && Object.hasOwnProperty.call(message, "newBox"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.newBox);
            if (message.awardPoint != null && Object.hasOwnProperty.call(message, "awardPoint"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.awardPoint);
            return writer;
        };

        /**
         * Encodes the specified SOpenTaskBox message, length delimited. Does not implicitly {@link game.SOpenTaskBox.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SOpenTaskBox
         * @static
         * @param {game.ISOpenTaskBox} message SOpenTaskBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SOpenTaskBox.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SOpenTaskBox message from the specified reader or buffer.
         * @function decode
         * @memberof game.SOpenTaskBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SOpenTaskBox} SOpenTaskBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SOpenTaskBox.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SOpenTaskBox();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.newBox = reader.int32();
                        break;
                    }
                case 3: {
                        message.awardPoint = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SOpenTaskBox message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SOpenTaskBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SOpenTaskBox} SOpenTaskBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SOpenTaskBox.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SOpenTaskBox message.
         * @function verify
         * @memberof game.SOpenTaskBox
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SOpenTaskBox.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            if (message.newBox != null && message.hasOwnProperty("newBox"))
                if (!$util.isInteger(message.newBox))
                    return "newBox: integer expected";
            if (message.awardPoint != null && message.hasOwnProperty("awardPoint"))
                if (!$util.isInteger(message.awardPoint))
                    return "awardPoint: integer expected";
            return null;
        };

        /**
         * Creates a SOpenTaskBox message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SOpenTaskBox
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SOpenTaskBox} SOpenTaskBox
         */
        SOpenTaskBox.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SOpenTaskBox)
                return object;
            var message = new $root.game.SOpenTaskBox();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SOpenTaskBox.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SOpenTaskBox.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            if (object.newBox != null)
                message.newBox = object.newBox | 0;
            if (object.awardPoint != null)
                message.awardPoint = object.awardPoint | 0;
            return message;
        };

        /**
         * Creates a plain object from a SOpenTaskBox message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SOpenTaskBox
         * @static
         * @param {game.SOpenTaskBox} message SOpenTaskBox
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SOpenTaskBox.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (options.defaults) {
                object.newBox = 0;
                object.awardPoint = 0;
            }
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            if (message.newBox != null && message.hasOwnProperty("newBox"))
                object.newBox = message.newBox;
            if (message.awardPoint != null && message.hasOwnProperty("awardPoint"))
                object.awardPoint = message.awardPoint;
            return object;
        };

        /**
         * Converts this SOpenTaskBox to JSON.
         * @function toJSON
         * @memberof game.SOpenTaskBox
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SOpenTaskBox.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SOpenTaskBox
         * @function getTypeUrl
         * @memberof game.SOpenTaskBox
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SOpenTaskBox.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SOpenTaskBox";
        };

        return SOpenTaskBox;
    })();

    game.CClaimTaskFreeAward = (function() {

        /**
         * Properties of a CClaimTaskFreeAward.
         * @memberof game
         * @interface ICClaimTaskFreeAward
         * @property {boolean|null} [isDouble] CClaimTaskFreeAward isDouble
         */

        /**
         * Constructs a new CClaimTaskFreeAward.
         * @memberof game
         * @classdesc Represents a CClaimTaskFreeAward.
         * @implements ICClaimTaskFreeAward
         * @constructor
         * @param {game.ICClaimTaskFreeAward=} [properties] Properties to set
         */
        function CClaimTaskFreeAward(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CClaimTaskFreeAward isDouble.
         * @member {boolean} isDouble
         * @memberof game.CClaimTaskFreeAward
         * @instance
         */
        CClaimTaskFreeAward.prototype.isDouble = false;

        /**
         * Creates a new CClaimTaskFreeAward instance using the specified properties.
         * @function create
         * @memberof game.CClaimTaskFreeAward
         * @static
         * @param {game.ICClaimTaskFreeAward=} [properties] Properties to set
         * @returns {game.CClaimTaskFreeAward} CClaimTaskFreeAward instance
         */
        CClaimTaskFreeAward.create = function create(properties) {
            return new CClaimTaskFreeAward(properties);
        };

        /**
         * Encodes the specified CClaimTaskFreeAward message. Does not implicitly {@link game.CClaimTaskFreeAward.verify|verify} messages.
         * @function encode
         * @memberof game.CClaimTaskFreeAward
         * @static
         * @param {game.ICClaimTaskFreeAward} message CClaimTaskFreeAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CClaimTaskFreeAward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.isDouble != null && Object.hasOwnProperty.call(message, "isDouble"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isDouble);
            return writer;
        };

        /**
         * Encodes the specified CClaimTaskFreeAward message, length delimited. Does not implicitly {@link game.CClaimTaskFreeAward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CClaimTaskFreeAward
         * @static
         * @param {game.ICClaimTaskFreeAward} message CClaimTaskFreeAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CClaimTaskFreeAward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CClaimTaskFreeAward message from the specified reader or buffer.
         * @function decode
         * @memberof game.CClaimTaskFreeAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CClaimTaskFreeAward} CClaimTaskFreeAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CClaimTaskFreeAward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CClaimTaskFreeAward();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.isDouble = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CClaimTaskFreeAward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CClaimTaskFreeAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CClaimTaskFreeAward} CClaimTaskFreeAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CClaimTaskFreeAward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CClaimTaskFreeAward message.
         * @function verify
         * @memberof game.CClaimTaskFreeAward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CClaimTaskFreeAward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.isDouble != null && message.hasOwnProperty("isDouble"))
                if (typeof message.isDouble !== "boolean")
                    return "isDouble: boolean expected";
            return null;
        };

        /**
         * Creates a CClaimTaskFreeAward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CClaimTaskFreeAward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CClaimTaskFreeAward} CClaimTaskFreeAward
         */
        CClaimTaskFreeAward.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CClaimTaskFreeAward)
                return object;
            var message = new $root.game.CClaimTaskFreeAward();
            if (object.isDouble != null)
                message.isDouble = Boolean(object.isDouble);
            return message;
        };

        /**
         * Creates a plain object from a CClaimTaskFreeAward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CClaimTaskFreeAward
         * @static
         * @param {game.CClaimTaskFreeAward} message CClaimTaskFreeAward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CClaimTaskFreeAward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.isDouble = false;
            if (message.isDouble != null && message.hasOwnProperty("isDouble"))
                object.isDouble = message.isDouble;
            return object;
        };

        /**
         * Converts this CClaimTaskFreeAward to JSON.
         * @function toJSON
         * @memberof game.CClaimTaskFreeAward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CClaimTaskFreeAward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CClaimTaskFreeAward
         * @function getTypeUrl
         * @memberof game.CClaimTaskFreeAward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CClaimTaskFreeAward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CClaimTaskFreeAward";
        };

        return CClaimTaskFreeAward;
    })();

    game.SClaimTaskFreeAward = (function() {

        /**
         * Properties of a SClaimTaskFreeAward.
         * @memberof game
         * @interface ISClaimTaskFreeAward
         * @property {Array.<common.IReward>|null} [awards] SClaimTaskFreeAward awards
         * @property {number|null} [awardPoint] SClaimTaskFreeAward awardPoint
         * @property {number|Long|null} [getTime] SClaimTaskFreeAward getTime
         * @property {number|null} [getIndex] SClaimTaskFreeAward getIndex
         */

        /**
         * Constructs a new SClaimTaskFreeAward.
         * @memberof game
         * @classdesc Represents a SClaimTaskFreeAward.
         * @implements ISClaimTaskFreeAward
         * @constructor
         * @param {game.ISClaimTaskFreeAward=} [properties] Properties to set
         */
        function SClaimTaskFreeAward(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SClaimTaskFreeAward awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SClaimTaskFreeAward
         * @instance
         */
        SClaimTaskFreeAward.prototype.awards = $util.emptyArray;

        /**
         * SClaimTaskFreeAward awardPoint.
         * @member {number} awardPoint
         * @memberof game.SClaimTaskFreeAward
         * @instance
         */
        SClaimTaskFreeAward.prototype.awardPoint = 0;

        /**
         * SClaimTaskFreeAward getTime.
         * @member {number|Long} getTime
         * @memberof game.SClaimTaskFreeAward
         * @instance
         */
        SClaimTaskFreeAward.prototype.getTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SClaimTaskFreeAward getIndex.
         * @member {number} getIndex
         * @memberof game.SClaimTaskFreeAward
         * @instance
         */
        SClaimTaskFreeAward.prototype.getIndex = 0;

        /**
         * Creates a new SClaimTaskFreeAward instance using the specified properties.
         * @function create
         * @memberof game.SClaimTaskFreeAward
         * @static
         * @param {game.ISClaimTaskFreeAward=} [properties] Properties to set
         * @returns {game.SClaimTaskFreeAward} SClaimTaskFreeAward instance
         */
        SClaimTaskFreeAward.create = function create(properties) {
            return new SClaimTaskFreeAward(properties);
        };

        /**
         * Encodes the specified SClaimTaskFreeAward message. Does not implicitly {@link game.SClaimTaskFreeAward.verify|verify} messages.
         * @function encode
         * @memberof game.SClaimTaskFreeAward
         * @static
         * @param {game.ISClaimTaskFreeAward} message SClaimTaskFreeAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SClaimTaskFreeAward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.awardPoint != null && Object.hasOwnProperty.call(message, "awardPoint"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.awardPoint);
            if (message.getTime != null && Object.hasOwnProperty.call(message, "getTime"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.getTime);
            if (message.getIndex != null && Object.hasOwnProperty.call(message, "getIndex"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.getIndex);
            return writer;
        };

        /**
         * Encodes the specified SClaimTaskFreeAward message, length delimited. Does not implicitly {@link game.SClaimTaskFreeAward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SClaimTaskFreeAward
         * @static
         * @param {game.ISClaimTaskFreeAward} message SClaimTaskFreeAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SClaimTaskFreeAward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SClaimTaskFreeAward message from the specified reader or buffer.
         * @function decode
         * @memberof game.SClaimTaskFreeAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SClaimTaskFreeAward} SClaimTaskFreeAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SClaimTaskFreeAward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SClaimTaskFreeAward();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.awardPoint = reader.int32();
                        break;
                    }
                case 3: {
                        message.getTime = reader.int64();
                        break;
                    }
                case 4: {
                        message.getIndex = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SClaimTaskFreeAward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SClaimTaskFreeAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SClaimTaskFreeAward} SClaimTaskFreeAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SClaimTaskFreeAward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SClaimTaskFreeAward message.
         * @function verify
         * @memberof game.SClaimTaskFreeAward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SClaimTaskFreeAward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            if (message.awardPoint != null && message.hasOwnProperty("awardPoint"))
                if (!$util.isInteger(message.awardPoint))
                    return "awardPoint: integer expected";
            if (message.getTime != null && message.hasOwnProperty("getTime"))
                if (!$util.isInteger(message.getTime) && !(message.getTime && $util.isInteger(message.getTime.low) && $util.isInteger(message.getTime.high)))
                    return "getTime: integer|Long expected";
            if (message.getIndex != null && message.hasOwnProperty("getIndex"))
                if (!$util.isInteger(message.getIndex))
                    return "getIndex: integer expected";
            return null;
        };

        /**
         * Creates a SClaimTaskFreeAward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SClaimTaskFreeAward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SClaimTaskFreeAward} SClaimTaskFreeAward
         */
        SClaimTaskFreeAward.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SClaimTaskFreeAward)
                return object;
            var message = new $root.game.SClaimTaskFreeAward();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SClaimTaskFreeAward.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SClaimTaskFreeAward.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            if (object.awardPoint != null)
                message.awardPoint = object.awardPoint | 0;
            if (object.getTime != null)
                if ($util.Long)
                    (message.getTime = $util.Long.fromValue(object.getTime)).unsigned = false;
                else if (typeof object.getTime === "string")
                    message.getTime = parseInt(object.getTime, 10);
                else if (typeof object.getTime === "number")
                    message.getTime = object.getTime;
                else if (typeof object.getTime === "object")
                    message.getTime = new $util.LongBits(object.getTime.low >>> 0, object.getTime.high >>> 0).toNumber();
            if (object.getIndex != null)
                message.getIndex = object.getIndex | 0;
            return message;
        };

        /**
         * Creates a plain object from a SClaimTaskFreeAward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SClaimTaskFreeAward
         * @static
         * @param {game.SClaimTaskFreeAward} message SClaimTaskFreeAward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SClaimTaskFreeAward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (options.defaults) {
                object.awardPoint = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.getTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.getTime = options.longs === String ? "0" : 0;
                object.getIndex = 0;
            }
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            if (message.awardPoint != null && message.hasOwnProperty("awardPoint"))
                object.awardPoint = message.awardPoint;
            if (message.getTime != null && message.hasOwnProperty("getTime"))
                if (typeof message.getTime === "number")
                    object.getTime = options.longs === String ? String(message.getTime) : message.getTime;
                else
                    object.getTime = options.longs === String ? $util.Long.prototype.toString.call(message.getTime) : options.longs === Number ? new $util.LongBits(message.getTime.low >>> 0, message.getTime.high >>> 0).toNumber() : message.getTime;
            if (message.getIndex != null && message.hasOwnProperty("getIndex"))
                object.getIndex = message.getIndex;
            return object;
        };

        /**
         * Converts this SClaimTaskFreeAward to JSON.
         * @function toJSON
         * @memberof game.SClaimTaskFreeAward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SClaimTaskFreeAward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SClaimTaskFreeAward
         * @function getTypeUrl
         * @memberof game.SClaimTaskFreeAward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SClaimTaskFreeAward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SClaimTaskFreeAward";
        };

        return SClaimTaskFreeAward;
    })();

    game.CFinishTask = (function() {

        /**
         * Properties of a CFinishTask.
         * @memberof game
         * @interface ICFinishTask
         * @property {number|null} [id] CFinishTask id
         */

        /**
         * Constructs a new CFinishTask.
         * @memberof game
         * @classdesc Represents a CFinishTask.
         * @implements ICFinishTask
         * @constructor
         * @param {game.ICFinishTask=} [properties] Properties to set
         */
        function CFinishTask(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CFinishTask id.
         * @member {number} id
         * @memberof game.CFinishTask
         * @instance
         */
        CFinishTask.prototype.id = 0;

        /**
         * Creates a new CFinishTask instance using the specified properties.
         * @function create
         * @memberof game.CFinishTask
         * @static
         * @param {game.ICFinishTask=} [properties] Properties to set
         * @returns {game.CFinishTask} CFinishTask instance
         */
        CFinishTask.create = function create(properties) {
            return new CFinishTask(properties);
        };

        /**
         * Encodes the specified CFinishTask message. Does not implicitly {@link game.CFinishTask.verify|verify} messages.
         * @function encode
         * @memberof game.CFinishTask
         * @static
         * @param {game.ICFinishTask} message CFinishTask message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CFinishTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            return writer;
        };

        /**
         * Encodes the specified CFinishTask message, length delimited. Does not implicitly {@link game.CFinishTask.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CFinishTask
         * @static
         * @param {game.ICFinishTask} message CFinishTask message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CFinishTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CFinishTask message from the specified reader or buffer.
         * @function decode
         * @memberof game.CFinishTask
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CFinishTask} CFinishTask
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CFinishTask.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CFinishTask();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CFinishTask message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CFinishTask
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CFinishTask} CFinishTask
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CFinishTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CFinishTask message.
         * @function verify
         * @memberof game.CFinishTask
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CFinishTask.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        /**
         * Creates a CFinishTask message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CFinishTask
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CFinishTask} CFinishTask
         */
        CFinishTask.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CFinishTask)
                return object;
            var message = new $root.game.CFinishTask();
            if (object.id != null)
                message.id = object.id | 0;
            return message;
        };

        /**
         * Creates a plain object from a CFinishTask message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CFinishTask
         * @static
         * @param {game.CFinishTask} message CFinishTask
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CFinishTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        /**
         * Converts this CFinishTask to JSON.
         * @function toJSON
         * @memberof game.CFinishTask
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CFinishTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CFinishTask
         * @function getTypeUrl
         * @memberof game.CFinishTask
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CFinishTask.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CFinishTask";
        };

        return CFinishTask;
    })();

    game.SFinishTask = (function() {

        /**
         * Properties of a SFinishTask.
         * @memberof game
         * @interface ISFinishTask
         * @property {Array.<common.IReward>|null} [awards] SFinishTask awards
         * @property {number|null} [awardPoint] SFinishTask awardPoint
         */

        /**
         * Constructs a new SFinishTask.
         * @memberof game
         * @classdesc Represents a SFinishTask.
         * @implements ISFinishTask
         * @constructor
         * @param {game.ISFinishTask=} [properties] Properties to set
         */
        function SFinishTask(properties) {
            this.awards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SFinishTask awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SFinishTask
         * @instance
         */
        SFinishTask.prototype.awards = $util.emptyArray;

        /**
         * SFinishTask awardPoint.
         * @member {number} awardPoint
         * @memberof game.SFinishTask
         * @instance
         */
        SFinishTask.prototype.awardPoint = 0;

        /**
         * Creates a new SFinishTask instance using the specified properties.
         * @function create
         * @memberof game.SFinishTask
         * @static
         * @param {game.ISFinishTask=} [properties] Properties to set
         * @returns {game.SFinishTask} SFinishTask instance
         */
        SFinishTask.create = function create(properties) {
            return new SFinishTask(properties);
        };

        /**
         * Encodes the specified SFinishTask message. Does not implicitly {@link game.SFinishTask.verify|verify} messages.
         * @function encode
         * @memberof game.SFinishTask
         * @static
         * @param {game.ISFinishTask} message SFinishTask message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SFinishTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.awardPoint != null && Object.hasOwnProperty.call(message, "awardPoint"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.awardPoint);
            return writer;
        };

        /**
         * Encodes the specified SFinishTask message, length delimited. Does not implicitly {@link game.SFinishTask.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SFinishTask
         * @static
         * @param {game.ISFinishTask} message SFinishTask message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SFinishTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SFinishTask message from the specified reader or buffer.
         * @function decode
         * @memberof game.SFinishTask
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SFinishTask} SFinishTask
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SFinishTask.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SFinishTask();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.awardPoint = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SFinishTask message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SFinishTask
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SFinishTask} SFinishTask
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SFinishTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SFinishTask message.
         * @function verify
         * @memberof game.SFinishTask
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SFinishTask.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            if (message.awardPoint != null && message.hasOwnProperty("awardPoint"))
                if (!$util.isInteger(message.awardPoint))
                    return "awardPoint: integer expected";
            return null;
        };

        /**
         * Creates a SFinishTask message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SFinishTask
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SFinishTask} SFinishTask
         */
        SFinishTask.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SFinishTask)
                return object;
            var message = new $root.game.SFinishTask();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SFinishTask.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SFinishTask.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            if (object.awardPoint != null)
                message.awardPoint = object.awardPoint | 0;
            return message;
        };

        /**
         * Creates a plain object from a SFinishTask message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SFinishTask
         * @static
         * @param {game.SFinishTask} message SFinishTask
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SFinishTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.awards = [];
            if (options.defaults)
                object.awardPoint = 0;
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            if (message.awardPoint != null && message.hasOwnProperty("awardPoint"))
                object.awardPoint = message.awardPoint;
            return object;
        };

        /**
         * Converts this SFinishTask to JSON.
         * @function toJSON
         * @memberof game.SFinishTask
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SFinishTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SFinishTask
         * @function getTypeUrl
         * @memberof game.SFinishTask
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SFinishTask.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SFinishTask";
        };

        return SFinishTask;
    })();

    game.CRefreshTask = (function() {

        /**
         * Properties of a CRefreshTask.
         * @memberof game
         * @interface ICRefreshTask
         * @property {number|null} [id] CRefreshTask id
         */

        /**
         * Constructs a new CRefreshTask.
         * @memberof game
         * @classdesc Represents a CRefreshTask.
         * @implements ICRefreshTask
         * @constructor
         * @param {game.ICRefreshTask=} [properties] Properties to set
         */
        function CRefreshTask(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CRefreshTask id.
         * @member {number} id
         * @memberof game.CRefreshTask
         * @instance
         */
        CRefreshTask.prototype.id = 0;

        /**
         * Creates a new CRefreshTask instance using the specified properties.
         * @function create
         * @memberof game.CRefreshTask
         * @static
         * @param {game.ICRefreshTask=} [properties] Properties to set
         * @returns {game.CRefreshTask} CRefreshTask instance
         */
        CRefreshTask.create = function create(properties) {
            return new CRefreshTask(properties);
        };

        /**
         * Encodes the specified CRefreshTask message. Does not implicitly {@link game.CRefreshTask.verify|verify} messages.
         * @function encode
         * @memberof game.CRefreshTask
         * @static
         * @param {game.ICRefreshTask} message CRefreshTask message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CRefreshTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            return writer;
        };

        /**
         * Encodes the specified CRefreshTask message, length delimited. Does not implicitly {@link game.CRefreshTask.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CRefreshTask
         * @static
         * @param {game.ICRefreshTask} message CRefreshTask message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CRefreshTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CRefreshTask message from the specified reader or buffer.
         * @function decode
         * @memberof game.CRefreshTask
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CRefreshTask} CRefreshTask
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CRefreshTask.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CRefreshTask();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CRefreshTask message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CRefreshTask
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CRefreshTask} CRefreshTask
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CRefreshTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CRefreshTask message.
         * @function verify
         * @memberof game.CRefreshTask
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CRefreshTask.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        /**
         * Creates a CRefreshTask message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CRefreshTask
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CRefreshTask} CRefreshTask
         */
        CRefreshTask.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CRefreshTask)
                return object;
            var message = new $root.game.CRefreshTask();
            if (object.id != null)
                message.id = object.id | 0;
            return message;
        };

        /**
         * Creates a plain object from a CRefreshTask message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CRefreshTask
         * @static
         * @param {game.CRefreshTask} message CRefreshTask
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CRefreshTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        /**
         * Converts this CRefreshTask to JSON.
         * @function toJSON
         * @memberof game.CRefreshTask
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CRefreshTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CRefreshTask
         * @function getTypeUrl
         * @memberof game.CRefreshTask
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CRefreshTask.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CRefreshTask";
        };

        return CRefreshTask;
    })();

    game.SRefreshTask = (function() {

        /**
         * Properties of a SRefreshTask.
         * @memberof game
         * @interface ISRefreshTask
         * @property {Array.<common.IReward>|null} [costs] SRefreshTask costs
         * @property {number|null} [newTaskId] SRefreshTask newTaskId
         */

        /**
         * Constructs a new SRefreshTask.
         * @memberof game
         * @classdesc Represents a SRefreshTask.
         * @implements ISRefreshTask
         * @constructor
         * @param {game.ISRefreshTask=} [properties] Properties to set
         */
        function SRefreshTask(properties) {
            this.costs = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SRefreshTask costs.
         * @member {Array.<common.IReward>} costs
         * @memberof game.SRefreshTask
         * @instance
         */
        SRefreshTask.prototype.costs = $util.emptyArray;

        /**
         * SRefreshTask newTaskId.
         * @member {number} newTaskId
         * @memberof game.SRefreshTask
         * @instance
         */
        SRefreshTask.prototype.newTaskId = 0;

        /**
         * Creates a new SRefreshTask instance using the specified properties.
         * @function create
         * @memberof game.SRefreshTask
         * @static
         * @param {game.ISRefreshTask=} [properties] Properties to set
         * @returns {game.SRefreshTask} SRefreshTask instance
         */
        SRefreshTask.create = function create(properties) {
            return new SRefreshTask(properties);
        };

        /**
         * Encodes the specified SRefreshTask message. Does not implicitly {@link game.SRefreshTask.verify|verify} messages.
         * @function encode
         * @memberof game.SRefreshTask
         * @static
         * @param {game.ISRefreshTask} message SRefreshTask message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SRefreshTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.costs != null && message.costs.length)
                for (var i = 0; i < message.costs.length; ++i)
                    $root.common.Reward.encode(message.costs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.newTaskId != null && Object.hasOwnProperty.call(message, "newTaskId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.newTaskId);
            return writer;
        };

        /**
         * Encodes the specified SRefreshTask message, length delimited. Does not implicitly {@link game.SRefreshTask.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SRefreshTask
         * @static
         * @param {game.ISRefreshTask} message SRefreshTask message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SRefreshTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SRefreshTask message from the specified reader or buffer.
         * @function decode
         * @memberof game.SRefreshTask
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SRefreshTask} SRefreshTask
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SRefreshTask.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SRefreshTask();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.costs && message.costs.length))
                            message.costs = [];
                        message.costs.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.newTaskId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SRefreshTask message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SRefreshTask
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SRefreshTask} SRefreshTask
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SRefreshTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SRefreshTask message.
         * @function verify
         * @memberof game.SRefreshTask
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SRefreshTask.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.costs != null && message.hasOwnProperty("costs")) {
                if (!Array.isArray(message.costs))
                    return "costs: array expected";
                for (var i = 0; i < message.costs.length; ++i) {
                    var error = $root.common.Reward.verify(message.costs[i]);
                    if (error)
                        return "costs." + error;
                }
            }
            if (message.newTaskId != null && message.hasOwnProperty("newTaskId"))
                if (!$util.isInteger(message.newTaskId))
                    return "newTaskId: integer expected";
            return null;
        };

        /**
         * Creates a SRefreshTask message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SRefreshTask
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SRefreshTask} SRefreshTask
         */
        SRefreshTask.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SRefreshTask)
                return object;
            var message = new $root.game.SRefreshTask();
            if (object.costs) {
                if (!Array.isArray(object.costs))
                    throw TypeError(".game.SRefreshTask.costs: array expected");
                message.costs = [];
                for (var i = 0; i < object.costs.length; ++i) {
                    if (typeof object.costs[i] !== "object")
                        throw TypeError(".game.SRefreshTask.costs: object expected");
                    message.costs[i] = $root.common.Reward.fromObject(object.costs[i]);
                }
            }
            if (object.newTaskId != null)
                message.newTaskId = object.newTaskId | 0;
            return message;
        };

        /**
         * Creates a plain object from a SRefreshTask message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SRefreshTask
         * @static
         * @param {game.SRefreshTask} message SRefreshTask
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SRefreshTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.costs = [];
            if (options.defaults)
                object.newTaskId = 0;
            if (message.costs && message.costs.length) {
                object.costs = [];
                for (var j = 0; j < message.costs.length; ++j)
                    object.costs[j] = $root.common.Reward.toObject(message.costs[j], options);
            }
            if (message.newTaskId != null && message.hasOwnProperty("newTaskId"))
                object.newTaskId = message.newTaskId;
            return object;
        };

        /**
         * Converts this SRefreshTask to JSON.
         * @function toJSON
         * @memberof game.SRefreshTask
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SRefreshTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SRefreshTask
         * @function getTypeUrl
         * @memberof game.SRefreshTask
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SRefreshTask.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SRefreshTask";
        };

        return SRefreshTask;
    })();

    game.CPutOnSale = (function() {

        /**
         * Properties of a CPutOnSale.
         * @memberof game
         * @interface ICPutOnSale
         * @property {number|null} [itemId] CPutOnSale itemId
         * @property {string|null} [itemType] CPutOnSale itemType
         * @property {string|null} [itemTag] CPutOnSale itemTag
         * @property {number|null} [itemCount] CPutOnSale itemCount
         * @property {number|null} [itemPrice] CPutOnSale itemPrice
         * @property {number|null} [publicityTime] CPutOnSale publicityTime
         */

        /**
         * Constructs a new CPutOnSale.
         * @memberof game
         * @classdesc Represents a CPutOnSale.
         * @implements ICPutOnSale
         * @constructor
         * @param {game.ICPutOnSale=} [properties] Properties to set
         */
        function CPutOnSale(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CPutOnSale itemId.
         * @member {number} itemId
         * @memberof game.CPutOnSale
         * @instance
         */
        CPutOnSale.prototype.itemId = 0;

        /**
         * CPutOnSale itemType.
         * @member {string} itemType
         * @memberof game.CPutOnSale
         * @instance
         */
        CPutOnSale.prototype.itemType = "";

        /**
         * CPutOnSale itemTag.
         * @member {string} itemTag
         * @memberof game.CPutOnSale
         * @instance
         */
        CPutOnSale.prototype.itemTag = "";

        /**
         * CPutOnSale itemCount.
         * @member {number} itemCount
         * @memberof game.CPutOnSale
         * @instance
         */
        CPutOnSale.prototype.itemCount = 0;

        /**
         * CPutOnSale itemPrice.
         * @member {number} itemPrice
         * @memberof game.CPutOnSale
         * @instance
         */
        CPutOnSale.prototype.itemPrice = 0;

        /**
         * CPutOnSale publicityTime.
         * @member {number} publicityTime
         * @memberof game.CPutOnSale
         * @instance
         */
        CPutOnSale.prototype.publicityTime = 0;

        /**
         * Creates a new CPutOnSale instance using the specified properties.
         * @function create
         * @memberof game.CPutOnSale
         * @static
         * @param {game.ICPutOnSale=} [properties] Properties to set
         * @returns {game.CPutOnSale} CPutOnSale instance
         */
        CPutOnSale.create = function create(properties) {
            return new CPutOnSale(properties);
        };

        /**
         * Encodes the specified CPutOnSale message. Does not implicitly {@link game.CPutOnSale.verify|verify} messages.
         * @function encode
         * @memberof game.CPutOnSale
         * @static
         * @param {game.ICPutOnSale} message CPutOnSale message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CPutOnSale.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.itemId != null && Object.hasOwnProperty.call(message, "itemId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.itemId);
            if (message.itemType != null && Object.hasOwnProperty.call(message, "itemType"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.itemType);
            if (message.itemTag != null && Object.hasOwnProperty.call(message, "itemTag"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.itemTag);
            if (message.itemCount != null && Object.hasOwnProperty.call(message, "itemCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.itemCount);
            if (message.itemPrice != null && Object.hasOwnProperty.call(message, "itemPrice"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.itemPrice);
            if (message.publicityTime != null && Object.hasOwnProperty.call(message, "publicityTime"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.publicityTime);
            return writer;
        };

        /**
         * Encodes the specified CPutOnSale message, length delimited. Does not implicitly {@link game.CPutOnSale.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CPutOnSale
         * @static
         * @param {game.ICPutOnSale} message CPutOnSale message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CPutOnSale.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CPutOnSale message from the specified reader or buffer.
         * @function decode
         * @memberof game.CPutOnSale
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CPutOnSale} CPutOnSale
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CPutOnSale.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CPutOnSale();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.itemId = reader.int32();
                        break;
                    }
                case 2: {
                        message.itemType = reader.string();
                        break;
                    }
                case 3: {
                        message.itemTag = reader.string();
                        break;
                    }
                case 4: {
                        message.itemCount = reader.int32();
                        break;
                    }
                case 5: {
                        message.itemPrice = reader.int32();
                        break;
                    }
                case 11: {
                        message.publicityTime = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CPutOnSale message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CPutOnSale
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CPutOnSale} CPutOnSale
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CPutOnSale.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CPutOnSale message.
         * @function verify
         * @memberof game.CPutOnSale
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CPutOnSale.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                if (!$util.isInteger(message.itemId))
                    return "itemId: integer expected";
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                if (!$util.isString(message.itemType))
                    return "itemType: string expected";
            if (message.itemTag != null && message.hasOwnProperty("itemTag"))
                if (!$util.isString(message.itemTag))
                    return "itemTag: string expected";
            if (message.itemCount != null && message.hasOwnProperty("itemCount"))
                if (!$util.isInteger(message.itemCount))
                    return "itemCount: integer expected";
            if (message.itemPrice != null && message.hasOwnProperty("itemPrice"))
                if (!$util.isInteger(message.itemPrice))
                    return "itemPrice: integer expected";
            if (message.publicityTime != null && message.hasOwnProperty("publicityTime"))
                if (!$util.isInteger(message.publicityTime))
                    return "publicityTime: integer expected";
            return null;
        };

        /**
         * Creates a CPutOnSale message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CPutOnSale
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CPutOnSale} CPutOnSale
         */
        CPutOnSale.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CPutOnSale)
                return object;
            var message = new $root.game.CPutOnSale();
            if (object.itemId != null)
                message.itemId = object.itemId | 0;
            if (object.itemType != null)
                message.itemType = String(object.itemType);
            if (object.itemTag != null)
                message.itemTag = String(object.itemTag);
            if (object.itemCount != null)
                message.itemCount = object.itemCount | 0;
            if (object.itemPrice != null)
                message.itemPrice = object.itemPrice | 0;
            if (object.publicityTime != null)
                message.publicityTime = object.publicityTime | 0;
            return message;
        };

        /**
         * Creates a plain object from a CPutOnSale message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CPutOnSale
         * @static
         * @param {game.CPutOnSale} message CPutOnSale
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CPutOnSale.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.itemId = 0;
                object.itemType = "";
                object.itemTag = "";
                object.itemCount = 0;
                object.itemPrice = 0;
                object.publicityTime = 0;
            }
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                object.itemId = message.itemId;
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                object.itemType = message.itemType;
            if (message.itemTag != null && message.hasOwnProperty("itemTag"))
                object.itemTag = message.itemTag;
            if (message.itemCount != null && message.hasOwnProperty("itemCount"))
                object.itemCount = message.itemCount;
            if (message.itemPrice != null && message.hasOwnProperty("itemPrice"))
                object.itemPrice = message.itemPrice;
            if (message.publicityTime != null && message.hasOwnProperty("publicityTime"))
                object.publicityTime = message.publicityTime;
            return object;
        };

        /**
         * Converts this CPutOnSale to JSON.
         * @function toJSON
         * @memberof game.CPutOnSale
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CPutOnSale.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CPutOnSale
         * @function getTypeUrl
         * @memberof game.CPutOnSale
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CPutOnSale.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CPutOnSale";
        };

        return CPutOnSale;
    })();

    game.SPutOnSale = (function() {

        /**
         * Properties of a SPutOnSale.
         * @memberof game
         * @interface ISPutOnSale
         * @property {common.ISaleItemInfo|null} [saleInfo] SPutOnSale saleInfo
         */

        /**
         * Constructs a new SPutOnSale.
         * @memberof game
         * @classdesc Represents a SPutOnSale.
         * @implements ISPutOnSale
         * @constructor
         * @param {game.ISPutOnSale=} [properties] Properties to set
         */
        function SPutOnSale(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SPutOnSale saleInfo.
         * @member {common.ISaleItemInfo|null|undefined} saleInfo
         * @memberof game.SPutOnSale
         * @instance
         */
        SPutOnSale.prototype.saleInfo = null;

        /**
         * Creates a new SPutOnSale instance using the specified properties.
         * @function create
         * @memberof game.SPutOnSale
         * @static
         * @param {game.ISPutOnSale=} [properties] Properties to set
         * @returns {game.SPutOnSale} SPutOnSale instance
         */
        SPutOnSale.create = function create(properties) {
            return new SPutOnSale(properties);
        };

        /**
         * Encodes the specified SPutOnSale message. Does not implicitly {@link game.SPutOnSale.verify|verify} messages.
         * @function encode
         * @memberof game.SPutOnSale
         * @static
         * @param {game.ISPutOnSale} message SPutOnSale message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SPutOnSale.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.saleInfo != null && Object.hasOwnProperty.call(message, "saleInfo"))
                $root.common.SaleItemInfo.encode(message.saleInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SPutOnSale message, length delimited. Does not implicitly {@link game.SPutOnSale.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SPutOnSale
         * @static
         * @param {game.ISPutOnSale} message SPutOnSale message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SPutOnSale.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SPutOnSale message from the specified reader or buffer.
         * @function decode
         * @memberof game.SPutOnSale
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SPutOnSale} SPutOnSale
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SPutOnSale.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SPutOnSale();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.saleInfo = $root.common.SaleItemInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SPutOnSale message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SPutOnSale
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SPutOnSale} SPutOnSale
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SPutOnSale.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SPutOnSale message.
         * @function verify
         * @memberof game.SPutOnSale
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SPutOnSale.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.saleInfo != null && message.hasOwnProperty("saleInfo")) {
                var error = $root.common.SaleItemInfo.verify(message.saleInfo);
                if (error)
                    return "saleInfo." + error;
            }
            return null;
        };

        /**
         * Creates a SPutOnSale message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SPutOnSale
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SPutOnSale} SPutOnSale
         */
        SPutOnSale.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SPutOnSale)
                return object;
            var message = new $root.game.SPutOnSale();
            if (object.saleInfo != null) {
                if (typeof object.saleInfo !== "object")
                    throw TypeError(".game.SPutOnSale.saleInfo: object expected");
                message.saleInfo = $root.common.SaleItemInfo.fromObject(object.saleInfo);
            }
            return message;
        };

        /**
         * Creates a plain object from a SPutOnSale message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SPutOnSale
         * @static
         * @param {game.SPutOnSale} message SPutOnSale
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SPutOnSale.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.saleInfo = null;
            if (message.saleInfo != null && message.hasOwnProperty("saleInfo"))
                object.saleInfo = $root.common.SaleItemInfo.toObject(message.saleInfo, options);
            return object;
        };

        /**
         * Converts this SPutOnSale to JSON.
         * @function toJSON
         * @memberof game.SPutOnSale
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SPutOnSale.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SPutOnSale
         * @function getTypeUrl
         * @memberof game.SPutOnSale
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SPutOnSale.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SPutOnSale";
        };

        return SPutOnSale;
    })();

    game.CTakeSaleOff = (function() {

        /**
         * Properties of a CTakeSaleOff.
         * @memberof game
         * @interface ICTakeSaleOff
         * @property {number|Long|null} [itemUuid] CTakeSaleOff itemUuid
         */

        /**
         * Constructs a new CTakeSaleOff.
         * @memberof game
         * @classdesc Represents a CTakeSaleOff.
         * @implements ICTakeSaleOff
         * @constructor
         * @param {game.ICTakeSaleOff=} [properties] Properties to set
         */
        function CTakeSaleOff(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CTakeSaleOff itemUuid.
         * @member {number|Long} itemUuid
         * @memberof game.CTakeSaleOff
         * @instance
         */
        CTakeSaleOff.prototype.itemUuid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new CTakeSaleOff instance using the specified properties.
         * @function create
         * @memberof game.CTakeSaleOff
         * @static
         * @param {game.ICTakeSaleOff=} [properties] Properties to set
         * @returns {game.CTakeSaleOff} CTakeSaleOff instance
         */
        CTakeSaleOff.create = function create(properties) {
            return new CTakeSaleOff(properties);
        };

        /**
         * Encodes the specified CTakeSaleOff message. Does not implicitly {@link game.CTakeSaleOff.verify|verify} messages.
         * @function encode
         * @memberof game.CTakeSaleOff
         * @static
         * @param {game.ICTakeSaleOff} message CTakeSaleOff message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CTakeSaleOff.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.itemUuid != null && Object.hasOwnProperty.call(message, "itemUuid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.itemUuid);
            return writer;
        };

        /**
         * Encodes the specified CTakeSaleOff message, length delimited. Does not implicitly {@link game.CTakeSaleOff.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CTakeSaleOff
         * @static
         * @param {game.ICTakeSaleOff} message CTakeSaleOff message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CTakeSaleOff.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CTakeSaleOff message from the specified reader or buffer.
         * @function decode
         * @memberof game.CTakeSaleOff
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CTakeSaleOff} CTakeSaleOff
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CTakeSaleOff.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CTakeSaleOff();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.itemUuid = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CTakeSaleOff message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CTakeSaleOff
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CTakeSaleOff} CTakeSaleOff
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CTakeSaleOff.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CTakeSaleOff message.
         * @function verify
         * @memberof game.CTakeSaleOff
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CTakeSaleOff.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.itemUuid != null && message.hasOwnProperty("itemUuid"))
                if (!$util.isInteger(message.itemUuid) && !(message.itemUuid && $util.isInteger(message.itemUuid.low) && $util.isInteger(message.itemUuid.high)))
                    return "itemUuid: integer|Long expected";
            return null;
        };

        /**
         * Creates a CTakeSaleOff message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CTakeSaleOff
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CTakeSaleOff} CTakeSaleOff
         */
        CTakeSaleOff.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CTakeSaleOff)
                return object;
            var message = new $root.game.CTakeSaleOff();
            if (object.itemUuid != null)
                if ($util.Long)
                    (message.itemUuid = $util.Long.fromValue(object.itemUuid)).unsigned = false;
                else if (typeof object.itemUuid === "string")
                    message.itemUuid = parseInt(object.itemUuid, 10);
                else if (typeof object.itemUuid === "number")
                    message.itemUuid = object.itemUuid;
                else if (typeof object.itemUuid === "object")
                    message.itemUuid = new $util.LongBits(object.itemUuid.low >>> 0, object.itemUuid.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a CTakeSaleOff message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CTakeSaleOff
         * @static
         * @param {game.CTakeSaleOff} message CTakeSaleOff
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CTakeSaleOff.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.itemUuid = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.itemUuid = options.longs === String ? "0" : 0;
            if (message.itemUuid != null && message.hasOwnProperty("itemUuid"))
                if (typeof message.itemUuid === "number")
                    object.itemUuid = options.longs === String ? String(message.itemUuid) : message.itemUuid;
                else
                    object.itemUuid = options.longs === String ? $util.Long.prototype.toString.call(message.itemUuid) : options.longs === Number ? new $util.LongBits(message.itemUuid.low >>> 0, message.itemUuid.high >>> 0).toNumber() : message.itemUuid;
            return object;
        };

        /**
         * Converts this CTakeSaleOff to JSON.
         * @function toJSON
         * @memberof game.CTakeSaleOff
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CTakeSaleOff.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CTakeSaleOff
         * @function getTypeUrl
         * @memberof game.CTakeSaleOff
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CTakeSaleOff.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CTakeSaleOff";
        };

        return CTakeSaleOff;
    })();

    game.STakeSaleOff = (function() {

        /**
         * Properties of a STakeSaleOff.
         * @memberof game
         * @interface ISTakeSaleOff
         * @property {common.IStorageItemInfo|null} [storageItem] STakeSaleOff storageItem
         */

        /**
         * Constructs a new STakeSaleOff.
         * @memberof game
         * @classdesc Represents a STakeSaleOff.
         * @implements ISTakeSaleOff
         * @constructor
         * @param {game.ISTakeSaleOff=} [properties] Properties to set
         */
        function STakeSaleOff(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * STakeSaleOff storageItem.
         * @member {common.IStorageItemInfo|null|undefined} storageItem
         * @memberof game.STakeSaleOff
         * @instance
         */
        STakeSaleOff.prototype.storageItem = null;

        /**
         * Creates a new STakeSaleOff instance using the specified properties.
         * @function create
         * @memberof game.STakeSaleOff
         * @static
         * @param {game.ISTakeSaleOff=} [properties] Properties to set
         * @returns {game.STakeSaleOff} STakeSaleOff instance
         */
        STakeSaleOff.create = function create(properties) {
            return new STakeSaleOff(properties);
        };

        /**
         * Encodes the specified STakeSaleOff message. Does not implicitly {@link game.STakeSaleOff.verify|verify} messages.
         * @function encode
         * @memberof game.STakeSaleOff
         * @static
         * @param {game.ISTakeSaleOff} message STakeSaleOff message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        STakeSaleOff.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.storageItem != null && Object.hasOwnProperty.call(message, "storageItem"))
                $root.common.StorageItemInfo.encode(message.storageItem, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified STakeSaleOff message, length delimited. Does not implicitly {@link game.STakeSaleOff.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.STakeSaleOff
         * @static
         * @param {game.ISTakeSaleOff} message STakeSaleOff message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        STakeSaleOff.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a STakeSaleOff message from the specified reader or buffer.
         * @function decode
         * @memberof game.STakeSaleOff
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.STakeSaleOff} STakeSaleOff
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        STakeSaleOff.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.STakeSaleOff();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.storageItem = $root.common.StorageItemInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a STakeSaleOff message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.STakeSaleOff
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.STakeSaleOff} STakeSaleOff
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        STakeSaleOff.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a STakeSaleOff message.
         * @function verify
         * @memberof game.STakeSaleOff
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        STakeSaleOff.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.storageItem != null && message.hasOwnProperty("storageItem")) {
                var error = $root.common.StorageItemInfo.verify(message.storageItem);
                if (error)
                    return "storageItem." + error;
            }
            return null;
        };

        /**
         * Creates a STakeSaleOff message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.STakeSaleOff
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.STakeSaleOff} STakeSaleOff
         */
        STakeSaleOff.fromObject = function fromObject(object) {
            if (object instanceof $root.game.STakeSaleOff)
                return object;
            var message = new $root.game.STakeSaleOff();
            if (object.storageItem != null) {
                if (typeof object.storageItem !== "object")
                    throw TypeError(".game.STakeSaleOff.storageItem: object expected");
                message.storageItem = $root.common.StorageItemInfo.fromObject(object.storageItem);
            }
            return message;
        };

        /**
         * Creates a plain object from a STakeSaleOff message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.STakeSaleOff
         * @static
         * @param {game.STakeSaleOff} message STakeSaleOff
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        STakeSaleOff.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.storageItem = null;
            if (message.storageItem != null && message.hasOwnProperty("storageItem"))
                object.storageItem = $root.common.StorageItemInfo.toObject(message.storageItem, options);
            return object;
        };

        /**
         * Converts this STakeSaleOff to JSON.
         * @function toJSON
         * @memberof game.STakeSaleOff
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        STakeSaleOff.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for STakeSaleOff
         * @function getTypeUrl
         * @memberof game.STakeSaleOff
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        STakeSaleOff.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.STakeSaleOff";
        };

        return STakeSaleOff;
    })();

    game.CGetMyStorage = (function() {

        /**
         * Properties of a CGetMyStorage.
         * @memberof game
         * @interface ICGetMyStorage
         */

        /**
         * Constructs a new CGetMyStorage.
         * @memberof game
         * @classdesc Represents a CGetMyStorage.
         * @implements ICGetMyStorage
         * @constructor
         * @param {game.ICGetMyStorage=} [properties] Properties to set
         */
        function CGetMyStorage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CGetMyStorage instance using the specified properties.
         * @function create
         * @memberof game.CGetMyStorage
         * @static
         * @param {game.ICGetMyStorage=} [properties] Properties to set
         * @returns {game.CGetMyStorage} CGetMyStorage instance
         */
        CGetMyStorage.create = function create(properties) {
            return new CGetMyStorage(properties);
        };

        /**
         * Encodes the specified CGetMyStorage message. Does not implicitly {@link game.CGetMyStorage.verify|verify} messages.
         * @function encode
         * @memberof game.CGetMyStorage
         * @static
         * @param {game.ICGetMyStorage} message CGetMyStorage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetMyStorage.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CGetMyStorage message, length delimited. Does not implicitly {@link game.CGetMyStorage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetMyStorage
         * @static
         * @param {game.ICGetMyStorage} message CGetMyStorage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetMyStorage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetMyStorage message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetMyStorage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetMyStorage} CGetMyStorage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetMyStorage.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetMyStorage();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetMyStorage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetMyStorage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetMyStorage} CGetMyStorage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetMyStorage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetMyStorage message.
         * @function verify
         * @memberof game.CGetMyStorage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetMyStorage.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CGetMyStorage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetMyStorage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetMyStorage} CGetMyStorage
         */
        CGetMyStorage.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetMyStorage)
                return object;
            return new $root.game.CGetMyStorage();
        };

        /**
         * Creates a plain object from a CGetMyStorage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetMyStorage
         * @static
         * @param {game.CGetMyStorage} message CGetMyStorage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetMyStorage.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CGetMyStorage to JSON.
         * @function toJSON
         * @memberof game.CGetMyStorage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetMyStorage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetMyStorage
         * @function getTypeUrl
         * @memberof game.CGetMyStorage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetMyStorage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetMyStorage";
        };

        return CGetMyStorage;
    })();

    game.SGetMyStorage = (function() {

        /**
         * Properties of a SGetMyStorage.
         * @memberof game
         * @interface ISGetMyStorage
         * @property {Array.<common.IStorageItemInfo>|null} [itemList] SGetMyStorage itemList
         * @property {common.IWalletInfo|null} [walletInfo] SGetMyStorage walletInfo
         */

        /**
         * Constructs a new SGetMyStorage.
         * @memberof game
         * @classdesc Represents a SGetMyStorage.
         * @implements ISGetMyStorage
         * @constructor
         * @param {game.ISGetMyStorage=} [properties] Properties to set
         */
        function SGetMyStorage(properties) {
            this.itemList = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGetMyStorage itemList.
         * @member {Array.<common.IStorageItemInfo>} itemList
         * @memberof game.SGetMyStorage
         * @instance
         */
        SGetMyStorage.prototype.itemList = $util.emptyArray;

        /**
         * SGetMyStorage walletInfo.
         * @member {common.IWalletInfo|null|undefined} walletInfo
         * @memberof game.SGetMyStorage
         * @instance
         */
        SGetMyStorage.prototype.walletInfo = null;

        /**
         * Creates a new SGetMyStorage instance using the specified properties.
         * @function create
         * @memberof game.SGetMyStorage
         * @static
         * @param {game.ISGetMyStorage=} [properties] Properties to set
         * @returns {game.SGetMyStorage} SGetMyStorage instance
         */
        SGetMyStorage.create = function create(properties) {
            return new SGetMyStorage(properties);
        };

        /**
         * Encodes the specified SGetMyStorage message. Does not implicitly {@link game.SGetMyStorage.verify|verify} messages.
         * @function encode
         * @memberof game.SGetMyStorage
         * @static
         * @param {game.ISGetMyStorage} message SGetMyStorage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetMyStorage.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.itemList != null && message.itemList.length)
                for (var i = 0; i < message.itemList.length; ++i)
                    $root.common.StorageItemInfo.encode(message.itemList[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.walletInfo != null && Object.hasOwnProperty.call(message, "walletInfo"))
                $root.common.WalletInfo.encode(message.walletInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SGetMyStorage message, length delimited. Does not implicitly {@link game.SGetMyStorage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetMyStorage
         * @static
         * @param {game.ISGetMyStorage} message SGetMyStorage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetMyStorage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetMyStorage message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetMyStorage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetMyStorage} SGetMyStorage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetMyStorage.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetMyStorage();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.itemList && message.itemList.length))
                            message.itemList = [];
                        message.itemList.push($root.common.StorageItemInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.walletInfo = $root.common.WalletInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetMyStorage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetMyStorage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetMyStorage} SGetMyStorage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetMyStorage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetMyStorage message.
         * @function verify
         * @memberof game.SGetMyStorage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetMyStorage.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.itemList != null && message.hasOwnProperty("itemList")) {
                if (!Array.isArray(message.itemList))
                    return "itemList: array expected";
                for (var i = 0; i < message.itemList.length; ++i) {
                    var error = $root.common.StorageItemInfo.verify(message.itemList[i]);
                    if (error)
                        return "itemList." + error;
                }
            }
            if (message.walletInfo != null && message.hasOwnProperty("walletInfo")) {
                var error = $root.common.WalletInfo.verify(message.walletInfo);
                if (error)
                    return "walletInfo." + error;
            }
            return null;
        };

        /**
         * Creates a SGetMyStorage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetMyStorage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetMyStorage} SGetMyStorage
         */
        SGetMyStorage.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetMyStorage)
                return object;
            var message = new $root.game.SGetMyStorage();
            if (object.itemList) {
                if (!Array.isArray(object.itemList))
                    throw TypeError(".game.SGetMyStorage.itemList: array expected");
                message.itemList = [];
                for (var i = 0; i < object.itemList.length; ++i) {
                    if (typeof object.itemList[i] !== "object")
                        throw TypeError(".game.SGetMyStorage.itemList: object expected");
                    message.itemList[i] = $root.common.StorageItemInfo.fromObject(object.itemList[i]);
                }
            }
            if (object.walletInfo != null) {
                if (typeof object.walletInfo !== "object")
                    throw TypeError(".game.SGetMyStorage.walletInfo: object expected");
                message.walletInfo = $root.common.WalletInfo.fromObject(object.walletInfo);
            }
            return message;
        };

        /**
         * Creates a plain object from a SGetMyStorage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetMyStorage
         * @static
         * @param {game.SGetMyStorage} message SGetMyStorage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetMyStorage.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.itemList = [];
            if (options.defaults)
                object.walletInfo = null;
            if (message.itemList && message.itemList.length) {
                object.itemList = [];
                for (var j = 0; j < message.itemList.length; ++j)
                    object.itemList[j] = $root.common.StorageItemInfo.toObject(message.itemList[j], options);
            }
            if (message.walletInfo != null && message.hasOwnProperty("walletInfo"))
                object.walletInfo = $root.common.WalletInfo.toObject(message.walletInfo, options);
            return object;
        };

        /**
         * Converts this SGetMyStorage to JSON.
         * @function toJSON
         * @memberof game.SGetMyStorage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetMyStorage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetMyStorage
         * @function getTypeUrl
         * @memberof game.SGetMyStorage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetMyStorage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetMyStorage";
        };

        return SGetMyStorage;
    })();

    game.CQuerySales = (function() {

        /**
         * Properties of a CQuerySales.
         * @memberof game
         * @interface ICQuerySales
         * @property {string|null} [itemType] CQuerySales itemType
         * @property {number|null} [itemId] CQuerySales itemId
         * @property {number|null} [offset] CQuerySales offset
         * @property {number|null} [limit] CQuerySales limit
         */

        /**
         * Constructs a new CQuerySales.
         * @memberof game
         * @classdesc Represents a CQuerySales.
         * @implements ICQuerySales
         * @constructor
         * @param {game.ICQuerySales=} [properties] Properties to set
         */
        function CQuerySales(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CQuerySales itemType.
         * @member {string} itemType
         * @memberof game.CQuerySales
         * @instance
         */
        CQuerySales.prototype.itemType = "";

        /**
         * CQuerySales itemId.
         * @member {number} itemId
         * @memberof game.CQuerySales
         * @instance
         */
        CQuerySales.prototype.itemId = 0;

        /**
         * CQuerySales offset.
         * @member {number} offset
         * @memberof game.CQuerySales
         * @instance
         */
        CQuerySales.prototype.offset = 0;

        /**
         * CQuerySales limit.
         * @member {number} limit
         * @memberof game.CQuerySales
         * @instance
         */
        CQuerySales.prototype.limit = 0;

        /**
         * Creates a new CQuerySales instance using the specified properties.
         * @function create
         * @memberof game.CQuerySales
         * @static
         * @param {game.ICQuerySales=} [properties] Properties to set
         * @returns {game.CQuerySales} CQuerySales instance
         */
        CQuerySales.create = function create(properties) {
            return new CQuerySales(properties);
        };

        /**
         * Encodes the specified CQuerySales message. Does not implicitly {@link game.CQuerySales.verify|verify} messages.
         * @function encode
         * @memberof game.CQuerySales
         * @static
         * @param {game.ICQuerySales} message CQuerySales message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CQuerySales.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.itemType != null && Object.hasOwnProperty.call(message, "itemType"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.itemType);
            if (message.itemId != null && Object.hasOwnProperty.call(message, "itemId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.itemId);
            if (message.offset != null && Object.hasOwnProperty.call(message, "offset"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.offset);
            if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.limit);
            return writer;
        };

        /**
         * Encodes the specified CQuerySales message, length delimited. Does not implicitly {@link game.CQuerySales.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CQuerySales
         * @static
         * @param {game.ICQuerySales} message CQuerySales message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CQuerySales.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CQuerySales message from the specified reader or buffer.
         * @function decode
         * @memberof game.CQuerySales
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CQuerySales} CQuerySales
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CQuerySales.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CQuerySales();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.itemType = reader.string();
                        break;
                    }
                case 2: {
                        message.itemId = reader.int32();
                        break;
                    }
                case 3: {
                        message.offset = reader.int32();
                        break;
                    }
                case 4: {
                        message.limit = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CQuerySales message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CQuerySales
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CQuerySales} CQuerySales
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CQuerySales.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CQuerySales message.
         * @function verify
         * @memberof game.CQuerySales
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CQuerySales.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                if (!$util.isString(message.itemType))
                    return "itemType: string expected";
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                if (!$util.isInteger(message.itemId))
                    return "itemId: integer expected";
            if (message.offset != null && message.hasOwnProperty("offset"))
                if (!$util.isInteger(message.offset))
                    return "offset: integer expected";
            if (message.limit != null && message.hasOwnProperty("limit"))
                if (!$util.isInteger(message.limit))
                    return "limit: integer expected";
            return null;
        };

        /**
         * Creates a CQuerySales message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CQuerySales
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CQuerySales} CQuerySales
         */
        CQuerySales.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CQuerySales)
                return object;
            var message = new $root.game.CQuerySales();
            if (object.itemType != null)
                message.itemType = String(object.itemType);
            if (object.itemId != null)
                message.itemId = object.itemId | 0;
            if (object.offset != null)
                message.offset = object.offset | 0;
            if (object.limit != null)
                message.limit = object.limit | 0;
            return message;
        };

        /**
         * Creates a plain object from a CQuerySales message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CQuerySales
         * @static
         * @param {game.CQuerySales} message CQuerySales
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CQuerySales.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.itemType = "";
                object.itemId = 0;
                object.offset = 0;
                object.limit = 0;
            }
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                object.itemType = message.itemType;
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                object.itemId = message.itemId;
            if (message.offset != null && message.hasOwnProperty("offset"))
                object.offset = message.offset;
            if (message.limit != null && message.hasOwnProperty("limit"))
                object.limit = message.limit;
            return object;
        };

        /**
         * Converts this CQuerySales to JSON.
         * @function toJSON
         * @memberof game.CQuerySales
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CQuerySales.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CQuerySales
         * @function getTypeUrl
         * @memberof game.CQuerySales
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CQuerySales.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CQuerySales";
        };

        return CQuerySales;
    })();

    game.SQuerySales = (function() {

        /**
         * Properties of a SQuerySales.
         * @memberof game
         * @interface ISQuerySales
         * @property {Array.<common.ISaleItemInfo>|null} [sales] SQuerySales sales
         */

        /**
         * Constructs a new SQuerySales.
         * @memberof game
         * @classdesc Represents a SQuerySales.
         * @implements ISQuerySales
         * @constructor
         * @param {game.ISQuerySales=} [properties] Properties to set
         */
        function SQuerySales(properties) {
            this.sales = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SQuerySales sales.
         * @member {Array.<common.ISaleItemInfo>} sales
         * @memberof game.SQuerySales
         * @instance
         */
        SQuerySales.prototype.sales = $util.emptyArray;

        /**
         * Creates a new SQuerySales instance using the specified properties.
         * @function create
         * @memberof game.SQuerySales
         * @static
         * @param {game.ISQuerySales=} [properties] Properties to set
         * @returns {game.SQuerySales} SQuerySales instance
         */
        SQuerySales.create = function create(properties) {
            return new SQuerySales(properties);
        };

        /**
         * Encodes the specified SQuerySales message. Does not implicitly {@link game.SQuerySales.verify|verify} messages.
         * @function encode
         * @memberof game.SQuerySales
         * @static
         * @param {game.ISQuerySales} message SQuerySales message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SQuerySales.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.sales != null && message.sales.length)
                for (var i = 0; i < message.sales.length; ++i)
                    $root.common.SaleItemInfo.encode(message.sales[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SQuerySales message, length delimited. Does not implicitly {@link game.SQuerySales.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SQuerySales
         * @static
         * @param {game.ISQuerySales} message SQuerySales message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SQuerySales.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SQuerySales message from the specified reader or buffer.
         * @function decode
         * @memberof game.SQuerySales
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SQuerySales} SQuerySales
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SQuerySales.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SQuerySales();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.sales && message.sales.length))
                            message.sales = [];
                        message.sales.push($root.common.SaleItemInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SQuerySales message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SQuerySales
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SQuerySales} SQuerySales
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SQuerySales.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SQuerySales message.
         * @function verify
         * @memberof game.SQuerySales
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SQuerySales.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.sales != null && message.hasOwnProperty("sales")) {
                if (!Array.isArray(message.sales))
                    return "sales: array expected";
                for (var i = 0; i < message.sales.length; ++i) {
                    var error = $root.common.SaleItemInfo.verify(message.sales[i]);
                    if (error)
                        return "sales." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SQuerySales message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SQuerySales
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SQuerySales} SQuerySales
         */
        SQuerySales.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SQuerySales)
                return object;
            var message = new $root.game.SQuerySales();
            if (object.sales) {
                if (!Array.isArray(object.sales))
                    throw TypeError(".game.SQuerySales.sales: array expected");
                message.sales = [];
                for (var i = 0; i < object.sales.length; ++i) {
                    if (typeof object.sales[i] !== "object")
                        throw TypeError(".game.SQuerySales.sales: object expected");
                    message.sales[i] = $root.common.SaleItemInfo.fromObject(object.sales[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SQuerySales message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SQuerySales
         * @static
         * @param {game.SQuerySales} message SQuerySales
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SQuerySales.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.sales = [];
            if (message.sales && message.sales.length) {
                object.sales = [];
                for (var j = 0; j < message.sales.length; ++j)
                    object.sales[j] = $root.common.SaleItemInfo.toObject(message.sales[j], options);
            }
            return object;
        };

        /**
         * Converts this SQuerySales to JSON.
         * @function toJSON
         * @memberof game.SQuerySales
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SQuerySales.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SQuerySales
         * @function getTypeUrl
         * @memberof game.SQuerySales
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SQuerySales.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SQuerySales";
        };

        return SQuerySales;
    })();

    game.CQueryGoods = (function() {

        /**
         * Properties of a CQueryGoods.
         * @memberof game
         * @interface ICQueryGoods
         * @property {string|null} [itemType] CQueryGoods itemType
         * @property {number|null} [offset] CQueryGoods offset
         * @property {number|null} [limit] CQueryGoods limit
         */

        /**
         * Constructs a new CQueryGoods.
         * @memberof game
         * @classdesc Represents a CQueryGoods.
         * @implements ICQueryGoods
         * @constructor
         * @param {game.ICQueryGoods=} [properties] Properties to set
         */
        function CQueryGoods(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CQueryGoods itemType.
         * @member {string} itemType
         * @memberof game.CQueryGoods
         * @instance
         */
        CQueryGoods.prototype.itemType = "";

        /**
         * CQueryGoods offset.
         * @member {number} offset
         * @memberof game.CQueryGoods
         * @instance
         */
        CQueryGoods.prototype.offset = 0;

        /**
         * CQueryGoods limit.
         * @member {number} limit
         * @memberof game.CQueryGoods
         * @instance
         */
        CQueryGoods.prototype.limit = 0;

        /**
         * Creates a new CQueryGoods instance using the specified properties.
         * @function create
         * @memberof game.CQueryGoods
         * @static
         * @param {game.ICQueryGoods=} [properties] Properties to set
         * @returns {game.CQueryGoods} CQueryGoods instance
         */
        CQueryGoods.create = function create(properties) {
            return new CQueryGoods(properties);
        };

        /**
         * Encodes the specified CQueryGoods message. Does not implicitly {@link game.CQueryGoods.verify|verify} messages.
         * @function encode
         * @memberof game.CQueryGoods
         * @static
         * @param {game.ICQueryGoods} message CQueryGoods message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CQueryGoods.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.itemType != null && Object.hasOwnProperty.call(message, "itemType"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.itemType);
            if (message.offset != null && Object.hasOwnProperty.call(message, "offset"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.offset);
            if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.limit);
            return writer;
        };

        /**
         * Encodes the specified CQueryGoods message, length delimited. Does not implicitly {@link game.CQueryGoods.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CQueryGoods
         * @static
         * @param {game.ICQueryGoods} message CQueryGoods message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CQueryGoods.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CQueryGoods message from the specified reader or buffer.
         * @function decode
         * @memberof game.CQueryGoods
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CQueryGoods} CQueryGoods
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CQueryGoods.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CQueryGoods();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.itemType = reader.string();
                        break;
                    }
                case 2: {
                        message.offset = reader.int32();
                        break;
                    }
                case 3: {
                        message.limit = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CQueryGoods message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CQueryGoods
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CQueryGoods} CQueryGoods
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CQueryGoods.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CQueryGoods message.
         * @function verify
         * @memberof game.CQueryGoods
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CQueryGoods.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                if (!$util.isString(message.itemType))
                    return "itemType: string expected";
            if (message.offset != null && message.hasOwnProperty("offset"))
                if (!$util.isInteger(message.offset))
                    return "offset: integer expected";
            if (message.limit != null && message.hasOwnProperty("limit"))
                if (!$util.isInteger(message.limit))
                    return "limit: integer expected";
            return null;
        };

        /**
         * Creates a CQueryGoods message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CQueryGoods
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CQueryGoods} CQueryGoods
         */
        CQueryGoods.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CQueryGoods)
                return object;
            var message = new $root.game.CQueryGoods();
            if (object.itemType != null)
                message.itemType = String(object.itemType);
            if (object.offset != null)
                message.offset = object.offset | 0;
            if (object.limit != null)
                message.limit = object.limit | 0;
            return message;
        };

        /**
         * Creates a plain object from a CQueryGoods message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CQueryGoods
         * @static
         * @param {game.CQueryGoods} message CQueryGoods
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CQueryGoods.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.itemType = "";
                object.offset = 0;
                object.limit = 0;
            }
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                object.itemType = message.itemType;
            if (message.offset != null && message.hasOwnProperty("offset"))
                object.offset = message.offset;
            if (message.limit != null && message.hasOwnProperty("limit"))
                object.limit = message.limit;
            return object;
        };

        /**
         * Converts this CQueryGoods to JSON.
         * @function toJSON
         * @memberof game.CQueryGoods
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CQueryGoods.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CQueryGoods
         * @function getTypeUrl
         * @memberof game.CQueryGoods
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CQueryGoods.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CQueryGoods";
        };

        return CQueryGoods;
    })();

    game.SQueryGoods = (function() {

        /**
         * Properties of a SQueryGoods.
         * @memberof game
         * @interface ISQueryGoods
         * @property {Array.<common.IGoodsInfo>|null} [goodsList] SQueryGoods goodsList
         */

        /**
         * Constructs a new SQueryGoods.
         * @memberof game
         * @classdesc Represents a SQueryGoods.
         * @implements ISQueryGoods
         * @constructor
         * @param {game.ISQueryGoods=} [properties] Properties to set
         */
        function SQueryGoods(properties) {
            this.goodsList = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SQueryGoods goodsList.
         * @member {Array.<common.IGoodsInfo>} goodsList
         * @memberof game.SQueryGoods
         * @instance
         */
        SQueryGoods.prototype.goodsList = $util.emptyArray;

        /**
         * Creates a new SQueryGoods instance using the specified properties.
         * @function create
         * @memberof game.SQueryGoods
         * @static
         * @param {game.ISQueryGoods=} [properties] Properties to set
         * @returns {game.SQueryGoods} SQueryGoods instance
         */
        SQueryGoods.create = function create(properties) {
            return new SQueryGoods(properties);
        };

        /**
         * Encodes the specified SQueryGoods message. Does not implicitly {@link game.SQueryGoods.verify|verify} messages.
         * @function encode
         * @memberof game.SQueryGoods
         * @static
         * @param {game.ISQueryGoods} message SQueryGoods message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SQueryGoods.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goodsList != null && message.goodsList.length)
                for (var i = 0; i < message.goodsList.length; ++i)
                    $root.common.GoodsInfo.encode(message.goodsList[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SQueryGoods message, length delimited. Does not implicitly {@link game.SQueryGoods.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SQueryGoods
         * @static
         * @param {game.ISQueryGoods} message SQueryGoods message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SQueryGoods.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SQueryGoods message from the specified reader or buffer.
         * @function decode
         * @memberof game.SQueryGoods
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SQueryGoods} SQueryGoods
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SQueryGoods.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SQueryGoods();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.goodsList && message.goodsList.length))
                            message.goodsList = [];
                        message.goodsList.push($root.common.GoodsInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SQueryGoods message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SQueryGoods
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SQueryGoods} SQueryGoods
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SQueryGoods.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SQueryGoods message.
         * @function verify
         * @memberof game.SQueryGoods
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SQueryGoods.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.goodsList != null && message.hasOwnProperty("goodsList")) {
                if (!Array.isArray(message.goodsList))
                    return "goodsList: array expected";
                for (var i = 0; i < message.goodsList.length; ++i) {
                    var error = $root.common.GoodsInfo.verify(message.goodsList[i]);
                    if (error)
                        return "goodsList." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SQueryGoods message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SQueryGoods
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SQueryGoods} SQueryGoods
         */
        SQueryGoods.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SQueryGoods)
                return object;
            var message = new $root.game.SQueryGoods();
            if (object.goodsList) {
                if (!Array.isArray(object.goodsList))
                    throw TypeError(".game.SQueryGoods.goodsList: array expected");
                message.goodsList = [];
                for (var i = 0; i < object.goodsList.length; ++i) {
                    if (typeof object.goodsList[i] !== "object")
                        throw TypeError(".game.SQueryGoods.goodsList: object expected");
                    message.goodsList[i] = $root.common.GoodsInfo.fromObject(object.goodsList[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SQueryGoods message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SQueryGoods
         * @static
         * @param {game.SQueryGoods} message SQueryGoods
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SQueryGoods.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.goodsList = [];
            if (message.goodsList && message.goodsList.length) {
                object.goodsList = [];
                for (var j = 0; j < message.goodsList.length; ++j)
                    object.goodsList[j] = $root.common.GoodsInfo.toObject(message.goodsList[j], options);
            }
            return object;
        };

        /**
         * Converts this SQueryGoods to JSON.
         * @function toJSON
         * @memberof game.SQueryGoods
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SQueryGoods.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SQueryGoods
         * @function getTypeUrl
         * @memberof game.SQueryGoods
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SQueryGoods.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SQueryGoods";
        };

        return SQueryGoods;
    })();

    game.CGetMySales = (function() {

        /**
         * Properties of a CGetMySales.
         * @memberof game
         * @interface ICGetMySales
         */

        /**
         * Constructs a new CGetMySales.
         * @memberof game
         * @classdesc Represents a CGetMySales.
         * @implements ICGetMySales
         * @constructor
         * @param {game.ICGetMySales=} [properties] Properties to set
         */
        function CGetMySales(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CGetMySales instance using the specified properties.
         * @function create
         * @memberof game.CGetMySales
         * @static
         * @param {game.ICGetMySales=} [properties] Properties to set
         * @returns {game.CGetMySales} CGetMySales instance
         */
        CGetMySales.create = function create(properties) {
            return new CGetMySales(properties);
        };

        /**
         * Encodes the specified CGetMySales message. Does not implicitly {@link game.CGetMySales.verify|verify} messages.
         * @function encode
         * @memberof game.CGetMySales
         * @static
         * @param {game.ICGetMySales} message CGetMySales message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetMySales.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CGetMySales message, length delimited. Does not implicitly {@link game.CGetMySales.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetMySales
         * @static
         * @param {game.ICGetMySales} message CGetMySales message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetMySales.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetMySales message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetMySales
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetMySales} CGetMySales
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetMySales.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetMySales();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetMySales message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetMySales
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetMySales} CGetMySales
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetMySales.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetMySales message.
         * @function verify
         * @memberof game.CGetMySales
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetMySales.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CGetMySales message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetMySales
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetMySales} CGetMySales
         */
        CGetMySales.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetMySales)
                return object;
            return new $root.game.CGetMySales();
        };

        /**
         * Creates a plain object from a CGetMySales message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetMySales
         * @static
         * @param {game.CGetMySales} message CGetMySales
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetMySales.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CGetMySales to JSON.
         * @function toJSON
         * @memberof game.CGetMySales
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetMySales.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetMySales
         * @function getTypeUrl
         * @memberof game.CGetMySales
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetMySales.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetMySales";
        };

        return CGetMySales;
    })();

    game.SGetMySales = (function() {

        /**
         * Properties of a SGetMySales.
         * @memberof game
         * @interface ISGetMySales
         * @property {Array.<common.ISaleItemInfo>|null} [sales] SGetMySales sales
         */

        /**
         * Constructs a new SGetMySales.
         * @memberof game
         * @classdesc Represents a SGetMySales.
         * @implements ISGetMySales
         * @constructor
         * @param {game.ISGetMySales=} [properties] Properties to set
         */
        function SGetMySales(properties) {
            this.sales = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGetMySales sales.
         * @member {Array.<common.ISaleItemInfo>} sales
         * @memberof game.SGetMySales
         * @instance
         */
        SGetMySales.prototype.sales = $util.emptyArray;

        /**
         * Creates a new SGetMySales instance using the specified properties.
         * @function create
         * @memberof game.SGetMySales
         * @static
         * @param {game.ISGetMySales=} [properties] Properties to set
         * @returns {game.SGetMySales} SGetMySales instance
         */
        SGetMySales.create = function create(properties) {
            return new SGetMySales(properties);
        };

        /**
         * Encodes the specified SGetMySales message. Does not implicitly {@link game.SGetMySales.verify|verify} messages.
         * @function encode
         * @memberof game.SGetMySales
         * @static
         * @param {game.ISGetMySales} message SGetMySales message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetMySales.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.sales != null && message.sales.length)
                for (var i = 0; i < message.sales.length; ++i)
                    $root.common.SaleItemInfo.encode(message.sales[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SGetMySales message, length delimited. Does not implicitly {@link game.SGetMySales.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetMySales
         * @static
         * @param {game.ISGetMySales} message SGetMySales message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetMySales.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetMySales message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetMySales
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetMySales} SGetMySales
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetMySales.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetMySales();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.sales && message.sales.length))
                            message.sales = [];
                        message.sales.push($root.common.SaleItemInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetMySales message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetMySales
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetMySales} SGetMySales
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetMySales.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetMySales message.
         * @function verify
         * @memberof game.SGetMySales
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetMySales.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.sales != null && message.hasOwnProperty("sales")) {
                if (!Array.isArray(message.sales))
                    return "sales: array expected";
                for (var i = 0; i < message.sales.length; ++i) {
                    var error = $root.common.SaleItemInfo.verify(message.sales[i]);
                    if (error)
                        return "sales." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SGetMySales message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetMySales
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetMySales} SGetMySales
         */
        SGetMySales.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetMySales)
                return object;
            var message = new $root.game.SGetMySales();
            if (object.sales) {
                if (!Array.isArray(object.sales))
                    throw TypeError(".game.SGetMySales.sales: array expected");
                message.sales = [];
                for (var i = 0; i < object.sales.length; ++i) {
                    if (typeof object.sales[i] !== "object")
                        throw TypeError(".game.SGetMySales.sales: object expected");
                    message.sales[i] = $root.common.SaleItemInfo.fromObject(object.sales[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SGetMySales message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetMySales
         * @static
         * @param {game.SGetMySales} message SGetMySales
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetMySales.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.sales = [];
            if (message.sales && message.sales.length) {
                object.sales = [];
                for (var j = 0; j < message.sales.length; ++j)
                    object.sales[j] = $root.common.SaleItemInfo.toObject(message.sales[j], options);
            }
            return object;
        };

        /**
         * Converts this SGetMySales to JSON.
         * @function toJSON
         * @memberof game.SGetMySales
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetMySales.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetMySales
         * @function getTypeUrl
         * @memberof game.SGetMySales
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetMySales.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetMySales";
        };

        return SGetMySales;
    })();

    game.CPurchaseItem = (function() {

        /**
         * Properties of a CPurchaseItem.
         * @memberof game
         * @interface ICPurchaseItem
         * @property {number|Long|null} [saleId] CPurchaseItem saleId
         * @property {number|null} [count] CPurchaseItem count
         */

        /**
         * Constructs a new CPurchaseItem.
         * @memberof game
         * @classdesc Represents a CPurchaseItem.
         * @implements ICPurchaseItem
         * @constructor
         * @param {game.ICPurchaseItem=} [properties] Properties to set
         */
        function CPurchaseItem(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CPurchaseItem saleId.
         * @member {number|Long} saleId
         * @memberof game.CPurchaseItem
         * @instance
         */
        CPurchaseItem.prototype.saleId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * CPurchaseItem count.
         * @member {number} count
         * @memberof game.CPurchaseItem
         * @instance
         */
        CPurchaseItem.prototype.count = 0;

        /**
         * Creates a new CPurchaseItem instance using the specified properties.
         * @function create
         * @memberof game.CPurchaseItem
         * @static
         * @param {game.ICPurchaseItem=} [properties] Properties to set
         * @returns {game.CPurchaseItem} CPurchaseItem instance
         */
        CPurchaseItem.create = function create(properties) {
            return new CPurchaseItem(properties);
        };

        /**
         * Encodes the specified CPurchaseItem message. Does not implicitly {@link game.CPurchaseItem.verify|verify} messages.
         * @function encode
         * @memberof game.CPurchaseItem
         * @static
         * @param {game.ICPurchaseItem} message CPurchaseItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CPurchaseItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.saleId != null && Object.hasOwnProperty.call(message, "saleId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.saleId);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.count);
            return writer;
        };

        /**
         * Encodes the specified CPurchaseItem message, length delimited. Does not implicitly {@link game.CPurchaseItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CPurchaseItem
         * @static
         * @param {game.ICPurchaseItem} message CPurchaseItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CPurchaseItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CPurchaseItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.CPurchaseItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CPurchaseItem} CPurchaseItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CPurchaseItem.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CPurchaseItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.saleId = reader.int64();
                        break;
                    }
                case 2: {
                        message.count = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CPurchaseItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CPurchaseItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CPurchaseItem} CPurchaseItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CPurchaseItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CPurchaseItem message.
         * @function verify
         * @memberof game.CPurchaseItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CPurchaseItem.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.saleId != null && message.hasOwnProperty("saleId"))
                if (!$util.isInteger(message.saleId) && !(message.saleId && $util.isInteger(message.saleId.low) && $util.isInteger(message.saleId.high)))
                    return "saleId: integer|Long expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        /**
         * Creates a CPurchaseItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CPurchaseItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CPurchaseItem} CPurchaseItem
         */
        CPurchaseItem.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CPurchaseItem)
                return object;
            var message = new $root.game.CPurchaseItem();
            if (object.saleId != null)
                if ($util.Long)
                    (message.saleId = $util.Long.fromValue(object.saleId)).unsigned = false;
                else if (typeof object.saleId === "string")
                    message.saleId = parseInt(object.saleId, 10);
                else if (typeof object.saleId === "number")
                    message.saleId = object.saleId;
                else if (typeof object.saleId === "object")
                    message.saleId = new $util.LongBits(object.saleId.low >>> 0, object.saleId.high >>> 0).toNumber();
            if (object.count != null)
                message.count = object.count | 0;
            return message;
        };

        /**
         * Creates a plain object from a CPurchaseItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CPurchaseItem
         * @static
         * @param {game.CPurchaseItem} message CPurchaseItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CPurchaseItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.saleId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.saleId = options.longs === String ? "0" : 0;
                object.count = 0;
            }
            if (message.saleId != null && message.hasOwnProperty("saleId"))
                if (typeof message.saleId === "number")
                    object.saleId = options.longs === String ? String(message.saleId) : message.saleId;
                else
                    object.saleId = options.longs === String ? $util.Long.prototype.toString.call(message.saleId) : options.longs === Number ? new $util.LongBits(message.saleId.low >>> 0, message.saleId.high >>> 0).toNumber() : message.saleId;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        /**
         * Converts this CPurchaseItem to JSON.
         * @function toJSON
         * @memberof game.CPurchaseItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CPurchaseItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CPurchaseItem
         * @function getTypeUrl
         * @memberof game.CPurchaseItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CPurchaseItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CPurchaseItem";
        };

        return CPurchaseItem;
    })();

    game.SPurchaseItem = (function() {

        /**
         * Properties of a SPurchaseItem.
         * @memberof game
         * @interface ISPurchaseItem
         * @property {common.IStorageItemInfo|null} [storageItem] SPurchaseItem storageItem
         */

        /**
         * Constructs a new SPurchaseItem.
         * @memberof game
         * @classdesc Represents a SPurchaseItem.
         * @implements ISPurchaseItem
         * @constructor
         * @param {game.ISPurchaseItem=} [properties] Properties to set
         */
        function SPurchaseItem(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SPurchaseItem storageItem.
         * @member {common.IStorageItemInfo|null|undefined} storageItem
         * @memberof game.SPurchaseItem
         * @instance
         */
        SPurchaseItem.prototype.storageItem = null;

        /**
         * Creates a new SPurchaseItem instance using the specified properties.
         * @function create
         * @memberof game.SPurchaseItem
         * @static
         * @param {game.ISPurchaseItem=} [properties] Properties to set
         * @returns {game.SPurchaseItem} SPurchaseItem instance
         */
        SPurchaseItem.create = function create(properties) {
            return new SPurchaseItem(properties);
        };

        /**
         * Encodes the specified SPurchaseItem message. Does not implicitly {@link game.SPurchaseItem.verify|verify} messages.
         * @function encode
         * @memberof game.SPurchaseItem
         * @static
         * @param {game.ISPurchaseItem} message SPurchaseItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SPurchaseItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.storageItem != null && Object.hasOwnProperty.call(message, "storageItem"))
                $root.common.StorageItemInfo.encode(message.storageItem, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SPurchaseItem message, length delimited. Does not implicitly {@link game.SPurchaseItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SPurchaseItem
         * @static
         * @param {game.ISPurchaseItem} message SPurchaseItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SPurchaseItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SPurchaseItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.SPurchaseItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SPurchaseItem} SPurchaseItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SPurchaseItem.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SPurchaseItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.storageItem = $root.common.StorageItemInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SPurchaseItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SPurchaseItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SPurchaseItem} SPurchaseItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SPurchaseItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SPurchaseItem message.
         * @function verify
         * @memberof game.SPurchaseItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SPurchaseItem.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.storageItem != null && message.hasOwnProperty("storageItem")) {
                var error = $root.common.StorageItemInfo.verify(message.storageItem);
                if (error)
                    return "storageItem." + error;
            }
            return null;
        };

        /**
         * Creates a SPurchaseItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SPurchaseItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SPurchaseItem} SPurchaseItem
         */
        SPurchaseItem.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SPurchaseItem)
                return object;
            var message = new $root.game.SPurchaseItem();
            if (object.storageItem != null) {
                if (typeof object.storageItem !== "object")
                    throw TypeError(".game.SPurchaseItem.storageItem: object expected");
                message.storageItem = $root.common.StorageItemInfo.fromObject(object.storageItem);
            }
            return message;
        };

        /**
         * Creates a plain object from a SPurchaseItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SPurchaseItem
         * @static
         * @param {game.SPurchaseItem} message SPurchaseItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SPurchaseItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.storageItem = null;
            if (message.storageItem != null && message.hasOwnProperty("storageItem"))
                object.storageItem = $root.common.StorageItemInfo.toObject(message.storageItem, options);
            return object;
        };

        /**
         * Converts this SPurchaseItem to JSON.
         * @function toJSON
         * @memberof game.SPurchaseItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SPurchaseItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SPurchaseItem
         * @function getTypeUrl
         * @memberof game.SPurchaseItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SPurchaseItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SPurchaseItem";
        };

        return SPurchaseItem;
    })();

    game.CGetRecords = (function() {

        /**
         * Properties of a CGetRecords.
         * @memberof game
         * @interface ICGetRecords
         */

        /**
         * Constructs a new CGetRecords.
         * @memberof game
         * @classdesc Represents a CGetRecords.
         * @implements ICGetRecords
         * @constructor
         * @param {game.ICGetRecords=} [properties] Properties to set
         */
        function CGetRecords(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CGetRecords instance using the specified properties.
         * @function create
         * @memberof game.CGetRecords
         * @static
         * @param {game.ICGetRecords=} [properties] Properties to set
         * @returns {game.CGetRecords} CGetRecords instance
         */
        CGetRecords.create = function create(properties) {
            return new CGetRecords(properties);
        };

        /**
         * Encodes the specified CGetRecords message. Does not implicitly {@link game.CGetRecords.verify|verify} messages.
         * @function encode
         * @memberof game.CGetRecords
         * @static
         * @param {game.ICGetRecords} message CGetRecords message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetRecords.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CGetRecords message, length delimited. Does not implicitly {@link game.CGetRecords.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetRecords
         * @static
         * @param {game.ICGetRecords} message CGetRecords message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetRecords.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetRecords message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetRecords
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetRecords} CGetRecords
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetRecords.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetRecords();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetRecords message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetRecords
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetRecords} CGetRecords
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetRecords.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetRecords message.
         * @function verify
         * @memberof game.CGetRecords
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetRecords.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CGetRecords message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetRecords
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetRecords} CGetRecords
         */
        CGetRecords.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetRecords)
                return object;
            return new $root.game.CGetRecords();
        };

        /**
         * Creates a plain object from a CGetRecords message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetRecords
         * @static
         * @param {game.CGetRecords} message CGetRecords
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetRecords.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CGetRecords to JSON.
         * @function toJSON
         * @memberof game.CGetRecords
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetRecords.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetRecords
         * @function getTypeUrl
         * @memberof game.CGetRecords
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetRecords.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetRecords";
        };

        return CGetRecords;
    })();

    game.SGetRecords = (function() {

        /**
         * Properties of a SGetRecords.
         * @memberof game
         * @interface ISGetRecords
         * @property {Array.<common.ISaleRecordInfo>|null} [records] SGetRecords records
         */

        /**
         * Constructs a new SGetRecords.
         * @memberof game
         * @classdesc Represents a SGetRecords.
         * @implements ISGetRecords
         * @constructor
         * @param {game.ISGetRecords=} [properties] Properties to set
         */
        function SGetRecords(properties) {
            this.records = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGetRecords records.
         * @member {Array.<common.ISaleRecordInfo>} records
         * @memberof game.SGetRecords
         * @instance
         */
        SGetRecords.prototype.records = $util.emptyArray;

        /**
         * Creates a new SGetRecords instance using the specified properties.
         * @function create
         * @memberof game.SGetRecords
         * @static
         * @param {game.ISGetRecords=} [properties] Properties to set
         * @returns {game.SGetRecords} SGetRecords instance
         */
        SGetRecords.create = function create(properties) {
            return new SGetRecords(properties);
        };

        /**
         * Encodes the specified SGetRecords message. Does not implicitly {@link game.SGetRecords.verify|verify} messages.
         * @function encode
         * @memberof game.SGetRecords
         * @static
         * @param {game.ISGetRecords} message SGetRecords message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetRecords.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.records != null && message.records.length)
                for (var i = 0; i < message.records.length; ++i)
                    $root.common.SaleRecordInfo.encode(message.records[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SGetRecords message, length delimited. Does not implicitly {@link game.SGetRecords.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetRecords
         * @static
         * @param {game.ISGetRecords} message SGetRecords message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetRecords.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetRecords message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetRecords
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetRecords} SGetRecords
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetRecords.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetRecords();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.records && message.records.length))
                            message.records = [];
                        message.records.push($root.common.SaleRecordInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetRecords message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetRecords
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetRecords} SGetRecords
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetRecords.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetRecords message.
         * @function verify
         * @memberof game.SGetRecords
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetRecords.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.records != null && message.hasOwnProperty("records")) {
                if (!Array.isArray(message.records))
                    return "records: array expected";
                for (var i = 0; i < message.records.length; ++i) {
                    var error = $root.common.SaleRecordInfo.verify(message.records[i]);
                    if (error)
                        return "records." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SGetRecords message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetRecords
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetRecords} SGetRecords
         */
        SGetRecords.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetRecords)
                return object;
            var message = new $root.game.SGetRecords();
            if (object.records) {
                if (!Array.isArray(object.records))
                    throw TypeError(".game.SGetRecords.records: array expected");
                message.records = [];
                for (var i = 0; i < object.records.length; ++i) {
                    if (typeof object.records[i] !== "object")
                        throw TypeError(".game.SGetRecords.records: object expected");
                    message.records[i] = $root.common.SaleRecordInfo.fromObject(object.records[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SGetRecords message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetRecords
         * @static
         * @param {game.SGetRecords} message SGetRecords
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetRecords.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.records = [];
            if (message.records && message.records.length) {
                object.records = [];
                for (var j = 0; j < message.records.length; ++j)
                    object.records[j] = $root.common.SaleRecordInfo.toObject(message.records[j], options);
            }
            return object;
        };

        /**
         * Converts this SGetRecords to JSON.
         * @function toJSON
         * @memberof game.SGetRecords
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetRecords.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetRecords
         * @function getTypeUrl
         * @memberof game.SGetRecords
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetRecords.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetRecords";
        };

        return SGetRecords;
    })();

    game.CMarkSale = (function() {

        /**
         * Properties of a CMarkSale.
         * @memberof game
         * @interface ICMarkSale
         * @property {number|Long|null} [saleId] CMarkSale saleId
         * @property {boolean|null} [cancel] CMarkSale cancel
         */

        /**
         * Constructs a new CMarkSale.
         * @memberof game
         * @classdesc Represents a CMarkSale.
         * @implements ICMarkSale
         * @constructor
         * @param {game.ICMarkSale=} [properties] Properties to set
         */
        function CMarkSale(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CMarkSale saleId.
         * @member {number|Long} saleId
         * @memberof game.CMarkSale
         * @instance
         */
        CMarkSale.prototype.saleId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * CMarkSale cancel.
         * @member {boolean} cancel
         * @memberof game.CMarkSale
         * @instance
         */
        CMarkSale.prototype.cancel = false;

        /**
         * Creates a new CMarkSale instance using the specified properties.
         * @function create
         * @memberof game.CMarkSale
         * @static
         * @param {game.ICMarkSale=} [properties] Properties to set
         * @returns {game.CMarkSale} CMarkSale instance
         */
        CMarkSale.create = function create(properties) {
            return new CMarkSale(properties);
        };

        /**
         * Encodes the specified CMarkSale message. Does not implicitly {@link game.CMarkSale.verify|verify} messages.
         * @function encode
         * @memberof game.CMarkSale
         * @static
         * @param {game.ICMarkSale} message CMarkSale message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CMarkSale.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.saleId != null && Object.hasOwnProperty.call(message, "saleId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.saleId);
            if (message.cancel != null && Object.hasOwnProperty.call(message, "cancel"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.cancel);
            return writer;
        };

        /**
         * Encodes the specified CMarkSale message, length delimited. Does not implicitly {@link game.CMarkSale.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CMarkSale
         * @static
         * @param {game.ICMarkSale} message CMarkSale message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CMarkSale.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CMarkSale message from the specified reader or buffer.
         * @function decode
         * @memberof game.CMarkSale
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CMarkSale} CMarkSale
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CMarkSale.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CMarkSale();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.saleId = reader.int64();
                        break;
                    }
                case 2: {
                        message.cancel = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CMarkSale message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CMarkSale
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CMarkSale} CMarkSale
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CMarkSale.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CMarkSale message.
         * @function verify
         * @memberof game.CMarkSale
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CMarkSale.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.saleId != null && message.hasOwnProperty("saleId"))
                if (!$util.isInteger(message.saleId) && !(message.saleId && $util.isInteger(message.saleId.low) && $util.isInteger(message.saleId.high)))
                    return "saleId: integer|Long expected";
            if (message.cancel != null && message.hasOwnProperty("cancel"))
                if (typeof message.cancel !== "boolean")
                    return "cancel: boolean expected";
            return null;
        };

        /**
         * Creates a CMarkSale message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CMarkSale
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CMarkSale} CMarkSale
         */
        CMarkSale.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CMarkSale)
                return object;
            var message = new $root.game.CMarkSale();
            if (object.saleId != null)
                if ($util.Long)
                    (message.saleId = $util.Long.fromValue(object.saleId)).unsigned = false;
                else if (typeof object.saleId === "string")
                    message.saleId = parseInt(object.saleId, 10);
                else if (typeof object.saleId === "number")
                    message.saleId = object.saleId;
                else if (typeof object.saleId === "object")
                    message.saleId = new $util.LongBits(object.saleId.low >>> 0, object.saleId.high >>> 0).toNumber();
            if (object.cancel != null)
                message.cancel = Boolean(object.cancel);
            return message;
        };

        /**
         * Creates a plain object from a CMarkSale message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CMarkSale
         * @static
         * @param {game.CMarkSale} message CMarkSale
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CMarkSale.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.saleId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.saleId = options.longs === String ? "0" : 0;
                object.cancel = false;
            }
            if (message.saleId != null && message.hasOwnProperty("saleId"))
                if (typeof message.saleId === "number")
                    object.saleId = options.longs === String ? String(message.saleId) : message.saleId;
                else
                    object.saleId = options.longs === String ? $util.Long.prototype.toString.call(message.saleId) : options.longs === Number ? new $util.LongBits(message.saleId.low >>> 0, message.saleId.high >>> 0).toNumber() : message.saleId;
            if (message.cancel != null && message.hasOwnProperty("cancel"))
                object.cancel = message.cancel;
            return object;
        };

        /**
         * Converts this CMarkSale to JSON.
         * @function toJSON
         * @memberof game.CMarkSale
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CMarkSale.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CMarkSale
         * @function getTypeUrl
         * @memberof game.CMarkSale
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CMarkSale.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CMarkSale";
        };

        return CMarkSale;
    })();

    game.SMarkSale = (function() {

        /**
         * Properties of a SMarkSale.
         * @memberof game
         * @interface ISMarkSale
         * @property {number|Long|null} [saleId] SMarkSale saleId
         */

        /**
         * Constructs a new SMarkSale.
         * @memberof game
         * @classdesc Represents a SMarkSale.
         * @implements ISMarkSale
         * @constructor
         * @param {game.ISMarkSale=} [properties] Properties to set
         */
        function SMarkSale(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SMarkSale saleId.
         * @member {number|Long} saleId
         * @memberof game.SMarkSale
         * @instance
         */
        SMarkSale.prototype.saleId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new SMarkSale instance using the specified properties.
         * @function create
         * @memberof game.SMarkSale
         * @static
         * @param {game.ISMarkSale=} [properties] Properties to set
         * @returns {game.SMarkSale} SMarkSale instance
         */
        SMarkSale.create = function create(properties) {
            return new SMarkSale(properties);
        };

        /**
         * Encodes the specified SMarkSale message. Does not implicitly {@link game.SMarkSale.verify|verify} messages.
         * @function encode
         * @memberof game.SMarkSale
         * @static
         * @param {game.ISMarkSale} message SMarkSale message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SMarkSale.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.saleId != null && Object.hasOwnProperty.call(message, "saleId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.saleId);
            return writer;
        };

        /**
         * Encodes the specified SMarkSale message, length delimited. Does not implicitly {@link game.SMarkSale.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SMarkSale
         * @static
         * @param {game.ISMarkSale} message SMarkSale message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SMarkSale.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SMarkSale message from the specified reader or buffer.
         * @function decode
         * @memberof game.SMarkSale
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SMarkSale} SMarkSale
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SMarkSale.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SMarkSale();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.saleId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SMarkSale message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SMarkSale
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SMarkSale} SMarkSale
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SMarkSale.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SMarkSale message.
         * @function verify
         * @memberof game.SMarkSale
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SMarkSale.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.saleId != null && message.hasOwnProperty("saleId"))
                if (!$util.isInteger(message.saleId) && !(message.saleId && $util.isInteger(message.saleId.low) && $util.isInteger(message.saleId.high)))
                    return "saleId: integer|Long expected";
            return null;
        };

        /**
         * Creates a SMarkSale message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SMarkSale
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SMarkSale} SMarkSale
         */
        SMarkSale.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SMarkSale)
                return object;
            var message = new $root.game.SMarkSale();
            if (object.saleId != null)
                if ($util.Long)
                    (message.saleId = $util.Long.fromValue(object.saleId)).unsigned = false;
                else if (typeof object.saleId === "string")
                    message.saleId = parseInt(object.saleId, 10);
                else if (typeof object.saleId === "number")
                    message.saleId = object.saleId;
                else if (typeof object.saleId === "object")
                    message.saleId = new $util.LongBits(object.saleId.low >>> 0, object.saleId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a SMarkSale message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SMarkSale
         * @static
         * @param {game.SMarkSale} message SMarkSale
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SMarkSale.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.saleId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.saleId = options.longs === String ? "0" : 0;
            if (message.saleId != null && message.hasOwnProperty("saleId"))
                if (typeof message.saleId === "number")
                    object.saleId = options.longs === String ? String(message.saleId) : message.saleId;
                else
                    object.saleId = options.longs === String ? $util.Long.prototype.toString.call(message.saleId) : options.longs === Number ? new $util.LongBits(message.saleId.low >>> 0, message.saleId.high >>> 0).toNumber() : message.saleId;
            return object;
        };

        /**
         * Converts this SMarkSale to JSON.
         * @function toJSON
         * @memberof game.SMarkSale
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SMarkSale.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SMarkSale
         * @function getTypeUrl
         * @memberof game.SMarkSale
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SMarkSale.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SMarkSale";
        };

        return SMarkSale;
    })();

    game.CGetMyMarkedSales = (function() {

        /**
         * Properties of a CGetMyMarkedSales.
         * @memberof game
         * @interface ICGetMyMarkedSales
         */

        /**
         * Constructs a new CGetMyMarkedSales.
         * @memberof game
         * @classdesc Represents a CGetMyMarkedSales.
         * @implements ICGetMyMarkedSales
         * @constructor
         * @param {game.ICGetMyMarkedSales=} [properties] Properties to set
         */
        function CGetMyMarkedSales(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new CGetMyMarkedSales instance using the specified properties.
         * @function create
         * @memberof game.CGetMyMarkedSales
         * @static
         * @param {game.ICGetMyMarkedSales=} [properties] Properties to set
         * @returns {game.CGetMyMarkedSales} CGetMyMarkedSales instance
         */
        CGetMyMarkedSales.create = function create(properties) {
            return new CGetMyMarkedSales(properties);
        };

        /**
         * Encodes the specified CGetMyMarkedSales message. Does not implicitly {@link game.CGetMyMarkedSales.verify|verify} messages.
         * @function encode
         * @memberof game.CGetMyMarkedSales
         * @static
         * @param {game.ICGetMyMarkedSales} message CGetMyMarkedSales message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetMyMarkedSales.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified CGetMyMarkedSales message, length delimited. Does not implicitly {@link game.CGetMyMarkedSales.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetMyMarkedSales
         * @static
         * @param {game.ICGetMyMarkedSales} message CGetMyMarkedSales message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetMyMarkedSales.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetMyMarkedSales message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetMyMarkedSales
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetMyMarkedSales} CGetMyMarkedSales
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetMyMarkedSales.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetMyMarkedSales();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetMyMarkedSales message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetMyMarkedSales
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetMyMarkedSales} CGetMyMarkedSales
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetMyMarkedSales.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetMyMarkedSales message.
         * @function verify
         * @memberof game.CGetMyMarkedSales
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetMyMarkedSales.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a CGetMyMarkedSales message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetMyMarkedSales
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetMyMarkedSales} CGetMyMarkedSales
         */
        CGetMyMarkedSales.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetMyMarkedSales)
                return object;
            return new $root.game.CGetMyMarkedSales();
        };

        /**
         * Creates a plain object from a CGetMyMarkedSales message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetMyMarkedSales
         * @static
         * @param {game.CGetMyMarkedSales} message CGetMyMarkedSales
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetMyMarkedSales.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this CGetMyMarkedSales to JSON.
         * @function toJSON
         * @memberof game.CGetMyMarkedSales
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetMyMarkedSales.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetMyMarkedSales
         * @function getTypeUrl
         * @memberof game.CGetMyMarkedSales
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetMyMarkedSales.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetMyMarkedSales";
        };

        return CGetMyMarkedSales;
    })();

    game.SGetMyMarkedSales = (function() {

        /**
         * Properties of a SGetMyMarkedSales.
         * @memberof game
         * @interface ISGetMyMarkedSales
         * @property {Array.<common.ISaleItemInfo>|null} [sales] SGetMyMarkedSales sales
         */

        /**
         * Constructs a new SGetMyMarkedSales.
         * @memberof game
         * @classdesc Represents a SGetMyMarkedSales.
         * @implements ISGetMyMarkedSales
         * @constructor
         * @param {game.ISGetMyMarkedSales=} [properties] Properties to set
         */
        function SGetMyMarkedSales(properties) {
            this.sales = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGetMyMarkedSales sales.
         * @member {Array.<common.ISaleItemInfo>} sales
         * @memberof game.SGetMyMarkedSales
         * @instance
         */
        SGetMyMarkedSales.prototype.sales = $util.emptyArray;

        /**
         * Creates a new SGetMyMarkedSales instance using the specified properties.
         * @function create
         * @memberof game.SGetMyMarkedSales
         * @static
         * @param {game.ISGetMyMarkedSales=} [properties] Properties to set
         * @returns {game.SGetMyMarkedSales} SGetMyMarkedSales instance
         */
        SGetMyMarkedSales.create = function create(properties) {
            return new SGetMyMarkedSales(properties);
        };

        /**
         * Encodes the specified SGetMyMarkedSales message. Does not implicitly {@link game.SGetMyMarkedSales.verify|verify} messages.
         * @function encode
         * @memberof game.SGetMyMarkedSales
         * @static
         * @param {game.ISGetMyMarkedSales} message SGetMyMarkedSales message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetMyMarkedSales.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.sales != null && message.sales.length)
                for (var i = 0; i < message.sales.length; ++i)
                    $root.common.SaleItemInfo.encode(message.sales[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SGetMyMarkedSales message, length delimited. Does not implicitly {@link game.SGetMyMarkedSales.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetMyMarkedSales
         * @static
         * @param {game.ISGetMyMarkedSales} message SGetMyMarkedSales message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetMyMarkedSales.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetMyMarkedSales message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetMyMarkedSales
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetMyMarkedSales} SGetMyMarkedSales
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetMyMarkedSales.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetMyMarkedSales();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.sales && message.sales.length))
                            message.sales = [];
                        message.sales.push($root.common.SaleItemInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetMyMarkedSales message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetMyMarkedSales
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetMyMarkedSales} SGetMyMarkedSales
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetMyMarkedSales.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetMyMarkedSales message.
         * @function verify
         * @memberof game.SGetMyMarkedSales
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetMyMarkedSales.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.sales != null && message.hasOwnProperty("sales")) {
                if (!Array.isArray(message.sales))
                    return "sales: array expected";
                for (var i = 0; i < message.sales.length; ++i) {
                    var error = $root.common.SaleItemInfo.verify(message.sales[i]);
                    if (error)
                        return "sales." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SGetMyMarkedSales message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetMyMarkedSales
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetMyMarkedSales} SGetMyMarkedSales
         */
        SGetMyMarkedSales.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetMyMarkedSales)
                return object;
            var message = new $root.game.SGetMyMarkedSales();
            if (object.sales) {
                if (!Array.isArray(object.sales))
                    throw TypeError(".game.SGetMyMarkedSales.sales: array expected");
                message.sales = [];
                for (var i = 0; i < object.sales.length; ++i) {
                    if (typeof object.sales[i] !== "object")
                        throw TypeError(".game.SGetMyMarkedSales.sales: object expected");
                    message.sales[i] = $root.common.SaleItemInfo.fromObject(object.sales[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SGetMyMarkedSales message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetMyMarkedSales
         * @static
         * @param {game.SGetMyMarkedSales} message SGetMyMarkedSales
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetMyMarkedSales.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.sales = [];
            if (message.sales && message.sales.length) {
                object.sales = [];
                for (var j = 0; j < message.sales.length; ++j)
                    object.sales[j] = $root.common.SaleItemInfo.toObject(message.sales[j], options);
            }
            return object;
        };

        /**
         * Converts this SGetMyMarkedSales to JSON.
         * @function toJSON
         * @memberof game.SGetMyMarkedSales
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetMyMarkedSales.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetMyMarkedSales
         * @function getTypeUrl
         * @memberof game.SGetMyMarkedSales
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetMyMarkedSales.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetMyMarkedSales";
        };

        return SGetMyMarkedSales;
    })();

    game.CMarkGoods = (function() {

        /**
         * Properties of a CMarkGoods.
         * @memberof game
         * @interface ICMarkGoods
         * @property {string|null} [itemType] CMarkGoods itemType
         * @property {number|null} [itemId] CMarkGoods itemId
         * @property {boolean|null} [cancel] CMarkGoods cancel
         */

        /**
         * Constructs a new CMarkGoods.
         * @memberof game
         * @classdesc Represents a CMarkGoods.
         * @implements ICMarkGoods
         * @constructor
         * @param {game.ICMarkGoods=} [properties] Properties to set
         */
        function CMarkGoods(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CMarkGoods itemType.
         * @member {string} itemType
         * @memberof game.CMarkGoods
         * @instance
         */
        CMarkGoods.prototype.itemType = "";

        /**
         * CMarkGoods itemId.
         * @member {number} itemId
         * @memberof game.CMarkGoods
         * @instance
         */
        CMarkGoods.prototype.itemId = 0;

        /**
         * CMarkGoods cancel.
         * @member {boolean} cancel
         * @memberof game.CMarkGoods
         * @instance
         */
        CMarkGoods.prototype.cancel = false;

        /**
         * Creates a new CMarkGoods instance using the specified properties.
         * @function create
         * @memberof game.CMarkGoods
         * @static
         * @param {game.ICMarkGoods=} [properties] Properties to set
         * @returns {game.CMarkGoods} CMarkGoods instance
         */
        CMarkGoods.create = function create(properties) {
            return new CMarkGoods(properties);
        };

        /**
         * Encodes the specified CMarkGoods message. Does not implicitly {@link game.CMarkGoods.verify|verify} messages.
         * @function encode
         * @memberof game.CMarkGoods
         * @static
         * @param {game.ICMarkGoods} message CMarkGoods message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CMarkGoods.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.itemType != null && Object.hasOwnProperty.call(message, "itemType"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.itemType);
            if (message.itemId != null && Object.hasOwnProperty.call(message, "itemId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.itemId);
            if (message.cancel != null && Object.hasOwnProperty.call(message, "cancel"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.cancel);
            return writer;
        };

        /**
         * Encodes the specified CMarkGoods message, length delimited. Does not implicitly {@link game.CMarkGoods.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CMarkGoods
         * @static
         * @param {game.ICMarkGoods} message CMarkGoods message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CMarkGoods.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CMarkGoods message from the specified reader or buffer.
         * @function decode
         * @memberof game.CMarkGoods
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CMarkGoods} CMarkGoods
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CMarkGoods.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CMarkGoods();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.itemType = reader.string();
                        break;
                    }
                case 2: {
                        message.itemId = reader.int32();
                        break;
                    }
                case 3: {
                        message.cancel = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CMarkGoods message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CMarkGoods
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CMarkGoods} CMarkGoods
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CMarkGoods.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CMarkGoods message.
         * @function verify
         * @memberof game.CMarkGoods
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CMarkGoods.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                if (!$util.isString(message.itemType))
                    return "itemType: string expected";
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                if (!$util.isInteger(message.itemId))
                    return "itemId: integer expected";
            if (message.cancel != null && message.hasOwnProperty("cancel"))
                if (typeof message.cancel !== "boolean")
                    return "cancel: boolean expected";
            return null;
        };

        /**
         * Creates a CMarkGoods message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CMarkGoods
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CMarkGoods} CMarkGoods
         */
        CMarkGoods.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CMarkGoods)
                return object;
            var message = new $root.game.CMarkGoods();
            if (object.itemType != null)
                message.itemType = String(object.itemType);
            if (object.itemId != null)
                message.itemId = object.itemId | 0;
            if (object.cancel != null)
                message.cancel = Boolean(object.cancel);
            return message;
        };

        /**
         * Creates a plain object from a CMarkGoods message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CMarkGoods
         * @static
         * @param {game.CMarkGoods} message CMarkGoods
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CMarkGoods.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.itemType = "";
                object.itemId = 0;
                object.cancel = false;
            }
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                object.itemType = message.itemType;
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                object.itemId = message.itemId;
            if (message.cancel != null && message.hasOwnProperty("cancel"))
                object.cancel = message.cancel;
            return object;
        };

        /**
         * Converts this CMarkGoods to JSON.
         * @function toJSON
         * @memberof game.CMarkGoods
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CMarkGoods.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CMarkGoods
         * @function getTypeUrl
         * @memberof game.CMarkGoods
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CMarkGoods.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CMarkGoods";
        };

        return CMarkGoods;
    })();

    game.SMarkGoods = (function() {

        /**
         * Properties of a SMarkGoods.
         * @memberof game
         * @interface ISMarkGoods
         * @property {string|null} [goodsKey] SMarkGoods goodsKey
         */

        /**
         * Constructs a new SMarkGoods.
         * @memberof game
         * @classdesc Represents a SMarkGoods.
         * @implements ISMarkGoods
         * @constructor
         * @param {game.ISMarkGoods=} [properties] Properties to set
         */
        function SMarkGoods(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SMarkGoods goodsKey.
         * @member {string} goodsKey
         * @memberof game.SMarkGoods
         * @instance
         */
        SMarkGoods.prototype.goodsKey = "";

        /**
         * Creates a new SMarkGoods instance using the specified properties.
         * @function create
         * @memberof game.SMarkGoods
         * @static
         * @param {game.ISMarkGoods=} [properties] Properties to set
         * @returns {game.SMarkGoods} SMarkGoods instance
         */
        SMarkGoods.create = function create(properties) {
            return new SMarkGoods(properties);
        };

        /**
         * Encodes the specified SMarkGoods message. Does not implicitly {@link game.SMarkGoods.verify|verify} messages.
         * @function encode
         * @memberof game.SMarkGoods
         * @static
         * @param {game.ISMarkGoods} message SMarkGoods message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SMarkGoods.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goodsKey != null && Object.hasOwnProperty.call(message, "goodsKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.goodsKey);
            return writer;
        };

        /**
         * Encodes the specified SMarkGoods message, length delimited. Does not implicitly {@link game.SMarkGoods.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SMarkGoods
         * @static
         * @param {game.ISMarkGoods} message SMarkGoods message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SMarkGoods.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SMarkGoods message from the specified reader or buffer.
         * @function decode
         * @memberof game.SMarkGoods
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SMarkGoods} SMarkGoods
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SMarkGoods.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SMarkGoods();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.goodsKey = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SMarkGoods message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SMarkGoods
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SMarkGoods} SMarkGoods
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SMarkGoods.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SMarkGoods message.
         * @function verify
         * @memberof game.SMarkGoods
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SMarkGoods.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.goodsKey != null && message.hasOwnProperty("goodsKey"))
                if (!$util.isString(message.goodsKey))
                    return "goodsKey: string expected";
            return null;
        };

        /**
         * Creates a SMarkGoods message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SMarkGoods
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SMarkGoods} SMarkGoods
         */
        SMarkGoods.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SMarkGoods)
                return object;
            var message = new $root.game.SMarkGoods();
            if (object.goodsKey != null)
                message.goodsKey = String(object.goodsKey);
            return message;
        };

        /**
         * Creates a plain object from a SMarkGoods message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SMarkGoods
         * @static
         * @param {game.SMarkGoods} message SMarkGoods
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SMarkGoods.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.goodsKey = "";
            if (message.goodsKey != null && message.hasOwnProperty("goodsKey"))
                object.goodsKey = message.goodsKey;
            return object;
        };

        /**
         * Converts this SMarkGoods to JSON.
         * @function toJSON
         * @memberof game.SMarkGoods
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SMarkGoods.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SMarkGoods
         * @function getTypeUrl
         * @memberof game.SMarkGoods
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SMarkGoods.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SMarkGoods";
        };

        return SMarkGoods;
    })();

    game.CGetMyMarkedGoods = (function() {

        /**
         * Properties of a CGetMyMarkedGoods.
         * @memberof game
         * @interface ICGetMyMarkedGoods
         * @property {Array.<string>|null} [goodsKey] CGetMyMarkedGoods goodsKey
         */

        /**
         * Constructs a new CGetMyMarkedGoods.
         * @memberof game
         * @classdesc Represents a CGetMyMarkedGoods.
         * @implements ICGetMyMarkedGoods
         * @constructor
         * @param {game.ICGetMyMarkedGoods=} [properties] Properties to set
         */
        function CGetMyMarkedGoods(properties) {
            this.goodsKey = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CGetMyMarkedGoods goodsKey.
         * @member {Array.<string>} goodsKey
         * @memberof game.CGetMyMarkedGoods
         * @instance
         */
        CGetMyMarkedGoods.prototype.goodsKey = $util.emptyArray;

        /**
         * Creates a new CGetMyMarkedGoods instance using the specified properties.
         * @function create
         * @memberof game.CGetMyMarkedGoods
         * @static
         * @param {game.ICGetMyMarkedGoods=} [properties] Properties to set
         * @returns {game.CGetMyMarkedGoods} CGetMyMarkedGoods instance
         */
        CGetMyMarkedGoods.create = function create(properties) {
            return new CGetMyMarkedGoods(properties);
        };

        /**
         * Encodes the specified CGetMyMarkedGoods message. Does not implicitly {@link game.CGetMyMarkedGoods.verify|verify} messages.
         * @function encode
         * @memberof game.CGetMyMarkedGoods
         * @static
         * @param {game.ICGetMyMarkedGoods} message CGetMyMarkedGoods message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetMyMarkedGoods.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goodsKey != null && message.goodsKey.length)
                for (var i = 0; i < message.goodsKey.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.goodsKey[i]);
            return writer;
        };

        /**
         * Encodes the specified CGetMyMarkedGoods message, length delimited. Does not implicitly {@link game.CGetMyMarkedGoods.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetMyMarkedGoods
         * @static
         * @param {game.ICGetMyMarkedGoods} message CGetMyMarkedGoods message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetMyMarkedGoods.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetMyMarkedGoods message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetMyMarkedGoods
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetMyMarkedGoods} CGetMyMarkedGoods
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetMyMarkedGoods.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetMyMarkedGoods();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.goodsKey && message.goodsKey.length))
                            message.goodsKey = [];
                        message.goodsKey.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetMyMarkedGoods message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetMyMarkedGoods
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetMyMarkedGoods} CGetMyMarkedGoods
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetMyMarkedGoods.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetMyMarkedGoods message.
         * @function verify
         * @memberof game.CGetMyMarkedGoods
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetMyMarkedGoods.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.goodsKey != null && message.hasOwnProperty("goodsKey")) {
                if (!Array.isArray(message.goodsKey))
                    return "goodsKey: array expected";
                for (var i = 0; i < message.goodsKey.length; ++i)
                    if (!$util.isString(message.goodsKey[i]))
                        return "goodsKey: string[] expected";
            }
            return null;
        };

        /**
         * Creates a CGetMyMarkedGoods message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetMyMarkedGoods
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetMyMarkedGoods} CGetMyMarkedGoods
         */
        CGetMyMarkedGoods.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetMyMarkedGoods)
                return object;
            var message = new $root.game.CGetMyMarkedGoods();
            if (object.goodsKey) {
                if (!Array.isArray(object.goodsKey))
                    throw TypeError(".game.CGetMyMarkedGoods.goodsKey: array expected");
                message.goodsKey = [];
                for (var i = 0; i < object.goodsKey.length; ++i)
                    message.goodsKey[i] = String(object.goodsKey[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a CGetMyMarkedGoods message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetMyMarkedGoods
         * @static
         * @param {game.CGetMyMarkedGoods} message CGetMyMarkedGoods
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetMyMarkedGoods.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.goodsKey = [];
            if (message.goodsKey && message.goodsKey.length) {
                object.goodsKey = [];
                for (var j = 0; j < message.goodsKey.length; ++j)
                    object.goodsKey[j] = message.goodsKey[j];
            }
            return object;
        };

        /**
         * Converts this CGetMyMarkedGoods to JSON.
         * @function toJSON
         * @memberof game.CGetMyMarkedGoods
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetMyMarkedGoods.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetMyMarkedGoods
         * @function getTypeUrl
         * @memberof game.CGetMyMarkedGoods
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetMyMarkedGoods.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetMyMarkedGoods";
        };

        return CGetMyMarkedGoods;
    })();

    game.SGetMyMarkedGoods = (function() {

        /**
         * Properties of a SGetMyMarkedGoods.
         * @memberof game
         * @interface ISGetMyMarkedGoods
         * @property {Array.<common.IGoodsInfo>|null} [goodsList] SGetMyMarkedGoods goodsList
         */

        /**
         * Constructs a new SGetMyMarkedGoods.
         * @memberof game
         * @classdesc Represents a SGetMyMarkedGoods.
         * @implements ISGetMyMarkedGoods
         * @constructor
         * @param {game.ISGetMyMarkedGoods=} [properties] Properties to set
         */
        function SGetMyMarkedGoods(properties) {
            this.goodsList = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGetMyMarkedGoods goodsList.
         * @member {Array.<common.IGoodsInfo>} goodsList
         * @memberof game.SGetMyMarkedGoods
         * @instance
         */
        SGetMyMarkedGoods.prototype.goodsList = $util.emptyArray;

        /**
         * Creates a new SGetMyMarkedGoods instance using the specified properties.
         * @function create
         * @memberof game.SGetMyMarkedGoods
         * @static
         * @param {game.ISGetMyMarkedGoods=} [properties] Properties to set
         * @returns {game.SGetMyMarkedGoods} SGetMyMarkedGoods instance
         */
        SGetMyMarkedGoods.create = function create(properties) {
            return new SGetMyMarkedGoods(properties);
        };

        /**
         * Encodes the specified SGetMyMarkedGoods message. Does not implicitly {@link game.SGetMyMarkedGoods.verify|verify} messages.
         * @function encode
         * @memberof game.SGetMyMarkedGoods
         * @static
         * @param {game.ISGetMyMarkedGoods} message SGetMyMarkedGoods message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetMyMarkedGoods.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goodsList != null && message.goodsList.length)
                for (var i = 0; i < message.goodsList.length; ++i)
                    $root.common.GoodsInfo.encode(message.goodsList[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SGetMyMarkedGoods message, length delimited. Does not implicitly {@link game.SGetMyMarkedGoods.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetMyMarkedGoods
         * @static
         * @param {game.ISGetMyMarkedGoods} message SGetMyMarkedGoods message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetMyMarkedGoods.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetMyMarkedGoods message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetMyMarkedGoods
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetMyMarkedGoods} SGetMyMarkedGoods
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetMyMarkedGoods.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetMyMarkedGoods();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.goodsList && message.goodsList.length))
                            message.goodsList = [];
                        message.goodsList.push($root.common.GoodsInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetMyMarkedGoods message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetMyMarkedGoods
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetMyMarkedGoods} SGetMyMarkedGoods
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetMyMarkedGoods.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetMyMarkedGoods message.
         * @function verify
         * @memberof game.SGetMyMarkedGoods
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetMyMarkedGoods.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.goodsList != null && message.hasOwnProperty("goodsList")) {
                if (!Array.isArray(message.goodsList))
                    return "goodsList: array expected";
                for (var i = 0; i < message.goodsList.length; ++i) {
                    var error = $root.common.GoodsInfo.verify(message.goodsList[i]);
                    if (error)
                        return "goodsList." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SGetMyMarkedGoods message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetMyMarkedGoods
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetMyMarkedGoods} SGetMyMarkedGoods
         */
        SGetMyMarkedGoods.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetMyMarkedGoods)
                return object;
            var message = new $root.game.SGetMyMarkedGoods();
            if (object.goodsList) {
                if (!Array.isArray(object.goodsList))
                    throw TypeError(".game.SGetMyMarkedGoods.goodsList: array expected");
                message.goodsList = [];
                for (var i = 0; i < object.goodsList.length; ++i) {
                    if (typeof object.goodsList[i] !== "object")
                        throw TypeError(".game.SGetMyMarkedGoods.goodsList: object expected");
                    message.goodsList[i] = $root.common.GoodsInfo.fromObject(object.goodsList[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SGetMyMarkedGoods message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetMyMarkedGoods
         * @static
         * @param {game.SGetMyMarkedGoods} message SGetMyMarkedGoods
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetMyMarkedGoods.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.goodsList = [];
            if (message.goodsList && message.goodsList.length) {
                object.goodsList = [];
                for (var j = 0; j < message.goodsList.length; ++j)
                    object.goodsList[j] = $root.common.GoodsInfo.toObject(message.goodsList[j], options);
            }
            return object;
        };

        /**
         * Converts this SGetMyMarkedGoods to JSON.
         * @function toJSON
         * @memberof game.SGetMyMarkedGoods
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetMyMarkedGoods.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetMyMarkedGoods
         * @function getTypeUrl
         * @memberof game.SGetMyMarkedGoods
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetMyMarkedGoods.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetMyMarkedGoods";
        };

        return SGetMyMarkedGoods;
    })();

    game.CGetRecommendPrice = (function() {

        /**
         * Properties of a CGetRecommendPrice.
         * @memberof game
         * @interface ICGetRecommendPrice
         * @property {number|null} [itemId] CGetRecommendPrice itemId
         */

        /**
         * Constructs a new CGetRecommendPrice.
         * @memberof game
         * @classdesc Represents a CGetRecommendPrice.
         * @implements ICGetRecommendPrice
         * @constructor
         * @param {game.ICGetRecommendPrice=} [properties] Properties to set
         */
        function CGetRecommendPrice(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CGetRecommendPrice itemId.
         * @member {number} itemId
         * @memberof game.CGetRecommendPrice
         * @instance
         */
        CGetRecommendPrice.prototype.itemId = 0;

        /**
         * Creates a new CGetRecommendPrice instance using the specified properties.
         * @function create
         * @memberof game.CGetRecommendPrice
         * @static
         * @param {game.ICGetRecommendPrice=} [properties] Properties to set
         * @returns {game.CGetRecommendPrice} CGetRecommendPrice instance
         */
        CGetRecommendPrice.create = function create(properties) {
            return new CGetRecommendPrice(properties);
        };

        /**
         * Encodes the specified CGetRecommendPrice message. Does not implicitly {@link game.CGetRecommendPrice.verify|verify} messages.
         * @function encode
         * @memberof game.CGetRecommendPrice
         * @static
         * @param {game.ICGetRecommendPrice} message CGetRecommendPrice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetRecommendPrice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.itemId != null && Object.hasOwnProperty.call(message, "itemId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.itemId);
            return writer;
        };

        /**
         * Encodes the specified CGetRecommendPrice message, length delimited. Does not implicitly {@link game.CGetRecommendPrice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CGetRecommendPrice
         * @static
         * @param {game.ICGetRecommendPrice} message CGetRecommendPrice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CGetRecommendPrice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CGetRecommendPrice message from the specified reader or buffer.
         * @function decode
         * @memberof game.CGetRecommendPrice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CGetRecommendPrice} CGetRecommendPrice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetRecommendPrice.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CGetRecommendPrice();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.itemId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CGetRecommendPrice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CGetRecommendPrice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CGetRecommendPrice} CGetRecommendPrice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CGetRecommendPrice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CGetRecommendPrice message.
         * @function verify
         * @memberof game.CGetRecommendPrice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CGetRecommendPrice.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                if (!$util.isInteger(message.itemId))
                    return "itemId: integer expected";
            return null;
        };

        /**
         * Creates a CGetRecommendPrice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CGetRecommendPrice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CGetRecommendPrice} CGetRecommendPrice
         */
        CGetRecommendPrice.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CGetRecommendPrice)
                return object;
            var message = new $root.game.CGetRecommendPrice();
            if (object.itemId != null)
                message.itemId = object.itemId | 0;
            return message;
        };

        /**
         * Creates a plain object from a CGetRecommendPrice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CGetRecommendPrice
         * @static
         * @param {game.CGetRecommendPrice} message CGetRecommendPrice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CGetRecommendPrice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.itemId = 0;
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                object.itemId = message.itemId;
            return object;
        };

        /**
         * Converts this CGetRecommendPrice to JSON.
         * @function toJSON
         * @memberof game.CGetRecommendPrice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CGetRecommendPrice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CGetRecommendPrice
         * @function getTypeUrl
         * @memberof game.CGetRecommendPrice
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CGetRecommendPrice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CGetRecommendPrice";
        };

        return CGetRecommendPrice;
    })();

    game.SGetRecommendPrice = (function() {

        /**
         * Properties of a SGetRecommendPrice.
         * @memberof game
         * @interface ISGetRecommendPrice
         * @property {number|null} [price] SGetRecommendPrice price
         */

        /**
         * Constructs a new SGetRecommendPrice.
         * @memberof game
         * @classdesc Represents a SGetRecommendPrice.
         * @implements ISGetRecommendPrice
         * @constructor
         * @param {game.ISGetRecommendPrice=} [properties] Properties to set
         */
        function SGetRecommendPrice(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SGetRecommendPrice price.
         * @member {number} price
         * @memberof game.SGetRecommendPrice
         * @instance
         */
        SGetRecommendPrice.prototype.price = 0;

        /**
         * Creates a new SGetRecommendPrice instance using the specified properties.
         * @function create
         * @memberof game.SGetRecommendPrice
         * @static
         * @param {game.ISGetRecommendPrice=} [properties] Properties to set
         * @returns {game.SGetRecommendPrice} SGetRecommendPrice instance
         */
        SGetRecommendPrice.create = function create(properties) {
            return new SGetRecommendPrice(properties);
        };

        /**
         * Encodes the specified SGetRecommendPrice message. Does not implicitly {@link game.SGetRecommendPrice.verify|verify} messages.
         * @function encode
         * @memberof game.SGetRecommendPrice
         * @static
         * @param {game.ISGetRecommendPrice} message SGetRecommendPrice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetRecommendPrice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.price != null && Object.hasOwnProperty.call(message, "price"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.price);
            return writer;
        };

        /**
         * Encodes the specified SGetRecommendPrice message, length delimited. Does not implicitly {@link game.SGetRecommendPrice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SGetRecommendPrice
         * @static
         * @param {game.ISGetRecommendPrice} message SGetRecommendPrice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SGetRecommendPrice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SGetRecommendPrice message from the specified reader or buffer.
         * @function decode
         * @memberof game.SGetRecommendPrice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SGetRecommendPrice} SGetRecommendPrice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetRecommendPrice.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SGetRecommendPrice();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.price = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SGetRecommendPrice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SGetRecommendPrice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SGetRecommendPrice} SGetRecommendPrice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SGetRecommendPrice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SGetRecommendPrice message.
         * @function verify
         * @memberof game.SGetRecommendPrice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SGetRecommendPrice.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.price != null && message.hasOwnProperty("price"))
                if (!$util.isInteger(message.price))
                    return "price: integer expected";
            return null;
        };

        /**
         * Creates a SGetRecommendPrice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SGetRecommendPrice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SGetRecommendPrice} SGetRecommendPrice
         */
        SGetRecommendPrice.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SGetRecommendPrice)
                return object;
            var message = new $root.game.SGetRecommendPrice();
            if (object.price != null)
                message.price = object.price | 0;
            return message;
        };

        /**
         * Creates a plain object from a SGetRecommendPrice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SGetRecommendPrice
         * @static
         * @param {game.SGetRecommendPrice} message SGetRecommendPrice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SGetRecommendPrice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.price = 0;
            if (message.price != null && message.hasOwnProperty("price"))
                object.price = message.price;
            return object;
        };

        /**
         * Converts this SGetRecommendPrice to JSON.
         * @function toJSON
         * @memberof game.SGetRecommendPrice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SGetRecommendPrice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SGetRecommendPrice
         * @function getTypeUrl
         * @memberof game.SGetRecommendPrice
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SGetRecommendPrice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SGetRecommendPrice";
        };

        return SGetRecommendPrice;
    })();

    game.CUnlockTreasureBox = (function() {

        /**
         * Properties of a CUnlockTreasureBox.
         * @memberof game
         * @interface ICUnlockTreasureBox
         * @property {number|Long|null} [bid] CUnlockTreasureBox bid
         * @property {boolean|null} [useCash] CUnlockTreasureBox useCash
         */

        /**
         * Constructs a new CUnlockTreasureBox.
         * @memberof game
         * @classdesc Represents a CUnlockTreasureBox.
         * @implements ICUnlockTreasureBox
         * @constructor
         * @param {game.ICUnlockTreasureBox=} [properties] Properties to set
         */
        function CUnlockTreasureBox(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CUnlockTreasureBox bid.
         * @member {number|Long} bid
         * @memberof game.CUnlockTreasureBox
         * @instance
         */
        CUnlockTreasureBox.prototype.bid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * CUnlockTreasureBox useCash.
         * @member {boolean} useCash
         * @memberof game.CUnlockTreasureBox
         * @instance
         */
        CUnlockTreasureBox.prototype.useCash = false;

        /**
         * Creates a new CUnlockTreasureBox instance using the specified properties.
         * @function create
         * @memberof game.CUnlockTreasureBox
         * @static
         * @param {game.ICUnlockTreasureBox=} [properties] Properties to set
         * @returns {game.CUnlockTreasureBox} CUnlockTreasureBox instance
         */
        CUnlockTreasureBox.create = function create(properties) {
            return new CUnlockTreasureBox(properties);
        };

        /**
         * Encodes the specified CUnlockTreasureBox message. Does not implicitly {@link game.CUnlockTreasureBox.verify|verify} messages.
         * @function encode
         * @memberof game.CUnlockTreasureBox
         * @static
         * @param {game.ICUnlockTreasureBox} message CUnlockTreasureBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CUnlockTreasureBox.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.bid != null && Object.hasOwnProperty.call(message, "bid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.bid);
            if (message.useCash != null && Object.hasOwnProperty.call(message, "useCash"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.useCash);
            return writer;
        };

        /**
         * Encodes the specified CUnlockTreasureBox message, length delimited. Does not implicitly {@link game.CUnlockTreasureBox.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CUnlockTreasureBox
         * @static
         * @param {game.ICUnlockTreasureBox} message CUnlockTreasureBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CUnlockTreasureBox.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CUnlockTreasureBox message from the specified reader or buffer.
         * @function decode
         * @memberof game.CUnlockTreasureBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CUnlockTreasureBox} CUnlockTreasureBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CUnlockTreasureBox.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CUnlockTreasureBox();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.bid = reader.int64();
                        break;
                    }
                case 2: {
                        message.useCash = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CUnlockTreasureBox message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CUnlockTreasureBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CUnlockTreasureBox} CUnlockTreasureBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CUnlockTreasureBox.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CUnlockTreasureBox message.
         * @function verify
         * @memberof game.CUnlockTreasureBox
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CUnlockTreasureBox.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.bid != null && message.hasOwnProperty("bid"))
                if (!$util.isInteger(message.bid) && !(message.bid && $util.isInteger(message.bid.low) && $util.isInteger(message.bid.high)))
                    return "bid: integer|Long expected";
            if (message.useCash != null && message.hasOwnProperty("useCash"))
                if (typeof message.useCash !== "boolean")
                    return "useCash: boolean expected";
            return null;
        };

        /**
         * Creates a CUnlockTreasureBox message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CUnlockTreasureBox
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CUnlockTreasureBox} CUnlockTreasureBox
         */
        CUnlockTreasureBox.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CUnlockTreasureBox)
                return object;
            var message = new $root.game.CUnlockTreasureBox();
            if (object.bid != null)
                if ($util.Long)
                    (message.bid = $util.Long.fromValue(object.bid)).unsigned = false;
                else if (typeof object.bid === "string")
                    message.bid = parseInt(object.bid, 10);
                else if (typeof object.bid === "number")
                    message.bid = object.bid;
                else if (typeof object.bid === "object")
                    message.bid = new $util.LongBits(object.bid.low >>> 0, object.bid.high >>> 0).toNumber();
            if (object.useCash != null)
                message.useCash = Boolean(object.useCash);
            return message;
        };

        /**
         * Creates a plain object from a CUnlockTreasureBox message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CUnlockTreasureBox
         * @static
         * @param {game.CUnlockTreasureBox} message CUnlockTreasureBox
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CUnlockTreasureBox.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.bid = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.bid = options.longs === String ? "0" : 0;
                object.useCash = false;
            }
            if (message.bid != null && message.hasOwnProperty("bid"))
                if (typeof message.bid === "number")
                    object.bid = options.longs === String ? String(message.bid) : message.bid;
                else
                    object.bid = options.longs === String ? $util.Long.prototype.toString.call(message.bid) : options.longs === Number ? new $util.LongBits(message.bid.low >>> 0, message.bid.high >>> 0).toNumber() : message.bid;
            if (message.useCash != null && message.hasOwnProperty("useCash"))
                object.useCash = message.useCash;
            return object;
        };

        /**
         * Converts this CUnlockTreasureBox to JSON.
         * @function toJSON
         * @memberof game.CUnlockTreasureBox
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CUnlockTreasureBox.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CUnlockTreasureBox
         * @function getTypeUrl
         * @memberof game.CUnlockTreasureBox
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CUnlockTreasureBox.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CUnlockTreasureBox";
        };

        return CUnlockTreasureBox;
    })();

    game.SUnlockTreasureBox = (function() {

        /**
         * Properties of a SUnlockTreasureBox.
         * @memberof game
         * @interface ISUnlockTreasureBox
         * @property {number|Long|null} [time] SUnlockTreasureBox time
         * @property {Array.<common.IReward>|null} [awards] SUnlockTreasureBox awards
         * @property {Array.<common.IReward>|null} [costs] SUnlockTreasureBox costs
         */

        /**
         * Constructs a new SUnlockTreasureBox.
         * @memberof game
         * @classdesc Represents a SUnlockTreasureBox.
         * @implements ISUnlockTreasureBox
         * @constructor
         * @param {game.ISUnlockTreasureBox=} [properties] Properties to set
         */
        function SUnlockTreasureBox(properties) {
            this.awards = [];
            this.costs = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SUnlockTreasureBox time.
         * @member {number|Long} time
         * @memberof game.SUnlockTreasureBox
         * @instance
         */
        SUnlockTreasureBox.prototype.time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SUnlockTreasureBox awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SUnlockTreasureBox
         * @instance
         */
        SUnlockTreasureBox.prototype.awards = $util.emptyArray;

        /**
         * SUnlockTreasureBox costs.
         * @member {Array.<common.IReward>} costs
         * @memberof game.SUnlockTreasureBox
         * @instance
         */
        SUnlockTreasureBox.prototype.costs = $util.emptyArray;

        /**
         * Creates a new SUnlockTreasureBox instance using the specified properties.
         * @function create
         * @memberof game.SUnlockTreasureBox
         * @static
         * @param {game.ISUnlockTreasureBox=} [properties] Properties to set
         * @returns {game.SUnlockTreasureBox} SUnlockTreasureBox instance
         */
        SUnlockTreasureBox.create = function create(properties) {
            return new SUnlockTreasureBox(properties);
        };

        /**
         * Encodes the specified SUnlockTreasureBox message. Does not implicitly {@link game.SUnlockTreasureBox.verify|verify} messages.
         * @function encode
         * @memberof game.SUnlockTreasureBox
         * @static
         * @param {game.ISUnlockTreasureBox} message SUnlockTreasureBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SUnlockTreasureBox.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.time);
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.costs != null && message.costs.length)
                for (var i = 0; i < message.costs.length; ++i)
                    $root.common.Reward.encode(message.costs[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SUnlockTreasureBox message, length delimited. Does not implicitly {@link game.SUnlockTreasureBox.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SUnlockTreasureBox
         * @static
         * @param {game.ISUnlockTreasureBox} message SUnlockTreasureBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SUnlockTreasureBox.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SUnlockTreasureBox message from the specified reader or buffer.
         * @function decode
         * @memberof game.SUnlockTreasureBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SUnlockTreasureBox} SUnlockTreasureBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SUnlockTreasureBox.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SUnlockTreasureBox();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.time = reader.int64();
                        break;
                    }
                case 2: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.costs && message.costs.length))
                            message.costs = [];
                        message.costs.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SUnlockTreasureBox message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SUnlockTreasureBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SUnlockTreasureBox} SUnlockTreasureBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SUnlockTreasureBox.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SUnlockTreasureBox message.
         * @function verify
         * @memberof game.SUnlockTreasureBox
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SUnlockTreasureBox.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.time != null && message.hasOwnProperty("time"))
                if (!$util.isInteger(message.time) && !(message.time && $util.isInteger(message.time.low) && $util.isInteger(message.time.high)))
                    return "time: integer|Long expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            if (message.costs != null && message.hasOwnProperty("costs")) {
                if (!Array.isArray(message.costs))
                    return "costs: array expected";
                for (var i = 0; i < message.costs.length; ++i) {
                    var error = $root.common.Reward.verify(message.costs[i]);
                    if (error)
                        return "costs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SUnlockTreasureBox message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SUnlockTreasureBox
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SUnlockTreasureBox} SUnlockTreasureBox
         */
        SUnlockTreasureBox.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SUnlockTreasureBox)
                return object;
            var message = new $root.game.SUnlockTreasureBox();
            if (object.time != null)
                if ($util.Long)
                    (message.time = $util.Long.fromValue(object.time)).unsigned = false;
                else if (typeof object.time === "string")
                    message.time = parseInt(object.time, 10);
                else if (typeof object.time === "number")
                    message.time = object.time;
                else if (typeof object.time === "object")
                    message.time = new $util.LongBits(object.time.low >>> 0, object.time.high >>> 0).toNumber();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SUnlockTreasureBox.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SUnlockTreasureBox.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            if (object.costs) {
                if (!Array.isArray(object.costs))
                    throw TypeError(".game.SUnlockTreasureBox.costs: array expected");
                message.costs = [];
                for (var i = 0; i < object.costs.length; ++i) {
                    if (typeof object.costs[i] !== "object")
                        throw TypeError(".game.SUnlockTreasureBox.costs: object expected");
                    message.costs[i] = $root.common.Reward.fromObject(object.costs[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SUnlockTreasureBox message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SUnlockTreasureBox
         * @static
         * @param {game.SUnlockTreasureBox} message SUnlockTreasureBox
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SUnlockTreasureBox.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.awards = [];
                object.costs = [];
            }
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.time = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.time = options.longs === String ? "0" : 0;
            if (message.time != null && message.hasOwnProperty("time"))
                if (typeof message.time === "number")
                    object.time = options.longs === String ? String(message.time) : message.time;
                else
                    object.time = options.longs === String ? $util.Long.prototype.toString.call(message.time) : options.longs === Number ? new $util.LongBits(message.time.low >>> 0, message.time.high >>> 0).toNumber() : message.time;
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            if (message.costs && message.costs.length) {
                object.costs = [];
                for (var j = 0; j < message.costs.length; ++j)
                    object.costs[j] = $root.common.Reward.toObject(message.costs[j], options);
            }
            return object;
        };

        /**
         * Converts this SUnlockTreasureBox to JSON.
         * @function toJSON
         * @memberof game.SUnlockTreasureBox
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SUnlockTreasureBox.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SUnlockTreasureBox
         * @function getTypeUrl
         * @memberof game.SUnlockTreasureBox
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SUnlockTreasureBox.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SUnlockTreasureBox";
        };

        return SUnlockTreasureBox;
    })();

    game.COpenTreasureBox = (function() {

        /**
         * Properties of a COpenTreasureBox.
         * @memberof game
         * @interface ICOpenTreasureBox
         * @property {number|Long|null} [bid] COpenTreasureBox bid
         * @property {boolean|null} [useCash] COpenTreasureBox useCash
         */

        /**
         * Constructs a new COpenTreasureBox.
         * @memberof game
         * @classdesc Represents a COpenTreasureBox.
         * @implements ICOpenTreasureBox
         * @constructor
         * @param {game.ICOpenTreasureBox=} [properties] Properties to set
         */
        function COpenTreasureBox(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * COpenTreasureBox bid.
         * @member {number|Long} bid
         * @memberof game.COpenTreasureBox
         * @instance
         */
        COpenTreasureBox.prototype.bid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * COpenTreasureBox useCash.
         * @member {boolean} useCash
         * @memberof game.COpenTreasureBox
         * @instance
         */
        COpenTreasureBox.prototype.useCash = false;

        /**
         * Creates a new COpenTreasureBox instance using the specified properties.
         * @function create
         * @memberof game.COpenTreasureBox
         * @static
         * @param {game.ICOpenTreasureBox=} [properties] Properties to set
         * @returns {game.COpenTreasureBox} COpenTreasureBox instance
         */
        COpenTreasureBox.create = function create(properties) {
            return new COpenTreasureBox(properties);
        };

        /**
         * Encodes the specified COpenTreasureBox message. Does not implicitly {@link game.COpenTreasureBox.verify|verify} messages.
         * @function encode
         * @memberof game.COpenTreasureBox
         * @static
         * @param {game.ICOpenTreasureBox} message COpenTreasureBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        COpenTreasureBox.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.bid != null && Object.hasOwnProperty.call(message, "bid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.bid);
            if (message.useCash != null && Object.hasOwnProperty.call(message, "useCash"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.useCash);
            return writer;
        };

        /**
         * Encodes the specified COpenTreasureBox message, length delimited. Does not implicitly {@link game.COpenTreasureBox.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.COpenTreasureBox
         * @static
         * @param {game.ICOpenTreasureBox} message COpenTreasureBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        COpenTreasureBox.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a COpenTreasureBox message from the specified reader or buffer.
         * @function decode
         * @memberof game.COpenTreasureBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.COpenTreasureBox} COpenTreasureBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        COpenTreasureBox.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.COpenTreasureBox();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.bid = reader.int64();
                        break;
                    }
                case 2: {
                        message.useCash = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a COpenTreasureBox message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.COpenTreasureBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.COpenTreasureBox} COpenTreasureBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        COpenTreasureBox.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a COpenTreasureBox message.
         * @function verify
         * @memberof game.COpenTreasureBox
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        COpenTreasureBox.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.bid != null && message.hasOwnProperty("bid"))
                if (!$util.isInteger(message.bid) && !(message.bid && $util.isInteger(message.bid.low) && $util.isInteger(message.bid.high)))
                    return "bid: integer|Long expected";
            if (message.useCash != null && message.hasOwnProperty("useCash"))
                if (typeof message.useCash !== "boolean")
                    return "useCash: boolean expected";
            return null;
        };

        /**
         * Creates a COpenTreasureBox message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.COpenTreasureBox
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.COpenTreasureBox} COpenTreasureBox
         */
        COpenTreasureBox.fromObject = function fromObject(object) {
            if (object instanceof $root.game.COpenTreasureBox)
                return object;
            var message = new $root.game.COpenTreasureBox();
            if (object.bid != null)
                if ($util.Long)
                    (message.bid = $util.Long.fromValue(object.bid)).unsigned = false;
                else if (typeof object.bid === "string")
                    message.bid = parseInt(object.bid, 10);
                else if (typeof object.bid === "number")
                    message.bid = object.bid;
                else if (typeof object.bid === "object")
                    message.bid = new $util.LongBits(object.bid.low >>> 0, object.bid.high >>> 0).toNumber();
            if (object.useCash != null)
                message.useCash = Boolean(object.useCash);
            return message;
        };

        /**
         * Creates a plain object from a COpenTreasureBox message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.COpenTreasureBox
         * @static
         * @param {game.COpenTreasureBox} message COpenTreasureBox
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        COpenTreasureBox.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.bid = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.bid = options.longs === String ? "0" : 0;
                object.useCash = false;
            }
            if (message.bid != null && message.hasOwnProperty("bid"))
                if (typeof message.bid === "number")
                    object.bid = options.longs === String ? String(message.bid) : message.bid;
                else
                    object.bid = options.longs === String ? $util.Long.prototype.toString.call(message.bid) : options.longs === Number ? new $util.LongBits(message.bid.low >>> 0, message.bid.high >>> 0).toNumber() : message.bid;
            if (message.useCash != null && message.hasOwnProperty("useCash"))
                object.useCash = message.useCash;
            return object;
        };

        /**
         * Converts this COpenTreasureBox to JSON.
         * @function toJSON
         * @memberof game.COpenTreasureBox
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        COpenTreasureBox.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for COpenTreasureBox
         * @function getTypeUrl
         * @memberof game.COpenTreasureBox
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        COpenTreasureBox.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.COpenTreasureBox";
        };

        return COpenTreasureBox;
    })();

    game.SOpenTreasureBox = (function() {

        /**
         * Properties of a SOpenTreasureBox.
         * @memberof game
         * @interface ISOpenTreasureBox
         * @property {Array.<common.IReward>|null} [awards] SOpenTreasureBox awards
         * @property {Array.<common.IReward>|null} [costs] SOpenTreasureBox costs
         */

        /**
         * Constructs a new SOpenTreasureBox.
         * @memberof game
         * @classdesc Represents a SOpenTreasureBox.
         * @implements ISOpenTreasureBox
         * @constructor
         * @param {game.ISOpenTreasureBox=} [properties] Properties to set
         */
        function SOpenTreasureBox(properties) {
            this.awards = [];
            this.costs = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SOpenTreasureBox awards.
         * @member {Array.<common.IReward>} awards
         * @memberof game.SOpenTreasureBox
         * @instance
         */
        SOpenTreasureBox.prototype.awards = $util.emptyArray;

        /**
         * SOpenTreasureBox costs.
         * @member {Array.<common.IReward>} costs
         * @memberof game.SOpenTreasureBox
         * @instance
         */
        SOpenTreasureBox.prototype.costs = $util.emptyArray;

        /**
         * Creates a new SOpenTreasureBox instance using the specified properties.
         * @function create
         * @memberof game.SOpenTreasureBox
         * @static
         * @param {game.ISOpenTreasureBox=} [properties] Properties to set
         * @returns {game.SOpenTreasureBox} SOpenTreasureBox instance
         */
        SOpenTreasureBox.create = function create(properties) {
            return new SOpenTreasureBox(properties);
        };

        /**
         * Encodes the specified SOpenTreasureBox message. Does not implicitly {@link game.SOpenTreasureBox.verify|verify} messages.
         * @function encode
         * @memberof game.SOpenTreasureBox
         * @static
         * @param {game.ISOpenTreasureBox} message SOpenTreasureBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SOpenTreasureBox.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awards != null && message.awards.length)
                for (var i = 0; i < message.awards.length; ++i)
                    $root.common.Reward.encode(message.awards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.costs != null && message.costs.length)
                for (var i = 0; i < message.costs.length; ++i)
                    $root.common.Reward.encode(message.costs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SOpenTreasureBox message, length delimited. Does not implicitly {@link game.SOpenTreasureBox.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SOpenTreasureBox
         * @static
         * @param {game.ISOpenTreasureBox} message SOpenTreasureBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SOpenTreasureBox.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SOpenTreasureBox message from the specified reader or buffer.
         * @function decode
         * @memberof game.SOpenTreasureBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SOpenTreasureBox} SOpenTreasureBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SOpenTreasureBox.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SOpenTreasureBox();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.awards && message.awards.length))
                            message.awards = [];
                        message.awards.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.costs && message.costs.length))
                            message.costs = [];
                        message.costs.push($root.common.Reward.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SOpenTreasureBox message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SOpenTreasureBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SOpenTreasureBox} SOpenTreasureBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SOpenTreasureBox.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SOpenTreasureBox message.
         * @function verify
         * @memberof game.SOpenTreasureBox
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SOpenTreasureBox.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awards != null && message.hasOwnProperty("awards")) {
                if (!Array.isArray(message.awards))
                    return "awards: array expected";
                for (var i = 0; i < message.awards.length; ++i) {
                    var error = $root.common.Reward.verify(message.awards[i]);
                    if (error)
                        return "awards." + error;
                }
            }
            if (message.costs != null && message.hasOwnProperty("costs")) {
                if (!Array.isArray(message.costs))
                    return "costs: array expected";
                for (var i = 0; i < message.costs.length; ++i) {
                    var error = $root.common.Reward.verify(message.costs[i]);
                    if (error)
                        return "costs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SOpenTreasureBox message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SOpenTreasureBox
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SOpenTreasureBox} SOpenTreasureBox
         */
        SOpenTreasureBox.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SOpenTreasureBox)
                return object;
            var message = new $root.game.SOpenTreasureBox();
            if (object.awards) {
                if (!Array.isArray(object.awards))
                    throw TypeError(".game.SOpenTreasureBox.awards: array expected");
                message.awards = [];
                for (var i = 0; i < object.awards.length; ++i) {
                    if (typeof object.awards[i] !== "object")
                        throw TypeError(".game.SOpenTreasureBox.awards: object expected");
                    message.awards[i] = $root.common.Reward.fromObject(object.awards[i]);
                }
            }
            if (object.costs) {
                if (!Array.isArray(object.costs))
                    throw TypeError(".game.SOpenTreasureBox.costs: array expected");
                message.costs = [];
                for (var i = 0; i < object.costs.length; ++i) {
                    if (typeof object.costs[i] !== "object")
                        throw TypeError(".game.SOpenTreasureBox.costs: object expected");
                    message.costs[i] = $root.common.Reward.fromObject(object.costs[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SOpenTreasureBox message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SOpenTreasureBox
         * @static
         * @param {game.SOpenTreasureBox} message SOpenTreasureBox
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SOpenTreasureBox.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.awards = [];
                object.costs = [];
            }
            if (message.awards && message.awards.length) {
                object.awards = [];
                for (var j = 0; j < message.awards.length; ++j)
                    object.awards[j] = $root.common.Reward.toObject(message.awards[j], options);
            }
            if (message.costs && message.costs.length) {
                object.costs = [];
                for (var j = 0; j < message.costs.length; ++j)
                    object.costs[j] = $root.common.Reward.toObject(message.costs[j], options);
            }
            return object;
        };

        /**
         * Converts this SOpenTreasureBox to JSON.
         * @function toJSON
         * @memberof game.SOpenTreasureBox
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SOpenTreasureBox.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SOpenTreasureBox
         * @function getTypeUrl
         * @memberof game.SOpenTreasureBox
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SOpenTreasureBox.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SOpenTreasureBox";
        };

        return SOpenTreasureBox;
    })();

    game.CSpeedUpTreasureBox = (function() {

        /**
         * Properties of a CSpeedUpTreasureBox.
         * @memberof game
         * @interface ICSpeedUpTreasureBox
         * @property {number|Long|null} [bid] CSpeedUpTreasureBox bid
         */

        /**
         * Constructs a new CSpeedUpTreasureBox.
         * @memberof game
         * @classdesc Represents a CSpeedUpTreasureBox.
         * @implements ICSpeedUpTreasureBox
         * @constructor
         * @param {game.ICSpeedUpTreasureBox=} [properties] Properties to set
         */
        function CSpeedUpTreasureBox(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSpeedUpTreasureBox bid.
         * @member {number|Long} bid
         * @memberof game.CSpeedUpTreasureBox
         * @instance
         */
        CSpeedUpTreasureBox.prototype.bid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new CSpeedUpTreasureBox instance using the specified properties.
         * @function create
         * @memberof game.CSpeedUpTreasureBox
         * @static
         * @param {game.ICSpeedUpTreasureBox=} [properties] Properties to set
         * @returns {game.CSpeedUpTreasureBox} CSpeedUpTreasureBox instance
         */
        CSpeedUpTreasureBox.create = function create(properties) {
            return new CSpeedUpTreasureBox(properties);
        };

        /**
         * Encodes the specified CSpeedUpTreasureBox message. Does not implicitly {@link game.CSpeedUpTreasureBox.verify|verify} messages.
         * @function encode
         * @memberof game.CSpeedUpTreasureBox
         * @static
         * @param {game.ICSpeedUpTreasureBox} message CSpeedUpTreasureBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSpeedUpTreasureBox.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.bid != null && Object.hasOwnProperty.call(message, "bid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.bid);
            return writer;
        };

        /**
         * Encodes the specified CSpeedUpTreasureBox message, length delimited. Does not implicitly {@link game.CSpeedUpTreasureBox.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.CSpeedUpTreasureBox
         * @static
         * @param {game.ICSpeedUpTreasureBox} message CSpeedUpTreasureBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSpeedUpTreasureBox.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSpeedUpTreasureBox message from the specified reader or buffer.
         * @function decode
         * @memberof game.CSpeedUpTreasureBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.CSpeedUpTreasureBox} CSpeedUpTreasureBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSpeedUpTreasureBox.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.CSpeedUpTreasureBox();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.bid = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSpeedUpTreasureBox message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.CSpeedUpTreasureBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.CSpeedUpTreasureBox} CSpeedUpTreasureBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSpeedUpTreasureBox.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSpeedUpTreasureBox message.
         * @function verify
         * @memberof game.CSpeedUpTreasureBox
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSpeedUpTreasureBox.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.bid != null && message.hasOwnProperty("bid"))
                if (!$util.isInteger(message.bid) && !(message.bid && $util.isInteger(message.bid.low) && $util.isInteger(message.bid.high)))
                    return "bid: integer|Long expected";
            return null;
        };

        /**
         * Creates a CSpeedUpTreasureBox message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.CSpeedUpTreasureBox
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.CSpeedUpTreasureBox} CSpeedUpTreasureBox
         */
        CSpeedUpTreasureBox.fromObject = function fromObject(object) {
            if (object instanceof $root.game.CSpeedUpTreasureBox)
                return object;
            var message = new $root.game.CSpeedUpTreasureBox();
            if (object.bid != null)
                if ($util.Long)
                    (message.bid = $util.Long.fromValue(object.bid)).unsigned = false;
                else if (typeof object.bid === "string")
                    message.bid = parseInt(object.bid, 10);
                else if (typeof object.bid === "number")
                    message.bid = object.bid;
                else if (typeof object.bid === "object")
                    message.bid = new $util.LongBits(object.bid.low >>> 0, object.bid.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a CSpeedUpTreasureBox message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.CSpeedUpTreasureBox
         * @static
         * @param {game.CSpeedUpTreasureBox} message CSpeedUpTreasureBox
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSpeedUpTreasureBox.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.bid = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.bid = options.longs === String ? "0" : 0;
            if (message.bid != null && message.hasOwnProperty("bid"))
                if (typeof message.bid === "number")
                    object.bid = options.longs === String ? String(message.bid) : message.bid;
                else
                    object.bid = options.longs === String ? $util.Long.prototype.toString.call(message.bid) : options.longs === Number ? new $util.LongBits(message.bid.low >>> 0, message.bid.high >>> 0).toNumber() : message.bid;
            return object;
        };

        /**
         * Converts this CSpeedUpTreasureBox to JSON.
         * @function toJSON
         * @memberof game.CSpeedUpTreasureBox
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSpeedUpTreasureBox.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSpeedUpTreasureBox
         * @function getTypeUrl
         * @memberof game.CSpeedUpTreasureBox
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSpeedUpTreasureBox.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.CSpeedUpTreasureBox";
        };

        return CSpeedUpTreasureBox;
    })();

    game.SSpeedUpTreasureBox = (function() {

        /**
         * Properties of a SSpeedUpTreasureBox.
         * @memberof game
         * @interface ISSpeedUpTreasureBox
         * @property {number|null} [boxId] SSpeedUpTreasureBox boxId
         * @property {number|Long|null} [time] SSpeedUpTreasureBox time
         */

        /**
         * Constructs a new SSpeedUpTreasureBox.
         * @memberof game
         * @classdesc Represents a SSpeedUpTreasureBox.
         * @implements ISSpeedUpTreasureBox
         * @constructor
         * @param {game.ISSpeedUpTreasureBox=} [properties] Properties to set
         */
        function SSpeedUpTreasureBox(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SSpeedUpTreasureBox boxId.
         * @member {number} boxId
         * @memberof game.SSpeedUpTreasureBox
         * @instance
         */
        SSpeedUpTreasureBox.prototype.boxId = 0;

        /**
         * SSpeedUpTreasureBox time.
         * @member {number|Long} time
         * @memberof game.SSpeedUpTreasureBox
         * @instance
         */
        SSpeedUpTreasureBox.prototype.time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new SSpeedUpTreasureBox instance using the specified properties.
         * @function create
         * @memberof game.SSpeedUpTreasureBox
         * @static
         * @param {game.ISSpeedUpTreasureBox=} [properties] Properties to set
         * @returns {game.SSpeedUpTreasureBox} SSpeedUpTreasureBox instance
         */
        SSpeedUpTreasureBox.create = function create(properties) {
            return new SSpeedUpTreasureBox(properties);
        };

        /**
         * Encodes the specified SSpeedUpTreasureBox message. Does not implicitly {@link game.SSpeedUpTreasureBox.verify|verify} messages.
         * @function encode
         * @memberof game.SSpeedUpTreasureBox
         * @static
         * @param {game.ISSpeedUpTreasureBox} message SSpeedUpTreasureBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSpeedUpTreasureBox.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.boxId != null && Object.hasOwnProperty.call(message, "boxId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.boxId);
            if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.time);
            return writer;
        };

        /**
         * Encodes the specified SSpeedUpTreasureBox message, length delimited. Does not implicitly {@link game.SSpeedUpTreasureBox.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SSpeedUpTreasureBox
         * @static
         * @param {game.ISSpeedUpTreasureBox} message SSpeedUpTreasureBox message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSpeedUpTreasureBox.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SSpeedUpTreasureBox message from the specified reader or buffer.
         * @function decode
         * @memberof game.SSpeedUpTreasureBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SSpeedUpTreasureBox} SSpeedUpTreasureBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSpeedUpTreasureBox.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.game.SSpeedUpTreasureBox();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.boxId = reader.int32();
                        break;
                    }
                case 2: {
                        message.time = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SSpeedUpTreasureBox message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SSpeedUpTreasureBox
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SSpeedUpTreasureBox} SSpeedUpTreasureBox
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSpeedUpTreasureBox.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SSpeedUpTreasureBox message.
         * @function verify
         * @memberof game.SSpeedUpTreasureBox
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SSpeedUpTreasureBox.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.boxId != null && message.hasOwnProperty("boxId"))
                if (!$util.isInteger(message.boxId))
                    return "boxId: integer expected";
            if (message.time != null && message.hasOwnProperty("time"))
                if (!$util.isInteger(message.time) && !(message.time && $util.isInteger(message.time.low) && $util.isInteger(message.time.high)))
                    return "time: integer|Long expected";
            return null;
        };

        /**
         * Creates a SSpeedUpTreasureBox message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SSpeedUpTreasureBox
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SSpeedUpTreasureBox} SSpeedUpTreasureBox
         */
        SSpeedUpTreasureBox.fromObject = function fromObject(object) {
            if (object instanceof $root.game.SSpeedUpTreasureBox)
                return object;
            var message = new $root.game.SSpeedUpTreasureBox();
            if (object.boxId != null)
                message.boxId = object.boxId | 0;
            if (object.time != null)
                if ($util.Long)
                    (message.time = $util.Long.fromValue(object.time)).unsigned = false;
                else if (typeof object.time === "string")
                    message.time = parseInt(object.time, 10);
                else if (typeof object.time === "number")
                    message.time = object.time;
                else if (typeof object.time === "object")
                    message.time = new $util.LongBits(object.time.low >>> 0, object.time.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a SSpeedUpTreasureBox message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SSpeedUpTreasureBox
         * @static
         * @param {game.SSpeedUpTreasureBox} message SSpeedUpTreasureBox
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SSpeedUpTreasureBox.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.boxId = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.time = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.time = options.longs === String ? "0" : 0;
            }
            if (message.boxId != null && message.hasOwnProperty("boxId"))
                object.boxId = message.boxId;
            if (message.time != null && message.hasOwnProperty("time"))
                if (typeof message.time === "number")
                    object.time = options.longs === String ? String(message.time) : message.time;
                else
                    object.time = options.longs === String ? $util.Long.prototype.toString.call(message.time) : options.longs === Number ? new $util.LongBits(message.time.low >>> 0, message.time.high >>> 0).toNumber() : message.time;
            return object;
        };

        /**
         * Converts this SSpeedUpTreasureBox to JSON.
         * @function toJSON
         * @memberof game.SSpeedUpTreasureBox
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SSpeedUpTreasureBox.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SSpeedUpTreasureBox
         * @function getTypeUrl
         * @memberof game.SSpeedUpTreasureBox
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SSpeedUpTreasureBox.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/game.SSpeedUpTreasureBox";
        };

        return SSpeedUpTreasureBox;
    })();

    return game;
})();

$root.common = (function() {

    /**
     * Namespace common.
     * @exports common
     * @namespace
     */
    var common = {};

    common.PlayerData = (function() {

        /**
         * Properties of a PlayerData.
         * @memberof common
         * @interface IPlayerData
         * @property {number|Long|null} [id] PlayerData id
         * @property {string|null} [name] PlayerData name
         * @property {number|Long|null} [guildId] PlayerData guildId
         */

        /**
         * Constructs a new PlayerData.
         * @memberof common
         * @classdesc Represents a PlayerData.
         * @implements IPlayerData
         * @constructor
         * @param {common.IPlayerData=} [properties] Properties to set
         */
        function PlayerData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PlayerData id.
         * @member {number|Long} id
         * @memberof common.PlayerData
         * @instance
         */
        PlayerData.prototype.id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PlayerData name.
         * @member {string} name
         * @memberof common.PlayerData
         * @instance
         */
        PlayerData.prototype.name = "";

        /**
         * PlayerData guildId.
         * @member {number|Long} guildId
         * @memberof common.PlayerData
         * @instance
         */
        PlayerData.prototype.guildId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new PlayerData instance using the specified properties.
         * @function create
         * @memberof common.PlayerData
         * @static
         * @param {common.IPlayerData=} [properties] Properties to set
         * @returns {common.PlayerData} PlayerData instance
         */
        PlayerData.create = function create(properties) {
            return new PlayerData(properties);
        };

        /**
         * Encodes the specified PlayerData message. Does not implicitly {@link common.PlayerData.verify|verify} messages.
         * @function encode
         * @memberof common.PlayerData
         * @static
         * @param {common.IPlayerData} message PlayerData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.id);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.guildId != null && Object.hasOwnProperty.call(message, "guildId"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.guildId);
            return writer;
        };

        /**
         * Encodes the specified PlayerData message, length delimited. Does not implicitly {@link common.PlayerData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.PlayerData
         * @static
         * @param {common.IPlayerData} message PlayerData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PlayerData message from the specified reader or buffer.
         * @function decode
         * @memberof common.PlayerData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.PlayerData} PlayerData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.PlayerData();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int64();
                        break;
                    }
                case 2: {
                        message.name = reader.string();
                        break;
                    }
                case 3: {
                        message.guildId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PlayerData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.PlayerData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.PlayerData} PlayerData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PlayerData message.
         * @function verify
         * @memberof common.PlayerData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id) && !(message.id && $util.isInteger(message.id.low) && $util.isInteger(message.id.high)))
                    return "id: integer|Long expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.guildId != null && message.hasOwnProperty("guildId"))
                if (!$util.isInteger(message.guildId) && !(message.guildId && $util.isInteger(message.guildId.low) && $util.isInteger(message.guildId.high)))
                    return "guildId: integer|Long expected";
            return null;
        };

        /**
         * Creates a PlayerData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.PlayerData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.PlayerData} PlayerData
         */
        PlayerData.fromObject = function fromObject(object) {
            if (object instanceof $root.common.PlayerData)
                return object;
            var message = new $root.common.PlayerData();
            if (object.id != null)
                if ($util.Long)
                    (message.id = $util.Long.fromValue(object.id)).unsigned = false;
                else if (typeof object.id === "string")
                    message.id = parseInt(object.id, 10);
                else if (typeof object.id === "number")
                    message.id = object.id;
                else if (typeof object.id === "object")
                    message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber();
            if (object.name != null)
                message.name = String(object.name);
            if (object.guildId != null)
                if ($util.Long)
                    (message.guildId = $util.Long.fromValue(object.guildId)).unsigned = false;
                else if (typeof object.guildId === "string")
                    message.guildId = parseInt(object.guildId, 10);
                else if (typeof object.guildId === "number")
                    message.guildId = object.guildId;
                else if (typeof object.guildId === "object")
                    message.guildId = new $util.LongBits(object.guildId.low >>> 0, object.guildId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a PlayerData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.PlayerData
         * @static
         * @param {common.PlayerData} message PlayerData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PlayerData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.id = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.id = options.longs === String ? "0" : 0;
                object.name = "";
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.guildId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.guildId = options.longs === String ? "0" : 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                if (typeof message.id === "number")
                    object.id = options.longs === String ? String(message.id) : message.id;
                else
                    object.id = options.longs === String ? $util.Long.prototype.toString.call(message.id) : options.longs === Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber() : message.id;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.guildId != null && message.hasOwnProperty("guildId"))
                if (typeof message.guildId === "number")
                    object.guildId = options.longs === String ? String(message.guildId) : message.guildId;
                else
                    object.guildId = options.longs === String ? $util.Long.prototype.toString.call(message.guildId) : options.longs === Number ? new $util.LongBits(message.guildId.low >>> 0, message.guildId.high >>> 0).toNumber() : message.guildId;
            return object;
        };

        /**
         * Converts this PlayerData to JSON.
         * @function toJSON
         * @memberof common.PlayerData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PlayerData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PlayerData
         * @function getTypeUrl
         * @memberof common.PlayerData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PlayerData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.PlayerData";
        };

        return PlayerData;
    })();

    common.UserInfo = (function() {

        /**
         * Properties of a UserInfo.
         * @memberof common
         * @interface IUserInfo
         * @property {number|Long|null} [unionId] UserInfo unionId
         * @property {string|null} [account] UserInfo account
         * @property {string|null} [nick] UserInfo nick
         * @property {string|null} [email] UserInfo email
         * @property {string|null} [phoneNumber] UserInfo phoneNumber
         * @property {number|null} [group] UserInfo group
         * @property {number|Long|null} [createTime] UserInfo createTime
         */

        /**
         * Constructs a new UserInfo.
         * @memberof common
         * @classdesc Represents a UserInfo.
         * @implements IUserInfo
         * @constructor
         * @param {common.IUserInfo=} [properties] Properties to set
         */
        function UserInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserInfo unionId.
         * @member {number|Long} unionId
         * @memberof common.UserInfo
         * @instance
         */
        UserInfo.prototype.unionId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * UserInfo account.
         * @member {string} account
         * @memberof common.UserInfo
         * @instance
         */
        UserInfo.prototype.account = "";

        /**
         * UserInfo nick.
         * @member {string} nick
         * @memberof common.UserInfo
         * @instance
         */
        UserInfo.prototype.nick = "";

        /**
         * UserInfo email.
         * @member {string} email
         * @memberof common.UserInfo
         * @instance
         */
        UserInfo.prototype.email = "";

        /**
         * UserInfo phoneNumber.
         * @member {string} phoneNumber
         * @memberof common.UserInfo
         * @instance
         */
        UserInfo.prototype.phoneNumber = "";

        /**
         * UserInfo group.
         * @member {number} group
         * @memberof common.UserInfo
         * @instance
         */
        UserInfo.prototype.group = 0;

        /**
         * UserInfo createTime.
         * @member {number|Long} createTime
         * @memberof common.UserInfo
         * @instance
         */
        UserInfo.prototype.createTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new UserInfo instance using the specified properties.
         * @function create
         * @memberof common.UserInfo
         * @static
         * @param {common.IUserInfo=} [properties] Properties to set
         * @returns {common.UserInfo} UserInfo instance
         */
        UserInfo.create = function create(properties) {
            return new UserInfo(properties);
        };

        /**
         * Encodes the specified UserInfo message. Does not implicitly {@link common.UserInfo.verify|verify} messages.
         * @function encode
         * @memberof common.UserInfo
         * @static
         * @param {common.IUserInfo} message UserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unionId != null && Object.hasOwnProperty.call(message, "unionId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.unionId);
            if (message.account != null && Object.hasOwnProperty.call(message, "account"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.account);
            if (message.nick != null && Object.hasOwnProperty.call(message, "nick"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.nick);
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.email);
            if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.phoneNumber);
            if (message.group != null && Object.hasOwnProperty.call(message, "group"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.group);
            if (message.createTime != null && Object.hasOwnProperty.call(message, "createTime"))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.createTime);
            return writer;
        };

        /**
         * Encodes the specified UserInfo message, length delimited. Does not implicitly {@link common.UserInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.UserInfo
         * @static
         * @param {common.IUserInfo} message UserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a UserInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.UserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.UserInfo} UserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.UserInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.unionId = reader.int64();
                        break;
                    }
                case 2: {
                        message.account = reader.string();
                        break;
                    }
                case 3: {
                        message.nick = reader.string();
                        break;
                    }
                case 4: {
                        message.email = reader.string();
                        break;
                    }
                case 5: {
                        message.phoneNumber = reader.string();
                        break;
                    }
                case 6: {
                        message.group = reader.int32();
                        break;
                    }
                case 7: {
                        message.createTime = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a UserInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.UserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.UserInfo} UserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UserInfo message.
         * @function verify
         * @memberof common.UserInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UserInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.unionId != null && message.hasOwnProperty("unionId"))
                if (!$util.isInteger(message.unionId) && !(message.unionId && $util.isInteger(message.unionId.low) && $util.isInteger(message.unionId.high)))
                    return "unionId: integer|Long expected";
            if (message.account != null && message.hasOwnProperty("account"))
                if (!$util.isString(message.account))
                    return "account: string expected";
            if (message.nick != null && message.hasOwnProperty("nick"))
                if (!$util.isString(message.nick))
                    return "nick: string expected";
            if (message.email != null && message.hasOwnProperty("email"))
                if (!$util.isString(message.email))
                    return "email: string expected";
            if (message.phoneNumber != null && message.hasOwnProperty("phoneNumber"))
                if (!$util.isString(message.phoneNumber))
                    return "phoneNumber: string expected";
            if (message.group != null && message.hasOwnProperty("group"))
                if (!$util.isInteger(message.group))
                    return "group: integer expected";
            if (message.createTime != null && message.hasOwnProperty("createTime"))
                if (!$util.isInteger(message.createTime) && !(message.createTime && $util.isInteger(message.createTime.low) && $util.isInteger(message.createTime.high)))
                    return "createTime: integer|Long expected";
            return null;
        };

        /**
         * Creates a UserInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.UserInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.UserInfo} UserInfo
         */
        UserInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.UserInfo)
                return object;
            var message = new $root.common.UserInfo();
            if (object.unionId != null)
                if ($util.Long)
                    (message.unionId = $util.Long.fromValue(object.unionId)).unsigned = false;
                else if (typeof object.unionId === "string")
                    message.unionId = parseInt(object.unionId, 10);
                else if (typeof object.unionId === "number")
                    message.unionId = object.unionId;
                else if (typeof object.unionId === "object")
                    message.unionId = new $util.LongBits(object.unionId.low >>> 0, object.unionId.high >>> 0).toNumber();
            if (object.account != null)
                message.account = String(object.account);
            if (object.nick != null)
                message.nick = String(object.nick);
            if (object.email != null)
                message.email = String(object.email);
            if (object.phoneNumber != null)
                message.phoneNumber = String(object.phoneNumber);
            if (object.group != null)
                message.group = object.group | 0;
            if (object.createTime != null)
                if ($util.Long)
                    (message.createTime = $util.Long.fromValue(object.createTime)).unsigned = false;
                else if (typeof object.createTime === "string")
                    message.createTime = parseInt(object.createTime, 10);
                else if (typeof object.createTime === "number")
                    message.createTime = object.createTime;
                else if (typeof object.createTime === "object")
                    message.createTime = new $util.LongBits(object.createTime.low >>> 0, object.createTime.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a UserInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.UserInfo
         * @static
         * @param {common.UserInfo} message UserInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.unionId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.unionId = options.longs === String ? "0" : 0;
                object.account = "";
                object.nick = "";
                object.email = "";
                object.phoneNumber = "";
                object.group = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.createTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.createTime = options.longs === String ? "0" : 0;
            }
            if (message.unionId != null && message.hasOwnProperty("unionId"))
                if (typeof message.unionId === "number")
                    object.unionId = options.longs === String ? String(message.unionId) : message.unionId;
                else
                    object.unionId = options.longs === String ? $util.Long.prototype.toString.call(message.unionId) : options.longs === Number ? new $util.LongBits(message.unionId.low >>> 0, message.unionId.high >>> 0).toNumber() : message.unionId;
            if (message.account != null && message.hasOwnProperty("account"))
                object.account = message.account;
            if (message.nick != null && message.hasOwnProperty("nick"))
                object.nick = message.nick;
            if (message.email != null && message.hasOwnProperty("email"))
                object.email = message.email;
            if (message.phoneNumber != null && message.hasOwnProperty("phoneNumber"))
                object.phoneNumber = message.phoneNumber;
            if (message.group != null && message.hasOwnProperty("group"))
                object.group = message.group;
            if (message.createTime != null && message.hasOwnProperty("createTime"))
                if (typeof message.createTime === "number")
                    object.createTime = options.longs === String ? String(message.createTime) : message.createTime;
                else
                    object.createTime = options.longs === String ? $util.Long.prototype.toString.call(message.createTime) : options.longs === Number ? new $util.LongBits(message.createTime.low >>> 0, message.createTime.high >>> 0).toNumber() : message.createTime;
            return object;
        };

        /**
         * Converts this UserInfo to JSON.
         * @function toJSON
         * @memberof common.UserInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserInfo
         * @function getTypeUrl
         * @memberof common.UserInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.UserInfo";
        };

        return UserInfo;
    })();

    common.WorldUserInfo = (function() {

        /**
         * Properties of a WorldUserInfo.
         * @memberof common
         * @interface IWorldUserInfo
         * @property {string|null} [uid] WorldUserInfo uid
         * @property {string|null} [nick] WorldUserInfo nick
         * @property {string|null} [gender] WorldUserInfo gender
         * @property {string|null} [country] WorldUserInfo country
         * @property {string|null} [avatar] WorldUserInfo avatar
         * @property {number|null} [level] WorldUserInfo level
         * @property {number|null} [cups] WorldUserInfo cups
         * @property {number|null} [gameId] WorldUserInfo gameId
         * @property {number|null} [gateId] WorldUserInfo gateId
         * @property {string|null} [gateAddress] WorldUserInfo gateAddress
         */

        /**
         * Constructs a new WorldUserInfo.
         * @memberof common
         * @classdesc Represents a WorldUserInfo.
         * @implements IWorldUserInfo
         * @constructor
         * @param {common.IWorldUserInfo=} [properties] Properties to set
         */
        function WorldUserInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WorldUserInfo uid.
         * @member {string} uid
         * @memberof common.WorldUserInfo
         * @instance
         */
        WorldUserInfo.prototype.uid = "";

        /**
         * WorldUserInfo nick.
         * @member {string} nick
         * @memberof common.WorldUserInfo
         * @instance
         */
        WorldUserInfo.prototype.nick = "";

        /**
         * WorldUserInfo gender.
         * @member {string} gender
         * @memberof common.WorldUserInfo
         * @instance
         */
        WorldUserInfo.prototype.gender = "";

        /**
         * WorldUserInfo country.
         * @member {string} country
         * @memberof common.WorldUserInfo
         * @instance
         */
        WorldUserInfo.prototype.country = "";

        /**
         * WorldUserInfo avatar.
         * @member {string} avatar
         * @memberof common.WorldUserInfo
         * @instance
         */
        WorldUserInfo.prototype.avatar = "";

        /**
         * WorldUserInfo level.
         * @member {number} level
         * @memberof common.WorldUserInfo
         * @instance
         */
        WorldUserInfo.prototype.level = 0;

        /**
         * WorldUserInfo cups.
         * @member {number} cups
         * @memberof common.WorldUserInfo
         * @instance
         */
        WorldUserInfo.prototype.cups = 0;

        /**
         * WorldUserInfo gameId.
         * @member {number} gameId
         * @memberof common.WorldUserInfo
         * @instance
         */
        WorldUserInfo.prototype.gameId = 0;

        /**
         * WorldUserInfo gateId.
         * @member {number} gateId
         * @memberof common.WorldUserInfo
         * @instance
         */
        WorldUserInfo.prototype.gateId = 0;

        /**
         * WorldUserInfo gateAddress.
         * @member {string} gateAddress
         * @memberof common.WorldUserInfo
         * @instance
         */
        WorldUserInfo.prototype.gateAddress = "";

        /**
         * Creates a new WorldUserInfo instance using the specified properties.
         * @function create
         * @memberof common.WorldUserInfo
         * @static
         * @param {common.IWorldUserInfo=} [properties] Properties to set
         * @returns {common.WorldUserInfo} WorldUserInfo instance
         */
        WorldUserInfo.create = function create(properties) {
            return new WorldUserInfo(properties);
        };

        /**
         * Encodes the specified WorldUserInfo message. Does not implicitly {@link common.WorldUserInfo.verify|verify} messages.
         * @function encode
         * @memberof common.WorldUserInfo
         * @static
         * @param {common.IWorldUserInfo} message WorldUserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WorldUserInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.nick != null && Object.hasOwnProperty.call(message, "nick"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.nick);
            if (message.gender != null && Object.hasOwnProperty.call(message, "gender"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.gender);
            if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.country);
            if (message.avatar != null && Object.hasOwnProperty.call(message, "avatar"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.avatar);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.level);
            if (message.cups != null && Object.hasOwnProperty.call(message, "cups"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.cups);
            if (message.gameId != null && Object.hasOwnProperty.call(message, "gameId"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.gameId);
            if (message.gateId != null && Object.hasOwnProperty.call(message, "gateId"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.gateId);
            if (message.gateAddress != null && Object.hasOwnProperty.call(message, "gateAddress"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.gateAddress);
            return writer;
        };

        /**
         * Encodes the specified WorldUserInfo message, length delimited. Does not implicitly {@link common.WorldUserInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.WorldUserInfo
         * @static
         * @param {common.IWorldUserInfo} message WorldUserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WorldUserInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WorldUserInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.WorldUserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.WorldUserInfo} WorldUserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WorldUserInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.WorldUserInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.nick = reader.string();
                        break;
                    }
                case 3: {
                        message.gender = reader.string();
                        break;
                    }
                case 4: {
                        message.country = reader.string();
                        break;
                    }
                case 5: {
                        message.avatar = reader.string();
                        break;
                    }
                case 6: {
                        message.level = reader.int32();
                        break;
                    }
                case 7: {
                        message.cups = reader.int32();
                        break;
                    }
                case 8: {
                        message.gameId = reader.int32();
                        break;
                    }
                case 9: {
                        message.gateId = reader.int32();
                        break;
                    }
                case 10: {
                        message.gateAddress = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WorldUserInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.WorldUserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.WorldUserInfo} WorldUserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WorldUserInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WorldUserInfo message.
         * @function verify
         * @memberof common.WorldUserInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WorldUserInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.nick != null && message.hasOwnProperty("nick"))
                if (!$util.isString(message.nick))
                    return "nick: string expected";
            if (message.gender != null && message.hasOwnProperty("gender"))
                if (!$util.isString(message.gender))
                    return "gender: string expected";
            if (message.country != null && message.hasOwnProperty("country"))
                if (!$util.isString(message.country))
                    return "country: string expected";
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                if (!$util.isString(message.avatar))
                    return "avatar: string expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.cups != null && message.hasOwnProperty("cups"))
                if (!$util.isInteger(message.cups))
                    return "cups: integer expected";
            if (message.gameId != null && message.hasOwnProperty("gameId"))
                if (!$util.isInteger(message.gameId))
                    return "gameId: integer expected";
            if (message.gateId != null && message.hasOwnProperty("gateId"))
                if (!$util.isInteger(message.gateId))
                    return "gateId: integer expected";
            if (message.gateAddress != null && message.hasOwnProperty("gateAddress"))
                if (!$util.isString(message.gateAddress))
                    return "gateAddress: string expected";
            return null;
        };

        /**
         * Creates a WorldUserInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.WorldUserInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.WorldUserInfo} WorldUserInfo
         */
        WorldUserInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.WorldUserInfo)
                return object;
            var message = new $root.common.WorldUserInfo();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.nick != null)
                message.nick = String(object.nick);
            if (object.gender != null)
                message.gender = String(object.gender);
            if (object.country != null)
                message.country = String(object.country);
            if (object.avatar != null)
                message.avatar = String(object.avatar);
            if (object.level != null)
                message.level = object.level | 0;
            if (object.cups != null)
                message.cups = object.cups | 0;
            if (object.gameId != null)
                message.gameId = object.gameId | 0;
            if (object.gateId != null)
                message.gateId = object.gateId | 0;
            if (object.gateAddress != null)
                message.gateAddress = String(object.gateAddress);
            return message;
        };

        /**
         * Creates a plain object from a WorldUserInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.WorldUserInfo
         * @static
         * @param {common.WorldUserInfo} message WorldUserInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WorldUserInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.uid = "";
                object.nick = "";
                object.gender = "";
                object.country = "";
                object.avatar = "";
                object.level = 0;
                object.cups = 0;
                object.gameId = 0;
                object.gateId = 0;
                object.gateAddress = "";
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.nick != null && message.hasOwnProperty("nick"))
                object.nick = message.nick;
            if (message.gender != null && message.hasOwnProperty("gender"))
                object.gender = message.gender;
            if (message.country != null && message.hasOwnProperty("country"))
                object.country = message.country;
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                object.avatar = message.avatar;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.cups != null && message.hasOwnProperty("cups"))
                object.cups = message.cups;
            if (message.gameId != null && message.hasOwnProperty("gameId"))
                object.gameId = message.gameId;
            if (message.gateId != null && message.hasOwnProperty("gateId"))
                object.gateId = message.gateId;
            if (message.gateAddress != null && message.hasOwnProperty("gateAddress"))
                object.gateAddress = message.gateAddress;
            return object;
        };

        /**
         * Converts this WorldUserInfo to JSON.
         * @function toJSON
         * @memberof common.WorldUserInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WorldUserInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WorldUserInfo
         * @function getTypeUrl
         * @memberof common.WorldUserInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WorldUserInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.WorldUserInfo";
        };

        return WorldUserInfo;
    })();

    common.EquipInfo = (function() {

        /**
         * Properties of an EquipInfo.
         * @memberof common
         * @interface IEquipInfo
         * @property {number|null} [id] EquipInfo id
         * @property {number|null} [hero] EquipInfo hero
         * @property {Array.<number>|null} [attr] EquipInfo attr
         */

        /**
         * Constructs a new EquipInfo.
         * @memberof common
         * @classdesc Represents an EquipInfo.
         * @implements IEquipInfo
         * @constructor
         * @param {common.IEquipInfo=} [properties] Properties to set
         */
        function EquipInfo(properties) {
            this.attr = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EquipInfo id.
         * @member {number} id
         * @memberof common.EquipInfo
         * @instance
         */
        EquipInfo.prototype.id = 0;

        /**
         * EquipInfo hero.
         * @member {number} hero
         * @memberof common.EquipInfo
         * @instance
         */
        EquipInfo.prototype.hero = 0;

        /**
         * EquipInfo attr.
         * @member {Array.<number>} attr
         * @memberof common.EquipInfo
         * @instance
         */
        EquipInfo.prototype.attr = $util.emptyArray;

        /**
         * Creates a new EquipInfo instance using the specified properties.
         * @function create
         * @memberof common.EquipInfo
         * @static
         * @param {common.IEquipInfo=} [properties] Properties to set
         * @returns {common.EquipInfo} EquipInfo instance
         */
        EquipInfo.create = function create(properties) {
            return new EquipInfo(properties);
        };

        /**
         * Encodes the specified EquipInfo message. Does not implicitly {@link common.EquipInfo.verify|verify} messages.
         * @function encode
         * @memberof common.EquipInfo
         * @static
         * @param {common.IEquipInfo} message EquipInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EquipInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.hero != null && Object.hasOwnProperty.call(message, "hero"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.hero);
            if (message.attr != null && message.attr.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (var i = 0; i < message.attr.length; ++i)
                    writer.int32(message.attr[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified EquipInfo message, length delimited. Does not implicitly {@link common.EquipInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.EquipInfo
         * @static
         * @param {common.IEquipInfo} message EquipInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EquipInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an EquipInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.EquipInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.EquipInfo} EquipInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EquipInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.EquipInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.hero = reader.int32();
                        break;
                    }
                case 3: {
                        if (!(message.attr && message.attr.length))
                            message.attr = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.attr.push(reader.int32());
                        } else
                            message.attr.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an EquipInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.EquipInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.EquipInfo} EquipInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EquipInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EquipInfo message.
         * @function verify
         * @memberof common.EquipInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EquipInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.hero != null && message.hasOwnProperty("hero"))
                if (!$util.isInteger(message.hero))
                    return "hero: integer expected";
            if (message.attr != null && message.hasOwnProperty("attr")) {
                if (!Array.isArray(message.attr))
                    return "attr: array expected";
                for (var i = 0; i < message.attr.length; ++i)
                    if (!$util.isInteger(message.attr[i]))
                        return "attr: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an EquipInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.EquipInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.EquipInfo} EquipInfo
         */
        EquipInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.EquipInfo)
                return object;
            var message = new $root.common.EquipInfo();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.hero != null)
                message.hero = object.hero | 0;
            if (object.attr) {
                if (!Array.isArray(object.attr))
                    throw TypeError(".common.EquipInfo.attr: array expected");
                message.attr = [];
                for (var i = 0; i < object.attr.length; ++i)
                    message.attr[i] = object.attr[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an EquipInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.EquipInfo
         * @static
         * @param {common.EquipInfo} message EquipInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EquipInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.attr = [];
            if (options.defaults) {
                object.id = 0;
                object.hero = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.hero != null && message.hasOwnProperty("hero"))
                object.hero = message.hero;
            if (message.attr && message.attr.length) {
                object.attr = [];
                for (var j = 0; j < message.attr.length; ++j)
                    object.attr[j] = message.attr[j];
            }
            return object;
        };

        /**
         * Converts this EquipInfo to JSON.
         * @function toJSON
         * @memberof common.EquipInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EquipInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EquipInfo
         * @function getTypeUrl
         * @memberof common.EquipInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EquipInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.EquipInfo";
        };

        return EquipInfo;
    })();

    common.Hero = (function() {

        /**
         * Properties of a Hero.
         * @memberof common
         * @interface IHero
         * @property {number|null} [level] Hero level
         * @property {number|null} [san] Hero san
         * @property {number|Long|null} [revocerTime] Hero revocerTime
         * @property {number|null} [modelId] Hero modelId
         * @property {number|null} [skinId] Hero skinId
         * @property {Array.<number|Long>|null} [equips] Hero equips
         */

        /**
         * Constructs a new Hero.
         * @memberof common
         * @classdesc Represents a Hero.
         * @implements IHero
         * @constructor
         * @param {common.IHero=} [properties] Properties to set
         */
        function Hero(properties) {
            this.equips = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Hero level.
         * @member {number} level
         * @memberof common.Hero
         * @instance
         */
        Hero.prototype.level = 0;

        /**
         * Hero san.
         * @member {number} san
         * @memberof common.Hero
         * @instance
         */
        Hero.prototype.san = 0;

        /**
         * Hero revocerTime.
         * @member {number|Long} revocerTime
         * @memberof common.Hero
         * @instance
         */
        Hero.prototype.revocerTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Hero modelId.
         * @member {number} modelId
         * @memberof common.Hero
         * @instance
         */
        Hero.prototype.modelId = 0;

        /**
         * Hero skinId.
         * @member {number} skinId
         * @memberof common.Hero
         * @instance
         */
        Hero.prototype.skinId = 0;

        /**
         * Hero equips.
         * @member {Array.<number|Long>} equips
         * @memberof common.Hero
         * @instance
         */
        Hero.prototype.equips = $util.emptyArray;

        /**
         * Creates a new Hero instance using the specified properties.
         * @function create
         * @memberof common.Hero
         * @static
         * @param {common.IHero=} [properties] Properties to set
         * @returns {common.Hero} Hero instance
         */
        Hero.create = function create(properties) {
            return new Hero(properties);
        };

        /**
         * Encodes the specified Hero message. Does not implicitly {@link common.Hero.verify|verify} messages.
         * @function encode
         * @memberof common.Hero
         * @static
         * @param {common.IHero} message Hero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Hero.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.level);
            if (message.san != null && Object.hasOwnProperty.call(message, "san"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.san);
            if (message.revocerTime != null && Object.hasOwnProperty.call(message, "revocerTime"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.revocerTime);
            if (message.modelId != null && Object.hasOwnProperty.call(message, "modelId"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.modelId);
            if (message.skinId != null && Object.hasOwnProperty.call(message, "skinId"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.skinId);
            if (message.equips != null && message.equips.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (var i = 0; i < message.equips.length; ++i)
                    writer.int64(message.equips[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Hero message, length delimited. Does not implicitly {@link common.Hero.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.Hero
         * @static
         * @param {common.IHero} message Hero message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Hero.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Hero message from the specified reader or buffer.
         * @function decode
         * @memberof common.Hero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.Hero} Hero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Hero.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.Hero();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.level = reader.int32();
                        break;
                    }
                case 2: {
                        message.san = reader.int32();
                        break;
                    }
                case 3: {
                        message.revocerTime = reader.int64();
                        break;
                    }
                case 4: {
                        message.modelId = reader.int32();
                        break;
                    }
                case 5: {
                        message.skinId = reader.int32();
                        break;
                    }
                case 6: {
                        if (!(message.equips && message.equips.length))
                            message.equips = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.equips.push(reader.int64());
                        } else
                            message.equips.push(reader.int64());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Hero message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.Hero
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.Hero} Hero
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Hero.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Hero message.
         * @function verify
         * @memberof common.Hero
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Hero.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.san != null && message.hasOwnProperty("san"))
                if (!$util.isInteger(message.san))
                    return "san: integer expected";
            if (message.revocerTime != null && message.hasOwnProperty("revocerTime"))
                if (!$util.isInteger(message.revocerTime) && !(message.revocerTime && $util.isInteger(message.revocerTime.low) && $util.isInteger(message.revocerTime.high)))
                    return "revocerTime: integer|Long expected";
            if (message.modelId != null && message.hasOwnProperty("modelId"))
                if (!$util.isInteger(message.modelId))
                    return "modelId: integer expected";
            if (message.skinId != null && message.hasOwnProperty("skinId"))
                if (!$util.isInteger(message.skinId))
                    return "skinId: integer expected";
            if (message.equips != null && message.hasOwnProperty("equips")) {
                if (!Array.isArray(message.equips))
                    return "equips: array expected";
                for (var i = 0; i < message.equips.length; ++i)
                    if (!$util.isInteger(message.equips[i]) && !(message.equips[i] && $util.isInteger(message.equips[i].low) && $util.isInteger(message.equips[i].high)))
                        return "equips: integer|Long[] expected";
            }
            return null;
        };

        /**
         * Creates a Hero message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.Hero
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.Hero} Hero
         */
        Hero.fromObject = function fromObject(object) {
            if (object instanceof $root.common.Hero)
                return object;
            var message = new $root.common.Hero();
            if (object.level != null)
                message.level = object.level | 0;
            if (object.san != null)
                message.san = object.san | 0;
            if (object.revocerTime != null)
                if ($util.Long)
                    (message.revocerTime = $util.Long.fromValue(object.revocerTime)).unsigned = false;
                else if (typeof object.revocerTime === "string")
                    message.revocerTime = parseInt(object.revocerTime, 10);
                else if (typeof object.revocerTime === "number")
                    message.revocerTime = object.revocerTime;
                else if (typeof object.revocerTime === "object")
                    message.revocerTime = new $util.LongBits(object.revocerTime.low >>> 0, object.revocerTime.high >>> 0).toNumber();
            if (object.modelId != null)
                message.modelId = object.modelId | 0;
            if (object.skinId != null)
                message.skinId = object.skinId | 0;
            if (object.equips) {
                if (!Array.isArray(object.equips))
                    throw TypeError(".common.Hero.equips: array expected");
                message.equips = [];
                for (var i = 0; i < object.equips.length; ++i)
                    if ($util.Long)
                        (message.equips[i] = $util.Long.fromValue(object.equips[i])).unsigned = false;
                    else if (typeof object.equips[i] === "string")
                        message.equips[i] = parseInt(object.equips[i], 10);
                    else if (typeof object.equips[i] === "number")
                        message.equips[i] = object.equips[i];
                    else if (typeof object.equips[i] === "object")
                        message.equips[i] = new $util.LongBits(object.equips[i].low >>> 0, object.equips[i].high >>> 0).toNumber();
            }
            return message;
        };

        /**
         * Creates a plain object from a Hero message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.Hero
         * @static
         * @param {common.Hero} message Hero
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Hero.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.equips = [];
            if (options.defaults) {
                object.level = 0;
                object.san = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.revocerTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.revocerTime = options.longs === String ? "0" : 0;
                object.modelId = 0;
                object.skinId = 0;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.san != null && message.hasOwnProperty("san"))
                object.san = message.san;
            if (message.revocerTime != null && message.hasOwnProperty("revocerTime"))
                if (typeof message.revocerTime === "number")
                    object.revocerTime = options.longs === String ? String(message.revocerTime) : message.revocerTime;
                else
                    object.revocerTime = options.longs === String ? $util.Long.prototype.toString.call(message.revocerTime) : options.longs === Number ? new $util.LongBits(message.revocerTime.low >>> 0, message.revocerTime.high >>> 0).toNumber() : message.revocerTime;
            if (message.modelId != null && message.hasOwnProperty("modelId"))
                object.modelId = message.modelId;
            if (message.skinId != null && message.hasOwnProperty("skinId"))
                object.skinId = message.skinId;
            if (message.equips && message.equips.length) {
                object.equips = [];
                for (var j = 0; j < message.equips.length; ++j)
                    if (typeof message.equips[j] === "number")
                        object.equips[j] = options.longs === String ? String(message.equips[j]) : message.equips[j];
                    else
                        object.equips[j] = options.longs === String ? $util.Long.prototype.toString.call(message.equips[j]) : options.longs === Number ? new $util.LongBits(message.equips[j].low >>> 0, message.equips[j].high >>> 0).toNumber() : message.equips[j];
            }
            return object;
        };

        /**
         * Converts this Hero to JSON.
         * @function toJSON
         * @memberof common.Hero
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Hero.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Hero
         * @function getTypeUrl
         * @memberof common.Hero
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Hero.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.Hero";
        };

        return Hero;
    })();

    common.BagItemNum = (function() {

        /**
         * Properties of a BagItemNum.
         * @memberof common
         * @interface IBagItemNum
         * @property {number|null} [num] BagItemNum num
         * @property {number|null} [tradable] BagItemNum tradable
         */

        /**
         * Constructs a new BagItemNum.
         * @memberof common
         * @classdesc Represents a BagItemNum.
         * @implements IBagItemNum
         * @constructor
         * @param {common.IBagItemNum=} [properties] Properties to set
         */
        function BagItemNum(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BagItemNum num.
         * @member {number} num
         * @memberof common.BagItemNum
         * @instance
         */
        BagItemNum.prototype.num = 0;

        /**
         * BagItemNum tradable.
         * @member {number} tradable
         * @memberof common.BagItemNum
         * @instance
         */
        BagItemNum.prototype.tradable = 0;

        /**
         * Creates a new BagItemNum instance using the specified properties.
         * @function create
         * @memberof common.BagItemNum
         * @static
         * @param {common.IBagItemNum=} [properties] Properties to set
         * @returns {common.BagItemNum} BagItemNum instance
         */
        BagItemNum.create = function create(properties) {
            return new BagItemNum(properties);
        };

        /**
         * Encodes the specified BagItemNum message. Does not implicitly {@link common.BagItemNum.verify|verify} messages.
         * @function encode
         * @memberof common.BagItemNum
         * @static
         * @param {common.IBagItemNum} message BagItemNum message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BagItemNum.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.num != null && Object.hasOwnProperty.call(message, "num"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.num);
            if (message.tradable != null && Object.hasOwnProperty.call(message, "tradable"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.tradable);
            return writer;
        };

        /**
         * Encodes the specified BagItemNum message, length delimited. Does not implicitly {@link common.BagItemNum.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.BagItemNum
         * @static
         * @param {common.IBagItemNum} message BagItemNum message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BagItemNum.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BagItemNum message from the specified reader or buffer.
         * @function decode
         * @memberof common.BagItemNum
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.BagItemNum} BagItemNum
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BagItemNum.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.BagItemNum();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.num = reader.int32();
                        break;
                    }
                case 2: {
                        message.tradable = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BagItemNum message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.BagItemNum
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.BagItemNum} BagItemNum
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BagItemNum.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BagItemNum message.
         * @function verify
         * @memberof common.BagItemNum
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BagItemNum.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.num != null && message.hasOwnProperty("num"))
                if (!$util.isInteger(message.num))
                    return "num: integer expected";
            if (message.tradable != null && message.hasOwnProperty("tradable"))
                if (!$util.isInteger(message.tradable))
                    return "tradable: integer expected";
            return null;
        };

        /**
         * Creates a BagItemNum message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.BagItemNum
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.BagItemNum} BagItemNum
         */
        BagItemNum.fromObject = function fromObject(object) {
            if (object instanceof $root.common.BagItemNum)
                return object;
            var message = new $root.common.BagItemNum();
            if (object.num != null)
                message.num = object.num | 0;
            if (object.tradable != null)
                message.tradable = object.tradable | 0;
            return message;
        };

        /**
         * Creates a plain object from a BagItemNum message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.BagItemNum
         * @static
         * @param {common.BagItemNum} message BagItemNum
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BagItemNum.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.num = 0;
                object.tradable = 0;
            }
            if (message.num != null && message.hasOwnProperty("num"))
                object.num = message.num;
            if (message.tradable != null && message.hasOwnProperty("tradable"))
                object.tradable = message.tradable;
            return object;
        };

        /**
         * Converts this BagItemNum to JSON.
         * @function toJSON
         * @memberof common.BagItemNum
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BagItemNum.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BagItemNum
         * @function getTypeUrl
         * @memberof common.BagItemNum
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BagItemNum.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.BagItemNum";
        };

        return BagItemNum;
    })();

    common.Collection = (function() {

        /**
         * Properties of a Collection.
         * @memberof common
         * @interface ICollection
         * @property {number|null} [level] Collection level
         * @property {number|null} [num] Collection num
         */

        /**
         * Constructs a new Collection.
         * @memberof common
         * @classdesc Represents a Collection.
         * @implements ICollection
         * @constructor
         * @param {common.ICollection=} [properties] Properties to set
         */
        function Collection(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Collection level.
         * @member {number} level
         * @memberof common.Collection
         * @instance
         */
        Collection.prototype.level = 0;

        /**
         * Collection num.
         * @member {number} num
         * @memberof common.Collection
         * @instance
         */
        Collection.prototype.num = 0;

        /**
         * Creates a new Collection instance using the specified properties.
         * @function create
         * @memberof common.Collection
         * @static
         * @param {common.ICollection=} [properties] Properties to set
         * @returns {common.Collection} Collection instance
         */
        Collection.create = function create(properties) {
            return new Collection(properties);
        };

        /**
         * Encodes the specified Collection message. Does not implicitly {@link common.Collection.verify|verify} messages.
         * @function encode
         * @memberof common.Collection
         * @static
         * @param {common.ICollection} message Collection message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Collection.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.level);
            if (message.num != null && Object.hasOwnProperty.call(message, "num"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.num);
            return writer;
        };

        /**
         * Encodes the specified Collection message, length delimited. Does not implicitly {@link common.Collection.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.Collection
         * @static
         * @param {common.ICollection} message Collection message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Collection.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Collection message from the specified reader or buffer.
         * @function decode
         * @memberof common.Collection
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.Collection} Collection
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Collection.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.Collection();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.level = reader.int32();
                        break;
                    }
                case 2: {
                        message.num = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Collection message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.Collection
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.Collection} Collection
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Collection.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Collection message.
         * @function verify
         * @memberof common.Collection
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Collection.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.num != null && message.hasOwnProperty("num"))
                if (!$util.isInteger(message.num))
                    return "num: integer expected";
            return null;
        };

        /**
         * Creates a Collection message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.Collection
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.Collection} Collection
         */
        Collection.fromObject = function fromObject(object) {
            if (object instanceof $root.common.Collection)
                return object;
            var message = new $root.common.Collection();
            if (object.level != null)
                message.level = object.level | 0;
            if (object.num != null)
                message.num = object.num | 0;
            return message;
        };

        /**
         * Creates a plain object from a Collection message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.Collection
         * @static
         * @param {common.Collection} message Collection
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Collection.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.level = 0;
                object.num = 0;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.num != null && message.hasOwnProperty("num"))
                object.num = message.num;
            return object;
        };

        /**
         * Converts this Collection to JSON.
         * @function toJSON
         * @memberof common.Collection
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Collection.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Collection
         * @function getTypeUrl
         * @memberof common.Collection
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Collection.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.Collection";
        };

        return Collection;
    })();

    common.BagInfo = (function() {

        /**
         * Properties of a BagInfo.
         * @memberof common
         * @interface IBagInfo
         * @property {Object.<string,common.IEquipInfo>|null} [equips] BagInfo equips
         * @property {Object.<string,common.IBagItemNum>|null} [items] BagInfo items
         * @property {Object.<string,common.IBagItemNum>|null} [cards] BagInfo cards
         * @property {Object.<string,number>|null} [driftBottles] BagInfo driftBottles
         * @property {Object.<string,number>|null} [picturePuzzles] BagInfo picturePuzzles
         * @property {Object.<string,number>|null} [collections] BagInfo collections
         * @property {Object.<string,number>|null} [diyFlags] BagInfo diyFlags
         * @property {number|null} [equipVolume] BagInfo equipVolume
         * @property {number|null} [extendNum] BagInfo extendNum
         */

        /**
         * Constructs a new BagInfo.
         * @memberof common
         * @classdesc Represents a BagInfo.
         * @implements IBagInfo
         * @constructor
         * @param {common.IBagInfo=} [properties] Properties to set
         */
        function BagInfo(properties) {
            this.equips = {};
            this.items = {};
            this.cards = {};
            this.driftBottles = {};
            this.picturePuzzles = {};
            this.collections = {};
            this.diyFlags = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BagInfo equips.
         * @member {Object.<string,common.IEquipInfo>} equips
         * @memberof common.BagInfo
         * @instance
         */
        BagInfo.prototype.equips = $util.emptyObject;

        /**
         * BagInfo items.
         * @member {Object.<string,common.IBagItemNum>} items
         * @memberof common.BagInfo
         * @instance
         */
        BagInfo.prototype.items = $util.emptyObject;

        /**
         * BagInfo cards.
         * @member {Object.<string,common.IBagItemNum>} cards
         * @memberof common.BagInfo
         * @instance
         */
        BagInfo.prototype.cards = $util.emptyObject;

        /**
         * BagInfo driftBottles.
         * @member {Object.<string,number>} driftBottles
         * @memberof common.BagInfo
         * @instance
         */
        BagInfo.prototype.driftBottles = $util.emptyObject;

        /**
         * BagInfo picturePuzzles.
         * @member {Object.<string,number>} picturePuzzles
         * @memberof common.BagInfo
         * @instance
         */
        BagInfo.prototype.picturePuzzles = $util.emptyObject;

        /**
         * BagInfo collections.
         * @member {Object.<string,number>} collections
         * @memberof common.BagInfo
         * @instance
         */
        BagInfo.prototype.collections = $util.emptyObject;

        /**
         * BagInfo diyFlags.
         * @member {Object.<string,number>} diyFlags
         * @memberof common.BagInfo
         * @instance
         */
        BagInfo.prototype.diyFlags = $util.emptyObject;

        /**
         * BagInfo equipVolume.
         * @member {number} equipVolume
         * @memberof common.BagInfo
         * @instance
         */
        BagInfo.prototype.equipVolume = 0;

        /**
         * BagInfo extendNum.
         * @member {number} extendNum
         * @memberof common.BagInfo
         * @instance
         */
        BagInfo.prototype.extendNum = 0;

        /**
         * Creates a new BagInfo instance using the specified properties.
         * @function create
         * @memberof common.BagInfo
         * @static
         * @param {common.IBagInfo=} [properties] Properties to set
         * @returns {common.BagInfo} BagInfo instance
         */
        BagInfo.create = function create(properties) {
            return new BagInfo(properties);
        };

        /**
         * Encodes the specified BagInfo message. Does not implicitly {@link common.BagInfo.verify|verify} messages.
         * @function encode
         * @memberof common.BagInfo
         * @static
         * @param {common.IBagInfo} message BagInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BagInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.equips != null && Object.hasOwnProperty.call(message, "equips"))
                for (var keys = Object.keys(message.equips), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int64(keys[i]);
                    $root.common.EquipInfo.encode(message.equips[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            if (message.items != null && Object.hasOwnProperty.call(message, "items"))
                for (var keys = Object.keys(message.items), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.BagItemNum.encode(message.items[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            if (message.cards != null && Object.hasOwnProperty.call(message, "cards"))
                for (var keys = Object.keys(message.cards), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.BagItemNum.encode(message.cards[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            if (message.driftBottles != null && Object.hasOwnProperty.call(message, "driftBottles"))
                for (var keys = Object.keys(message.driftBottles), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 4, wireType 2 =*/34).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.driftBottles[keys[i]]).ldelim();
            if (message.picturePuzzles != null && Object.hasOwnProperty.call(message, "picturePuzzles"))
                for (var keys = Object.keys(message.picturePuzzles), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 5, wireType 2 =*/42).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.picturePuzzles[keys[i]]).ldelim();
            if (message.collections != null && Object.hasOwnProperty.call(message, "collections"))
                for (var keys = Object.keys(message.collections), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.collections[keys[i]]).ldelim();
            if (message.diyFlags != null && Object.hasOwnProperty.call(message, "diyFlags"))
                for (var keys = Object.keys(message.diyFlags), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 7, wireType 2 =*/58).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.diyFlags[keys[i]]).ldelim();
            if (message.equipVolume != null && Object.hasOwnProperty.call(message, "equipVolume"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.equipVolume);
            if (message.extendNum != null && Object.hasOwnProperty.call(message, "extendNum"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.extendNum);
            return writer;
        };

        /**
         * Encodes the specified BagInfo message, length delimited. Does not implicitly {@link common.BagInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.BagInfo
         * @static
         * @param {common.IBagInfo} message BagInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BagInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BagInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.BagInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.BagInfo} BagInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BagInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.BagInfo(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.equips === $util.emptyObject)
                            message.equips = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int64();
                                break;
                            case 2:
                                value = $root.common.EquipInfo.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.equips[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        break;
                    }
                case 2: {
                        if (message.items === $util.emptyObject)
                            message.items = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.BagItemNum.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.items[key] = value;
                        break;
                    }
                case 3: {
                        if (message.cards === $util.emptyObject)
                            message.cards = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.BagItemNum.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.cards[key] = value;
                        break;
                    }
                case 4: {
                        if (message.driftBottles === $util.emptyObject)
                            message.driftBottles = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int32();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.driftBottles[key] = value;
                        break;
                    }
                case 5: {
                        if (message.picturePuzzles === $util.emptyObject)
                            message.picturePuzzles = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int32();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.picturePuzzles[key] = value;
                        break;
                    }
                case 6: {
                        if (message.collections === $util.emptyObject)
                            message.collections = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int32();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.collections[key] = value;
                        break;
                    }
                case 7: {
                        if (message.diyFlags === $util.emptyObject)
                            message.diyFlags = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int32();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.diyFlags[key] = value;
                        break;
                    }
                case 8: {
                        message.equipVolume = reader.int32();
                        break;
                    }
                case 9: {
                        message.extendNum = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BagInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.BagInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.BagInfo} BagInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BagInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BagInfo message.
         * @function verify
         * @memberof common.BagInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BagInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.equips != null && message.hasOwnProperty("equips")) {
                if (!$util.isObject(message.equips))
                    return "equips: object expected";
                var key = Object.keys(message.equips);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "equips: integer|Long key{k:int64} expected";
                    {
                        var error = $root.common.EquipInfo.verify(message.equips[key[i]]);
                        if (error)
                            return "equips." + error;
                    }
                }
            }
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!$util.isObject(message.items))
                    return "items: object expected";
                var key = Object.keys(message.items);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "items: integer key{k:int32} expected";
                    {
                        var error = $root.common.BagItemNum.verify(message.items[key[i]]);
                        if (error)
                            return "items." + error;
                    }
                }
            }
            if (message.cards != null && message.hasOwnProperty("cards")) {
                if (!$util.isObject(message.cards))
                    return "cards: object expected";
                var key = Object.keys(message.cards);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "cards: integer key{k:int32} expected";
                    {
                        var error = $root.common.BagItemNum.verify(message.cards[key[i]]);
                        if (error)
                            return "cards." + error;
                    }
                }
            }
            if (message.driftBottles != null && message.hasOwnProperty("driftBottles")) {
                if (!$util.isObject(message.driftBottles))
                    return "driftBottles: object expected";
                var key = Object.keys(message.driftBottles);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "driftBottles: integer key{k:int32} expected";
                    if (!$util.isInteger(message.driftBottles[key[i]]))
                        return "driftBottles: integer{k:int32} expected";
                }
            }
            if (message.picturePuzzles != null && message.hasOwnProperty("picturePuzzles")) {
                if (!$util.isObject(message.picturePuzzles))
                    return "picturePuzzles: object expected";
                var key = Object.keys(message.picturePuzzles);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "picturePuzzles: integer key{k:int32} expected";
                    if (!$util.isInteger(message.picturePuzzles[key[i]]))
                        return "picturePuzzles: integer{k:int32} expected";
                }
            }
            if (message.collections != null && message.hasOwnProperty("collections")) {
                if (!$util.isObject(message.collections))
                    return "collections: object expected";
                var key = Object.keys(message.collections);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "collections: integer key{k:int32} expected";
                    if (!$util.isInteger(message.collections[key[i]]))
                        return "collections: integer{k:int32} expected";
                }
            }
            if (message.diyFlags != null && message.hasOwnProperty("diyFlags")) {
                if (!$util.isObject(message.diyFlags))
                    return "diyFlags: object expected";
                var key = Object.keys(message.diyFlags);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "diyFlags: integer key{k:int32} expected";
                    if (!$util.isInteger(message.diyFlags[key[i]]))
                        return "diyFlags: integer{k:int32} expected";
                }
            }
            if (message.equipVolume != null && message.hasOwnProperty("equipVolume"))
                if (!$util.isInteger(message.equipVolume))
                    return "equipVolume: integer expected";
            if (message.extendNum != null && message.hasOwnProperty("extendNum"))
                if (!$util.isInteger(message.extendNum))
                    return "extendNum: integer expected";
            return null;
        };

        /**
         * Creates a BagInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.BagInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.BagInfo} BagInfo
         */
        BagInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.BagInfo)
                return object;
            var message = new $root.common.BagInfo();
            if (object.equips) {
                if (typeof object.equips !== "object")
                    throw TypeError(".common.BagInfo.equips: object expected");
                message.equips = {};
                for (var keys = Object.keys(object.equips), i = 0; i < keys.length; ++i) {
                    if (typeof object.equips[keys[i]] !== "object")
                        throw TypeError(".common.BagInfo.equips: object expected");
                    message.equips[keys[i]] = $root.common.EquipInfo.fromObject(object.equips[keys[i]]);
                }
            }
            if (object.items) {
                if (typeof object.items !== "object")
                    throw TypeError(".common.BagInfo.items: object expected");
                message.items = {};
                for (var keys = Object.keys(object.items), i = 0; i < keys.length; ++i) {
                    if (typeof object.items[keys[i]] !== "object")
                        throw TypeError(".common.BagInfo.items: object expected");
                    message.items[keys[i]] = $root.common.BagItemNum.fromObject(object.items[keys[i]]);
                }
            }
            if (object.cards) {
                if (typeof object.cards !== "object")
                    throw TypeError(".common.BagInfo.cards: object expected");
                message.cards = {};
                for (var keys = Object.keys(object.cards), i = 0; i < keys.length; ++i) {
                    if (typeof object.cards[keys[i]] !== "object")
                        throw TypeError(".common.BagInfo.cards: object expected");
                    message.cards[keys[i]] = $root.common.BagItemNum.fromObject(object.cards[keys[i]]);
                }
            }
            if (object.driftBottles) {
                if (typeof object.driftBottles !== "object")
                    throw TypeError(".common.BagInfo.driftBottles: object expected");
                message.driftBottles = {};
                for (var keys = Object.keys(object.driftBottles), i = 0; i < keys.length; ++i)
                    message.driftBottles[keys[i]] = object.driftBottles[keys[i]] | 0;
            }
            if (object.picturePuzzles) {
                if (typeof object.picturePuzzles !== "object")
                    throw TypeError(".common.BagInfo.picturePuzzles: object expected");
                message.picturePuzzles = {};
                for (var keys = Object.keys(object.picturePuzzles), i = 0; i < keys.length; ++i)
                    message.picturePuzzles[keys[i]] = object.picturePuzzles[keys[i]] | 0;
            }
            if (object.collections) {
                if (typeof object.collections !== "object")
                    throw TypeError(".common.BagInfo.collections: object expected");
                message.collections = {};
                for (var keys = Object.keys(object.collections), i = 0; i < keys.length; ++i)
                    message.collections[keys[i]] = object.collections[keys[i]] | 0;
            }
            if (object.diyFlags) {
                if (typeof object.diyFlags !== "object")
                    throw TypeError(".common.BagInfo.diyFlags: object expected");
                message.diyFlags = {};
                for (var keys = Object.keys(object.diyFlags), i = 0; i < keys.length; ++i)
                    message.diyFlags[keys[i]] = object.diyFlags[keys[i]] | 0;
            }
            if (object.equipVolume != null)
                message.equipVolume = object.equipVolume | 0;
            if (object.extendNum != null)
                message.extendNum = object.extendNum | 0;
            return message;
        };

        /**
         * Creates a plain object from a BagInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.BagInfo
         * @static
         * @param {common.BagInfo} message BagInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BagInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.objects || options.defaults) {
                object.equips = {};
                object.items = {};
                object.cards = {};
                object.driftBottles = {};
                object.picturePuzzles = {};
                object.collections = {};
                object.diyFlags = {};
            }
            if (options.defaults) {
                object.equipVolume = 0;
                object.extendNum = 0;
            }
            var keys2;
            if (message.equips && (keys2 = Object.keys(message.equips)).length) {
                object.equips = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.equips[keys2[j]] = $root.common.EquipInfo.toObject(message.equips[keys2[j]], options);
            }
            if (message.items && (keys2 = Object.keys(message.items)).length) {
                object.items = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.items[keys2[j]] = $root.common.BagItemNum.toObject(message.items[keys2[j]], options);
            }
            if (message.cards && (keys2 = Object.keys(message.cards)).length) {
                object.cards = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.cards[keys2[j]] = $root.common.BagItemNum.toObject(message.cards[keys2[j]], options);
            }
            if (message.driftBottles && (keys2 = Object.keys(message.driftBottles)).length) {
                object.driftBottles = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.driftBottles[keys2[j]] = message.driftBottles[keys2[j]];
            }
            if (message.picturePuzzles && (keys2 = Object.keys(message.picturePuzzles)).length) {
                object.picturePuzzles = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.picturePuzzles[keys2[j]] = message.picturePuzzles[keys2[j]];
            }
            if (message.collections && (keys2 = Object.keys(message.collections)).length) {
                object.collections = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.collections[keys2[j]] = message.collections[keys2[j]];
            }
            if (message.diyFlags && (keys2 = Object.keys(message.diyFlags)).length) {
                object.diyFlags = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.diyFlags[keys2[j]] = message.diyFlags[keys2[j]];
            }
            if (message.equipVolume != null && message.hasOwnProperty("equipVolume"))
                object.equipVolume = message.equipVolume;
            if (message.extendNum != null && message.hasOwnProperty("extendNum"))
                object.extendNum = message.extendNum;
            return object;
        };

        /**
         * Converts this BagInfo to JSON.
         * @function toJSON
         * @memberof common.BagInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BagInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BagInfo
         * @function getTypeUrl
         * @memberof common.BagInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BagInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.BagInfo";
        };

        return BagInfo;
    })();

    common.GoodsRecordInfo = (function() {

        /**
         * Properties of a GoodsRecordInfo.
         * @memberof common
         * @interface IGoodsRecordInfo
         * @property {number|null} [buyTimes] GoodsRecordInfo buyTimes
         * @property {number|null} [totalBuyTimes] GoodsRecordInfo totalBuyTimes
         * @property {number|null} [requestTimes] GoodsRecordInfo requestTimes
         * @property {number|Long|null} [refreshTime] GoodsRecordInfo refreshTime
         * @property {string|null} [awards] GoodsRecordInfo awards
         */

        /**
         * Constructs a new GoodsRecordInfo.
         * @memberof common
         * @classdesc Represents a GoodsRecordInfo.
         * @implements IGoodsRecordInfo
         * @constructor
         * @param {common.IGoodsRecordInfo=} [properties] Properties to set
         */
        function GoodsRecordInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GoodsRecordInfo buyTimes.
         * @member {number} buyTimes
         * @memberof common.GoodsRecordInfo
         * @instance
         */
        GoodsRecordInfo.prototype.buyTimes = 0;

        /**
         * GoodsRecordInfo totalBuyTimes.
         * @member {number} totalBuyTimes
         * @memberof common.GoodsRecordInfo
         * @instance
         */
        GoodsRecordInfo.prototype.totalBuyTimes = 0;

        /**
         * GoodsRecordInfo requestTimes.
         * @member {number} requestTimes
         * @memberof common.GoodsRecordInfo
         * @instance
         */
        GoodsRecordInfo.prototype.requestTimes = 0;

        /**
         * GoodsRecordInfo refreshTime.
         * @member {number|Long} refreshTime
         * @memberof common.GoodsRecordInfo
         * @instance
         */
        GoodsRecordInfo.prototype.refreshTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GoodsRecordInfo awards.
         * @member {string} awards
         * @memberof common.GoodsRecordInfo
         * @instance
         */
        GoodsRecordInfo.prototype.awards = "";

        /**
         * Creates a new GoodsRecordInfo instance using the specified properties.
         * @function create
         * @memberof common.GoodsRecordInfo
         * @static
         * @param {common.IGoodsRecordInfo=} [properties] Properties to set
         * @returns {common.GoodsRecordInfo} GoodsRecordInfo instance
         */
        GoodsRecordInfo.create = function create(properties) {
            return new GoodsRecordInfo(properties);
        };

        /**
         * Encodes the specified GoodsRecordInfo message. Does not implicitly {@link common.GoodsRecordInfo.verify|verify} messages.
         * @function encode
         * @memberof common.GoodsRecordInfo
         * @static
         * @param {common.IGoodsRecordInfo} message GoodsRecordInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GoodsRecordInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.buyTimes != null && Object.hasOwnProperty.call(message, "buyTimes"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.buyTimes);
            if (message.totalBuyTimes != null && Object.hasOwnProperty.call(message, "totalBuyTimes"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.totalBuyTimes);
            if (message.requestTimes != null && Object.hasOwnProperty.call(message, "requestTimes"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.requestTimes);
            if (message.refreshTime != null && Object.hasOwnProperty.call(message, "refreshTime"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.refreshTime);
            if (message.awards != null && Object.hasOwnProperty.call(message, "awards"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.awards);
            return writer;
        };

        /**
         * Encodes the specified GoodsRecordInfo message, length delimited. Does not implicitly {@link common.GoodsRecordInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.GoodsRecordInfo
         * @static
         * @param {common.IGoodsRecordInfo} message GoodsRecordInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GoodsRecordInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GoodsRecordInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.GoodsRecordInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.GoodsRecordInfo} GoodsRecordInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GoodsRecordInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.GoodsRecordInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.buyTimes = reader.int32();
                        break;
                    }
                case 2: {
                        message.totalBuyTimes = reader.int32();
                        break;
                    }
                case 3: {
                        message.requestTimes = reader.int32();
                        break;
                    }
                case 4: {
                        message.refreshTime = reader.int64();
                        break;
                    }
                case 5: {
                        message.awards = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GoodsRecordInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.GoodsRecordInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.GoodsRecordInfo} GoodsRecordInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GoodsRecordInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GoodsRecordInfo message.
         * @function verify
         * @memberof common.GoodsRecordInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GoodsRecordInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.buyTimes != null && message.hasOwnProperty("buyTimes"))
                if (!$util.isInteger(message.buyTimes))
                    return "buyTimes: integer expected";
            if (message.totalBuyTimes != null && message.hasOwnProperty("totalBuyTimes"))
                if (!$util.isInteger(message.totalBuyTimes))
                    return "totalBuyTimes: integer expected";
            if (message.requestTimes != null && message.hasOwnProperty("requestTimes"))
                if (!$util.isInteger(message.requestTimes))
                    return "requestTimes: integer expected";
            if (message.refreshTime != null && message.hasOwnProperty("refreshTime"))
                if (!$util.isInteger(message.refreshTime) && !(message.refreshTime && $util.isInteger(message.refreshTime.low) && $util.isInteger(message.refreshTime.high)))
                    return "refreshTime: integer|Long expected";
            if (message.awards != null && message.hasOwnProperty("awards"))
                if (!$util.isString(message.awards))
                    return "awards: string expected";
            return null;
        };

        /**
         * Creates a GoodsRecordInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.GoodsRecordInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.GoodsRecordInfo} GoodsRecordInfo
         */
        GoodsRecordInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.GoodsRecordInfo)
                return object;
            var message = new $root.common.GoodsRecordInfo();
            if (object.buyTimes != null)
                message.buyTimes = object.buyTimes | 0;
            if (object.totalBuyTimes != null)
                message.totalBuyTimes = object.totalBuyTimes | 0;
            if (object.requestTimes != null)
                message.requestTimes = object.requestTimes | 0;
            if (object.refreshTime != null)
                if ($util.Long)
                    (message.refreshTime = $util.Long.fromValue(object.refreshTime)).unsigned = false;
                else if (typeof object.refreshTime === "string")
                    message.refreshTime = parseInt(object.refreshTime, 10);
                else if (typeof object.refreshTime === "number")
                    message.refreshTime = object.refreshTime;
                else if (typeof object.refreshTime === "object")
                    message.refreshTime = new $util.LongBits(object.refreshTime.low >>> 0, object.refreshTime.high >>> 0).toNumber();
            if (object.awards != null)
                message.awards = String(object.awards);
            return message;
        };

        /**
         * Creates a plain object from a GoodsRecordInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.GoodsRecordInfo
         * @static
         * @param {common.GoodsRecordInfo} message GoodsRecordInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GoodsRecordInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.buyTimes = 0;
                object.totalBuyTimes = 0;
                object.requestTimes = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.refreshTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.refreshTime = options.longs === String ? "0" : 0;
                object.awards = "";
            }
            if (message.buyTimes != null && message.hasOwnProperty("buyTimes"))
                object.buyTimes = message.buyTimes;
            if (message.totalBuyTimes != null && message.hasOwnProperty("totalBuyTimes"))
                object.totalBuyTimes = message.totalBuyTimes;
            if (message.requestTimes != null && message.hasOwnProperty("requestTimes"))
                object.requestTimes = message.requestTimes;
            if (message.refreshTime != null && message.hasOwnProperty("refreshTime"))
                if (typeof message.refreshTime === "number")
                    object.refreshTime = options.longs === String ? String(message.refreshTime) : message.refreshTime;
                else
                    object.refreshTime = options.longs === String ? $util.Long.prototype.toString.call(message.refreshTime) : options.longs === Number ? new $util.LongBits(message.refreshTime.low >>> 0, message.refreshTime.high >>> 0).toNumber() : message.refreshTime;
            if (message.awards != null && message.hasOwnProperty("awards"))
                object.awards = message.awards;
            return object;
        };

        /**
         * Converts this GoodsRecordInfo to JSON.
         * @function toJSON
         * @memberof common.GoodsRecordInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GoodsRecordInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GoodsRecordInfo
         * @function getTypeUrl
         * @memberof common.GoodsRecordInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GoodsRecordInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.GoodsRecordInfo";
        };

        return GoodsRecordInfo;
    })();

    common.ShopInfo = (function() {

        /**
         * Properties of a ShopInfo.
         * @memberof common
         * @interface IShopInfo
         * @property {Object.<string,common.IGoodsRecordInfo>|null} [goods] ShopInfo goods
         */

        /**
         * Constructs a new ShopInfo.
         * @memberof common
         * @classdesc Represents a ShopInfo.
         * @implements IShopInfo
         * @constructor
         * @param {common.IShopInfo=} [properties] Properties to set
         */
        function ShopInfo(properties) {
            this.goods = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ShopInfo goods.
         * @member {Object.<string,common.IGoodsRecordInfo>} goods
         * @memberof common.ShopInfo
         * @instance
         */
        ShopInfo.prototype.goods = $util.emptyObject;

        /**
         * Creates a new ShopInfo instance using the specified properties.
         * @function create
         * @memberof common.ShopInfo
         * @static
         * @param {common.IShopInfo=} [properties] Properties to set
         * @returns {common.ShopInfo} ShopInfo instance
         */
        ShopInfo.create = function create(properties) {
            return new ShopInfo(properties);
        };

        /**
         * Encodes the specified ShopInfo message. Does not implicitly {@link common.ShopInfo.verify|verify} messages.
         * @function encode
         * @memberof common.ShopInfo
         * @static
         * @param {common.IShopInfo} message ShopInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ShopInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods != null && Object.hasOwnProperty.call(message, "goods"))
                for (var keys = Object.keys(message.goods), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.GoodsRecordInfo.encode(message.goods[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            return writer;
        };

        /**
         * Encodes the specified ShopInfo message, length delimited. Does not implicitly {@link common.ShopInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.ShopInfo
         * @static
         * @param {common.IShopInfo} message ShopInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ShopInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ShopInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.ShopInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.ShopInfo} ShopInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ShopInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.ShopInfo(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.goods === $util.emptyObject)
                            message.goods = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.GoodsRecordInfo.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.goods[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ShopInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.ShopInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.ShopInfo} ShopInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ShopInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ShopInfo message.
         * @function verify
         * @memberof common.ShopInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ShopInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.goods != null && message.hasOwnProperty("goods")) {
                if (!$util.isObject(message.goods))
                    return "goods: object expected";
                var key = Object.keys(message.goods);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "goods: integer key{k:int32} expected";
                    {
                        var error = $root.common.GoodsRecordInfo.verify(message.goods[key[i]]);
                        if (error)
                            return "goods." + error;
                    }
                }
            }
            return null;
        };

        /**
         * Creates a ShopInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.ShopInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.ShopInfo} ShopInfo
         */
        ShopInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.ShopInfo)
                return object;
            var message = new $root.common.ShopInfo();
            if (object.goods) {
                if (typeof object.goods !== "object")
                    throw TypeError(".common.ShopInfo.goods: object expected");
                message.goods = {};
                for (var keys = Object.keys(object.goods), i = 0; i < keys.length; ++i) {
                    if (typeof object.goods[keys[i]] !== "object")
                        throw TypeError(".common.ShopInfo.goods: object expected");
                    message.goods[keys[i]] = $root.common.GoodsRecordInfo.fromObject(object.goods[keys[i]]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ShopInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.ShopInfo
         * @static
         * @param {common.ShopInfo} message ShopInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ShopInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.objects || options.defaults)
                object.goods = {};
            var keys2;
            if (message.goods && (keys2 = Object.keys(message.goods)).length) {
                object.goods = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.goods[keys2[j]] = $root.common.GoodsRecordInfo.toObject(message.goods[keys2[j]], options);
            }
            return object;
        };

        /**
         * Converts this ShopInfo to JSON.
         * @function toJSON
         * @memberof common.ShopInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ShopInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ShopInfo
         * @function getTypeUrl
         * @memberof common.ShopInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ShopInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.ShopInfo";
        };

        return ShopInfo;
    })();

    common.TaskFreeAward = (function() {

        /**
         * Properties of a TaskFreeAward.
         * @memberof common
         * @interface ITaskFreeAward
         * @property {Array.<number>|null} [ids] TaskFreeAward ids
         * @property {number|null} [getIndex] TaskFreeAward getIndex
         * @property {number|Long|null} [getTime] TaskFreeAward getTime
         */

        /**
         * Constructs a new TaskFreeAward.
         * @memberof common
         * @classdesc Represents a TaskFreeAward.
         * @implements ITaskFreeAward
         * @constructor
         * @param {common.ITaskFreeAward=} [properties] Properties to set
         */
        function TaskFreeAward(properties) {
            this.ids = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TaskFreeAward ids.
         * @member {Array.<number>} ids
         * @memberof common.TaskFreeAward
         * @instance
         */
        TaskFreeAward.prototype.ids = $util.emptyArray;

        /**
         * TaskFreeAward getIndex.
         * @member {number} getIndex
         * @memberof common.TaskFreeAward
         * @instance
         */
        TaskFreeAward.prototype.getIndex = 0;

        /**
         * TaskFreeAward getTime.
         * @member {number|Long} getTime
         * @memberof common.TaskFreeAward
         * @instance
         */
        TaskFreeAward.prototype.getTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new TaskFreeAward instance using the specified properties.
         * @function create
         * @memberof common.TaskFreeAward
         * @static
         * @param {common.ITaskFreeAward=} [properties] Properties to set
         * @returns {common.TaskFreeAward} TaskFreeAward instance
         */
        TaskFreeAward.create = function create(properties) {
            return new TaskFreeAward(properties);
        };

        /**
         * Encodes the specified TaskFreeAward message. Does not implicitly {@link common.TaskFreeAward.verify|verify} messages.
         * @function encode
         * @memberof common.TaskFreeAward
         * @static
         * @param {common.ITaskFreeAward} message TaskFreeAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TaskFreeAward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ids != null && message.ids.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (var i = 0; i < message.ids.length; ++i)
                    writer.int32(message.ids[i]);
                writer.ldelim();
            }
            if (message.getIndex != null && Object.hasOwnProperty.call(message, "getIndex"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.getIndex);
            if (message.getTime != null && Object.hasOwnProperty.call(message, "getTime"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.getTime);
            return writer;
        };

        /**
         * Encodes the specified TaskFreeAward message, length delimited. Does not implicitly {@link common.TaskFreeAward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.TaskFreeAward
         * @static
         * @param {common.ITaskFreeAward} message TaskFreeAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TaskFreeAward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TaskFreeAward message from the specified reader or buffer.
         * @function decode
         * @memberof common.TaskFreeAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.TaskFreeAward} TaskFreeAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TaskFreeAward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.TaskFreeAward();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ids && message.ids.length))
                            message.ids = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ids.push(reader.int32());
                        } else
                            message.ids.push(reader.int32());
                        break;
                    }
                case 2: {
                        message.getIndex = reader.int32();
                        break;
                    }
                case 3: {
                        message.getTime = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TaskFreeAward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.TaskFreeAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.TaskFreeAward} TaskFreeAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TaskFreeAward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TaskFreeAward message.
         * @function verify
         * @memberof common.TaskFreeAward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TaskFreeAward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ids != null && message.hasOwnProperty("ids")) {
                if (!Array.isArray(message.ids))
                    return "ids: array expected";
                for (var i = 0; i < message.ids.length; ++i)
                    if (!$util.isInteger(message.ids[i]))
                        return "ids: integer[] expected";
            }
            if (message.getIndex != null && message.hasOwnProperty("getIndex"))
                if (!$util.isInteger(message.getIndex))
                    return "getIndex: integer expected";
            if (message.getTime != null && message.hasOwnProperty("getTime"))
                if (!$util.isInteger(message.getTime) && !(message.getTime && $util.isInteger(message.getTime.low) && $util.isInteger(message.getTime.high)))
                    return "getTime: integer|Long expected";
            return null;
        };

        /**
         * Creates a TaskFreeAward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.TaskFreeAward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.TaskFreeAward} TaskFreeAward
         */
        TaskFreeAward.fromObject = function fromObject(object) {
            if (object instanceof $root.common.TaskFreeAward)
                return object;
            var message = new $root.common.TaskFreeAward();
            if (object.ids) {
                if (!Array.isArray(object.ids))
                    throw TypeError(".common.TaskFreeAward.ids: array expected");
                message.ids = [];
                for (var i = 0; i < object.ids.length; ++i)
                    message.ids[i] = object.ids[i] | 0;
            }
            if (object.getIndex != null)
                message.getIndex = object.getIndex | 0;
            if (object.getTime != null)
                if ($util.Long)
                    (message.getTime = $util.Long.fromValue(object.getTime)).unsigned = false;
                else if (typeof object.getTime === "string")
                    message.getTime = parseInt(object.getTime, 10);
                else if (typeof object.getTime === "number")
                    message.getTime = object.getTime;
                else if (typeof object.getTime === "object")
                    message.getTime = new $util.LongBits(object.getTime.low >>> 0, object.getTime.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a TaskFreeAward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.TaskFreeAward
         * @static
         * @param {common.TaskFreeAward} message TaskFreeAward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TaskFreeAward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.ids = [];
            if (options.defaults) {
                object.getIndex = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.getTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.getTime = options.longs === String ? "0" : 0;
            }
            if (message.ids && message.ids.length) {
                object.ids = [];
                for (var j = 0; j < message.ids.length; ++j)
                    object.ids[j] = message.ids[j];
            }
            if (message.getIndex != null && message.hasOwnProperty("getIndex"))
                object.getIndex = message.getIndex;
            if (message.getTime != null && message.hasOwnProperty("getTime"))
                if (typeof message.getTime === "number")
                    object.getTime = options.longs === String ? String(message.getTime) : message.getTime;
                else
                    object.getTime = options.longs === String ? $util.Long.prototype.toString.call(message.getTime) : options.longs === Number ? new $util.LongBits(message.getTime.low >>> 0, message.getTime.high >>> 0).toNumber() : message.getTime;
            return object;
        };

        /**
         * Converts this TaskFreeAward to JSON.
         * @function toJSON
         * @memberof common.TaskFreeAward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TaskFreeAward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TaskFreeAward
         * @function getTypeUrl
         * @memberof common.TaskFreeAward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TaskFreeAward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.TaskFreeAward";
        };

        return TaskFreeAward;
    })();

    common.TaskInfo = (function() {

        /**
         * Properties of a TaskInfo.
         * @memberof common
         * @interface ITaskInfo
         * @property {number|null} [awardPoint] TaskInfo awardPoint
         * @property {number|null} [box] TaskInfo box
         * @property {common.ITaskFreeAward|null} [free] TaskInfo free
         * @property {number|null} [refreshNum] TaskInfo refreshNum
         * @property {Array.<number>|null} [boxSeq] TaskInfo boxSeq
         * @property {string|null} [boxSeqVer] TaskInfo boxSeqVer
         * @property {Array.<number>|null} [freeSeq] TaskInfo freeSeq
         * @property {string|null} [freeSeqVer] TaskInfo freeSeqVer
         * @property {Object.<string,number>|null} [taskList] TaskInfo taskList
         */

        /**
         * Constructs a new TaskInfo.
         * @memberof common
         * @classdesc Represents a TaskInfo.
         * @implements ITaskInfo
         * @constructor
         * @param {common.ITaskInfo=} [properties] Properties to set
         */
        function TaskInfo(properties) {
            this.boxSeq = [];
            this.freeSeq = [];
            this.taskList = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TaskInfo awardPoint.
         * @member {number} awardPoint
         * @memberof common.TaskInfo
         * @instance
         */
        TaskInfo.prototype.awardPoint = 0;

        /**
         * TaskInfo box.
         * @member {number} box
         * @memberof common.TaskInfo
         * @instance
         */
        TaskInfo.prototype.box = 0;

        /**
         * TaskInfo free.
         * @member {common.ITaskFreeAward|null|undefined} free
         * @memberof common.TaskInfo
         * @instance
         */
        TaskInfo.prototype.free = null;

        /**
         * TaskInfo refreshNum.
         * @member {number} refreshNum
         * @memberof common.TaskInfo
         * @instance
         */
        TaskInfo.prototype.refreshNum = 0;

        /**
         * TaskInfo boxSeq.
         * @member {Array.<number>} boxSeq
         * @memberof common.TaskInfo
         * @instance
         */
        TaskInfo.prototype.boxSeq = $util.emptyArray;

        /**
         * TaskInfo boxSeqVer.
         * @member {string} boxSeqVer
         * @memberof common.TaskInfo
         * @instance
         */
        TaskInfo.prototype.boxSeqVer = "";

        /**
         * TaskInfo freeSeq.
         * @member {Array.<number>} freeSeq
         * @memberof common.TaskInfo
         * @instance
         */
        TaskInfo.prototype.freeSeq = $util.emptyArray;

        /**
         * TaskInfo freeSeqVer.
         * @member {string} freeSeqVer
         * @memberof common.TaskInfo
         * @instance
         */
        TaskInfo.prototype.freeSeqVer = "";

        /**
         * TaskInfo taskList.
         * @member {Object.<string,number>} taskList
         * @memberof common.TaskInfo
         * @instance
         */
        TaskInfo.prototype.taskList = $util.emptyObject;

        /**
         * Creates a new TaskInfo instance using the specified properties.
         * @function create
         * @memberof common.TaskInfo
         * @static
         * @param {common.ITaskInfo=} [properties] Properties to set
         * @returns {common.TaskInfo} TaskInfo instance
         */
        TaskInfo.create = function create(properties) {
            return new TaskInfo(properties);
        };

        /**
         * Encodes the specified TaskInfo message. Does not implicitly {@link common.TaskInfo.verify|verify} messages.
         * @function encode
         * @memberof common.TaskInfo
         * @static
         * @param {common.ITaskInfo} message TaskInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TaskInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awardPoint != null && Object.hasOwnProperty.call(message, "awardPoint"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.awardPoint);
            if (message.box != null && Object.hasOwnProperty.call(message, "box"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.box);
            if (message.free != null && Object.hasOwnProperty.call(message, "free"))
                $root.common.TaskFreeAward.encode(message.free, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.refreshNum != null && Object.hasOwnProperty.call(message, "refreshNum"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.refreshNum);
            if (message.boxSeq != null && message.boxSeq.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (var i = 0; i < message.boxSeq.length; ++i)
                    writer.int32(message.boxSeq[i]);
                writer.ldelim();
            }
            if (message.boxSeqVer != null && Object.hasOwnProperty.call(message, "boxSeqVer"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.boxSeqVer);
            if (message.freeSeq != null && message.freeSeq.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (var i = 0; i < message.freeSeq.length; ++i)
                    writer.int32(message.freeSeq[i]);
                writer.ldelim();
            }
            if (message.freeSeqVer != null && Object.hasOwnProperty.call(message, "freeSeqVer"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.freeSeqVer);
            if (message.taskList != null && Object.hasOwnProperty.call(message, "taskList"))
                for (var keys = Object.keys(message.taskList), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 9, wireType 2 =*/74).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.taskList[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified TaskInfo message, length delimited. Does not implicitly {@link common.TaskInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.TaskInfo
         * @static
         * @param {common.ITaskInfo} message TaskInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TaskInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TaskInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.TaskInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.TaskInfo} TaskInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TaskInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.TaskInfo(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.awardPoint = reader.int32();
                        break;
                    }
                case 2: {
                        message.box = reader.int32();
                        break;
                    }
                case 3: {
                        message.free = $root.common.TaskFreeAward.decode(reader, reader.uint32());
                        break;
                    }
                case 4: {
                        message.refreshNum = reader.int32();
                        break;
                    }
                case 5: {
                        if (!(message.boxSeq && message.boxSeq.length))
                            message.boxSeq = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.boxSeq.push(reader.int32());
                        } else
                            message.boxSeq.push(reader.int32());
                        break;
                    }
                case 6: {
                        message.boxSeqVer = reader.string();
                        break;
                    }
                case 7: {
                        if (!(message.freeSeq && message.freeSeq.length))
                            message.freeSeq = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.freeSeq.push(reader.int32());
                        } else
                            message.freeSeq.push(reader.int32());
                        break;
                    }
                case 8: {
                        message.freeSeqVer = reader.string();
                        break;
                    }
                case 9: {
                        if (message.taskList === $util.emptyObject)
                            message.taskList = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int32();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.taskList[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TaskInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.TaskInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.TaskInfo} TaskInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TaskInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TaskInfo message.
         * @function verify
         * @memberof common.TaskInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TaskInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.awardPoint != null && message.hasOwnProperty("awardPoint"))
                if (!$util.isInteger(message.awardPoint))
                    return "awardPoint: integer expected";
            if (message.box != null && message.hasOwnProperty("box"))
                if (!$util.isInteger(message.box))
                    return "box: integer expected";
            if (message.free != null && message.hasOwnProperty("free")) {
                var error = $root.common.TaskFreeAward.verify(message.free);
                if (error)
                    return "free." + error;
            }
            if (message.refreshNum != null && message.hasOwnProperty("refreshNum"))
                if (!$util.isInteger(message.refreshNum))
                    return "refreshNum: integer expected";
            if (message.boxSeq != null && message.hasOwnProperty("boxSeq")) {
                if (!Array.isArray(message.boxSeq))
                    return "boxSeq: array expected";
                for (var i = 0; i < message.boxSeq.length; ++i)
                    if (!$util.isInteger(message.boxSeq[i]))
                        return "boxSeq: integer[] expected";
            }
            if (message.boxSeqVer != null && message.hasOwnProperty("boxSeqVer"))
                if (!$util.isString(message.boxSeqVer))
                    return "boxSeqVer: string expected";
            if (message.freeSeq != null && message.hasOwnProperty("freeSeq")) {
                if (!Array.isArray(message.freeSeq))
                    return "freeSeq: array expected";
                for (var i = 0; i < message.freeSeq.length; ++i)
                    if (!$util.isInteger(message.freeSeq[i]))
                        return "freeSeq: integer[] expected";
            }
            if (message.freeSeqVer != null && message.hasOwnProperty("freeSeqVer"))
                if (!$util.isString(message.freeSeqVer))
                    return "freeSeqVer: string expected";
            if (message.taskList != null && message.hasOwnProperty("taskList")) {
                if (!$util.isObject(message.taskList))
                    return "taskList: object expected";
                var key = Object.keys(message.taskList);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "taskList: integer key{k:int32} expected";
                    if (!$util.isInteger(message.taskList[key[i]]))
                        return "taskList: integer{k:int32} expected";
                }
            }
            return null;
        };

        /**
         * Creates a TaskInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.TaskInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.TaskInfo} TaskInfo
         */
        TaskInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.TaskInfo)
                return object;
            var message = new $root.common.TaskInfo();
            if (object.awardPoint != null)
                message.awardPoint = object.awardPoint | 0;
            if (object.box != null)
                message.box = object.box | 0;
            if (object.free != null) {
                if (typeof object.free !== "object")
                    throw TypeError(".common.TaskInfo.free: object expected");
                message.free = $root.common.TaskFreeAward.fromObject(object.free);
            }
            if (object.refreshNum != null)
                message.refreshNum = object.refreshNum | 0;
            if (object.boxSeq) {
                if (!Array.isArray(object.boxSeq))
                    throw TypeError(".common.TaskInfo.boxSeq: array expected");
                message.boxSeq = [];
                for (var i = 0; i < object.boxSeq.length; ++i)
                    message.boxSeq[i] = object.boxSeq[i] | 0;
            }
            if (object.boxSeqVer != null)
                message.boxSeqVer = String(object.boxSeqVer);
            if (object.freeSeq) {
                if (!Array.isArray(object.freeSeq))
                    throw TypeError(".common.TaskInfo.freeSeq: array expected");
                message.freeSeq = [];
                for (var i = 0; i < object.freeSeq.length; ++i)
                    message.freeSeq[i] = object.freeSeq[i] | 0;
            }
            if (object.freeSeqVer != null)
                message.freeSeqVer = String(object.freeSeqVer);
            if (object.taskList) {
                if (typeof object.taskList !== "object")
                    throw TypeError(".common.TaskInfo.taskList: object expected");
                message.taskList = {};
                for (var keys = Object.keys(object.taskList), i = 0; i < keys.length; ++i)
                    message.taskList[keys[i]] = object.taskList[keys[i]] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a TaskInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.TaskInfo
         * @static
         * @param {common.TaskInfo} message TaskInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TaskInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.boxSeq = [];
                object.freeSeq = [];
            }
            if (options.objects || options.defaults)
                object.taskList = {};
            if (options.defaults) {
                object.awardPoint = 0;
                object.box = 0;
                object.free = null;
                object.refreshNum = 0;
                object.boxSeqVer = "";
                object.freeSeqVer = "";
            }
            if (message.awardPoint != null && message.hasOwnProperty("awardPoint"))
                object.awardPoint = message.awardPoint;
            if (message.box != null && message.hasOwnProperty("box"))
                object.box = message.box;
            if (message.free != null && message.hasOwnProperty("free"))
                object.free = $root.common.TaskFreeAward.toObject(message.free, options);
            if (message.refreshNum != null && message.hasOwnProperty("refreshNum"))
                object.refreshNum = message.refreshNum;
            if (message.boxSeq && message.boxSeq.length) {
                object.boxSeq = [];
                for (var j = 0; j < message.boxSeq.length; ++j)
                    object.boxSeq[j] = message.boxSeq[j];
            }
            if (message.boxSeqVer != null && message.hasOwnProperty("boxSeqVer"))
                object.boxSeqVer = message.boxSeqVer;
            if (message.freeSeq && message.freeSeq.length) {
                object.freeSeq = [];
                for (var j = 0; j < message.freeSeq.length; ++j)
                    object.freeSeq[j] = message.freeSeq[j];
            }
            if (message.freeSeqVer != null && message.hasOwnProperty("freeSeqVer"))
                object.freeSeqVer = message.freeSeqVer;
            var keys2;
            if (message.taskList && (keys2 = Object.keys(message.taskList)).length) {
                object.taskList = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.taskList[keys2[j]] = message.taskList[keys2[j]];
            }
            return object;
        };

        /**
         * Converts this TaskInfo to JSON.
         * @function toJSON
         * @memberof common.TaskInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TaskInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TaskInfo
         * @function getTypeUrl
         * @memberof common.TaskInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TaskInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.TaskInfo";
        };

        return TaskInfo;
    })();

    common.WaitEventData = (function() {

        /**
         * Properties of a WaitEventData.
         * @memberof common
         * @interface IWaitEventData
         * @property {number|null} [id] WaitEventData id
         * @property {number|null} [team] WaitEventData team
         * @property {number|null} [tool] WaitEventData tool
         */

        /**
         * Constructs a new WaitEventData.
         * @memberof common
         * @classdesc Represents a WaitEventData.
         * @implements IWaitEventData
         * @constructor
         * @param {common.IWaitEventData=} [properties] Properties to set
         */
        function WaitEventData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WaitEventData id.
         * @member {number} id
         * @memberof common.WaitEventData
         * @instance
         */
        WaitEventData.prototype.id = 0;

        /**
         * WaitEventData team.
         * @member {number} team
         * @memberof common.WaitEventData
         * @instance
         */
        WaitEventData.prototype.team = 0;

        /**
         * WaitEventData tool.
         * @member {number} tool
         * @memberof common.WaitEventData
         * @instance
         */
        WaitEventData.prototype.tool = 0;

        /**
         * Creates a new WaitEventData instance using the specified properties.
         * @function create
         * @memberof common.WaitEventData
         * @static
         * @param {common.IWaitEventData=} [properties] Properties to set
         * @returns {common.WaitEventData} WaitEventData instance
         */
        WaitEventData.create = function create(properties) {
            return new WaitEventData(properties);
        };

        /**
         * Encodes the specified WaitEventData message. Does not implicitly {@link common.WaitEventData.verify|verify} messages.
         * @function encode
         * @memberof common.WaitEventData
         * @static
         * @param {common.IWaitEventData} message WaitEventData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WaitEventData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.team != null && Object.hasOwnProperty.call(message, "team"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.team);
            if (message.tool != null && Object.hasOwnProperty.call(message, "tool"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.tool);
            return writer;
        };

        /**
         * Encodes the specified WaitEventData message, length delimited. Does not implicitly {@link common.WaitEventData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.WaitEventData
         * @static
         * @param {common.IWaitEventData} message WaitEventData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WaitEventData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WaitEventData message from the specified reader or buffer.
         * @function decode
         * @memberof common.WaitEventData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.WaitEventData} WaitEventData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WaitEventData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.WaitEventData();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.team = reader.int32();
                        break;
                    }
                case 3: {
                        message.tool = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WaitEventData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.WaitEventData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.WaitEventData} WaitEventData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WaitEventData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WaitEventData message.
         * @function verify
         * @memberof common.WaitEventData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WaitEventData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.team != null && message.hasOwnProperty("team"))
                if (!$util.isInteger(message.team))
                    return "team: integer expected";
            if (message.tool != null && message.hasOwnProperty("tool"))
                if (!$util.isInteger(message.tool))
                    return "tool: integer expected";
            return null;
        };

        /**
         * Creates a WaitEventData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.WaitEventData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.WaitEventData} WaitEventData
         */
        WaitEventData.fromObject = function fromObject(object) {
            if (object instanceof $root.common.WaitEventData)
                return object;
            var message = new $root.common.WaitEventData();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.team != null)
                message.team = object.team | 0;
            if (object.tool != null)
                message.tool = object.tool | 0;
            return message;
        };

        /**
         * Creates a plain object from a WaitEventData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.WaitEventData
         * @static
         * @param {common.WaitEventData} message WaitEventData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WaitEventData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.team = 0;
                object.tool = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.team != null && message.hasOwnProperty("team"))
                object.team = message.team;
            if (message.tool != null && message.hasOwnProperty("tool"))
                object.tool = message.tool;
            return object;
        };

        /**
         * Converts this WaitEventData to JSON.
         * @function toJSON
         * @memberof common.WaitEventData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WaitEventData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WaitEventData
         * @function getTypeUrl
         * @memberof common.WaitEventData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WaitEventData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.WaitEventData";
        };

        return WaitEventData;
    })();

    common.UserBaseInfo = (function() {

        /**
         * Properties of a UserBaseInfo.
         * @memberof common
         * @interface IUserBaseInfo
         * @property {string|null} [nick] UserBaseInfo nick
         * @property {string|null} [gender] UserBaseInfo gender
         * @property {string|null} [country] UserBaseInfo country
         * @property {number|null} [avatar] UserBaseInfo avatar
         * @property {string|null} [imel] UserBaseInfo imel
         * @property {string|null} [device] UserBaseInfo device
         */

        /**
         * Constructs a new UserBaseInfo.
         * @memberof common
         * @classdesc Represents a UserBaseInfo.
         * @implements IUserBaseInfo
         * @constructor
         * @param {common.IUserBaseInfo=} [properties] Properties to set
         */
        function UserBaseInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserBaseInfo nick.
         * @member {string} nick
         * @memberof common.UserBaseInfo
         * @instance
         */
        UserBaseInfo.prototype.nick = "";

        /**
         * UserBaseInfo gender.
         * @member {string} gender
         * @memberof common.UserBaseInfo
         * @instance
         */
        UserBaseInfo.prototype.gender = "";

        /**
         * UserBaseInfo country.
         * @member {string} country
         * @memberof common.UserBaseInfo
         * @instance
         */
        UserBaseInfo.prototype.country = "";

        /**
         * UserBaseInfo avatar.
         * @member {number} avatar
         * @memberof common.UserBaseInfo
         * @instance
         */
        UserBaseInfo.prototype.avatar = 0;

        /**
         * UserBaseInfo imel.
         * @member {string} imel
         * @memberof common.UserBaseInfo
         * @instance
         */
        UserBaseInfo.prototype.imel = "";

        /**
         * UserBaseInfo device.
         * @member {string} device
         * @memberof common.UserBaseInfo
         * @instance
         */
        UserBaseInfo.prototype.device = "";

        /**
         * Creates a new UserBaseInfo instance using the specified properties.
         * @function create
         * @memberof common.UserBaseInfo
         * @static
         * @param {common.IUserBaseInfo=} [properties] Properties to set
         * @returns {common.UserBaseInfo} UserBaseInfo instance
         */
        UserBaseInfo.create = function create(properties) {
            return new UserBaseInfo(properties);
        };

        /**
         * Encodes the specified UserBaseInfo message. Does not implicitly {@link common.UserBaseInfo.verify|verify} messages.
         * @function encode
         * @memberof common.UserBaseInfo
         * @static
         * @param {common.IUserBaseInfo} message UserBaseInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserBaseInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.nick != null && Object.hasOwnProperty.call(message, "nick"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.nick);
            if (message.gender != null && Object.hasOwnProperty.call(message, "gender"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.gender);
            if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.country);
            if (message.avatar != null && Object.hasOwnProperty.call(message, "avatar"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.avatar);
            if (message.imel != null && Object.hasOwnProperty.call(message, "imel"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.imel);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.device);
            return writer;
        };

        /**
         * Encodes the specified UserBaseInfo message, length delimited. Does not implicitly {@link common.UserBaseInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.UserBaseInfo
         * @static
         * @param {common.IUserBaseInfo} message UserBaseInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserBaseInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a UserBaseInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.UserBaseInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.UserBaseInfo} UserBaseInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserBaseInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.UserBaseInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.nick = reader.string();
                        break;
                    }
                case 2: {
                        message.gender = reader.string();
                        break;
                    }
                case 3: {
                        message.country = reader.string();
                        break;
                    }
                case 4: {
                        message.avatar = reader.int32();
                        break;
                    }
                case 5: {
                        message.imel = reader.string();
                        break;
                    }
                case 6: {
                        message.device = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a UserBaseInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.UserBaseInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.UserBaseInfo} UserBaseInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserBaseInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UserBaseInfo message.
         * @function verify
         * @memberof common.UserBaseInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UserBaseInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.nick != null && message.hasOwnProperty("nick"))
                if (!$util.isString(message.nick))
                    return "nick: string expected";
            if (message.gender != null && message.hasOwnProperty("gender"))
                if (!$util.isString(message.gender))
                    return "gender: string expected";
            if (message.country != null && message.hasOwnProperty("country"))
                if (!$util.isString(message.country))
                    return "country: string expected";
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                if (!$util.isInteger(message.avatar))
                    return "avatar: integer expected";
            if (message.imel != null && message.hasOwnProperty("imel"))
                if (!$util.isString(message.imel))
                    return "imel: string expected";
            if (message.device != null && message.hasOwnProperty("device"))
                if (!$util.isString(message.device))
                    return "device: string expected";
            return null;
        };

        /**
         * Creates a UserBaseInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.UserBaseInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.UserBaseInfo} UserBaseInfo
         */
        UserBaseInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.UserBaseInfo)
                return object;
            var message = new $root.common.UserBaseInfo();
            if (object.nick != null)
                message.nick = String(object.nick);
            if (object.gender != null)
                message.gender = String(object.gender);
            if (object.country != null)
                message.country = String(object.country);
            if (object.avatar != null)
                message.avatar = object.avatar | 0;
            if (object.imel != null)
                message.imel = String(object.imel);
            if (object.device != null)
                message.device = String(object.device);
            return message;
        };

        /**
         * Creates a plain object from a UserBaseInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.UserBaseInfo
         * @static
         * @param {common.UserBaseInfo} message UserBaseInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserBaseInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.nick = "";
                object.gender = "";
                object.country = "";
                object.avatar = 0;
                object.imel = "";
                object.device = "";
            }
            if (message.nick != null && message.hasOwnProperty("nick"))
                object.nick = message.nick;
            if (message.gender != null && message.hasOwnProperty("gender"))
                object.gender = message.gender;
            if (message.country != null && message.hasOwnProperty("country"))
                object.country = message.country;
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                object.avatar = message.avatar;
            if (message.imel != null && message.hasOwnProperty("imel"))
                object.imel = message.imel;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = message.device;
            return object;
        };

        /**
         * Converts this UserBaseInfo to JSON.
         * @function toJSON
         * @memberof common.UserBaseInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserBaseInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserBaseInfo
         * @function getTypeUrl
         * @memberof common.UserBaseInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserBaseInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.UserBaseInfo";
        };

        return UserBaseInfo;
    })();

    common.LoginInfo = (function() {

        /**
         * Properties of a LoginInfo.
         * @memberof common
         * @interface ILoginInfo
         * @property {number|Long|null} [firstTime] LoginInfo firstTime
         * @property {number|Long|null} [lastTime] LoginInfo lastTime
         * @property {string|null} [lastIp] LoginInfo lastIp
         * @property {number|null} [loginDays] LoginInfo loginDays
         */

        /**
         * Constructs a new LoginInfo.
         * @memberof common
         * @classdesc Represents a LoginInfo.
         * @implements ILoginInfo
         * @constructor
         * @param {common.ILoginInfo=} [properties] Properties to set
         */
        function LoginInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LoginInfo firstTime.
         * @member {number|Long} firstTime
         * @memberof common.LoginInfo
         * @instance
         */
        LoginInfo.prototype.firstTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LoginInfo lastTime.
         * @member {number|Long} lastTime
         * @memberof common.LoginInfo
         * @instance
         */
        LoginInfo.prototype.lastTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LoginInfo lastIp.
         * @member {string} lastIp
         * @memberof common.LoginInfo
         * @instance
         */
        LoginInfo.prototype.lastIp = "";

        /**
         * LoginInfo loginDays.
         * @member {number} loginDays
         * @memberof common.LoginInfo
         * @instance
         */
        LoginInfo.prototype.loginDays = 0;

        /**
         * Creates a new LoginInfo instance using the specified properties.
         * @function create
         * @memberof common.LoginInfo
         * @static
         * @param {common.ILoginInfo=} [properties] Properties to set
         * @returns {common.LoginInfo} LoginInfo instance
         */
        LoginInfo.create = function create(properties) {
            return new LoginInfo(properties);
        };

        /**
         * Encodes the specified LoginInfo message. Does not implicitly {@link common.LoginInfo.verify|verify} messages.
         * @function encode
         * @memberof common.LoginInfo
         * @static
         * @param {common.ILoginInfo} message LoginInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.firstTime != null && Object.hasOwnProperty.call(message, "firstTime"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.firstTime);
            if (message.lastTime != null && Object.hasOwnProperty.call(message, "lastTime"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.lastTime);
            if (message.lastIp != null && Object.hasOwnProperty.call(message, "lastIp"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.lastIp);
            if (message.loginDays != null && Object.hasOwnProperty.call(message, "loginDays"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.loginDays);
            return writer;
        };

        /**
         * Encodes the specified LoginInfo message, length delimited. Does not implicitly {@link common.LoginInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.LoginInfo
         * @static
         * @param {common.ILoginInfo} message LoginInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LoginInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.LoginInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.LoginInfo} LoginInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.LoginInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.firstTime = reader.int64();
                        break;
                    }
                case 2: {
                        message.lastTime = reader.int64();
                        break;
                    }
                case 3: {
                        message.lastIp = reader.string();
                        break;
                    }
                case 4: {
                        message.loginDays = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LoginInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.LoginInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.LoginInfo} LoginInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LoginInfo message.
         * @function verify
         * @memberof common.LoginInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LoginInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.firstTime != null && message.hasOwnProperty("firstTime"))
                if (!$util.isInteger(message.firstTime) && !(message.firstTime && $util.isInteger(message.firstTime.low) && $util.isInteger(message.firstTime.high)))
                    return "firstTime: integer|Long expected";
            if (message.lastTime != null && message.hasOwnProperty("lastTime"))
                if (!$util.isInteger(message.lastTime) && !(message.lastTime && $util.isInteger(message.lastTime.low) && $util.isInteger(message.lastTime.high)))
                    return "lastTime: integer|Long expected";
            if (message.lastIp != null && message.hasOwnProperty("lastIp"))
                if (!$util.isString(message.lastIp))
                    return "lastIp: string expected";
            if (message.loginDays != null && message.hasOwnProperty("loginDays"))
                if (!$util.isInteger(message.loginDays))
                    return "loginDays: integer expected";
            return null;
        };

        /**
         * Creates a LoginInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.LoginInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.LoginInfo} LoginInfo
         */
        LoginInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.LoginInfo)
                return object;
            var message = new $root.common.LoginInfo();
            if (object.firstTime != null)
                if ($util.Long)
                    (message.firstTime = $util.Long.fromValue(object.firstTime)).unsigned = false;
                else if (typeof object.firstTime === "string")
                    message.firstTime = parseInt(object.firstTime, 10);
                else if (typeof object.firstTime === "number")
                    message.firstTime = object.firstTime;
                else if (typeof object.firstTime === "object")
                    message.firstTime = new $util.LongBits(object.firstTime.low >>> 0, object.firstTime.high >>> 0).toNumber();
            if (object.lastTime != null)
                if ($util.Long)
                    (message.lastTime = $util.Long.fromValue(object.lastTime)).unsigned = false;
                else if (typeof object.lastTime === "string")
                    message.lastTime = parseInt(object.lastTime, 10);
                else if (typeof object.lastTime === "number")
                    message.lastTime = object.lastTime;
                else if (typeof object.lastTime === "object")
                    message.lastTime = new $util.LongBits(object.lastTime.low >>> 0, object.lastTime.high >>> 0).toNumber();
            if (object.lastIp != null)
                message.lastIp = String(object.lastIp);
            if (object.loginDays != null)
                message.loginDays = object.loginDays | 0;
            return message;
        };

        /**
         * Creates a plain object from a LoginInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.LoginInfo
         * @static
         * @param {common.LoginInfo} message LoginInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LoginInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.firstTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.firstTime = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.lastTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.lastTime = options.longs === String ? "0" : 0;
                object.lastIp = "";
                object.loginDays = 0;
            }
            if (message.firstTime != null && message.hasOwnProperty("firstTime"))
                if (typeof message.firstTime === "number")
                    object.firstTime = options.longs === String ? String(message.firstTime) : message.firstTime;
                else
                    object.firstTime = options.longs === String ? $util.Long.prototype.toString.call(message.firstTime) : options.longs === Number ? new $util.LongBits(message.firstTime.low >>> 0, message.firstTime.high >>> 0).toNumber() : message.firstTime;
            if (message.lastTime != null && message.hasOwnProperty("lastTime"))
                if (typeof message.lastTime === "number")
                    object.lastTime = options.longs === String ? String(message.lastTime) : message.lastTime;
                else
                    object.lastTime = options.longs === String ? $util.Long.prototype.toString.call(message.lastTime) : options.longs === Number ? new $util.LongBits(message.lastTime.low >>> 0, message.lastTime.high >>> 0).toNumber() : message.lastTime;
            if (message.lastIp != null && message.hasOwnProperty("lastIp"))
                object.lastIp = message.lastIp;
            if (message.loginDays != null && message.hasOwnProperty("loginDays"))
                object.loginDays = message.loginDays;
            return object;
        };

        /**
         * Converts this LoginInfo to JSON.
         * @function toJSON
         * @memberof common.LoginInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LoginInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LoginInfo
         * @function getTypeUrl
         * @memberof common.LoginInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LoginInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.LoginInfo";
        };

        return LoginInfo;
    })();

    common.PaymentInfo = (function() {

        /**
         * Properties of a PaymentInfo.
         * @memberof common
         * @interface IPaymentInfo
         * @property {number|null} [totalEcharge] PaymentInfo totalEcharge
         * @property {number|null} [dailyRecharge] PaymentInfo dailyRecharge
         * @property {number|null} [lastRecharge] PaymentInfo lastRecharge
         * @property {number|Long|null} [lastRechargeTime] PaymentInfo lastRechargeTime
         */

        /**
         * Constructs a new PaymentInfo.
         * @memberof common
         * @classdesc Represents a PaymentInfo.
         * @implements IPaymentInfo
         * @constructor
         * @param {common.IPaymentInfo=} [properties] Properties to set
         */
        function PaymentInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PaymentInfo totalEcharge.
         * @member {number} totalEcharge
         * @memberof common.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.totalEcharge = 0;

        /**
         * PaymentInfo dailyRecharge.
         * @member {number} dailyRecharge
         * @memberof common.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.dailyRecharge = 0;

        /**
         * PaymentInfo lastRecharge.
         * @member {number} lastRecharge
         * @memberof common.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.lastRecharge = 0;

        /**
         * PaymentInfo lastRechargeTime.
         * @member {number|Long} lastRechargeTime
         * @memberof common.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.lastRechargeTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new PaymentInfo instance using the specified properties.
         * @function create
         * @memberof common.PaymentInfo
         * @static
         * @param {common.IPaymentInfo=} [properties] Properties to set
         * @returns {common.PaymentInfo} PaymentInfo instance
         */
        PaymentInfo.create = function create(properties) {
            return new PaymentInfo(properties);
        };

        /**
         * Encodes the specified PaymentInfo message. Does not implicitly {@link common.PaymentInfo.verify|verify} messages.
         * @function encode
         * @memberof common.PaymentInfo
         * @static
         * @param {common.IPaymentInfo} message PaymentInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PaymentInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.totalEcharge != null && Object.hasOwnProperty.call(message, "totalEcharge"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.totalEcharge);
            if (message.dailyRecharge != null && Object.hasOwnProperty.call(message, "dailyRecharge"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.dailyRecharge);
            if (message.lastRecharge != null && Object.hasOwnProperty.call(message, "lastRecharge"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.lastRecharge);
            if (message.lastRechargeTime != null && Object.hasOwnProperty.call(message, "lastRechargeTime"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.lastRechargeTime);
            return writer;
        };

        /**
         * Encodes the specified PaymentInfo message, length delimited. Does not implicitly {@link common.PaymentInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.PaymentInfo
         * @static
         * @param {common.IPaymentInfo} message PaymentInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PaymentInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PaymentInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.PaymentInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.PaymentInfo} PaymentInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PaymentInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.PaymentInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.totalEcharge = reader.int32();
                        break;
                    }
                case 2: {
                        message.dailyRecharge = reader.int32();
                        break;
                    }
                case 3: {
                        message.lastRecharge = reader.int32();
                        break;
                    }
                case 4: {
                        message.lastRechargeTime = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PaymentInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.PaymentInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.PaymentInfo} PaymentInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PaymentInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PaymentInfo message.
         * @function verify
         * @memberof common.PaymentInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PaymentInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.totalEcharge != null && message.hasOwnProperty("totalEcharge"))
                if (!$util.isInteger(message.totalEcharge))
                    return "totalEcharge: integer expected";
            if (message.dailyRecharge != null && message.hasOwnProperty("dailyRecharge"))
                if (!$util.isInteger(message.dailyRecharge))
                    return "dailyRecharge: integer expected";
            if (message.lastRecharge != null && message.hasOwnProperty("lastRecharge"))
                if (!$util.isInteger(message.lastRecharge))
                    return "lastRecharge: integer expected";
            if (message.lastRechargeTime != null && message.hasOwnProperty("lastRechargeTime"))
                if (!$util.isInteger(message.lastRechargeTime) && !(message.lastRechargeTime && $util.isInteger(message.lastRechargeTime.low) && $util.isInteger(message.lastRechargeTime.high)))
                    return "lastRechargeTime: integer|Long expected";
            return null;
        };

        /**
         * Creates a PaymentInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.PaymentInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.PaymentInfo} PaymentInfo
         */
        PaymentInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.PaymentInfo)
                return object;
            var message = new $root.common.PaymentInfo();
            if (object.totalEcharge != null)
                message.totalEcharge = object.totalEcharge | 0;
            if (object.dailyRecharge != null)
                message.dailyRecharge = object.dailyRecharge | 0;
            if (object.lastRecharge != null)
                message.lastRecharge = object.lastRecharge | 0;
            if (object.lastRechargeTime != null)
                if ($util.Long)
                    (message.lastRechargeTime = $util.Long.fromValue(object.lastRechargeTime)).unsigned = false;
                else if (typeof object.lastRechargeTime === "string")
                    message.lastRechargeTime = parseInt(object.lastRechargeTime, 10);
                else if (typeof object.lastRechargeTime === "number")
                    message.lastRechargeTime = object.lastRechargeTime;
                else if (typeof object.lastRechargeTime === "object")
                    message.lastRechargeTime = new $util.LongBits(object.lastRechargeTime.low >>> 0, object.lastRechargeTime.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a PaymentInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.PaymentInfo
         * @static
         * @param {common.PaymentInfo} message PaymentInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PaymentInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.totalEcharge = 0;
                object.dailyRecharge = 0;
                object.lastRecharge = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.lastRechargeTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.lastRechargeTime = options.longs === String ? "0" : 0;
            }
            if (message.totalEcharge != null && message.hasOwnProperty("totalEcharge"))
                object.totalEcharge = message.totalEcharge;
            if (message.dailyRecharge != null && message.hasOwnProperty("dailyRecharge"))
                object.dailyRecharge = message.dailyRecharge;
            if (message.lastRecharge != null && message.hasOwnProperty("lastRecharge"))
                object.lastRecharge = message.lastRecharge;
            if (message.lastRechargeTime != null && message.hasOwnProperty("lastRechargeTime"))
                if (typeof message.lastRechargeTime === "number")
                    object.lastRechargeTime = options.longs === String ? String(message.lastRechargeTime) : message.lastRechargeTime;
                else
                    object.lastRechargeTime = options.longs === String ? $util.Long.prototype.toString.call(message.lastRechargeTime) : options.longs === Number ? new $util.LongBits(message.lastRechargeTime.low >>> 0, message.lastRechargeTime.high >>> 0).toNumber() : message.lastRechargeTime;
            return object;
        };

        /**
         * Converts this PaymentInfo to JSON.
         * @function toJSON
         * @memberof common.PaymentInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PaymentInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PaymentInfo
         * @function getTypeUrl
         * @memberof common.PaymentInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PaymentInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.PaymentInfo";
        };

        return PaymentInfo;
    })();

    common.CosumeInfo = (function() {

        /**
         * Properties of a CosumeInfo.
         * @memberof common
         * @interface ICosumeInfo
         * @property {number|null} [cash] CosumeInfo cash
         * @property {number|null} [bindcash] CosumeInfo bindcash
         */

        /**
         * Constructs a new CosumeInfo.
         * @memberof common
         * @classdesc Represents a CosumeInfo.
         * @implements ICosumeInfo
         * @constructor
         * @param {common.ICosumeInfo=} [properties] Properties to set
         */
        function CosumeInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CosumeInfo cash.
         * @member {number} cash
         * @memberof common.CosumeInfo
         * @instance
         */
        CosumeInfo.prototype.cash = 0;

        /**
         * CosumeInfo bindcash.
         * @member {number} bindcash
         * @memberof common.CosumeInfo
         * @instance
         */
        CosumeInfo.prototype.bindcash = 0;

        /**
         * Creates a new CosumeInfo instance using the specified properties.
         * @function create
         * @memberof common.CosumeInfo
         * @static
         * @param {common.ICosumeInfo=} [properties] Properties to set
         * @returns {common.CosumeInfo} CosumeInfo instance
         */
        CosumeInfo.create = function create(properties) {
            return new CosumeInfo(properties);
        };

        /**
         * Encodes the specified CosumeInfo message. Does not implicitly {@link common.CosumeInfo.verify|verify} messages.
         * @function encode
         * @memberof common.CosumeInfo
         * @static
         * @param {common.ICosumeInfo} message CosumeInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CosumeInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.cash != null && Object.hasOwnProperty.call(message, "cash"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.cash);
            if (message.bindcash != null && Object.hasOwnProperty.call(message, "bindcash"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.bindcash);
            return writer;
        };

        /**
         * Encodes the specified CosumeInfo message, length delimited. Does not implicitly {@link common.CosumeInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.CosumeInfo
         * @static
         * @param {common.ICosumeInfo} message CosumeInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CosumeInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CosumeInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.CosumeInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.CosumeInfo} CosumeInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CosumeInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.CosumeInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.cash = reader.int32();
                        break;
                    }
                case 2: {
                        message.bindcash = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CosumeInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.CosumeInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.CosumeInfo} CosumeInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CosumeInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CosumeInfo message.
         * @function verify
         * @memberof common.CosumeInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CosumeInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.cash != null && message.hasOwnProperty("cash"))
                if (!$util.isInteger(message.cash))
                    return "cash: integer expected";
            if (message.bindcash != null && message.hasOwnProperty("bindcash"))
                if (!$util.isInteger(message.bindcash))
                    return "bindcash: integer expected";
            return null;
        };

        /**
         * Creates a CosumeInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.CosumeInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.CosumeInfo} CosumeInfo
         */
        CosumeInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.CosumeInfo)
                return object;
            var message = new $root.common.CosumeInfo();
            if (object.cash != null)
                message.cash = object.cash | 0;
            if (object.bindcash != null)
                message.bindcash = object.bindcash | 0;
            return message;
        };

        /**
         * Creates a plain object from a CosumeInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.CosumeInfo
         * @static
         * @param {common.CosumeInfo} message CosumeInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CosumeInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.cash = 0;
                object.bindcash = 0;
            }
            if (message.cash != null && message.hasOwnProperty("cash"))
                object.cash = message.cash;
            if (message.bindcash != null && message.hasOwnProperty("bindcash"))
                object.bindcash = message.bindcash;
            return object;
        };

        /**
         * Converts this CosumeInfo to JSON.
         * @function toJSON
         * @memberof common.CosumeInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CosumeInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CosumeInfo
         * @function getTypeUrl
         * @memberof common.CosumeInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CosumeInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.CosumeInfo";
        };

        return CosumeInfo;
    })();

    common.ResourceInfo = (function() {

        /**
         * Properties of a ResourceInfo.
         * @memberof common
         * @interface IResourceInfo
         * @property {number|Long|null} [cash] ResourceInfo cash
         * @property {number|Long|null} [bindcash] ResourceInfo bindcash
         * @property {number|Long|null} [exp] ResourceInfo exp
         * @property {number|Long|null} [gold] ResourceInfo gold
         * @property {number|null} [fairyAge] ResourceInfo fairyAge
         * @property {number|null} [candy] ResourceInfo candy
         */

        /**
         * Constructs a new ResourceInfo.
         * @memberof common
         * @classdesc Represents a ResourceInfo.
         * @implements IResourceInfo
         * @constructor
         * @param {common.IResourceInfo=} [properties] Properties to set
         */
        function ResourceInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ResourceInfo cash.
         * @member {number|Long} cash
         * @memberof common.ResourceInfo
         * @instance
         */
        ResourceInfo.prototype.cash = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ResourceInfo bindcash.
         * @member {number|Long} bindcash
         * @memberof common.ResourceInfo
         * @instance
         */
        ResourceInfo.prototype.bindcash = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ResourceInfo exp.
         * @member {number|Long} exp
         * @memberof common.ResourceInfo
         * @instance
         */
        ResourceInfo.prototype.exp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ResourceInfo gold.
         * @member {number|Long} gold
         * @memberof common.ResourceInfo
         * @instance
         */
        ResourceInfo.prototype.gold = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ResourceInfo fairyAge.
         * @member {number} fairyAge
         * @memberof common.ResourceInfo
         * @instance
         */
        ResourceInfo.prototype.fairyAge = 0;

        /**
         * ResourceInfo candy.
         * @member {number} candy
         * @memberof common.ResourceInfo
         * @instance
         */
        ResourceInfo.prototype.candy = 0;

        /**
         * Creates a new ResourceInfo instance using the specified properties.
         * @function create
         * @memberof common.ResourceInfo
         * @static
         * @param {common.IResourceInfo=} [properties] Properties to set
         * @returns {common.ResourceInfo} ResourceInfo instance
         */
        ResourceInfo.create = function create(properties) {
            return new ResourceInfo(properties);
        };

        /**
         * Encodes the specified ResourceInfo message. Does not implicitly {@link common.ResourceInfo.verify|verify} messages.
         * @function encode
         * @memberof common.ResourceInfo
         * @static
         * @param {common.IResourceInfo} message ResourceInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResourceInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.cash != null && Object.hasOwnProperty.call(message, "cash"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.cash);
            if (message.bindcash != null && Object.hasOwnProperty.call(message, "bindcash"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.bindcash);
            if (message.exp != null && Object.hasOwnProperty.call(message, "exp"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.exp);
            if (message.gold != null && Object.hasOwnProperty.call(message, "gold"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.gold);
            if (message.fairyAge != null && Object.hasOwnProperty.call(message, "fairyAge"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.fairyAge);
            if (message.candy != null && Object.hasOwnProperty.call(message, "candy"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.candy);
            return writer;
        };

        /**
         * Encodes the specified ResourceInfo message, length delimited. Does not implicitly {@link common.ResourceInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.ResourceInfo
         * @static
         * @param {common.IResourceInfo} message ResourceInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResourceInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ResourceInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.ResourceInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.ResourceInfo} ResourceInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResourceInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.ResourceInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.cash = reader.int64();
                        break;
                    }
                case 2: {
                        message.bindcash = reader.int64();
                        break;
                    }
                case 3: {
                        message.exp = reader.int64();
                        break;
                    }
                case 4: {
                        message.gold = reader.int64();
                        break;
                    }
                case 5: {
                        message.fairyAge = reader.int32();
                        break;
                    }
                case 6: {
                        message.candy = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ResourceInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.ResourceInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.ResourceInfo} ResourceInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResourceInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ResourceInfo message.
         * @function verify
         * @memberof common.ResourceInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ResourceInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.cash != null && message.hasOwnProperty("cash"))
                if (!$util.isInteger(message.cash) && !(message.cash && $util.isInteger(message.cash.low) && $util.isInteger(message.cash.high)))
                    return "cash: integer|Long expected";
            if (message.bindcash != null && message.hasOwnProperty("bindcash"))
                if (!$util.isInteger(message.bindcash) && !(message.bindcash && $util.isInteger(message.bindcash.low) && $util.isInteger(message.bindcash.high)))
                    return "bindcash: integer|Long expected";
            if (message.exp != null && message.hasOwnProperty("exp"))
                if (!$util.isInteger(message.exp) && !(message.exp && $util.isInteger(message.exp.low) && $util.isInteger(message.exp.high)))
                    return "exp: integer|Long expected";
            if (message.gold != null && message.hasOwnProperty("gold"))
                if (!$util.isInteger(message.gold) && !(message.gold && $util.isInteger(message.gold.low) && $util.isInteger(message.gold.high)))
                    return "gold: integer|Long expected";
            if (message.fairyAge != null && message.hasOwnProperty("fairyAge"))
                if (!$util.isInteger(message.fairyAge))
                    return "fairyAge: integer expected";
            if (message.candy != null && message.hasOwnProperty("candy"))
                if (!$util.isInteger(message.candy))
                    return "candy: integer expected";
            return null;
        };

        /**
         * Creates a ResourceInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.ResourceInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.ResourceInfo} ResourceInfo
         */
        ResourceInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.ResourceInfo)
                return object;
            var message = new $root.common.ResourceInfo();
            if (object.cash != null)
                if ($util.Long)
                    (message.cash = $util.Long.fromValue(object.cash)).unsigned = false;
                else if (typeof object.cash === "string")
                    message.cash = parseInt(object.cash, 10);
                else if (typeof object.cash === "number")
                    message.cash = object.cash;
                else if (typeof object.cash === "object")
                    message.cash = new $util.LongBits(object.cash.low >>> 0, object.cash.high >>> 0).toNumber();
            if (object.bindcash != null)
                if ($util.Long)
                    (message.bindcash = $util.Long.fromValue(object.bindcash)).unsigned = false;
                else if (typeof object.bindcash === "string")
                    message.bindcash = parseInt(object.bindcash, 10);
                else if (typeof object.bindcash === "number")
                    message.bindcash = object.bindcash;
                else if (typeof object.bindcash === "object")
                    message.bindcash = new $util.LongBits(object.bindcash.low >>> 0, object.bindcash.high >>> 0).toNumber();
            if (object.exp != null)
                if ($util.Long)
                    (message.exp = $util.Long.fromValue(object.exp)).unsigned = false;
                else if (typeof object.exp === "string")
                    message.exp = parseInt(object.exp, 10);
                else if (typeof object.exp === "number")
                    message.exp = object.exp;
                else if (typeof object.exp === "object")
                    message.exp = new $util.LongBits(object.exp.low >>> 0, object.exp.high >>> 0).toNumber();
            if (object.gold != null)
                if ($util.Long)
                    (message.gold = $util.Long.fromValue(object.gold)).unsigned = false;
                else if (typeof object.gold === "string")
                    message.gold = parseInt(object.gold, 10);
                else if (typeof object.gold === "number")
                    message.gold = object.gold;
                else if (typeof object.gold === "object")
                    message.gold = new $util.LongBits(object.gold.low >>> 0, object.gold.high >>> 0).toNumber();
            if (object.fairyAge != null)
                message.fairyAge = object.fairyAge | 0;
            if (object.candy != null)
                message.candy = object.candy | 0;
            return message;
        };

        /**
         * Creates a plain object from a ResourceInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.ResourceInfo
         * @static
         * @param {common.ResourceInfo} message ResourceInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ResourceInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.cash = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.cash = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.bindcash = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.bindcash = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.exp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.exp = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.gold = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.gold = options.longs === String ? "0" : 0;
                object.fairyAge = 0;
                object.candy = 0;
            }
            if (message.cash != null && message.hasOwnProperty("cash"))
                if (typeof message.cash === "number")
                    object.cash = options.longs === String ? String(message.cash) : message.cash;
                else
                    object.cash = options.longs === String ? $util.Long.prototype.toString.call(message.cash) : options.longs === Number ? new $util.LongBits(message.cash.low >>> 0, message.cash.high >>> 0).toNumber() : message.cash;
            if (message.bindcash != null && message.hasOwnProperty("bindcash"))
                if (typeof message.bindcash === "number")
                    object.bindcash = options.longs === String ? String(message.bindcash) : message.bindcash;
                else
                    object.bindcash = options.longs === String ? $util.Long.prototype.toString.call(message.bindcash) : options.longs === Number ? new $util.LongBits(message.bindcash.low >>> 0, message.bindcash.high >>> 0).toNumber() : message.bindcash;
            if (message.exp != null && message.hasOwnProperty("exp"))
                if (typeof message.exp === "number")
                    object.exp = options.longs === String ? String(message.exp) : message.exp;
                else
                    object.exp = options.longs === String ? $util.Long.prototype.toString.call(message.exp) : options.longs === Number ? new $util.LongBits(message.exp.low >>> 0, message.exp.high >>> 0).toNumber() : message.exp;
            if (message.gold != null && message.hasOwnProperty("gold"))
                if (typeof message.gold === "number")
                    object.gold = options.longs === String ? String(message.gold) : message.gold;
                else
                    object.gold = options.longs === String ? $util.Long.prototype.toString.call(message.gold) : options.longs === Number ? new $util.LongBits(message.gold.low >>> 0, message.gold.high >>> 0).toNumber() : message.gold;
            if (message.fairyAge != null && message.hasOwnProperty("fairyAge"))
                object.fairyAge = message.fairyAge;
            if (message.candy != null && message.hasOwnProperty("candy"))
                object.candy = message.candy;
            return object;
        };

        /**
         * Converts this ResourceInfo to JSON.
         * @function toJSON
         * @memberof common.ResourceInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ResourceInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ResourceInfo
         * @function getTypeUrl
         * @memberof common.ResourceInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ResourceInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.ResourceInfo";
        };

        return ResourceInfo;
    })();

    common.HeroTeam = (function() {

        /**
         * Properties of a HeroTeam.
         * @memberof common
         * @interface IHeroTeam
         * @property {Array.<number>|null} [heros] HeroTeam heros
         */

        /**
         * Constructs a new HeroTeam.
         * @memberof common
         * @classdesc Represents a HeroTeam.
         * @implements IHeroTeam
         * @constructor
         * @param {common.IHeroTeam=} [properties] Properties to set
         */
        function HeroTeam(properties) {
            this.heros = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HeroTeam heros.
         * @member {Array.<number>} heros
         * @memberof common.HeroTeam
         * @instance
         */
        HeroTeam.prototype.heros = $util.emptyArray;

        /**
         * Creates a new HeroTeam instance using the specified properties.
         * @function create
         * @memberof common.HeroTeam
         * @static
         * @param {common.IHeroTeam=} [properties] Properties to set
         * @returns {common.HeroTeam} HeroTeam instance
         */
        HeroTeam.create = function create(properties) {
            return new HeroTeam(properties);
        };

        /**
         * Encodes the specified HeroTeam message. Does not implicitly {@link common.HeroTeam.verify|verify} messages.
         * @function encode
         * @memberof common.HeroTeam
         * @static
         * @param {common.IHeroTeam} message HeroTeam message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HeroTeam.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.heros != null && message.heros.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (var i = 0; i < message.heros.length; ++i)
                    writer.int32(message.heros[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified HeroTeam message, length delimited. Does not implicitly {@link common.HeroTeam.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.HeroTeam
         * @static
         * @param {common.IHeroTeam} message HeroTeam message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HeroTeam.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a HeroTeam message from the specified reader or buffer.
         * @function decode
         * @memberof common.HeroTeam
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.HeroTeam} HeroTeam
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HeroTeam.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.HeroTeam();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.heros && message.heros.length))
                            message.heros = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.heros.push(reader.int32());
                        } else
                            message.heros.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a HeroTeam message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.HeroTeam
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.HeroTeam} HeroTeam
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HeroTeam.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HeroTeam message.
         * @function verify
         * @memberof common.HeroTeam
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HeroTeam.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.heros != null && message.hasOwnProperty("heros")) {
                if (!Array.isArray(message.heros))
                    return "heros: array expected";
                for (var i = 0; i < message.heros.length; ++i)
                    if (!$util.isInteger(message.heros[i]))
                        return "heros: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a HeroTeam message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.HeroTeam
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.HeroTeam} HeroTeam
         */
        HeroTeam.fromObject = function fromObject(object) {
            if (object instanceof $root.common.HeroTeam)
                return object;
            var message = new $root.common.HeroTeam();
            if (object.heros) {
                if (!Array.isArray(object.heros))
                    throw TypeError(".common.HeroTeam.heros: array expected");
                message.heros = [];
                for (var i = 0; i < object.heros.length; ++i)
                    message.heros[i] = object.heros[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a HeroTeam message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.HeroTeam
         * @static
         * @param {common.HeroTeam} message HeroTeam
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HeroTeam.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.heros = [];
            if (message.heros && message.heros.length) {
                object.heros = [];
                for (var j = 0; j < message.heros.length; ++j)
                    object.heros[j] = message.heros[j];
            }
            return object;
        };

        /**
         * Converts this HeroTeam to JSON.
         * @function toJSON
         * @memberof common.HeroTeam
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HeroTeam.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HeroTeam
         * @function getTypeUrl
         * @memberof common.HeroTeam
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HeroTeam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.HeroTeam";
        };

        return HeroTeam;
    })();

    common.BoxInfo = (function() {

        /**
         * Properties of a BoxInfo.
         * @memberof common
         * @interface IBoxInfo
         * @property {number|null} [bid] BoxInfo bid
         * @property {number|null} [id] BoxInfo id
         * @property {number|Long|null} [time] BoxInfo time
         */

        /**
         * Constructs a new BoxInfo.
         * @memberof common
         * @classdesc Represents a BoxInfo.
         * @implements IBoxInfo
         * @constructor
         * @param {common.IBoxInfo=} [properties] Properties to set
         */
        function BoxInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BoxInfo bid.
         * @member {number} bid
         * @memberof common.BoxInfo
         * @instance
         */
        BoxInfo.prototype.bid = 0;

        /**
         * BoxInfo id.
         * @member {number} id
         * @memberof common.BoxInfo
         * @instance
         */
        BoxInfo.prototype.id = 0;

        /**
         * BoxInfo time.
         * @member {number|Long} time
         * @memberof common.BoxInfo
         * @instance
         */
        BoxInfo.prototype.time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new BoxInfo instance using the specified properties.
         * @function create
         * @memberof common.BoxInfo
         * @static
         * @param {common.IBoxInfo=} [properties] Properties to set
         * @returns {common.BoxInfo} BoxInfo instance
         */
        BoxInfo.create = function create(properties) {
            return new BoxInfo(properties);
        };

        /**
         * Encodes the specified BoxInfo message. Does not implicitly {@link common.BoxInfo.verify|verify} messages.
         * @function encode
         * @memberof common.BoxInfo
         * @static
         * @param {common.IBoxInfo} message BoxInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BoxInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.bid != null && Object.hasOwnProperty.call(message, "bid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.bid);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.id);
            if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.time);
            return writer;
        };

        /**
         * Encodes the specified BoxInfo message, length delimited. Does not implicitly {@link common.BoxInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.BoxInfo
         * @static
         * @param {common.IBoxInfo} message BoxInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BoxInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BoxInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.BoxInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.BoxInfo} BoxInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BoxInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.BoxInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.bid = reader.int32();
                        break;
                    }
                case 2: {
                        message.id = reader.int32();
                        break;
                    }
                case 3: {
                        message.time = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BoxInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.BoxInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.BoxInfo} BoxInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BoxInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BoxInfo message.
         * @function verify
         * @memberof common.BoxInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BoxInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.bid != null && message.hasOwnProperty("bid"))
                if (!$util.isInteger(message.bid))
                    return "bid: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.time != null && message.hasOwnProperty("time"))
                if (!$util.isInteger(message.time) && !(message.time && $util.isInteger(message.time.low) && $util.isInteger(message.time.high)))
                    return "time: integer|Long expected";
            return null;
        };

        /**
         * Creates a BoxInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.BoxInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.BoxInfo} BoxInfo
         */
        BoxInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.BoxInfo)
                return object;
            var message = new $root.common.BoxInfo();
            if (object.bid != null)
                message.bid = object.bid | 0;
            if (object.id != null)
                message.id = object.id | 0;
            if (object.time != null)
                if ($util.Long)
                    (message.time = $util.Long.fromValue(object.time)).unsigned = false;
                else if (typeof object.time === "string")
                    message.time = parseInt(object.time, 10);
                else if (typeof object.time === "number")
                    message.time = object.time;
                else if (typeof object.time === "object")
                    message.time = new $util.LongBits(object.time.low >>> 0, object.time.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a BoxInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.BoxInfo
         * @static
         * @param {common.BoxInfo} message BoxInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BoxInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.bid = 0;
                object.id = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.time = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.time = options.longs === String ? "0" : 0;
            }
            if (message.bid != null && message.hasOwnProperty("bid"))
                object.bid = message.bid;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.time != null && message.hasOwnProperty("time"))
                if (typeof message.time === "number")
                    object.time = options.longs === String ? String(message.time) : message.time;
                else
                    object.time = options.longs === String ? $util.Long.prototype.toString.call(message.time) : options.longs === Number ? new $util.LongBits(message.time.low >>> 0, message.time.high >>> 0).toNumber() : message.time;
            return object;
        };

        /**
         * Converts this BoxInfo to JSON.
         * @function toJSON
         * @memberof common.BoxInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BoxInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BoxInfo
         * @function getTypeUrl
         * @memberof common.BoxInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BoxInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.BoxInfo";
        };

        return BoxInfo;
    })();

    common.DropInfo = (function() {

        /**
         * Properties of a DropInfo.
         * @memberof common
         * @interface IDropInfo
         * @property {number|null} [num] DropInfo num
         * @property {number|null} [cycle] DropInfo cycle
         */

        /**
         * Constructs a new DropInfo.
         * @memberof common
         * @classdesc Represents a DropInfo.
         * @implements IDropInfo
         * @constructor
         * @param {common.IDropInfo=} [properties] Properties to set
         */
        function DropInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DropInfo num.
         * @member {number} num
         * @memberof common.DropInfo
         * @instance
         */
        DropInfo.prototype.num = 0;

        /**
         * DropInfo cycle.
         * @member {number} cycle
         * @memberof common.DropInfo
         * @instance
         */
        DropInfo.prototype.cycle = 0;

        /**
         * Creates a new DropInfo instance using the specified properties.
         * @function create
         * @memberof common.DropInfo
         * @static
         * @param {common.IDropInfo=} [properties] Properties to set
         * @returns {common.DropInfo} DropInfo instance
         */
        DropInfo.create = function create(properties) {
            return new DropInfo(properties);
        };

        /**
         * Encodes the specified DropInfo message. Does not implicitly {@link common.DropInfo.verify|verify} messages.
         * @function encode
         * @memberof common.DropInfo
         * @static
         * @param {common.IDropInfo} message DropInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DropInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.num != null && Object.hasOwnProperty.call(message, "num"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.num);
            if (message.cycle != null && Object.hasOwnProperty.call(message, "cycle"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.cycle);
            return writer;
        };

        /**
         * Encodes the specified DropInfo message, length delimited. Does not implicitly {@link common.DropInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.DropInfo
         * @static
         * @param {common.IDropInfo} message DropInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DropInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a DropInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.DropInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.DropInfo} DropInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DropInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.DropInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.num = reader.int32();
                        break;
                    }
                case 2: {
                        message.cycle = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DropInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.DropInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.DropInfo} DropInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DropInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DropInfo message.
         * @function verify
         * @memberof common.DropInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DropInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.num != null && message.hasOwnProperty("num"))
                if (!$util.isInteger(message.num))
                    return "num: integer expected";
            if (message.cycle != null && message.hasOwnProperty("cycle"))
                if (!$util.isInteger(message.cycle))
                    return "cycle: integer expected";
            return null;
        };

        /**
         * Creates a DropInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.DropInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.DropInfo} DropInfo
         */
        DropInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.DropInfo)
                return object;
            var message = new $root.common.DropInfo();
            if (object.num != null)
                message.num = object.num | 0;
            if (object.cycle != null)
                message.cycle = object.cycle | 0;
            return message;
        };

        /**
         * Creates a plain object from a DropInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.DropInfo
         * @static
         * @param {common.DropInfo} message DropInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DropInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.num = 0;
                object.cycle = 0;
            }
            if (message.num != null && message.hasOwnProperty("num"))
                object.num = message.num;
            if (message.cycle != null && message.hasOwnProperty("cycle"))
                object.cycle = message.cycle;
            return object;
        };

        /**
         * Converts this DropInfo to JSON.
         * @function toJSON
         * @memberof common.DropInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DropInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DropInfo
         * @function getTypeUrl
         * @memberof common.DropInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DropInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.DropInfo";
        };

        return DropInfo;
    })();

    common.ArenaInfo = (function() {

        /**
         * Properties of an ArenaInfo.
         * @memberof common
         * @interface IArenaInfo
         * @property {number|null} [historyCups] ArenaInfo historyCups
         * @property {Array.<number>|null} [prizeRecord] ArenaInfo prizeRecord
         * @property {number|null} [winBoxIndex] ArenaInfo winBoxIndex
         * @property {number|null} [winCount] ArenaInfo winCount
         * @property {number|null} [curWinRepeat] ArenaInfo curWinRepeat
         * @property {number|null} [maxWinRepeat] ArenaInfo maxWinRepeat
         * @property {number|null} [totalWin] ArenaInfo totalWin
         * @property {number|null} [turnTheTide] ArenaInfo turnTheTide
         * @property {number|null} [seasonJoinCount] ArenaInfo seasonJoinCount
         * @property {number|null} [seasonWinCount] ArenaInfo seasonWinCount
         * @property {number|null} [preLeagueId] ArenaInfo preLeagueId
         * @property {number|null} [curLeagueId] ArenaInfo curLeagueId
         * @property {number|null} [preRankGlobal] ArenaInfo preRankGlobal
         * @property {number|null} [preRankLocal] ArenaInfo preRankLocal
         * @property {number|null} [curRankGlobal] ArenaInfo curRankGlobal
         * @property {number|null} [curRankLocal] ArenaInfo curRankLocal
         */

        /**
         * Constructs a new ArenaInfo.
         * @memberof common
         * @classdesc Represents an ArenaInfo.
         * @implements IArenaInfo
         * @constructor
         * @param {common.IArenaInfo=} [properties] Properties to set
         */
        function ArenaInfo(properties) {
            this.prizeRecord = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ArenaInfo historyCups.
         * @member {number} historyCups
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.historyCups = 0;

        /**
         * ArenaInfo prizeRecord.
         * @member {Array.<number>} prizeRecord
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.prizeRecord = $util.emptyArray;

        /**
         * ArenaInfo winBoxIndex.
         * @member {number} winBoxIndex
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.winBoxIndex = 0;

        /**
         * ArenaInfo winCount.
         * @member {number} winCount
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.winCount = 0;

        /**
         * ArenaInfo curWinRepeat.
         * @member {number} curWinRepeat
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.curWinRepeat = 0;

        /**
         * ArenaInfo maxWinRepeat.
         * @member {number} maxWinRepeat
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.maxWinRepeat = 0;

        /**
         * ArenaInfo totalWin.
         * @member {number} totalWin
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.totalWin = 0;

        /**
         * ArenaInfo turnTheTide.
         * @member {number} turnTheTide
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.turnTheTide = 0;

        /**
         * ArenaInfo seasonJoinCount.
         * @member {number} seasonJoinCount
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.seasonJoinCount = 0;

        /**
         * ArenaInfo seasonWinCount.
         * @member {number} seasonWinCount
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.seasonWinCount = 0;

        /**
         * ArenaInfo preLeagueId.
         * @member {number} preLeagueId
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.preLeagueId = 0;

        /**
         * ArenaInfo curLeagueId.
         * @member {number} curLeagueId
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.curLeagueId = 0;

        /**
         * ArenaInfo preRankGlobal.
         * @member {number} preRankGlobal
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.preRankGlobal = 0;

        /**
         * ArenaInfo preRankLocal.
         * @member {number} preRankLocal
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.preRankLocal = 0;

        /**
         * ArenaInfo curRankGlobal.
         * @member {number} curRankGlobal
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.curRankGlobal = 0;

        /**
         * ArenaInfo curRankLocal.
         * @member {number} curRankLocal
         * @memberof common.ArenaInfo
         * @instance
         */
        ArenaInfo.prototype.curRankLocal = 0;

        /**
         * Creates a new ArenaInfo instance using the specified properties.
         * @function create
         * @memberof common.ArenaInfo
         * @static
         * @param {common.IArenaInfo=} [properties] Properties to set
         * @returns {common.ArenaInfo} ArenaInfo instance
         */
        ArenaInfo.create = function create(properties) {
            return new ArenaInfo(properties);
        };

        /**
         * Encodes the specified ArenaInfo message. Does not implicitly {@link common.ArenaInfo.verify|verify} messages.
         * @function encode
         * @memberof common.ArenaInfo
         * @static
         * @param {common.IArenaInfo} message ArenaInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ArenaInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.historyCups != null && Object.hasOwnProperty.call(message, "historyCups"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.historyCups);
            if (message.prizeRecord != null && message.prizeRecord.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (var i = 0; i < message.prizeRecord.length; ++i)
                    writer.int32(message.prizeRecord[i]);
                writer.ldelim();
            }
            if (message.winBoxIndex != null && Object.hasOwnProperty.call(message, "winBoxIndex"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.winBoxIndex);
            if (message.winCount != null && Object.hasOwnProperty.call(message, "winCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.winCount);
            if (message.curWinRepeat != null && Object.hasOwnProperty.call(message, "curWinRepeat"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.curWinRepeat);
            if (message.maxWinRepeat != null && Object.hasOwnProperty.call(message, "maxWinRepeat"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.maxWinRepeat);
            if (message.totalWin != null && Object.hasOwnProperty.call(message, "totalWin"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.totalWin);
            if (message.turnTheTide != null && Object.hasOwnProperty.call(message, "turnTheTide"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.turnTheTide);
            if (message.seasonJoinCount != null && Object.hasOwnProperty.call(message, "seasonJoinCount"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.seasonJoinCount);
            if (message.seasonWinCount != null && Object.hasOwnProperty.call(message, "seasonWinCount"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.seasonWinCount);
            if (message.preLeagueId != null && Object.hasOwnProperty.call(message, "preLeagueId"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.preLeagueId);
            if (message.curLeagueId != null && Object.hasOwnProperty.call(message, "curLeagueId"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.curLeagueId);
            if (message.preRankGlobal != null && Object.hasOwnProperty.call(message, "preRankGlobal"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.preRankGlobal);
            if (message.preRankLocal != null && Object.hasOwnProperty.call(message, "preRankLocal"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.preRankLocal);
            if (message.curRankGlobal != null && Object.hasOwnProperty.call(message, "curRankGlobal"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.curRankGlobal);
            if (message.curRankLocal != null && Object.hasOwnProperty.call(message, "curRankLocal"))
                writer.uint32(/* id 16, wireType 0 =*/128).int32(message.curRankLocal);
            return writer;
        };

        /**
         * Encodes the specified ArenaInfo message, length delimited. Does not implicitly {@link common.ArenaInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.ArenaInfo
         * @static
         * @param {common.IArenaInfo} message ArenaInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ArenaInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ArenaInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.ArenaInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.ArenaInfo} ArenaInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ArenaInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.ArenaInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.historyCups = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.prizeRecord && message.prizeRecord.length))
                            message.prizeRecord = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.prizeRecord.push(reader.int32());
                        } else
                            message.prizeRecord.push(reader.int32());
                        break;
                    }
                case 3: {
                        message.winBoxIndex = reader.int32();
                        break;
                    }
                case 4: {
                        message.winCount = reader.int32();
                        break;
                    }
                case 5: {
                        message.curWinRepeat = reader.int32();
                        break;
                    }
                case 6: {
                        message.maxWinRepeat = reader.int32();
                        break;
                    }
                case 7: {
                        message.totalWin = reader.int32();
                        break;
                    }
                case 8: {
                        message.turnTheTide = reader.int32();
                        break;
                    }
                case 9: {
                        message.seasonJoinCount = reader.int32();
                        break;
                    }
                case 10: {
                        message.seasonWinCount = reader.int32();
                        break;
                    }
                case 11: {
                        message.preLeagueId = reader.int32();
                        break;
                    }
                case 12: {
                        message.curLeagueId = reader.int32();
                        break;
                    }
                case 13: {
                        message.preRankGlobal = reader.int32();
                        break;
                    }
                case 14: {
                        message.preRankLocal = reader.int32();
                        break;
                    }
                case 15: {
                        message.curRankGlobal = reader.int32();
                        break;
                    }
                case 16: {
                        message.curRankLocal = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ArenaInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.ArenaInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.ArenaInfo} ArenaInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ArenaInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ArenaInfo message.
         * @function verify
         * @memberof common.ArenaInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ArenaInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.historyCups != null && message.hasOwnProperty("historyCups"))
                if (!$util.isInteger(message.historyCups))
                    return "historyCups: integer expected";
            if (message.prizeRecord != null && message.hasOwnProperty("prizeRecord")) {
                if (!Array.isArray(message.prizeRecord))
                    return "prizeRecord: array expected";
                for (var i = 0; i < message.prizeRecord.length; ++i)
                    if (!$util.isInteger(message.prizeRecord[i]))
                        return "prizeRecord: integer[] expected";
            }
            if (message.winBoxIndex != null && message.hasOwnProperty("winBoxIndex"))
                if (!$util.isInteger(message.winBoxIndex))
                    return "winBoxIndex: integer expected";
            if (message.winCount != null && message.hasOwnProperty("winCount"))
                if (!$util.isInteger(message.winCount))
                    return "winCount: integer expected";
            if (message.curWinRepeat != null && message.hasOwnProperty("curWinRepeat"))
                if (!$util.isInteger(message.curWinRepeat))
                    return "curWinRepeat: integer expected";
            if (message.maxWinRepeat != null && message.hasOwnProperty("maxWinRepeat"))
                if (!$util.isInteger(message.maxWinRepeat))
                    return "maxWinRepeat: integer expected";
            if (message.totalWin != null && message.hasOwnProperty("totalWin"))
                if (!$util.isInteger(message.totalWin))
                    return "totalWin: integer expected";
            if (message.turnTheTide != null && message.hasOwnProperty("turnTheTide"))
                if (!$util.isInteger(message.turnTheTide))
                    return "turnTheTide: integer expected";
            if (message.seasonJoinCount != null && message.hasOwnProperty("seasonJoinCount"))
                if (!$util.isInteger(message.seasonJoinCount))
                    return "seasonJoinCount: integer expected";
            if (message.seasonWinCount != null && message.hasOwnProperty("seasonWinCount"))
                if (!$util.isInteger(message.seasonWinCount))
                    return "seasonWinCount: integer expected";
            if (message.preLeagueId != null && message.hasOwnProperty("preLeagueId"))
                if (!$util.isInteger(message.preLeagueId))
                    return "preLeagueId: integer expected";
            if (message.curLeagueId != null && message.hasOwnProperty("curLeagueId"))
                if (!$util.isInteger(message.curLeagueId))
                    return "curLeagueId: integer expected";
            if (message.preRankGlobal != null && message.hasOwnProperty("preRankGlobal"))
                if (!$util.isInteger(message.preRankGlobal))
                    return "preRankGlobal: integer expected";
            if (message.preRankLocal != null && message.hasOwnProperty("preRankLocal"))
                if (!$util.isInteger(message.preRankLocal))
                    return "preRankLocal: integer expected";
            if (message.curRankGlobal != null && message.hasOwnProperty("curRankGlobal"))
                if (!$util.isInteger(message.curRankGlobal))
                    return "curRankGlobal: integer expected";
            if (message.curRankLocal != null && message.hasOwnProperty("curRankLocal"))
                if (!$util.isInteger(message.curRankLocal))
                    return "curRankLocal: integer expected";
            return null;
        };

        /**
         * Creates an ArenaInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.ArenaInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.ArenaInfo} ArenaInfo
         */
        ArenaInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.ArenaInfo)
                return object;
            var message = new $root.common.ArenaInfo();
            if (object.historyCups != null)
                message.historyCups = object.historyCups | 0;
            if (object.prizeRecord) {
                if (!Array.isArray(object.prizeRecord))
                    throw TypeError(".common.ArenaInfo.prizeRecord: array expected");
                message.prizeRecord = [];
                for (var i = 0; i < object.prizeRecord.length; ++i)
                    message.prizeRecord[i] = object.prizeRecord[i] | 0;
            }
            if (object.winBoxIndex != null)
                message.winBoxIndex = object.winBoxIndex | 0;
            if (object.winCount != null)
                message.winCount = object.winCount | 0;
            if (object.curWinRepeat != null)
                message.curWinRepeat = object.curWinRepeat | 0;
            if (object.maxWinRepeat != null)
                message.maxWinRepeat = object.maxWinRepeat | 0;
            if (object.totalWin != null)
                message.totalWin = object.totalWin | 0;
            if (object.turnTheTide != null)
                message.turnTheTide = object.turnTheTide | 0;
            if (object.seasonJoinCount != null)
                message.seasonJoinCount = object.seasonJoinCount | 0;
            if (object.seasonWinCount != null)
                message.seasonWinCount = object.seasonWinCount | 0;
            if (object.preLeagueId != null)
                message.preLeagueId = object.preLeagueId | 0;
            if (object.curLeagueId != null)
                message.curLeagueId = object.curLeagueId | 0;
            if (object.preRankGlobal != null)
                message.preRankGlobal = object.preRankGlobal | 0;
            if (object.preRankLocal != null)
                message.preRankLocal = object.preRankLocal | 0;
            if (object.curRankGlobal != null)
                message.curRankGlobal = object.curRankGlobal | 0;
            if (object.curRankLocal != null)
                message.curRankLocal = object.curRankLocal | 0;
            return message;
        };

        /**
         * Creates a plain object from an ArenaInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.ArenaInfo
         * @static
         * @param {common.ArenaInfo} message ArenaInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ArenaInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.prizeRecord = [];
            if (options.defaults) {
                object.historyCups = 0;
                object.winBoxIndex = 0;
                object.winCount = 0;
                object.curWinRepeat = 0;
                object.maxWinRepeat = 0;
                object.totalWin = 0;
                object.turnTheTide = 0;
                object.seasonJoinCount = 0;
                object.seasonWinCount = 0;
                object.preLeagueId = 0;
                object.curLeagueId = 0;
                object.preRankGlobal = 0;
                object.preRankLocal = 0;
                object.curRankGlobal = 0;
                object.curRankLocal = 0;
            }
            if (message.historyCups != null && message.hasOwnProperty("historyCups"))
                object.historyCups = message.historyCups;
            if (message.prizeRecord && message.prizeRecord.length) {
                object.prizeRecord = [];
                for (var j = 0; j < message.prizeRecord.length; ++j)
                    object.prizeRecord[j] = message.prizeRecord[j];
            }
            if (message.winBoxIndex != null && message.hasOwnProperty("winBoxIndex"))
                object.winBoxIndex = message.winBoxIndex;
            if (message.winCount != null && message.hasOwnProperty("winCount"))
                object.winCount = message.winCount;
            if (message.curWinRepeat != null && message.hasOwnProperty("curWinRepeat"))
                object.curWinRepeat = message.curWinRepeat;
            if (message.maxWinRepeat != null && message.hasOwnProperty("maxWinRepeat"))
                object.maxWinRepeat = message.maxWinRepeat;
            if (message.totalWin != null && message.hasOwnProperty("totalWin"))
                object.totalWin = message.totalWin;
            if (message.turnTheTide != null && message.hasOwnProperty("turnTheTide"))
                object.turnTheTide = message.turnTheTide;
            if (message.seasonJoinCount != null && message.hasOwnProperty("seasonJoinCount"))
                object.seasonJoinCount = message.seasonJoinCount;
            if (message.seasonWinCount != null && message.hasOwnProperty("seasonWinCount"))
                object.seasonWinCount = message.seasonWinCount;
            if (message.preLeagueId != null && message.hasOwnProperty("preLeagueId"))
                object.preLeagueId = message.preLeagueId;
            if (message.curLeagueId != null && message.hasOwnProperty("curLeagueId"))
                object.curLeagueId = message.curLeagueId;
            if (message.preRankGlobal != null && message.hasOwnProperty("preRankGlobal"))
                object.preRankGlobal = message.preRankGlobal;
            if (message.preRankLocal != null && message.hasOwnProperty("preRankLocal"))
                object.preRankLocal = message.preRankLocal;
            if (message.curRankGlobal != null && message.hasOwnProperty("curRankGlobal"))
                object.curRankGlobal = message.curRankGlobal;
            if (message.curRankLocal != null && message.hasOwnProperty("curRankLocal"))
                object.curRankLocal = message.curRankLocal;
            return object;
        };

        /**
         * Converts this ArenaInfo to JSON.
         * @function toJSON
         * @memberof common.ArenaInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ArenaInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ArenaInfo
         * @function getTypeUrl
         * @memberof common.ArenaInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ArenaInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.ArenaInfo";
        };

        return ArenaInfo;
    })();

    common.MarkInfo = (function() {

        /**
         * Properties of a MarkInfo.
         * @memberof common
         * @interface IMarkInfo
         * @property {number|null} [userNameModifyCount] MarkInfo userNameModifyCount
         * @property {number|null} [guildNameModifyCount] MarkInfo guildNameModifyCount
         * @property {number|null} [adTimesToday] MarkInfo adTimesToday
         */

        /**
         * Constructs a new MarkInfo.
         * @memberof common
         * @classdesc Represents a MarkInfo.
         * @implements IMarkInfo
         * @constructor
         * @param {common.IMarkInfo=} [properties] Properties to set
         */
        function MarkInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MarkInfo userNameModifyCount.
         * @member {number} userNameModifyCount
         * @memberof common.MarkInfo
         * @instance
         */
        MarkInfo.prototype.userNameModifyCount = 0;

        /**
         * MarkInfo guildNameModifyCount.
         * @member {number} guildNameModifyCount
         * @memberof common.MarkInfo
         * @instance
         */
        MarkInfo.prototype.guildNameModifyCount = 0;

        /**
         * MarkInfo adTimesToday.
         * @member {number} adTimesToday
         * @memberof common.MarkInfo
         * @instance
         */
        MarkInfo.prototype.adTimesToday = 0;

        /**
         * Creates a new MarkInfo instance using the specified properties.
         * @function create
         * @memberof common.MarkInfo
         * @static
         * @param {common.IMarkInfo=} [properties] Properties to set
         * @returns {common.MarkInfo} MarkInfo instance
         */
        MarkInfo.create = function create(properties) {
            return new MarkInfo(properties);
        };

        /**
         * Encodes the specified MarkInfo message. Does not implicitly {@link common.MarkInfo.verify|verify} messages.
         * @function encode
         * @memberof common.MarkInfo
         * @static
         * @param {common.IMarkInfo} message MarkInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MarkInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.userNameModifyCount != null && Object.hasOwnProperty.call(message, "userNameModifyCount"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.userNameModifyCount);
            if (message.guildNameModifyCount != null && Object.hasOwnProperty.call(message, "guildNameModifyCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.guildNameModifyCount);
            if (message.adTimesToday != null && Object.hasOwnProperty.call(message, "adTimesToday"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.adTimesToday);
            return writer;
        };

        /**
         * Encodes the specified MarkInfo message, length delimited. Does not implicitly {@link common.MarkInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.MarkInfo
         * @static
         * @param {common.IMarkInfo} message MarkInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MarkInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MarkInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.MarkInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.MarkInfo} MarkInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MarkInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.MarkInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.userNameModifyCount = reader.int32();
                        break;
                    }
                case 2: {
                        message.guildNameModifyCount = reader.int32();
                        break;
                    }
                case 3: {
                        message.adTimesToday = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MarkInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.MarkInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.MarkInfo} MarkInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MarkInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MarkInfo message.
         * @function verify
         * @memberof common.MarkInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MarkInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.userNameModifyCount != null && message.hasOwnProperty("userNameModifyCount"))
                if (!$util.isInteger(message.userNameModifyCount))
                    return "userNameModifyCount: integer expected";
            if (message.guildNameModifyCount != null && message.hasOwnProperty("guildNameModifyCount"))
                if (!$util.isInteger(message.guildNameModifyCount))
                    return "guildNameModifyCount: integer expected";
            if (message.adTimesToday != null && message.hasOwnProperty("adTimesToday"))
                if (!$util.isInteger(message.adTimesToday))
                    return "adTimesToday: integer expected";
            return null;
        };

        /**
         * Creates a MarkInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.MarkInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.MarkInfo} MarkInfo
         */
        MarkInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.MarkInfo)
                return object;
            var message = new $root.common.MarkInfo();
            if (object.userNameModifyCount != null)
                message.userNameModifyCount = object.userNameModifyCount | 0;
            if (object.guildNameModifyCount != null)
                message.guildNameModifyCount = object.guildNameModifyCount | 0;
            if (object.adTimesToday != null)
                message.adTimesToday = object.adTimesToday | 0;
            return message;
        };

        /**
         * Creates a plain object from a MarkInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.MarkInfo
         * @static
         * @param {common.MarkInfo} message MarkInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MarkInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.userNameModifyCount = 0;
                object.guildNameModifyCount = 0;
                object.adTimesToday = 0;
            }
            if (message.userNameModifyCount != null && message.hasOwnProperty("userNameModifyCount"))
                object.userNameModifyCount = message.userNameModifyCount;
            if (message.guildNameModifyCount != null && message.hasOwnProperty("guildNameModifyCount"))
                object.guildNameModifyCount = message.guildNameModifyCount;
            if (message.adTimesToday != null && message.hasOwnProperty("adTimesToday"))
                object.adTimesToday = message.adTimesToday;
            return object;
        };

        /**
         * Converts this MarkInfo to JSON.
         * @function toJSON
         * @memberof common.MarkInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MarkInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MarkInfo
         * @function getTypeUrl
         * @memberof common.MarkInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MarkInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.MarkInfo";
        };

        return MarkInfo;
    })();

    common.BindInfo = (function() {

        /**
         * Properties of a BindInfo.
         * @memberof common
         * @interface IBindInfo
         * @property {string|null} [wx] BindInfo wx
         * @property {string|null} [qq] BindInfo qq
         * @property {string|null} [fb] BindInfo fb
         * @property {string|null} [gmail] BindInfo gmail
         * @property {string|null} [twitter] BindInfo twitter
         */

        /**
         * Constructs a new BindInfo.
         * @memberof common
         * @classdesc Represents a BindInfo.
         * @implements IBindInfo
         * @constructor
         * @param {common.IBindInfo=} [properties] Properties to set
         */
        function BindInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BindInfo wx.
         * @member {string} wx
         * @memberof common.BindInfo
         * @instance
         */
        BindInfo.prototype.wx = "";

        /**
         * BindInfo qq.
         * @member {string} qq
         * @memberof common.BindInfo
         * @instance
         */
        BindInfo.prototype.qq = "";

        /**
         * BindInfo fb.
         * @member {string} fb
         * @memberof common.BindInfo
         * @instance
         */
        BindInfo.prototype.fb = "";

        /**
         * BindInfo gmail.
         * @member {string} gmail
         * @memberof common.BindInfo
         * @instance
         */
        BindInfo.prototype.gmail = "";

        /**
         * BindInfo twitter.
         * @member {string} twitter
         * @memberof common.BindInfo
         * @instance
         */
        BindInfo.prototype.twitter = "";

        /**
         * Creates a new BindInfo instance using the specified properties.
         * @function create
         * @memberof common.BindInfo
         * @static
         * @param {common.IBindInfo=} [properties] Properties to set
         * @returns {common.BindInfo} BindInfo instance
         */
        BindInfo.create = function create(properties) {
            return new BindInfo(properties);
        };

        /**
         * Encodes the specified BindInfo message. Does not implicitly {@link common.BindInfo.verify|verify} messages.
         * @function encode
         * @memberof common.BindInfo
         * @static
         * @param {common.IBindInfo} message BindInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BindInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.wx != null && Object.hasOwnProperty.call(message, "wx"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.wx);
            if (message.qq != null && Object.hasOwnProperty.call(message, "qq"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.qq);
            if (message.fb != null && Object.hasOwnProperty.call(message, "fb"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.fb);
            if (message.gmail != null && Object.hasOwnProperty.call(message, "gmail"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.gmail);
            if (message.twitter != null && Object.hasOwnProperty.call(message, "twitter"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.twitter);
            return writer;
        };

        /**
         * Encodes the specified BindInfo message, length delimited. Does not implicitly {@link common.BindInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.BindInfo
         * @static
         * @param {common.IBindInfo} message BindInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BindInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BindInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.BindInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.BindInfo} BindInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BindInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.BindInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.wx = reader.string();
                        break;
                    }
                case 2: {
                        message.qq = reader.string();
                        break;
                    }
                case 3: {
                        message.fb = reader.string();
                        break;
                    }
                case 4: {
                        message.gmail = reader.string();
                        break;
                    }
                case 5: {
                        message.twitter = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BindInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.BindInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.BindInfo} BindInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BindInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BindInfo message.
         * @function verify
         * @memberof common.BindInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BindInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.wx != null && message.hasOwnProperty("wx"))
                if (!$util.isString(message.wx))
                    return "wx: string expected";
            if (message.qq != null && message.hasOwnProperty("qq"))
                if (!$util.isString(message.qq))
                    return "qq: string expected";
            if (message.fb != null && message.hasOwnProperty("fb"))
                if (!$util.isString(message.fb))
                    return "fb: string expected";
            if (message.gmail != null && message.hasOwnProperty("gmail"))
                if (!$util.isString(message.gmail))
                    return "gmail: string expected";
            if (message.twitter != null && message.hasOwnProperty("twitter"))
                if (!$util.isString(message.twitter))
                    return "twitter: string expected";
            return null;
        };

        /**
         * Creates a BindInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.BindInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.BindInfo} BindInfo
         */
        BindInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.BindInfo)
                return object;
            var message = new $root.common.BindInfo();
            if (object.wx != null)
                message.wx = String(object.wx);
            if (object.qq != null)
                message.qq = String(object.qq);
            if (object.fb != null)
                message.fb = String(object.fb);
            if (object.gmail != null)
                message.gmail = String(object.gmail);
            if (object.twitter != null)
                message.twitter = String(object.twitter);
            return message;
        };

        /**
         * Creates a plain object from a BindInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.BindInfo
         * @static
         * @param {common.BindInfo} message BindInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BindInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.wx = "";
                object.qq = "";
                object.fb = "";
                object.gmail = "";
                object.twitter = "";
            }
            if (message.wx != null && message.hasOwnProperty("wx"))
                object.wx = message.wx;
            if (message.qq != null && message.hasOwnProperty("qq"))
                object.qq = message.qq;
            if (message.fb != null && message.hasOwnProperty("fb"))
                object.fb = message.fb;
            if (message.gmail != null && message.hasOwnProperty("gmail"))
                object.gmail = message.gmail;
            if (message.twitter != null && message.hasOwnProperty("twitter"))
                object.twitter = message.twitter;
            return object;
        };

        /**
         * Converts this BindInfo to JSON.
         * @function toJSON
         * @memberof common.BindInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BindInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BindInfo
         * @function getTypeUrl
         * @memberof common.BindInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BindInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.BindInfo";
        };

        return BindInfo;
    })();

    common.AdventureTeamInfo = (function() {

        /**
         * Properties of an AdventureTeamInfo.
         * @memberof common
         * @interface IAdventureTeamInfo
         * @property {number|null} [id] AdventureTeamInfo id
         * @property {number|Long|null} [startTime] AdventureTeamInfo startTime
         * @property {number|null} [costTime] AdventureTeamInfo costTime
         * @property {number|Long|null} [moveStartTime] AdventureTeamInfo moveStartTime
         * @property {number|null} [moveStartPoint] AdventureTeamInfo moveStartPoint
         * @property {number|null} [moveTarget] AdventureTeamInfo moveTarget
         * @property {number|null} [usingTool] AdventureTeamInfo usingTool
         * @property {Object.<string,number>|null} [slots] AdventureTeamInfo slots
         */

        /**
         * Constructs a new AdventureTeamInfo.
         * @memberof common
         * @classdesc Represents an AdventureTeamInfo.
         * @implements IAdventureTeamInfo
         * @constructor
         * @param {common.IAdventureTeamInfo=} [properties] Properties to set
         */
        function AdventureTeamInfo(properties) {
            this.slots = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AdventureTeamInfo id.
         * @member {number} id
         * @memberof common.AdventureTeamInfo
         * @instance
         */
        AdventureTeamInfo.prototype.id = 0;

        /**
         * AdventureTeamInfo startTime.
         * @member {number|Long} startTime
         * @memberof common.AdventureTeamInfo
         * @instance
         */
        AdventureTeamInfo.prototype.startTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AdventureTeamInfo costTime.
         * @member {number} costTime
         * @memberof common.AdventureTeamInfo
         * @instance
         */
        AdventureTeamInfo.prototype.costTime = 0;

        /**
         * AdventureTeamInfo moveStartTime.
         * @member {number|Long} moveStartTime
         * @memberof common.AdventureTeamInfo
         * @instance
         */
        AdventureTeamInfo.prototype.moveStartTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AdventureTeamInfo moveStartPoint.
         * @member {number} moveStartPoint
         * @memberof common.AdventureTeamInfo
         * @instance
         */
        AdventureTeamInfo.prototype.moveStartPoint = 0;

        /**
         * AdventureTeamInfo moveTarget.
         * @member {number} moveTarget
         * @memberof common.AdventureTeamInfo
         * @instance
         */
        AdventureTeamInfo.prototype.moveTarget = 0;

        /**
         * AdventureTeamInfo usingTool.
         * @member {number} usingTool
         * @memberof common.AdventureTeamInfo
         * @instance
         */
        AdventureTeamInfo.prototype.usingTool = 0;

        /**
         * AdventureTeamInfo slots.
         * @member {Object.<string,number>} slots
         * @memberof common.AdventureTeamInfo
         * @instance
         */
        AdventureTeamInfo.prototype.slots = $util.emptyObject;

        /**
         * Creates a new AdventureTeamInfo instance using the specified properties.
         * @function create
         * @memberof common.AdventureTeamInfo
         * @static
         * @param {common.IAdventureTeamInfo=} [properties] Properties to set
         * @returns {common.AdventureTeamInfo} AdventureTeamInfo instance
         */
        AdventureTeamInfo.create = function create(properties) {
            return new AdventureTeamInfo(properties);
        };

        /**
         * Encodes the specified AdventureTeamInfo message. Does not implicitly {@link common.AdventureTeamInfo.verify|verify} messages.
         * @function encode
         * @memberof common.AdventureTeamInfo
         * @static
         * @param {common.IAdventureTeamInfo} message AdventureTeamInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AdventureTeamInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.startTime);
            if (message.costTime != null && Object.hasOwnProperty.call(message, "costTime"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.costTime);
            if (message.moveStartTime != null && Object.hasOwnProperty.call(message, "moveStartTime"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.moveStartTime);
            if (message.moveStartPoint != null && Object.hasOwnProperty.call(message, "moveStartPoint"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.moveStartPoint);
            if (message.moveTarget != null && Object.hasOwnProperty.call(message, "moveTarget"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.moveTarget);
            if (message.usingTool != null && Object.hasOwnProperty.call(message, "usingTool"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.usingTool);
            if (message.slots != null && Object.hasOwnProperty.call(message, "slots"))
                for (var keys = Object.keys(message.slots), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.slots[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AdventureTeamInfo message, length delimited. Does not implicitly {@link common.AdventureTeamInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.AdventureTeamInfo
         * @static
         * @param {common.IAdventureTeamInfo} message AdventureTeamInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AdventureTeamInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AdventureTeamInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.AdventureTeamInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.AdventureTeamInfo} AdventureTeamInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AdventureTeamInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.AdventureTeamInfo(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.startTime = reader.int64();
                        break;
                    }
                case 3: {
                        message.costTime = reader.int32();
                        break;
                    }
                case 4: {
                        message.moveStartTime = reader.int64();
                        break;
                    }
                case 5: {
                        message.moveStartPoint = reader.int32();
                        break;
                    }
                case 6: {
                        message.moveTarget = reader.int32();
                        break;
                    }
                case 7: {
                        message.usingTool = reader.int32();
                        break;
                    }
                case 8: {
                        if (message.slots === $util.emptyObject)
                            message.slots = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int32();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.slots[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AdventureTeamInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.AdventureTeamInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.AdventureTeamInfo} AdventureTeamInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AdventureTeamInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AdventureTeamInfo message.
         * @function verify
         * @memberof common.AdventureTeamInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AdventureTeamInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.startTime != null && message.hasOwnProperty("startTime"))
                if (!$util.isInteger(message.startTime) && !(message.startTime && $util.isInteger(message.startTime.low) && $util.isInteger(message.startTime.high)))
                    return "startTime: integer|Long expected";
            if (message.costTime != null && message.hasOwnProperty("costTime"))
                if (!$util.isInteger(message.costTime))
                    return "costTime: integer expected";
            if (message.moveStartTime != null && message.hasOwnProperty("moveStartTime"))
                if (!$util.isInteger(message.moveStartTime) && !(message.moveStartTime && $util.isInteger(message.moveStartTime.low) && $util.isInteger(message.moveStartTime.high)))
                    return "moveStartTime: integer|Long expected";
            if (message.moveStartPoint != null && message.hasOwnProperty("moveStartPoint"))
                if (!$util.isInteger(message.moveStartPoint))
                    return "moveStartPoint: integer expected";
            if (message.moveTarget != null && message.hasOwnProperty("moveTarget"))
                if (!$util.isInteger(message.moveTarget))
                    return "moveTarget: integer expected";
            if (message.usingTool != null && message.hasOwnProperty("usingTool"))
                if (!$util.isInteger(message.usingTool))
                    return "usingTool: integer expected";
            if (message.slots != null && message.hasOwnProperty("slots")) {
                if (!$util.isObject(message.slots))
                    return "slots: object expected";
                var key = Object.keys(message.slots);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "slots: integer key{k:int32} expected";
                    if (!$util.isInteger(message.slots[key[i]]))
                        return "slots: integer{k:int32} expected";
                }
            }
            return null;
        };

        /**
         * Creates an AdventureTeamInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.AdventureTeamInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.AdventureTeamInfo} AdventureTeamInfo
         */
        AdventureTeamInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.AdventureTeamInfo)
                return object;
            var message = new $root.common.AdventureTeamInfo();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.startTime != null)
                if ($util.Long)
                    (message.startTime = $util.Long.fromValue(object.startTime)).unsigned = false;
                else if (typeof object.startTime === "string")
                    message.startTime = parseInt(object.startTime, 10);
                else if (typeof object.startTime === "number")
                    message.startTime = object.startTime;
                else if (typeof object.startTime === "object")
                    message.startTime = new $util.LongBits(object.startTime.low >>> 0, object.startTime.high >>> 0).toNumber();
            if (object.costTime != null)
                message.costTime = object.costTime | 0;
            if (object.moveStartTime != null)
                if ($util.Long)
                    (message.moveStartTime = $util.Long.fromValue(object.moveStartTime)).unsigned = false;
                else if (typeof object.moveStartTime === "string")
                    message.moveStartTime = parseInt(object.moveStartTime, 10);
                else if (typeof object.moveStartTime === "number")
                    message.moveStartTime = object.moveStartTime;
                else if (typeof object.moveStartTime === "object")
                    message.moveStartTime = new $util.LongBits(object.moveStartTime.low >>> 0, object.moveStartTime.high >>> 0).toNumber();
            if (object.moveStartPoint != null)
                message.moveStartPoint = object.moveStartPoint | 0;
            if (object.moveTarget != null)
                message.moveTarget = object.moveTarget | 0;
            if (object.usingTool != null)
                message.usingTool = object.usingTool | 0;
            if (object.slots) {
                if (typeof object.slots !== "object")
                    throw TypeError(".common.AdventureTeamInfo.slots: object expected");
                message.slots = {};
                for (var keys = Object.keys(object.slots), i = 0; i < keys.length; ++i)
                    message.slots[keys[i]] = object.slots[keys[i]] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an AdventureTeamInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.AdventureTeamInfo
         * @static
         * @param {common.AdventureTeamInfo} message AdventureTeamInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AdventureTeamInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.objects || options.defaults)
                object.slots = {};
            if (options.defaults) {
                object.id = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.startTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.startTime = options.longs === String ? "0" : 0;
                object.costTime = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.moveStartTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.moveStartTime = options.longs === String ? "0" : 0;
                object.moveStartPoint = 0;
                object.moveTarget = 0;
                object.usingTool = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.startTime != null && message.hasOwnProperty("startTime"))
                if (typeof message.startTime === "number")
                    object.startTime = options.longs === String ? String(message.startTime) : message.startTime;
                else
                    object.startTime = options.longs === String ? $util.Long.prototype.toString.call(message.startTime) : options.longs === Number ? new $util.LongBits(message.startTime.low >>> 0, message.startTime.high >>> 0).toNumber() : message.startTime;
            if (message.costTime != null && message.hasOwnProperty("costTime"))
                object.costTime = message.costTime;
            if (message.moveStartTime != null && message.hasOwnProperty("moveStartTime"))
                if (typeof message.moveStartTime === "number")
                    object.moveStartTime = options.longs === String ? String(message.moveStartTime) : message.moveStartTime;
                else
                    object.moveStartTime = options.longs === String ? $util.Long.prototype.toString.call(message.moveStartTime) : options.longs === Number ? new $util.LongBits(message.moveStartTime.low >>> 0, message.moveStartTime.high >>> 0).toNumber() : message.moveStartTime;
            if (message.moveStartPoint != null && message.hasOwnProperty("moveStartPoint"))
                object.moveStartPoint = message.moveStartPoint;
            if (message.moveTarget != null && message.hasOwnProperty("moveTarget"))
                object.moveTarget = message.moveTarget;
            if (message.usingTool != null && message.hasOwnProperty("usingTool"))
                object.usingTool = message.usingTool;
            var keys2;
            if (message.slots && (keys2 = Object.keys(message.slots)).length) {
                object.slots = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.slots[keys2[j]] = message.slots[keys2[j]];
            }
            return object;
        };

        /**
         * Converts this AdventureTeamInfo to JSON.
         * @function toJSON
         * @memberof common.AdventureTeamInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AdventureTeamInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AdventureTeamInfo
         * @function getTypeUrl
         * @memberof common.AdventureTeamInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AdventureTeamInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.AdventureTeamInfo";
        };

        return AdventureTeamInfo;
    })();

    common.AdventureAreaInfo = (function() {

        /**
         * Properties of an AdventureAreaInfo.
         * @memberof common
         * @interface IAdventureAreaInfo
         * @property {number|null} [level] AdventureAreaInfo level
         * @property {number|null} [progress] AdventureAreaInfo progress
         * @property {Object.<string,number>|null} [onceAward] AdventureAreaInfo onceAward
         * @property {Object.<string,number>|null} [upgradeAward] AdventureAreaInfo upgradeAward
         */

        /**
         * Constructs a new AdventureAreaInfo.
         * @memberof common
         * @classdesc Represents an AdventureAreaInfo.
         * @implements IAdventureAreaInfo
         * @constructor
         * @param {common.IAdventureAreaInfo=} [properties] Properties to set
         */
        function AdventureAreaInfo(properties) {
            this.onceAward = {};
            this.upgradeAward = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AdventureAreaInfo level.
         * @member {number} level
         * @memberof common.AdventureAreaInfo
         * @instance
         */
        AdventureAreaInfo.prototype.level = 0;

        /**
         * AdventureAreaInfo progress.
         * @member {number} progress
         * @memberof common.AdventureAreaInfo
         * @instance
         */
        AdventureAreaInfo.prototype.progress = 0;

        /**
         * AdventureAreaInfo onceAward.
         * @member {Object.<string,number>} onceAward
         * @memberof common.AdventureAreaInfo
         * @instance
         */
        AdventureAreaInfo.prototype.onceAward = $util.emptyObject;

        /**
         * AdventureAreaInfo upgradeAward.
         * @member {Object.<string,number>} upgradeAward
         * @memberof common.AdventureAreaInfo
         * @instance
         */
        AdventureAreaInfo.prototype.upgradeAward = $util.emptyObject;

        /**
         * Creates a new AdventureAreaInfo instance using the specified properties.
         * @function create
         * @memberof common.AdventureAreaInfo
         * @static
         * @param {common.IAdventureAreaInfo=} [properties] Properties to set
         * @returns {common.AdventureAreaInfo} AdventureAreaInfo instance
         */
        AdventureAreaInfo.create = function create(properties) {
            return new AdventureAreaInfo(properties);
        };

        /**
         * Encodes the specified AdventureAreaInfo message. Does not implicitly {@link common.AdventureAreaInfo.verify|verify} messages.
         * @function encode
         * @memberof common.AdventureAreaInfo
         * @static
         * @param {common.IAdventureAreaInfo} message AdventureAreaInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AdventureAreaInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.level);
            if (message.progress != null && Object.hasOwnProperty.call(message, "progress"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.progress);
            if (message.onceAward != null && Object.hasOwnProperty.call(message, "onceAward"))
                for (var keys = Object.keys(message.onceAward), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.onceAward[keys[i]]).ldelim();
            if (message.upgradeAward != null && Object.hasOwnProperty.call(message, "upgradeAward"))
                for (var keys = Object.keys(message.upgradeAward), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 4, wireType 2 =*/34).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.upgradeAward[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AdventureAreaInfo message, length delimited. Does not implicitly {@link common.AdventureAreaInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.AdventureAreaInfo
         * @static
         * @param {common.IAdventureAreaInfo} message AdventureAreaInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AdventureAreaInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AdventureAreaInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.AdventureAreaInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.AdventureAreaInfo} AdventureAreaInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AdventureAreaInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.AdventureAreaInfo(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.level = reader.int32();
                        break;
                    }
                case 2: {
                        message.progress = reader.int32();
                        break;
                    }
                case 3: {
                        if (message.onceAward === $util.emptyObject)
                            message.onceAward = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int32();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.onceAward[key] = value;
                        break;
                    }
                case 4: {
                        if (message.upgradeAward === $util.emptyObject)
                            message.upgradeAward = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int32();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.upgradeAward[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AdventureAreaInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.AdventureAreaInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.AdventureAreaInfo} AdventureAreaInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AdventureAreaInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AdventureAreaInfo message.
         * @function verify
         * @memberof common.AdventureAreaInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AdventureAreaInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.progress != null && message.hasOwnProperty("progress"))
                if (!$util.isInteger(message.progress))
                    return "progress: integer expected";
            if (message.onceAward != null && message.hasOwnProperty("onceAward")) {
                if (!$util.isObject(message.onceAward))
                    return "onceAward: object expected";
                var key = Object.keys(message.onceAward);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "onceAward: integer key{k:int32} expected";
                    if (!$util.isInteger(message.onceAward[key[i]]))
                        return "onceAward: integer{k:int32} expected";
                }
            }
            if (message.upgradeAward != null && message.hasOwnProperty("upgradeAward")) {
                if (!$util.isObject(message.upgradeAward))
                    return "upgradeAward: object expected";
                var key = Object.keys(message.upgradeAward);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "upgradeAward: integer key{k:int32} expected";
                    if (!$util.isInteger(message.upgradeAward[key[i]]))
                        return "upgradeAward: integer{k:int32} expected";
                }
            }
            return null;
        };

        /**
         * Creates an AdventureAreaInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.AdventureAreaInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.AdventureAreaInfo} AdventureAreaInfo
         */
        AdventureAreaInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.AdventureAreaInfo)
                return object;
            var message = new $root.common.AdventureAreaInfo();
            if (object.level != null)
                message.level = object.level | 0;
            if (object.progress != null)
                message.progress = object.progress | 0;
            if (object.onceAward) {
                if (typeof object.onceAward !== "object")
                    throw TypeError(".common.AdventureAreaInfo.onceAward: object expected");
                message.onceAward = {};
                for (var keys = Object.keys(object.onceAward), i = 0; i < keys.length; ++i)
                    message.onceAward[keys[i]] = object.onceAward[keys[i]] | 0;
            }
            if (object.upgradeAward) {
                if (typeof object.upgradeAward !== "object")
                    throw TypeError(".common.AdventureAreaInfo.upgradeAward: object expected");
                message.upgradeAward = {};
                for (var keys = Object.keys(object.upgradeAward), i = 0; i < keys.length; ++i)
                    message.upgradeAward[keys[i]] = object.upgradeAward[keys[i]] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an AdventureAreaInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.AdventureAreaInfo
         * @static
         * @param {common.AdventureAreaInfo} message AdventureAreaInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AdventureAreaInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.objects || options.defaults) {
                object.onceAward = {};
                object.upgradeAward = {};
            }
            if (options.defaults) {
                object.level = 0;
                object.progress = 0;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.progress != null && message.hasOwnProperty("progress"))
                object.progress = message.progress;
            var keys2;
            if (message.onceAward && (keys2 = Object.keys(message.onceAward)).length) {
                object.onceAward = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.onceAward[keys2[j]] = message.onceAward[keys2[j]];
            }
            if (message.upgradeAward && (keys2 = Object.keys(message.upgradeAward)).length) {
                object.upgradeAward = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.upgradeAward[keys2[j]] = message.upgradeAward[keys2[j]];
            }
            return object;
        };

        /**
         * Converts this AdventureAreaInfo to JSON.
         * @function toJSON
         * @memberof common.AdventureAreaInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AdventureAreaInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AdventureAreaInfo
         * @function getTypeUrl
         * @memberof common.AdventureAreaInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AdventureAreaInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.AdventureAreaInfo";
        };

        return AdventureAreaInfo;
    })();

    common.EventModInfo = (function() {

        /**
         * Properties of an EventModInfo.
         * @memberof common
         * @interface IEventModInfo
         * @property {Array.<number>|null} [seq] EventModInfo seq
         */

        /**
         * Constructs a new EventModInfo.
         * @memberof common
         * @classdesc Represents an EventModInfo.
         * @implements IEventModInfo
         * @constructor
         * @param {common.IEventModInfo=} [properties] Properties to set
         */
        function EventModInfo(properties) {
            this.seq = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EventModInfo seq.
         * @member {Array.<number>} seq
         * @memberof common.EventModInfo
         * @instance
         */
        EventModInfo.prototype.seq = $util.emptyArray;

        /**
         * Creates a new EventModInfo instance using the specified properties.
         * @function create
         * @memberof common.EventModInfo
         * @static
         * @param {common.IEventModInfo=} [properties] Properties to set
         * @returns {common.EventModInfo} EventModInfo instance
         */
        EventModInfo.create = function create(properties) {
            return new EventModInfo(properties);
        };

        /**
         * Encodes the specified EventModInfo message. Does not implicitly {@link common.EventModInfo.verify|verify} messages.
         * @function encode
         * @memberof common.EventModInfo
         * @static
         * @param {common.IEventModInfo} message EventModInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EventModInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seq != null && message.seq.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (var i = 0; i < message.seq.length; ++i)
                    writer.int32(message.seq[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified EventModInfo message, length delimited. Does not implicitly {@link common.EventModInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.EventModInfo
         * @static
         * @param {common.IEventModInfo} message EventModInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EventModInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an EventModInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.EventModInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.EventModInfo} EventModInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EventModInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.EventModInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.seq && message.seq.length))
                            message.seq = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.seq.push(reader.int32());
                        } else
                            message.seq.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an EventModInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.EventModInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.EventModInfo} EventModInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EventModInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EventModInfo message.
         * @function verify
         * @memberof common.EventModInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EventModInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.seq != null && message.hasOwnProperty("seq")) {
                if (!Array.isArray(message.seq))
                    return "seq: array expected";
                for (var i = 0; i < message.seq.length; ++i)
                    if (!$util.isInteger(message.seq[i]))
                        return "seq: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an EventModInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.EventModInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.EventModInfo} EventModInfo
         */
        EventModInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.EventModInfo)
                return object;
            var message = new $root.common.EventModInfo();
            if (object.seq) {
                if (!Array.isArray(object.seq))
                    throw TypeError(".common.EventModInfo.seq: array expected");
                message.seq = [];
                for (var i = 0; i < object.seq.length; ++i)
                    message.seq[i] = object.seq[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an EventModInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.EventModInfo
         * @static
         * @param {common.EventModInfo} message EventModInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EventModInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.seq = [];
            if (message.seq && message.seq.length) {
                object.seq = [];
                for (var j = 0; j < message.seq.length; ++j)
                    object.seq[j] = message.seq[j];
            }
            return object;
        };

        /**
         * Converts this EventModInfo to JSON.
         * @function toJSON
         * @memberof common.EventModInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EventModInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EventModInfo
         * @function getTypeUrl
         * @memberof common.EventModInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EventModInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.EventModInfo";
        };

        return EventModInfo;
    })();

    common.RandomEventInfo = (function() {

        /**
         * Properties of a RandomEventInfo.
         * @memberof common
         * @interface IRandomEventInfo
         * @property {string|null} [version] RandomEventInfo version
         * @property {common.IEventModInfo|null} [mod] RandomEventInfo mod
         */

        /**
         * Constructs a new RandomEventInfo.
         * @memberof common
         * @classdesc Represents a RandomEventInfo.
         * @implements IRandomEventInfo
         * @constructor
         * @param {common.IRandomEventInfo=} [properties] Properties to set
         */
        function RandomEventInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RandomEventInfo version.
         * @member {string} version
         * @memberof common.RandomEventInfo
         * @instance
         */
        RandomEventInfo.prototype.version = "";

        /**
         * RandomEventInfo mod.
         * @member {common.IEventModInfo|null|undefined} mod
         * @memberof common.RandomEventInfo
         * @instance
         */
        RandomEventInfo.prototype.mod = null;

        /**
         * Creates a new RandomEventInfo instance using the specified properties.
         * @function create
         * @memberof common.RandomEventInfo
         * @static
         * @param {common.IRandomEventInfo=} [properties] Properties to set
         * @returns {common.RandomEventInfo} RandomEventInfo instance
         */
        RandomEventInfo.create = function create(properties) {
            return new RandomEventInfo(properties);
        };

        /**
         * Encodes the specified RandomEventInfo message. Does not implicitly {@link common.RandomEventInfo.verify|verify} messages.
         * @function encode
         * @memberof common.RandomEventInfo
         * @static
         * @param {common.IRandomEventInfo} message RandomEventInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RandomEventInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.version);
            if (message.mod != null && Object.hasOwnProperty.call(message, "mod"))
                $root.common.EventModInfo.encode(message.mod, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RandomEventInfo message, length delimited. Does not implicitly {@link common.RandomEventInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.RandomEventInfo
         * @static
         * @param {common.IRandomEventInfo} message RandomEventInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RandomEventInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RandomEventInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.RandomEventInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.RandomEventInfo} RandomEventInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RandomEventInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.RandomEventInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.version = reader.string();
                        break;
                    }
                case 2: {
                        message.mod = $root.common.EventModInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RandomEventInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.RandomEventInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.RandomEventInfo} RandomEventInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RandomEventInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RandomEventInfo message.
         * @function verify
         * @memberof common.RandomEventInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RandomEventInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isString(message.version))
                    return "version: string expected";
            if (message.mod != null && message.hasOwnProperty("mod")) {
                var error = $root.common.EventModInfo.verify(message.mod);
                if (error)
                    return "mod." + error;
            }
            return null;
        };

        /**
         * Creates a RandomEventInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.RandomEventInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.RandomEventInfo} RandomEventInfo
         */
        RandomEventInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.RandomEventInfo)
                return object;
            var message = new $root.common.RandomEventInfo();
            if (object.version != null)
                message.version = String(object.version);
            if (object.mod != null) {
                if (typeof object.mod !== "object")
                    throw TypeError(".common.RandomEventInfo.mod: object expected");
                message.mod = $root.common.EventModInfo.fromObject(object.mod);
            }
            return message;
        };

        /**
         * Creates a plain object from a RandomEventInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.RandomEventInfo
         * @static
         * @param {common.RandomEventInfo} message RandomEventInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RandomEventInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.version = "";
                object.mod = null;
            }
            if (message.version != null && message.hasOwnProperty("version"))
                object.version = message.version;
            if (message.mod != null && message.hasOwnProperty("mod"))
                object.mod = $root.common.EventModInfo.toObject(message.mod, options);
            return object;
        };

        /**
         * Converts this RandomEventInfo to JSON.
         * @function toJSON
         * @memberof common.RandomEventInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RandomEventInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RandomEventInfo
         * @function getTypeUrl
         * @memberof common.RandomEventInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RandomEventInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.RandomEventInfo";
        };

        return RandomEventInfo;
    })();

    common.AdventureInfo = (function() {

        /**
         * Properties of an AdventureInfo.
         * @memberof common
         * @interface IAdventureInfo
         * @property {common.IAdventureTeamInfo|null} [teams] AdventureInfo teams
         * @property {number|null} [curSelectTeam] AdventureInfo curSelectTeam
         * @property {Object.<string,common.IAdventureAreaInfo>|null} [areas] AdventureInfo areas
         * @property {common.IRandomEventInfo|null} [randomEvent] AdventureInfo randomEvent
         */

        /**
         * Constructs a new AdventureInfo.
         * @memberof common
         * @classdesc Represents an AdventureInfo.
         * @implements IAdventureInfo
         * @constructor
         * @param {common.IAdventureInfo=} [properties] Properties to set
         */
        function AdventureInfo(properties) {
            this.areas = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AdventureInfo teams.
         * @member {common.IAdventureTeamInfo|null|undefined} teams
         * @memberof common.AdventureInfo
         * @instance
         */
        AdventureInfo.prototype.teams = null;

        /**
         * AdventureInfo curSelectTeam.
         * @member {number} curSelectTeam
         * @memberof common.AdventureInfo
         * @instance
         */
        AdventureInfo.prototype.curSelectTeam = 0;

        /**
         * AdventureInfo areas.
         * @member {Object.<string,common.IAdventureAreaInfo>} areas
         * @memberof common.AdventureInfo
         * @instance
         */
        AdventureInfo.prototype.areas = $util.emptyObject;

        /**
         * AdventureInfo randomEvent.
         * @member {common.IRandomEventInfo|null|undefined} randomEvent
         * @memberof common.AdventureInfo
         * @instance
         */
        AdventureInfo.prototype.randomEvent = null;

        /**
         * Creates a new AdventureInfo instance using the specified properties.
         * @function create
         * @memberof common.AdventureInfo
         * @static
         * @param {common.IAdventureInfo=} [properties] Properties to set
         * @returns {common.AdventureInfo} AdventureInfo instance
         */
        AdventureInfo.create = function create(properties) {
            return new AdventureInfo(properties);
        };

        /**
         * Encodes the specified AdventureInfo message. Does not implicitly {@link common.AdventureInfo.verify|verify} messages.
         * @function encode
         * @memberof common.AdventureInfo
         * @static
         * @param {common.IAdventureInfo} message AdventureInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AdventureInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.teams != null && Object.hasOwnProperty.call(message, "teams"))
                $root.common.AdventureTeamInfo.encode(message.teams, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.curSelectTeam != null && Object.hasOwnProperty.call(message, "curSelectTeam"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.curSelectTeam);
            if (message.areas != null && Object.hasOwnProperty.call(message, "areas"))
                for (var keys = Object.keys(message.areas), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.AdventureAreaInfo.encode(message.areas[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            if (message.randomEvent != null && Object.hasOwnProperty.call(message, "randomEvent"))
                $root.common.RandomEventInfo.encode(message.randomEvent, writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AdventureInfo message, length delimited. Does not implicitly {@link common.AdventureInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.AdventureInfo
         * @static
         * @param {common.IAdventureInfo} message AdventureInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AdventureInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AdventureInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.AdventureInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.AdventureInfo} AdventureInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AdventureInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.AdventureInfo(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.teams = $root.common.AdventureTeamInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.curSelectTeam = reader.int32();
                        break;
                    }
                case 3: {
                        if (message.areas === $util.emptyObject)
                            message.areas = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.AdventureAreaInfo.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.areas[key] = value;
                        break;
                    }
                case 4: {
                        message.randomEvent = $root.common.RandomEventInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AdventureInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.AdventureInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.AdventureInfo} AdventureInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AdventureInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AdventureInfo message.
         * @function verify
         * @memberof common.AdventureInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AdventureInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.teams != null && message.hasOwnProperty("teams")) {
                var error = $root.common.AdventureTeamInfo.verify(message.teams);
                if (error)
                    return "teams." + error;
            }
            if (message.curSelectTeam != null && message.hasOwnProperty("curSelectTeam"))
                if (!$util.isInteger(message.curSelectTeam))
                    return "curSelectTeam: integer expected";
            if (message.areas != null && message.hasOwnProperty("areas")) {
                if (!$util.isObject(message.areas))
                    return "areas: object expected";
                var key = Object.keys(message.areas);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "areas: integer key{k:int32} expected";
                    {
                        var error = $root.common.AdventureAreaInfo.verify(message.areas[key[i]]);
                        if (error)
                            return "areas." + error;
                    }
                }
            }
            if (message.randomEvent != null && message.hasOwnProperty("randomEvent")) {
                var error = $root.common.RandomEventInfo.verify(message.randomEvent);
                if (error)
                    return "randomEvent." + error;
            }
            return null;
        };

        /**
         * Creates an AdventureInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.AdventureInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.AdventureInfo} AdventureInfo
         */
        AdventureInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.AdventureInfo)
                return object;
            var message = new $root.common.AdventureInfo();
            if (object.teams != null) {
                if (typeof object.teams !== "object")
                    throw TypeError(".common.AdventureInfo.teams: object expected");
                message.teams = $root.common.AdventureTeamInfo.fromObject(object.teams);
            }
            if (object.curSelectTeam != null)
                message.curSelectTeam = object.curSelectTeam | 0;
            if (object.areas) {
                if (typeof object.areas !== "object")
                    throw TypeError(".common.AdventureInfo.areas: object expected");
                message.areas = {};
                for (var keys = Object.keys(object.areas), i = 0; i < keys.length; ++i) {
                    if (typeof object.areas[keys[i]] !== "object")
                        throw TypeError(".common.AdventureInfo.areas: object expected");
                    message.areas[keys[i]] = $root.common.AdventureAreaInfo.fromObject(object.areas[keys[i]]);
                }
            }
            if (object.randomEvent != null) {
                if (typeof object.randomEvent !== "object")
                    throw TypeError(".common.AdventureInfo.randomEvent: object expected");
                message.randomEvent = $root.common.RandomEventInfo.fromObject(object.randomEvent);
            }
            return message;
        };

        /**
         * Creates a plain object from an AdventureInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.AdventureInfo
         * @static
         * @param {common.AdventureInfo} message AdventureInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AdventureInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.objects || options.defaults)
                object.areas = {};
            if (options.defaults) {
                object.teams = null;
                object.curSelectTeam = 0;
                object.randomEvent = null;
            }
            if (message.teams != null && message.hasOwnProperty("teams"))
                object.teams = $root.common.AdventureTeamInfo.toObject(message.teams, options);
            if (message.curSelectTeam != null && message.hasOwnProperty("curSelectTeam"))
                object.curSelectTeam = message.curSelectTeam;
            var keys2;
            if (message.areas && (keys2 = Object.keys(message.areas)).length) {
                object.areas = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.areas[keys2[j]] = $root.common.AdventureAreaInfo.toObject(message.areas[keys2[j]], options);
            }
            if (message.randomEvent != null && message.hasOwnProperty("randomEvent"))
                object.randomEvent = $root.common.RandomEventInfo.toObject(message.randomEvent, options);
            return object;
        };

        /**
         * Converts this AdventureInfo to JSON.
         * @function toJSON
         * @memberof common.AdventureInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AdventureInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AdventureInfo
         * @function getTypeUrl
         * @memberof common.AdventureInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AdventureInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.AdventureInfo";
        };

        return AdventureInfo;
    })();

    common.EventInfo = (function() {

        /**
         * Properties of an EventInfo.
         * @memberof common
         * @interface IEventInfo
         * @property {Array.<common.IWaitEventData>|null} [waitQueue] EventInfo waitQueue
         * @property {Object.<string,number>|null} [happend] EventInfo happend
         */

        /**
         * Constructs a new EventInfo.
         * @memberof common
         * @classdesc Represents an EventInfo.
         * @implements IEventInfo
         * @constructor
         * @param {common.IEventInfo=} [properties] Properties to set
         */
        function EventInfo(properties) {
            this.waitQueue = [];
            this.happend = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EventInfo waitQueue.
         * @member {Array.<common.IWaitEventData>} waitQueue
         * @memberof common.EventInfo
         * @instance
         */
        EventInfo.prototype.waitQueue = $util.emptyArray;

        /**
         * EventInfo happend.
         * @member {Object.<string,number>} happend
         * @memberof common.EventInfo
         * @instance
         */
        EventInfo.prototype.happend = $util.emptyObject;

        /**
         * Creates a new EventInfo instance using the specified properties.
         * @function create
         * @memberof common.EventInfo
         * @static
         * @param {common.IEventInfo=} [properties] Properties to set
         * @returns {common.EventInfo} EventInfo instance
         */
        EventInfo.create = function create(properties) {
            return new EventInfo(properties);
        };

        /**
         * Encodes the specified EventInfo message. Does not implicitly {@link common.EventInfo.verify|verify} messages.
         * @function encode
         * @memberof common.EventInfo
         * @static
         * @param {common.IEventInfo} message EventInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EventInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.waitQueue != null && message.waitQueue.length)
                for (var i = 0; i < message.waitQueue.length; ++i)
                    $root.common.WaitEventData.encode(message.waitQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.happend != null && Object.hasOwnProperty.call(message, "happend"))
                for (var keys = Object.keys(message.happend), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.happend[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified EventInfo message, length delimited. Does not implicitly {@link common.EventInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.EventInfo
         * @static
         * @param {common.IEventInfo} message EventInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EventInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an EventInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.EventInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.EventInfo} EventInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EventInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.EventInfo(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.waitQueue && message.waitQueue.length))
                            message.waitQueue = [];
                        message.waitQueue.push($root.common.WaitEventData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (message.happend === $util.emptyObject)
                            message.happend = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int32();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.happend[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an EventInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.EventInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.EventInfo} EventInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EventInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EventInfo message.
         * @function verify
         * @memberof common.EventInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EventInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.waitQueue != null && message.hasOwnProperty("waitQueue")) {
                if (!Array.isArray(message.waitQueue))
                    return "waitQueue: array expected";
                for (var i = 0; i < message.waitQueue.length; ++i) {
                    var error = $root.common.WaitEventData.verify(message.waitQueue[i]);
                    if (error)
                        return "waitQueue." + error;
                }
            }
            if (message.happend != null && message.hasOwnProperty("happend")) {
                if (!$util.isObject(message.happend))
                    return "happend: object expected";
                var key = Object.keys(message.happend);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "happend: integer key{k:int32} expected";
                    if (!$util.isInteger(message.happend[key[i]]))
                        return "happend: integer{k:int32} expected";
                }
            }
            return null;
        };

        /**
         * Creates an EventInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.EventInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.EventInfo} EventInfo
         */
        EventInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.EventInfo)
                return object;
            var message = new $root.common.EventInfo();
            if (object.waitQueue) {
                if (!Array.isArray(object.waitQueue))
                    throw TypeError(".common.EventInfo.waitQueue: array expected");
                message.waitQueue = [];
                for (var i = 0; i < object.waitQueue.length; ++i) {
                    if (typeof object.waitQueue[i] !== "object")
                        throw TypeError(".common.EventInfo.waitQueue: object expected");
                    message.waitQueue[i] = $root.common.WaitEventData.fromObject(object.waitQueue[i]);
                }
            }
            if (object.happend) {
                if (typeof object.happend !== "object")
                    throw TypeError(".common.EventInfo.happend: object expected");
                message.happend = {};
                for (var keys = Object.keys(object.happend), i = 0; i < keys.length; ++i)
                    message.happend[keys[i]] = object.happend[keys[i]] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an EventInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.EventInfo
         * @static
         * @param {common.EventInfo} message EventInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EventInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.waitQueue = [];
            if (options.objects || options.defaults)
                object.happend = {};
            if (message.waitQueue && message.waitQueue.length) {
                object.waitQueue = [];
                for (var j = 0; j < message.waitQueue.length; ++j)
                    object.waitQueue[j] = $root.common.WaitEventData.toObject(message.waitQueue[j], options);
            }
            var keys2;
            if (message.happend && (keys2 = Object.keys(message.happend)).length) {
                object.happend = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.happend[keys2[j]] = message.happend[keys2[j]];
            }
            return object;
        };

        /**
         * Converts this EventInfo to JSON.
         * @function toJSON
         * @memberof common.EventInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EventInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EventInfo
         * @function getTypeUrl
         * @memberof common.EventInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EventInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.EventInfo";
        };

        return EventInfo;
    })();

    common.ToolInfo = (function() {

        /**
         * Properties of a ToolInfo.
         * @memberof common
         * @interface IToolInfo
         * @property {number|null} [id] ToolInfo id
         * @property {number|Long|null} [useTime] ToolInfo useTime
         * @property {number|null} [useNum] ToolInfo useNum
         * @property {number|null} [adNum] ToolInfo adNum
         * @property {number|null} [hasNum] ToolInfo hasNum
         */

        /**
         * Constructs a new ToolInfo.
         * @memberof common
         * @classdesc Represents a ToolInfo.
         * @implements IToolInfo
         * @constructor
         * @param {common.IToolInfo=} [properties] Properties to set
         */
        function ToolInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ToolInfo id.
         * @member {number} id
         * @memberof common.ToolInfo
         * @instance
         */
        ToolInfo.prototype.id = 0;

        /**
         * ToolInfo useTime.
         * @member {number|Long} useTime
         * @memberof common.ToolInfo
         * @instance
         */
        ToolInfo.prototype.useTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ToolInfo useNum.
         * @member {number} useNum
         * @memberof common.ToolInfo
         * @instance
         */
        ToolInfo.prototype.useNum = 0;

        /**
         * ToolInfo adNum.
         * @member {number} adNum
         * @memberof common.ToolInfo
         * @instance
         */
        ToolInfo.prototype.adNum = 0;

        /**
         * ToolInfo hasNum.
         * @member {number} hasNum
         * @memberof common.ToolInfo
         * @instance
         */
        ToolInfo.prototype.hasNum = 0;

        /**
         * Creates a new ToolInfo instance using the specified properties.
         * @function create
         * @memberof common.ToolInfo
         * @static
         * @param {common.IToolInfo=} [properties] Properties to set
         * @returns {common.ToolInfo} ToolInfo instance
         */
        ToolInfo.create = function create(properties) {
            return new ToolInfo(properties);
        };

        /**
         * Encodes the specified ToolInfo message. Does not implicitly {@link common.ToolInfo.verify|verify} messages.
         * @function encode
         * @memberof common.ToolInfo
         * @static
         * @param {common.IToolInfo} message ToolInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ToolInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.useTime != null && Object.hasOwnProperty.call(message, "useTime"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.useTime);
            if (message.useNum != null && Object.hasOwnProperty.call(message, "useNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.useNum);
            if (message.adNum != null && Object.hasOwnProperty.call(message, "adNum"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.adNum);
            if (message.hasNum != null && Object.hasOwnProperty.call(message, "hasNum"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.hasNum);
            return writer;
        };

        /**
         * Encodes the specified ToolInfo message, length delimited. Does not implicitly {@link common.ToolInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.ToolInfo
         * @static
         * @param {common.IToolInfo} message ToolInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ToolInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ToolInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.ToolInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.ToolInfo} ToolInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ToolInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.ToolInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.useTime = reader.int64();
                        break;
                    }
                case 3: {
                        message.useNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.adNum = reader.int32();
                        break;
                    }
                case 5: {
                        message.hasNum = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ToolInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.ToolInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.ToolInfo} ToolInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ToolInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ToolInfo message.
         * @function verify
         * @memberof common.ToolInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ToolInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.useTime != null && message.hasOwnProperty("useTime"))
                if (!$util.isInteger(message.useTime) && !(message.useTime && $util.isInteger(message.useTime.low) && $util.isInteger(message.useTime.high)))
                    return "useTime: integer|Long expected";
            if (message.useNum != null && message.hasOwnProperty("useNum"))
                if (!$util.isInteger(message.useNum))
                    return "useNum: integer expected";
            if (message.adNum != null && message.hasOwnProperty("adNum"))
                if (!$util.isInteger(message.adNum))
                    return "adNum: integer expected";
            if (message.hasNum != null && message.hasOwnProperty("hasNum"))
                if (!$util.isInteger(message.hasNum))
                    return "hasNum: integer expected";
            return null;
        };

        /**
         * Creates a ToolInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.ToolInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.ToolInfo} ToolInfo
         */
        ToolInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.ToolInfo)
                return object;
            var message = new $root.common.ToolInfo();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.useTime != null)
                if ($util.Long)
                    (message.useTime = $util.Long.fromValue(object.useTime)).unsigned = false;
                else if (typeof object.useTime === "string")
                    message.useTime = parseInt(object.useTime, 10);
                else if (typeof object.useTime === "number")
                    message.useTime = object.useTime;
                else if (typeof object.useTime === "object")
                    message.useTime = new $util.LongBits(object.useTime.low >>> 0, object.useTime.high >>> 0).toNumber();
            if (object.useNum != null)
                message.useNum = object.useNum | 0;
            if (object.adNum != null)
                message.adNum = object.adNum | 0;
            if (object.hasNum != null)
                message.hasNum = object.hasNum | 0;
            return message;
        };

        /**
         * Creates a plain object from a ToolInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.ToolInfo
         * @static
         * @param {common.ToolInfo} message ToolInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ToolInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.useTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.useTime = options.longs === String ? "0" : 0;
                object.useNum = 0;
                object.adNum = 0;
                object.hasNum = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.useTime != null && message.hasOwnProperty("useTime"))
                if (typeof message.useTime === "number")
                    object.useTime = options.longs === String ? String(message.useTime) : message.useTime;
                else
                    object.useTime = options.longs === String ? $util.Long.prototype.toString.call(message.useTime) : options.longs === Number ? new $util.LongBits(message.useTime.low >>> 0, message.useTime.high >>> 0).toNumber() : message.useTime;
            if (message.useNum != null && message.hasOwnProperty("useNum"))
                object.useNum = message.useNum;
            if (message.adNum != null && message.hasOwnProperty("adNum"))
                object.adNum = message.adNum;
            if (message.hasNum != null && message.hasOwnProperty("hasNum"))
                object.hasNum = message.hasNum;
            return object;
        };

        /**
         * Converts this ToolInfo to JSON.
         * @function toJSON
         * @memberof common.ToolInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ToolInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ToolInfo
         * @function getTypeUrl
         * @memberof common.ToolInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ToolInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.ToolInfo";
        };

        return ToolInfo;
    })();

    common.CollectionInfo = (function() {

        /**
         * Properties of a CollectionInfo.
         * @memberof common
         * @interface ICollectionInfo
         * @property {number|null} [id] CollectionInfo id
         * @property {number|null} [level] CollectionInfo level
         */

        /**
         * Constructs a new CollectionInfo.
         * @memberof common
         * @classdesc Represents a CollectionInfo.
         * @implements ICollectionInfo
         * @constructor
         * @param {common.ICollectionInfo=} [properties] Properties to set
         */
        function CollectionInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CollectionInfo id.
         * @member {number} id
         * @memberof common.CollectionInfo
         * @instance
         */
        CollectionInfo.prototype.id = 0;

        /**
         * CollectionInfo level.
         * @member {number} level
         * @memberof common.CollectionInfo
         * @instance
         */
        CollectionInfo.prototype.level = 0;

        /**
         * Creates a new CollectionInfo instance using the specified properties.
         * @function create
         * @memberof common.CollectionInfo
         * @static
         * @param {common.ICollectionInfo=} [properties] Properties to set
         * @returns {common.CollectionInfo} CollectionInfo instance
         */
        CollectionInfo.create = function create(properties) {
            return new CollectionInfo(properties);
        };

        /**
         * Encodes the specified CollectionInfo message. Does not implicitly {@link common.CollectionInfo.verify|verify} messages.
         * @function encode
         * @memberof common.CollectionInfo
         * @static
         * @param {common.ICollectionInfo} message CollectionInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CollectionInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.level);
            return writer;
        };

        /**
         * Encodes the specified CollectionInfo message, length delimited. Does not implicitly {@link common.CollectionInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.CollectionInfo
         * @static
         * @param {common.ICollectionInfo} message CollectionInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CollectionInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CollectionInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.CollectionInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.CollectionInfo} CollectionInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CollectionInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.CollectionInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.level = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CollectionInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.CollectionInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.CollectionInfo} CollectionInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CollectionInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CollectionInfo message.
         * @function verify
         * @memberof common.CollectionInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CollectionInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            return null;
        };

        /**
         * Creates a CollectionInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.CollectionInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.CollectionInfo} CollectionInfo
         */
        CollectionInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.CollectionInfo)
                return object;
            var message = new $root.common.CollectionInfo();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.level != null)
                message.level = object.level | 0;
            return message;
        };

        /**
         * Creates a plain object from a CollectionInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.CollectionInfo
         * @static
         * @param {common.CollectionInfo} message CollectionInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CollectionInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.level = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            return object;
        };

        /**
         * Converts this CollectionInfo to JSON.
         * @function toJSON
         * @memberof common.CollectionInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CollectionInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CollectionInfo
         * @function getTypeUrl
         * @memberof common.CollectionInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CollectionInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.CollectionInfo";
        };

        return CollectionInfo;
    })();

    common.WorkTableInfo = (function() {

        /**
         * Properties of a WorkTableInfo.
         * @memberof common
         * @interface IWorkTableInfo
         * @property {Object.<string,common.IToolInfo>|null} [tools] WorkTableInfo tools
         * @property {Object.<string,common.ICollectionInfo>|null} [collections] WorkTableInfo collections
         */

        /**
         * Constructs a new WorkTableInfo.
         * @memberof common
         * @classdesc Represents a WorkTableInfo.
         * @implements IWorkTableInfo
         * @constructor
         * @param {common.IWorkTableInfo=} [properties] Properties to set
         */
        function WorkTableInfo(properties) {
            this.tools = {};
            this.collections = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WorkTableInfo tools.
         * @member {Object.<string,common.IToolInfo>} tools
         * @memberof common.WorkTableInfo
         * @instance
         */
        WorkTableInfo.prototype.tools = $util.emptyObject;

        /**
         * WorkTableInfo collections.
         * @member {Object.<string,common.ICollectionInfo>} collections
         * @memberof common.WorkTableInfo
         * @instance
         */
        WorkTableInfo.prototype.collections = $util.emptyObject;

        /**
         * Creates a new WorkTableInfo instance using the specified properties.
         * @function create
         * @memberof common.WorkTableInfo
         * @static
         * @param {common.IWorkTableInfo=} [properties] Properties to set
         * @returns {common.WorkTableInfo} WorkTableInfo instance
         */
        WorkTableInfo.create = function create(properties) {
            return new WorkTableInfo(properties);
        };

        /**
         * Encodes the specified WorkTableInfo message. Does not implicitly {@link common.WorkTableInfo.verify|verify} messages.
         * @function encode
         * @memberof common.WorkTableInfo
         * @static
         * @param {common.IWorkTableInfo} message WorkTableInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WorkTableInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tools != null && Object.hasOwnProperty.call(message, "tools"))
                for (var keys = Object.keys(message.tools), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.ToolInfo.encode(message.tools[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            if (message.collections != null && Object.hasOwnProperty.call(message, "collections"))
                for (var keys = Object.keys(message.collections), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.CollectionInfo.encode(message.collections[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            return writer;
        };

        /**
         * Encodes the specified WorkTableInfo message, length delimited. Does not implicitly {@link common.WorkTableInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.WorkTableInfo
         * @static
         * @param {common.IWorkTableInfo} message WorkTableInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WorkTableInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WorkTableInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.WorkTableInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.WorkTableInfo} WorkTableInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WorkTableInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.WorkTableInfo(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.tools === $util.emptyObject)
                            message.tools = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.ToolInfo.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.tools[key] = value;
                        break;
                    }
                case 2: {
                        if (message.collections === $util.emptyObject)
                            message.collections = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.CollectionInfo.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.collections[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WorkTableInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.WorkTableInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.WorkTableInfo} WorkTableInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WorkTableInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WorkTableInfo message.
         * @function verify
         * @memberof common.WorkTableInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WorkTableInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.tools != null && message.hasOwnProperty("tools")) {
                if (!$util.isObject(message.tools))
                    return "tools: object expected";
                var key = Object.keys(message.tools);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "tools: integer key{k:int32} expected";
                    {
                        var error = $root.common.ToolInfo.verify(message.tools[key[i]]);
                        if (error)
                            return "tools." + error;
                    }
                }
            }
            if (message.collections != null && message.hasOwnProperty("collections")) {
                if (!$util.isObject(message.collections))
                    return "collections: object expected";
                var key = Object.keys(message.collections);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "collections: integer key{k:int32} expected";
                    {
                        var error = $root.common.CollectionInfo.verify(message.collections[key[i]]);
                        if (error)
                            return "collections." + error;
                    }
                }
            }
            return null;
        };

        /**
         * Creates a WorkTableInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.WorkTableInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.WorkTableInfo} WorkTableInfo
         */
        WorkTableInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.WorkTableInfo)
                return object;
            var message = new $root.common.WorkTableInfo();
            if (object.tools) {
                if (typeof object.tools !== "object")
                    throw TypeError(".common.WorkTableInfo.tools: object expected");
                message.tools = {};
                for (var keys = Object.keys(object.tools), i = 0; i < keys.length; ++i) {
                    if (typeof object.tools[keys[i]] !== "object")
                        throw TypeError(".common.WorkTableInfo.tools: object expected");
                    message.tools[keys[i]] = $root.common.ToolInfo.fromObject(object.tools[keys[i]]);
                }
            }
            if (object.collections) {
                if (typeof object.collections !== "object")
                    throw TypeError(".common.WorkTableInfo.collections: object expected");
                message.collections = {};
                for (var keys = Object.keys(object.collections), i = 0; i < keys.length; ++i) {
                    if (typeof object.collections[keys[i]] !== "object")
                        throw TypeError(".common.WorkTableInfo.collections: object expected");
                    message.collections[keys[i]] = $root.common.CollectionInfo.fromObject(object.collections[keys[i]]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a WorkTableInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.WorkTableInfo
         * @static
         * @param {common.WorkTableInfo} message WorkTableInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WorkTableInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.objects || options.defaults) {
                object.tools = {};
                object.collections = {};
            }
            var keys2;
            if (message.tools && (keys2 = Object.keys(message.tools)).length) {
                object.tools = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.tools[keys2[j]] = $root.common.ToolInfo.toObject(message.tools[keys2[j]], options);
            }
            if (message.collections && (keys2 = Object.keys(message.collections)).length) {
                object.collections = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.collections[keys2[j]] = $root.common.CollectionInfo.toObject(message.collections[keys2[j]], options);
            }
            return object;
        };

        /**
         * Converts this WorkTableInfo to JSON.
         * @function toJSON
         * @memberof common.WorkTableInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WorkTableInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WorkTableInfo
         * @function getTypeUrl
         * @memberof common.WorkTableInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WorkTableInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.WorkTableInfo";
        };

        return WorkTableInfo;
    })();

    common.FairyThemeInfo = (function() {

        /**
         * Properties of a FairyThemeInfo.
         * @memberof common
         * @interface IFairyThemeInfo
         * @property {number|null} [id] FairyThemeInfo id
         * @property {number|null} [level] FairyThemeInfo level
         * @property {boolean|null} [specialOpened] FairyThemeInfo specialOpened
         * @property {number|Long|null} [recordNormal] FairyThemeInfo recordNormal
         * @property {number|Long|null} [recordSpecial] FairyThemeInfo recordSpecial
         */

        /**
         * Constructs a new FairyThemeInfo.
         * @memberof common
         * @classdesc Represents a FairyThemeInfo.
         * @implements IFairyThemeInfo
         * @constructor
         * @param {common.IFairyThemeInfo=} [properties] Properties to set
         */
        function FairyThemeInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FairyThemeInfo id.
         * @member {number} id
         * @memberof common.FairyThemeInfo
         * @instance
         */
        FairyThemeInfo.prototype.id = 0;

        /**
         * FairyThemeInfo level.
         * @member {number} level
         * @memberof common.FairyThemeInfo
         * @instance
         */
        FairyThemeInfo.prototype.level = 0;

        /**
         * FairyThemeInfo specialOpened.
         * @member {boolean} specialOpened
         * @memberof common.FairyThemeInfo
         * @instance
         */
        FairyThemeInfo.prototype.specialOpened = false;

        /**
         * FairyThemeInfo recordNormal.
         * @member {number|Long} recordNormal
         * @memberof common.FairyThemeInfo
         * @instance
         */
        FairyThemeInfo.prototype.recordNormal = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * FairyThemeInfo recordSpecial.
         * @member {number|Long} recordSpecial
         * @memberof common.FairyThemeInfo
         * @instance
         */
        FairyThemeInfo.prototype.recordSpecial = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new FairyThemeInfo instance using the specified properties.
         * @function create
         * @memberof common.FairyThemeInfo
         * @static
         * @param {common.IFairyThemeInfo=} [properties] Properties to set
         * @returns {common.FairyThemeInfo} FairyThemeInfo instance
         */
        FairyThemeInfo.create = function create(properties) {
            return new FairyThemeInfo(properties);
        };

        /**
         * Encodes the specified FairyThemeInfo message. Does not implicitly {@link common.FairyThemeInfo.verify|verify} messages.
         * @function encode
         * @memberof common.FairyThemeInfo
         * @static
         * @param {common.IFairyThemeInfo} message FairyThemeInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FairyThemeInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.level);
            if (message.specialOpened != null && Object.hasOwnProperty.call(message, "specialOpened"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.specialOpened);
            if (message.recordNormal != null && Object.hasOwnProperty.call(message, "recordNormal"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.recordNormal);
            if (message.recordSpecial != null && Object.hasOwnProperty.call(message, "recordSpecial"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.recordSpecial);
            return writer;
        };

        /**
         * Encodes the specified FairyThemeInfo message, length delimited. Does not implicitly {@link common.FairyThemeInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.FairyThemeInfo
         * @static
         * @param {common.IFairyThemeInfo} message FairyThemeInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FairyThemeInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FairyThemeInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.FairyThemeInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.FairyThemeInfo} FairyThemeInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FairyThemeInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.FairyThemeInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.level = reader.int32();
                        break;
                    }
                case 3: {
                        message.specialOpened = reader.bool();
                        break;
                    }
                case 4: {
                        message.recordNormal = reader.int64();
                        break;
                    }
                case 5: {
                        message.recordSpecial = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FairyThemeInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.FairyThemeInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.FairyThemeInfo} FairyThemeInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FairyThemeInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FairyThemeInfo message.
         * @function verify
         * @memberof common.FairyThemeInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FairyThemeInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.specialOpened != null && message.hasOwnProperty("specialOpened"))
                if (typeof message.specialOpened !== "boolean")
                    return "specialOpened: boolean expected";
            if (message.recordNormal != null && message.hasOwnProperty("recordNormal"))
                if (!$util.isInteger(message.recordNormal) && !(message.recordNormal && $util.isInteger(message.recordNormal.low) && $util.isInteger(message.recordNormal.high)))
                    return "recordNormal: integer|Long expected";
            if (message.recordSpecial != null && message.hasOwnProperty("recordSpecial"))
                if (!$util.isInteger(message.recordSpecial) && !(message.recordSpecial && $util.isInteger(message.recordSpecial.low) && $util.isInteger(message.recordSpecial.high)))
                    return "recordSpecial: integer|Long expected";
            return null;
        };

        /**
         * Creates a FairyThemeInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.FairyThemeInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.FairyThemeInfo} FairyThemeInfo
         */
        FairyThemeInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.FairyThemeInfo)
                return object;
            var message = new $root.common.FairyThemeInfo();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.level != null)
                message.level = object.level | 0;
            if (object.specialOpened != null)
                message.specialOpened = Boolean(object.specialOpened);
            if (object.recordNormal != null)
                if ($util.Long)
                    (message.recordNormal = $util.Long.fromValue(object.recordNormal)).unsigned = false;
                else if (typeof object.recordNormal === "string")
                    message.recordNormal = parseInt(object.recordNormal, 10);
                else if (typeof object.recordNormal === "number")
                    message.recordNormal = object.recordNormal;
                else if (typeof object.recordNormal === "object")
                    message.recordNormal = new $util.LongBits(object.recordNormal.low >>> 0, object.recordNormal.high >>> 0).toNumber();
            if (object.recordSpecial != null)
                if ($util.Long)
                    (message.recordSpecial = $util.Long.fromValue(object.recordSpecial)).unsigned = false;
                else if (typeof object.recordSpecial === "string")
                    message.recordSpecial = parseInt(object.recordSpecial, 10);
                else if (typeof object.recordSpecial === "number")
                    message.recordSpecial = object.recordSpecial;
                else if (typeof object.recordSpecial === "object")
                    message.recordSpecial = new $util.LongBits(object.recordSpecial.low >>> 0, object.recordSpecial.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a FairyThemeInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.FairyThemeInfo
         * @static
         * @param {common.FairyThemeInfo} message FairyThemeInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FairyThemeInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.level = 0;
                object.specialOpened = false;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.recordNormal = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.recordNormal = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.recordSpecial = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.recordSpecial = options.longs === String ? "0" : 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.specialOpened != null && message.hasOwnProperty("specialOpened"))
                object.specialOpened = message.specialOpened;
            if (message.recordNormal != null && message.hasOwnProperty("recordNormal"))
                if (typeof message.recordNormal === "number")
                    object.recordNormal = options.longs === String ? String(message.recordNormal) : message.recordNormal;
                else
                    object.recordNormal = options.longs === String ? $util.Long.prototype.toString.call(message.recordNormal) : options.longs === Number ? new $util.LongBits(message.recordNormal.low >>> 0, message.recordNormal.high >>> 0).toNumber() : message.recordNormal;
            if (message.recordSpecial != null && message.hasOwnProperty("recordSpecial"))
                if (typeof message.recordSpecial === "number")
                    object.recordSpecial = options.longs === String ? String(message.recordSpecial) : message.recordSpecial;
                else
                    object.recordSpecial = options.longs === String ? $util.Long.prototype.toString.call(message.recordSpecial) : options.longs === Number ? new $util.LongBits(message.recordSpecial.low >>> 0, message.recordSpecial.high >>> 0).toNumber() : message.recordSpecial;
            return object;
        };

        /**
         * Converts this FairyThemeInfo to JSON.
         * @function toJSON
         * @memberof common.FairyThemeInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FairyThemeInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FairyThemeInfo
         * @function getTypeUrl
         * @memberof common.FairyThemeInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FairyThemeInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.FairyThemeInfo";
        };

        return FairyThemeInfo;
    })();

    common.GuideInfo = (function() {

        /**
         * Properties of a GuideInfo.
         * @memberof common
         * @interface IGuideInfo
         * @property {number|null} [taskId] GuideInfo taskId
         * @property {number|null} [stepId] GuideInfo stepId
         * @property {Object.<string,number>|null} [jumpRecord] GuideInfo jumpRecord
         * @property {Array.<number>|null} [finishedTaskIds] GuideInfo finishedTaskIds
         */

        /**
         * Constructs a new GuideInfo.
         * @memberof common
         * @classdesc Represents a GuideInfo.
         * @implements IGuideInfo
         * @constructor
         * @param {common.IGuideInfo=} [properties] Properties to set
         */
        function GuideInfo(properties) {
            this.jumpRecord = {};
            this.finishedTaskIds = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GuideInfo taskId.
         * @member {number} taskId
         * @memberof common.GuideInfo
         * @instance
         */
        GuideInfo.prototype.taskId = 0;

        /**
         * GuideInfo stepId.
         * @member {number} stepId
         * @memberof common.GuideInfo
         * @instance
         */
        GuideInfo.prototype.stepId = 0;

        /**
         * GuideInfo jumpRecord.
         * @member {Object.<string,number>} jumpRecord
         * @memberof common.GuideInfo
         * @instance
         */
        GuideInfo.prototype.jumpRecord = $util.emptyObject;

        /**
         * GuideInfo finishedTaskIds.
         * @member {Array.<number>} finishedTaskIds
         * @memberof common.GuideInfo
         * @instance
         */
        GuideInfo.prototype.finishedTaskIds = $util.emptyArray;

        /**
         * Creates a new GuideInfo instance using the specified properties.
         * @function create
         * @memberof common.GuideInfo
         * @static
         * @param {common.IGuideInfo=} [properties] Properties to set
         * @returns {common.GuideInfo} GuideInfo instance
         */
        GuideInfo.create = function create(properties) {
            return new GuideInfo(properties);
        };

        /**
         * Encodes the specified GuideInfo message. Does not implicitly {@link common.GuideInfo.verify|verify} messages.
         * @function encode
         * @memberof common.GuideInfo
         * @static
         * @param {common.IGuideInfo} message GuideInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuideInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.taskId);
            if (message.stepId != null && Object.hasOwnProperty.call(message, "stepId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.stepId);
            if (message.jumpRecord != null && Object.hasOwnProperty.call(message, "jumpRecord"))
                for (var keys = Object.keys(message.jumpRecord), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.jumpRecord[keys[i]]).ldelim();
            if (message.finishedTaskIds != null && message.finishedTaskIds.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (var i = 0; i < message.finishedTaskIds.length; ++i)
                    writer.int32(message.finishedTaskIds[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GuideInfo message, length delimited. Does not implicitly {@link common.GuideInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.GuideInfo
         * @static
         * @param {common.IGuideInfo} message GuideInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuideInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GuideInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.GuideInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.GuideInfo} GuideInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuideInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.GuideInfo(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.taskId = reader.int32();
                        break;
                    }
                case 2: {
                        message.stepId = reader.int32();
                        break;
                    }
                case 3: {
                        if (message.jumpRecord === $util.emptyObject)
                            message.jumpRecord = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int32();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.jumpRecord[key] = value;
                        break;
                    }
                case 4: {
                        if (!(message.finishedTaskIds && message.finishedTaskIds.length))
                            message.finishedTaskIds = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.finishedTaskIds.push(reader.int32());
                        } else
                            message.finishedTaskIds.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GuideInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.GuideInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.GuideInfo} GuideInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuideInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GuideInfo message.
         * @function verify
         * @memberof common.GuideInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GuideInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.taskId != null && message.hasOwnProperty("taskId"))
                if (!$util.isInteger(message.taskId))
                    return "taskId: integer expected";
            if (message.stepId != null && message.hasOwnProperty("stepId"))
                if (!$util.isInteger(message.stepId))
                    return "stepId: integer expected";
            if (message.jumpRecord != null && message.hasOwnProperty("jumpRecord")) {
                if (!$util.isObject(message.jumpRecord))
                    return "jumpRecord: object expected";
                var key = Object.keys(message.jumpRecord);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "jumpRecord: integer key{k:int32} expected";
                    if (!$util.isInteger(message.jumpRecord[key[i]]))
                        return "jumpRecord: integer{k:int32} expected";
                }
            }
            if (message.finishedTaskIds != null && message.hasOwnProperty("finishedTaskIds")) {
                if (!Array.isArray(message.finishedTaskIds))
                    return "finishedTaskIds: array expected";
                for (var i = 0; i < message.finishedTaskIds.length; ++i)
                    if (!$util.isInteger(message.finishedTaskIds[i]))
                        return "finishedTaskIds: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a GuideInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.GuideInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.GuideInfo} GuideInfo
         */
        GuideInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.GuideInfo)
                return object;
            var message = new $root.common.GuideInfo();
            if (object.taskId != null)
                message.taskId = object.taskId | 0;
            if (object.stepId != null)
                message.stepId = object.stepId | 0;
            if (object.jumpRecord) {
                if (typeof object.jumpRecord !== "object")
                    throw TypeError(".common.GuideInfo.jumpRecord: object expected");
                message.jumpRecord = {};
                for (var keys = Object.keys(object.jumpRecord), i = 0; i < keys.length; ++i)
                    message.jumpRecord[keys[i]] = object.jumpRecord[keys[i]] | 0;
            }
            if (object.finishedTaskIds) {
                if (!Array.isArray(object.finishedTaskIds))
                    throw TypeError(".common.GuideInfo.finishedTaskIds: array expected");
                message.finishedTaskIds = [];
                for (var i = 0; i < object.finishedTaskIds.length; ++i)
                    message.finishedTaskIds[i] = object.finishedTaskIds[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a GuideInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.GuideInfo
         * @static
         * @param {common.GuideInfo} message GuideInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GuideInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.finishedTaskIds = [];
            if (options.objects || options.defaults)
                object.jumpRecord = {};
            if (options.defaults) {
                object.taskId = 0;
                object.stepId = 0;
            }
            if (message.taskId != null && message.hasOwnProperty("taskId"))
                object.taskId = message.taskId;
            if (message.stepId != null && message.hasOwnProperty("stepId"))
                object.stepId = message.stepId;
            var keys2;
            if (message.jumpRecord && (keys2 = Object.keys(message.jumpRecord)).length) {
                object.jumpRecord = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.jumpRecord[keys2[j]] = message.jumpRecord[keys2[j]];
            }
            if (message.finishedTaskIds && message.finishedTaskIds.length) {
                object.finishedTaskIds = [];
                for (var j = 0; j < message.finishedTaskIds.length; ++j)
                    object.finishedTaskIds[j] = message.finishedTaskIds[j];
            }
            return object;
        };

        /**
         * Converts this GuideInfo to JSON.
         * @function toJSON
         * @memberof common.GuideInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GuideInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GuideInfo
         * @function getTypeUrl
         * @memberof common.GuideInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GuideInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.GuideInfo";
        };

        return GuideInfo;
    })();

    common.UserGuildInfo = (function() {

        /**
         * Properties of a UserGuildInfo.
         * @memberof common
         * @interface IUserGuildInfo
         * @property {string|null} [gid] UserGuildInfo gid
         * @property {string|null} [name] UserGuildInfo name
         * @property {number|null} [flag] UserGuildInfo flag
         * @property {string|null} [location] UserGuildInfo location
         */

        /**
         * Constructs a new UserGuildInfo.
         * @memberof common
         * @classdesc Represents a UserGuildInfo.
         * @implements IUserGuildInfo
         * @constructor
         * @param {common.IUserGuildInfo=} [properties] Properties to set
         */
        function UserGuildInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserGuildInfo gid.
         * @member {string} gid
         * @memberof common.UserGuildInfo
         * @instance
         */
        UserGuildInfo.prototype.gid = "";

        /**
         * UserGuildInfo name.
         * @member {string} name
         * @memberof common.UserGuildInfo
         * @instance
         */
        UserGuildInfo.prototype.name = "";

        /**
         * UserGuildInfo flag.
         * @member {number} flag
         * @memberof common.UserGuildInfo
         * @instance
         */
        UserGuildInfo.prototype.flag = 0;

        /**
         * UserGuildInfo location.
         * @member {string} location
         * @memberof common.UserGuildInfo
         * @instance
         */
        UserGuildInfo.prototype.location = "";

        /**
         * Creates a new UserGuildInfo instance using the specified properties.
         * @function create
         * @memberof common.UserGuildInfo
         * @static
         * @param {common.IUserGuildInfo=} [properties] Properties to set
         * @returns {common.UserGuildInfo} UserGuildInfo instance
         */
        UserGuildInfo.create = function create(properties) {
            return new UserGuildInfo(properties);
        };

        /**
         * Encodes the specified UserGuildInfo message. Does not implicitly {@link common.UserGuildInfo.verify|verify} messages.
         * @function encode
         * @memberof common.UserGuildInfo
         * @static
         * @param {common.IUserGuildInfo} message UserGuildInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserGuildInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gid != null && Object.hasOwnProperty.call(message, "gid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.gid);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.flag);
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.location);
            return writer;
        };

        /**
         * Encodes the specified UserGuildInfo message, length delimited. Does not implicitly {@link common.UserGuildInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.UserGuildInfo
         * @static
         * @param {common.IUserGuildInfo} message UserGuildInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserGuildInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a UserGuildInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.UserGuildInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.UserGuildInfo} UserGuildInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserGuildInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.UserGuildInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.gid = reader.string();
                        break;
                    }
                case 2: {
                        message.name = reader.string();
                        break;
                    }
                case 3: {
                        message.flag = reader.int32();
                        break;
                    }
                case 4: {
                        message.location = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a UserGuildInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.UserGuildInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.UserGuildInfo} UserGuildInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserGuildInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UserGuildInfo message.
         * @function verify
         * @memberof common.UserGuildInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UserGuildInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.gid != null && message.hasOwnProperty("gid"))
                if (!$util.isString(message.gid))
                    return "gid: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.flag != null && message.hasOwnProperty("flag"))
                if (!$util.isInteger(message.flag))
                    return "flag: integer expected";
            if (message.location != null && message.hasOwnProperty("location"))
                if (!$util.isString(message.location))
                    return "location: string expected";
            return null;
        };

        /**
         * Creates a UserGuildInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.UserGuildInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.UserGuildInfo} UserGuildInfo
         */
        UserGuildInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.UserGuildInfo)
                return object;
            var message = new $root.common.UserGuildInfo();
            if (object.gid != null)
                message.gid = String(object.gid);
            if (object.name != null)
                message.name = String(object.name);
            if (object.flag != null)
                message.flag = object.flag | 0;
            if (object.location != null)
                message.location = String(object.location);
            return message;
        };

        /**
         * Creates a plain object from a UserGuildInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.UserGuildInfo
         * @static
         * @param {common.UserGuildInfo} message UserGuildInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserGuildInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.gid = "";
                object.name = "";
                object.flag = 0;
                object.location = "";
            }
            if (message.gid != null && message.hasOwnProperty("gid"))
                object.gid = message.gid;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.flag != null && message.hasOwnProperty("flag"))
                object.flag = message.flag;
            if (message.location != null && message.hasOwnProperty("location"))
                object.location = message.location;
            return object;
        };

        /**
         * Converts this UserGuildInfo to JSON.
         * @function toJSON
         * @memberof common.UserGuildInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserGuildInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserGuildInfo
         * @function getTypeUrl
         * @memberof common.UserGuildInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserGuildInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.UserGuildInfo";
        };

        return UserGuildInfo;
    })();

    common.UserProfile = (function() {

        /**
         * Properties of a UserProfile.
         * @memberof common
         * @interface IUserProfile
         * @property {string|null} [uid] UserProfile uid
         * @property {string|null} [openId] UserProfile openId
         * @property {common.IUserBaseInfo|null} [info] UserProfile info
         * @property {common.ILoginInfo|null} [login] UserProfile login
         * @property {number|null} [level] UserProfile level
         * @property {number|null} [cups] UserProfile cups
         * @property {number|null} [historyMaxCups] UserProfile historyMaxCups
         * @property {number|null} [adTimes] UserProfile adTimes
         * @property {number|null} [guest] UserProfile guest
         * @property {common.IPaymentInfo|null} [payment] UserProfile payment
         * @property {common.ICosumeInfo|null} [consume] UserProfile consume
         * @property {common.IResourceInfo|null} [resource] UserProfile resource
         * @property {Object.<string,common.IHero>|null} [heros] UserProfile heros
         * @property {Object.<string,common.IHeroTeam>|null} [heroTeams] UserProfile heroTeams
         * @property {number|null} [curHeroTeam] UserProfile curHeroTeam
         * @property {common.IBagInfo|null} [bag] UserProfile bag
         * @property {common.IShopInfo|null} [shop] UserProfile shop
         * @property {Object.<string,number>|null} [mail] UserProfile mail
         * @property {common.ITaskInfo|null} [task] UserProfile task
         * @property {Object.<string,common.IBoxInfo>|null} [box] UserProfile box
         * @property {Object.<string,common.IDropInfo>|null} [drop] UserProfile drop
         * @property {common.IArenaInfo|null} [arena] UserProfile arena
         * @property {common.IMarkInfo|null} [mark] UserProfile mark
         * @property {common.IBindInfo|null} [bind] UserProfile bind
         * @property {Array.<number>|null} [hasAvatars] UserProfile hasAvatars
         * @property {common.IAdventureInfo|null} [adventure] UserProfile adventure
         * @property {common.IEventInfo|null} [events] UserProfile events
         * @property {common.IWorkTableInfo|null} [workTable] UserProfile workTable
         * @property {common.IFairyThemeInfo|null} [fairyTheme] UserProfile fairyTheme
         * @property {common.IGuideInfo|null} [guide] UserProfile guide
         * @property {Array.<number>|null} [unlockedSkins] UserProfile unlockedSkins
         * @property {Object.<string,number|Long>|null} [skinExpire] UserProfile skinExpire
         * @property {Array.<number>|null} [markedSaleIds] UserProfile markedSaleIds
         * @property {Array.<string>|null} [markedGoods] UserProfile markedGoods
         * @property {Object.<string,number>|null} [diyFlags] UserProfile diyFlags
         * @property {string|null} [customData] UserProfile customData
         * @property {common.IUserGuildInfo|null} [guild] UserProfile guild
         */

        /**
         * Constructs a new UserProfile.
         * @memberof common
         * @classdesc Represents a UserProfile.
         * @implements IUserProfile
         * @constructor
         * @param {common.IUserProfile=} [properties] Properties to set
         */
        function UserProfile(properties) {
            this.heros = {};
            this.heroTeams = {};
            this.mail = {};
            this.box = {};
            this.drop = {};
            this.hasAvatars = [];
            this.unlockedSkins = [];
            this.skinExpire = {};
            this.markedSaleIds = [];
            this.markedGoods = [];
            this.diyFlags = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserProfile uid.
         * @member {string} uid
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.uid = "";

        /**
         * UserProfile openId.
         * @member {string} openId
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.openId = "";

        /**
         * UserProfile info.
         * @member {common.IUserBaseInfo|null|undefined} info
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.info = null;

        /**
         * UserProfile login.
         * @member {common.ILoginInfo|null|undefined} login
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.login = null;

        /**
         * UserProfile level.
         * @member {number} level
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.level = 0;

        /**
         * UserProfile cups.
         * @member {number} cups
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.cups = 0;

        /**
         * UserProfile historyMaxCups.
         * @member {number} historyMaxCups
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.historyMaxCups = 0;

        /**
         * UserProfile adTimes.
         * @member {number} adTimes
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.adTimes = 0;

        /**
         * UserProfile guest.
         * @member {number} guest
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.guest = 0;

        /**
         * UserProfile payment.
         * @member {common.IPaymentInfo|null|undefined} payment
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.payment = null;

        /**
         * UserProfile consume.
         * @member {common.ICosumeInfo|null|undefined} consume
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.consume = null;

        /**
         * UserProfile resource.
         * @member {common.IResourceInfo|null|undefined} resource
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.resource = null;

        /**
         * UserProfile heros.
         * @member {Object.<string,common.IHero>} heros
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.heros = $util.emptyObject;

        /**
         * UserProfile heroTeams.
         * @member {Object.<string,common.IHeroTeam>} heroTeams
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.heroTeams = $util.emptyObject;

        /**
         * UserProfile curHeroTeam.
         * @member {number} curHeroTeam
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.curHeroTeam = 0;

        /**
         * UserProfile bag.
         * @member {common.IBagInfo|null|undefined} bag
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.bag = null;

        /**
         * UserProfile shop.
         * @member {common.IShopInfo|null|undefined} shop
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.shop = null;

        /**
         * UserProfile mail.
         * @member {Object.<string,number>} mail
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.mail = $util.emptyObject;

        /**
         * UserProfile task.
         * @member {common.ITaskInfo|null|undefined} task
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.task = null;

        /**
         * UserProfile box.
         * @member {Object.<string,common.IBoxInfo>} box
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.box = $util.emptyObject;

        /**
         * UserProfile drop.
         * @member {Object.<string,common.IDropInfo>} drop
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.drop = $util.emptyObject;

        /**
         * UserProfile arena.
         * @member {common.IArenaInfo|null|undefined} arena
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.arena = null;

        /**
         * UserProfile mark.
         * @member {common.IMarkInfo|null|undefined} mark
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.mark = null;

        /**
         * UserProfile bind.
         * @member {common.IBindInfo|null|undefined} bind
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.bind = null;

        /**
         * UserProfile hasAvatars.
         * @member {Array.<number>} hasAvatars
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.hasAvatars = $util.emptyArray;

        /**
         * UserProfile adventure.
         * @member {common.IAdventureInfo|null|undefined} adventure
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.adventure = null;

        /**
         * UserProfile events.
         * @member {common.IEventInfo|null|undefined} events
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.events = null;

        /**
         * UserProfile workTable.
         * @member {common.IWorkTableInfo|null|undefined} workTable
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.workTable = null;

        /**
         * UserProfile fairyTheme.
         * @member {common.IFairyThemeInfo|null|undefined} fairyTheme
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.fairyTheme = null;

        /**
         * UserProfile guide.
         * @member {common.IGuideInfo|null|undefined} guide
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.guide = null;

        /**
         * UserProfile unlockedSkins.
         * @member {Array.<number>} unlockedSkins
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.unlockedSkins = $util.emptyArray;

        /**
         * UserProfile skinExpire.
         * @member {Object.<string,number|Long>} skinExpire
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.skinExpire = $util.emptyObject;

        /**
         * UserProfile markedSaleIds.
         * @member {Array.<number>} markedSaleIds
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.markedSaleIds = $util.emptyArray;

        /**
         * UserProfile markedGoods.
         * @member {Array.<string>} markedGoods
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.markedGoods = $util.emptyArray;

        /**
         * UserProfile diyFlags.
         * @member {Object.<string,number>} diyFlags
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.diyFlags = $util.emptyObject;

        /**
         * UserProfile customData.
         * @member {string} customData
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.customData = "";

        /**
         * UserProfile guild.
         * @member {common.IUserGuildInfo|null|undefined} guild
         * @memberof common.UserProfile
         * @instance
         */
        UserProfile.prototype.guild = null;

        /**
         * Creates a new UserProfile instance using the specified properties.
         * @function create
         * @memberof common.UserProfile
         * @static
         * @param {common.IUserProfile=} [properties] Properties to set
         * @returns {common.UserProfile} UserProfile instance
         */
        UserProfile.create = function create(properties) {
            return new UserProfile(properties);
        };

        /**
         * Encodes the specified UserProfile message. Does not implicitly {@link common.UserProfile.verify|verify} messages.
         * @function encode
         * @memberof common.UserProfile
         * @static
         * @param {common.IUserProfile} message UserProfile message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserProfile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.openId != null && Object.hasOwnProperty.call(message, "openId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.openId);
            if (message.info != null && Object.hasOwnProperty.call(message, "info"))
                $root.common.UserBaseInfo.encode(message.info, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.login != null && Object.hasOwnProperty.call(message, "login"))
                $root.common.LoginInfo.encode(message.login, writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.level);
            if (message.cups != null && Object.hasOwnProperty.call(message, "cups"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.cups);
            if (message.historyMaxCups != null && Object.hasOwnProperty.call(message, "historyMaxCups"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.historyMaxCups);
            if (message.adTimes != null && Object.hasOwnProperty.call(message, "adTimes"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.adTimes);
            if (message.guest != null && Object.hasOwnProperty.call(message, "guest"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.guest);
            if (message.payment != null && Object.hasOwnProperty.call(message, "payment"))
                $root.common.PaymentInfo.encode(message.payment, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.consume != null && Object.hasOwnProperty.call(message, "consume"))
                $root.common.CosumeInfo.encode(message.consume, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.resource != null && Object.hasOwnProperty.call(message, "resource"))
                $root.common.ResourceInfo.encode(message.resource, writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.heros != null && Object.hasOwnProperty.call(message, "heros"))
                for (var keys = Object.keys(message.heros), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 13, wireType 2 =*/106).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.Hero.encode(message.heros[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            if (message.heroTeams != null && Object.hasOwnProperty.call(message, "heroTeams"))
                for (var keys = Object.keys(message.heroTeams), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 14, wireType 2 =*/114).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.HeroTeam.encode(message.heroTeams[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            if (message.curHeroTeam != null && Object.hasOwnProperty.call(message, "curHeroTeam"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.curHeroTeam);
            if (message.bag != null && Object.hasOwnProperty.call(message, "bag"))
                $root.common.BagInfo.encode(message.bag, writer.uint32(/* id 16, wireType 2 =*/130).fork()).ldelim();
            if (message.shop != null && Object.hasOwnProperty.call(message, "shop"))
                $root.common.ShopInfo.encode(message.shop, writer.uint32(/* id 17, wireType 2 =*/138).fork()).ldelim();
            if (message.mail != null && Object.hasOwnProperty.call(message, "mail"))
                for (var keys = Object.keys(message.mail), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 18, wireType 2 =*/146).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.mail[keys[i]]).ldelim();
            if (message.task != null && Object.hasOwnProperty.call(message, "task"))
                $root.common.TaskInfo.encode(message.task, writer.uint32(/* id 19, wireType 2 =*/154).fork()).ldelim();
            if (message.box != null && Object.hasOwnProperty.call(message, "box"))
                for (var keys = Object.keys(message.box), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 20, wireType 2 =*/162).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.BoxInfo.encode(message.box[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            if (message.drop != null && Object.hasOwnProperty.call(message, "drop"))
                for (var keys = Object.keys(message.drop), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 21, wireType 2 =*/170).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.DropInfo.encode(message.drop[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            if (message.arena != null && Object.hasOwnProperty.call(message, "arena"))
                $root.common.ArenaInfo.encode(message.arena, writer.uint32(/* id 22, wireType 2 =*/178).fork()).ldelim();
            if (message.mark != null && Object.hasOwnProperty.call(message, "mark"))
                $root.common.MarkInfo.encode(message.mark, writer.uint32(/* id 23, wireType 2 =*/186).fork()).ldelim();
            if (message.bind != null && Object.hasOwnProperty.call(message, "bind"))
                $root.common.BindInfo.encode(message.bind, writer.uint32(/* id 24, wireType 2 =*/194).fork()).ldelim();
            if (message.hasAvatars != null && message.hasAvatars.length) {
                writer.uint32(/* id 25, wireType 2 =*/202).fork();
                for (var i = 0; i < message.hasAvatars.length; ++i)
                    writer.int32(message.hasAvatars[i]);
                writer.ldelim();
            }
            if (message.adventure != null && Object.hasOwnProperty.call(message, "adventure"))
                $root.common.AdventureInfo.encode(message.adventure, writer.uint32(/* id 26, wireType 2 =*/210).fork()).ldelim();
            if (message.events != null && Object.hasOwnProperty.call(message, "events"))
                $root.common.EventInfo.encode(message.events, writer.uint32(/* id 27, wireType 2 =*/218).fork()).ldelim();
            if (message.workTable != null && Object.hasOwnProperty.call(message, "workTable"))
                $root.common.WorkTableInfo.encode(message.workTable, writer.uint32(/* id 28, wireType 2 =*/226).fork()).ldelim();
            if (message.fairyTheme != null && Object.hasOwnProperty.call(message, "fairyTheme"))
                $root.common.FairyThemeInfo.encode(message.fairyTheme, writer.uint32(/* id 29, wireType 2 =*/234).fork()).ldelim();
            if (message.guide != null && Object.hasOwnProperty.call(message, "guide"))
                $root.common.GuideInfo.encode(message.guide, writer.uint32(/* id 30, wireType 2 =*/242).fork()).ldelim();
            if (message.unlockedSkins != null && message.unlockedSkins.length) {
                writer.uint32(/* id 31, wireType 2 =*/250).fork();
                for (var i = 0; i < message.unlockedSkins.length; ++i)
                    writer.int32(message.unlockedSkins[i]);
                writer.ldelim();
            }
            if (message.skinExpire != null && Object.hasOwnProperty.call(message, "skinExpire"))
                for (var keys = Object.keys(message.skinExpire), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 32, wireType 2 =*/258).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.skinExpire[keys[i]]).ldelim();
            if (message.markedSaleIds != null && message.markedSaleIds.length) {
                writer.uint32(/* id 33, wireType 2 =*/266).fork();
                for (var i = 0; i < message.markedSaleIds.length; ++i)
                    writer.int32(message.markedSaleIds[i]);
                writer.ldelim();
            }
            if (message.markedGoods != null && message.markedGoods.length)
                for (var i = 0; i < message.markedGoods.length; ++i)
                    writer.uint32(/* id 34, wireType 2 =*/274).string(message.markedGoods[i]);
            if (message.diyFlags != null && Object.hasOwnProperty.call(message, "diyFlags"))
                for (var keys = Object.keys(message.diyFlags), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 35, wireType 2 =*/282).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.diyFlags[keys[i]]).ldelim();
            if (message.customData != null && Object.hasOwnProperty.call(message, "customData"))
                writer.uint32(/* id 36, wireType 2 =*/290).string(message.customData);
            if (message.guild != null && Object.hasOwnProperty.call(message, "guild"))
                $root.common.UserGuildInfo.encode(message.guild, writer.uint32(/* id 37, wireType 2 =*/298).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified UserProfile message, length delimited. Does not implicitly {@link common.UserProfile.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.UserProfile
         * @static
         * @param {common.IUserProfile} message UserProfile message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserProfile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a UserProfile message from the specified reader or buffer.
         * @function decode
         * @memberof common.UserProfile
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.UserProfile} UserProfile
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserProfile.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.UserProfile(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.openId = reader.string();
                        break;
                    }
                case 3: {
                        message.info = $root.common.UserBaseInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 4: {
                        message.login = $root.common.LoginInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 5: {
                        message.level = reader.int32();
                        break;
                    }
                case 6: {
                        message.cups = reader.int32();
                        break;
                    }
                case 7: {
                        message.historyMaxCups = reader.int32();
                        break;
                    }
                case 8: {
                        message.adTimes = reader.int32();
                        break;
                    }
                case 9: {
                        message.guest = reader.int32();
                        break;
                    }
                case 10: {
                        message.payment = $root.common.PaymentInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 11: {
                        message.consume = $root.common.CosumeInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 12: {
                        message.resource = $root.common.ResourceInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 13: {
                        if (message.heros === $util.emptyObject)
                            message.heros = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.Hero.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.heros[key] = value;
                        break;
                    }
                case 14: {
                        if (message.heroTeams === $util.emptyObject)
                            message.heroTeams = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.HeroTeam.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.heroTeams[key] = value;
                        break;
                    }
                case 15: {
                        message.curHeroTeam = reader.int32();
                        break;
                    }
                case 16: {
                        message.bag = $root.common.BagInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 17: {
                        message.shop = $root.common.ShopInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 18: {
                        if (message.mail === $util.emptyObject)
                            message.mail = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = reader.int32();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.mail[key] = value;
                        break;
                    }
                case 19: {
                        message.task = $root.common.TaskInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 20: {
                        if (message.box === $util.emptyObject)
                            message.box = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.BoxInfo.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.box[key] = value;
                        break;
                    }
                case 21: {
                        if (message.drop === $util.emptyObject)
                            message.drop = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.DropInfo.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.drop[key] = value;
                        break;
                    }
                case 22: {
                        message.arena = $root.common.ArenaInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 23: {
                        message.mark = $root.common.MarkInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 24: {
                        message.bind = $root.common.BindInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 25: {
                        if (!(message.hasAvatars && message.hasAvatars.length))
                            message.hasAvatars = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.hasAvatars.push(reader.int32());
                        } else
                            message.hasAvatars.push(reader.int32());
                        break;
                    }
                case 26: {
                        message.adventure = $root.common.AdventureInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 27: {
                        message.events = $root.common.EventInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 28: {
                        message.workTable = $root.common.WorkTableInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 29: {
                        message.fairyTheme = $root.common.FairyThemeInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 30: {
                        message.guide = $root.common.GuideInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 31: {
                        if (!(message.unlockedSkins && message.unlockedSkins.length))
                            message.unlockedSkins = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.unlockedSkins.push(reader.int32());
                        } else
                            message.unlockedSkins.push(reader.int32());
                        break;
                    }
                case 32: {
                        if (message.skinExpire === $util.emptyObject)
                            message.skinExpire = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int64();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.skinExpire[key] = value;
                        break;
                    }
                case 33: {
                        if (!(message.markedSaleIds && message.markedSaleIds.length))
                            message.markedSaleIds = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.markedSaleIds.push(reader.int32());
                        } else
                            message.markedSaleIds.push(reader.int32());
                        break;
                    }
                case 34: {
                        if (!(message.markedGoods && message.markedGoods.length))
                            message.markedGoods = [];
                        message.markedGoods.push(reader.string());
                        break;
                    }
                case 35: {
                        if (message.diyFlags === $util.emptyObject)
                            message.diyFlags = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int32();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.diyFlags[key] = value;
                        break;
                    }
                case 36: {
                        message.customData = reader.string();
                        break;
                    }
                case 37: {
                        message.guild = $root.common.UserGuildInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a UserProfile message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.UserProfile
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.UserProfile} UserProfile
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserProfile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UserProfile message.
         * @function verify
         * @memberof common.UserProfile
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UserProfile.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.openId != null && message.hasOwnProperty("openId"))
                if (!$util.isString(message.openId))
                    return "openId: string expected";
            if (message.info != null && message.hasOwnProperty("info")) {
                var error = $root.common.UserBaseInfo.verify(message.info);
                if (error)
                    return "info." + error;
            }
            if (message.login != null && message.hasOwnProperty("login")) {
                var error = $root.common.LoginInfo.verify(message.login);
                if (error)
                    return "login." + error;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.cups != null && message.hasOwnProperty("cups"))
                if (!$util.isInteger(message.cups))
                    return "cups: integer expected";
            if (message.historyMaxCups != null && message.hasOwnProperty("historyMaxCups"))
                if (!$util.isInteger(message.historyMaxCups))
                    return "historyMaxCups: integer expected";
            if (message.adTimes != null && message.hasOwnProperty("adTimes"))
                if (!$util.isInteger(message.adTimes))
                    return "adTimes: integer expected";
            if (message.guest != null && message.hasOwnProperty("guest"))
                if (!$util.isInteger(message.guest))
                    return "guest: integer expected";
            if (message.payment != null && message.hasOwnProperty("payment")) {
                var error = $root.common.PaymentInfo.verify(message.payment);
                if (error)
                    return "payment." + error;
            }
            if (message.consume != null && message.hasOwnProperty("consume")) {
                var error = $root.common.CosumeInfo.verify(message.consume);
                if (error)
                    return "consume." + error;
            }
            if (message.resource != null && message.hasOwnProperty("resource")) {
                var error = $root.common.ResourceInfo.verify(message.resource);
                if (error)
                    return "resource." + error;
            }
            if (message.heros != null && message.hasOwnProperty("heros")) {
                if (!$util.isObject(message.heros))
                    return "heros: object expected";
                var key = Object.keys(message.heros);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "heros: integer key{k:int32} expected";
                    {
                        var error = $root.common.Hero.verify(message.heros[key[i]]);
                        if (error)
                            return "heros." + error;
                    }
                }
            }
            if (message.heroTeams != null && message.hasOwnProperty("heroTeams")) {
                if (!$util.isObject(message.heroTeams))
                    return "heroTeams: object expected";
                var key = Object.keys(message.heroTeams);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "heroTeams: integer key{k:int32} expected";
                    {
                        var error = $root.common.HeroTeam.verify(message.heroTeams[key[i]]);
                        if (error)
                            return "heroTeams." + error;
                    }
                }
            }
            if (message.curHeroTeam != null && message.hasOwnProperty("curHeroTeam"))
                if (!$util.isInteger(message.curHeroTeam))
                    return "curHeroTeam: integer expected";
            if (message.bag != null && message.hasOwnProperty("bag")) {
                var error = $root.common.BagInfo.verify(message.bag);
                if (error)
                    return "bag." + error;
            }
            if (message.shop != null && message.hasOwnProperty("shop")) {
                var error = $root.common.ShopInfo.verify(message.shop);
                if (error)
                    return "shop." + error;
            }
            if (message.mail != null && message.hasOwnProperty("mail")) {
                if (!$util.isObject(message.mail))
                    return "mail: object expected";
                var key = Object.keys(message.mail);
                for (var i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.mail[key[i]]))
                        return "mail: integer{k:string} expected";
            }
            if (message.task != null && message.hasOwnProperty("task")) {
                var error = $root.common.TaskInfo.verify(message.task);
                if (error)
                    return "task." + error;
            }
            if (message.box != null && message.hasOwnProperty("box")) {
                if (!$util.isObject(message.box))
                    return "box: object expected";
                var key = Object.keys(message.box);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "box: integer key{k:int32} expected";
                    {
                        var error = $root.common.BoxInfo.verify(message.box[key[i]]);
                        if (error)
                            return "box." + error;
                    }
                }
            }
            if (message.drop != null && message.hasOwnProperty("drop")) {
                if (!$util.isObject(message.drop))
                    return "drop: object expected";
                var key = Object.keys(message.drop);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "drop: integer key{k:int32} expected";
                    {
                        var error = $root.common.DropInfo.verify(message.drop[key[i]]);
                        if (error)
                            return "drop." + error;
                    }
                }
            }
            if (message.arena != null && message.hasOwnProperty("arena")) {
                var error = $root.common.ArenaInfo.verify(message.arena);
                if (error)
                    return "arena." + error;
            }
            if (message.mark != null && message.hasOwnProperty("mark")) {
                var error = $root.common.MarkInfo.verify(message.mark);
                if (error)
                    return "mark." + error;
            }
            if (message.bind != null && message.hasOwnProperty("bind")) {
                var error = $root.common.BindInfo.verify(message.bind);
                if (error)
                    return "bind." + error;
            }
            if (message.hasAvatars != null && message.hasOwnProperty("hasAvatars")) {
                if (!Array.isArray(message.hasAvatars))
                    return "hasAvatars: array expected";
                for (var i = 0; i < message.hasAvatars.length; ++i)
                    if (!$util.isInteger(message.hasAvatars[i]))
                        return "hasAvatars: integer[] expected";
            }
            if (message.adventure != null && message.hasOwnProperty("adventure")) {
                var error = $root.common.AdventureInfo.verify(message.adventure);
                if (error)
                    return "adventure." + error;
            }
            if (message.events != null && message.hasOwnProperty("events")) {
                var error = $root.common.EventInfo.verify(message.events);
                if (error)
                    return "events." + error;
            }
            if (message.workTable != null && message.hasOwnProperty("workTable")) {
                var error = $root.common.WorkTableInfo.verify(message.workTable);
                if (error)
                    return "workTable." + error;
            }
            if (message.fairyTheme != null && message.hasOwnProperty("fairyTheme")) {
                var error = $root.common.FairyThemeInfo.verify(message.fairyTheme);
                if (error)
                    return "fairyTheme." + error;
            }
            if (message.guide != null && message.hasOwnProperty("guide")) {
                var error = $root.common.GuideInfo.verify(message.guide);
                if (error)
                    return "guide." + error;
            }
            if (message.unlockedSkins != null && message.hasOwnProperty("unlockedSkins")) {
                if (!Array.isArray(message.unlockedSkins))
                    return "unlockedSkins: array expected";
                for (var i = 0; i < message.unlockedSkins.length; ++i)
                    if (!$util.isInteger(message.unlockedSkins[i]))
                        return "unlockedSkins: integer[] expected";
            }
            if (message.skinExpire != null && message.hasOwnProperty("skinExpire")) {
                if (!$util.isObject(message.skinExpire))
                    return "skinExpire: object expected";
                var key = Object.keys(message.skinExpire);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "skinExpire: integer key{k:int32} expected";
                    if (!$util.isInteger(message.skinExpire[key[i]]) && !(message.skinExpire[key[i]] && $util.isInteger(message.skinExpire[key[i]].low) && $util.isInteger(message.skinExpire[key[i]].high)))
                        return "skinExpire: integer|Long{k:int32} expected";
                }
            }
            if (message.markedSaleIds != null && message.hasOwnProperty("markedSaleIds")) {
                if (!Array.isArray(message.markedSaleIds))
                    return "markedSaleIds: array expected";
                for (var i = 0; i < message.markedSaleIds.length; ++i)
                    if (!$util.isInteger(message.markedSaleIds[i]))
                        return "markedSaleIds: integer[] expected";
            }
            if (message.markedGoods != null && message.hasOwnProperty("markedGoods")) {
                if (!Array.isArray(message.markedGoods))
                    return "markedGoods: array expected";
                for (var i = 0; i < message.markedGoods.length; ++i)
                    if (!$util.isString(message.markedGoods[i]))
                        return "markedGoods: string[] expected";
            }
            if (message.diyFlags != null && message.hasOwnProperty("diyFlags")) {
                if (!$util.isObject(message.diyFlags))
                    return "diyFlags: object expected";
                var key = Object.keys(message.diyFlags);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "diyFlags: integer key{k:int32} expected";
                    if (!$util.isInteger(message.diyFlags[key[i]]))
                        return "diyFlags: integer{k:int32} expected";
                }
            }
            if (message.customData != null && message.hasOwnProperty("customData"))
                if (!$util.isString(message.customData))
                    return "customData: string expected";
            if (message.guild != null && message.hasOwnProperty("guild")) {
                var error = $root.common.UserGuildInfo.verify(message.guild);
                if (error)
                    return "guild." + error;
            }
            return null;
        };

        /**
         * Creates a UserProfile message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.UserProfile
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.UserProfile} UserProfile
         */
        UserProfile.fromObject = function fromObject(object) {
            if (object instanceof $root.common.UserProfile)
                return object;
            var message = new $root.common.UserProfile();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.openId != null)
                message.openId = String(object.openId);
            if (object.info != null) {
                if (typeof object.info !== "object")
                    throw TypeError(".common.UserProfile.info: object expected");
                message.info = $root.common.UserBaseInfo.fromObject(object.info);
            }
            if (object.login != null) {
                if (typeof object.login !== "object")
                    throw TypeError(".common.UserProfile.login: object expected");
                message.login = $root.common.LoginInfo.fromObject(object.login);
            }
            if (object.level != null)
                message.level = object.level | 0;
            if (object.cups != null)
                message.cups = object.cups | 0;
            if (object.historyMaxCups != null)
                message.historyMaxCups = object.historyMaxCups | 0;
            if (object.adTimes != null)
                message.adTimes = object.adTimes | 0;
            if (object.guest != null)
                message.guest = object.guest | 0;
            if (object.payment != null) {
                if (typeof object.payment !== "object")
                    throw TypeError(".common.UserProfile.payment: object expected");
                message.payment = $root.common.PaymentInfo.fromObject(object.payment);
            }
            if (object.consume != null) {
                if (typeof object.consume !== "object")
                    throw TypeError(".common.UserProfile.consume: object expected");
                message.consume = $root.common.CosumeInfo.fromObject(object.consume);
            }
            if (object.resource != null) {
                if (typeof object.resource !== "object")
                    throw TypeError(".common.UserProfile.resource: object expected");
                message.resource = $root.common.ResourceInfo.fromObject(object.resource);
            }
            if (object.heros) {
                if (typeof object.heros !== "object")
                    throw TypeError(".common.UserProfile.heros: object expected");
                message.heros = {};
                for (var keys = Object.keys(object.heros), i = 0; i < keys.length; ++i) {
                    if (typeof object.heros[keys[i]] !== "object")
                        throw TypeError(".common.UserProfile.heros: object expected");
                    message.heros[keys[i]] = $root.common.Hero.fromObject(object.heros[keys[i]]);
                }
            }
            if (object.heroTeams) {
                if (typeof object.heroTeams !== "object")
                    throw TypeError(".common.UserProfile.heroTeams: object expected");
                message.heroTeams = {};
                for (var keys = Object.keys(object.heroTeams), i = 0; i < keys.length; ++i) {
                    if (typeof object.heroTeams[keys[i]] !== "object")
                        throw TypeError(".common.UserProfile.heroTeams: object expected");
                    message.heroTeams[keys[i]] = $root.common.HeroTeam.fromObject(object.heroTeams[keys[i]]);
                }
            }
            if (object.curHeroTeam != null)
                message.curHeroTeam = object.curHeroTeam | 0;
            if (object.bag != null) {
                if (typeof object.bag !== "object")
                    throw TypeError(".common.UserProfile.bag: object expected");
                message.bag = $root.common.BagInfo.fromObject(object.bag);
            }
            if (object.shop != null) {
                if (typeof object.shop !== "object")
                    throw TypeError(".common.UserProfile.shop: object expected");
                message.shop = $root.common.ShopInfo.fromObject(object.shop);
            }
            if (object.mail) {
                if (typeof object.mail !== "object")
                    throw TypeError(".common.UserProfile.mail: object expected");
                message.mail = {};
                for (var keys = Object.keys(object.mail), i = 0; i < keys.length; ++i)
                    message.mail[keys[i]] = object.mail[keys[i]] | 0;
            }
            if (object.task != null) {
                if (typeof object.task !== "object")
                    throw TypeError(".common.UserProfile.task: object expected");
                message.task = $root.common.TaskInfo.fromObject(object.task);
            }
            if (object.box) {
                if (typeof object.box !== "object")
                    throw TypeError(".common.UserProfile.box: object expected");
                message.box = {};
                for (var keys = Object.keys(object.box), i = 0; i < keys.length; ++i) {
                    if (typeof object.box[keys[i]] !== "object")
                        throw TypeError(".common.UserProfile.box: object expected");
                    message.box[keys[i]] = $root.common.BoxInfo.fromObject(object.box[keys[i]]);
                }
            }
            if (object.drop) {
                if (typeof object.drop !== "object")
                    throw TypeError(".common.UserProfile.drop: object expected");
                message.drop = {};
                for (var keys = Object.keys(object.drop), i = 0; i < keys.length; ++i) {
                    if (typeof object.drop[keys[i]] !== "object")
                        throw TypeError(".common.UserProfile.drop: object expected");
                    message.drop[keys[i]] = $root.common.DropInfo.fromObject(object.drop[keys[i]]);
                }
            }
            if (object.arena != null) {
                if (typeof object.arena !== "object")
                    throw TypeError(".common.UserProfile.arena: object expected");
                message.arena = $root.common.ArenaInfo.fromObject(object.arena);
            }
            if (object.mark != null) {
                if (typeof object.mark !== "object")
                    throw TypeError(".common.UserProfile.mark: object expected");
                message.mark = $root.common.MarkInfo.fromObject(object.mark);
            }
            if (object.bind != null) {
                if (typeof object.bind !== "object")
                    throw TypeError(".common.UserProfile.bind: object expected");
                message.bind = $root.common.BindInfo.fromObject(object.bind);
            }
            if (object.hasAvatars) {
                if (!Array.isArray(object.hasAvatars))
                    throw TypeError(".common.UserProfile.hasAvatars: array expected");
                message.hasAvatars = [];
                for (var i = 0; i < object.hasAvatars.length; ++i)
                    message.hasAvatars[i] = object.hasAvatars[i] | 0;
            }
            if (object.adventure != null) {
                if (typeof object.adventure !== "object")
                    throw TypeError(".common.UserProfile.adventure: object expected");
                message.adventure = $root.common.AdventureInfo.fromObject(object.adventure);
            }
            if (object.events != null) {
                if (typeof object.events !== "object")
                    throw TypeError(".common.UserProfile.events: object expected");
                message.events = $root.common.EventInfo.fromObject(object.events);
            }
            if (object.workTable != null) {
                if (typeof object.workTable !== "object")
                    throw TypeError(".common.UserProfile.workTable: object expected");
                message.workTable = $root.common.WorkTableInfo.fromObject(object.workTable);
            }
            if (object.fairyTheme != null) {
                if (typeof object.fairyTheme !== "object")
                    throw TypeError(".common.UserProfile.fairyTheme: object expected");
                message.fairyTheme = $root.common.FairyThemeInfo.fromObject(object.fairyTheme);
            }
            if (object.guide != null) {
                if (typeof object.guide !== "object")
                    throw TypeError(".common.UserProfile.guide: object expected");
                message.guide = $root.common.GuideInfo.fromObject(object.guide);
            }
            if (object.unlockedSkins) {
                if (!Array.isArray(object.unlockedSkins))
                    throw TypeError(".common.UserProfile.unlockedSkins: array expected");
                message.unlockedSkins = [];
                for (var i = 0; i < object.unlockedSkins.length; ++i)
                    message.unlockedSkins[i] = object.unlockedSkins[i] | 0;
            }
            if (object.skinExpire) {
                if (typeof object.skinExpire !== "object")
                    throw TypeError(".common.UserProfile.skinExpire: object expected");
                message.skinExpire = {};
                for (var keys = Object.keys(object.skinExpire), i = 0; i < keys.length; ++i)
                    if ($util.Long)
                        (message.skinExpire[keys[i]] = $util.Long.fromValue(object.skinExpire[keys[i]])).unsigned = false;
                    else if (typeof object.skinExpire[keys[i]] === "string")
                        message.skinExpire[keys[i]] = parseInt(object.skinExpire[keys[i]], 10);
                    else if (typeof object.skinExpire[keys[i]] === "number")
                        message.skinExpire[keys[i]] = object.skinExpire[keys[i]];
                    else if (typeof object.skinExpire[keys[i]] === "object")
                        message.skinExpire[keys[i]] = new $util.LongBits(object.skinExpire[keys[i]].low >>> 0, object.skinExpire[keys[i]].high >>> 0).toNumber();
            }
            if (object.markedSaleIds) {
                if (!Array.isArray(object.markedSaleIds))
                    throw TypeError(".common.UserProfile.markedSaleIds: array expected");
                message.markedSaleIds = [];
                for (var i = 0; i < object.markedSaleIds.length; ++i)
                    message.markedSaleIds[i] = object.markedSaleIds[i] | 0;
            }
            if (object.markedGoods) {
                if (!Array.isArray(object.markedGoods))
                    throw TypeError(".common.UserProfile.markedGoods: array expected");
                message.markedGoods = [];
                for (var i = 0; i < object.markedGoods.length; ++i)
                    message.markedGoods[i] = String(object.markedGoods[i]);
            }
            if (object.diyFlags) {
                if (typeof object.diyFlags !== "object")
                    throw TypeError(".common.UserProfile.diyFlags: object expected");
                message.diyFlags = {};
                for (var keys = Object.keys(object.diyFlags), i = 0; i < keys.length; ++i)
                    message.diyFlags[keys[i]] = object.diyFlags[keys[i]] | 0;
            }
            if (object.customData != null)
                message.customData = String(object.customData);
            if (object.guild != null) {
                if (typeof object.guild !== "object")
                    throw TypeError(".common.UserProfile.guild: object expected");
                message.guild = $root.common.UserGuildInfo.fromObject(object.guild);
            }
            return message;
        };

        /**
         * Creates a plain object from a UserProfile message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.UserProfile
         * @static
         * @param {common.UserProfile} message UserProfile
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserProfile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.hasAvatars = [];
                object.unlockedSkins = [];
                object.markedSaleIds = [];
                object.markedGoods = [];
            }
            if (options.objects || options.defaults) {
                object.heros = {};
                object.heroTeams = {};
                object.mail = {};
                object.box = {};
                object.drop = {};
                object.skinExpire = {};
                object.diyFlags = {};
            }
            if (options.defaults) {
                object.uid = "";
                object.openId = "";
                object.info = null;
                object.login = null;
                object.level = 0;
                object.cups = 0;
                object.historyMaxCups = 0;
                object.adTimes = 0;
                object.guest = 0;
                object.payment = null;
                object.consume = null;
                object.resource = null;
                object.curHeroTeam = 0;
                object.bag = null;
                object.shop = null;
                object.task = null;
                object.arena = null;
                object.mark = null;
                object.bind = null;
                object.adventure = null;
                object.events = null;
                object.workTable = null;
                object.fairyTheme = null;
                object.guide = null;
                object.customData = "";
                object.guild = null;
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.openId != null && message.hasOwnProperty("openId"))
                object.openId = message.openId;
            if (message.info != null && message.hasOwnProperty("info"))
                object.info = $root.common.UserBaseInfo.toObject(message.info, options);
            if (message.login != null && message.hasOwnProperty("login"))
                object.login = $root.common.LoginInfo.toObject(message.login, options);
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.cups != null && message.hasOwnProperty("cups"))
                object.cups = message.cups;
            if (message.historyMaxCups != null && message.hasOwnProperty("historyMaxCups"))
                object.historyMaxCups = message.historyMaxCups;
            if (message.adTimes != null && message.hasOwnProperty("adTimes"))
                object.adTimes = message.adTimes;
            if (message.guest != null && message.hasOwnProperty("guest"))
                object.guest = message.guest;
            if (message.payment != null && message.hasOwnProperty("payment"))
                object.payment = $root.common.PaymentInfo.toObject(message.payment, options);
            if (message.consume != null && message.hasOwnProperty("consume"))
                object.consume = $root.common.CosumeInfo.toObject(message.consume, options);
            if (message.resource != null && message.hasOwnProperty("resource"))
                object.resource = $root.common.ResourceInfo.toObject(message.resource, options);
            var keys2;
            if (message.heros && (keys2 = Object.keys(message.heros)).length) {
                object.heros = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.heros[keys2[j]] = $root.common.Hero.toObject(message.heros[keys2[j]], options);
            }
            if (message.heroTeams && (keys2 = Object.keys(message.heroTeams)).length) {
                object.heroTeams = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.heroTeams[keys2[j]] = $root.common.HeroTeam.toObject(message.heroTeams[keys2[j]], options);
            }
            if (message.curHeroTeam != null && message.hasOwnProperty("curHeroTeam"))
                object.curHeroTeam = message.curHeroTeam;
            if (message.bag != null && message.hasOwnProperty("bag"))
                object.bag = $root.common.BagInfo.toObject(message.bag, options);
            if (message.shop != null && message.hasOwnProperty("shop"))
                object.shop = $root.common.ShopInfo.toObject(message.shop, options);
            if (message.mail && (keys2 = Object.keys(message.mail)).length) {
                object.mail = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.mail[keys2[j]] = message.mail[keys2[j]];
            }
            if (message.task != null && message.hasOwnProperty("task"))
                object.task = $root.common.TaskInfo.toObject(message.task, options);
            if (message.box && (keys2 = Object.keys(message.box)).length) {
                object.box = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.box[keys2[j]] = $root.common.BoxInfo.toObject(message.box[keys2[j]], options);
            }
            if (message.drop && (keys2 = Object.keys(message.drop)).length) {
                object.drop = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.drop[keys2[j]] = $root.common.DropInfo.toObject(message.drop[keys2[j]], options);
            }
            if (message.arena != null && message.hasOwnProperty("arena"))
                object.arena = $root.common.ArenaInfo.toObject(message.arena, options);
            if (message.mark != null && message.hasOwnProperty("mark"))
                object.mark = $root.common.MarkInfo.toObject(message.mark, options);
            if (message.bind != null && message.hasOwnProperty("bind"))
                object.bind = $root.common.BindInfo.toObject(message.bind, options);
            if (message.hasAvatars && message.hasAvatars.length) {
                object.hasAvatars = [];
                for (var j = 0; j < message.hasAvatars.length; ++j)
                    object.hasAvatars[j] = message.hasAvatars[j];
            }
            if (message.adventure != null && message.hasOwnProperty("adventure"))
                object.adventure = $root.common.AdventureInfo.toObject(message.adventure, options);
            if (message.events != null && message.hasOwnProperty("events"))
                object.events = $root.common.EventInfo.toObject(message.events, options);
            if (message.workTable != null && message.hasOwnProperty("workTable"))
                object.workTable = $root.common.WorkTableInfo.toObject(message.workTable, options);
            if (message.fairyTheme != null && message.hasOwnProperty("fairyTheme"))
                object.fairyTheme = $root.common.FairyThemeInfo.toObject(message.fairyTheme, options);
            if (message.guide != null && message.hasOwnProperty("guide"))
                object.guide = $root.common.GuideInfo.toObject(message.guide, options);
            if (message.unlockedSkins && message.unlockedSkins.length) {
                object.unlockedSkins = [];
                for (var j = 0; j < message.unlockedSkins.length; ++j)
                    object.unlockedSkins[j] = message.unlockedSkins[j];
            }
            if (message.skinExpire && (keys2 = Object.keys(message.skinExpire)).length) {
                object.skinExpire = {};
                for (var j = 0; j < keys2.length; ++j)
                    if (typeof message.skinExpire[keys2[j]] === "number")
                        object.skinExpire[keys2[j]] = options.longs === String ? String(message.skinExpire[keys2[j]]) : message.skinExpire[keys2[j]];
                    else
                        object.skinExpire[keys2[j]] = options.longs === String ? $util.Long.prototype.toString.call(message.skinExpire[keys2[j]]) : options.longs === Number ? new $util.LongBits(message.skinExpire[keys2[j]].low >>> 0, message.skinExpire[keys2[j]].high >>> 0).toNumber() : message.skinExpire[keys2[j]];
            }
            if (message.markedSaleIds && message.markedSaleIds.length) {
                object.markedSaleIds = [];
                for (var j = 0; j < message.markedSaleIds.length; ++j)
                    object.markedSaleIds[j] = message.markedSaleIds[j];
            }
            if (message.markedGoods && message.markedGoods.length) {
                object.markedGoods = [];
                for (var j = 0; j < message.markedGoods.length; ++j)
                    object.markedGoods[j] = message.markedGoods[j];
            }
            if (message.diyFlags && (keys2 = Object.keys(message.diyFlags)).length) {
                object.diyFlags = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.diyFlags[keys2[j]] = message.diyFlags[keys2[j]];
            }
            if (message.customData != null && message.hasOwnProperty("customData"))
                object.customData = message.customData;
            if (message.guild != null && message.hasOwnProperty("guild"))
                object.guild = $root.common.UserGuildInfo.toObject(message.guild, options);
            return object;
        };

        /**
         * Converts this UserProfile to JSON.
         * @function toJSON
         * @memberof common.UserProfile
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserProfile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserProfile
         * @function getTypeUrl
         * @memberof common.UserProfile
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserProfile.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.UserProfile";
        };

        return UserProfile;
    })();

    common.Reward = (function() {

        /**
         * Properties of a Reward.
         * @memberof common
         * @interface IReward
         * @property {string|null} [type] Reward type
         * @property {string|null} [id] Reward id
         * @property {number|Long|null} [count] Reward count
         * @property {boolean|null} [tradable] Reward tradable
         * @property {boolean|null} [isBoxAward] Reward isBoxAward
         * @property {number|null} [boxId] Reward boxId
         */

        /**
         * Constructs a new Reward.
         * @memberof common
         * @classdesc Represents a Reward.
         * @implements IReward
         * @constructor
         * @param {common.IReward=} [properties] Properties to set
         */
        function Reward(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Reward type.
         * @member {string} type
         * @memberof common.Reward
         * @instance
         */
        Reward.prototype.type = "";

        /**
         * Reward id.
         * @member {string} id
         * @memberof common.Reward
         * @instance
         */
        Reward.prototype.id = "";

        /**
         * Reward count.
         * @member {number|Long} count
         * @memberof common.Reward
         * @instance
         */
        Reward.prototype.count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Reward tradable.
         * @member {boolean} tradable
         * @memberof common.Reward
         * @instance
         */
        Reward.prototype.tradable = false;

        /**
         * Reward isBoxAward.
         * @member {boolean} isBoxAward
         * @memberof common.Reward
         * @instance
         */
        Reward.prototype.isBoxAward = false;

        /**
         * Reward boxId.
         * @member {number} boxId
         * @memberof common.Reward
         * @instance
         */
        Reward.prototype.boxId = 0;

        /**
         * Creates a new Reward instance using the specified properties.
         * @function create
         * @memberof common.Reward
         * @static
         * @param {common.IReward=} [properties] Properties to set
         * @returns {common.Reward} Reward instance
         */
        Reward.create = function create(properties) {
            return new Reward(properties);
        };

        /**
         * Encodes the specified Reward message. Does not implicitly {@link common.Reward.verify|verify} messages.
         * @function encode
         * @memberof common.Reward
         * @static
         * @param {common.IReward} message Reward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Reward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.type);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.count);
            if (message.tradable != null && Object.hasOwnProperty.call(message, "tradable"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.tradable);
            if (message.isBoxAward != null && Object.hasOwnProperty.call(message, "isBoxAward"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.isBoxAward);
            if (message.boxId != null && Object.hasOwnProperty.call(message, "boxId"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.boxId);
            return writer;
        };

        /**
         * Encodes the specified Reward message, length delimited. Does not implicitly {@link common.Reward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.Reward
         * @static
         * @param {common.IReward} message Reward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Reward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Reward message from the specified reader or buffer.
         * @function decode
         * @memberof common.Reward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.Reward} Reward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Reward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.Reward();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.type = reader.string();
                        break;
                    }
                case 2: {
                        message.id = reader.string();
                        break;
                    }
                case 3: {
                        message.count = reader.int64();
                        break;
                    }
                case 4: {
                        message.tradable = reader.bool();
                        break;
                    }
                case 5: {
                        message.isBoxAward = reader.bool();
                        break;
                    }
                case 6: {
                        message.boxId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Reward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.Reward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.Reward} Reward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Reward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Reward message.
         * @function verify
         * @memberof common.Reward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Reward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count) && !(message.count && $util.isInteger(message.count.low) && $util.isInteger(message.count.high)))
                    return "count: integer|Long expected";
            if (message.tradable != null && message.hasOwnProperty("tradable"))
                if (typeof message.tradable !== "boolean")
                    return "tradable: boolean expected";
            if (message.isBoxAward != null && message.hasOwnProperty("isBoxAward"))
                if (typeof message.isBoxAward !== "boolean")
                    return "isBoxAward: boolean expected";
            if (message.boxId != null && message.hasOwnProperty("boxId"))
                if (!$util.isInteger(message.boxId))
                    return "boxId: integer expected";
            return null;
        };

        /**
         * Creates a Reward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.Reward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.Reward} Reward
         */
        Reward.fromObject = function fromObject(object) {
            if (object instanceof $root.common.Reward)
                return object;
            var message = new $root.common.Reward();
            if (object.type != null)
                message.type = String(object.type);
            if (object.id != null)
                message.id = String(object.id);
            if (object.count != null)
                if ($util.Long)
                    (message.count = $util.Long.fromValue(object.count)).unsigned = false;
                else if (typeof object.count === "string")
                    message.count = parseInt(object.count, 10);
                else if (typeof object.count === "number")
                    message.count = object.count;
                else if (typeof object.count === "object")
                    message.count = new $util.LongBits(object.count.low >>> 0, object.count.high >>> 0).toNumber();
            if (object.tradable != null)
                message.tradable = Boolean(object.tradable);
            if (object.isBoxAward != null)
                message.isBoxAward = Boolean(object.isBoxAward);
            if (object.boxId != null)
                message.boxId = object.boxId | 0;
            return message;
        };

        /**
         * Creates a plain object from a Reward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.Reward
         * @static
         * @param {common.Reward} message Reward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Reward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.type = "";
                object.id = "";
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.count = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.count = options.longs === String ? "0" : 0;
                object.tradable = false;
                object.isBoxAward = false;
                object.boxId = 0;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.count != null && message.hasOwnProperty("count"))
                if (typeof message.count === "number")
                    object.count = options.longs === String ? String(message.count) : message.count;
                else
                    object.count = options.longs === String ? $util.Long.prototype.toString.call(message.count) : options.longs === Number ? new $util.LongBits(message.count.low >>> 0, message.count.high >>> 0).toNumber() : message.count;
            if (message.tradable != null && message.hasOwnProperty("tradable"))
                object.tradable = message.tradable;
            if (message.isBoxAward != null && message.hasOwnProperty("isBoxAward"))
                object.isBoxAward = message.isBoxAward;
            if (message.boxId != null && message.hasOwnProperty("boxId"))
                object.boxId = message.boxId;
            return object;
        };

        /**
         * Converts this Reward to JSON.
         * @function toJSON
         * @memberof common.Reward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Reward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Reward
         * @function getTypeUrl
         * @memberof common.Reward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Reward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.Reward";
        };

        return Reward;
    })();

    common.FriendGuildInfo = (function() {

        /**
         * Properties of a FriendGuildInfo.
         * @memberof common
         * @interface IFriendGuildInfo
         * @property {string|null} [gid] FriendGuildInfo gid
         * @property {string|null} [name] FriendGuildInfo name
         * @property {number|null} [flag] FriendGuildInfo flag
         * @property {string|null} [type] FriendGuildInfo type
         */

        /**
         * Constructs a new FriendGuildInfo.
         * @memberof common
         * @classdesc Represents a FriendGuildInfo.
         * @implements IFriendGuildInfo
         * @constructor
         * @param {common.IFriendGuildInfo=} [properties] Properties to set
         */
        function FriendGuildInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FriendGuildInfo gid.
         * @member {string} gid
         * @memberof common.FriendGuildInfo
         * @instance
         */
        FriendGuildInfo.prototype.gid = "";

        /**
         * FriendGuildInfo name.
         * @member {string} name
         * @memberof common.FriendGuildInfo
         * @instance
         */
        FriendGuildInfo.prototype.name = "";

        /**
         * FriendGuildInfo flag.
         * @member {number} flag
         * @memberof common.FriendGuildInfo
         * @instance
         */
        FriendGuildInfo.prototype.flag = 0;

        /**
         * FriendGuildInfo type.
         * @member {string} type
         * @memberof common.FriendGuildInfo
         * @instance
         */
        FriendGuildInfo.prototype.type = "";

        /**
         * Creates a new FriendGuildInfo instance using the specified properties.
         * @function create
         * @memberof common.FriendGuildInfo
         * @static
         * @param {common.IFriendGuildInfo=} [properties] Properties to set
         * @returns {common.FriendGuildInfo} FriendGuildInfo instance
         */
        FriendGuildInfo.create = function create(properties) {
            return new FriendGuildInfo(properties);
        };

        /**
         * Encodes the specified FriendGuildInfo message. Does not implicitly {@link common.FriendGuildInfo.verify|verify} messages.
         * @function encode
         * @memberof common.FriendGuildInfo
         * @static
         * @param {common.IFriendGuildInfo} message FriendGuildInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FriendGuildInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gid != null && Object.hasOwnProperty.call(message, "gid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.gid);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.flag);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.type);
            return writer;
        };

        /**
         * Encodes the specified FriendGuildInfo message, length delimited. Does not implicitly {@link common.FriendGuildInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.FriendGuildInfo
         * @static
         * @param {common.IFriendGuildInfo} message FriendGuildInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FriendGuildInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FriendGuildInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.FriendGuildInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.FriendGuildInfo} FriendGuildInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FriendGuildInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.FriendGuildInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.gid = reader.string();
                        break;
                    }
                case 2: {
                        message.name = reader.string();
                        break;
                    }
                case 3: {
                        message.flag = reader.int32();
                        break;
                    }
                case 4: {
                        message.type = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FriendGuildInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.FriendGuildInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.FriendGuildInfo} FriendGuildInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FriendGuildInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FriendGuildInfo message.
         * @function verify
         * @memberof common.FriendGuildInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FriendGuildInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.gid != null && message.hasOwnProperty("gid"))
                if (!$util.isString(message.gid))
                    return "gid: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.flag != null && message.hasOwnProperty("flag"))
                if (!$util.isInteger(message.flag))
                    return "flag: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            return null;
        };

        /**
         * Creates a FriendGuildInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.FriendGuildInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.FriendGuildInfo} FriendGuildInfo
         */
        FriendGuildInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.FriendGuildInfo)
                return object;
            var message = new $root.common.FriendGuildInfo();
            if (object.gid != null)
                message.gid = String(object.gid);
            if (object.name != null)
                message.name = String(object.name);
            if (object.flag != null)
                message.flag = object.flag | 0;
            if (object.type != null)
                message.type = String(object.type);
            return message;
        };

        /**
         * Creates a plain object from a FriendGuildInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.FriendGuildInfo
         * @static
         * @param {common.FriendGuildInfo} message FriendGuildInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FriendGuildInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.gid = "";
                object.name = "";
                object.flag = 0;
                object.type = "";
            }
            if (message.gid != null && message.hasOwnProperty("gid"))
                object.gid = message.gid;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.flag != null && message.hasOwnProperty("flag"))
                object.flag = message.flag;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        /**
         * Converts this FriendGuildInfo to JSON.
         * @function toJSON
         * @memberof common.FriendGuildInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FriendGuildInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FriendGuildInfo
         * @function getTypeUrl
         * @memberof common.FriendGuildInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FriendGuildInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.FriendGuildInfo";
        };

        return FriendGuildInfo;
    })();

    common.FriendUserInfo = (function() {

        /**
         * Properties of a FriendUserInfo.
         * @memberof common
         * @interface IFriendUserInfo
         * @property {string|null} [uid] FriendUserInfo uid
         * @property {string|null} [name] FriendUserInfo name
         * @property {string|null} [avatar] FriendUserInfo avatar
         * @property {string|null} [country] FriendUserInfo country
         * @property {number|null} [level] FriendUserInfo level
         * @property {number|null} [online] FriendUserInfo online
         * @property {common.IFriendGuildInfo|null} [guild] FriendUserInfo guild
         * @property {string|null} [extra] FriendUserInfo extra
         */

        /**
         * Constructs a new FriendUserInfo.
         * @memberof common
         * @classdesc Represents a FriendUserInfo.
         * @implements IFriendUserInfo
         * @constructor
         * @param {common.IFriendUserInfo=} [properties] Properties to set
         */
        function FriendUserInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FriendUserInfo uid.
         * @member {string} uid
         * @memberof common.FriendUserInfo
         * @instance
         */
        FriendUserInfo.prototype.uid = "";

        /**
         * FriendUserInfo name.
         * @member {string} name
         * @memberof common.FriendUserInfo
         * @instance
         */
        FriendUserInfo.prototype.name = "";

        /**
         * FriendUserInfo avatar.
         * @member {string} avatar
         * @memberof common.FriendUserInfo
         * @instance
         */
        FriendUserInfo.prototype.avatar = "";

        /**
         * FriendUserInfo country.
         * @member {string} country
         * @memberof common.FriendUserInfo
         * @instance
         */
        FriendUserInfo.prototype.country = "";

        /**
         * FriendUserInfo level.
         * @member {number} level
         * @memberof common.FriendUserInfo
         * @instance
         */
        FriendUserInfo.prototype.level = 0;

        /**
         * FriendUserInfo online.
         * @member {number} online
         * @memberof common.FriendUserInfo
         * @instance
         */
        FriendUserInfo.prototype.online = 0;

        /**
         * FriendUserInfo guild.
         * @member {common.IFriendGuildInfo|null|undefined} guild
         * @memberof common.FriendUserInfo
         * @instance
         */
        FriendUserInfo.prototype.guild = null;

        /**
         * FriendUserInfo extra.
         * @member {string} extra
         * @memberof common.FriendUserInfo
         * @instance
         */
        FriendUserInfo.prototype.extra = "";

        /**
         * Creates a new FriendUserInfo instance using the specified properties.
         * @function create
         * @memberof common.FriendUserInfo
         * @static
         * @param {common.IFriendUserInfo=} [properties] Properties to set
         * @returns {common.FriendUserInfo} FriendUserInfo instance
         */
        FriendUserInfo.create = function create(properties) {
            return new FriendUserInfo(properties);
        };

        /**
         * Encodes the specified FriendUserInfo message. Does not implicitly {@link common.FriendUserInfo.verify|verify} messages.
         * @function encode
         * @memberof common.FriendUserInfo
         * @static
         * @param {common.IFriendUserInfo} message FriendUserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FriendUserInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.avatar != null && Object.hasOwnProperty.call(message, "avatar"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.avatar);
            if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.country);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.level);
            if (message.online != null && Object.hasOwnProperty.call(message, "online"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.online);
            if (message.guild != null && Object.hasOwnProperty.call(message, "guild"))
                $root.common.FriendGuildInfo.encode(message.guild, writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.extra != null && Object.hasOwnProperty.call(message, "extra"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.extra);
            return writer;
        };

        /**
         * Encodes the specified FriendUserInfo message, length delimited. Does not implicitly {@link common.FriendUserInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.FriendUserInfo
         * @static
         * @param {common.IFriendUserInfo} message FriendUserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FriendUserInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FriendUserInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.FriendUserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.FriendUserInfo} FriendUserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FriendUserInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.FriendUserInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.name = reader.string();
                        break;
                    }
                case 3: {
                        message.avatar = reader.string();
                        break;
                    }
                case 4: {
                        message.country = reader.string();
                        break;
                    }
                case 5: {
                        message.level = reader.int32();
                        break;
                    }
                case 6: {
                        message.online = reader.int32();
                        break;
                    }
                case 7: {
                        message.guild = $root.common.FriendGuildInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 8: {
                        message.extra = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FriendUserInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.FriendUserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.FriendUserInfo} FriendUserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FriendUserInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FriendUserInfo message.
         * @function verify
         * @memberof common.FriendUserInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FriendUserInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                if (!$util.isString(message.avatar))
                    return "avatar: string expected";
            if (message.country != null && message.hasOwnProperty("country"))
                if (!$util.isString(message.country))
                    return "country: string expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.online != null && message.hasOwnProperty("online"))
                if (!$util.isInteger(message.online))
                    return "online: integer expected";
            if (message.guild != null && message.hasOwnProperty("guild")) {
                var error = $root.common.FriendGuildInfo.verify(message.guild);
                if (error)
                    return "guild." + error;
            }
            if (message.extra != null && message.hasOwnProperty("extra"))
                if (!$util.isString(message.extra))
                    return "extra: string expected";
            return null;
        };

        /**
         * Creates a FriendUserInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.FriendUserInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.FriendUserInfo} FriendUserInfo
         */
        FriendUserInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.FriendUserInfo)
                return object;
            var message = new $root.common.FriendUserInfo();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.name != null)
                message.name = String(object.name);
            if (object.avatar != null)
                message.avatar = String(object.avatar);
            if (object.country != null)
                message.country = String(object.country);
            if (object.level != null)
                message.level = object.level | 0;
            if (object.online != null)
                message.online = object.online | 0;
            if (object.guild != null) {
                if (typeof object.guild !== "object")
                    throw TypeError(".common.FriendUserInfo.guild: object expected");
                message.guild = $root.common.FriendGuildInfo.fromObject(object.guild);
            }
            if (object.extra != null)
                message.extra = String(object.extra);
            return message;
        };

        /**
         * Creates a plain object from a FriendUserInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.FriendUserInfo
         * @static
         * @param {common.FriendUserInfo} message FriendUserInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FriendUserInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.uid = "";
                object.name = "";
                object.avatar = "";
                object.country = "";
                object.level = 0;
                object.online = 0;
                object.guild = null;
                object.extra = "";
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                object.avatar = message.avatar;
            if (message.country != null && message.hasOwnProperty("country"))
                object.country = message.country;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.online != null && message.hasOwnProperty("online"))
                object.online = message.online;
            if (message.guild != null && message.hasOwnProperty("guild"))
                object.guild = $root.common.FriendGuildInfo.toObject(message.guild, options);
            if (message.extra != null && message.hasOwnProperty("extra"))
                object.extra = message.extra;
            return object;
        };

        /**
         * Converts this FriendUserInfo to JSON.
         * @function toJSON
         * @memberof common.FriendUserInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FriendUserInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FriendUserInfo
         * @function getTypeUrl
         * @memberof common.FriendUserInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FriendUserInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.FriendUserInfo";
        };

        return FriendUserInfo;
    })();

    common.ApplyInfo = (function() {

        /**
         * Properties of an ApplyInfo.
         * @memberof common
         * @interface IApplyInfo
         * @property {string|null} [uid] ApplyInfo uid
         * @property {string|null} [name] ApplyInfo name
         * @property {string|null} [avatar] ApplyInfo avatar
         * @property {string|null} [country] ApplyInfo country
         * @property {number|null} [level] ApplyInfo level
         * @property {common.IFriendGuildInfo|null} [guild] ApplyInfo guild
         * @property {string|null} [extra] ApplyInfo extra
         */

        /**
         * Constructs a new ApplyInfo.
         * @memberof common
         * @classdesc Represents an ApplyInfo.
         * @implements IApplyInfo
         * @constructor
         * @param {common.IApplyInfo=} [properties] Properties to set
         */
        function ApplyInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ApplyInfo uid.
         * @member {string} uid
         * @memberof common.ApplyInfo
         * @instance
         */
        ApplyInfo.prototype.uid = "";

        /**
         * ApplyInfo name.
         * @member {string} name
         * @memberof common.ApplyInfo
         * @instance
         */
        ApplyInfo.prototype.name = "";

        /**
         * ApplyInfo avatar.
         * @member {string} avatar
         * @memberof common.ApplyInfo
         * @instance
         */
        ApplyInfo.prototype.avatar = "";

        /**
         * ApplyInfo country.
         * @member {string} country
         * @memberof common.ApplyInfo
         * @instance
         */
        ApplyInfo.prototype.country = "";

        /**
         * ApplyInfo level.
         * @member {number} level
         * @memberof common.ApplyInfo
         * @instance
         */
        ApplyInfo.prototype.level = 0;

        /**
         * ApplyInfo guild.
         * @member {common.IFriendGuildInfo|null|undefined} guild
         * @memberof common.ApplyInfo
         * @instance
         */
        ApplyInfo.prototype.guild = null;

        /**
         * ApplyInfo extra.
         * @member {string} extra
         * @memberof common.ApplyInfo
         * @instance
         */
        ApplyInfo.prototype.extra = "";

        /**
         * Creates a new ApplyInfo instance using the specified properties.
         * @function create
         * @memberof common.ApplyInfo
         * @static
         * @param {common.IApplyInfo=} [properties] Properties to set
         * @returns {common.ApplyInfo} ApplyInfo instance
         */
        ApplyInfo.create = function create(properties) {
            return new ApplyInfo(properties);
        };

        /**
         * Encodes the specified ApplyInfo message. Does not implicitly {@link common.ApplyInfo.verify|verify} messages.
         * @function encode
         * @memberof common.ApplyInfo
         * @static
         * @param {common.IApplyInfo} message ApplyInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ApplyInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.avatar != null && Object.hasOwnProperty.call(message, "avatar"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.avatar);
            if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.country);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.level);
            if (message.guild != null && Object.hasOwnProperty.call(message, "guild"))
                $root.common.FriendGuildInfo.encode(message.guild, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.extra != null && Object.hasOwnProperty.call(message, "extra"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.extra);
            return writer;
        };

        /**
         * Encodes the specified ApplyInfo message, length delimited. Does not implicitly {@link common.ApplyInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.ApplyInfo
         * @static
         * @param {common.IApplyInfo} message ApplyInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ApplyInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ApplyInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.ApplyInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.ApplyInfo} ApplyInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ApplyInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.ApplyInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.name = reader.string();
                        break;
                    }
                case 3: {
                        message.avatar = reader.string();
                        break;
                    }
                case 4: {
                        message.country = reader.string();
                        break;
                    }
                case 5: {
                        message.level = reader.int32();
                        break;
                    }
                case 6: {
                        message.guild = $root.common.FriendGuildInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 7: {
                        message.extra = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ApplyInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.ApplyInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.ApplyInfo} ApplyInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ApplyInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ApplyInfo message.
         * @function verify
         * @memberof common.ApplyInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ApplyInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                if (!$util.isString(message.avatar))
                    return "avatar: string expected";
            if (message.country != null && message.hasOwnProperty("country"))
                if (!$util.isString(message.country))
                    return "country: string expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.guild != null && message.hasOwnProperty("guild")) {
                var error = $root.common.FriendGuildInfo.verify(message.guild);
                if (error)
                    return "guild." + error;
            }
            if (message.extra != null && message.hasOwnProperty("extra"))
                if (!$util.isString(message.extra))
                    return "extra: string expected";
            return null;
        };

        /**
         * Creates an ApplyInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.ApplyInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.ApplyInfo} ApplyInfo
         */
        ApplyInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.ApplyInfo)
                return object;
            var message = new $root.common.ApplyInfo();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.name != null)
                message.name = String(object.name);
            if (object.avatar != null)
                message.avatar = String(object.avatar);
            if (object.country != null)
                message.country = String(object.country);
            if (object.level != null)
                message.level = object.level | 0;
            if (object.guild != null) {
                if (typeof object.guild !== "object")
                    throw TypeError(".common.ApplyInfo.guild: object expected");
                message.guild = $root.common.FriendGuildInfo.fromObject(object.guild);
            }
            if (object.extra != null)
                message.extra = String(object.extra);
            return message;
        };

        /**
         * Creates a plain object from an ApplyInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.ApplyInfo
         * @static
         * @param {common.ApplyInfo} message ApplyInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ApplyInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.uid = "";
                object.name = "";
                object.avatar = "";
                object.country = "";
                object.level = 0;
                object.guild = null;
                object.extra = "";
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                object.avatar = message.avatar;
            if (message.country != null && message.hasOwnProperty("country"))
                object.country = message.country;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.guild != null && message.hasOwnProperty("guild"))
                object.guild = $root.common.FriendGuildInfo.toObject(message.guild, options);
            if (message.extra != null && message.hasOwnProperty("extra"))
                object.extra = message.extra;
            return object;
        };

        /**
         * Converts this ApplyInfo to JSON.
         * @function toJSON
         * @memberof common.ApplyInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ApplyInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ApplyInfo
         * @function getTypeUrl
         * @memberof common.ApplyInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ApplyInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.ApplyInfo";
        };

        return ApplyInfo;
    })();

    /**
     * KickPlayerReason enum.
     * @name common.KickPlayerReason
     * @enum {number}
     * @property {number} ClientLeave=0 ClientLeave value
     * @property {number} DuplicateLogin=1 DuplicateLogin value
     * @property {number} ServerShutdown=2 ServerShutdown value
     * @property {number} GMKick=3 GMKick value
     * @property {number} Banned=4 Banned value
     */
    common.KickPlayerReason = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "ClientLeave"] = 0;
        values[valuesById[1] = "DuplicateLogin"] = 1;
        values[valuesById[2] = "ServerShutdown"] = 2;
        values[valuesById[3] = "GMKick"] = 3;
        values[valuesById[4] = "Banned"] = 4;
        return values;
    })();

    /**
     * ServerType enum.
     * @name common.ServerType
     * @enum {number}
     * @property {number} ServerTypeUnknown=0 ServerTypeUnknown value
     * @property {number} ServerTypeGate=1 ServerTypeGate value
     * @property {number} ServerTypeGame=2 ServerTypeGame value
     * @property {number} ServerTypeGuild=3 ServerTypeGuild value
     */
    common.ServerType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "ServerTypeUnknown"] = 0;
        values[valuesById[1] = "ServerTypeGate"] = 1;
        values[valuesById[2] = "ServerTypeGame"] = 2;
        values[valuesById[3] = "ServerTypeGuild"] = 3;
        return values;
    })();

    /**
     * GuildEventType enum.
     * @name common.GuildEventType
     * @enum {number}
     * @property {number} GUILD_EVENT_UNKNOWN=0 GUILD_EVENT_UNKNOWN value
     * @property {number} GUILD_EVENT_CREATE=1 GUILD_EVENT_CREATE value
     * @property {number} GUILD_EVENT_JOIN=2 GUILD_EVENT_JOIN value
     * @property {number} GUILD_EVENT_LEAVE=3 GUILD_EVENT_LEAVE value
     * @property {number} GUILD_EVENT_KICK=4 GUILD_EVENT_KICK value
     * @property {number} GUILD_EVENT_MODIFY_NOTICE=5 GUILD_EVENT_MODIFY_NOTICE value
     * @property {number} GUILD_EVENT_MODIFY_FLAG=6 GUILD_EVENT_MODIFY_FLAG value
     * @property {number} GUILD_EVENT_MODIFY_NAME=7 GUILD_EVENT_MODIFY_NAME value
     * @property {number} GUILD_EVENT_TRANSFER=8 GUILD_EVENT_TRANSFER value
     */
    common.GuildEventType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "GUILD_EVENT_UNKNOWN"] = 0;
        values[valuesById[1] = "GUILD_EVENT_CREATE"] = 1;
        values[valuesById[2] = "GUILD_EVENT_JOIN"] = 2;
        values[valuesById[3] = "GUILD_EVENT_LEAVE"] = 3;
        values[valuesById[4] = "GUILD_EVENT_KICK"] = 4;
        values[valuesById[5] = "GUILD_EVENT_MODIFY_NOTICE"] = 5;
        values[valuesById[6] = "GUILD_EVENT_MODIFY_FLAG"] = 6;
        values[valuesById[7] = "GUILD_EVENT_MODIFY_NAME"] = 7;
        values[valuesById[8] = "GUILD_EVENT_TRANSFER"] = 8;
        return values;
    })();

    /**
     * GuildMemberType enum.
     * @name common.GuildMemberType
     * @enum {number}
     * @property {number} GUILD_ROLE_NORMAL=0 GUILD_ROLE_NORMAL value
     * @property {number} GUILD_ROLE_ELITE=1 GUILD_ROLE_ELITE value
     * @property {number} GUILD_ROLE_DEPUTY=2 GUILD_ROLE_DEPUTY value
     * @property {number} GUILD_ROLE_LEADER=3 GUILD_ROLE_LEADER value
     */
    common.GuildMemberType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "GUILD_ROLE_NORMAL"] = 0;
        values[valuesById[1] = "GUILD_ROLE_ELITE"] = 1;
        values[valuesById[2] = "GUILD_ROLE_DEPUTY"] = 2;
        values[valuesById[3] = "GUILD_ROLE_LEADER"] = 3;
        return values;
    })();

    /**
     * GuildPermissionType enum.
     * @name common.GuildPermissionType
     * @enum {number}
     * @property {number} GUILD_PERMISSION_UNKNOWN=0 GUILD_PERMISSION_UNKNOWN value
     * @property {number} GUILD_PERMISSION_HANDLE_APPLY=1 GUILD_PERMISSION_HANDLE_APPLY value
     * @property {number} GUILD_PERMISSION_LEAVE=2 GUILD_PERMISSION_LEAVE value
     * @property {number} GUILD_PERMISSION_OPEN_SETTING=3 GUILD_PERMISSION_OPEN_SETTING value
     * @property {number} GUILD_PERMISSION_MODIFY_NAME=4 GUILD_PERMISSION_MODIFY_NAME value
     * @property {number} GUILD_PERMISSION_MODIFY_FLAG=5 GUILD_PERMISSION_MODIFY_FLAG value
     * @property {number} GUILD_PERMISSION_MODIFY_NOTICE=6 GUILD_PERMISSION_MODIFY_NOTICE value
     * @property {number} GUILD_PERMISSION_SET_DUTY=7 GUILD_PERMISSION_SET_DUTY value
     * @property {number} GUILD_PERMISSION_TRANSFER_LEADER=8 GUILD_PERMISSION_TRANSFER_LEADER value
     */
    common.GuildPermissionType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "GUILD_PERMISSION_UNKNOWN"] = 0;
        values[valuesById[1] = "GUILD_PERMISSION_HANDLE_APPLY"] = 1;
        values[valuesById[2] = "GUILD_PERMISSION_LEAVE"] = 2;
        values[valuesById[3] = "GUILD_PERMISSION_OPEN_SETTING"] = 3;
        values[valuesById[4] = "GUILD_PERMISSION_MODIFY_NAME"] = 4;
        values[valuesById[5] = "GUILD_PERMISSION_MODIFY_FLAG"] = 5;
        values[valuesById[6] = "GUILD_PERMISSION_MODIFY_NOTICE"] = 6;
        values[valuesById[7] = "GUILD_PERMISSION_SET_DUTY"] = 7;
        values[valuesById[8] = "GUILD_PERMISSION_TRANSFER_LEADER"] = 8;
        return values;
    })();

    common.GuildUserInfo = (function() {

        /**
         * Properties of a GuildUserInfo.
         * @memberof common
         * @interface IGuildUserInfo
         * @property {string|null} [uid] GuildUserInfo uid
         * @property {string|null} [name] GuildUserInfo name
         * @property {number|null} [avatar] GuildUserInfo avatar
         * @property {string|null} [country] GuildUserInfo country
         * @property {number|null} [level] GuildUserInfo level
         * @property {number|null} [cups] GuildUserInfo cups
         */

        /**
         * Constructs a new GuildUserInfo.
         * @memberof common
         * @classdesc Represents a GuildUserInfo.
         * @implements IGuildUserInfo
         * @constructor
         * @param {common.IGuildUserInfo=} [properties] Properties to set
         */
        function GuildUserInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GuildUserInfo uid.
         * @member {string} uid
         * @memberof common.GuildUserInfo
         * @instance
         */
        GuildUserInfo.prototype.uid = "";

        /**
         * GuildUserInfo name.
         * @member {string} name
         * @memberof common.GuildUserInfo
         * @instance
         */
        GuildUserInfo.prototype.name = "";

        /**
         * GuildUserInfo avatar.
         * @member {number} avatar
         * @memberof common.GuildUserInfo
         * @instance
         */
        GuildUserInfo.prototype.avatar = 0;

        /**
         * GuildUserInfo country.
         * @member {string} country
         * @memberof common.GuildUserInfo
         * @instance
         */
        GuildUserInfo.prototype.country = "";

        /**
         * GuildUserInfo level.
         * @member {number} level
         * @memberof common.GuildUserInfo
         * @instance
         */
        GuildUserInfo.prototype.level = 0;

        /**
         * GuildUserInfo cups.
         * @member {number} cups
         * @memberof common.GuildUserInfo
         * @instance
         */
        GuildUserInfo.prototype.cups = 0;

        /**
         * Creates a new GuildUserInfo instance using the specified properties.
         * @function create
         * @memberof common.GuildUserInfo
         * @static
         * @param {common.IGuildUserInfo=} [properties] Properties to set
         * @returns {common.GuildUserInfo} GuildUserInfo instance
         */
        GuildUserInfo.create = function create(properties) {
            return new GuildUserInfo(properties);
        };

        /**
         * Encodes the specified GuildUserInfo message. Does not implicitly {@link common.GuildUserInfo.verify|verify} messages.
         * @function encode
         * @memberof common.GuildUserInfo
         * @static
         * @param {common.IGuildUserInfo} message GuildUserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuildUserInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.avatar != null && Object.hasOwnProperty.call(message, "avatar"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.avatar);
            if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.country);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.level);
            if (message.cups != null && Object.hasOwnProperty.call(message, "cups"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.cups);
            return writer;
        };

        /**
         * Encodes the specified GuildUserInfo message, length delimited. Does not implicitly {@link common.GuildUserInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.GuildUserInfo
         * @static
         * @param {common.IGuildUserInfo} message GuildUserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuildUserInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GuildUserInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.GuildUserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.GuildUserInfo} GuildUserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuildUserInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.GuildUserInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.name = reader.string();
                        break;
                    }
                case 3: {
                        message.avatar = reader.int32();
                        break;
                    }
                case 4: {
                        message.country = reader.string();
                        break;
                    }
                case 5: {
                        message.level = reader.int32();
                        break;
                    }
                case 6: {
                        message.cups = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GuildUserInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.GuildUserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.GuildUserInfo} GuildUserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuildUserInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GuildUserInfo message.
         * @function verify
         * @memberof common.GuildUserInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GuildUserInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                if (!$util.isInteger(message.avatar))
                    return "avatar: integer expected";
            if (message.country != null && message.hasOwnProperty("country"))
                if (!$util.isString(message.country))
                    return "country: string expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.cups != null && message.hasOwnProperty("cups"))
                if (!$util.isInteger(message.cups))
                    return "cups: integer expected";
            return null;
        };

        /**
         * Creates a GuildUserInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.GuildUserInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.GuildUserInfo} GuildUserInfo
         */
        GuildUserInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.GuildUserInfo)
                return object;
            var message = new $root.common.GuildUserInfo();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.name != null)
                message.name = String(object.name);
            if (object.avatar != null)
                message.avatar = object.avatar | 0;
            if (object.country != null)
                message.country = String(object.country);
            if (object.level != null)
                message.level = object.level | 0;
            if (object.cups != null)
                message.cups = object.cups | 0;
            return message;
        };

        /**
         * Creates a plain object from a GuildUserInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.GuildUserInfo
         * @static
         * @param {common.GuildUserInfo} message GuildUserInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GuildUserInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.uid = "";
                object.name = "";
                object.avatar = 0;
                object.country = "";
                object.level = 0;
                object.cups = 0;
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                object.avatar = message.avatar;
            if (message.country != null && message.hasOwnProperty("country"))
                object.country = message.country;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.cups != null && message.hasOwnProperty("cups"))
                object.cups = message.cups;
            return object;
        };

        /**
         * Converts this GuildUserInfo to JSON.
         * @function toJSON
         * @memberof common.GuildUserInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GuildUserInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GuildUserInfo
         * @function getTypeUrl
         * @memberof common.GuildUserInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GuildUserInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.GuildUserInfo";
        };

        return GuildUserInfo;
    })();

    common.GuildMemberInfo = (function() {

        /**
         * Properties of a GuildMemberInfo.
         * @memberof common
         * @interface IGuildMemberInfo
         * @property {string|null} [uid] GuildMemberInfo uid
         * @property {string|null} [name] GuildMemberInfo name
         * @property {string|null} [avatar] GuildMemberInfo avatar
         * @property {number|null} [level] GuildMemberInfo level
         * @property {string|null} [country] GuildMemberInfo country
         * @property {number|null} [cups] GuildMemberInfo cups
         * @property {common.GuildMemberType|null} [role] GuildMemberInfo role
         * @property {number|null} [joinTime] GuildMemberInfo joinTime
         * @property {number|null} [offlineTime] GuildMemberInfo offlineTime
         * @property {number|null} [weekContribute] GuildMemberInfo weekContribute
         * @property {string|null} [extra] GuildMemberInfo extra
         */

        /**
         * Constructs a new GuildMemberInfo.
         * @memberof common
         * @classdesc Represents a GuildMemberInfo.
         * @implements IGuildMemberInfo
         * @constructor
         * @param {common.IGuildMemberInfo=} [properties] Properties to set
         */
        function GuildMemberInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GuildMemberInfo uid.
         * @member {string} uid
         * @memberof common.GuildMemberInfo
         * @instance
         */
        GuildMemberInfo.prototype.uid = "";

        /**
         * GuildMemberInfo name.
         * @member {string} name
         * @memberof common.GuildMemberInfo
         * @instance
         */
        GuildMemberInfo.prototype.name = "";

        /**
         * GuildMemberInfo avatar.
         * @member {string} avatar
         * @memberof common.GuildMemberInfo
         * @instance
         */
        GuildMemberInfo.prototype.avatar = "";

        /**
         * GuildMemberInfo level.
         * @member {number} level
         * @memberof common.GuildMemberInfo
         * @instance
         */
        GuildMemberInfo.prototype.level = 0;

        /**
         * GuildMemberInfo country.
         * @member {string} country
         * @memberof common.GuildMemberInfo
         * @instance
         */
        GuildMemberInfo.prototype.country = "";

        /**
         * GuildMemberInfo cups.
         * @member {number} cups
         * @memberof common.GuildMemberInfo
         * @instance
         */
        GuildMemberInfo.prototype.cups = 0;

        /**
         * GuildMemberInfo role.
         * @member {common.GuildMemberType} role
         * @memberof common.GuildMemberInfo
         * @instance
         */
        GuildMemberInfo.prototype.role = 0;

        /**
         * GuildMemberInfo joinTime.
         * @member {number} joinTime
         * @memberof common.GuildMemberInfo
         * @instance
         */
        GuildMemberInfo.prototype.joinTime = 0;

        /**
         * GuildMemberInfo offlineTime.
         * @member {number} offlineTime
         * @memberof common.GuildMemberInfo
         * @instance
         */
        GuildMemberInfo.prototype.offlineTime = 0;

        /**
         * GuildMemberInfo weekContribute.
         * @member {number} weekContribute
         * @memberof common.GuildMemberInfo
         * @instance
         */
        GuildMemberInfo.prototype.weekContribute = 0;

        /**
         * GuildMemberInfo extra.
         * @member {string} extra
         * @memberof common.GuildMemberInfo
         * @instance
         */
        GuildMemberInfo.prototype.extra = "";

        /**
         * Creates a new GuildMemberInfo instance using the specified properties.
         * @function create
         * @memberof common.GuildMemberInfo
         * @static
         * @param {common.IGuildMemberInfo=} [properties] Properties to set
         * @returns {common.GuildMemberInfo} GuildMemberInfo instance
         */
        GuildMemberInfo.create = function create(properties) {
            return new GuildMemberInfo(properties);
        };

        /**
         * Encodes the specified GuildMemberInfo message. Does not implicitly {@link common.GuildMemberInfo.verify|verify} messages.
         * @function encode
         * @memberof common.GuildMemberInfo
         * @static
         * @param {common.IGuildMemberInfo} message GuildMemberInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuildMemberInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.avatar != null && Object.hasOwnProperty.call(message, "avatar"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.avatar);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.level);
            if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.country);
            if (message.cups != null && Object.hasOwnProperty.call(message, "cups"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.cups);
            if (message.role != null && Object.hasOwnProperty.call(message, "role"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.role);
            if (message.joinTime != null && Object.hasOwnProperty.call(message, "joinTime"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.joinTime);
            if (message.offlineTime != null && Object.hasOwnProperty.call(message, "offlineTime"))
                writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.offlineTime);
            if (message.weekContribute != null && Object.hasOwnProperty.call(message, "weekContribute"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.weekContribute);
            if (message.extra != null && Object.hasOwnProperty.call(message, "extra"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.extra);
            return writer;
        };

        /**
         * Encodes the specified GuildMemberInfo message, length delimited. Does not implicitly {@link common.GuildMemberInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.GuildMemberInfo
         * @static
         * @param {common.IGuildMemberInfo} message GuildMemberInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuildMemberInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GuildMemberInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.GuildMemberInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.GuildMemberInfo} GuildMemberInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuildMemberInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.GuildMemberInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.name = reader.string();
                        break;
                    }
                case 3: {
                        message.avatar = reader.string();
                        break;
                    }
                case 4: {
                        message.level = reader.int32();
                        break;
                    }
                case 5: {
                        message.country = reader.string();
                        break;
                    }
                case 6: {
                        message.cups = reader.int32();
                        break;
                    }
                case 7: {
                        message.role = reader.int32();
                        break;
                    }
                case 8: {
                        message.joinTime = reader.uint32();
                        break;
                    }
                case 9: {
                        message.offlineTime = reader.uint32();
                        break;
                    }
                case 10: {
                        message.weekContribute = reader.int32();
                        break;
                    }
                case 11: {
                        message.extra = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GuildMemberInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.GuildMemberInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.GuildMemberInfo} GuildMemberInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuildMemberInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GuildMemberInfo message.
         * @function verify
         * @memberof common.GuildMemberInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GuildMemberInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                if (!$util.isString(message.avatar))
                    return "avatar: string expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.country != null && message.hasOwnProperty("country"))
                if (!$util.isString(message.country))
                    return "country: string expected";
            if (message.cups != null && message.hasOwnProperty("cups"))
                if (!$util.isInteger(message.cups))
                    return "cups: integer expected";
            if (message.role != null && message.hasOwnProperty("role"))
                switch (message.role) {
                default:
                    return "role: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                    break;
                }
            if (message.joinTime != null && message.hasOwnProperty("joinTime"))
                if (!$util.isInteger(message.joinTime))
                    return "joinTime: integer expected";
            if (message.offlineTime != null && message.hasOwnProperty("offlineTime"))
                if (!$util.isInteger(message.offlineTime))
                    return "offlineTime: integer expected";
            if (message.weekContribute != null && message.hasOwnProperty("weekContribute"))
                if (!$util.isInteger(message.weekContribute))
                    return "weekContribute: integer expected";
            if (message.extra != null && message.hasOwnProperty("extra"))
                if (!$util.isString(message.extra))
                    return "extra: string expected";
            return null;
        };

        /**
         * Creates a GuildMemberInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.GuildMemberInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.GuildMemberInfo} GuildMemberInfo
         */
        GuildMemberInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.GuildMemberInfo)
                return object;
            var message = new $root.common.GuildMemberInfo();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.name != null)
                message.name = String(object.name);
            if (object.avatar != null)
                message.avatar = String(object.avatar);
            if (object.level != null)
                message.level = object.level | 0;
            if (object.country != null)
                message.country = String(object.country);
            if (object.cups != null)
                message.cups = object.cups | 0;
            switch (object.role) {
            default:
                if (typeof object.role === "number") {
                    message.role = object.role;
                    break;
                }
                break;
            case "GUILD_ROLE_NORMAL":
            case 0:
                message.role = 0;
                break;
            case "GUILD_ROLE_ELITE":
            case 1:
                message.role = 1;
                break;
            case "GUILD_ROLE_DEPUTY":
            case 2:
                message.role = 2;
                break;
            case "GUILD_ROLE_LEADER":
            case 3:
                message.role = 3;
                break;
            }
            if (object.joinTime != null)
                message.joinTime = object.joinTime >>> 0;
            if (object.offlineTime != null)
                message.offlineTime = object.offlineTime >>> 0;
            if (object.weekContribute != null)
                message.weekContribute = object.weekContribute | 0;
            if (object.extra != null)
                message.extra = String(object.extra);
            return message;
        };

        /**
         * Creates a plain object from a GuildMemberInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.GuildMemberInfo
         * @static
         * @param {common.GuildMemberInfo} message GuildMemberInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GuildMemberInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.uid = "";
                object.name = "";
                object.avatar = "";
                object.level = 0;
                object.country = "";
                object.cups = 0;
                object.role = options.enums === String ? "GUILD_ROLE_NORMAL" : 0;
                object.joinTime = 0;
                object.offlineTime = 0;
                object.weekContribute = 0;
                object.extra = "";
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                object.avatar = message.avatar;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.country != null && message.hasOwnProperty("country"))
                object.country = message.country;
            if (message.cups != null && message.hasOwnProperty("cups"))
                object.cups = message.cups;
            if (message.role != null && message.hasOwnProperty("role"))
                object.role = options.enums === String ? $root.common.GuildMemberType[message.role] === undefined ? message.role : $root.common.GuildMemberType[message.role] : message.role;
            if (message.joinTime != null && message.hasOwnProperty("joinTime"))
                object.joinTime = message.joinTime;
            if (message.offlineTime != null && message.hasOwnProperty("offlineTime"))
                object.offlineTime = message.offlineTime;
            if (message.weekContribute != null && message.hasOwnProperty("weekContribute"))
                object.weekContribute = message.weekContribute;
            if (message.extra != null && message.hasOwnProperty("extra"))
                object.extra = message.extra;
            return object;
        };

        /**
         * Converts this GuildMemberInfo to JSON.
         * @function toJSON
         * @memberof common.GuildMemberInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GuildMemberInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GuildMemberInfo
         * @function getTypeUrl
         * @memberof common.GuildMemberInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GuildMemberInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.GuildMemberInfo";
        };

        return GuildMemberInfo;
    })();

    common.GuildApplyInfo = (function() {

        /**
         * Properties of a GuildApplyInfo.
         * @memberof common
         * @interface IGuildApplyInfo
         * @property {string|null} [uid] GuildApplyInfo uid
         * @property {string|null} [name] GuildApplyInfo name
         * @property {number|null} [avatar] GuildApplyInfo avatar
         * @property {number|null} [level] GuildApplyInfo level
         * @property {string|null} [country] GuildApplyInfo country
         * @property {string|null} [msg] GuildApplyInfo msg
         * @property {number|null} [applyTime] GuildApplyInfo applyTime
         * @property {number|null} [cups] GuildApplyInfo cups
         */

        /**
         * Constructs a new GuildApplyInfo.
         * @memberof common
         * @classdesc Represents a GuildApplyInfo.
         * @implements IGuildApplyInfo
         * @constructor
         * @param {common.IGuildApplyInfo=} [properties] Properties to set
         */
        function GuildApplyInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GuildApplyInfo uid.
         * @member {string} uid
         * @memberof common.GuildApplyInfo
         * @instance
         */
        GuildApplyInfo.prototype.uid = "";

        /**
         * GuildApplyInfo name.
         * @member {string} name
         * @memberof common.GuildApplyInfo
         * @instance
         */
        GuildApplyInfo.prototype.name = "";

        /**
         * GuildApplyInfo avatar.
         * @member {number} avatar
         * @memberof common.GuildApplyInfo
         * @instance
         */
        GuildApplyInfo.prototype.avatar = 0;

        /**
         * GuildApplyInfo level.
         * @member {number} level
         * @memberof common.GuildApplyInfo
         * @instance
         */
        GuildApplyInfo.prototype.level = 0;

        /**
         * GuildApplyInfo country.
         * @member {string} country
         * @memberof common.GuildApplyInfo
         * @instance
         */
        GuildApplyInfo.prototype.country = "";

        /**
         * GuildApplyInfo msg.
         * @member {string} msg
         * @memberof common.GuildApplyInfo
         * @instance
         */
        GuildApplyInfo.prototype.msg = "";

        /**
         * GuildApplyInfo applyTime.
         * @member {number} applyTime
         * @memberof common.GuildApplyInfo
         * @instance
         */
        GuildApplyInfo.prototype.applyTime = 0;

        /**
         * GuildApplyInfo cups.
         * @member {number} cups
         * @memberof common.GuildApplyInfo
         * @instance
         */
        GuildApplyInfo.prototype.cups = 0;

        /**
         * Creates a new GuildApplyInfo instance using the specified properties.
         * @function create
         * @memberof common.GuildApplyInfo
         * @static
         * @param {common.IGuildApplyInfo=} [properties] Properties to set
         * @returns {common.GuildApplyInfo} GuildApplyInfo instance
         */
        GuildApplyInfo.create = function create(properties) {
            return new GuildApplyInfo(properties);
        };

        /**
         * Encodes the specified GuildApplyInfo message. Does not implicitly {@link common.GuildApplyInfo.verify|verify} messages.
         * @function encode
         * @memberof common.GuildApplyInfo
         * @static
         * @param {common.IGuildApplyInfo} message GuildApplyInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuildApplyInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.avatar != null && Object.hasOwnProperty.call(message, "avatar"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.avatar);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.level);
            if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.country);
            if (message.msg != null && Object.hasOwnProperty.call(message, "msg"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.msg);
            if (message.applyTime != null && Object.hasOwnProperty.call(message, "applyTime"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.applyTime);
            if (message.cups != null && Object.hasOwnProperty.call(message, "cups"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.cups);
            return writer;
        };

        /**
         * Encodes the specified GuildApplyInfo message, length delimited. Does not implicitly {@link common.GuildApplyInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.GuildApplyInfo
         * @static
         * @param {common.IGuildApplyInfo} message GuildApplyInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuildApplyInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GuildApplyInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.GuildApplyInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.GuildApplyInfo} GuildApplyInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuildApplyInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.GuildApplyInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.name = reader.string();
                        break;
                    }
                case 3: {
                        message.avatar = reader.int32();
                        break;
                    }
                case 4: {
                        message.level = reader.int32();
                        break;
                    }
                case 5: {
                        message.country = reader.string();
                        break;
                    }
                case 6: {
                        message.msg = reader.string();
                        break;
                    }
                case 7: {
                        message.applyTime = reader.uint32();
                        break;
                    }
                case 8: {
                        message.cups = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GuildApplyInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.GuildApplyInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.GuildApplyInfo} GuildApplyInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuildApplyInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GuildApplyInfo message.
         * @function verify
         * @memberof common.GuildApplyInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GuildApplyInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                if (!$util.isInteger(message.avatar))
                    return "avatar: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.country != null && message.hasOwnProperty("country"))
                if (!$util.isString(message.country))
                    return "country: string expected";
            if (message.msg != null && message.hasOwnProperty("msg"))
                if (!$util.isString(message.msg))
                    return "msg: string expected";
            if (message.applyTime != null && message.hasOwnProperty("applyTime"))
                if (!$util.isInteger(message.applyTime))
                    return "applyTime: integer expected";
            if (message.cups != null && message.hasOwnProperty("cups"))
                if (!$util.isInteger(message.cups))
                    return "cups: integer expected";
            return null;
        };

        /**
         * Creates a GuildApplyInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.GuildApplyInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.GuildApplyInfo} GuildApplyInfo
         */
        GuildApplyInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.GuildApplyInfo)
                return object;
            var message = new $root.common.GuildApplyInfo();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.name != null)
                message.name = String(object.name);
            if (object.avatar != null)
                message.avatar = object.avatar | 0;
            if (object.level != null)
                message.level = object.level | 0;
            if (object.country != null)
                message.country = String(object.country);
            if (object.msg != null)
                message.msg = String(object.msg);
            if (object.applyTime != null)
                message.applyTime = object.applyTime >>> 0;
            if (object.cups != null)
                message.cups = object.cups | 0;
            return message;
        };

        /**
         * Creates a plain object from a GuildApplyInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.GuildApplyInfo
         * @static
         * @param {common.GuildApplyInfo} message GuildApplyInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GuildApplyInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.uid = "";
                object.name = "";
                object.avatar = 0;
                object.level = 0;
                object.country = "";
                object.msg = "";
                object.applyTime = 0;
                object.cups = 0;
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                object.avatar = message.avatar;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.country != null && message.hasOwnProperty("country"))
                object.country = message.country;
            if (message.msg != null && message.hasOwnProperty("msg"))
                object.msg = message.msg;
            if (message.applyTime != null && message.hasOwnProperty("applyTime"))
                object.applyTime = message.applyTime;
            if (message.cups != null && message.hasOwnProperty("cups"))
                object.cups = message.cups;
            return object;
        };

        /**
         * Converts this GuildApplyInfo to JSON.
         * @function toJSON
         * @memberof common.GuildApplyInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GuildApplyInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GuildApplyInfo
         * @function getTypeUrl
         * @memberof common.GuildApplyInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GuildApplyInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.GuildApplyInfo";
        };

        return GuildApplyInfo;
    })();

    common.GuildSimpleInfo = (function() {

        /**
         * Properties of a GuildSimpleInfo.
         * @memberof common
         * @interface IGuildSimpleInfo
         * @property {string|null} [gid] GuildSimpleInfo gid
         * @property {string|null} [name] GuildSimpleInfo name
         * @property {number|null} [flag] GuildSimpleInfo flag
         * @property {string|null} [location] GuildSimpleInfo location
         * @property {number|null} [joinType] GuildSimpleInfo joinType
         * @property {number|null} [requireCups] GuildSimpleInfo requireCups
         */

        /**
         * Constructs a new GuildSimpleInfo.
         * @memberof common
         * @classdesc Represents a GuildSimpleInfo.
         * @implements IGuildSimpleInfo
         * @constructor
         * @param {common.IGuildSimpleInfo=} [properties] Properties to set
         */
        function GuildSimpleInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GuildSimpleInfo gid.
         * @member {string} gid
         * @memberof common.GuildSimpleInfo
         * @instance
         */
        GuildSimpleInfo.prototype.gid = "";

        /**
         * GuildSimpleInfo name.
         * @member {string} name
         * @memberof common.GuildSimpleInfo
         * @instance
         */
        GuildSimpleInfo.prototype.name = "";

        /**
         * GuildSimpleInfo flag.
         * @member {number} flag
         * @memberof common.GuildSimpleInfo
         * @instance
         */
        GuildSimpleInfo.prototype.flag = 0;

        /**
         * GuildSimpleInfo location.
         * @member {string} location
         * @memberof common.GuildSimpleInfo
         * @instance
         */
        GuildSimpleInfo.prototype.location = "";

        /**
         * GuildSimpleInfo joinType.
         * @member {number} joinType
         * @memberof common.GuildSimpleInfo
         * @instance
         */
        GuildSimpleInfo.prototype.joinType = 0;

        /**
         * GuildSimpleInfo requireCups.
         * @member {number} requireCups
         * @memberof common.GuildSimpleInfo
         * @instance
         */
        GuildSimpleInfo.prototype.requireCups = 0;

        /**
         * Creates a new GuildSimpleInfo instance using the specified properties.
         * @function create
         * @memberof common.GuildSimpleInfo
         * @static
         * @param {common.IGuildSimpleInfo=} [properties] Properties to set
         * @returns {common.GuildSimpleInfo} GuildSimpleInfo instance
         */
        GuildSimpleInfo.create = function create(properties) {
            return new GuildSimpleInfo(properties);
        };

        /**
         * Encodes the specified GuildSimpleInfo message. Does not implicitly {@link common.GuildSimpleInfo.verify|verify} messages.
         * @function encode
         * @memberof common.GuildSimpleInfo
         * @static
         * @param {common.IGuildSimpleInfo} message GuildSimpleInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuildSimpleInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gid != null && Object.hasOwnProperty.call(message, "gid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.gid);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.flag);
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.location);
            if (message.joinType != null && Object.hasOwnProperty.call(message, "joinType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.joinType);
            if (message.requireCups != null && Object.hasOwnProperty.call(message, "requireCups"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.requireCups);
            return writer;
        };

        /**
         * Encodes the specified GuildSimpleInfo message, length delimited. Does not implicitly {@link common.GuildSimpleInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.GuildSimpleInfo
         * @static
         * @param {common.IGuildSimpleInfo} message GuildSimpleInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuildSimpleInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GuildSimpleInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.GuildSimpleInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.GuildSimpleInfo} GuildSimpleInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuildSimpleInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.GuildSimpleInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.gid = reader.string();
                        break;
                    }
                case 2: {
                        message.name = reader.string();
                        break;
                    }
                case 3: {
                        message.flag = reader.uint32();
                        break;
                    }
                case 4: {
                        message.location = reader.string();
                        break;
                    }
                case 5: {
                        message.joinType = reader.int32();
                        break;
                    }
                case 6: {
                        message.requireCups = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GuildSimpleInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.GuildSimpleInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.GuildSimpleInfo} GuildSimpleInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuildSimpleInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GuildSimpleInfo message.
         * @function verify
         * @memberof common.GuildSimpleInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GuildSimpleInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.gid != null && message.hasOwnProperty("gid"))
                if (!$util.isString(message.gid))
                    return "gid: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.flag != null && message.hasOwnProperty("flag"))
                if (!$util.isInteger(message.flag))
                    return "flag: integer expected";
            if (message.location != null && message.hasOwnProperty("location"))
                if (!$util.isString(message.location))
                    return "location: string expected";
            if (message.joinType != null && message.hasOwnProperty("joinType"))
                if (!$util.isInteger(message.joinType))
                    return "joinType: integer expected";
            if (message.requireCups != null && message.hasOwnProperty("requireCups"))
                if (!$util.isInteger(message.requireCups))
                    return "requireCups: integer expected";
            return null;
        };

        /**
         * Creates a GuildSimpleInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.GuildSimpleInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.GuildSimpleInfo} GuildSimpleInfo
         */
        GuildSimpleInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.GuildSimpleInfo)
                return object;
            var message = new $root.common.GuildSimpleInfo();
            if (object.gid != null)
                message.gid = String(object.gid);
            if (object.name != null)
                message.name = String(object.name);
            if (object.flag != null)
                message.flag = object.flag >>> 0;
            if (object.location != null)
                message.location = String(object.location);
            if (object.joinType != null)
                message.joinType = object.joinType | 0;
            if (object.requireCups != null)
                message.requireCups = object.requireCups | 0;
            return message;
        };

        /**
         * Creates a plain object from a GuildSimpleInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.GuildSimpleInfo
         * @static
         * @param {common.GuildSimpleInfo} message GuildSimpleInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GuildSimpleInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.gid = "";
                object.name = "";
                object.flag = 0;
                object.location = "";
                object.joinType = 0;
                object.requireCups = 0;
            }
            if (message.gid != null && message.hasOwnProperty("gid"))
                object.gid = message.gid;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.flag != null && message.hasOwnProperty("flag"))
                object.flag = message.flag;
            if (message.location != null && message.hasOwnProperty("location"))
                object.location = message.location;
            if (message.joinType != null && message.hasOwnProperty("joinType"))
                object.joinType = message.joinType;
            if (message.requireCups != null && message.hasOwnProperty("requireCups"))
                object.requireCups = message.requireCups;
            return object;
        };

        /**
         * Converts this GuildSimpleInfo to JSON.
         * @function toJSON
         * @memberof common.GuildSimpleInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GuildSimpleInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GuildSimpleInfo
         * @function getTypeUrl
         * @memberof common.GuildSimpleInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GuildSimpleInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.GuildSimpleInfo";
        };

        return GuildSimpleInfo;
    })();

    common.GuildInfo = (function() {

        /**
         * Properties of a GuildInfo.
         * @memberof common
         * @interface IGuildInfo
         * @property {string|null} [gid] GuildInfo gid
         * @property {string|null} [name] GuildInfo name
         * @property {number|null} [flag] GuildInfo flag
         * @property {string|null} [location] GuildInfo location
         * @property {number|null} [joinType] GuildInfo joinType
         * @property {number|null} [requireCups] GuildInfo requireCups
         * @property {string|null} [notice] GuildInfo notice
         * @property {number|null} [weekContribute] GuildInfo weekContribute
         * @property {number|null} [totalCups] GuildInfo totalCups
         * @property {number|Long|null} [offlineTimestamp] GuildInfo offlineTimestamp
         * @property {string|null} [leaderId] GuildInfo leaderId
         * @property {Object.<string,common.IGuildMemberInfo>|null} [members] GuildInfo members
         */

        /**
         * Constructs a new GuildInfo.
         * @memberof common
         * @classdesc Represents a GuildInfo.
         * @implements IGuildInfo
         * @constructor
         * @param {common.IGuildInfo=} [properties] Properties to set
         */
        function GuildInfo(properties) {
            this.members = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GuildInfo gid.
         * @member {string} gid
         * @memberof common.GuildInfo
         * @instance
         */
        GuildInfo.prototype.gid = "";

        /**
         * GuildInfo name.
         * @member {string} name
         * @memberof common.GuildInfo
         * @instance
         */
        GuildInfo.prototype.name = "";

        /**
         * GuildInfo flag.
         * @member {number} flag
         * @memberof common.GuildInfo
         * @instance
         */
        GuildInfo.prototype.flag = 0;

        /**
         * GuildInfo location.
         * @member {string} location
         * @memberof common.GuildInfo
         * @instance
         */
        GuildInfo.prototype.location = "";

        /**
         * GuildInfo joinType.
         * @member {number} joinType
         * @memberof common.GuildInfo
         * @instance
         */
        GuildInfo.prototype.joinType = 0;

        /**
         * GuildInfo requireCups.
         * @member {number} requireCups
         * @memberof common.GuildInfo
         * @instance
         */
        GuildInfo.prototype.requireCups = 0;

        /**
         * GuildInfo notice.
         * @member {string} notice
         * @memberof common.GuildInfo
         * @instance
         */
        GuildInfo.prototype.notice = "";

        /**
         * GuildInfo weekContribute.
         * @member {number} weekContribute
         * @memberof common.GuildInfo
         * @instance
         */
        GuildInfo.prototype.weekContribute = 0;

        /**
         * GuildInfo totalCups.
         * @member {number} totalCups
         * @memberof common.GuildInfo
         * @instance
         */
        GuildInfo.prototype.totalCups = 0;

        /**
         * GuildInfo offlineTimestamp.
         * @member {number|Long} offlineTimestamp
         * @memberof common.GuildInfo
         * @instance
         */
        GuildInfo.prototype.offlineTimestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GuildInfo leaderId.
         * @member {string} leaderId
         * @memberof common.GuildInfo
         * @instance
         */
        GuildInfo.prototype.leaderId = "";

        /**
         * GuildInfo members.
         * @member {Object.<string,common.IGuildMemberInfo>} members
         * @memberof common.GuildInfo
         * @instance
         */
        GuildInfo.prototype.members = $util.emptyObject;

        /**
         * Creates a new GuildInfo instance using the specified properties.
         * @function create
         * @memberof common.GuildInfo
         * @static
         * @param {common.IGuildInfo=} [properties] Properties to set
         * @returns {common.GuildInfo} GuildInfo instance
         */
        GuildInfo.create = function create(properties) {
            return new GuildInfo(properties);
        };

        /**
         * Encodes the specified GuildInfo message. Does not implicitly {@link common.GuildInfo.verify|verify} messages.
         * @function encode
         * @memberof common.GuildInfo
         * @static
         * @param {common.IGuildInfo} message GuildInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuildInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gid != null && Object.hasOwnProperty.call(message, "gid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.gid);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.flag);
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.location);
            if (message.joinType != null && Object.hasOwnProperty.call(message, "joinType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.joinType);
            if (message.requireCups != null && Object.hasOwnProperty.call(message, "requireCups"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.requireCups);
            if (message.notice != null && Object.hasOwnProperty.call(message, "notice"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.notice);
            if (message.weekContribute != null && Object.hasOwnProperty.call(message, "weekContribute"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.weekContribute);
            if (message.totalCups != null && Object.hasOwnProperty.call(message, "totalCups"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.totalCups);
            if (message.offlineTimestamp != null && Object.hasOwnProperty.call(message, "offlineTimestamp"))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.offlineTimestamp);
            if (message.leaderId != null && Object.hasOwnProperty.call(message, "leaderId"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.leaderId);
            if (message.members != null && Object.hasOwnProperty.call(message, "members"))
                for (var keys = Object.keys(message.members), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 12, wireType 2 =*/98).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.common.GuildMemberInfo.encode(message.members[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            return writer;
        };

        /**
         * Encodes the specified GuildInfo message, length delimited. Does not implicitly {@link common.GuildInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.GuildInfo
         * @static
         * @param {common.IGuildInfo} message GuildInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuildInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GuildInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.GuildInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.GuildInfo} GuildInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuildInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.GuildInfo(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.gid = reader.string();
                        break;
                    }
                case 2: {
                        message.name = reader.string();
                        break;
                    }
                case 3: {
                        message.flag = reader.uint32();
                        break;
                    }
                case 4: {
                        message.location = reader.string();
                        break;
                    }
                case 5: {
                        message.joinType = reader.int32();
                        break;
                    }
                case 6: {
                        message.requireCups = reader.int32();
                        break;
                    }
                case 7: {
                        message.notice = reader.string();
                        break;
                    }
                case 8: {
                        message.weekContribute = reader.int32();
                        break;
                    }
                case 9: {
                        message.totalCups = reader.int32();
                        break;
                    }
                case 10: {
                        message.offlineTimestamp = reader.int64();
                        break;
                    }
                case 11: {
                        message.leaderId = reader.string();
                        break;
                    }
                case 12: {
                        if (message.members === $util.emptyObject)
                            message.members = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = $root.common.GuildMemberInfo.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.members[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GuildInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.GuildInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.GuildInfo} GuildInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuildInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GuildInfo message.
         * @function verify
         * @memberof common.GuildInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GuildInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.gid != null && message.hasOwnProperty("gid"))
                if (!$util.isString(message.gid))
                    return "gid: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.flag != null && message.hasOwnProperty("flag"))
                if (!$util.isInteger(message.flag))
                    return "flag: integer expected";
            if (message.location != null && message.hasOwnProperty("location"))
                if (!$util.isString(message.location))
                    return "location: string expected";
            if (message.joinType != null && message.hasOwnProperty("joinType"))
                if (!$util.isInteger(message.joinType))
                    return "joinType: integer expected";
            if (message.requireCups != null && message.hasOwnProperty("requireCups"))
                if (!$util.isInteger(message.requireCups))
                    return "requireCups: integer expected";
            if (message.notice != null && message.hasOwnProperty("notice"))
                if (!$util.isString(message.notice))
                    return "notice: string expected";
            if (message.weekContribute != null && message.hasOwnProperty("weekContribute"))
                if (!$util.isInteger(message.weekContribute))
                    return "weekContribute: integer expected";
            if (message.totalCups != null && message.hasOwnProperty("totalCups"))
                if (!$util.isInteger(message.totalCups))
                    return "totalCups: integer expected";
            if (message.offlineTimestamp != null && message.hasOwnProperty("offlineTimestamp"))
                if (!$util.isInteger(message.offlineTimestamp) && !(message.offlineTimestamp && $util.isInteger(message.offlineTimestamp.low) && $util.isInteger(message.offlineTimestamp.high)))
                    return "offlineTimestamp: integer|Long expected";
            if (message.leaderId != null && message.hasOwnProperty("leaderId"))
                if (!$util.isString(message.leaderId))
                    return "leaderId: string expected";
            if (message.members != null && message.hasOwnProperty("members")) {
                if (!$util.isObject(message.members))
                    return "members: object expected";
                var key = Object.keys(message.members);
                for (var i = 0; i < key.length; ++i) {
                    var error = $root.common.GuildMemberInfo.verify(message.members[key[i]]);
                    if (error)
                        return "members." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GuildInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.GuildInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.GuildInfo} GuildInfo
         */
        GuildInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.GuildInfo)
                return object;
            var message = new $root.common.GuildInfo();
            if (object.gid != null)
                message.gid = String(object.gid);
            if (object.name != null)
                message.name = String(object.name);
            if (object.flag != null)
                message.flag = object.flag >>> 0;
            if (object.location != null)
                message.location = String(object.location);
            if (object.joinType != null)
                message.joinType = object.joinType | 0;
            if (object.requireCups != null)
                message.requireCups = object.requireCups | 0;
            if (object.notice != null)
                message.notice = String(object.notice);
            if (object.weekContribute != null)
                message.weekContribute = object.weekContribute | 0;
            if (object.totalCups != null)
                message.totalCups = object.totalCups | 0;
            if (object.offlineTimestamp != null)
                if ($util.Long)
                    (message.offlineTimestamp = $util.Long.fromValue(object.offlineTimestamp)).unsigned = false;
                else if (typeof object.offlineTimestamp === "string")
                    message.offlineTimestamp = parseInt(object.offlineTimestamp, 10);
                else if (typeof object.offlineTimestamp === "number")
                    message.offlineTimestamp = object.offlineTimestamp;
                else if (typeof object.offlineTimestamp === "object")
                    message.offlineTimestamp = new $util.LongBits(object.offlineTimestamp.low >>> 0, object.offlineTimestamp.high >>> 0).toNumber();
            if (object.leaderId != null)
                message.leaderId = String(object.leaderId);
            if (object.members) {
                if (typeof object.members !== "object")
                    throw TypeError(".common.GuildInfo.members: object expected");
                message.members = {};
                for (var keys = Object.keys(object.members), i = 0; i < keys.length; ++i) {
                    if (typeof object.members[keys[i]] !== "object")
                        throw TypeError(".common.GuildInfo.members: object expected");
                    message.members[keys[i]] = $root.common.GuildMemberInfo.fromObject(object.members[keys[i]]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GuildInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.GuildInfo
         * @static
         * @param {common.GuildInfo} message GuildInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GuildInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.objects || options.defaults)
                object.members = {};
            if (options.defaults) {
                object.gid = "";
                object.name = "";
                object.flag = 0;
                object.location = "";
                object.joinType = 0;
                object.requireCups = 0;
                object.notice = "";
                object.weekContribute = 0;
                object.totalCups = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.offlineTimestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.offlineTimestamp = options.longs === String ? "0" : 0;
                object.leaderId = "";
            }
            if (message.gid != null && message.hasOwnProperty("gid"))
                object.gid = message.gid;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.flag != null && message.hasOwnProperty("flag"))
                object.flag = message.flag;
            if (message.location != null && message.hasOwnProperty("location"))
                object.location = message.location;
            if (message.joinType != null && message.hasOwnProperty("joinType"))
                object.joinType = message.joinType;
            if (message.requireCups != null && message.hasOwnProperty("requireCups"))
                object.requireCups = message.requireCups;
            if (message.notice != null && message.hasOwnProperty("notice"))
                object.notice = message.notice;
            if (message.weekContribute != null && message.hasOwnProperty("weekContribute"))
                object.weekContribute = message.weekContribute;
            if (message.totalCups != null && message.hasOwnProperty("totalCups"))
                object.totalCups = message.totalCups;
            if (message.offlineTimestamp != null && message.hasOwnProperty("offlineTimestamp"))
                if (typeof message.offlineTimestamp === "number")
                    object.offlineTimestamp = options.longs === String ? String(message.offlineTimestamp) : message.offlineTimestamp;
                else
                    object.offlineTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.offlineTimestamp) : options.longs === Number ? new $util.LongBits(message.offlineTimestamp.low >>> 0, message.offlineTimestamp.high >>> 0).toNumber() : message.offlineTimestamp;
            if (message.leaderId != null && message.hasOwnProperty("leaderId"))
                object.leaderId = message.leaderId;
            var keys2;
            if (message.members && (keys2 = Object.keys(message.members)).length) {
                object.members = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.members[keys2[j]] = $root.common.GuildMemberInfo.toObject(message.members[keys2[j]], options);
            }
            return object;
        };

        /**
         * Converts this GuildInfo to JSON.
         * @function toJSON
         * @memberof common.GuildInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GuildInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GuildInfo
         * @function getTypeUrl
         * @memberof common.GuildInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GuildInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.GuildInfo";
        };

        return GuildInfo;
    })();

    common.GuildLogInfo = (function() {

        /**
         * Properties of a GuildLogInfo.
         * @memberof common
         * @interface IGuildLogInfo
         * @property {number|null} [event] GuildLogInfo event
         * @property {Array.<string>|null} [params] GuildLogInfo params
         */

        /**
         * Constructs a new GuildLogInfo.
         * @memberof common
         * @classdesc Represents a GuildLogInfo.
         * @implements IGuildLogInfo
         * @constructor
         * @param {common.IGuildLogInfo=} [properties] Properties to set
         */
        function GuildLogInfo(properties) {
            this.params = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GuildLogInfo event.
         * @member {number} event
         * @memberof common.GuildLogInfo
         * @instance
         */
        GuildLogInfo.prototype.event = 0;

        /**
         * GuildLogInfo params.
         * @member {Array.<string>} params
         * @memberof common.GuildLogInfo
         * @instance
         */
        GuildLogInfo.prototype.params = $util.emptyArray;

        /**
         * Creates a new GuildLogInfo instance using the specified properties.
         * @function create
         * @memberof common.GuildLogInfo
         * @static
         * @param {common.IGuildLogInfo=} [properties] Properties to set
         * @returns {common.GuildLogInfo} GuildLogInfo instance
         */
        GuildLogInfo.create = function create(properties) {
            return new GuildLogInfo(properties);
        };

        /**
         * Encodes the specified GuildLogInfo message. Does not implicitly {@link common.GuildLogInfo.verify|verify} messages.
         * @function encode
         * @memberof common.GuildLogInfo
         * @static
         * @param {common.IGuildLogInfo} message GuildLogInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuildLogInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.event != null && Object.hasOwnProperty.call(message, "event"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.event);
            if (message.params != null && message.params.length)
                for (var i = 0; i < message.params.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.params[i]);
            return writer;
        };

        /**
         * Encodes the specified GuildLogInfo message, length delimited. Does not implicitly {@link common.GuildLogInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.GuildLogInfo
         * @static
         * @param {common.IGuildLogInfo} message GuildLogInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GuildLogInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GuildLogInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.GuildLogInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.GuildLogInfo} GuildLogInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuildLogInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.GuildLogInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.event = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.params && message.params.length))
                            message.params = [];
                        message.params.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GuildLogInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.GuildLogInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.GuildLogInfo} GuildLogInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GuildLogInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GuildLogInfo message.
         * @function verify
         * @memberof common.GuildLogInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GuildLogInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.event != null && message.hasOwnProperty("event"))
                if (!$util.isInteger(message.event))
                    return "event: integer expected";
            if (message.params != null && message.hasOwnProperty("params")) {
                if (!Array.isArray(message.params))
                    return "params: array expected";
                for (var i = 0; i < message.params.length; ++i)
                    if (!$util.isString(message.params[i]))
                        return "params: string[] expected";
            }
            return null;
        };

        /**
         * Creates a GuildLogInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.GuildLogInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.GuildLogInfo} GuildLogInfo
         */
        GuildLogInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.GuildLogInfo)
                return object;
            var message = new $root.common.GuildLogInfo();
            if (object.event != null)
                message.event = object.event | 0;
            if (object.params) {
                if (!Array.isArray(object.params))
                    throw TypeError(".common.GuildLogInfo.params: array expected");
                message.params = [];
                for (var i = 0; i < object.params.length; ++i)
                    message.params[i] = String(object.params[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GuildLogInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.GuildLogInfo
         * @static
         * @param {common.GuildLogInfo} message GuildLogInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GuildLogInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.params = [];
            if (options.defaults)
                object.event = 0;
            if (message.event != null && message.hasOwnProperty("event"))
                object.event = message.event;
            if (message.params && message.params.length) {
                object.params = [];
                for (var j = 0; j < message.params.length; ++j)
                    object.params[j] = message.params[j];
            }
            return object;
        };

        /**
         * Converts this GuildLogInfo to JSON.
         * @function toJSON
         * @memberof common.GuildLogInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GuildLogInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GuildLogInfo
         * @function getTypeUrl
         * @memberof common.GuildLogInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GuildLogInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.GuildLogInfo";
        };

        return GuildLogInfo;
    })();

    /**
     * BattleState enum.
     * @name common.BattleState
     * @enum {number}
     * @property {number} BATTLE_STATE_UNKNOWN=0 BATTLE_STATE_UNKNOWN value
     * @property {number} BATTLE_STATE_INIT=1 BATTLE_STATE_INIT value
     * @property {number} BATTLE_STATE_LOADING=2 BATTLE_STATE_LOADING value
     * @property {number} BATTLE_STATE_COUNT_DOWN=3 BATTLE_STATE_COUNT_DOWN value
     * @property {number} BATTLE_STATE_FIGHTING=4 BATTLE_STATE_FIGHTING value
     * @property {number} BATTLE_STATE_OVER=5 BATTLE_STATE_OVER value
     * @property {number} BATTLE_STATE_DELETE=6 BATTLE_STATE_DELETE value
     */
    common.BattleState = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "BATTLE_STATE_UNKNOWN"] = 0;
        values[valuesById[1] = "BATTLE_STATE_INIT"] = 1;
        values[valuesById[2] = "BATTLE_STATE_LOADING"] = 2;
        values[valuesById[3] = "BATTLE_STATE_COUNT_DOWN"] = 3;
        values[valuesById[4] = "BATTLE_STATE_FIGHTING"] = 4;
        values[valuesById[5] = "BATTLE_STATE_OVER"] = 5;
        values[valuesById[6] = "BATTLE_STATE_DELETE"] = 6;
        return values;
    })();

    /**
     * BattleFieldType enum.
     * @name common.BattleFieldType
     * @enum {number}
     * @property {number} BATTLE_FIELD_INVALID=0 BATTLE_FIELD_INVALID value
     * @property {number} BATTLE_FIELD_NORMAL=1 BATTLE_FIELD_NORMAL value
     * @property {number} BATTLE_FIELD_REPLAY=2 BATTLE_FIELD_REPLAY value
     * @property {number} BATTLE_FIELD_CHECK_RESULT=3 BATTLE_FIELD_CHECK_RESULT value
     */
    common.BattleFieldType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "BATTLE_FIELD_INVALID"] = 0;
        values[valuesById[1] = "BATTLE_FIELD_NORMAL"] = 1;
        values[valuesById[2] = "BATTLE_FIELD_REPLAY"] = 2;
        values[valuesById[3] = "BATTLE_FIELD_CHECK_RESULT"] = 3;
        return values;
    })();

    /**
     * BattleSide enum.
     * @name common.BattleSide
     * @enum {number}
     * @property {number} LEFT=0 LEFT value
     * @property {number} RIGHT=1 RIGHT value
     * @property {number} MAX=3 MAX value
     */
    common.BattleSide = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "LEFT"] = 0;
        values[valuesById[1] = "RIGHT"] = 1;
        values[valuesById[3] = "MAX"] = 3;
        return values;
    })();

    /**
     * RouteType enum.
     * @name common.RouteType
     * @enum {number}
     * @property {number} DOWN=0 DOWN value
     * @property {number} MIDDLE=1 MIDDLE value
     * @property {number} UP=2 UP value
     */
    common.RouteType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "DOWN"] = 0;
        values[valuesById[1] = "MIDDLE"] = 1;
        values[valuesById[2] = "UP"] = 2;
        return values;
    })();

    /**
     * HeroState enum.
     * @name common.HeroState
     * @enum {number}
     * @property {number} HERO_STATE_INIT=0 HERO_STATE_INIT value
     * @property {number} HERO_STATE_SEARCH=1 HERO_STATE_SEARCH value
     * @property {number} HERO_STATE_MOVE=2 HERO_STATE_MOVE value
     * @property {number} HERO_STATE_ATTACK=3 HERO_STATE_ATTACK value
     * @property {number} HERO_STATE_DELETE=4 HERO_STATE_DELETE value
     * @property {number} HERO_STATE_OVER=888 HERO_STATE_OVER value
     * @property {number} HERO_STATE_DEAD=999 HERO_STATE_DEAD value
     * @property {number} HERO_STATE_ERROR=1888 HERO_STATE_ERROR value
     */
    common.HeroState = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "HERO_STATE_INIT"] = 0;
        values[valuesById[1] = "HERO_STATE_SEARCH"] = 1;
        values[valuesById[2] = "HERO_STATE_MOVE"] = 2;
        values[valuesById[3] = "HERO_STATE_ATTACK"] = 3;
        values[valuesById[4] = "HERO_STATE_DELETE"] = 4;
        values[valuesById[888] = "HERO_STATE_OVER"] = 888;
        values[valuesById[999] = "HERO_STATE_DEAD"] = 999;
        values[valuesById[1888] = "HERO_STATE_ERROR"] = 1888;
        return values;
    })();

    /**
     * BulletState enum.
     * @name common.BulletState
     * @enum {number}
     * @property {number} BULLET_STATE_NORMAL=0 BULLET_STATE_NORMAL value
     * @property {number} BULLET_STATE_DEAD=1 BULLET_STATE_DEAD value
     * @property {number} BULLET_STATE_DELETE=2 BULLET_STATE_DELETE value
     */
    common.BulletState = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "BULLET_STATE_NORMAL"] = 0;
        values[valuesById[1] = "BULLET_STATE_DEAD"] = 1;
        values[valuesById[2] = "BULLET_STATE_DELETE"] = 2;
        return values;
    })();

    /**
     * TargetType enum.
     * @name common.TargetType
     * @enum {number}
     * @property {number} TARGET_TYPE_All=0 TARGET_TYPE_All value
     * @property {number} TARGET_TYPE_FRIEND=1 TARGET_TYPE_FRIEND value
     * @property {number} TARGET_TYPE_ENEMY=2 TARGET_TYPE_ENEMY value
     */
    common.TargetType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "TARGET_TYPE_All"] = 0;
        values[valuesById[1] = "TARGET_TYPE_FRIEND"] = 1;
        values[valuesById[2] = "TARGET_TYPE_ENEMY"] = 2;
        return values;
    })();

    /**
     * BuffEffectType enum.
     * @name common.BuffEffectType
     * @enum {number}
     * @property {number} EFFECT_TYPE_UNKNOWN=0 EFFECT_TYPE_UNKNOWN value
     * @property {number} BAN_MOVE=1 BAN_MOVE value
     * @property {number} BAN_ATTACK=2 BAN_ATTACK value
     * @property {number} IMMUNE_CONTROL=3 IMMUNE_CONTROL value
     * @property {number} IMMUNE_DAMAGE=4 IMMUNE_DAMAGE value
     * @property {number} INVINCIBLE=5 INVINCIBLE value
     */
    common.BuffEffectType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "EFFECT_TYPE_UNKNOWN"] = 0;
        values[valuesById[1] = "BAN_MOVE"] = 1;
        values[valuesById[2] = "BAN_ATTACK"] = 2;
        values[valuesById[3] = "IMMUNE_CONTROL"] = 3;
        values[valuesById[4] = "IMMUNE_DAMAGE"] = 4;
        values[valuesById[5] = "INVINCIBLE"] = 5;
        return values;
    })();

    /**
     * SkillFindTargetWay enum.
     * @name common.SkillFindTargetWay
     * @enum {number}
     * @property {number} SINGLE=0 SINGLE value
     */
    common.SkillFindTargetWay = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "SINGLE"] = 0;
        return values;
    })();

    /**
     * DamageAttrIndex enum.
     * @name common.DamageAttrIndex
     * @enum {number}
     * @property {number} Damage=0 Damage value
     * @property {number} Healing=1 Healing value
     * @property {number} CritPer=2 CritPer value
     * @property {number} BlockPer=3 BlockPer value
     * @property {number} DodgePer=4 DodgePer value
     * @property {number} CritDmgPer=5 CritDmgPer value
     * @property {number} SuckBloodPer=6 SuckBloodPer value
     */
    common.DamageAttrIndex = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Damage"] = 0;
        values[valuesById[1] = "Healing"] = 1;
        values[valuesById[2] = "CritPer"] = 2;
        values[valuesById[3] = "BlockPer"] = 3;
        values[valuesById[4] = "DodgePer"] = 4;
        values[valuesById[5] = "CritDmgPer"] = 5;
        values[valuesById[6] = "SuckBloodPer"] = 6;
        return values;
    })();

    /**
     * DamageType enum.
     * @name common.DamageType
     * @enum {number}
     * @property {number} NORMAL=0 NORMAL value
     * @property {number} DODGE=1 DODGE value
     * @property {number} BLOCK=2 BLOCK value
     * @property {number} CRIT=3 CRIT value
     * @property {number} IMMUNE=999 IMMUNE value
     */
    common.DamageType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "NORMAL"] = 0;
        values[valuesById[1] = "DODGE"] = 1;
        values[valuesById[2] = "BLOCK"] = 2;
        values[valuesById[3] = "CRIT"] = 3;
        values[valuesById[999] = "IMMUNE"] = 999;
        return values;
    })();

    /**
     * CardRemoveType enum.
     * @name common.CardRemoveType
     * @enum {number}
     * @property {number} REMOVE_TYPE_UNKNOWN=0 REMOVE_TYPE_UNKNOWN value
     * @property {number} REPLAY=1 REPLAY value
     * @property {number} COMMAND=2 COMMAND value
     * @property {number} DROP=3 DROP value
     */
    common.CardRemoveType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "REMOVE_TYPE_UNKNOWN"] = 0;
        values[valuesById[1] = "REPLAY"] = 1;
        values[valuesById[2] = "COMMAND"] = 2;
        values[valuesById[3] = "DROP"] = 3;
        return values;
    })();

    common.RouteInfo = (function() {

        /**
         * Properties of a RouteInfo.
         * @memberof common
         * @interface IRouteInfo
         * @property {number|null} [route] RouteInfo route
         * @property {number|null} [posX] RouteInfo posX
         * @property {number|null} [posY] RouteInfo posY
         */

        /**
         * Constructs a new RouteInfo.
         * @memberof common
         * @classdesc Represents a RouteInfo.
         * @implements IRouteInfo
         * @constructor
         * @param {common.IRouteInfo=} [properties] Properties to set
         */
        function RouteInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RouteInfo route.
         * @member {number} route
         * @memberof common.RouteInfo
         * @instance
         */
        RouteInfo.prototype.route = 0;

        /**
         * RouteInfo posX.
         * @member {number} posX
         * @memberof common.RouteInfo
         * @instance
         */
        RouteInfo.prototype.posX = 0;

        /**
         * RouteInfo posY.
         * @member {number} posY
         * @memberof common.RouteInfo
         * @instance
         */
        RouteInfo.prototype.posY = 0;

        /**
         * Creates a new RouteInfo instance using the specified properties.
         * @function create
         * @memberof common.RouteInfo
         * @static
         * @param {common.IRouteInfo=} [properties] Properties to set
         * @returns {common.RouteInfo} RouteInfo instance
         */
        RouteInfo.create = function create(properties) {
            return new RouteInfo(properties);
        };

        /**
         * Encodes the specified RouteInfo message. Does not implicitly {@link common.RouteInfo.verify|verify} messages.
         * @function encode
         * @memberof common.RouteInfo
         * @static
         * @param {common.IRouteInfo} message RouteInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RouteInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.route != null && Object.hasOwnProperty.call(message, "route"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.route);
            if (message.posX != null && Object.hasOwnProperty.call(message, "posX"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.posX);
            if (message.posY != null && Object.hasOwnProperty.call(message, "posY"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.posY);
            return writer;
        };

        /**
         * Encodes the specified RouteInfo message, length delimited. Does not implicitly {@link common.RouteInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.RouteInfo
         * @static
         * @param {common.IRouteInfo} message RouteInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RouteInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RouteInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.RouteInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.RouteInfo} RouteInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RouteInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.RouteInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.route = reader.int32();
                        break;
                    }
                case 2: {
                        message.posX = reader.int32();
                        break;
                    }
                case 3: {
                        message.posY = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RouteInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.RouteInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.RouteInfo} RouteInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RouteInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RouteInfo message.
         * @function verify
         * @memberof common.RouteInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RouteInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.route != null && message.hasOwnProperty("route"))
                if (!$util.isInteger(message.route))
                    return "route: integer expected";
            if (message.posX != null && message.hasOwnProperty("posX"))
                if (!$util.isInteger(message.posX))
                    return "posX: integer expected";
            if (message.posY != null && message.hasOwnProperty("posY"))
                if (!$util.isInteger(message.posY))
                    return "posY: integer expected";
            return null;
        };

        /**
         * Creates a RouteInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.RouteInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.RouteInfo} RouteInfo
         */
        RouteInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.RouteInfo)
                return object;
            var message = new $root.common.RouteInfo();
            if (object.route != null)
                message.route = object.route | 0;
            if (object.posX != null)
                message.posX = object.posX | 0;
            if (object.posY != null)
                message.posY = object.posY | 0;
            return message;
        };

        /**
         * Creates a plain object from a RouteInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.RouteInfo
         * @static
         * @param {common.RouteInfo} message RouteInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RouteInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.route = 0;
                object.posX = 0;
                object.posY = 0;
            }
            if (message.route != null && message.hasOwnProperty("route"))
                object.route = message.route;
            if (message.posX != null && message.hasOwnProperty("posX"))
                object.posX = message.posX;
            if (message.posY != null && message.hasOwnProperty("posY"))
                object.posY = message.posY;
            return object;
        };

        /**
         * Converts this RouteInfo to JSON.
         * @function toJSON
         * @memberof common.RouteInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RouteInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RouteInfo
         * @function getTypeUrl
         * @memberof common.RouteInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RouteInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.RouteInfo";
        };

        return RouteInfo;
    })();

    common.CommandRoute = (function() {

        /**
         * Properties of a CommandRoute.
         * @memberof common
         * @interface ICommandRoute
         * @property {number|null} [route] CommandRoute route
         * @property {number|null} [posX] CommandRoute posX
         * @property {number|null} [posY] CommandRoute posY
         * @property {boolean|null} [front] CommandRoute front
         */

        /**
         * Constructs a new CommandRoute.
         * @memberof common
         * @classdesc Represents a CommandRoute.
         * @implements ICommandRoute
         * @constructor
         * @param {common.ICommandRoute=} [properties] Properties to set
         */
        function CommandRoute(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CommandRoute route.
         * @member {number} route
         * @memberof common.CommandRoute
         * @instance
         */
        CommandRoute.prototype.route = 0;

        /**
         * CommandRoute posX.
         * @member {number} posX
         * @memberof common.CommandRoute
         * @instance
         */
        CommandRoute.prototype.posX = 0;

        /**
         * CommandRoute posY.
         * @member {number} posY
         * @memberof common.CommandRoute
         * @instance
         */
        CommandRoute.prototype.posY = 0;

        /**
         * CommandRoute front.
         * @member {boolean} front
         * @memberof common.CommandRoute
         * @instance
         */
        CommandRoute.prototype.front = false;

        /**
         * Creates a new CommandRoute instance using the specified properties.
         * @function create
         * @memberof common.CommandRoute
         * @static
         * @param {common.ICommandRoute=} [properties] Properties to set
         * @returns {common.CommandRoute} CommandRoute instance
         */
        CommandRoute.create = function create(properties) {
            return new CommandRoute(properties);
        };

        /**
         * Encodes the specified CommandRoute message. Does not implicitly {@link common.CommandRoute.verify|verify} messages.
         * @function encode
         * @memberof common.CommandRoute
         * @static
         * @param {common.ICommandRoute} message CommandRoute message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CommandRoute.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.route != null && Object.hasOwnProperty.call(message, "route"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.route);
            if (message.posX != null && Object.hasOwnProperty.call(message, "posX"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.posX);
            if (message.posY != null && Object.hasOwnProperty.call(message, "posY"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.posY);
            if (message.front != null && Object.hasOwnProperty.call(message, "front"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.front);
            return writer;
        };

        /**
         * Encodes the specified CommandRoute message, length delimited. Does not implicitly {@link common.CommandRoute.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.CommandRoute
         * @static
         * @param {common.ICommandRoute} message CommandRoute message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CommandRoute.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CommandRoute message from the specified reader or buffer.
         * @function decode
         * @memberof common.CommandRoute
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.CommandRoute} CommandRoute
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CommandRoute.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.CommandRoute();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.route = reader.int32();
                        break;
                    }
                case 2: {
                        message.posX = reader.int32();
                        break;
                    }
                case 3: {
                        message.posY = reader.int32();
                        break;
                    }
                case 4: {
                        message.front = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CommandRoute message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.CommandRoute
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.CommandRoute} CommandRoute
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CommandRoute.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CommandRoute message.
         * @function verify
         * @memberof common.CommandRoute
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CommandRoute.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.route != null && message.hasOwnProperty("route"))
                if (!$util.isInteger(message.route))
                    return "route: integer expected";
            if (message.posX != null && message.hasOwnProperty("posX"))
                if (!$util.isInteger(message.posX))
                    return "posX: integer expected";
            if (message.posY != null && message.hasOwnProperty("posY"))
                if (!$util.isInteger(message.posY))
                    return "posY: integer expected";
            if (message.front != null && message.hasOwnProperty("front"))
                if (typeof message.front !== "boolean")
                    return "front: boolean expected";
            return null;
        };

        /**
         * Creates a CommandRoute message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.CommandRoute
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.CommandRoute} CommandRoute
         */
        CommandRoute.fromObject = function fromObject(object) {
            if (object instanceof $root.common.CommandRoute)
                return object;
            var message = new $root.common.CommandRoute();
            if (object.route != null)
                message.route = object.route | 0;
            if (object.posX != null)
                message.posX = object.posX | 0;
            if (object.posY != null)
                message.posY = object.posY | 0;
            if (object.front != null)
                message.front = Boolean(object.front);
            return message;
        };

        /**
         * Creates a plain object from a CommandRoute message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.CommandRoute
         * @static
         * @param {common.CommandRoute} message CommandRoute
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CommandRoute.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.route = 0;
                object.posX = 0;
                object.posY = 0;
                object.front = false;
            }
            if (message.route != null && message.hasOwnProperty("route"))
                object.route = message.route;
            if (message.posX != null && message.hasOwnProperty("posX"))
                object.posX = message.posX;
            if (message.posY != null && message.hasOwnProperty("posY"))
                object.posY = message.posY;
            if (message.front != null && message.hasOwnProperty("front"))
                object.front = message.front;
            return object;
        };

        /**
         * Converts this CommandRoute to JSON.
         * @function toJSON
         * @memberof common.CommandRoute
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CommandRoute.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CommandRoute
         * @function getTypeUrl
         * @memberof common.CommandRoute
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CommandRoute.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.CommandRoute";
        };

        return CommandRoute;
    })();

    common.Command = (function() {

        /**
         * Properties of a Command.
         * @memberof common
         * @interface ICommand
         * @property {number|Long|null} [instId] Command instId
         * @property {number|null} [card] Command card
         * @property {Array.<common.ICommandRoute>|null} [routes] Command routes
         */

        /**
         * Constructs a new Command.
         * @memberof common
         * @classdesc Represents a Command.
         * @implements ICommand
         * @constructor
         * @param {common.ICommand=} [properties] Properties to set
         */
        function Command(properties) {
            this.routes = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Command instId.
         * @member {number|Long} instId
         * @memberof common.Command
         * @instance
         */
        Command.prototype.instId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Command card.
         * @member {number} card
         * @memberof common.Command
         * @instance
         */
        Command.prototype.card = 0;

        /**
         * Command routes.
         * @member {Array.<common.ICommandRoute>} routes
         * @memberof common.Command
         * @instance
         */
        Command.prototype.routes = $util.emptyArray;

        /**
         * Creates a new Command instance using the specified properties.
         * @function create
         * @memberof common.Command
         * @static
         * @param {common.ICommand=} [properties] Properties to set
         * @returns {common.Command} Command instance
         */
        Command.create = function create(properties) {
            return new Command(properties);
        };

        /**
         * Encodes the specified Command message. Does not implicitly {@link common.Command.verify|verify} messages.
         * @function encode
         * @memberof common.Command
         * @static
         * @param {common.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.instId != null && Object.hasOwnProperty.call(message, "instId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.instId);
            if (message.card != null && Object.hasOwnProperty.call(message, "card"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.card);
            if (message.routes != null && message.routes.length)
                for (var i = 0; i < message.routes.length; ++i)
                    $root.common.CommandRoute.encode(message.routes[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Command message, length delimited. Does not implicitly {@link common.Command.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.Command
         * @static
         * @param {common.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Command message from the specified reader or buffer.
         * @function decode
         * @memberof common.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.Command();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.instId = reader.int64();
                        break;
                    }
                case 2: {
                        message.card = reader.int32();
                        break;
                    }
                case 3: {
                        if (!(message.routes && message.routes.length))
                            message.routes = [];
                        message.routes.push($root.common.CommandRoute.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Command message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Command message.
         * @function verify
         * @memberof common.Command
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Command.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.instId != null && message.hasOwnProperty("instId"))
                if (!$util.isInteger(message.instId) && !(message.instId && $util.isInteger(message.instId.low) && $util.isInteger(message.instId.high)))
                    return "instId: integer|Long expected";
            if (message.card != null && message.hasOwnProperty("card"))
                if (!$util.isInteger(message.card))
                    return "card: integer expected";
            if (message.routes != null && message.hasOwnProperty("routes")) {
                if (!Array.isArray(message.routes))
                    return "routes: array expected";
                for (var i = 0; i < message.routes.length; ++i) {
                    var error = $root.common.CommandRoute.verify(message.routes[i]);
                    if (error)
                        return "routes." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Command message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.Command
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.Command} Command
         */
        Command.fromObject = function fromObject(object) {
            if (object instanceof $root.common.Command)
                return object;
            var message = new $root.common.Command();
            if (object.instId != null)
                if ($util.Long)
                    (message.instId = $util.Long.fromValue(object.instId)).unsigned = false;
                else if (typeof object.instId === "string")
                    message.instId = parseInt(object.instId, 10);
                else if (typeof object.instId === "number")
                    message.instId = object.instId;
                else if (typeof object.instId === "object")
                    message.instId = new $util.LongBits(object.instId.low >>> 0, object.instId.high >>> 0).toNumber();
            if (object.card != null)
                message.card = object.card | 0;
            if (object.routes) {
                if (!Array.isArray(object.routes))
                    throw TypeError(".common.Command.routes: array expected");
                message.routes = [];
                for (var i = 0; i < object.routes.length; ++i) {
                    if (typeof object.routes[i] !== "object")
                        throw TypeError(".common.Command.routes: object expected");
                    message.routes[i] = $root.common.CommandRoute.fromObject(object.routes[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Command message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.Command
         * @static
         * @param {common.Command} message Command
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Command.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.routes = [];
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.instId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.instId = options.longs === String ? "0" : 0;
                object.card = 0;
            }
            if (message.instId != null && message.hasOwnProperty("instId"))
                if (typeof message.instId === "number")
                    object.instId = options.longs === String ? String(message.instId) : message.instId;
                else
                    object.instId = options.longs === String ? $util.Long.prototype.toString.call(message.instId) : options.longs === Number ? new $util.LongBits(message.instId.low >>> 0, message.instId.high >>> 0).toNumber() : message.instId;
            if (message.card != null && message.hasOwnProperty("card"))
                object.card = message.card;
            if (message.routes && message.routes.length) {
                object.routes = [];
                for (var j = 0; j < message.routes.length; ++j)
                    object.routes[j] = $root.common.CommandRoute.toObject(message.routes[j], options);
            }
            return object;
        };

        /**
         * Converts this Command to JSON.
         * @function toJSON
         * @memberof common.Command
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Command.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Command
         * @function getTypeUrl
         * @memberof common.Command
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Command.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.Command";
        };

        return Command;
    })();

    common.RemoveCardCommand = (function() {

        /**
         * Properties of a RemoveCardCommand.
         * @memberof common
         * @interface IRemoveCardCommand
         * @property {number|null} [instId] RemoveCardCommand instId
         */

        /**
         * Constructs a new RemoveCardCommand.
         * @memberof common
         * @classdesc Represents a RemoveCardCommand.
         * @implements IRemoveCardCommand
         * @constructor
         * @param {common.IRemoveCardCommand=} [properties] Properties to set
         */
        function RemoveCardCommand(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RemoveCardCommand instId.
         * @member {number} instId
         * @memberof common.RemoveCardCommand
         * @instance
         */
        RemoveCardCommand.prototype.instId = 0;

        /**
         * Creates a new RemoveCardCommand instance using the specified properties.
         * @function create
         * @memberof common.RemoveCardCommand
         * @static
         * @param {common.IRemoveCardCommand=} [properties] Properties to set
         * @returns {common.RemoveCardCommand} RemoveCardCommand instance
         */
        RemoveCardCommand.create = function create(properties) {
            return new RemoveCardCommand(properties);
        };

        /**
         * Encodes the specified RemoveCardCommand message. Does not implicitly {@link common.RemoveCardCommand.verify|verify} messages.
         * @function encode
         * @memberof common.RemoveCardCommand
         * @static
         * @param {common.IRemoveCardCommand} message RemoveCardCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RemoveCardCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.instId != null && Object.hasOwnProperty.call(message, "instId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.instId);
            return writer;
        };

        /**
         * Encodes the specified RemoveCardCommand message, length delimited. Does not implicitly {@link common.RemoveCardCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.RemoveCardCommand
         * @static
         * @param {common.IRemoveCardCommand} message RemoveCardCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RemoveCardCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RemoveCardCommand message from the specified reader or buffer.
         * @function decode
         * @memberof common.RemoveCardCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.RemoveCardCommand} RemoveCardCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RemoveCardCommand.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.RemoveCardCommand();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.instId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RemoveCardCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.RemoveCardCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.RemoveCardCommand} RemoveCardCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RemoveCardCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RemoveCardCommand message.
         * @function verify
         * @memberof common.RemoveCardCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RemoveCardCommand.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.instId != null && message.hasOwnProperty("instId"))
                if (!$util.isInteger(message.instId))
                    return "instId: integer expected";
            return null;
        };

        /**
         * Creates a RemoveCardCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.RemoveCardCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.RemoveCardCommand} RemoveCardCommand
         */
        RemoveCardCommand.fromObject = function fromObject(object) {
            if (object instanceof $root.common.RemoveCardCommand)
                return object;
            var message = new $root.common.RemoveCardCommand();
            if (object.instId != null)
                message.instId = object.instId | 0;
            return message;
        };

        /**
         * Creates a plain object from a RemoveCardCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.RemoveCardCommand
         * @static
         * @param {common.RemoveCardCommand} message RemoveCardCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RemoveCardCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.instId = 0;
            if (message.instId != null && message.hasOwnProperty("instId"))
                object.instId = message.instId;
            return object;
        };

        /**
         * Converts this RemoveCardCommand to JSON.
         * @function toJSON
         * @memberof common.RemoveCardCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RemoveCardCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RemoveCardCommand
         * @function getTypeUrl
         * @memberof common.RemoveCardCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RemoveCardCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.RemoveCardCommand";
        };

        return RemoveCardCommand;
    })();

    common.Equip = (function() {

        /**
         * Properties of an Equip.
         * @memberof common
         * @interface IEquip
         * @property {number|Long|null} [id] Equip id
         */

        /**
         * Constructs a new Equip.
         * @memberof common
         * @classdesc Represents an Equip.
         * @implements IEquip
         * @constructor
         * @param {common.IEquip=} [properties] Properties to set
         */
        function Equip(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Equip id.
         * @member {number|Long} id
         * @memberof common.Equip
         * @instance
         */
        Equip.prototype.id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new Equip instance using the specified properties.
         * @function create
         * @memberof common.Equip
         * @static
         * @param {common.IEquip=} [properties] Properties to set
         * @returns {common.Equip} Equip instance
         */
        Equip.create = function create(properties) {
            return new Equip(properties);
        };

        /**
         * Encodes the specified Equip message. Does not implicitly {@link common.Equip.verify|verify} messages.
         * @function encode
         * @memberof common.Equip
         * @static
         * @param {common.IEquip} message Equip message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Equip.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.id);
            return writer;
        };

        /**
         * Encodes the specified Equip message, length delimited. Does not implicitly {@link common.Equip.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.Equip
         * @static
         * @param {common.IEquip} message Equip message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Equip.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Equip message from the specified reader or buffer.
         * @function decode
         * @memberof common.Equip
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.Equip} Equip
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Equip.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.Equip();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Equip message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.Equip
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.Equip} Equip
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Equip.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Equip message.
         * @function verify
         * @memberof common.Equip
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Equip.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id) && !(message.id && $util.isInteger(message.id.low) && $util.isInteger(message.id.high)))
                    return "id: integer|Long expected";
            return null;
        };

        /**
         * Creates an Equip message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.Equip
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.Equip} Equip
         */
        Equip.fromObject = function fromObject(object) {
            if (object instanceof $root.common.Equip)
                return object;
            var message = new $root.common.Equip();
            if (object.id != null)
                if ($util.Long)
                    (message.id = $util.Long.fromValue(object.id)).unsigned = false;
                else if (typeof object.id === "string")
                    message.id = parseInt(object.id, 10);
                else if (typeof object.id === "number")
                    message.id = object.id;
                else if (typeof object.id === "object")
                    message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an Equip message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.Equip
         * @static
         * @param {common.Equip} message Equip
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Equip.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.id = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.id = options.longs === String ? "0" : 0;
            if (message.id != null && message.hasOwnProperty("id"))
                if (typeof message.id === "number")
                    object.id = options.longs === String ? String(message.id) : message.id;
                else
                    object.id = options.longs === String ? $util.Long.prototype.toString.call(message.id) : options.longs === Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber() : message.id;
            return object;
        };

        /**
         * Converts this Equip to JSON.
         * @function toJSON
         * @memberof common.Equip
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Equip.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Equip
         * @function getTypeUrl
         * @memberof common.Equip
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Equip.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.Equip";
        };

        return Equip;
    })();

    common.Card = (function() {

        /**
         * Properties of a Card.
         * @memberof common
         * @interface ICard
         * @property {number|null} [id] Card id
         * @property {number|null} [level] Card level
         * @property {Array.<common.IEquip>|null} [equips] Card equips
         * @property {number|null} [modelId] Card modelId
         */

        /**
         * Constructs a new Card.
         * @memberof common
         * @classdesc Represents a Card.
         * @implements ICard
         * @constructor
         * @param {common.ICard=} [properties] Properties to set
         */
        function Card(properties) {
            this.equips = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Card id.
         * @member {number} id
         * @memberof common.Card
         * @instance
         */
        Card.prototype.id = 0;

        /**
         * Card level.
         * @member {number} level
         * @memberof common.Card
         * @instance
         */
        Card.prototype.level = 0;

        /**
         * Card equips.
         * @member {Array.<common.IEquip>} equips
         * @memberof common.Card
         * @instance
         */
        Card.prototype.equips = $util.emptyArray;

        /**
         * Card modelId.
         * @member {number} modelId
         * @memberof common.Card
         * @instance
         */
        Card.prototype.modelId = 0;

        /**
         * Creates a new Card instance using the specified properties.
         * @function create
         * @memberof common.Card
         * @static
         * @param {common.ICard=} [properties] Properties to set
         * @returns {common.Card} Card instance
         */
        Card.create = function create(properties) {
            return new Card(properties);
        };

        /**
         * Encodes the specified Card message. Does not implicitly {@link common.Card.verify|verify} messages.
         * @function encode
         * @memberof common.Card
         * @static
         * @param {common.ICard} message Card message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Card.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.level);
            if (message.equips != null && message.equips.length)
                for (var i = 0; i < message.equips.length; ++i)
                    $root.common.Equip.encode(message.equips[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.modelId != null && Object.hasOwnProperty.call(message, "modelId"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.modelId);
            return writer;
        };

        /**
         * Encodes the specified Card message, length delimited. Does not implicitly {@link common.Card.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.Card
         * @static
         * @param {common.ICard} message Card message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Card.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Card message from the specified reader or buffer.
         * @function decode
         * @memberof common.Card
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.Card} Card
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Card.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.Card();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.level = reader.int32();
                        break;
                    }
                case 3: {
                        if (!(message.equips && message.equips.length))
                            message.equips = [];
                        message.equips.push($root.common.Equip.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.modelId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Card message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.Card
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.Card} Card
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Card.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Card message.
         * @function verify
         * @memberof common.Card
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Card.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.equips != null && message.hasOwnProperty("equips")) {
                if (!Array.isArray(message.equips))
                    return "equips: array expected";
                for (var i = 0; i < message.equips.length; ++i) {
                    var error = $root.common.Equip.verify(message.equips[i]);
                    if (error)
                        return "equips." + error;
                }
            }
            if (message.modelId != null && message.hasOwnProperty("modelId"))
                if (!$util.isInteger(message.modelId))
                    return "modelId: integer expected";
            return null;
        };

        /**
         * Creates a Card message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.Card
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.Card} Card
         */
        Card.fromObject = function fromObject(object) {
            if (object instanceof $root.common.Card)
                return object;
            var message = new $root.common.Card();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.level != null)
                message.level = object.level | 0;
            if (object.equips) {
                if (!Array.isArray(object.equips))
                    throw TypeError(".common.Card.equips: array expected");
                message.equips = [];
                for (var i = 0; i < object.equips.length; ++i) {
                    if (typeof object.equips[i] !== "object")
                        throw TypeError(".common.Card.equips: object expected");
                    message.equips[i] = $root.common.Equip.fromObject(object.equips[i]);
                }
            }
            if (object.modelId != null)
                message.modelId = object.modelId | 0;
            return message;
        };

        /**
         * Creates a plain object from a Card message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.Card
         * @static
         * @param {common.Card} message Card
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Card.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.equips = [];
            if (options.defaults) {
                object.id = 0;
                object.level = 0;
                object.modelId = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.equips && message.equips.length) {
                object.equips = [];
                for (var j = 0; j < message.equips.length; ++j)
                    object.equips[j] = $root.common.Equip.toObject(message.equips[j], options);
            }
            if (message.modelId != null && message.hasOwnProperty("modelId"))
                object.modelId = message.modelId;
            return object;
        };

        /**
         * Converts this Card to JSON.
         * @function toJSON
         * @memberof common.Card
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Card.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Card
         * @function getTypeUrl
         * @memberof common.Card
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Card.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.Card";
        };

        return Card;
    })();

    common.HandCardInst = (function() {

        /**
         * Properties of a HandCardInst.
         * @memberof common
         * @interface IHandCardInst
         * @property {number|Long|null} [id] HandCardInst id
         * @property {number|null} [cardId] HandCardInst cardId
         */

        /**
         * Constructs a new HandCardInst.
         * @memberof common
         * @classdesc Represents a HandCardInst.
         * @implements IHandCardInst
         * @constructor
         * @param {common.IHandCardInst=} [properties] Properties to set
         */
        function HandCardInst(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HandCardInst id.
         * @member {number|Long} id
         * @memberof common.HandCardInst
         * @instance
         */
        HandCardInst.prototype.id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * HandCardInst cardId.
         * @member {number} cardId
         * @memberof common.HandCardInst
         * @instance
         */
        HandCardInst.prototype.cardId = 0;

        /**
         * Creates a new HandCardInst instance using the specified properties.
         * @function create
         * @memberof common.HandCardInst
         * @static
         * @param {common.IHandCardInst=} [properties] Properties to set
         * @returns {common.HandCardInst} HandCardInst instance
         */
        HandCardInst.create = function create(properties) {
            return new HandCardInst(properties);
        };

        /**
         * Encodes the specified HandCardInst message. Does not implicitly {@link common.HandCardInst.verify|verify} messages.
         * @function encode
         * @memberof common.HandCardInst
         * @static
         * @param {common.IHandCardInst} message HandCardInst message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HandCardInst.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.id);
            if (message.cardId != null && Object.hasOwnProperty.call(message, "cardId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.cardId);
            return writer;
        };

        /**
         * Encodes the specified HandCardInst message, length delimited. Does not implicitly {@link common.HandCardInst.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.HandCardInst
         * @static
         * @param {common.IHandCardInst} message HandCardInst message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HandCardInst.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a HandCardInst message from the specified reader or buffer.
         * @function decode
         * @memberof common.HandCardInst
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.HandCardInst} HandCardInst
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HandCardInst.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.HandCardInst();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int64();
                        break;
                    }
                case 2: {
                        message.cardId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a HandCardInst message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.HandCardInst
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.HandCardInst} HandCardInst
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HandCardInst.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HandCardInst message.
         * @function verify
         * @memberof common.HandCardInst
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HandCardInst.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id) && !(message.id && $util.isInteger(message.id.low) && $util.isInteger(message.id.high)))
                    return "id: integer|Long expected";
            if (message.cardId != null && message.hasOwnProperty("cardId"))
                if (!$util.isInteger(message.cardId))
                    return "cardId: integer expected";
            return null;
        };

        /**
         * Creates a HandCardInst message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.HandCardInst
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.HandCardInst} HandCardInst
         */
        HandCardInst.fromObject = function fromObject(object) {
            if (object instanceof $root.common.HandCardInst)
                return object;
            var message = new $root.common.HandCardInst();
            if (object.id != null)
                if ($util.Long)
                    (message.id = $util.Long.fromValue(object.id)).unsigned = false;
                else if (typeof object.id === "string")
                    message.id = parseInt(object.id, 10);
                else if (typeof object.id === "number")
                    message.id = object.id;
                else if (typeof object.id === "object")
                    message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber();
            if (object.cardId != null)
                message.cardId = object.cardId | 0;
            return message;
        };

        /**
         * Creates a plain object from a HandCardInst message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.HandCardInst
         * @static
         * @param {common.HandCardInst} message HandCardInst
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HandCardInst.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.id = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.id = options.longs === String ? "0" : 0;
                object.cardId = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                if (typeof message.id === "number")
                    object.id = options.longs === String ? String(message.id) : message.id;
                else
                    object.id = options.longs === String ? $util.Long.prototype.toString.call(message.id) : options.longs === Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber() : message.id;
            if (message.cardId != null && message.hasOwnProperty("cardId"))
                object.cardId = message.cardId;
            return object;
        };

        /**
         * Converts this HandCardInst to JSON.
         * @function toJSON
         * @memberof common.HandCardInst
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HandCardInst.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HandCardInst
         * @function getTypeUrl
         * @memberof common.HandCardInst
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HandCardInst.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.HandCardInst";
        };

        return HandCardInst;
    })();

    common.BattlePlayerData = (function() {

        /**
         * Properties of a BattlePlayerData.
         * @memberof common
         * @interface IBattlePlayerData
         * @property {string|null} [uid] BattlePlayerData uid
         * @property {string|null} [nick] BattlePlayerData nick
         * @property {number|null} [avatar] BattlePlayerData avatar
         * @property {number|null} [level] BattlePlayerData level
         * @property {string|null} [country] BattlePlayerData country
         * @property {number|null} [cup] BattlePlayerData cup
         * @property {string|null} [guildName] BattlePlayerData guildName
         * @property {number|Long|null} [guildFlag] BattlePlayerData guildFlag
         * @property {Array.<common.ICard>|null} [cards] BattlePlayerData cards
         * @property {Array.<common.IHandCardInst>|null} [initCards] BattlePlayerData initCards
         * @property {number|null} [initEnergy] BattlePlayerData initEnergy
         * @property {number|null} [nextFrame] BattlePlayerData nextFrame
         * @property {number|null} [cardNum] BattlePlayerData cardNum
         * @property {number|null} [dealTimes] BattlePlayerData dealTimes
         */

        /**
         * Constructs a new BattlePlayerData.
         * @memberof common
         * @classdesc Represents a BattlePlayerData.
         * @implements IBattlePlayerData
         * @constructor
         * @param {common.IBattlePlayerData=} [properties] Properties to set
         */
        function BattlePlayerData(properties) {
            this.cards = [];
            this.initCards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BattlePlayerData uid.
         * @member {string} uid
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.uid = "";

        /**
         * BattlePlayerData nick.
         * @member {string} nick
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.nick = "";

        /**
         * BattlePlayerData avatar.
         * @member {number} avatar
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.avatar = 0;

        /**
         * BattlePlayerData level.
         * @member {number} level
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.level = 0;

        /**
         * BattlePlayerData country.
         * @member {string} country
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.country = "";

        /**
         * BattlePlayerData cup.
         * @member {number} cup
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.cup = 0;

        /**
         * BattlePlayerData guildName.
         * @member {string} guildName
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.guildName = "";

        /**
         * BattlePlayerData guildFlag.
         * @member {number|Long} guildFlag
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.guildFlag = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * BattlePlayerData cards.
         * @member {Array.<common.ICard>} cards
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.cards = $util.emptyArray;

        /**
         * BattlePlayerData initCards.
         * @member {Array.<common.IHandCardInst>} initCards
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.initCards = $util.emptyArray;

        /**
         * BattlePlayerData initEnergy.
         * @member {number} initEnergy
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.initEnergy = 0;

        /**
         * BattlePlayerData nextFrame.
         * @member {number} nextFrame
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.nextFrame = 0;

        /**
         * BattlePlayerData cardNum.
         * @member {number} cardNum
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.cardNum = 0;

        /**
         * BattlePlayerData dealTimes.
         * @member {number} dealTimes
         * @memberof common.BattlePlayerData
         * @instance
         */
        BattlePlayerData.prototype.dealTimes = 0;

        /**
         * Creates a new BattlePlayerData instance using the specified properties.
         * @function create
         * @memberof common.BattlePlayerData
         * @static
         * @param {common.IBattlePlayerData=} [properties] Properties to set
         * @returns {common.BattlePlayerData} BattlePlayerData instance
         */
        BattlePlayerData.create = function create(properties) {
            return new BattlePlayerData(properties);
        };

        /**
         * Encodes the specified BattlePlayerData message. Does not implicitly {@link common.BattlePlayerData.verify|verify} messages.
         * @function encode
         * @memberof common.BattlePlayerData
         * @static
         * @param {common.IBattlePlayerData} message BattlePlayerData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BattlePlayerData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.nick != null && Object.hasOwnProperty.call(message, "nick"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.nick);
            if (message.avatar != null && Object.hasOwnProperty.call(message, "avatar"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.avatar);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.level);
            if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.country);
            if (message.cup != null && Object.hasOwnProperty.call(message, "cup"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.cup);
            if (message.guildName != null && Object.hasOwnProperty.call(message, "guildName"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.guildName);
            if (message.guildFlag != null && Object.hasOwnProperty.call(message, "guildFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.guildFlag);
            if (message.cards != null && message.cards.length)
                for (var i = 0; i < message.cards.length; ++i)
                    $root.common.Card.encode(message.cards[i], writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            if (message.initCards != null && message.initCards.length)
                for (var i = 0; i < message.initCards.length; ++i)
                    $root.common.HandCardInst.encode(message.initCards[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.initEnergy != null && Object.hasOwnProperty.call(message, "initEnergy"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.initEnergy);
            if (message.nextFrame != null && Object.hasOwnProperty.call(message, "nextFrame"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.nextFrame);
            if (message.cardNum != null && Object.hasOwnProperty.call(message, "cardNum"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.cardNum);
            if (message.dealTimes != null && Object.hasOwnProperty.call(message, "dealTimes"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.dealTimes);
            return writer;
        };

        /**
         * Encodes the specified BattlePlayerData message, length delimited. Does not implicitly {@link common.BattlePlayerData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.BattlePlayerData
         * @static
         * @param {common.IBattlePlayerData} message BattlePlayerData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BattlePlayerData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BattlePlayerData message from the specified reader or buffer.
         * @function decode
         * @memberof common.BattlePlayerData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.BattlePlayerData} BattlePlayerData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BattlePlayerData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.BattlePlayerData();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.nick = reader.string();
                        break;
                    }
                case 3: {
                        message.avatar = reader.int32();
                        break;
                    }
                case 4: {
                        message.level = reader.int32();
                        break;
                    }
                case 5: {
                        message.country = reader.string();
                        break;
                    }
                case 6: {
                        message.cup = reader.int32();
                        break;
                    }
                case 7: {
                        message.guildName = reader.string();
                        break;
                    }
                case 8: {
                        message.guildFlag = reader.int64();
                        break;
                    }
                case 9: {
                        if (!(message.cards && message.cards.length))
                            message.cards = [];
                        message.cards.push($root.common.Card.decode(reader, reader.uint32()));
                        break;
                    }
                case 10: {
                        if (!(message.initCards && message.initCards.length))
                            message.initCards = [];
                        message.initCards.push($root.common.HandCardInst.decode(reader, reader.uint32()));
                        break;
                    }
                case 11: {
                        message.initEnergy = reader.int32();
                        break;
                    }
                case 12: {
                        message.nextFrame = reader.int32();
                        break;
                    }
                case 13: {
                        message.cardNum = reader.int32();
                        break;
                    }
                case 14: {
                        message.dealTimes = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BattlePlayerData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.BattlePlayerData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.BattlePlayerData} BattlePlayerData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BattlePlayerData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BattlePlayerData message.
         * @function verify
         * @memberof common.BattlePlayerData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BattlePlayerData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.nick != null && message.hasOwnProperty("nick"))
                if (!$util.isString(message.nick))
                    return "nick: string expected";
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                if (!$util.isInteger(message.avatar))
                    return "avatar: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.country != null && message.hasOwnProperty("country"))
                if (!$util.isString(message.country))
                    return "country: string expected";
            if (message.cup != null && message.hasOwnProperty("cup"))
                if (!$util.isInteger(message.cup))
                    return "cup: integer expected";
            if (message.guildName != null && message.hasOwnProperty("guildName"))
                if (!$util.isString(message.guildName))
                    return "guildName: string expected";
            if (message.guildFlag != null && message.hasOwnProperty("guildFlag"))
                if (!$util.isInteger(message.guildFlag) && !(message.guildFlag && $util.isInteger(message.guildFlag.low) && $util.isInteger(message.guildFlag.high)))
                    return "guildFlag: integer|Long expected";
            if (message.cards != null && message.hasOwnProperty("cards")) {
                if (!Array.isArray(message.cards))
                    return "cards: array expected";
                for (var i = 0; i < message.cards.length; ++i) {
                    var error = $root.common.Card.verify(message.cards[i]);
                    if (error)
                        return "cards." + error;
                }
            }
            if (message.initCards != null && message.hasOwnProperty("initCards")) {
                if (!Array.isArray(message.initCards))
                    return "initCards: array expected";
                for (var i = 0; i < message.initCards.length; ++i) {
                    var error = $root.common.HandCardInst.verify(message.initCards[i]);
                    if (error)
                        return "initCards." + error;
                }
            }
            if (message.initEnergy != null && message.hasOwnProperty("initEnergy"))
                if (!$util.isInteger(message.initEnergy))
                    return "initEnergy: integer expected";
            if (message.nextFrame != null && message.hasOwnProperty("nextFrame"))
                if (!$util.isInteger(message.nextFrame))
                    return "nextFrame: integer expected";
            if (message.cardNum != null && message.hasOwnProperty("cardNum"))
                if (!$util.isInteger(message.cardNum))
                    return "cardNum: integer expected";
            if (message.dealTimes != null && message.hasOwnProperty("dealTimes"))
                if (!$util.isInteger(message.dealTimes))
                    return "dealTimes: integer expected";
            return null;
        };

        /**
         * Creates a BattlePlayerData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.BattlePlayerData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.BattlePlayerData} BattlePlayerData
         */
        BattlePlayerData.fromObject = function fromObject(object) {
            if (object instanceof $root.common.BattlePlayerData)
                return object;
            var message = new $root.common.BattlePlayerData();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.nick != null)
                message.nick = String(object.nick);
            if (object.avatar != null)
                message.avatar = object.avatar | 0;
            if (object.level != null)
                message.level = object.level | 0;
            if (object.country != null)
                message.country = String(object.country);
            if (object.cup != null)
                message.cup = object.cup | 0;
            if (object.guildName != null)
                message.guildName = String(object.guildName);
            if (object.guildFlag != null)
                if ($util.Long)
                    (message.guildFlag = $util.Long.fromValue(object.guildFlag)).unsigned = false;
                else if (typeof object.guildFlag === "string")
                    message.guildFlag = parseInt(object.guildFlag, 10);
                else if (typeof object.guildFlag === "number")
                    message.guildFlag = object.guildFlag;
                else if (typeof object.guildFlag === "object")
                    message.guildFlag = new $util.LongBits(object.guildFlag.low >>> 0, object.guildFlag.high >>> 0).toNumber();
            if (object.cards) {
                if (!Array.isArray(object.cards))
                    throw TypeError(".common.BattlePlayerData.cards: array expected");
                message.cards = [];
                for (var i = 0; i < object.cards.length; ++i) {
                    if (typeof object.cards[i] !== "object")
                        throw TypeError(".common.BattlePlayerData.cards: object expected");
                    message.cards[i] = $root.common.Card.fromObject(object.cards[i]);
                }
            }
            if (object.initCards) {
                if (!Array.isArray(object.initCards))
                    throw TypeError(".common.BattlePlayerData.initCards: array expected");
                message.initCards = [];
                for (var i = 0; i < object.initCards.length; ++i) {
                    if (typeof object.initCards[i] !== "object")
                        throw TypeError(".common.BattlePlayerData.initCards: object expected");
                    message.initCards[i] = $root.common.HandCardInst.fromObject(object.initCards[i]);
                }
            }
            if (object.initEnergy != null)
                message.initEnergy = object.initEnergy | 0;
            if (object.nextFrame != null)
                message.nextFrame = object.nextFrame | 0;
            if (object.cardNum != null)
                message.cardNum = object.cardNum | 0;
            if (object.dealTimes != null)
                message.dealTimes = object.dealTimes | 0;
            return message;
        };

        /**
         * Creates a plain object from a BattlePlayerData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.BattlePlayerData
         * @static
         * @param {common.BattlePlayerData} message BattlePlayerData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BattlePlayerData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.cards = [];
                object.initCards = [];
            }
            if (options.defaults) {
                object.uid = "";
                object.nick = "";
                object.avatar = 0;
                object.level = 0;
                object.country = "";
                object.cup = 0;
                object.guildName = "";
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.guildFlag = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.guildFlag = options.longs === String ? "0" : 0;
                object.initEnergy = 0;
                object.nextFrame = 0;
                object.cardNum = 0;
                object.dealTimes = 0;
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.nick != null && message.hasOwnProperty("nick"))
                object.nick = message.nick;
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                object.avatar = message.avatar;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.country != null && message.hasOwnProperty("country"))
                object.country = message.country;
            if (message.cup != null && message.hasOwnProperty("cup"))
                object.cup = message.cup;
            if (message.guildName != null && message.hasOwnProperty("guildName"))
                object.guildName = message.guildName;
            if (message.guildFlag != null && message.hasOwnProperty("guildFlag"))
                if (typeof message.guildFlag === "number")
                    object.guildFlag = options.longs === String ? String(message.guildFlag) : message.guildFlag;
                else
                    object.guildFlag = options.longs === String ? $util.Long.prototype.toString.call(message.guildFlag) : options.longs === Number ? new $util.LongBits(message.guildFlag.low >>> 0, message.guildFlag.high >>> 0).toNumber() : message.guildFlag;
            if (message.cards && message.cards.length) {
                object.cards = [];
                for (var j = 0; j < message.cards.length; ++j)
                    object.cards[j] = $root.common.Card.toObject(message.cards[j], options);
            }
            if (message.initCards && message.initCards.length) {
                object.initCards = [];
                for (var j = 0; j < message.initCards.length; ++j)
                    object.initCards[j] = $root.common.HandCardInst.toObject(message.initCards[j], options);
            }
            if (message.initEnergy != null && message.hasOwnProperty("initEnergy"))
                object.initEnergy = message.initEnergy;
            if (message.nextFrame != null && message.hasOwnProperty("nextFrame"))
                object.nextFrame = message.nextFrame;
            if (message.cardNum != null && message.hasOwnProperty("cardNum"))
                object.cardNum = message.cardNum;
            if (message.dealTimes != null && message.hasOwnProperty("dealTimes"))
                object.dealTimes = message.dealTimes;
            return object;
        };

        /**
         * Converts this BattlePlayerData to JSON.
         * @function toJSON
         * @memberof common.BattlePlayerData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BattlePlayerData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BattlePlayerData
         * @function getTypeUrl
         * @memberof common.BattlePlayerData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BattlePlayerData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.BattlePlayerData";
        };

        return BattlePlayerData;
    })();

    common.BattleResult = (function() {

        /**
         * Properties of a BattleResult.
         * @memberof common
         * @interface IBattleResult
         * @property {string|null} [winner] BattleResult winner
         * @property {Array.<number>|null} [cups] BattleResult cups
         * @property {Array.<number>|null} [crowns] BattleResult crowns
         * @property {number|null} [turnTheTile] BattleResult turnTheTile
         * @property {number|null} [endFrame] BattleResult endFrame
         */

        /**
         * Constructs a new BattleResult.
         * @memberof common
         * @classdesc Represents a BattleResult.
         * @implements IBattleResult
         * @constructor
         * @param {common.IBattleResult=} [properties] Properties to set
         */
        function BattleResult(properties) {
            this.cups = [];
            this.crowns = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BattleResult winner.
         * @member {string} winner
         * @memberof common.BattleResult
         * @instance
         */
        BattleResult.prototype.winner = "";

        /**
         * BattleResult cups.
         * @member {Array.<number>} cups
         * @memberof common.BattleResult
         * @instance
         */
        BattleResult.prototype.cups = $util.emptyArray;

        /**
         * BattleResult crowns.
         * @member {Array.<number>} crowns
         * @memberof common.BattleResult
         * @instance
         */
        BattleResult.prototype.crowns = $util.emptyArray;

        /**
         * BattleResult turnTheTile.
         * @member {number} turnTheTile
         * @memberof common.BattleResult
         * @instance
         */
        BattleResult.prototype.turnTheTile = 0;

        /**
         * BattleResult endFrame.
         * @member {number} endFrame
         * @memberof common.BattleResult
         * @instance
         */
        BattleResult.prototype.endFrame = 0;

        /**
         * Creates a new BattleResult instance using the specified properties.
         * @function create
         * @memberof common.BattleResult
         * @static
         * @param {common.IBattleResult=} [properties] Properties to set
         * @returns {common.BattleResult} BattleResult instance
         */
        BattleResult.create = function create(properties) {
            return new BattleResult(properties);
        };

        /**
         * Encodes the specified BattleResult message. Does not implicitly {@link common.BattleResult.verify|verify} messages.
         * @function encode
         * @memberof common.BattleResult
         * @static
         * @param {common.IBattleResult} message BattleResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BattleResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.winner != null && Object.hasOwnProperty.call(message, "winner"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.winner);
            if (message.cups != null && message.cups.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (var i = 0; i < message.cups.length; ++i)
                    writer.int32(message.cups[i]);
                writer.ldelim();
            }
            if (message.crowns != null && message.crowns.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (var i = 0; i < message.crowns.length; ++i)
                    writer.int32(message.crowns[i]);
                writer.ldelim();
            }
            if (message.turnTheTile != null && Object.hasOwnProperty.call(message, "turnTheTile"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.turnTheTile);
            if (message.endFrame != null && Object.hasOwnProperty.call(message, "endFrame"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.endFrame);
            return writer;
        };

        /**
         * Encodes the specified BattleResult message, length delimited. Does not implicitly {@link common.BattleResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.BattleResult
         * @static
         * @param {common.IBattleResult} message BattleResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BattleResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BattleResult message from the specified reader or buffer.
         * @function decode
         * @memberof common.BattleResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.BattleResult} BattleResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BattleResult.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.BattleResult();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.winner = reader.string();
                        break;
                    }
                case 2: {
                        if (!(message.cups && message.cups.length))
                            message.cups = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.cups.push(reader.int32());
                        } else
                            message.cups.push(reader.int32());
                        break;
                    }
                case 3: {
                        if (!(message.crowns && message.crowns.length))
                            message.crowns = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.crowns.push(reader.int32());
                        } else
                            message.crowns.push(reader.int32());
                        break;
                    }
                case 4: {
                        message.turnTheTile = reader.int32();
                        break;
                    }
                case 5: {
                        message.endFrame = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BattleResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.BattleResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.BattleResult} BattleResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BattleResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BattleResult message.
         * @function verify
         * @memberof common.BattleResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BattleResult.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.winner != null && message.hasOwnProperty("winner"))
                if (!$util.isString(message.winner))
                    return "winner: string expected";
            if (message.cups != null && message.hasOwnProperty("cups")) {
                if (!Array.isArray(message.cups))
                    return "cups: array expected";
                for (var i = 0; i < message.cups.length; ++i)
                    if (!$util.isInteger(message.cups[i]))
                        return "cups: integer[] expected";
            }
            if (message.crowns != null && message.hasOwnProperty("crowns")) {
                if (!Array.isArray(message.crowns))
                    return "crowns: array expected";
                for (var i = 0; i < message.crowns.length; ++i)
                    if (!$util.isInteger(message.crowns[i]))
                        return "crowns: integer[] expected";
            }
            if (message.turnTheTile != null && message.hasOwnProperty("turnTheTile"))
                if (!$util.isInteger(message.turnTheTile))
                    return "turnTheTile: integer expected";
            if (message.endFrame != null && message.hasOwnProperty("endFrame"))
                if (!$util.isInteger(message.endFrame))
                    return "endFrame: integer expected";
            return null;
        };

        /**
         * Creates a BattleResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.BattleResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.BattleResult} BattleResult
         */
        BattleResult.fromObject = function fromObject(object) {
            if (object instanceof $root.common.BattleResult)
                return object;
            var message = new $root.common.BattleResult();
            if (object.winner != null)
                message.winner = String(object.winner);
            if (object.cups) {
                if (!Array.isArray(object.cups))
                    throw TypeError(".common.BattleResult.cups: array expected");
                message.cups = [];
                for (var i = 0; i < object.cups.length; ++i)
                    message.cups[i] = object.cups[i] | 0;
            }
            if (object.crowns) {
                if (!Array.isArray(object.crowns))
                    throw TypeError(".common.BattleResult.crowns: array expected");
                message.crowns = [];
                for (var i = 0; i < object.crowns.length; ++i)
                    message.crowns[i] = object.crowns[i] | 0;
            }
            if (object.turnTheTile != null)
                message.turnTheTile = object.turnTheTile | 0;
            if (object.endFrame != null)
                message.endFrame = object.endFrame | 0;
            return message;
        };

        /**
         * Creates a plain object from a BattleResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.BattleResult
         * @static
         * @param {common.BattleResult} message BattleResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BattleResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.cups = [];
                object.crowns = [];
            }
            if (options.defaults) {
                object.winner = "";
                object.turnTheTile = 0;
                object.endFrame = 0;
            }
            if (message.winner != null && message.hasOwnProperty("winner"))
                object.winner = message.winner;
            if (message.cups && message.cups.length) {
                object.cups = [];
                for (var j = 0; j < message.cups.length; ++j)
                    object.cups[j] = message.cups[j];
            }
            if (message.crowns && message.crowns.length) {
                object.crowns = [];
                for (var j = 0; j < message.crowns.length; ++j)
                    object.crowns[j] = message.crowns[j];
            }
            if (message.turnTheTile != null && message.hasOwnProperty("turnTheTile"))
                object.turnTheTile = message.turnTheTile;
            if (message.endFrame != null && message.hasOwnProperty("endFrame"))
                object.endFrame = message.endFrame;
            return object;
        };

        /**
         * Converts this BattleResult to JSON.
         * @function toJSON
         * @memberof common.BattleResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BattleResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BattleResult
         * @function getTypeUrl
         * @memberof common.BattleResult
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BattleResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.BattleResult";
        };

        return BattleResult;
    })();

    common.CardInstData = (function() {

        /**
         * Properties of a CardInstData.
         * @memberof common
         * @interface ICardInstData
         * @property {number|null} [level] CardInstData level
         * @property {number|null} [modelId] CardInstData modelId
         */

        /**
         * Constructs a new CardInstData.
         * @memberof common
         * @classdesc Represents a CardInstData.
         * @implements ICardInstData
         * @constructor
         * @param {common.ICardInstData=} [properties] Properties to set
         */
        function CardInstData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CardInstData level.
         * @member {number} level
         * @memberof common.CardInstData
         * @instance
         */
        CardInstData.prototype.level = 0;

        /**
         * CardInstData modelId.
         * @member {number} modelId
         * @memberof common.CardInstData
         * @instance
         */
        CardInstData.prototype.modelId = 0;

        /**
         * Creates a new CardInstData instance using the specified properties.
         * @function create
         * @memberof common.CardInstData
         * @static
         * @param {common.ICardInstData=} [properties] Properties to set
         * @returns {common.CardInstData} CardInstData instance
         */
        CardInstData.create = function create(properties) {
            return new CardInstData(properties);
        };

        /**
         * Encodes the specified CardInstData message. Does not implicitly {@link common.CardInstData.verify|verify} messages.
         * @function encode
         * @memberof common.CardInstData
         * @static
         * @param {common.ICardInstData} message CardInstData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CardInstData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.level);
            if (message.modelId != null && Object.hasOwnProperty.call(message, "modelId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.modelId);
            return writer;
        };

        /**
         * Encodes the specified CardInstData message, length delimited. Does not implicitly {@link common.CardInstData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.CardInstData
         * @static
         * @param {common.ICardInstData} message CardInstData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CardInstData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CardInstData message from the specified reader or buffer.
         * @function decode
         * @memberof common.CardInstData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.CardInstData} CardInstData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CardInstData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.CardInstData();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.level = reader.int32();
                        break;
                    }
                case 2: {
                        message.modelId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CardInstData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.CardInstData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.CardInstData} CardInstData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CardInstData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CardInstData message.
         * @function verify
         * @memberof common.CardInstData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CardInstData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.modelId != null && message.hasOwnProperty("modelId"))
                if (!$util.isInteger(message.modelId))
                    return "modelId: integer expected";
            return null;
        };

        /**
         * Creates a CardInstData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.CardInstData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.CardInstData} CardInstData
         */
        CardInstData.fromObject = function fromObject(object) {
            if (object instanceof $root.common.CardInstData)
                return object;
            var message = new $root.common.CardInstData();
            if (object.level != null)
                message.level = object.level | 0;
            if (object.modelId != null)
                message.modelId = object.modelId | 0;
            return message;
        };

        /**
         * Creates a plain object from a CardInstData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.CardInstData
         * @static
         * @param {common.CardInstData} message CardInstData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CardInstData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.level = 0;
                object.modelId = 0;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.modelId != null && message.hasOwnProperty("modelId"))
                object.modelId = message.modelId;
            return object;
        };

        /**
         * Converts this CardInstData to JSON.
         * @function toJSON
         * @memberof common.CardInstData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CardInstData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CardInstData
         * @function getTypeUrl
         * @memberof common.CardInstData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CardInstData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.CardInstData";
        };

        return CardInstData;
    })();

    common.ClientCommand = (function() {

        /**
         * Properties of a ClientCommand.
         * @memberof common
         * @interface IClientCommand
         * @property {string|null} [uid] ClientCommand uid
         * @property {number|null} [frame] ClientCommand frame
         * @property {number|null} [instId] ClientCommand instId
         * @property {number|null} [card] ClientCommand card
         * @property {Array.<common.ICommandRoute>|null} [routes] ClientCommand routes
         */

        /**
         * Constructs a new ClientCommand.
         * @memberof common
         * @classdesc Represents a ClientCommand.
         * @implements IClientCommand
         * @constructor
         * @param {common.IClientCommand=} [properties] Properties to set
         */
        function ClientCommand(properties) {
            this.routes = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ClientCommand uid.
         * @member {string} uid
         * @memberof common.ClientCommand
         * @instance
         */
        ClientCommand.prototype.uid = "";

        /**
         * ClientCommand frame.
         * @member {number} frame
         * @memberof common.ClientCommand
         * @instance
         */
        ClientCommand.prototype.frame = 0;

        /**
         * ClientCommand instId.
         * @member {number} instId
         * @memberof common.ClientCommand
         * @instance
         */
        ClientCommand.prototype.instId = 0;

        /**
         * ClientCommand card.
         * @member {number} card
         * @memberof common.ClientCommand
         * @instance
         */
        ClientCommand.prototype.card = 0;

        /**
         * ClientCommand routes.
         * @member {Array.<common.ICommandRoute>} routes
         * @memberof common.ClientCommand
         * @instance
         */
        ClientCommand.prototype.routes = $util.emptyArray;

        /**
         * Creates a new ClientCommand instance using the specified properties.
         * @function create
         * @memberof common.ClientCommand
         * @static
         * @param {common.IClientCommand=} [properties] Properties to set
         * @returns {common.ClientCommand} ClientCommand instance
         */
        ClientCommand.create = function create(properties) {
            return new ClientCommand(properties);
        };

        /**
         * Encodes the specified ClientCommand message. Does not implicitly {@link common.ClientCommand.verify|verify} messages.
         * @function encode
         * @memberof common.ClientCommand
         * @static
         * @param {common.IClientCommand} message ClientCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClientCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.frame != null && Object.hasOwnProperty.call(message, "frame"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.frame);
            if (message.instId != null && Object.hasOwnProperty.call(message, "instId"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.instId);
            if (message.card != null && Object.hasOwnProperty.call(message, "card"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.card);
            if (message.routes != null && message.routes.length)
                for (var i = 0; i < message.routes.length; ++i)
                    $root.common.CommandRoute.encode(message.routes[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ClientCommand message, length delimited. Does not implicitly {@link common.ClientCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.ClientCommand
         * @static
         * @param {common.IClientCommand} message ClientCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClientCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ClientCommand message from the specified reader or buffer.
         * @function decode
         * @memberof common.ClientCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.ClientCommand} ClientCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClientCommand.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.ClientCommand();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.frame = reader.int32();
                        break;
                    }
                case 3: {
                        message.instId = reader.int32();
                        break;
                    }
                case 4: {
                        message.card = reader.int32();
                        break;
                    }
                case 5: {
                        if (!(message.routes && message.routes.length))
                            message.routes = [];
                        message.routes.push($root.common.CommandRoute.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ClientCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.ClientCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.ClientCommand} ClientCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClientCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ClientCommand message.
         * @function verify
         * @memberof common.ClientCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ClientCommand.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.frame != null && message.hasOwnProperty("frame"))
                if (!$util.isInteger(message.frame))
                    return "frame: integer expected";
            if (message.instId != null && message.hasOwnProperty("instId"))
                if (!$util.isInteger(message.instId))
                    return "instId: integer expected";
            if (message.card != null && message.hasOwnProperty("card"))
                if (!$util.isInteger(message.card))
                    return "card: integer expected";
            if (message.routes != null && message.hasOwnProperty("routes")) {
                if (!Array.isArray(message.routes))
                    return "routes: array expected";
                for (var i = 0; i < message.routes.length; ++i) {
                    var error = $root.common.CommandRoute.verify(message.routes[i]);
                    if (error)
                        return "routes." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ClientCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.ClientCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.ClientCommand} ClientCommand
         */
        ClientCommand.fromObject = function fromObject(object) {
            if (object instanceof $root.common.ClientCommand)
                return object;
            var message = new $root.common.ClientCommand();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.frame != null)
                message.frame = object.frame | 0;
            if (object.instId != null)
                message.instId = object.instId | 0;
            if (object.card != null)
                message.card = object.card | 0;
            if (object.routes) {
                if (!Array.isArray(object.routes))
                    throw TypeError(".common.ClientCommand.routes: array expected");
                message.routes = [];
                for (var i = 0; i < object.routes.length; ++i) {
                    if (typeof object.routes[i] !== "object")
                        throw TypeError(".common.ClientCommand.routes: object expected");
                    message.routes[i] = $root.common.CommandRoute.fromObject(object.routes[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ClientCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.ClientCommand
         * @static
         * @param {common.ClientCommand} message ClientCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ClientCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.routes = [];
            if (options.defaults) {
                object.uid = "";
                object.frame = 0;
                object.instId = 0;
                object.card = 0;
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.frame != null && message.hasOwnProperty("frame"))
                object.frame = message.frame;
            if (message.instId != null && message.hasOwnProperty("instId"))
                object.instId = message.instId;
            if (message.card != null && message.hasOwnProperty("card"))
                object.card = message.card;
            if (message.routes && message.routes.length) {
                object.routes = [];
                for (var j = 0; j < message.routes.length; ++j)
                    object.routes[j] = $root.common.CommandRoute.toObject(message.routes[j], options);
            }
            return object;
        };

        /**
         * Converts this ClientCommand to JSON.
         * @function toJSON
         * @memberof common.ClientCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ClientCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ClientCommand
         * @function getTypeUrl
         * @memberof common.ClientCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ClientCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.ClientCommand";
        };

        return ClientCommand;
    })();

    common.FrameData = (function() {

        /**
         * Properties of a FrameData.
         * @memberof common
         * @interface IFrameData
         * @property {number|null} [frame] FrameData frame
         * @property {Array.<common.IClientCommand>|null} [commands] FrameData commands
         * @property {Array.<common.IReplayPower>|null} [addPower] FrameData addPower
         * @property {Array.<common.IReplayHandCard>|null} [addCards] FrameData addCards
         */

        /**
         * Constructs a new FrameData.
         * @memberof common
         * @classdesc Represents a FrameData.
         * @implements IFrameData
         * @constructor
         * @param {common.IFrameData=} [properties] Properties to set
         */
        function FrameData(properties) {
            this.commands = [];
            this.addPower = [];
            this.addCards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrameData frame.
         * @member {number} frame
         * @memberof common.FrameData
         * @instance
         */
        FrameData.prototype.frame = 0;

        /**
         * FrameData commands.
         * @member {Array.<common.IClientCommand>} commands
         * @memberof common.FrameData
         * @instance
         */
        FrameData.prototype.commands = $util.emptyArray;

        /**
         * FrameData addPower.
         * @member {Array.<common.IReplayPower>} addPower
         * @memberof common.FrameData
         * @instance
         */
        FrameData.prototype.addPower = $util.emptyArray;

        /**
         * FrameData addCards.
         * @member {Array.<common.IReplayHandCard>} addCards
         * @memberof common.FrameData
         * @instance
         */
        FrameData.prototype.addCards = $util.emptyArray;

        /**
         * Creates a new FrameData instance using the specified properties.
         * @function create
         * @memberof common.FrameData
         * @static
         * @param {common.IFrameData=} [properties] Properties to set
         * @returns {common.FrameData} FrameData instance
         */
        FrameData.create = function create(properties) {
            return new FrameData(properties);
        };

        /**
         * Encodes the specified FrameData message. Does not implicitly {@link common.FrameData.verify|verify} messages.
         * @function encode
         * @memberof common.FrameData
         * @static
         * @param {common.IFrameData} message FrameData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrameData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.frame != null && Object.hasOwnProperty.call(message, "frame"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.frame);
            if (message.commands != null && message.commands.length)
                for (var i = 0; i < message.commands.length; ++i)
                    $root.common.ClientCommand.encode(message.commands[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.addPower != null && message.addPower.length)
                for (var i = 0; i < message.addPower.length; ++i)
                    $root.common.ReplayPower.encode(message.addPower[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.addCards != null && message.addCards.length)
                for (var i = 0; i < message.addCards.length; ++i)
                    $root.common.ReplayHandCard.encode(message.addCards[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified FrameData message, length delimited. Does not implicitly {@link common.FrameData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.FrameData
         * @static
         * @param {common.IFrameData} message FrameData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrameData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrameData message from the specified reader or buffer.
         * @function decode
         * @memberof common.FrameData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.FrameData} FrameData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrameData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.FrameData();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.frame = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.commands && message.commands.length))
                            message.commands = [];
                        message.commands.push($root.common.ClientCommand.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.addPower && message.addPower.length))
                            message.addPower = [];
                        message.addPower.push($root.common.ReplayPower.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.addCards && message.addCards.length))
                            message.addCards = [];
                        message.addCards.push($root.common.ReplayHandCard.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrameData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.FrameData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.FrameData} FrameData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrameData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrameData message.
         * @function verify
         * @memberof common.FrameData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrameData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.frame != null && message.hasOwnProperty("frame"))
                if (!$util.isInteger(message.frame))
                    return "frame: integer expected";
            if (message.commands != null && message.hasOwnProperty("commands")) {
                if (!Array.isArray(message.commands))
                    return "commands: array expected";
                for (var i = 0; i < message.commands.length; ++i) {
                    var error = $root.common.ClientCommand.verify(message.commands[i]);
                    if (error)
                        return "commands." + error;
                }
            }
            if (message.addPower != null && message.hasOwnProperty("addPower")) {
                if (!Array.isArray(message.addPower))
                    return "addPower: array expected";
                for (var i = 0; i < message.addPower.length; ++i) {
                    var error = $root.common.ReplayPower.verify(message.addPower[i]);
                    if (error)
                        return "addPower." + error;
                }
            }
            if (message.addCards != null && message.hasOwnProperty("addCards")) {
                if (!Array.isArray(message.addCards))
                    return "addCards: array expected";
                for (var i = 0; i < message.addCards.length; ++i) {
                    var error = $root.common.ReplayHandCard.verify(message.addCards[i]);
                    if (error)
                        return "addCards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a FrameData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.FrameData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.FrameData} FrameData
         */
        FrameData.fromObject = function fromObject(object) {
            if (object instanceof $root.common.FrameData)
                return object;
            var message = new $root.common.FrameData();
            if (object.frame != null)
                message.frame = object.frame | 0;
            if (object.commands) {
                if (!Array.isArray(object.commands))
                    throw TypeError(".common.FrameData.commands: array expected");
                message.commands = [];
                for (var i = 0; i < object.commands.length; ++i) {
                    if (typeof object.commands[i] !== "object")
                        throw TypeError(".common.FrameData.commands: object expected");
                    message.commands[i] = $root.common.ClientCommand.fromObject(object.commands[i]);
                }
            }
            if (object.addPower) {
                if (!Array.isArray(object.addPower))
                    throw TypeError(".common.FrameData.addPower: array expected");
                message.addPower = [];
                for (var i = 0; i < object.addPower.length; ++i) {
                    if (typeof object.addPower[i] !== "object")
                        throw TypeError(".common.FrameData.addPower: object expected");
                    message.addPower[i] = $root.common.ReplayPower.fromObject(object.addPower[i]);
                }
            }
            if (object.addCards) {
                if (!Array.isArray(object.addCards))
                    throw TypeError(".common.FrameData.addCards: array expected");
                message.addCards = [];
                for (var i = 0; i < object.addCards.length; ++i) {
                    if (typeof object.addCards[i] !== "object")
                        throw TypeError(".common.FrameData.addCards: object expected");
                    message.addCards[i] = $root.common.ReplayHandCard.fromObject(object.addCards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a FrameData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.FrameData
         * @static
         * @param {common.FrameData} message FrameData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrameData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults) {
                object.commands = [];
                object.addPower = [];
                object.addCards = [];
            }
            if (options.defaults)
                object.frame = 0;
            if (message.frame != null && message.hasOwnProperty("frame"))
                object.frame = message.frame;
            if (message.commands && message.commands.length) {
                object.commands = [];
                for (var j = 0; j < message.commands.length; ++j)
                    object.commands[j] = $root.common.ClientCommand.toObject(message.commands[j], options);
            }
            if (message.addPower && message.addPower.length) {
                object.addPower = [];
                for (var j = 0; j < message.addPower.length; ++j)
                    object.addPower[j] = $root.common.ReplayPower.toObject(message.addPower[j], options);
            }
            if (message.addCards && message.addCards.length) {
                object.addCards = [];
                for (var j = 0; j < message.addCards.length; ++j)
                    object.addCards[j] = $root.common.ReplayHandCard.toObject(message.addCards[j], options);
            }
            return object;
        };

        /**
         * Converts this FrameData to JSON.
         * @function toJSON
         * @memberof common.FrameData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrameData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrameData
         * @function getTypeUrl
         * @memberof common.FrameData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrameData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.FrameData";
        };

        return FrameData;
    })();

    common.BattleFieldData = (function() {

        /**
         * Properties of a BattleFieldData.
         * @memberof common
         * @interface IBattleFieldData
         * @property {number|null} [curFrame] BattleFieldData curFrame
         * @property {number|Long|null} [seed] BattleFieldData seed
         * @property {Array.<common.IBattlePlayerData>|null} [playerData] BattleFieldData playerData
         * @property {Object.<string,common.IFrameData>|null} [frameData] BattleFieldData frameData
         * @property {number|null} [battleConfigId] BattleFieldData battleConfigId
         */

        /**
         * Constructs a new BattleFieldData.
         * @memberof common
         * @classdesc Represents a BattleFieldData.
         * @implements IBattleFieldData
         * @constructor
         * @param {common.IBattleFieldData=} [properties] Properties to set
         */
        function BattleFieldData(properties) {
            this.playerData = [];
            this.frameData = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BattleFieldData curFrame.
         * @member {number} curFrame
         * @memberof common.BattleFieldData
         * @instance
         */
        BattleFieldData.prototype.curFrame = 0;

        /**
         * BattleFieldData seed.
         * @member {number|Long} seed
         * @memberof common.BattleFieldData
         * @instance
         */
        BattleFieldData.prototype.seed = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * BattleFieldData playerData.
         * @member {Array.<common.IBattlePlayerData>} playerData
         * @memberof common.BattleFieldData
         * @instance
         */
        BattleFieldData.prototype.playerData = $util.emptyArray;

        /**
         * BattleFieldData frameData.
         * @member {Object.<string,common.IFrameData>} frameData
         * @memberof common.BattleFieldData
         * @instance
         */
        BattleFieldData.prototype.frameData = $util.emptyObject;

        /**
         * BattleFieldData battleConfigId.
         * @member {number} battleConfigId
         * @memberof common.BattleFieldData
         * @instance
         */
        BattleFieldData.prototype.battleConfigId = 0;

        /**
         * Creates a new BattleFieldData instance using the specified properties.
         * @function create
         * @memberof common.BattleFieldData
         * @static
         * @param {common.IBattleFieldData=} [properties] Properties to set
         * @returns {common.BattleFieldData} BattleFieldData instance
         */
        BattleFieldData.create = function create(properties) {
            return new BattleFieldData(properties);
        };

        /**
         * Encodes the specified BattleFieldData message. Does not implicitly {@link common.BattleFieldData.verify|verify} messages.
         * @function encode
         * @memberof common.BattleFieldData
         * @static
         * @param {common.IBattleFieldData} message BattleFieldData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BattleFieldData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.curFrame != null && Object.hasOwnProperty.call(message, "curFrame"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.curFrame);
            if (message.seed != null && Object.hasOwnProperty.call(message, "seed"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.seed);
            if (message.playerData != null && message.playerData.length)
                for (var i = 0; i < message.playerData.length; ++i)
                    $root.common.BattlePlayerData.encode(message.playerData[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.frameData != null && Object.hasOwnProperty.call(message, "frameData"))
                for (var keys = Object.keys(message.frameData), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 4, wireType 2 =*/34).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.FrameData.encode(message.frameData[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            if (message.battleConfigId != null && Object.hasOwnProperty.call(message, "battleConfigId"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.battleConfigId);
            return writer;
        };

        /**
         * Encodes the specified BattleFieldData message, length delimited. Does not implicitly {@link common.BattleFieldData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.BattleFieldData
         * @static
         * @param {common.IBattleFieldData} message BattleFieldData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BattleFieldData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BattleFieldData message from the specified reader or buffer.
         * @function decode
         * @memberof common.BattleFieldData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.BattleFieldData} BattleFieldData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BattleFieldData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.BattleFieldData(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.curFrame = reader.int32();
                        break;
                    }
                case 2: {
                        message.seed = reader.int64();
                        break;
                    }
                case 3: {
                        if (!(message.playerData && message.playerData.length))
                            message.playerData = [];
                        message.playerData.push($root.common.BattlePlayerData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (message.frameData === $util.emptyObject)
                            message.frameData = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.FrameData.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.frameData[key] = value;
                        break;
                    }
                case 5: {
                        message.battleConfigId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BattleFieldData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.BattleFieldData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.BattleFieldData} BattleFieldData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BattleFieldData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BattleFieldData message.
         * @function verify
         * @memberof common.BattleFieldData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BattleFieldData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.curFrame != null && message.hasOwnProperty("curFrame"))
                if (!$util.isInteger(message.curFrame))
                    return "curFrame: integer expected";
            if (message.seed != null && message.hasOwnProperty("seed"))
                if (!$util.isInteger(message.seed) && !(message.seed && $util.isInteger(message.seed.low) && $util.isInteger(message.seed.high)))
                    return "seed: integer|Long expected";
            if (message.playerData != null && message.hasOwnProperty("playerData")) {
                if (!Array.isArray(message.playerData))
                    return "playerData: array expected";
                for (var i = 0; i < message.playerData.length; ++i) {
                    var error = $root.common.BattlePlayerData.verify(message.playerData[i]);
                    if (error)
                        return "playerData." + error;
                }
            }
            if (message.frameData != null && message.hasOwnProperty("frameData")) {
                if (!$util.isObject(message.frameData))
                    return "frameData: object expected";
                var key = Object.keys(message.frameData);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "frameData: integer key{k:int32} expected";
                    {
                        var error = $root.common.FrameData.verify(message.frameData[key[i]]);
                        if (error)
                            return "frameData." + error;
                    }
                }
            }
            if (message.battleConfigId != null && message.hasOwnProperty("battleConfigId"))
                if (!$util.isInteger(message.battleConfigId))
                    return "battleConfigId: integer expected";
            return null;
        };

        /**
         * Creates a BattleFieldData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.BattleFieldData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.BattleFieldData} BattleFieldData
         */
        BattleFieldData.fromObject = function fromObject(object) {
            if (object instanceof $root.common.BattleFieldData)
                return object;
            var message = new $root.common.BattleFieldData();
            if (object.curFrame != null)
                message.curFrame = object.curFrame | 0;
            if (object.seed != null)
                if ($util.Long)
                    (message.seed = $util.Long.fromValue(object.seed)).unsigned = false;
                else if (typeof object.seed === "string")
                    message.seed = parseInt(object.seed, 10);
                else if (typeof object.seed === "number")
                    message.seed = object.seed;
                else if (typeof object.seed === "object")
                    message.seed = new $util.LongBits(object.seed.low >>> 0, object.seed.high >>> 0).toNumber();
            if (object.playerData) {
                if (!Array.isArray(object.playerData))
                    throw TypeError(".common.BattleFieldData.playerData: array expected");
                message.playerData = [];
                for (var i = 0; i < object.playerData.length; ++i) {
                    if (typeof object.playerData[i] !== "object")
                        throw TypeError(".common.BattleFieldData.playerData: object expected");
                    message.playerData[i] = $root.common.BattlePlayerData.fromObject(object.playerData[i]);
                }
            }
            if (object.frameData) {
                if (typeof object.frameData !== "object")
                    throw TypeError(".common.BattleFieldData.frameData: object expected");
                message.frameData = {};
                for (var keys = Object.keys(object.frameData), i = 0; i < keys.length; ++i) {
                    if (typeof object.frameData[keys[i]] !== "object")
                        throw TypeError(".common.BattleFieldData.frameData: object expected");
                    message.frameData[keys[i]] = $root.common.FrameData.fromObject(object.frameData[keys[i]]);
                }
            }
            if (object.battleConfigId != null)
                message.battleConfigId = object.battleConfigId | 0;
            return message;
        };

        /**
         * Creates a plain object from a BattleFieldData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.BattleFieldData
         * @static
         * @param {common.BattleFieldData} message BattleFieldData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BattleFieldData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.playerData = [];
            if (options.objects || options.defaults)
                object.frameData = {};
            if (options.defaults) {
                object.curFrame = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.seed = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.seed = options.longs === String ? "0" : 0;
                object.battleConfigId = 0;
            }
            if (message.curFrame != null && message.hasOwnProperty("curFrame"))
                object.curFrame = message.curFrame;
            if (message.seed != null && message.hasOwnProperty("seed"))
                if (typeof message.seed === "number")
                    object.seed = options.longs === String ? String(message.seed) : message.seed;
                else
                    object.seed = options.longs === String ? $util.Long.prototype.toString.call(message.seed) : options.longs === Number ? new $util.LongBits(message.seed.low >>> 0, message.seed.high >>> 0).toNumber() : message.seed;
            if (message.playerData && message.playerData.length) {
                object.playerData = [];
                for (var j = 0; j < message.playerData.length; ++j)
                    object.playerData[j] = $root.common.BattlePlayerData.toObject(message.playerData[j], options);
            }
            var keys2;
            if (message.frameData && (keys2 = Object.keys(message.frameData)).length) {
                object.frameData = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.frameData[keys2[j]] = $root.common.FrameData.toObject(message.frameData[keys2[j]], options);
            }
            if (message.battleConfigId != null && message.hasOwnProperty("battleConfigId"))
                object.battleConfigId = message.battleConfigId;
            return object;
        };

        /**
         * Converts this BattleFieldData to JSON.
         * @function toJSON
         * @memberof common.BattleFieldData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BattleFieldData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BattleFieldData
         * @function getTypeUrl
         * @memberof common.BattleFieldData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BattleFieldData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.BattleFieldData";
        };

        return BattleFieldData;
    })();

    common.ReplayHandCard = (function() {

        /**
         * Properties of a ReplayHandCard.
         * @memberof common
         * @interface IReplayHandCard
         * @property {string|null} [uid] ReplayHandCard uid
         * @property {common.IHandCardInst|null} [card] ReplayHandCard card
         * @property {number|null} [nextFrame] ReplayHandCard nextFrame
         * @property {number|null} [cardNum] ReplayHandCard cardNum
         */

        /**
         * Constructs a new ReplayHandCard.
         * @memberof common
         * @classdesc Represents a ReplayHandCard.
         * @implements IReplayHandCard
         * @constructor
         * @param {common.IReplayHandCard=} [properties] Properties to set
         */
        function ReplayHandCard(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReplayHandCard uid.
         * @member {string} uid
         * @memberof common.ReplayHandCard
         * @instance
         */
        ReplayHandCard.prototype.uid = "";

        /**
         * ReplayHandCard card.
         * @member {common.IHandCardInst|null|undefined} card
         * @memberof common.ReplayHandCard
         * @instance
         */
        ReplayHandCard.prototype.card = null;

        /**
         * ReplayHandCard nextFrame.
         * @member {number} nextFrame
         * @memberof common.ReplayHandCard
         * @instance
         */
        ReplayHandCard.prototype.nextFrame = 0;

        /**
         * ReplayHandCard cardNum.
         * @member {number} cardNum
         * @memberof common.ReplayHandCard
         * @instance
         */
        ReplayHandCard.prototype.cardNum = 0;

        /**
         * Creates a new ReplayHandCard instance using the specified properties.
         * @function create
         * @memberof common.ReplayHandCard
         * @static
         * @param {common.IReplayHandCard=} [properties] Properties to set
         * @returns {common.ReplayHandCard} ReplayHandCard instance
         */
        ReplayHandCard.create = function create(properties) {
            return new ReplayHandCard(properties);
        };

        /**
         * Encodes the specified ReplayHandCard message. Does not implicitly {@link common.ReplayHandCard.verify|verify} messages.
         * @function encode
         * @memberof common.ReplayHandCard
         * @static
         * @param {common.IReplayHandCard} message ReplayHandCard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReplayHandCard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.card != null && Object.hasOwnProperty.call(message, "card"))
                $root.common.HandCardInst.encode(message.card, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.nextFrame != null && Object.hasOwnProperty.call(message, "nextFrame"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.nextFrame);
            if (message.cardNum != null && Object.hasOwnProperty.call(message, "cardNum"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.cardNum);
            return writer;
        };

        /**
         * Encodes the specified ReplayHandCard message, length delimited. Does not implicitly {@link common.ReplayHandCard.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.ReplayHandCard
         * @static
         * @param {common.IReplayHandCard} message ReplayHandCard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReplayHandCard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ReplayHandCard message from the specified reader or buffer.
         * @function decode
         * @memberof common.ReplayHandCard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.ReplayHandCard} ReplayHandCard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReplayHandCard.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.ReplayHandCard();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.card = $root.common.HandCardInst.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.nextFrame = reader.int32();
                        break;
                    }
                case 4: {
                        message.cardNum = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ReplayHandCard message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.ReplayHandCard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.ReplayHandCard} ReplayHandCard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReplayHandCard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReplayHandCard message.
         * @function verify
         * @memberof common.ReplayHandCard
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReplayHandCard.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.card != null && message.hasOwnProperty("card")) {
                var error = $root.common.HandCardInst.verify(message.card);
                if (error)
                    return "card." + error;
            }
            if (message.nextFrame != null && message.hasOwnProperty("nextFrame"))
                if (!$util.isInteger(message.nextFrame))
                    return "nextFrame: integer expected";
            if (message.cardNum != null && message.hasOwnProperty("cardNum"))
                if (!$util.isInteger(message.cardNum))
                    return "cardNum: integer expected";
            return null;
        };

        /**
         * Creates a ReplayHandCard message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.ReplayHandCard
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.ReplayHandCard} ReplayHandCard
         */
        ReplayHandCard.fromObject = function fromObject(object) {
            if (object instanceof $root.common.ReplayHandCard)
                return object;
            var message = new $root.common.ReplayHandCard();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.card != null) {
                if (typeof object.card !== "object")
                    throw TypeError(".common.ReplayHandCard.card: object expected");
                message.card = $root.common.HandCardInst.fromObject(object.card);
            }
            if (object.nextFrame != null)
                message.nextFrame = object.nextFrame | 0;
            if (object.cardNum != null)
                message.cardNum = object.cardNum | 0;
            return message;
        };

        /**
         * Creates a plain object from a ReplayHandCard message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.ReplayHandCard
         * @static
         * @param {common.ReplayHandCard} message ReplayHandCard
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReplayHandCard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.uid = "";
                object.card = null;
                object.nextFrame = 0;
                object.cardNum = 0;
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.card != null && message.hasOwnProperty("card"))
                object.card = $root.common.HandCardInst.toObject(message.card, options);
            if (message.nextFrame != null && message.hasOwnProperty("nextFrame"))
                object.nextFrame = message.nextFrame;
            if (message.cardNum != null && message.hasOwnProperty("cardNum"))
                object.cardNum = message.cardNum;
            return object;
        };

        /**
         * Converts this ReplayHandCard to JSON.
         * @function toJSON
         * @memberof common.ReplayHandCard
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReplayHandCard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReplayHandCard
         * @function getTypeUrl
         * @memberof common.ReplayHandCard
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReplayHandCard.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.ReplayHandCard";
        };

        return ReplayHandCard;
    })();

    common.ReplayPower = (function() {

        /**
         * Properties of a ReplayPower.
         * @memberof common
         * @interface IReplayPower
         * @property {string|null} [uid] ReplayPower uid
         * @property {number|null} [power] ReplayPower power
         * @property {number|null} [nextFrame] ReplayPower nextFrame
         */

        /**
         * Constructs a new ReplayPower.
         * @memberof common
         * @classdesc Represents a ReplayPower.
         * @implements IReplayPower
         * @constructor
         * @param {common.IReplayPower=} [properties] Properties to set
         */
        function ReplayPower(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReplayPower uid.
         * @member {string} uid
         * @memberof common.ReplayPower
         * @instance
         */
        ReplayPower.prototype.uid = "";

        /**
         * ReplayPower power.
         * @member {number} power
         * @memberof common.ReplayPower
         * @instance
         */
        ReplayPower.prototype.power = 0;

        /**
         * ReplayPower nextFrame.
         * @member {number} nextFrame
         * @memberof common.ReplayPower
         * @instance
         */
        ReplayPower.prototype.nextFrame = 0;

        /**
         * Creates a new ReplayPower instance using the specified properties.
         * @function create
         * @memberof common.ReplayPower
         * @static
         * @param {common.IReplayPower=} [properties] Properties to set
         * @returns {common.ReplayPower} ReplayPower instance
         */
        ReplayPower.create = function create(properties) {
            return new ReplayPower(properties);
        };

        /**
         * Encodes the specified ReplayPower message. Does not implicitly {@link common.ReplayPower.verify|verify} messages.
         * @function encode
         * @memberof common.ReplayPower
         * @static
         * @param {common.IReplayPower} message ReplayPower message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReplayPower.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.power != null && Object.hasOwnProperty.call(message, "power"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.power);
            if (message.nextFrame != null && Object.hasOwnProperty.call(message, "nextFrame"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.nextFrame);
            return writer;
        };

        /**
         * Encodes the specified ReplayPower message, length delimited. Does not implicitly {@link common.ReplayPower.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.ReplayPower
         * @static
         * @param {common.IReplayPower} message ReplayPower message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReplayPower.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ReplayPower message from the specified reader or buffer.
         * @function decode
         * @memberof common.ReplayPower
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.ReplayPower} ReplayPower
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReplayPower.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.ReplayPower();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.power = reader.int32();
                        break;
                    }
                case 3: {
                        message.nextFrame = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ReplayPower message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.ReplayPower
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.ReplayPower} ReplayPower
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReplayPower.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReplayPower message.
         * @function verify
         * @memberof common.ReplayPower
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReplayPower.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.power != null && message.hasOwnProperty("power"))
                if (!$util.isInteger(message.power))
                    return "power: integer expected";
            if (message.nextFrame != null && message.hasOwnProperty("nextFrame"))
                if (!$util.isInteger(message.nextFrame))
                    return "nextFrame: integer expected";
            return null;
        };

        /**
         * Creates a ReplayPower message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.ReplayPower
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.ReplayPower} ReplayPower
         */
        ReplayPower.fromObject = function fromObject(object) {
            if (object instanceof $root.common.ReplayPower)
                return object;
            var message = new $root.common.ReplayPower();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.power != null)
                message.power = object.power | 0;
            if (object.nextFrame != null)
                message.nextFrame = object.nextFrame | 0;
            return message;
        };

        /**
         * Creates a plain object from a ReplayPower message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.ReplayPower
         * @static
         * @param {common.ReplayPower} message ReplayPower
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReplayPower.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.uid = "";
                object.power = 0;
                object.nextFrame = 0;
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.power != null && message.hasOwnProperty("power"))
                object.power = message.power;
            if (message.nextFrame != null && message.hasOwnProperty("nextFrame"))
                object.nextFrame = message.nextFrame;
            return object;
        };

        /**
         * Converts this ReplayPower to JSON.
         * @function toJSON
         * @memberof common.ReplayPower
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReplayPower.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReplayPower
         * @function getTypeUrl
         * @memberof common.ReplayPower
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReplayPower.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.ReplayPower";
        };

        return ReplayPower;
    })();

    common.ReplayData = (function() {

        /**
         * Properties of a ReplayData.
         * @memberof common
         * @interface IReplayData
         * @property {string|null} [key] ReplayData key
         * @property {number|Long|null} [seed] ReplayData seed
         * @property {number|null} [time] ReplayData time
         * @property {Array.<common.IBattlePlayerData>|null} [playerData] ReplayData playerData
         * @property {Object.<string,common.IFrameData>|null} [frameList] ReplayData frameList
         * @property {common.IBattleResult|null} [result] ReplayData result
         * @property {string|null} [version] ReplayData version
         */

        /**
         * Constructs a new ReplayData.
         * @memberof common
         * @classdesc Represents a ReplayData.
         * @implements IReplayData
         * @constructor
         * @param {common.IReplayData=} [properties] Properties to set
         */
        function ReplayData(properties) {
            this.playerData = [];
            this.frameList = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReplayData key.
         * @member {string} key
         * @memberof common.ReplayData
         * @instance
         */
        ReplayData.prototype.key = "";

        /**
         * ReplayData seed.
         * @member {number|Long} seed
         * @memberof common.ReplayData
         * @instance
         */
        ReplayData.prototype.seed = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ReplayData time.
         * @member {number} time
         * @memberof common.ReplayData
         * @instance
         */
        ReplayData.prototype.time = 0;

        /**
         * ReplayData playerData.
         * @member {Array.<common.IBattlePlayerData>} playerData
         * @memberof common.ReplayData
         * @instance
         */
        ReplayData.prototype.playerData = $util.emptyArray;

        /**
         * ReplayData frameList.
         * @member {Object.<string,common.IFrameData>} frameList
         * @memberof common.ReplayData
         * @instance
         */
        ReplayData.prototype.frameList = $util.emptyObject;

        /**
         * ReplayData result.
         * @member {common.IBattleResult|null|undefined} result
         * @memberof common.ReplayData
         * @instance
         */
        ReplayData.prototype.result = null;

        /**
         * ReplayData version.
         * @member {string} version
         * @memberof common.ReplayData
         * @instance
         */
        ReplayData.prototype.version = "";

        /**
         * Creates a new ReplayData instance using the specified properties.
         * @function create
         * @memberof common.ReplayData
         * @static
         * @param {common.IReplayData=} [properties] Properties to set
         * @returns {common.ReplayData} ReplayData instance
         */
        ReplayData.create = function create(properties) {
            return new ReplayData(properties);
        };

        /**
         * Encodes the specified ReplayData message. Does not implicitly {@link common.ReplayData.verify|verify} messages.
         * @function encode
         * @memberof common.ReplayData
         * @static
         * @param {common.IReplayData} message ReplayData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReplayData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.seed != null && Object.hasOwnProperty.call(message, "seed"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.seed);
            if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.time);
            if (message.playerData != null && message.playerData.length)
                for (var i = 0; i < message.playerData.length; ++i)
                    $root.common.BattlePlayerData.encode(message.playerData[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.frameList != null && Object.hasOwnProperty.call(message, "frameList"))
                for (var keys = Object.keys(message.frameList), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 5, wireType 2 =*/42).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.common.FrameData.encode(message.frameList[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                $root.common.BattleResult.encode(message.result, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.version);
            return writer;
        };

        /**
         * Encodes the specified ReplayData message, length delimited. Does not implicitly {@link common.ReplayData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.ReplayData
         * @static
         * @param {common.IReplayData} message ReplayData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReplayData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ReplayData message from the specified reader or buffer.
         * @function decode
         * @memberof common.ReplayData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.ReplayData} ReplayData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReplayData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.ReplayData(), key, value;
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.key = reader.string();
                        break;
                    }
                case 2: {
                        message.seed = reader.int64();
                        break;
                    }
                case 3: {
                        message.time = reader.int32();
                        break;
                    }
                case 4: {
                        if (!(message.playerData && message.playerData.length))
                            message.playerData = [];
                        message.playerData.push($root.common.BattlePlayerData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (message.frameList === $util.emptyObject)
                            message.frameList = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.common.FrameData.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.frameList[key] = value;
                        break;
                    }
                case 6: {
                        message.result = $root.common.BattleResult.decode(reader, reader.uint32());
                        break;
                    }
                case 7: {
                        message.version = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ReplayData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.ReplayData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.ReplayData} ReplayData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReplayData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReplayData message.
         * @function verify
         * @memberof common.ReplayData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReplayData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.key != null && message.hasOwnProperty("key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.seed != null && message.hasOwnProperty("seed"))
                if (!$util.isInteger(message.seed) && !(message.seed && $util.isInteger(message.seed.low) && $util.isInteger(message.seed.high)))
                    return "seed: integer|Long expected";
            if (message.time != null && message.hasOwnProperty("time"))
                if (!$util.isInteger(message.time))
                    return "time: integer expected";
            if (message.playerData != null && message.hasOwnProperty("playerData")) {
                if (!Array.isArray(message.playerData))
                    return "playerData: array expected";
                for (var i = 0; i < message.playerData.length; ++i) {
                    var error = $root.common.BattlePlayerData.verify(message.playerData[i]);
                    if (error)
                        return "playerData." + error;
                }
            }
            if (message.frameList != null && message.hasOwnProperty("frameList")) {
                if (!$util.isObject(message.frameList))
                    return "frameList: object expected";
                var key = Object.keys(message.frameList);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "frameList: integer key{k:int32} expected";
                    {
                        var error = $root.common.FrameData.verify(message.frameList[key[i]]);
                        if (error)
                            return "frameList." + error;
                    }
                }
            }
            if (message.result != null && message.hasOwnProperty("result")) {
                var error = $root.common.BattleResult.verify(message.result);
                if (error)
                    return "result." + error;
            }
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isString(message.version))
                    return "version: string expected";
            return null;
        };

        /**
         * Creates a ReplayData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.ReplayData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.ReplayData} ReplayData
         */
        ReplayData.fromObject = function fromObject(object) {
            if (object instanceof $root.common.ReplayData)
                return object;
            var message = new $root.common.ReplayData();
            if (object.key != null)
                message.key = String(object.key);
            if (object.seed != null)
                if ($util.Long)
                    (message.seed = $util.Long.fromValue(object.seed)).unsigned = false;
                else if (typeof object.seed === "string")
                    message.seed = parseInt(object.seed, 10);
                else if (typeof object.seed === "number")
                    message.seed = object.seed;
                else if (typeof object.seed === "object")
                    message.seed = new $util.LongBits(object.seed.low >>> 0, object.seed.high >>> 0).toNumber();
            if (object.time != null)
                message.time = object.time | 0;
            if (object.playerData) {
                if (!Array.isArray(object.playerData))
                    throw TypeError(".common.ReplayData.playerData: array expected");
                message.playerData = [];
                for (var i = 0; i < object.playerData.length; ++i) {
                    if (typeof object.playerData[i] !== "object")
                        throw TypeError(".common.ReplayData.playerData: object expected");
                    message.playerData[i] = $root.common.BattlePlayerData.fromObject(object.playerData[i]);
                }
            }
            if (object.frameList) {
                if (typeof object.frameList !== "object")
                    throw TypeError(".common.ReplayData.frameList: object expected");
                message.frameList = {};
                for (var keys = Object.keys(object.frameList), i = 0; i < keys.length; ++i) {
                    if (typeof object.frameList[keys[i]] !== "object")
                        throw TypeError(".common.ReplayData.frameList: object expected");
                    message.frameList[keys[i]] = $root.common.FrameData.fromObject(object.frameList[keys[i]]);
                }
            }
            if (object.result != null) {
                if (typeof object.result !== "object")
                    throw TypeError(".common.ReplayData.result: object expected");
                message.result = $root.common.BattleResult.fromObject(object.result);
            }
            if (object.version != null)
                message.version = String(object.version);
            return message;
        };

        /**
         * Creates a plain object from a ReplayData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.ReplayData
         * @static
         * @param {common.ReplayData} message ReplayData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReplayData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.playerData = [];
            if (options.objects || options.defaults)
                object.frameList = {};
            if (options.defaults) {
                object.key = "";
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.seed = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.seed = options.longs === String ? "0" : 0;
                object.time = 0;
                object.result = null;
                object.version = "";
            }
            if (message.key != null && message.hasOwnProperty("key"))
                object.key = message.key;
            if (message.seed != null && message.hasOwnProperty("seed"))
                if (typeof message.seed === "number")
                    object.seed = options.longs === String ? String(message.seed) : message.seed;
                else
                    object.seed = options.longs === String ? $util.Long.prototype.toString.call(message.seed) : options.longs === Number ? new $util.LongBits(message.seed.low >>> 0, message.seed.high >>> 0).toNumber() : message.seed;
            if (message.time != null && message.hasOwnProperty("time"))
                object.time = message.time;
            if (message.playerData && message.playerData.length) {
                object.playerData = [];
                for (var j = 0; j < message.playerData.length; ++j)
                    object.playerData[j] = $root.common.BattlePlayerData.toObject(message.playerData[j], options);
            }
            var keys2;
            if (message.frameList && (keys2 = Object.keys(message.frameList)).length) {
                object.frameList = {};
                for (var j = 0; j < keys2.length; ++j)
                    object.frameList[keys2[j]] = $root.common.FrameData.toObject(message.frameList[keys2[j]], options);
            }
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = $root.common.BattleResult.toObject(message.result, options);
            if (message.version != null && message.hasOwnProperty("version"))
                object.version = message.version;
            return object;
        };

        /**
         * Converts this ReplayData to JSON.
         * @function toJSON
         * @memberof common.ReplayData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReplayData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReplayData
         * @function getTypeUrl
         * @memberof common.ReplayData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReplayData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.ReplayData";
        };

        return ReplayData;
    })();

    common.ReplayCard = (function() {

        /**
         * Properties of a ReplayCard.
         * @memberof common
         * @interface IReplayCard
         * @property {number|null} [id] ReplayCard id
         * @property {number|null} [level] ReplayCard level
         */

        /**
         * Constructs a new ReplayCard.
         * @memberof common
         * @classdesc Represents a ReplayCard.
         * @implements IReplayCard
         * @constructor
         * @param {common.IReplayCard=} [properties] Properties to set
         */
        function ReplayCard(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReplayCard id.
         * @member {number} id
         * @memberof common.ReplayCard
         * @instance
         */
        ReplayCard.prototype.id = 0;

        /**
         * ReplayCard level.
         * @member {number} level
         * @memberof common.ReplayCard
         * @instance
         */
        ReplayCard.prototype.level = 0;

        /**
         * Creates a new ReplayCard instance using the specified properties.
         * @function create
         * @memberof common.ReplayCard
         * @static
         * @param {common.IReplayCard=} [properties] Properties to set
         * @returns {common.ReplayCard} ReplayCard instance
         */
        ReplayCard.create = function create(properties) {
            return new ReplayCard(properties);
        };

        /**
         * Encodes the specified ReplayCard message. Does not implicitly {@link common.ReplayCard.verify|verify} messages.
         * @function encode
         * @memberof common.ReplayCard
         * @static
         * @param {common.IReplayCard} message ReplayCard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReplayCard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.level);
            return writer;
        };

        /**
         * Encodes the specified ReplayCard message, length delimited. Does not implicitly {@link common.ReplayCard.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.ReplayCard
         * @static
         * @param {common.IReplayCard} message ReplayCard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReplayCard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ReplayCard message from the specified reader or buffer.
         * @function decode
         * @memberof common.ReplayCard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.ReplayCard} ReplayCard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReplayCard.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.ReplayCard();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int32();
                        break;
                    }
                case 2: {
                        message.level = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ReplayCard message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.ReplayCard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.ReplayCard} ReplayCard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReplayCard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReplayCard message.
         * @function verify
         * @memberof common.ReplayCard
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReplayCard.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            return null;
        };

        /**
         * Creates a ReplayCard message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.ReplayCard
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.ReplayCard} ReplayCard
         */
        ReplayCard.fromObject = function fromObject(object) {
            if (object instanceof $root.common.ReplayCard)
                return object;
            var message = new $root.common.ReplayCard();
            if (object.id != null)
                message.id = object.id | 0;
            if (object.level != null)
                message.level = object.level | 0;
            return message;
        };

        /**
         * Creates a plain object from a ReplayCard message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.ReplayCard
         * @static
         * @param {common.ReplayCard} message ReplayCard
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReplayCard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.id = 0;
                object.level = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            return object;
        };

        /**
         * Converts this ReplayCard to JSON.
         * @function toJSON
         * @memberof common.ReplayCard
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReplayCard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReplayCard
         * @function getTypeUrl
         * @memberof common.ReplayCard
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReplayCard.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.ReplayCard";
        };

        return ReplayCard;
    })();

    common.ReplayPlayerSummary = (function() {

        /**
         * Properties of a ReplayPlayerSummary.
         * @memberof common
         * @interface IReplayPlayerSummary
         * @property {string|null} [uid] ReplayPlayerSummary uid
         * @property {string|null} [nick] ReplayPlayerSummary nick
         * @property {string|null} [avatar] ReplayPlayerSummary avatar
         * @property {number|null} [level] ReplayPlayerSummary level
         * @property {string|null} [country] ReplayPlayerSummary country
         * @property {number|null} [cup] ReplayPlayerSummary cup
         * @property {string|null} [guildName] ReplayPlayerSummary guildName
         * @property {number|Long|null} [guildFlag] ReplayPlayerSummary guildFlag
         * @property {Array.<common.IReplayCard>|null} [cards] ReplayPlayerSummary cards
         */

        /**
         * Constructs a new ReplayPlayerSummary.
         * @memberof common
         * @classdesc Represents a ReplayPlayerSummary.
         * @implements IReplayPlayerSummary
         * @constructor
         * @param {common.IReplayPlayerSummary=} [properties] Properties to set
         */
        function ReplayPlayerSummary(properties) {
            this.cards = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReplayPlayerSummary uid.
         * @member {string} uid
         * @memberof common.ReplayPlayerSummary
         * @instance
         */
        ReplayPlayerSummary.prototype.uid = "";

        /**
         * ReplayPlayerSummary nick.
         * @member {string} nick
         * @memberof common.ReplayPlayerSummary
         * @instance
         */
        ReplayPlayerSummary.prototype.nick = "";

        /**
         * ReplayPlayerSummary avatar.
         * @member {string} avatar
         * @memberof common.ReplayPlayerSummary
         * @instance
         */
        ReplayPlayerSummary.prototype.avatar = "";

        /**
         * ReplayPlayerSummary level.
         * @member {number} level
         * @memberof common.ReplayPlayerSummary
         * @instance
         */
        ReplayPlayerSummary.prototype.level = 0;

        /**
         * ReplayPlayerSummary country.
         * @member {string} country
         * @memberof common.ReplayPlayerSummary
         * @instance
         */
        ReplayPlayerSummary.prototype.country = "";

        /**
         * ReplayPlayerSummary cup.
         * @member {number} cup
         * @memberof common.ReplayPlayerSummary
         * @instance
         */
        ReplayPlayerSummary.prototype.cup = 0;

        /**
         * ReplayPlayerSummary guildName.
         * @member {string} guildName
         * @memberof common.ReplayPlayerSummary
         * @instance
         */
        ReplayPlayerSummary.prototype.guildName = "";

        /**
         * ReplayPlayerSummary guildFlag.
         * @member {number|Long} guildFlag
         * @memberof common.ReplayPlayerSummary
         * @instance
         */
        ReplayPlayerSummary.prototype.guildFlag = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ReplayPlayerSummary cards.
         * @member {Array.<common.IReplayCard>} cards
         * @memberof common.ReplayPlayerSummary
         * @instance
         */
        ReplayPlayerSummary.prototype.cards = $util.emptyArray;

        /**
         * Creates a new ReplayPlayerSummary instance using the specified properties.
         * @function create
         * @memberof common.ReplayPlayerSummary
         * @static
         * @param {common.IReplayPlayerSummary=} [properties] Properties to set
         * @returns {common.ReplayPlayerSummary} ReplayPlayerSummary instance
         */
        ReplayPlayerSummary.create = function create(properties) {
            return new ReplayPlayerSummary(properties);
        };

        /**
         * Encodes the specified ReplayPlayerSummary message. Does not implicitly {@link common.ReplayPlayerSummary.verify|verify} messages.
         * @function encode
         * @memberof common.ReplayPlayerSummary
         * @static
         * @param {common.IReplayPlayerSummary} message ReplayPlayerSummary message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReplayPlayerSummary.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.uid);
            if (message.nick != null && Object.hasOwnProperty.call(message, "nick"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.nick);
            if (message.avatar != null && Object.hasOwnProperty.call(message, "avatar"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.avatar);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.level);
            if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.country);
            if (message.cup != null && Object.hasOwnProperty.call(message, "cup"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.cup);
            if (message.guildName != null && Object.hasOwnProperty.call(message, "guildName"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.guildName);
            if (message.guildFlag != null && Object.hasOwnProperty.call(message, "guildFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.guildFlag);
            if (message.cards != null && message.cards.length)
                for (var i = 0; i < message.cards.length; ++i)
                    $root.common.ReplayCard.encode(message.cards[i], writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ReplayPlayerSummary message, length delimited. Does not implicitly {@link common.ReplayPlayerSummary.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.ReplayPlayerSummary
         * @static
         * @param {common.IReplayPlayerSummary} message ReplayPlayerSummary message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReplayPlayerSummary.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ReplayPlayerSummary message from the specified reader or buffer.
         * @function decode
         * @memberof common.ReplayPlayerSummary
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.ReplayPlayerSummary} ReplayPlayerSummary
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReplayPlayerSummary.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.ReplayPlayerSummary();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.string();
                        break;
                    }
                case 2: {
                        message.nick = reader.string();
                        break;
                    }
                case 3: {
                        message.avatar = reader.string();
                        break;
                    }
                case 4: {
                        message.level = reader.int32();
                        break;
                    }
                case 5: {
                        message.country = reader.string();
                        break;
                    }
                case 6: {
                        message.cup = reader.int32();
                        break;
                    }
                case 7: {
                        message.guildName = reader.string();
                        break;
                    }
                case 8: {
                        message.guildFlag = reader.int64();
                        break;
                    }
                case 9: {
                        if (!(message.cards && message.cards.length))
                            message.cards = [];
                        message.cards.push($root.common.ReplayCard.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ReplayPlayerSummary message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.ReplayPlayerSummary
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.ReplayPlayerSummary} ReplayPlayerSummary
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReplayPlayerSummary.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReplayPlayerSummary message.
         * @function verify
         * @memberof common.ReplayPlayerSummary
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReplayPlayerSummary.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.nick != null && message.hasOwnProperty("nick"))
                if (!$util.isString(message.nick))
                    return "nick: string expected";
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                if (!$util.isString(message.avatar))
                    return "avatar: string expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.country != null && message.hasOwnProperty("country"))
                if (!$util.isString(message.country))
                    return "country: string expected";
            if (message.cup != null && message.hasOwnProperty("cup"))
                if (!$util.isInteger(message.cup))
                    return "cup: integer expected";
            if (message.guildName != null && message.hasOwnProperty("guildName"))
                if (!$util.isString(message.guildName))
                    return "guildName: string expected";
            if (message.guildFlag != null && message.hasOwnProperty("guildFlag"))
                if (!$util.isInteger(message.guildFlag) && !(message.guildFlag && $util.isInteger(message.guildFlag.low) && $util.isInteger(message.guildFlag.high)))
                    return "guildFlag: integer|Long expected";
            if (message.cards != null && message.hasOwnProperty("cards")) {
                if (!Array.isArray(message.cards))
                    return "cards: array expected";
                for (var i = 0; i < message.cards.length; ++i) {
                    var error = $root.common.ReplayCard.verify(message.cards[i]);
                    if (error)
                        return "cards." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ReplayPlayerSummary message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.ReplayPlayerSummary
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.ReplayPlayerSummary} ReplayPlayerSummary
         */
        ReplayPlayerSummary.fromObject = function fromObject(object) {
            if (object instanceof $root.common.ReplayPlayerSummary)
                return object;
            var message = new $root.common.ReplayPlayerSummary();
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.nick != null)
                message.nick = String(object.nick);
            if (object.avatar != null)
                message.avatar = String(object.avatar);
            if (object.level != null)
                message.level = object.level | 0;
            if (object.country != null)
                message.country = String(object.country);
            if (object.cup != null)
                message.cup = object.cup | 0;
            if (object.guildName != null)
                message.guildName = String(object.guildName);
            if (object.guildFlag != null)
                if ($util.Long)
                    (message.guildFlag = $util.Long.fromValue(object.guildFlag)).unsigned = false;
                else if (typeof object.guildFlag === "string")
                    message.guildFlag = parseInt(object.guildFlag, 10);
                else if (typeof object.guildFlag === "number")
                    message.guildFlag = object.guildFlag;
                else if (typeof object.guildFlag === "object")
                    message.guildFlag = new $util.LongBits(object.guildFlag.low >>> 0, object.guildFlag.high >>> 0).toNumber();
            if (object.cards) {
                if (!Array.isArray(object.cards))
                    throw TypeError(".common.ReplayPlayerSummary.cards: array expected");
                message.cards = [];
                for (var i = 0; i < object.cards.length; ++i) {
                    if (typeof object.cards[i] !== "object")
                        throw TypeError(".common.ReplayPlayerSummary.cards: object expected");
                    message.cards[i] = $root.common.ReplayCard.fromObject(object.cards[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ReplayPlayerSummary message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.ReplayPlayerSummary
         * @static
         * @param {common.ReplayPlayerSummary} message ReplayPlayerSummary
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReplayPlayerSummary.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.cards = [];
            if (options.defaults) {
                object.uid = "";
                object.nick = "";
                object.avatar = "";
                object.level = 0;
                object.country = "";
                object.cup = 0;
                object.guildName = "";
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.guildFlag = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.guildFlag = options.longs === String ? "0" : 0;
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.nick != null && message.hasOwnProperty("nick"))
                object.nick = message.nick;
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                object.avatar = message.avatar;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.country != null && message.hasOwnProperty("country"))
                object.country = message.country;
            if (message.cup != null && message.hasOwnProperty("cup"))
                object.cup = message.cup;
            if (message.guildName != null && message.hasOwnProperty("guildName"))
                object.guildName = message.guildName;
            if (message.guildFlag != null && message.hasOwnProperty("guildFlag"))
                if (typeof message.guildFlag === "number")
                    object.guildFlag = options.longs === String ? String(message.guildFlag) : message.guildFlag;
                else
                    object.guildFlag = options.longs === String ? $util.Long.prototype.toString.call(message.guildFlag) : options.longs === Number ? new $util.LongBits(message.guildFlag.low >>> 0, message.guildFlag.high >>> 0).toNumber() : message.guildFlag;
            if (message.cards && message.cards.length) {
                object.cards = [];
                for (var j = 0; j < message.cards.length; ++j)
                    object.cards[j] = $root.common.ReplayCard.toObject(message.cards[j], options);
            }
            return object;
        };

        /**
         * Converts this ReplayPlayerSummary to JSON.
         * @function toJSON
         * @memberof common.ReplayPlayerSummary
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReplayPlayerSummary.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReplayPlayerSummary
         * @function getTypeUrl
         * @memberof common.ReplayPlayerSummary
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReplayPlayerSummary.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.ReplayPlayerSummary";
        };

        return ReplayPlayerSummary;
    })();

    common.ReplaySummaryData = (function() {

        /**
         * Properties of a ReplaySummaryData.
         * @memberof common
         * @interface IReplaySummaryData
         * @property {string|null} [key] ReplaySummaryData key
         * @property {number|null} [time] ReplaySummaryData time
         * @property {common.IBattleResult|null} [result] ReplaySummaryData result
         * @property {Array.<common.IReplayPlayerSummary>|null} [playerData] ReplaySummaryData playerData
         * @property {string|null} [version] ReplaySummaryData version
         * @property {boolean|null} [play] ReplaySummaryData play
         */

        /**
         * Constructs a new ReplaySummaryData.
         * @memberof common
         * @classdesc Represents a ReplaySummaryData.
         * @implements IReplaySummaryData
         * @constructor
         * @param {common.IReplaySummaryData=} [properties] Properties to set
         */
        function ReplaySummaryData(properties) {
            this.playerData = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReplaySummaryData key.
         * @member {string} key
         * @memberof common.ReplaySummaryData
         * @instance
         */
        ReplaySummaryData.prototype.key = "";

        /**
         * ReplaySummaryData time.
         * @member {number} time
         * @memberof common.ReplaySummaryData
         * @instance
         */
        ReplaySummaryData.prototype.time = 0;

        /**
         * ReplaySummaryData result.
         * @member {common.IBattleResult|null|undefined} result
         * @memberof common.ReplaySummaryData
         * @instance
         */
        ReplaySummaryData.prototype.result = null;

        /**
         * ReplaySummaryData playerData.
         * @member {Array.<common.IReplayPlayerSummary>} playerData
         * @memberof common.ReplaySummaryData
         * @instance
         */
        ReplaySummaryData.prototype.playerData = $util.emptyArray;

        /**
         * ReplaySummaryData version.
         * @member {string} version
         * @memberof common.ReplaySummaryData
         * @instance
         */
        ReplaySummaryData.prototype.version = "";

        /**
         * ReplaySummaryData play.
         * @member {boolean} play
         * @memberof common.ReplaySummaryData
         * @instance
         */
        ReplaySummaryData.prototype.play = false;

        /**
         * Creates a new ReplaySummaryData instance using the specified properties.
         * @function create
         * @memberof common.ReplaySummaryData
         * @static
         * @param {common.IReplaySummaryData=} [properties] Properties to set
         * @returns {common.ReplaySummaryData} ReplaySummaryData instance
         */
        ReplaySummaryData.create = function create(properties) {
            return new ReplaySummaryData(properties);
        };

        /**
         * Encodes the specified ReplaySummaryData message. Does not implicitly {@link common.ReplaySummaryData.verify|verify} messages.
         * @function encode
         * @memberof common.ReplaySummaryData
         * @static
         * @param {common.IReplaySummaryData} message ReplaySummaryData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReplaySummaryData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.time);
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                $root.common.BattleResult.encode(message.result, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.playerData != null && message.playerData.length)
                for (var i = 0; i < message.playerData.length; ++i)
                    $root.common.ReplayPlayerSummary.encode(message.playerData[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.version);
            if (message.play != null && Object.hasOwnProperty.call(message, "play"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.play);
            return writer;
        };

        /**
         * Encodes the specified ReplaySummaryData message, length delimited. Does not implicitly {@link common.ReplaySummaryData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.ReplaySummaryData
         * @static
         * @param {common.IReplaySummaryData} message ReplaySummaryData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReplaySummaryData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ReplaySummaryData message from the specified reader or buffer.
         * @function decode
         * @memberof common.ReplaySummaryData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.ReplaySummaryData} ReplaySummaryData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReplaySummaryData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.ReplaySummaryData();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.key = reader.string();
                        break;
                    }
                case 2: {
                        message.time = reader.int32();
                        break;
                    }
                case 3: {
                        message.result = $root.common.BattleResult.decode(reader, reader.uint32());
                        break;
                    }
                case 4: {
                        if (!(message.playerData && message.playerData.length))
                            message.playerData = [];
                        message.playerData.push($root.common.ReplayPlayerSummary.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.version = reader.string();
                        break;
                    }
                case 6: {
                        message.play = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ReplaySummaryData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.ReplaySummaryData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.ReplaySummaryData} ReplaySummaryData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReplaySummaryData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReplaySummaryData message.
         * @function verify
         * @memberof common.ReplaySummaryData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReplaySummaryData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.key != null && message.hasOwnProperty("key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.time != null && message.hasOwnProperty("time"))
                if (!$util.isInteger(message.time))
                    return "time: integer expected";
            if (message.result != null && message.hasOwnProperty("result")) {
                var error = $root.common.BattleResult.verify(message.result);
                if (error)
                    return "result." + error;
            }
            if (message.playerData != null && message.hasOwnProperty("playerData")) {
                if (!Array.isArray(message.playerData))
                    return "playerData: array expected";
                for (var i = 0; i < message.playerData.length; ++i) {
                    var error = $root.common.ReplayPlayerSummary.verify(message.playerData[i]);
                    if (error)
                        return "playerData." + error;
                }
            }
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isString(message.version))
                    return "version: string expected";
            if (message.play != null && message.hasOwnProperty("play"))
                if (typeof message.play !== "boolean")
                    return "play: boolean expected";
            return null;
        };

        /**
         * Creates a ReplaySummaryData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.ReplaySummaryData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.ReplaySummaryData} ReplaySummaryData
         */
        ReplaySummaryData.fromObject = function fromObject(object) {
            if (object instanceof $root.common.ReplaySummaryData)
                return object;
            var message = new $root.common.ReplaySummaryData();
            if (object.key != null)
                message.key = String(object.key);
            if (object.time != null)
                message.time = object.time | 0;
            if (object.result != null) {
                if (typeof object.result !== "object")
                    throw TypeError(".common.ReplaySummaryData.result: object expected");
                message.result = $root.common.BattleResult.fromObject(object.result);
            }
            if (object.playerData) {
                if (!Array.isArray(object.playerData))
                    throw TypeError(".common.ReplaySummaryData.playerData: array expected");
                message.playerData = [];
                for (var i = 0; i < object.playerData.length; ++i) {
                    if (typeof object.playerData[i] !== "object")
                        throw TypeError(".common.ReplaySummaryData.playerData: object expected");
                    message.playerData[i] = $root.common.ReplayPlayerSummary.fromObject(object.playerData[i]);
                }
            }
            if (object.version != null)
                message.version = String(object.version);
            if (object.play != null)
                message.play = Boolean(object.play);
            return message;
        };

        /**
         * Creates a plain object from a ReplaySummaryData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.ReplaySummaryData
         * @static
         * @param {common.ReplaySummaryData} message ReplaySummaryData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReplaySummaryData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.arrays || options.defaults)
                object.playerData = [];
            if (options.defaults) {
                object.key = "";
                object.time = 0;
                object.result = null;
                object.version = "";
                object.play = false;
            }
            if (message.key != null && message.hasOwnProperty("key"))
                object.key = message.key;
            if (message.time != null && message.hasOwnProperty("time"))
                object.time = message.time;
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = $root.common.BattleResult.toObject(message.result, options);
            if (message.playerData && message.playerData.length) {
                object.playerData = [];
                for (var j = 0; j < message.playerData.length; ++j)
                    object.playerData[j] = $root.common.ReplayPlayerSummary.toObject(message.playerData[j], options);
            }
            if (message.version != null && message.hasOwnProperty("version"))
                object.version = message.version;
            if (message.play != null && message.hasOwnProperty("play"))
                object.play = message.play;
            return object;
        };

        /**
         * Converts this ReplaySummaryData to JSON.
         * @function toJSON
         * @memberof common.ReplaySummaryData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReplaySummaryData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReplaySummaryData
         * @function getTypeUrl
         * @memberof common.ReplaySummaryData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReplaySummaryData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.ReplaySummaryData";
        };

        return ReplaySummaryData;
    })();

    common.DamageLabelData = (function() {

        /**
         * Properties of a DamageLabelData.
         * @memberof common
         * @interface IDamageLabelData
         * @property {number|null} [flag] DamageLabelData flag
         * @property {number|null} [val] DamageLabelData val
         * @property {number|null} [posX] DamageLabelData posX
         * @property {number|null} [posY] DamageLabelData posY
         */

        /**
         * Constructs a new DamageLabelData.
         * @memberof common
         * @classdesc Represents a DamageLabelData.
         * @implements IDamageLabelData
         * @constructor
         * @param {common.IDamageLabelData=} [properties] Properties to set
         */
        function DamageLabelData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DamageLabelData flag.
         * @member {number} flag
         * @memberof common.DamageLabelData
         * @instance
         */
        DamageLabelData.prototype.flag = 0;

        /**
         * DamageLabelData val.
         * @member {number} val
         * @memberof common.DamageLabelData
         * @instance
         */
        DamageLabelData.prototype.val = 0;

        /**
         * DamageLabelData posX.
         * @member {number} posX
         * @memberof common.DamageLabelData
         * @instance
         */
        DamageLabelData.prototype.posX = 0;

        /**
         * DamageLabelData posY.
         * @member {number} posY
         * @memberof common.DamageLabelData
         * @instance
         */
        DamageLabelData.prototype.posY = 0;

        /**
         * Creates a new DamageLabelData instance using the specified properties.
         * @function create
         * @memberof common.DamageLabelData
         * @static
         * @param {common.IDamageLabelData=} [properties] Properties to set
         * @returns {common.DamageLabelData} DamageLabelData instance
         */
        DamageLabelData.create = function create(properties) {
            return new DamageLabelData(properties);
        };

        /**
         * Encodes the specified DamageLabelData message. Does not implicitly {@link common.DamageLabelData.verify|verify} messages.
         * @function encode
         * @memberof common.DamageLabelData
         * @static
         * @param {common.IDamageLabelData} message DamageLabelData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DamageLabelData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.flag);
            if (message.val != null && Object.hasOwnProperty.call(message, "val"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.val);
            if (message.posX != null && Object.hasOwnProperty.call(message, "posX"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.posX);
            if (message.posY != null && Object.hasOwnProperty.call(message, "posY"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.posY);
            return writer;
        };

        /**
         * Encodes the specified DamageLabelData message, length delimited. Does not implicitly {@link common.DamageLabelData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.DamageLabelData
         * @static
         * @param {common.IDamageLabelData} message DamageLabelData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DamageLabelData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a DamageLabelData message from the specified reader or buffer.
         * @function decode
         * @memberof common.DamageLabelData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.DamageLabelData} DamageLabelData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DamageLabelData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.DamageLabelData();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.flag = reader.int32();
                        break;
                    }
                case 2: {
                        message.val = reader.int32();
                        break;
                    }
                case 3: {
                        message.posX = reader.int32();
                        break;
                    }
                case 4: {
                        message.posY = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DamageLabelData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.DamageLabelData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.DamageLabelData} DamageLabelData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DamageLabelData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DamageLabelData message.
         * @function verify
         * @memberof common.DamageLabelData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DamageLabelData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.flag != null && message.hasOwnProperty("flag"))
                if (!$util.isInteger(message.flag))
                    return "flag: integer expected";
            if (message.val != null && message.hasOwnProperty("val"))
                if (!$util.isInteger(message.val))
                    return "val: integer expected";
            if (message.posX != null && message.hasOwnProperty("posX"))
                if (!$util.isInteger(message.posX))
                    return "posX: integer expected";
            if (message.posY != null && message.hasOwnProperty("posY"))
                if (!$util.isInteger(message.posY))
                    return "posY: integer expected";
            return null;
        };

        /**
         * Creates a DamageLabelData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.DamageLabelData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.DamageLabelData} DamageLabelData
         */
        DamageLabelData.fromObject = function fromObject(object) {
            if (object instanceof $root.common.DamageLabelData)
                return object;
            var message = new $root.common.DamageLabelData();
            if (object.flag != null)
                message.flag = object.flag | 0;
            if (object.val != null)
                message.val = object.val | 0;
            if (object.posX != null)
                message.posX = object.posX | 0;
            if (object.posY != null)
                message.posY = object.posY | 0;
            return message;
        };

        /**
         * Creates a plain object from a DamageLabelData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.DamageLabelData
         * @static
         * @param {common.DamageLabelData} message DamageLabelData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DamageLabelData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.flag = 0;
                object.val = 0;
                object.posX = 0;
                object.posY = 0;
            }
            if (message.flag != null && message.hasOwnProperty("flag"))
                object.flag = message.flag;
            if (message.val != null && message.hasOwnProperty("val"))
                object.val = message.val;
            if (message.posX != null && message.hasOwnProperty("posX"))
                object.posX = message.posX;
            if (message.posY != null && message.hasOwnProperty("posY"))
                object.posY = message.posY;
            return object;
        };

        /**
         * Converts this DamageLabelData to JSON.
         * @function toJSON
         * @memberof common.DamageLabelData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DamageLabelData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DamageLabelData
         * @function getTypeUrl
         * @memberof common.DamageLabelData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DamageLabelData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.DamageLabelData";
        };

        return DamageLabelData;
    })();

    /**
     * SaleItemStatus enum.
     * @name common.SaleItemStatus
     * @enum {number}
     * @property {number} SALE_ITEM_STATE_UNKNOWN=0 SALE_ITEM_STATE_UNKNOWN value
     * @property {number} SALE_ITEM_STATE_PUBLICITY=1 SALE_ITEM_STATE_PUBLICITY value
     * @property {number} SALE_ITEM_STATE_TRADING=2 SALE_ITEM_STATE_TRADING value
     * @property {number} SALE_ITEM_STATE_VERIFY=3 SALE_ITEM_STATE_VERIFY value
     * @property {number} SALE_ITEM_STATE_CLAIM=4 SALE_ITEM_STATE_CLAIM value
     * @property {number} SALE_ITEM_STATE_EXPIRE=5 SALE_ITEM_STATE_EXPIRE value
     */
    common.SaleItemStatus = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "SALE_ITEM_STATE_UNKNOWN"] = 0;
        values[valuesById[1] = "SALE_ITEM_STATE_PUBLICITY"] = 1;
        values[valuesById[2] = "SALE_ITEM_STATE_TRADING"] = 2;
        values[valuesById[3] = "SALE_ITEM_STATE_VERIFY"] = 3;
        values[valuesById[4] = "SALE_ITEM_STATE_CLAIM"] = 4;
        values[valuesById[5] = "SALE_ITEM_STATE_EXPIRE"] = 5;
        return values;
    })();

    /**
     * SaleRecordType enum.
     * @name common.SaleRecordType
     * @enum {number}
     * @property {number} SALE_RECORD_TYPE_SALE=0 SALE_RECORD_TYPE_SALE value
     * @property {number} SALE_RECORD_TYPE_BUY=1 SALE_RECORD_TYPE_BUY value
     */
    common.SaleRecordType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "SALE_RECORD_TYPE_SALE"] = 0;
        values[valuesById[1] = "SALE_RECORD_TYPE_BUY"] = 1;
        return values;
    })();

    common.StorageItemInfo = (function() {

        /**
         * Properties of a StorageItemInfo.
         * @memberof common
         * @interface IStorageItemInfo
         * @property {number|Long|null} [itemUuid] StorageItemInfo itemUuid
         * @property {number|null} [itemId] StorageItemInfo itemId
         * @property {string|null} [itemType] StorageItemInfo itemType
         * @property {string|null} [itemTag] StorageItemInfo itemTag
         * @property {number|null} [itemCount] StorageItemInfo itemCount
         */

        /**
         * Constructs a new StorageItemInfo.
         * @memberof common
         * @classdesc Represents a StorageItemInfo.
         * @implements IStorageItemInfo
         * @constructor
         * @param {common.IStorageItemInfo=} [properties] Properties to set
         */
        function StorageItemInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StorageItemInfo itemUuid.
         * @member {number|Long} itemUuid
         * @memberof common.StorageItemInfo
         * @instance
         */
        StorageItemInfo.prototype.itemUuid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * StorageItemInfo itemId.
         * @member {number} itemId
         * @memberof common.StorageItemInfo
         * @instance
         */
        StorageItemInfo.prototype.itemId = 0;

        /**
         * StorageItemInfo itemType.
         * @member {string} itemType
         * @memberof common.StorageItemInfo
         * @instance
         */
        StorageItemInfo.prototype.itemType = "";

        /**
         * StorageItemInfo itemTag.
         * @member {string} itemTag
         * @memberof common.StorageItemInfo
         * @instance
         */
        StorageItemInfo.prototype.itemTag = "";

        /**
         * StorageItemInfo itemCount.
         * @member {number} itemCount
         * @memberof common.StorageItemInfo
         * @instance
         */
        StorageItemInfo.prototype.itemCount = 0;

        /**
         * Creates a new StorageItemInfo instance using the specified properties.
         * @function create
         * @memberof common.StorageItemInfo
         * @static
         * @param {common.IStorageItemInfo=} [properties] Properties to set
         * @returns {common.StorageItemInfo} StorageItemInfo instance
         */
        StorageItemInfo.create = function create(properties) {
            return new StorageItemInfo(properties);
        };

        /**
         * Encodes the specified StorageItemInfo message. Does not implicitly {@link common.StorageItemInfo.verify|verify} messages.
         * @function encode
         * @memberof common.StorageItemInfo
         * @static
         * @param {common.IStorageItemInfo} message StorageItemInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StorageItemInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.itemUuid != null && Object.hasOwnProperty.call(message, "itemUuid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.itemUuid);
            if (message.itemId != null && Object.hasOwnProperty.call(message, "itemId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.itemId);
            if (message.itemType != null && Object.hasOwnProperty.call(message, "itemType"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.itemType);
            if (message.itemTag != null && Object.hasOwnProperty.call(message, "itemTag"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.itemTag);
            if (message.itemCount != null && Object.hasOwnProperty.call(message, "itemCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.itemCount);
            return writer;
        };

        /**
         * Encodes the specified StorageItemInfo message, length delimited. Does not implicitly {@link common.StorageItemInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.StorageItemInfo
         * @static
         * @param {common.IStorageItemInfo} message StorageItemInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StorageItemInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StorageItemInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.StorageItemInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.StorageItemInfo} StorageItemInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StorageItemInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.StorageItemInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.itemUuid = reader.int64();
                        break;
                    }
                case 2: {
                        message.itemId = reader.int32();
                        break;
                    }
                case 3: {
                        message.itemType = reader.string();
                        break;
                    }
                case 4: {
                        message.itemTag = reader.string();
                        break;
                    }
                case 5: {
                        message.itemCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StorageItemInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.StorageItemInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.StorageItemInfo} StorageItemInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StorageItemInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StorageItemInfo message.
         * @function verify
         * @memberof common.StorageItemInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StorageItemInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.itemUuid != null && message.hasOwnProperty("itemUuid"))
                if (!$util.isInteger(message.itemUuid) && !(message.itemUuid && $util.isInteger(message.itemUuid.low) && $util.isInteger(message.itemUuid.high)))
                    return "itemUuid: integer|Long expected";
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                if (!$util.isInteger(message.itemId))
                    return "itemId: integer expected";
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                if (!$util.isString(message.itemType))
                    return "itemType: string expected";
            if (message.itemTag != null && message.hasOwnProperty("itemTag"))
                if (!$util.isString(message.itemTag))
                    return "itemTag: string expected";
            if (message.itemCount != null && message.hasOwnProperty("itemCount"))
                if (!$util.isInteger(message.itemCount))
                    return "itemCount: integer expected";
            return null;
        };

        /**
         * Creates a StorageItemInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.StorageItemInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.StorageItemInfo} StorageItemInfo
         */
        StorageItemInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.StorageItemInfo)
                return object;
            var message = new $root.common.StorageItemInfo();
            if (object.itemUuid != null)
                if ($util.Long)
                    (message.itemUuid = $util.Long.fromValue(object.itemUuid)).unsigned = false;
                else if (typeof object.itemUuid === "string")
                    message.itemUuid = parseInt(object.itemUuid, 10);
                else if (typeof object.itemUuid === "number")
                    message.itemUuid = object.itemUuid;
                else if (typeof object.itemUuid === "object")
                    message.itemUuid = new $util.LongBits(object.itemUuid.low >>> 0, object.itemUuid.high >>> 0).toNumber();
            if (object.itemId != null)
                message.itemId = object.itemId | 0;
            if (object.itemType != null)
                message.itemType = String(object.itemType);
            if (object.itemTag != null)
                message.itemTag = String(object.itemTag);
            if (object.itemCount != null)
                message.itemCount = object.itemCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a StorageItemInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.StorageItemInfo
         * @static
         * @param {common.StorageItemInfo} message StorageItemInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StorageItemInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.itemUuid = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.itemUuid = options.longs === String ? "0" : 0;
                object.itemId = 0;
                object.itemType = "";
                object.itemTag = "";
                object.itemCount = 0;
            }
            if (message.itemUuid != null && message.hasOwnProperty("itemUuid"))
                if (typeof message.itemUuid === "number")
                    object.itemUuid = options.longs === String ? String(message.itemUuid) : message.itemUuid;
                else
                    object.itemUuid = options.longs === String ? $util.Long.prototype.toString.call(message.itemUuid) : options.longs === Number ? new $util.LongBits(message.itemUuid.low >>> 0, message.itemUuid.high >>> 0).toNumber() : message.itemUuid;
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                object.itemId = message.itemId;
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                object.itemType = message.itemType;
            if (message.itemTag != null && message.hasOwnProperty("itemTag"))
                object.itemTag = message.itemTag;
            if (message.itemCount != null && message.hasOwnProperty("itemCount"))
                object.itemCount = message.itemCount;
            return object;
        };

        /**
         * Converts this StorageItemInfo to JSON.
         * @function toJSON
         * @memberof common.StorageItemInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StorageItemInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StorageItemInfo
         * @function getTypeUrl
         * @memberof common.StorageItemInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StorageItemInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.StorageItemInfo";
        };

        return StorageItemInfo;
    })();

    common.SaleItemInfo = (function() {

        /**
         * Properties of a SaleItemInfo.
         * @memberof common
         * @interface ISaleItemInfo
         * @property {number|Long|null} [saleId] SaleItemInfo saleId
         * @property {number|null} [itemId] SaleItemInfo itemId
         * @property {string|null} [itemType] SaleItemInfo itemType
         * @property {string|null} [itemTag] SaleItemInfo itemTag
         * @property {number|null} [itemCount] SaleItemInfo itemCount
         * @property {number|null} [status] SaleItemInfo status
         * @property {number|Long|null} [createAt] SaleItemInfo createAt
         * @property {number|null} [price] SaleItemInfo price
         * @property {number|null} [markCount] SaleItemInfo markCount
         * @property {string|null} [sellerId] SaleItemInfo sellerId
         * @property {number|null} [publicityTime] SaleItemInfo publicityTime
         */

        /**
         * Constructs a new SaleItemInfo.
         * @memberof common
         * @classdesc Represents a SaleItemInfo.
         * @implements ISaleItemInfo
         * @constructor
         * @param {common.ISaleItemInfo=} [properties] Properties to set
         */
        function SaleItemInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SaleItemInfo saleId.
         * @member {number|Long} saleId
         * @memberof common.SaleItemInfo
         * @instance
         */
        SaleItemInfo.prototype.saleId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SaleItemInfo itemId.
         * @member {number} itemId
         * @memberof common.SaleItemInfo
         * @instance
         */
        SaleItemInfo.prototype.itemId = 0;

        /**
         * SaleItemInfo itemType.
         * @member {string} itemType
         * @memberof common.SaleItemInfo
         * @instance
         */
        SaleItemInfo.prototype.itemType = "";

        /**
         * SaleItemInfo itemTag.
         * @member {string} itemTag
         * @memberof common.SaleItemInfo
         * @instance
         */
        SaleItemInfo.prototype.itemTag = "";

        /**
         * SaleItemInfo itemCount.
         * @member {number} itemCount
         * @memberof common.SaleItemInfo
         * @instance
         */
        SaleItemInfo.prototype.itemCount = 0;

        /**
         * SaleItemInfo status.
         * @member {number} status
         * @memberof common.SaleItemInfo
         * @instance
         */
        SaleItemInfo.prototype.status = 0;

        /**
         * SaleItemInfo createAt.
         * @member {number|Long} createAt
         * @memberof common.SaleItemInfo
         * @instance
         */
        SaleItemInfo.prototype.createAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SaleItemInfo price.
         * @member {number} price
         * @memberof common.SaleItemInfo
         * @instance
         */
        SaleItemInfo.prototype.price = 0;

        /**
         * SaleItemInfo markCount.
         * @member {number} markCount
         * @memberof common.SaleItemInfo
         * @instance
         */
        SaleItemInfo.prototype.markCount = 0;

        /**
         * SaleItemInfo sellerId.
         * @member {string} sellerId
         * @memberof common.SaleItemInfo
         * @instance
         */
        SaleItemInfo.prototype.sellerId = "";

        /**
         * SaleItemInfo publicityTime.
         * @member {number} publicityTime
         * @memberof common.SaleItemInfo
         * @instance
         */
        SaleItemInfo.prototype.publicityTime = 0;

        /**
         * Creates a new SaleItemInfo instance using the specified properties.
         * @function create
         * @memberof common.SaleItemInfo
         * @static
         * @param {common.ISaleItemInfo=} [properties] Properties to set
         * @returns {common.SaleItemInfo} SaleItemInfo instance
         */
        SaleItemInfo.create = function create(properties) {
            return new SaleItemInfo(properties);
        };

        /**
         * Encodes the specified SaleItemInfo message. Does not implicitly {@link common.SaleItemInfo.verify|verify} messages.
         * @function encode
         * @memberof common.SaleItemInfo
         * @static
         * @param {common.ISaleItemInfo} message SaleItemInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SaleItemInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.saleId != null && Object.hasOwnProperty.call(message, "saleId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.saleId);
            if (message.itemId != null && Object.hasOwnProperty.call(message, "itemId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.itemId);
            if (message.itemType != null && Object.hasOwnProperty.call(message, "itemType"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.itemType);
            if (message.itemTag != null && Object.hasOwnProperty.call(message, "itemTag"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.itemTag);
            if (message.itemCount != null && Object.hasOwnProperty.call(message, "itemCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.itemCount);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.status);
            if (message.createAt != null && Object.hasOwnProperty.call(message, "createAt"))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.createAt);
            if (message.price != null && Object.hasOwnProperty.call(message, "price"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.price);
            if (message.markCount != null && Object.hasOwnProperty.call(message, "markCount"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.markCount);
            if (message.sellerId != null && Object.hasOwnProperty.call(message, "sellerId"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.sellerId);
            if (message.publicityTime != null && Object.hasOwnProperty.call(message, "publicityTime"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.publicityTime);
            return writer;
        };

        /**
         * Encodes the specified SaleItemInfo message, length delimited. Does not implicitly {@link common.SaleItemInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.SaleItemInfo
         * @static
         * @param {common.ISaleItemInfo} message SaleItemInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SaleItemInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SaleItemInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.SaleItemInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.SaleItemInfo} SaleItemInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SaleItemInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.SaleItemInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.saleId = reader.int64();
                        break;
                    }
                case 2: {
                        message.itemId = reader.int32();
                        break;
                    }
                case 3: {
                        message.itemType = reader.string();
                        break;
                    }
                case 4: {
                        message.itemTag = reader.string();
                        break;
                    }
                case 5: {
                        message.itemCount = reader.int32();
                        break;
                    }
                case 6: {
                        message.status = reader.int32();
                        break;
                    }
                case 7: {
                        message.createAt = reader.int64();
                        break;
                    }
                case 8: {
                        message.price = reader.int32();
                        break;
                    }
                case 9: {
                        message.markCount = reader.int32();
                        break;
                    }
                case 10: {
                        message.sellerId = reader.string();
                        break;
                    }
                case 11: {
                        message.publicityTime = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SaleItemInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.SaleItemInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.SaleItemInfo} SaleItemInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SaleItemInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SaleItemInfo message.
         * @function verify
         * @memberof common.SaleItemInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SaleItemInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.saleId != null && message.hasOwnProperty("saleId"))
                if (!$util.isInteger(message.saleId) && !(message.saleId && $util.isInteger(message.saleId.low) && $util.isInteger(message.saleId.high)))
                    return "saleId: integer|Long expected";
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                if (!$util.isInteger(message.itemId))
                    return "itemId: integer expected";
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                if (!$util.isString(message.itemType))
                    return "itemType: string expected";
            if (message.itemTag != null && message.hasOwnProperty("itemTag"))
                if (!$util.isString(message.itemTag))
                    return "itemTag: string expected";
            if (message.itemCount != null && message.hasOwnProperty("itemCount"))
                if (!$util.isInteger(message.itemCount))
                    return "itemCount: integer expected";
            if (message.status != null && message.hasOwnProperty("status"))
                if (!$util.isInteger(message.status))
                    return "status: integer expected";
            if (message.createAt != null && message.hasOwnProperty("createAt"))
                if (!$util.isInteger(message.createAt) && !(message.createAt && $util.isInteger(message.createAt.low) && $util.isInteger(message.createAt.high)))
                    return "createAt: integer|Long expected";
            if (message.price != null && message.hasOwnProperty("price"))
                if (!$util.isInteger(message.price))
                    return "price: integer expected";
            if (message.markCount != null && message.hasOwnProperty("markCount"))
                if (!$util.isInteger(message.markCount))
                    return "markCount: integer expected";
            if (message.sellerId != null && message.hasOwnProperty("sellerId"))
                if (!$util.isString(message.sellerId))
                    return "sellerId: string expected";
            if (message.publicityTime != null && message.hasOwnProperty("publicityTime"))
                if (!$util.isInteger(message.publicityTime))
                    return "publicityTime: integer expected";
            return null;
        };

        /**
         * Creates a SaleItemInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.SaleItemInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.SaleItemInfo} SaleItemInfo
         */
        SaleItemInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.SaleItemInfo)
                return object;
            var message = new $root.common.SaleItemInfo();
            if (object.saleId != null)
                if ($util.Long)
                    (message.saleId = $util.Long.fromValue(object.saleId)).unsigned = false;
                else if (typeof object.saleId === "string")
                    message.saleId = parseInt(object.saleId, 10);
                else if (typeof object.saleId === "number")
                    message.saleId = object.saleId;
                else if (typeof object.saleId === "object")
                    message.saleId = new $util.LongBits(object.saleId.low >>> 0, object.saleId.high >>> 0).toNumber();
            if (object.itemId != null)
                message.itemId = object.itemId | 0;
            if (object.itemType != null)
                message.itemType = String(object.itemType);
            if (object.itemTag != null)
                message.itemTag = String(object.itemTag);
            if (object.itemCount != null)
                message.itemCount = object.itemCount | 0;
            if (object.status != null)
                message.status = object.status | 0;
            if (object.createAt != null)
                if ($util.Long)
                    (message.createAt = $util.Long.fromValue(object.createAt)).unsigned = false;
                else if (typeof object.createAt === "string")
                    message.createAt = parseInt(object.createAt, 10);
                else if (typeof object.createAt === "number")
                    message.createAt = object.createAt;
                else if (typeof object.createAt === "object")
                    message.createAt = new $util.LongBits(object.createAt.low >>> 0, object.createAt.high >>> 0).toNumber();
            if (object.price != null)
                message.price = object.price | 0;
            if (object.markCount != null)
                message.markCount = object.markCount | 0;
            if (object.sellerId != null)
                message.sellerId = String(object.sellerId);
            if (object.publicityTime != null)
                message.publicityTime = object.publicityTime | 0;
            return message;
        };

        /**
         * Creates a plain object from a SaleItemInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.SaleItemInfo
         * @static
         * @param {common.SaleItemInfo} message SaleItemInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SaleItemInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.saleId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.saleId = options.longs === String ? "0" : 0;
                object.itemId = 0;
                object.itemType = "";
                object.itemTag = "";
                object.itemCount = 0;
                object.status = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.createAt = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.createAt = options.longs === String ? "0" : 0;
                object.price = 0;
                object.markCount = 0;
                object.sellerId = "";
                object.publicityTime = 0;
            }
            if (message.saleId != null && message.hasOwnProperty("saleId"))
                if (typeof message.saleId === "number")
                    object.saleId = options.longs === String ? String(message.saleId) : message.saleId;
                else
                    object.saleId = options.longs === String ? $util.Long.prototype.toString.call(message.saleId) : options.longs === Number ? new $util.LongBits(message.saleId.low >>> 0, message.saleId.high >>> 0).toNumber() : message.saleId;
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                object.itemId = message.itemId;
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                object.itemType = message.itemType;
            if (message.itemTag != null && message.hasOwnProperty("itemTag"))
                object.itemTag = message.itemTag;
            if (message.itemCount != null && message.hasOwnProperty("itemCount"))
                object.itemCount = message.itemCount;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = message.status;
            if (message.createAt != null && message.hasOwnProperty("createAt"))
                if (typeof message.createAt === "number")
                    object.createAt = options.longs === String ? String(message.createAt) : message.createAt;
                else
                    object.createAt = options.longs === String ? $util.Long.prototype.toString.call(message.createAt) : options.longs === Number ? new $util.LongBits(message.createAt.low >>> 0, message.createAt.high >>> 0).toNumber() : message.createAt;
            if (message.price != null && message.hasOwnProperty("price"))
                object.price = message.price;
            if (message.markCount != null && message.hasOwnProperty("markCount"))
                object.markCount = message.markCount;
            if (message.sellerId != null && message.hasOwnProperty("sellerId"))
                object.sellerId = message.sellerId;
            if (message.publicityTime != null && message.hasOwnProperty("publicityTime"))
                object.publicityTime = message.publicityTime;
            return object;
        };

        /**
         * Converts this SaleItemInfo to JSON.
         * @function toJSON
         * @memberof common.SaleItemInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SaleItemInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SaleItemInfo
         * @function getTypeUrl
         * @memberof common.SaleItemInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SaleItemInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.SaleItemInfo";
        };

        return SaleItemInfo;
    })();

    common.SaleRecordInfo = (function() {

        /**
         * Properties of a SaleRecordInfo.
         * @memberof common
         * @interface ISaleRecordInfo
         * @property {common.SaleRecordType|null} [recordType] SaleRecordInfo recordType
         * @property {number|null} [itemId] SaleRecordInfo itemId
         * @property {number|null} [itemCount] SaleRecordInfo itemCount
         * @property {number|null} [itemPrice] SaleRecordInfo itemPrice
         * @property {number|Long|null} [timestamp] SaleRecordInfo timestamp
         * @property {string|null} [itemType] SaleRecordInfo itemType
         */

        /**
         * Constructs a new SaleRecordInfo.
         * @memberof common
         * @classdesc Represents a SaleRecordInfo.
         * @implements ISaleRecordInfo
         * @constructor
         * @param {common.ISaleRecordInfo=} [properties] Properties to set
         */
        function SaleRecordInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SaleRecordInfo recordType.
         * @member {common.SaleRecordType} recordType
         * @memberof common.SaleRecordInfo
         * @instance
         */
        SaleRecordInfo.prototype.recordType = 0;

        /**
         * SaleRecordInfo itemId.
         * @member {number} itemId
         * @memberof common.SaleRecordInfo
         * @instance
         */
        SaleRecordInfo.prototype.itemId = 0;

        /**
         * SaleRecordInfo itemCount.
         * @member {number} itemCount
         * @memberof common.SaleRecordInfo
         * @instance
         */
        SaleRecordInfo.prototype.itemCount = 0;

        /**
         * SaleRecordInfo itemPrice.
         * @member {number} itemPrice
         * @memberof common.SaleRecordInfo
         * @instance
         */
        SaleRecordInfo.prototype.itemPrice = 0;

        /**
         * SaleRecordInfo timestamp.
         * @member {number|Long} timestamp
         * @memberof common.SaleRecordInfo
         * @instance
         */
        SaleRecordInfo.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SaleRecordInfo itemType.
         * @member {string} itemType
         * @memberof common.SaleRecordInfo
         * @instance
         */
        SaleRecordInfo.prototype.itemType = "";

        /**
         * Creates a new SaleRecordInfo instance using the specified properties.
         * @function create
         * @memberof common.SaleRecordInfo
         * @static
         * @param {common.ISaleRecordInfo=} [properties] Properties to set
         * @returns {common.SaleRecordInfo} SaleRecordInfo instance
         */
        SaleRecordInfo.create = function create(properties) {
            return new SaleRecordInfo(properties);
        };

        /**
         * Encodes the specified SaleRecordInfo message. Does not implicitly {@link common.SaleRecordInfo.verify|verify} messages.
         * @function encode
         * @memberof common.SaleRecordInfo
         * @static
         * @param {common.ISaleRecordInfo} message SaleRecordInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SaleRecordInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.recordType != null && Object.hasOwnProperty.call(message, "recordType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.recordType);
            if (message.itemId != null && Object.hasOwnProperty.call(message, "itemId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.itemId);
            if (message.itemCount != null && Object.hasOwnProperty.call(message, "itemCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.itemCount);
            if (message.itemPrice != null && Object.hasOwnProperty.call(message, "itemPrice"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.itemPrice);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.timestamp);
            if (message.itemType != null && Object.hasOwnProperty.call(message, "itemType"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.itemType);
            return writer;
        };

        /**
         * Encodes the specified SaleRecordInfo message, length delimited. Does not implicitly {@link common.SaleRecordInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.SaleRecordInfo
         * @static
         * @param {common.ISaleRecordInfo} message SaleRecordInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SaleRecordInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SaleRecordInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.SaleRecordInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.SaleRecordInfo} SaleRecordInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SaleRecordInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.SaleRecordInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.recordType = reader.int32();
                        break;
                    }
                case 2: {
                        message.itemId = reader.int32();
                        break;
                    }
                case 3: {
                        message.itemCount = reader.int32();
                        break;
                    }
                case 4: {
                        message.itemPrice = reader.int32();
                        break;
                    }
                case 5: {
                        message.timestamp = reader.int64();
                        break;
                    }
                case 6: {
                        message.itemType = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SaleRecordInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.SaleRecordInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.SaleRecordInfo} SaleRecordInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SaleRecordInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SaleRecordInfo message.
         * @function verify
         * @memberof common.SaleRecordInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SaleRecordInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.recordType != null && message.hasOwnProperty("recordType"))
                switch (message.recordType) {
                default:
                    return "recordType: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                if (!$util.isInteger(message.itemId))
                    return "itemId: integer expected";
            if (message.itemCount != null && message.hasOwnProperty("itemCount"))
                if (!$util.isInteger(message.itemCount))
                    return "itemCount: integer expected";
            if (message.itemPrice != null && message.hasOwnProperty("itemPrice"))
                if (!$util.isInteger(message.itemPrice))
                    return "itemPrice: integer expected";
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                if (!$util.isString(message.itemType))
                    return "itemType: string expected";
            return null;
        };

        /**
         * Creates a SaleRecordInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.SaleRecordInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.SaleRecordInfo} SaleRecordInfo
         */
        SaleRecordInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.SaleRecordInfo)
                return object;
            var message = new $root.common.SaleRecordInfo();
            switch (object.recordType) {
            default:
                if (typeof object.recordType === "number") {
                    message.recordType = object.recordType;
                    break;
                }
                break;
            case "SALE_RECORD_TYPE_SALE":
            case 0:
                message.recordType = 0;
                break;
            case "SALE_RECORD_TYPE_BUY":
            case 1:
                message.recordType = 1;
                break;
            }
            if (object.itemId != null)
                message.itemId = object.itemId | 0;
            if (object.itemCount != null)
                message.itemCount = object.itemCount | 0;
            if (object.itemPrice != null)
                message.itemPrice = object.itemPrice | 0;
            if (object.timestamp != null)
                if ($util.Long)
                    (message.timestamp = $util.Long.fromValue(object.timestamp)).unsigned = false;
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
            if (object.itemType != null)
                message.itemType = String(object.itemType);
            return message;
        };

        /**
         * Creates a plain object from a SaleRecordInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.SaleRecordInfo
         * @static
         * @param {common.SaleRecordInfo} message SaleRecordInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SaleRecordInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.recordType = options.enums === String ? "SALE_RECORD_TYPE_SALE" : 0;
                object.itemId = 0;
                object.itemCount = 0;
                object.itemPrice = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.timestamp = options.longs === String ? "0" : 0;
                object.itemType = "";
            }
            if (message.recordType != null && message.hasOwnProperty("recordType"))
                object.recordType = options.enums === String ? $root.common.SaleRecordType[message.recordType] === undefined ? message.recordType : $root.common.SaleRecordType[message.recordType] : message.recordType;
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                object.itemId = message.itemId;
            if (message.itemCount != null && message.hasOwnProperty("itemCount"))
                object.itemCount = message.itemCount;
            if (message.itemPrice != null && message.hasOwnProperty("itemPrice"))
                object.itemPrice = message.itemPrice;
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                object.itemType = message.itemType;
            return object;
        };

        /**
         * Converts this SaleRecordInfo to JSON.
         * @function toJSON
         * @memberof common.SaleRecordInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SaleRecordInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SaleRecordInfo
         * @function getTypeUrl
         * @memberof common.SaleRecordInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SaleRecordInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.SaleRecordInfo";
        };

        return SaleRecordInfo;
    })();

    common.WalletInfo = (function() {

        /**
         * Properties of a WalletInfo.
         * @memberof common
         * @interface IWalletInfo
         * @property {number|null} [balance] WalletInfo balance
         */

        /**
         * Constructs a new WalletInfo.
         * @memberof common
         * @classdesc Represents a WalletInfo.
         * @implements IWalletInfo
         * @constructor
         * @param {common.IWalletInfo=} [properties] Properties to set
         */
        function WalletInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WalletInfo balance.
         * @member {number} balance
         * @memberof common.WalletInfo
         * @instance
         */
        WalletInfo.prototype.balance = 0;

        /**
         * Creates a new WalletInfo instance using the specified properties.
         * @function create
         * @memberof common.WalletInfo
         * @static
         * @param {common.IWalletInfo=} [properties] Properties to set
         * @returns {common.WalletInfo} WalletInfo instance
         */
        WalletInfo.create = function create(properties) {
            return new WalletInfo(properties);
        };

        /**
         * Encodes the specified WalletInfo message. Does not implicitly {@link common.WalletInfo.verify|verify} messages.
         * @function encode
         * @memberof common.WalletInfo
         * @static
         * @param {common.IWalletInfo} message WalletInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WalletInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.balance != null && Object.hasOwnProperty.call(message, "balance"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.balance);
            return writer;
        };

        /**
         * Encodes the specified WalletInfo message, length delimited. Does not implicitly {@link common.WalletInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.WalletInfo
         * @static
         * @param {common.IWalletInfo} message WalletInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WalletInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WalletInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.WalletInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.WalletInfo} WalletInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WalletInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.WalletInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.balance = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WalletInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.WalletInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.WalletInfo} WalletInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WalletInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WalletInfo message.
         * @function verify
         * @memberof common.WalletInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WalletInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.balance != null && message.hasOwnProperty("balance"))
                if (!$util.isInteger(message.balance))
                    return "balance: integer expected";
            return null;
        };

        /**
         * Creates a WalletInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.WalletInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.WalletInfo} WalletInfo
         */
        WalletInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.WalletInfo)
                return object;
            var message = new $root.common.WalletInfo();
            if (object.balance != null)
                message.balance = object.balance | 0;
            return message;
        };

        /**
         * Creates a plain object from a WalletInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.WalletInfo
         * @static
         * @param {common.WalletInfo} message WalletInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WalletInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults)
                object.balance = 0;
            if (message.balance != null && message.hasOwnProperty("balance"))
                object.balance = message.balance;
            return object;
        };

        /**
         * Converts this WalletInfo to JSON.
         * @function toJSON
         * @memberof common.WalletInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WalletInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WalletInfo
         * @function getTypeUrl
         * @memberof common.WalletInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WalletInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.WalletInfo";
        };

        return WalletInfo;
    })();

    common.GoodsInfo = (function() {

        /**
         * Properties of a GoodsInfo.
         * @memberof common
         * @interface IGoodsInfo
         * @property {string|null} [itemType] GoodsInfo itemType
         * @property {number|null} [itemId] GoodsInfo itemId
         * @property {number|null} [itemCount] GoodsInfo itemCount
         * @property {number|null} [markCount] GoodsInfo markCount
         */

        /**
         * Constructs a new GoodsInfo.
         * @memberof common
         * @classdesc Represents a GoodsInfo.
         * @implements IGoodsInfo
         * @constructor
         * @param {common.IGoodsInfo=} [properties] Properties to set
         */
        function GoodsInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GoodsInfo itemType.
         * @member {string} itemType
         * @memberof common.GoodsInfo
         * @instance
         */
        GoodsInfo.prototype.itemType = "";

        /**
         * GoodsInfo itemId.
         * @member {number} itemId
         * @memberof common.GoodsInfo
         * @instance
         */
        GoodsInfo.prototype.itemId = 0;

        /**
         * GoodsInfo itemCount.
         * @member {number} itemCount
         * @memberof common.GoodsInfo
         * @instance
         */
        GoodsInfo.prototype.itemCount = 0;

        /**
         * GoodsInfo markCount.
         * @member {number} markCount
         * @memberof common.GoodsInfo
         * @instance
         */
        GoodsInfo.prototype.markCount = 0;

        /**
         * Creates a new GoodsInfo instance using the specified properties.
         * @function create
         * @memberof common.GoodsInfo
         * @static
         * @param {common.IGoodsInfo=} [properties] Properties to set
         * @returns {common.GoodsInfo} GoodsInfo instance
         */
        GoodsInfo.create = function create(properties) {
            return new GoodsInfo(properties);
        };

        /**
         * Encodes the specified GoodsInfo message. Does not implicitly {@link common.GoodsInfo.verify|verify} messages.
         * @function encode
         * @memberof common.GoodsInfo
         * @static
         * @param {common.IGoodsInfo} message GoodsInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GoodsInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.itemType != null && Object.hasOwnProperty.call(message, "itemType"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.itemType);
            if (message.itemId != null && Object.hasOwnProperty.call(message, "itemId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.itemId);
            if (message.itemCount != null && Object.hasOwnProperty.call(message, "itemCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.itemCount);
            if (message.markCount != null && Object.hasOwnProperty.call(message, "markCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.markCount);
            return writer;
        };

        /**
         * Encodes the specified GoodsInfo message, length delimited. Does not implicitly {@link common.GoodsInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof common.GoodsInfo
         * @static
         * @param {common.IGoodsInfo} message GoodsInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GoodsInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GoodsInfo message from the specified reader or buffer.
         * @function decode
         * @memberof common.GoodsInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {common.GoodsInfo} GoodsInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GoodsInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            var end = length === undefined ? reader.len : reader.pos + length, message = new $root.common.GoodsInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.itemType = reader.string();
                        break;
                    }
                case 2: {
                        message.itemId = reader.int32();
                        break;
                    }
                case 3: {
                        message.itemCount = reader.int32();
                        break;
                    }
                case 4: {
                        message.markCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GoodsInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof common.GoodsInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {common.GoodsInfo} GoodsInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GoodsInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GoodsInfo message.
         * @function verify
         * @memberof common.GoodsInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GoodsInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                if (!$util.isString(message.itemType))
                    return "itemType: string expected";
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                if (!$util.isInteger(message.itemId))
                    return "itemId: integer expected";
            if (message.itemCount != null && message.hasOwnProperty("itemCount"))
                if (!$util.isInteger(message.itemCount))
                    return "itemCount: integer expected";
            if (message.markCount != null && message.hasOwnProperty("markCount"))
                if (!$util.isInteger(message.markCount))
                    return "markCount: integer expected";
            return null;
        };

        /**
         * Creates a GoodsInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof common.GoodsInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {common.GoodsInfo} GoodsInfo
         */
        GoodsInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.common.GoodsInfo)
                return object;
            var message = new $root.common.GoodsInfo();
            if (object.itemType != null)
                message.itemType = String(object.itemType);
            if (object.itemId != null)
                message.itemId = object.itemId | 0;
            if (object.itemCount != null)
                message.itemCount = object.itemCount | 0;
            if (object.markCount != null)
                message.markCount = object.markCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a GoodsInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof common.GoodsInfo
         * @static
         * @param {common.GoodsInfo} message GoodsInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GoodsInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            var object = {};
            if (options.defaults) {
                object.itemType = "";
                object.itemId = 0;
                object.itemCount = 0;
                object.markCount = 0;
            }
            if (message.itemType != null && message.hasOwnProperty("itemType"))
                object.itemType = message.itemType;
            if (message.itemId != null && message.hasOwnProperty("itemId"))
                object.itemId = message.itemId;
            if (message.itemCount != null && message.hasOwnProperty("itemCount"))
                object.itemCount = message.itemCount;
            if (message.markCount != null && message.hasOwnProperty("markCount"))
                object.markCount = message.markCount;
            return object;
        };

        /**
         * Converts this GoodsInfo to JSON.
         * @function toJSON
         * @memberof common.GoodsInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GoodsInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GoodsInfo
         * @function getTypeUrl
         * @memberof common.GoodsInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GoodsInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/common.GoodsInfo";
        };

        return GoodsInfo;
    })();

    return common;
})();

module.exports = $root;
