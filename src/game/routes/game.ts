import { Session } from "../../framework/network";


export class GameRouter {
    static async CKickPlayers(session: Session, data: any, protoType: protobuf.Type) {

    }

    static async CKickAllPlayers(session: Session, data: any, protoType: protobuf.Type) {

    }
}